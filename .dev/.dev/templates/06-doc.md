# 06 — Док (конфа → `docs/`)

> Заполняет: агент-доккер в конце задачи. Пишется так, чтобы читал человек, не знающий контекста.
> Исходник остаётся здесь (след задачи); постоянные правила/сценарии выносятся в `docs/`.

- **Задача:** <slug + одна строка>
- **Тип:** bug / feature / доработка / рефакторинг
- **Дата / кто вёл:** <...>
- **GitHub Issue / PR:** <ссылки>
- **Куда вынесено в `docs/`:** <файл(ы) `docs/…`, если родилось постоянное правило; иначе «нет»>

## Суть
- **Что сделали и зачем:** <для человека, без жаргона>
- **Для бага — суть бага:** <что было не так, как проявлялось, первопричина>

## Бизнесово
- **Что это меняет для пользователя/процесса:** <...>

## Технически
- **Что поменяли:** <модули, подход — со ссылками `file:line` и на коммиты>
- **Почему так (какие решения):** <ссылка на `02-arch-decision.md`; отвергнутые варианты>

## След
- Ссылки: `tasks/<slug>/` (триаж, анализ, арх-решение, контракт, тесты, ревью), коммиты, PR.
