# 00 — Триаж

> Заполняет: первый агент, взявшийся за задачу. Первое действие по любой задаче.

- **Задача (одной строкой):** <что просят>
- **Тип:** `bug` | `feature` | `доработка` | `рефакторинг`
- **Размер:** `S` (часы) | `M` | `L`
- **GitHub Issue:** <#номер, метка `ready`; если без трекера — «нет»>
- **Стоит ли делать сейчас / зачем:** <ценность, срочность; если сомнительно — вынести человеку>

## Маршрут (из типа+размера, см. `.dev/PROCESS.md` §1)
- [ ] 0 триаж (этот файл)
- [ ] 1 анализ + арх-коучинг
- [ ] 🔒 ГЕЙТ 1: арх + стек  (bug — только если меняется архитектура)
- [ ] 2 контракт (пропуск для S)
- [ ] 3 тест-дизайн (+ маппинг на TC-ID)
- [ ] 4 код (вертикальный срез)
- [ ] 5 независимое ревью + прогон гейтов
- [ ] 🔒 ГЕЙТ 2 приёмка (опц.)
- [ ] 6 док → `docs/`

## Что затрагивает (первая прикидка)
<модули/сервисы/данные/слои, которые вероятно заденем: backend `api`/`services`/`models`/`db`/`tasks`, фронт `frontend/src`, мобилка `mobile/`, БД-миграция…>
