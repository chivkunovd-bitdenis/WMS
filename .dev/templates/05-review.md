# 05 — Независимое ревью + прогон

> Заполняет: агент-ревьюер, который НЕ писал этот код. Установка: искать, где плохо и где контракт
> не выполнен, а не подтверждать, что всё хорошо.

## Прогон гейтов
- **Тесты (стадия 3):** ✅ зелёные / ❌ красные — <детали, что упало>
- **Backend-гейт:** `cd backend && ruff check . && mypy . && pytest` — <✅/❌>
- **Frontend-гейт (если трогали):** `cd frontend && npm run build && npm run test:e2e` — <✅/❌/н.п.>

## Ревью по стандарту кода (`.dev/PROCESS.md` §4)
- **Читаемость / стиль как вокруг:** <ок / замечания>
- **Разделение слоёв (`api`/`services`/`models`/`db`/`tasks`):** <ок / где смешано>
- **MUI дизайн-система (новые экраны ФФ):** <ок / замечания / н.п.>
- **Мусор (мёртвый код, отладка, лишний рефактор):** <ок / что убрать>
- **Соответствие контракту (`03`):** <каждый критерий закрыт? где нет>
- **Блок `### Test coverage` в PR:** <есть, осмысленный / нет>

## Находки
| Важность | Файл:строка | Проблема | Что сделать |
|----------|-------------|----------|-------------|
| high/med/low | <...> | <...> | <...> |

## Вердикт
- **Статус:** ✅ принять / 🔁 вернуть на код / ⏳ ждёт правок
- **🔒 ГЕЙТ 2 (если включён):** приёмка человеком — <статус, кто, когда>
