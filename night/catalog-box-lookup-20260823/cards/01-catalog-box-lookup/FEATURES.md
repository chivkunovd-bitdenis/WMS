ФИЧ: 1

## Фичи

### 1. Защитить потоковый ввод от позднего ответа повторного скана

Что меняется словами оператора: если оператор дважды отправил один внутренний ШК, получил результат второго запроса и уже начал вводить следующий код, поздний ответ первого запроса не выделяет и не заменяет начатый следующий ввод. Найденный короб по-прежнему остаётся один, а сканер остаётся готов к следующему считыванию.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/tests-e2e/catalog-box-lookup.spec.ts`

Зависимости: нет; это ремонт только незакрытой находки повторного review для `S-16-TC-014`.

Как проверить: в сценарии `S-16-TC-014` задержать первый из двух одинаковых lookup-запросов, успешно завершить второй, затем через реальный ввод в поле сканера начать следующий штрихкод до освобождения первого ответа. После позднего ответа первого запроса проверить, что в поле остаётся уже начатый новый код, его выделение не восстановлено на старом коде и последующий ввод дописывается к новому коду, а не заменяет его; одновременно в разделе остаётся ровно один адресно найденный короб и ошибка поиска не показана.

## Порядок

1. Выполнить фичу 1 как единственный оставшийся атом. Параллельных частей нет: она меняет один пользовательский сценарий в одном frontend-файле.

## Что осталось за бортом

- Нет: повторный review признал предыдущие находки № 1, № 2, № 4 и № 5 закрытыми; они не возвращаются в этот ремонт.
