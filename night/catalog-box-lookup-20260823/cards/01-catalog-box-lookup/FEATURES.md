ФИЧ: 1

## Фичи

### 1. Починить последовательный ввод в регрессии повторного скана

Операторский сценарий `S-16-TC-014` должен передавать фактическую ситуацию: после успешного повторного скана поле уже содержит старый внутренний ШК, поэтому перед посимвольным вводом следующего ШК тест явно выделяет это значение. Затем поздний ответ первого запроса не имеет права перехватить это выделение или заменить уже введённый следующий код. Никакое поведение экрана, API и данных не меняется — исправляется только противоречивое исходное состояние теста.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/tests-e2e/catalog-box-lookup.spec.ts`

Зависимости: нет. Это ремонт уже принятой карточки; ранее реализованные backend- и frontend-части не переоткрываются.

Как проверить: запустить целевой Playwright-тест `catalog deduplicates repeated scans while the first lookup is pending`. Он должен явно заменить прежний ШК посимвольным `INB-NEXT-SCAN`, освободить поздний ответ первого одинакового скана и подтвердить, что в фокусном поле остаётся новый код с кареткой в конце, найдена только одна строка короба и ошибка поиска не показана.

## Порядок

1. Выполнить фичу 1. Других незакрытых находок из повторного review нет, поэтому параллельных атомов нет.

## Что осталось за бортом

- Уже принятые API, read-модель, экран каталога и остальные E2E-сценарии не входят в этот ремонт: повторный review подтвердил их вне границ находки.
- Локальный запуск Playwright может быть недоступен без установленных frontend-зависимостей; это ограничение проверки, а не отдельная продуктовая фича.
