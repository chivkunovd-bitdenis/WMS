ФИЧ: 8

## Фичи

### 1. Зарегистрировать владельца локального блока коробов на S-16

Что меняется для оператора: поведение каталога не меняется; его локальный блок «Короба и грузоместа» получает явного владельца в реестре экранов, поэтому дальнейшие изолированные правки остаются в границе S-16.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/screens.registry.json`

Зависимости: нет.

Как проверить: JSON реестра разбирается, а у записи маршрута `/app/ff/products` с идентификатором `S-16` есть путь `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfCatalogInboundPackages.tsx`; других экранов и зон реестра правка не меняет.

### 2. Передавать состояние полностью разложенного короба независимо от статуса приёмки

Что меняется для оператора: при адресном скане короба, в котором весь товар уже разложен, каталог получает явный признак этого состояния даже когда другая часть той же приёмки ещё находится в сортировке. Интерфейс следующей фичи сможет честно показать «Товар из короба уже разложен», а не пустой короб.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/backend/app/services/inbound_package_catalog_service.py`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/backend/app/api/inbound_package_catalog.py`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/backend/tests/test_inbound_package_catalog.py`

Зависимости: нет.

Как проверить: backend-тест создаёт приёмку в `sorting` с двумя коробами, у одного `quantity == posted_qty`, выполняет `GET /operations/inbound-packages/lookup` по его `INB-*` и проверяет явное состояние полной раскладки при `remaining_qty = 0`; обычный список по-прежнему не включает этот короб.

### 3. Ограничить чтение обычного списка актуальными объектами на уровне запросов

Что меняется для оператора: первое раскрытие раздела не читает строки и грузоместа всей исторической приёмки тенанта, а загружает только объекты, которые могут появиться в обычном списке.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/backend/app/services/inbound_package_catalog_service.py`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/backend/tests/test_inbound_package_catalog.py`

Зависимости: 2 — read-модель и тестовые примеры полной раскладки должны уже иметь точное состояние.

Как проверить: тест с актуальным коробом и историческими завершёнными/полностью разложенными объектами фиксирует тот же состав и порядок HTTP-ответа, а проверка SQL-запросов подтверждает, что строки исключённых коробов и завершённые грузоместа не материализуются для обычного `GET /operations/inbound-packages`.

### 4. Сохранить адресный результат и показать полную раскладку в блоке каталога

Что меняется для оператора: результат точного скана остаётся видимым во время загрузки или повторной загрузки обычного списка и после неё, даже если он намеренно отсутствует в обычном списке. Для короба с явным признаком полной раскладки показывается спокойное состояние «Товар из короба уже разложен» вне зависимости от статуса всей приёмки.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfCatalogInboundPackages.tsx`

Зависимости: 2.

Как проверить: на S-16 отсканировать полностью разложенный `INB-*` в приёмке `sorting`, затем вызвать первоначальную загрузку списка и «Повторить» после её ошибки; найденная строка остаётся раскрытой и показывает «Товар из короба уже разложен», а не пустой состав и не исчезает.

### 5. Не скрывать товар чужого селлера при адресном скане

Что меняется для оператора: администратор, выбравший селлера A в фильтре товаров, всё равно видит строки состава отсканированного короба селлера B; фильтр влияет на таблицу товаров, но не искажает физический короб.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfProductsCatalogScreen.tsx`

Зависимости: нет.

Как проверить: под администратором выбрать селлера A, отсканировать короб селлера B с положительным остатком и убедиться, что раскрытая строка содержит SKU, название, ШК и остаток товара B, а таблица товаров продолжает показывать только селлера A.

### 6. Игнорировать устаревшие ответы сканирования и не перехватывать следующий ввод

Что меняется для оператора: поздняя ошибка старого скана не заменяет успешный результат нового скана, а завершение старого запроса не выделяет текст, который оператор уже начал вводить для следующего ШК.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfCatalogInboundPackages.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfProductsCatalogScreen.tsx`

Зависимости: 4 — адресный результат имеет самостоятельный жизненный цикл.

Как проверить: задержать неуспешный lookup первого `INB-*`, до его ответа успешно отсканировать второй код, затем начать третий; остаётся открыт второй объект, ошибка не появляется, а текст третьего кода не выделен и не заменяется поздним завершением первого запроса.

### 7. Не дублировать знак номера в заголовке приёмки

Что меняется для оператора: в заголовке объекта номер приёмки отображается один раз — «Приёмка №000001», если `display_number` уже содержит `№`.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/src/screens/v2/FfCatalogInboundPackages.tsx`

Зависимости: нет.

Как проверить: открыть объект с `request_display_number = "№000001"` и убедиться, что заголовок содержит ровно «Приёмка №000001», без «№ №»; fallback на идентификатор приёмки сохраняет единственный префикс.

### 8. Укрепить e2e-доказательство остатка и поточного сканирования

Что меняется для оператора: поведения не добавляются; пользовательские сценарии защищают уже исправленные состояния от возврата ошибок при следующей правке.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/frontend/tests-e2e/catalog-box-lookup.spec.ts`

Зависимости: 4, 5, 6, 7.

Как проверить: Playwright-сценарии с TC-ID проверяют значение `QtyCell` точным селектором после частичной и полной раскладки, сохраняют адресный результат при загрузке/повторе, воспроизводят задержанный отказ первого lookup после успешного второго и проверяют скан короба селлера B при активном фильтре A. Тесты не используют произвольный поиск цифры по всему accordion.

## Порядок

1. Сначала сделать фичу 1: она фиксирует файловую границу S-16 и не зависит от поведения.
2. Затем последовательно сделать фичи 2 и 3: обе меняют read-модель, а оптимизацию списка нужно накладывать на уже зафиксированный контракт адресного состояния.
3. После фичи 2 сделать фичу 4, затем фичу 6: сначала адресная строка становится независимой от списка, затем её жизненный цикл защищается от гонки запросов.
4. Фичи 5 и 7 независимы от фич 1–4 и друг от друга; их можно делать параллельно, если исполнители не трогают один и тот же файл одновременно. В одной рабочей копии их выполнять последовательно, чтобы не было конфликта в `FfProductsCatalogScreen.tsx` и `FfCatalogInboundPackages.tsx`.
5. Завершить фичей 8 только после всех пользовательских исправлений: она даёт точные регрессионные доказательства для остатков, адресной строки, гонок, фильтра селлера и номера приёмки.

## Что осталось за бортом

- Новых продуктовых правил в контракте не не хватает: этот план покрывает только восемь ещё не закрытых находок из `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/night/catalog-box-lookup-20260823/cards/01-catalog-box-lookup/REVIEW.md` и не возвращает принятую карточку к полной реализации.
- Изменение `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/catalog-box-lookup-20260823/lane-1-01-catalog-box-lookup/night/catalog-box-lookup-20260823/cards/01-catalog-box-lookup/MOCKUP.html`, ui-kit, схемы базы, миграций, маршрутов и экранов S-17/S-19/S-20 не требуется для этих находок и остаётся вне scope.
- В ревью зафиксировано ограничение окружения: отсутствует локальный `tsc`, поэтому фактический frontend build и Playwright должны выполняться в окружении с установленными зависимостями; это не продуктовая фича и не причина расширять план.
