ФИЧ: 3

## Фичи

### 1. Объяснить недоступность будущих месяцев

Оператор рядом с выбором периода видит короткое объяснение «Будущие месяцы недоступны: расчёт ещё не начался», поэтому ограничение календаря не выглядит ошибкой и соответствует S-11.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts`

Зависимости: нет.

Проверка: открыть `/app/ff/inventory` как сотрудник ФФ, увидеть у поля «Месяц» объяснение недоступности будущих месяцев; попытка выбрать месяц позже текущего по-прежнему недоступна. Playwright-сценарий должен проверить видимый текст и ограничение `max` у поля.

### 2. Зафиксировать ширины колонок истории габаритов

В диалоге «История габаритов» все пять колонок получают явные ширины, чтобы длинные источник или имя автора не сдвигали дату, объём и применённую версию.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts`

Зависимости: нет.

Проверка: открыть SKU с историей габаритов и диалог «История габаритов»; видны колонки «Дата», «Источник», «Габариты / объём», «Автор», «Применено» в этом порядке, а длинные тестовые значения не меняют расположение даты и статуса. Playwright-сценарий должен открыть диалог через UI и проверить все заголовки и видимый статус «Действует».

### 3. Сделать печатную SKU-детализацию проверяемой по ставке

В A4-предпросмотре расчёта хранения все колонки получают явные ширины, числовые значения остаются справа, а между «Литро-дни» и «Сумма, ₽» появляется колонка «Ставка, ₽/л·день» со значением `rate_snapshot`. Это один тесно связанный слой одной печатной таблицы.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts`

Зависимости: нет.

Проверка: открыть из зафиксированной строки «Расчёт хранения за <месяц>» и убедиться, что в печатной таблице видны все заголовки, включая «Ставка, ₽/л·день», а для SKU показано значение снимка ставки; длинный артикул не сдвигает числовые колонки. Playwright-сценарий должен открыть preview через кнопку печати и проверить заголовок новой колонки, значение ставки и видимый итог.

## Порядок

Фичи 1, 2 и 3 независимы: все меняют только frontend-экран S-11 и не требуют новой модели, API, ui-kit или backend-порядка из `ARCH-CROSS.md`. Их можно выполнять параллельно, но при последовательной работе удобен порядок 1 → 2 → 3, потому что он идёт от фильтра к диалогу и затем к печатной детализации.

## Что осталось за бортом

- Принятая ранее реализация экрана хранения, его API, модели, расчёт и финансовый контракт 08/09 не возвращаются в работу: `DESIGN-REVIEW.md` не отметил в них незакрытых находок.
- Расширение ui-kit `PrintAction` уже принято проверкой дизайна; отдельная ui-kit-фича не нужна.
- Замечания `ui_guard.py` по трём чужим экранам не относятся к S-11 и не входят в эту карточку.
- Секреты, ключи, токены, `.env`, персональные данные и кабинеты учётных данных не открывались и не использовались.
