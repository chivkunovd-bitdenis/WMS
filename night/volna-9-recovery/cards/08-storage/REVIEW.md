# Ревью · 08-storage

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 13

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/screens.registry.json:802` — реестр по-прежнему объявляет экран S-11 компонентом `FfPlaceholderPage` и разрешает ему только `src/screens/ff/FfPlaceholderPage.tsx`, тогда как карточка меняет `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/App.tsx:3121` и добавляет `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx`; при проверке границ S-11 оба файла оказываются вне зарегистрированного экрана — цена: карточка нарушает обязательную границу экрана и не может пройти предсливочный guard как корректная правка S-11.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:9` — весь рабочий экран построен на трёх захардкоженных селлерах и локальных `useState`, а формирование, тариф, обмер и фиксация не вызывают API; после перезагрузки введённый обмер и «зафиксированный» документ исчезают, следующий календарный месяц всё равно открывается как `2026-07`, а каждый tenant видит вымышленные суммы «Красотки», «Норда» и «Вектора» — цена: экран нельзя использовать для начисления денег, и демонстрационные данные выдаются оператору за реальные.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:19` — фиксация безусловно импортирует отсутствующие в этой ветке `app.models.billing.BillingTariffVersion` и `BillingLedgerEntry`; при любом реальном запросе администратора `_billing_models()` бросает `billing_models_unavailable`, а API маскирует это как 404 в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:109` — цена: центральный сценарий фиксации и публикации начисления не работает вообще, поскольку обязательная зависимость 09-A не внедрена до 08-B.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_measurement_service.py:65` — движения сначала отбираются через текущие неудалённые `StorageLocation`, склад затем допускает запрещённый fallback через текущую ячейку, `Warehouse.is_operational` не проверяется, а `movement_start_id` и `movement_end_id` при создании строки не заполняются; если ячейку удалили или перенесли после движения, прошлая операция исчезнет либо сменит склад, а служебный склад с активной ячейкой попадёт в тарификацию — цена: сумма хранения по складу недоказуема и нарушает приоритетный контракт 07-A/`ARCH-CROSS.md`.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_measurement_service.py:102` — расчёт выбирает единственное событие с `applied=true` на момент запуска и применяет его объём ко всем дням месяца в строках 152–178; если товар обмерили 20 июля, новый объём будет начислен задним числом за 1–19 июля, хотя архитектура говорит, что ручная версия действует с момента сохранения — цена: каждое изменение габаритов искажает уже прошедшую часть открытого периода.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:63` — сервис выбирает одну последнюю ставку с `effective_from <= period_end` на весь месячный объём, не отдаёт приоритет индивидуальной ставке при одинаковой дате и не ограничивает начисление датой первой ставки; если тариф включили 20 июля либо одновременно задали общий и индивидуальный тариф, весь июль будет начислен по одной недетерминированной ставке — цена: прямое завышение счёта или потеря выручки.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:127` — повторная печать выбирает все ledger-строки хранения данного селлера без фильтра по statement, складу или периоду; после фиксации июля и августа печать любого из этих документов вернёт измерения обоих месяцев и всех складов — цена: зафиксированный A4 меняет состав со временем и показывает неверное основание начисления.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:50` — печатный DTO содержит только `product_id` и литро-дни, всегда возвращает `total_amount="0"` и не содержит обязательных селлера, склада, SKU/артикула, объёма и источника, ставки-снимка и суммы строки; даже после появления 09-A портал не сможет воспроизвести утверждённый A4 и покажет нулевой итог при ненулевом ledger — цена: селлер получает документ, который не совпадает с начислением и не позволяет проверить сумму.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/alembic/versions/20260822_0095_product_dimension_events.py:17` — миграция добавляет источник и журнал, но не переносит в них существующие габариты; после обновления у ранее вручную заполненного товара `dimensions_source=NULL`, поэтому следующий WB-импорт в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/wildberries_product_import_service.py:342` оставит старые три размера, но заменит `volume_liters` объёмом WB и объявит источник `wb` — цена: быстрый снимок становится внутренне противоречивым, а хранение начисляется по объёму, не соответствующему видимым размерам.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/catalog_service.py:839` — «Вернуть данные WB» вызывает `_record_dimension_event(..., force_new=True)` с тем же fingerprint, хотя миграция запрещает дубль `(product_id, fingerprint)`; если администратор возвращает уже сохранённое последнее наблюдение WB после ручного обмера, commit получает `IntegrityError` и API отвечает 500 — цена: утверждённое действие возврата к WB неработоспособно в штатном сценарии.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/products.py:770` — ручной PATCH не передаёт `author_user_id=user.id`, поэтому событие и текущий снимок сохраняются без автора; при споре с селлером история покажет ручной обмер, но не докажет, кто его внёс — цена: журнал не выполняет свою главную аудиторскую функцию.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/products.py:748` — тот же PATCH разрешён роли `FULFILLMENT_SELLER`, хотя контракт даёт внесение обмера сотруднику с `inventory`, а онлайн-экран селлера исключён; селлер может уменьшить собственные габариты перед пересчётом, и это событие станет действующим источником для тарификации — цена: плательщик может сам уменьшить базу своего начисления за хранение.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/tests/test_storage_measurement_service.py:6` — тесты расчёта проверяют только две функции дат, тест фиксации в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/tests/test_storage_statement_service.py:6` закрепляет отсутствие billing-моделей как ожидаемый успех, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:3` покрывает только два локальных действия без входа и серверного результата; целевой прогон даёт `10 passed`, оставаясь зелёным при поломках 2–12 — цена: CI не защищает формулу, роли, идемпотентность, неизменяемость документа и реальный путь администратора из 20 назначенных кейсов.

## Проверено и нормально

- Полный продуктовый diff сверён с `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/night/volna-9-recovery/cards/08-storage/CONTRACT.md`, `FEATURES.md`, `ARCH.md`, картой волны и приоритетным `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/night/volna-9-recovery/ARCH-CROSS.md`; собственных таблиц тарифа или второго финансового ledger карточка не добавила.
- Стадийные артефакты в `night/`, `tests/cases/`, `docs/evidence/` и `docs/blockers/` не считались выходом за границы; `docs/blockers/S-11.md` уже фиксирует три новые экранные блокировки и расхождения UI/server шестью обязательными полями.
- Tenant-фильтр и проверка принадлежности селлеру присутствуют в чтении истории габаритов и печати statement; повторный rebuild больше не дописывает строки в уже закрытый scope, а нулевые draft-statements создаются для найденных seller/warehouse scope.
- Подпись `PrintAction` исправлена на «Печать накладной» без изменения публичного интерфейса. Целевой backend-прогон `pytest -q tests/test_products_api.py tests/test_storage_measurement_service.py tests/test_storage_models.py tests/test_storage_statement_service.py tests/test_wb_import_dimensions.py` завершился `10 passed`; именно недостаточность этих проверок описана в находке 13.
