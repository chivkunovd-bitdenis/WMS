# Ревью · 08-storage · проверка ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:192` — экран теперь отправляет сохранение общей ставки в `POST /operations/storage/tariffs`, но такого маршрута в серверном приложении нет: после списка в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:307` зарегистрированы только rebuild, fix и print. При первом входе администратора без тарифа действие «Задать тариф» неизбежно получает 404 и оставляет диалог с ошибкой; тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:35` скрывает разрыв, подменяя несуществующий endpoint успешным ответом. Цена: `S-11-TC-002` и весь первый коммерческий запуск хранения по-прежнему не работают на реальном API.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:192` — общая ставка склада и индивидуальная ставка селлера сохраняются двумя последовательными запросами без общей атомарной операции. Если первый запрос прошёл, а второй в строке 194 получил временный отказ или конфликт, экран сообщает общий неуспех, хотя общая ставка уже записана; повтор «Сохранить» снова отправит ту же общую версию и упрётся в уникальность даты, не дойдя до исключения селлера. Цена: администратор получает частично применённый тариф и штатно не может завершить редкий сценарий пары «селлер + склад»; назначенный `S-11-TC-002` проверяет только один запрос и останется зелёным.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:42` — дата начала тарифа по умолчанию вычисляется через UTC (`toISOString()`), хотя контракт задаёт календарные границы по МСК. При открытии диалога между 00:00 и 02:59 по Москве поле получает вчерашнюю дату; администратор, приняв видимое значение по умолчанию, создаст ставку на сутки раньше намерения «действует вперёд». Цена: неверная дата начала финансового тарифа и потенциальное лишнее начисление за предыдущие московские сутки; тест зафиксировал константу и часовой пояс этого случая не проверяет.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист; после него проверен ремонтный diff `fbcc88a6..3df8a2a5`. Пункты 2–7 закрыты: тариф привязан к складу, A4 использует количество из ledger, лишние модели удалены, цепочка Alembic исправлена, файлы карточки проходят `ruff`, а непустой сценарий сотрудника снова проверяет отсутствие фиксации.
- Пункт 1 закрыт визуально, но не сквозным поведением: полноценная форма появилась, однако серверного маршрута записи нет, что отражено в находке 1. Новые блокировки формы проверены по обоим слоям; серверного слоя сохранения тарифа пока нет вообще.
- Полный набор назначенных backend-тестов карточки прошёл: `49 passed`. Frontend production build прошёл; целевой `Actions.test.ts` прошёл: `3 passed`. `git diff --check` на ремонтном diff чист.
- Полный `mypy app` показывает пять ошибок в четырёх соседних FBS/reporting-файлах вне переданного списка реализации карточки; по правилу границ они не записаны как находки `08-storage`. Секреты, ключи, токены, `.env`, кабинеты учётных данных и production не открывались и не использовались; продуктовый код не изменялся.
