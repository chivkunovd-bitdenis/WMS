# Ревью · 08-storage

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 10

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:69` — экран всегда загружает сводку через `GET /operations/storage/statements`, но в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:126`—`210` существуют только rebuild, fix и print; при открытии реального `/app/ff/inventory` сервер вернёт 404, экран останется в состоянии «Не удалось загрузить расчёты хранения», а таблица, тариф, раскрытие селлера и фиксация будут недоступны — цена: весь утверждённый операторский сценарий S-11 не работает вне мокированного Playwright.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:21` — фиксация динамически импортирует `app.models.billing.BillingTariffVersion` и `BillingLedgerEntry`, которых в этой ветке нет, поэтому любой реальный запрос fix завершается `billing_models_unavailable`; одновременно диалог тарифа в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:185` содержит только сообщение об отсутствующем API и не умеет сохранить ставку — цена: администратор не может ни задать цену, ни зафиксировать даже нулевой месяц, хотя оба действия входят в контракт карточки.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/alembic/versions/20260822_0097_storage_movement_scope.py:14` — миграция добавляет nullable `InventoryMovement.warehouse_id`, но не делает backfill, не добавляет обязательный `seller_id` из контракта 07-A и не обновляет writer: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/inventory_service.py:561` продолжает создавать движение без `warehouse_id`; rebuild читает только строки с заполненным складом в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_measurement_service.py:151` — при обычной приёмке и отгрузке все старые и новые движения выпадают, а система создаёт нулевые statements вместо фактического хранения. Дополнительно `is_operational=true` назначается всем legacy-складам без исключения служебных `FBS WB *`, поэтому эти склады попадут в набор расчётных scopes — цена: потеря выручки по реальным остаткам и одновременно лишние документы по техническим складам.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_measurement_service.py:103` — временная шкала берёт все `ProductDimensionEvent` по `observed_at`, не различая применённое значение и неприменённое WB-наблюдение; после ручного обмера импорт WB корректно пишет `applied=false` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/wildberries_product_import_service.py:334`, но расчёт со дня этого импорта всё равно выбирает WB-объём — цена: импорт не меняет карточку товара на экране, однако молча меняет сумму хранения, прямо нарушая правило «ручной обмер не затирается».

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/catalog_service.py:795` — fingerprint дедуплицирует не только повторный WB-импорт, но и новый осознанный ручной обмер с тем же значением; если оператор измерил 10×10×10, затем вернул WB и через неделю снова измерил 10×10×10, код реактивирует старое событие и в строках 817—822 переписывает его автора вместо создания нового события с новым временем — цена: неизменяемый журнал теряет факт повторного обмера и при споре показывает ложные дату/автора действующей версии.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:81` — выбирается только тариф, действовавший на первый день месяца, и одна ставка умножается на все агрегированные литро-дни; при первом тарифе с 10 июля фиксация ответит `tariff_not_found`, а при смене ставки 20 июля весь месяц будет начислен по старой цене — цена: потеря либо завышение денег и нарушение контрактного правила «ставка действует только вперёд с указанной даты».

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_measurement_service.py:135` — для текущего месяца граница расчёта остаётся последним числом месяца, поэтому текущий положительный остаток продлевается в ещё не наступившие дни; сервер фиксации в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:43` не запрещает закрыть такой месяц — например, запуск 22 августа начислит хранение до 1 сентября и позволит опубликовать его в ledger — цена: селлеру выставляются будущие, ещё не оказанные литро-дни.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:33` — UI ожидает у истории поля `created_at`, `author_name`, `is_current` и источники `wildberries`/`container`, тогда как API в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/products.py:240` отдаёт `observed_at`, `author_user_id`, `applied` и источники `wb`/`container_override`; на реальном ответе дата будет Invalid Date, автор превратится в «Импорт», действующая версия покажется «История», а WB и тара — «Неизвестно» — цена: оператор не может использовать журнал как доказательство обмера или безопасно понять, к какой версии возвращается.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/App.tsx:2759` — маршрут S-11 разрешён при праве `cells OR inventory`, хотя backend требует `inventory`; сотрудник только с доступом к ячейкам увидит пункт и экран, но каждый запрос получит 403 и штатная работа с ячейками сменится необъяснимой ошибкой хранения — цена: новая экранная блокировка расходится с серверным правилом и ломает доступ роли вне контракта.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:8` — Playwright подменяет отсутствующий list endpoint, несовместимый DTO истории и успешную фиксацию собственными ответами; сценарий формирования в строках 29—33 не проверяет тело запроса, завершение фоновой задачи или новый результат, а production-код в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:102` после ответа `202 pending` сразу перечитывает старую сводку без polling — цена: тесты зелёные при поломках 1, 2 и 8, а в реальной Celery-конфигурации кнопка завершится до расчёта и покажет оператору прежние суммы как новые.

## Проверено и нормально

- Полный продуктовый diff от `d62f9afb` до `dad176ea` сверен с `CONTRACT.md`, `FEATURES.md`, `ARCH.md`, `MAP.md` и приоритетным `ARCH-CROSS.md`; стадийные артефакты ролей не считались выходом за границы. Реализационные файлы входят в переданный владельцем список.
- Tenant-изоляция есть в истории габаритов, rebuild и statement-запросах; ручной обмер ограничен `inventory`, возврат WB и фиксация на сервере требуют администратора. Отрицательные `quantity_days`/`liter_days` запрещены ограничениями БД, а уникальности закрывают прямой дубль measurement/statement.
- Исправления прежнего ревью подтверждены: миграция 0095 продолжает 0094 и переносит legacy-габариты; расчёт режет период по движениям и событиям габаритов; нулевой ledger использует id statement; печать ненулевого документа связывает строки по source id.
- Целевой backend-прогон: `21 passed`. `git diff --check` чист. Frontend build не запускался из-за отсутствующих зависимостей в рабочей копии (`tsc: command not found`). Секреты, ключи, токены, `.env` и кабинеты учётных данных не читались.
