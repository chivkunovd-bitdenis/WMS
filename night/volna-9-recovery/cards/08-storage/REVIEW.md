# Ревью · 08-storage · повторная проверка ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 5

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:334` — новая ручка только сохраняет версии тарифа, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:196` после ответа лишь повторно загружает прежние statements; пересчёт открытых черновиков не запускается ни на одном слое. Если администратор меняет ставку при уже сформированном черновике, диалог закрывается, но видимая предварительная сумма остаётся рассчитанной по старым данным до отдельного ручного «Сформировать за месяц». Это расходится с `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/night/volna-9-recovery/cards/08-storage/CONTRACT.md:76`, где изменение тарифа обязано пересчитать открытые черновики. Цена: оператор может проверить и затем зафиксировать финансовый расчёт, не увидев ожидаемого результата смены ставки.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:81` — `valid_from` объявлен как произвольная дата, а ручка в строке 334 не проверяет её относительно сегодняшнего дня по МСК; экран в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:241` также не задаёт минимальную дату. Поэтому 23 августа 2026 года администратор может сохранить начало 1 августа 2026 года — именно такую уже прошедшую дату вводит назначенный тест в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:36`. Открытый августовский расчёт получит ставку задним числом, хотя контракт требует действия только вперёд. Цена: ретроактивное финансовое начисление и нарушение `S-11-TC-002`.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:384` — сервис без чтения `Warehouse` и `Seller` записывает переданные UUID вместе с `tenant_id` текущего администратора и не проверяет ни принадлежность обеих сущностей этому tenant, ни `Warehouse.is_operational`. Администратор tenant A, отправив UUID склада или селлера tenant B (либо собственного служебного склада), получает 201: внешние ключи в БД проверяют существование UUID, но не согласованность tenant. Цена: межтенантное загрязнение финансовых данных и тариф на склад, который контракт прямо запрещает тарифицировать; нарушен обязательный инвариант `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/night/volna-9-recovery/cards/08-storage/ARCH.md:138`.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:79` — серверная модель не требует положительную ставку. Значение `0` проходит до commit, потому что схема допускает `amount >= 0`, после чего склад считается настроенным и хранение становится бесплатным; отрицательное значение падает на ограничении БД и перехватывается в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/services/storage_statement_service.py:411` как ложный `tariff_already_exists`. UI проверяет положительность, но прямой API-запрос обходит экран. Цена: тихая потеря выручки при нулевой ставке и неверная диагностика вместо ошибки валидации при отрицательной; backend-тесты эти границы не покрывают.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:33` — после снятия мока `S-11-TC-002` не стал тестом живого API. При `tariffConfigured=false` helper в строке 12 возвращает `statements: []`, поэтому селектор «Селлер» пуст и `selectOption('seller-1')` в строке 38 завершается до POST. Даже без исключения запрос ушёл бы с фиктивным токеном `storage-e2e-token` и не-UUID склада `warehouse-1` из строк 10–12, то есть реальный сервер ответил бы 401 либо 422, а диалог в строке 42 не закрылся бы. Цена: обязательный сквозной тест ремонта фактически не исполняет успешное сохранение и не защищает от возврата 404/неатомарного контракта.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист, после чего проверен только ремонтный продуктовый diff `3df8a2a5..1ba96b02` и назначенные кейсы `S-11-TC-001`—`S-11-TC-020`. Исходные находки про отсутствующий маршрут, два последовательных POST и UTC-дату структурно исправлены: маршрут зарегистрирован, тело объединено, московская дата вынесена в отдельную утилиту.
- Все семь файлов ремонтной реализации входят в явно переданный список файлов карточки; изменения в `night/` признаны стадийными артефактами и не считались выходом за границы. `git diff --check` на ремонтном diff и `npx tsc --noEmit -p tsconfig.app.json` прошли.
- Атомарность двух INSERT при конфликте второго, запрет для `fulfillment_staff` и совпадение ограничения роли между экраном и сервером проверены по коду и целевым backend-тестам; здесь расхождения слоёв нет. Новых операторских блокировок, требующих дополнения `docs/blockers/S-11.md`, ремонт не добавил.
- Повторно запустить pytest и Vitest в этой рабочей копии не удалось из-за заполненного диска: Python не смог создать временный файл, Vite получил `ENOSPC`. Это ограничение повторной верификации, а не отдельная находка к продуктовым файлам. Секреты, ключи, токены, `.env`, кабинеты учётных данных и production не открывались и не использовались.
