# Ревью · 08-storage · повторный проход ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/ui-kit/DataTable.tsx:50`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/ui-kit/DataTable.tsx:53`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:457` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:505` — первая замороженная находка не закрыта для видимой области и A4. У таблицы истории сумма объявленных ширин равна 880 px, у печатной таблицы — 990 px, тогда как `Dialog maxWidth="md"` ограничен 900 px, а после внутренних отступов `DialogContent` под таблицу остаётся около 852 px. Фиксированная раскладка сохраняет широкую таблицу внутри `TableContainer`, который прокручивается по горизонтали: последние колонки могут совпадать со своими заголовками, но оставаться правее видимой области. Новые проверки сравнивают правый край последней колонки только с правым краем самой широкой таблицы, а не с контейнером, предпросмотром или печатной страницей; они также не включают print media и не проверяют PDF. Поэтому тест останется зелёным, когда «Ставка» и «Сумма» доступны лишь после горизонтальной прокрутки и обрезаются на A4. Цена: оператор может распечатать финансовое доказательство без ставки и суммы строки, то есть исходная проблема атомов 1 и 3 сохраняется.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/ui-kit/DataTable.tsx:83`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/ui-kit/DataTable.tsx:89` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/ui-kit/Cells.tsx:65` — ремонт добавил `overflow: hidden` каждой ячейке, но `ProductCell` выводит SKU с `whiteSpace: 'nowrap'` и не даёт ни переноса, ни многоточия с подсказкой. В детализации хранения при SKU длиннее 150 px значение теперь молча обрезается границей ячейки; два SKU с одинаковым длинным префиксом выглядят одинаково, хотя относятся к разным товарам. Тесты используют длинные автора и артикул как обычный текст, поэтому эта регрессия останется зелёной. Цена: оператор может открыть историю или внести обмер не тому товару, а ошибочный объём затем влияет на начисление хранения.

## Проверено и нормально

- Предыдущий `REVIEW.md` из коммита `7e94419d9de1e310e9e16b401f92f2461a13104c` использован как замороженный чек-лист; полностью прочитан только ремонтный diff до текущего `HEAD`, а незатронутые части исходной реализации повторно не переоткрывались.
- Вторая прежняя находка закрыта: карточка `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/docs/blockers/S-11.md` теперь содержит шесть полей, правдиво описывает одинаковое правило будущего месяца на экране и сервере и убирает устаревшее расхождение слоёв.
- Третья прежняя находка закрыта: `TC-NEW-STORAGE-REVIEW-*` удалены, будущий месяц закреплён постоянным `S-11-TC-022`, печатная проверка связана с `S-11-TC-009`; оба номера назначены в карточке и описаны в постоянной базе кейсов. Ремонтные файлы входят в явно разрешённые границы, стадийные артефакты ролей выходом за экран не считались.
- Прошли `npm run test:unit -- src/screens/ff/FfStoragePage.test.ts` (6 тестов), `npx tsc --noEmit -p tsconfig.app.json`, `npm run build` и `git diff --check`. Три целевых Playwright-сценария перечисляются, но фактический запуск остановлен до тестов запретом среды слушать `127.0.0.1:18000`; боевой прод, Wildberries, секреты, ключи, токены, `.env` и кабинеты учётных данных не открывались.
