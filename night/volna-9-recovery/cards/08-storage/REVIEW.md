# Ревью · 08-storage · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 7

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/src/screens/ff/FfStoragePage.tsx:201` — пункт 2 предыдущего ревью закрыт только со стороны моделей: диалог по-прежнему прямо сообщает, что API тарифа не опубликован, не содержит ставки и даты начала и не имеет действия «Сохранить»; при первом входе администратора без тарифа кнопка «Задать тариф» приводит в тупик, после чего сформировать и зафиксировать первый расчёт штатным путём невозможно — цена: основной сценарий `S-11-TC-002` и коммерческий запуск хранения остаются неработоспособными.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/models/billing.py:94` — добавленная ремонтом `BillingTariffVersion` не имеет `warehouse_id`, хотя контракт требует ставку операционного склада и override пары «селлер + склад»; вдобавок `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:243` считает `tariff_configured` на весь tenant без выбранного склада и селлера. При двух складах с разными договорными ценами уникальный индекс не позволит сохранить две общие ставки на одну дату, а единственная ставка будет применена обоим складам; персональная ставка одного селлера также объявит тариф настроенным для остальных — цена: неверные начисления и невозможность выполнить утверждённую тарифную модель.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/api/storage.py:156` — печатная строка берёт `liter_days` из измерения за весь месяц, тогда как ремонт тарификации публикует в ledger только литро-дни после `valid_from` и берёт сумму из ledger в строках 157—161. В уже закреплённом тестом сценарии ставки с 10 июля для 1 штуки объёмом 1 л документ покажет `31` литро-день, ставку `2.00` и сумму `44.00` вместо арифметически согласованных `22 × 2.00`; при смене ставки внутри месяца округлённая средняя ставка также может не перемножаться в итог — цена: A4-доказательство расходится с финансовым начислением и непригодно для спора с селлером.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/models/billing.py:181` — ремонтом зарегистрированы `BillingInvoice` и `BillingRunIssue`, которых нет ни в контракте 08/этапе 09-A, ни в миграции `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/alembic/versions/20260822_0094_billing_financial_core.py`; тест использует `Base.metadata.create_all()` и потому скрывает расхождение. После обычного production-обновления Alembic ORM считает таблицы существующими, но БД их не создаёт, и первый код 09-B, который запишет счёт или `storage_period_not_closed`, получит `relation does not exist` — цена: выход за атомарный контракт и будущий 500 на выставлении счетов.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/tests/test_storage_movement_scope.py:37` — новый тест требует, чтобы миграция движения с собственным `revision = "20260821_0094"` одновременно имела `down_revision = "20260821_0094"`; фактическая и правильная цепочка указывает на `20260821_0093`, поэтому целевой прогон падает: `1 failed, 31 passed` — цена: обязательный pytest-gate карточки красный, а замена кода под ожидание теста создала бы цикл Alembic.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/alembic/versions/20260822_0094_billing_financial_core.py:3` — новая миграция не проходит обязательный `ruff check`: неотсортированный import и 29 превышений длины строки; кроме того, `git diff --check` находит лишние пустые строки в конце `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/app/models/billing.py:226` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/backend/tests/test_billing_models.py:40` — цена: технический gate ветки гарантированно красный ещё до проверки поведения.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-08-storage/frontend/tests-e2e/storage.spec.ts:114` — ремонт заменил прежнюю проверку сотрудника с тарифом на пустое состояние без тарифа и удалил раскрытие строки с проверкой отсутствия `storage-fix`; тест останется зелёным, если сотруднику с `inventory` случайно показать административную фиксацию, потому что в новом сценарии statements пусты и кнопка вообще не может отрисоваться — цена: регрессия финансового разграничения ролей не будет поймана назначенным `S-11-TC-012`.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист, затем проверен только ремонтный diff `dad176ea..383f3c13`. Закрыты пункты 1, 3, 4, 5, 7, 8, 9 и 10: list endpoint существует, движения получают frozen seller/warehouse, служебные склады исключаются, WB-наблюдение не затирает ручной обмер, ручные события не дедуплицируются, текущий месяц не фиксируется, DTO истории совпадает с UI, доступ требует `inventory`, фоновая задача опрашивается до завершения.
- Продуктовые файлы сверены с переданным владельцем списком, `CONTRACT.md`, `FEATURES.md`, `CASES.md`, `ARCH.md`, `MAP.md` и приоритетным `ARCH-CROSS.md`; стадийные артефакты ролей не считались выходом за границы. Tenant-фильтры и запрет фиксации сотрудником на сервере сохранены.
- Целевой backend-прогон дал `31 passed, 1 failed`; ошибка полностью приведена в находке 5. Frontend production build и три unit-теста `PrintAction` проходят. Запуск Playwright не состоялся из-за запрета песочницы на bind `127.0.0.1:18000`, а не из-за утверждаемого продуктового дефекта.
- Секреты, ключи, токены, `.env`, персональные кабинеты и боевой production не открывались и не использовались. Продуктовый код не изменялся.
