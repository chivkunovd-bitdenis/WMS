# REVIEW · 01-wb-marking

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 9

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:161` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_fbs_kiz.py:66` — решение WB `required` отображается в `accepted`, а тест закрепляет это как норму. Если WB вернёт противоречивый снимок `required` с непустым, например оставшимся от прежней попытки, значением, ветка `required` без значения не сработает, `_apply_meta_detail_to_marking` поставит `accepted`, а `compute_delivery_allowed` разрешит сдачу. По контракту проходными являются только `filled` и `optional`, а противоречие должно закрываться безопасно; цена — ложный зелёный статус и отказ WB при сдаче поставки.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:548` — совместимое поле `check_status` всё ещё заполняется из устаревшего `row.meta.checkStatus` и не выводится из `metaDetails.decision`. При актуальном ответе только с `metaDetails` решение `invalid` поставит `meta_status=rejected`, но оставит прежний `check_status=ok`; мобильный ТСД или портал, читающий совместимое поле, покажет подтверждение одновременно с отказом. Цена — противоречащие статусы и нарушение обратной совместимости, ради которой поле оставлено.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_autopoll_service.py:267` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:605` — пропущенный в частичном ответе `order_id` передаётся как пустой `meta_batch`, после чего сервис всё равно пересчитывает агрегаты заказа, запускает синхронизацию упаковочного контура и увеличивает `synced`. Если WB вернул 99 строк из 100, сотый заказ будет учтён как успешно синхронизированный без полученного ответа. Цена — ложный успех мониторинга и изменение производных данных при сценарии, где контракт требует полностью сохранить состояние пропущенного заказа; целевые тесты новую функцию автополла не вызывают и останутся зелёными.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:505` — идемпотентность `wb_orphaned` реализована парой `SELECT`, затем `INSERT` без блокировки строки и без уникального ограничения в `MarkingCodeEvent`. Два параллельных запуска оба могут увидеть отсутствие события и записать по одному. Цена — дубли аудита именно в обязательном кейсе параллельной сверки вместо одного факта расхождения.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:532` — ручной путь читает локальные привязки до сетевого ожидания WB и применяет ответ без `FOR UPDATE` и без повторного чтения. Если оператор закрепит новый КИЗ, пока запрос находится в сети, старый снимок перезапишет агрегат заказа и `metadata_delivery_allowed`. Цена — сдача будет разрешена или заблокирована по уже неактуальной физической привязке.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/wildberries_fbs_client.py:652` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_wildberries_marketplace_fbs_client.py:148` — внешний `Retry-After` используется без верхней границы, а тест одобряет ожидание 1,5 секунды, но не проверяет контрактный короткий ограниченный повтор. При `Retry-After: 3600` обработка продавца остановится на час и не перейдёт к следующим пачкам. Цена — произвольная внешняя задержка фонового цикла, хотя архитектурный контракт прямо запрещает удерживать обработку продавца долгим ожиданием.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_marking_code_events.py:126` — тест `wb_orphaned` сам вставляет `MarkingCodeEvent` в БД и потому не проверяет вызов сверки, переход `missing`/`replacement_required`, однократность или сохранность КИЗ внутри новой сервисной логики. Он останется зелёным, если `_record_wb_orphaned_once` перестанет вызываться или начнёт дублировать события. Цена — обязательный аудит-факт не защищён тестом поведения.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:588` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:605` — для `required` без значения и неизвестного `decision` код меняет статус, но не записывает полученные `decision/value/reason` в `FbsOrderMarking.meta_details_json`; сводка `FbsOrder.meta_details_json` теряет сырой `decision`, а неизвестные ключи отбрасываются полностью. При новом решении или ключе WB исходный ответ, нужный для расследования, будет потерян. Цена — нарушение центрального требования хранить настоящий ответ без потерь и невозможность доказать причину состояния постфактум.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:573` — любое непустое значение WB, отличающееся от локального, превращается в `replacement_required` независимо от `decision`. Если WB вернёт для другого значения `invalid`, `pending`, `optional` или неизвестный вердикт, код проигнорирует официальный `decision`, запишет `wb_orphaned` и покажет замену; контракт разрешает такую ветку только для заполненного подтверждённого значения. Цена — ложная классификация расхождения и неверный аудит, хотя сдача при этом безопасно останется заблокированной.

## Проверено и нормально

- Полный продуктовый diff от точки входа карточки `d62f9afb` проверен: реализация ограничена восемью разрешёнными backend-файлами; файлы `night/` и `tests/cases/` рассматривались как стадийные артефакты и не считались выходом за границы.
- Автоматического освобождения КИЗ, сброса `marking_code_id`, перевода кода в `available`/`void`, записи в живой кабинет WB, изменений UI, формата внешнего API или расписания в diff нет; граница `ARCH-CROSS.md` соблюдена. Нового отдельного UI- или server-only запрета в коде не добавлено, поэтому новый файл `docs/blockers/S-xx.md` не требуется.
- Устаревшее одиночное чтение `fetch_marketplace_order_meta` удалено, batch POST остаётся единственным путём чтения; DTO сохраняет `reason`, а запросы режутся на последовательные пачки не более 100 уникальных ID.
- `ruff` по восьми изменённым файлам прошёл; пять целевых наборов дали `93 passed`. Зелёные тесты не моделируют сценарии из находок выше.
