# REVIEW · 01-wb-marking

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 4

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:282` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:595` — сырой ответ сохраняется только в `FbsOrderMarking.meta_details_json`, но сводка `FbsOrder.meta_details_json` заново строится без `decision` и с локальным `mark.value` вместо значения WB; неизвестные `metaDetails[].key` отбрасываются фильтром целиком. Если WB вернёт `filled` с другим КИЗ либо добавит новый ключ, в заказе исчезнут настоящий удалённый value/decision и весь неизвестный блок, хотя `ARCH.md:45,77,138` требует сохранять их без потери именно в сводке заказа. Цена — расследование расхождения увидит локальный КИЗ вместо ответа WB, а изменение схемы WB будет уничтожено при записи.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:496` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_fbs_kiz.py:2640` — совместимое поле `check_status` отображается не по утверждённому контракту и тест закрепляет расхождение: `required` без значения становится `error` вместо `new`, а неизвестный `decision` и отсутствующий ожидаемый `kind` становятся `checking` вместо `error` (`ARCH.md:52`). При `required` существующий клиент покажет оператору «Ошибка» вместо нового отсутствующего значения, а при неизвестном ответе — безобидное «Проверяется» вместо безопасного отказа. Цена — мобильный ТСД и текущий портал получают другую семантику совместимого API-поля.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:592` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:632` — сервис продолжает читать удалённый объект `row.meta.checkStatus`, хотя контракт делает `metaDetails` единственным источником и прямо запрещает legacy fallback. Если строка содержит актуальный `metaDetails` для текущего КИЗ и противоречащий старый `meta` для прежней привязки того же kind, старой строке будет записан статус из deprecated-объекта; после удаления текущей привязки этот испорченный исторический статус снова станет fallback-состоянием заказа. Цена — два источника истины и возвращение ложного статуса из формата, который WB уже выводит из эксплуатации.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_fbs_kiz.py:2782` — тест однократности `wb_orphaned` запускает две сверки последовательно, поэтому остаётся зелёным, если удалить блокировку `MarkingCode` на `fbs_marking_service.py:521`. При двух одновременных воркерах оба тогда выполнят `SELECT` до чужого `INSERT` и создадут два события; назначенный `S-03-TC-004` требует именно параллельный и повторный запуск. Цена — обязательная защита от дублей проверена только для повтора, но не для конкурентного сценария, ради которого добавлен `FOR UPDATE`.

## Проверено и нормально

- Полный продуктовый diff карточки после точки входа `d62f9afb` проверен: девять изменённых файлов реализации входят в явно разрешённый список; артефакты `night/` не считались выходом за границы. UI, расписание, боевой контур и запись в кабинет WB не менялись.
- Прежние дефекты с ложным `required → accepted`, пропущенным `order_id`, неограниченным `Retry-After`, повторной записью `wb_orphaned` и классификацией любого несовпавшего значения как замены в текущем коде исправлены. КИЗ при `missing`/`replacement_required` не освобождается, ссылка и жизненный статус сохраняются.
- Батчинг идёт последовательно по уникальным ID пачками 100, ошибка одной пачки не останавливает следующую, а пропущенная строка не увеличивает счётчик успеха и не запускает производное обновление упаковки. Новых UI- или server-only блокировок не появилось, отдельный `docs/blockers/S-xx.md` не требуется.
- Адресный `ruff` по девяти изменённым backend-файлам прошёл; целевые наборы `test_fbs_kiz.py`, `test_fbs_marking.py`, `test_marking_code_events.py` и `test_wildberries_marketplace_fbs_client.py` дали `90 passed`. Адресный `mypy` упёрся только в четыре уже существующие ошибки трёх импортируемых посторонних сервисов.
