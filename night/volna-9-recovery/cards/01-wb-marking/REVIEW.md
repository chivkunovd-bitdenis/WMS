# REVIEW · 01-wb-marking

Вердикт: APPROVED.

ВЕРДИКТ: ЧИСТО

## Находки

## Проверено и нормально

- Замороженный чек-лист предыдущего ревью проверен полностью. Пропущенный в успешной пачке `order_id` теперь проходит безопасное применение: прежний положительный вердикт снимается в `unknown`, свежая дата, уведомление и счётчик успеха не появляются; локальная привязка, статус КИЗ, причина и сырой блок сохраняются.
- Штатная E2E-заглушка в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/wildberries_fbs_client.py` возвращает сохранённую маркировку через рабочий `metaDetails`. Несовпадающее заполненное значение WB при любом решении, включая `invalid`, переводится в `replacement_required`, не освобождает КИЗ и создаёт один `wb_orphaned` при параллельных и повторных сверках.
- Повтор после `429` выполняется ровно один раз и соблюдает как числовой `Retry-After`, так и HTTP-дату. Ремонтный продуктовый diff после прошлого вердикта ограничен четырьмя разрешёнными файлами `fbs_autopoll_service.py`, `fbs_marking_service.py`, `test_fbs_kiz.py` и `test_fbs_marking.py`; frontend, API-форматы и новые операторские блокировки не менялись.
- Адресные проверки поведения прошли: `18 passed`; `ruff` по затронутым сервисам и тестам — без ошибок; `git diff --check` — без ошибок. Тесты проверяют итоговые статусы, сохранность занятого КИЗ, отсутствие ложного успеха, последовательные пачки `100/100/1`, продолжение после отказа пачки, рабочий mock-контракт и обе формы `Retry-After`.
