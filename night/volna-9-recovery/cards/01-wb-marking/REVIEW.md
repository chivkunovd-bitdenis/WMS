# REVIEW · 01-wb-marking

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 4

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_autopoll_service.py:269` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:582` — пропущенный в успешной пачке `order_id` вообще не передаётся в применение, а ручной путь при отсутствии строки оставляет прежний `meta_status`; если до неполного ответа заказ был `accepted`, он таким и останется вместо требуемого контрактом безопасного `unknown`. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_fbs_marking.py:484` проверяет только отсутствие вызова и счётчика успеха, но не итоговый статус ранее подтверждённой маркировки. Цена — существующий портал или ТСД продолжит показывать старое зелёное подтверждение после того, как WB перестал возвращать задание; это прямо расходится с `CONTRACT.md:24-27` и `FEATURES.md:33`.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/wildberries_fbs_client.py:633` — встроенная заглушка `e2e_mock_wb_marketplace_marking` по-прежнему возвращает данные только в удалённом `row.meta`, оставляя `meta_details=()`, тогда как новая сверка намеренно читает только `metaDetails`. При обычном локальном или браузерном сценарии под этой заглушкой `PUT` сохранит КИЗ в mock-хранилище, но последующее чтение сочтёт ожидаемый `kind` отсутствующим и переведёт маркировку в `unknown/error`; изменённые тесты обходят дефект собственными `fake_meta_batch`, поэтому остаются зелёными. Цена — штатная тестовая поверхность доказывает не то поведение, которое выполняет реальный batch-контракт, и сценарий печати/привязки под mock может ложно заблокировать дальнейшую сдачу.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/fbs_marking_service.py:615` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_fbs_kiz.py:2663` — несовпадающее заполненное значение становится `replacement_required` только для решений `accepted/filled`, а тест прямо закрепляет `invalid` с чужим значением как обычный `rejected`. Если WB вернёт `invalid` и одновременно другой КИЗ, система не зафиксирует требуемое пунктом 6 контракта расхождение значений и не создаст `wb_orphaned`. Цена — аудит потеряет факт, что удалённая и физическая привязки различаются, хотя локальный КИЗ останется занят и потребует ручной сверки.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/app/services/wildberries_fbs_client.py:655` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-01-wb-marking/backend/tests/test_wildberries_marketplace_fbs_client.py:149` — код не соблюдает полученный `Retry-After`: любое числовое значение обрезается до одной секунды, а допустимая HTTP-дата разбирается как ошибка и превращается в нулевую задержку; тест с `Retry-After: 3600` утверждает именно сокращённое ожидание. При реальном 429 с будущей датой или задержкой больше секунды повтор уйдёт раньше разрешённого окна и с высокой вероятностью снова получит 429. Цена — пачка останется несверенной до следующего цикла и создаст лишний запрос, хотя `FEATURES.md:7` и кейс `S-03-TC-005` требуют один раз дождаться `Retry-After`.

## Проверено и нормально

- Полный продуктовый diff от точки входа `d62f9afbef496916e0def30ba1c498ac31605a05` прочитан: все 11 файлов реализации и тестов входят в явно разрешённый список; `night/` и `tests/cases/` учтены как стадийные артефакты и не считались выходом за границы. Frontend-диффа нет, состав экранов `S-03`, `S-14` и `S-15` не менялся.
- Сохранность КИЗ при `missing` и `replacement_required` соблюдена: `marking_code_id`, статус и пул не освобождаются; конкурентный и повторный сценарий оставляет один `wb_orphaned`. Сырой `decision`, `value`, `reason` и неизвестный `key` сохраняются в сводке заказа, legacy `row.meta` в рабочем применении больше не читается.
- Батчи формируются по уникальным ID последовательно по 100, сопоставляются по `order_id`, а ошибка одной пачки не останавливает следующую. Устаревшее одиночное чтение удалено; формат существующих API-ответов и пользовательские подписи не менялись.
- Нового места, где оператору добавили отдельное «нельзя», в диффе нет: используется существующая серверная проверка `marking_not_allowed` и существующие экранные состояния, поэтому новый `docs/blockers/S-xx.md` не требуется. `ruff` по изменённым backend-файлам, `git diff --check` и адресные тесты прошли: `90 passed` плюс два затронутых интеграционных теста.
