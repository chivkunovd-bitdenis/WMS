ФИЧ: 2

## Фичи

### 1. Оставить одно сообщение об ошибке загрузки складов в FBS

Оператор в S-03 при недоступности списка складов видит ровно один `ErrorNotice` с действием «Обновите страницу», а не два одинаковых аварийных сообщения. Ошибка продолжает блокировать зависимые от склада действия; успешная загрузка и переключение склада не меняются.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsOrdersScreen.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/tests-e2e/ff-fbs-supply.spec.ts`

Зависит от: нет.

Как проверить: в Playwright замокировать ошибочный ответ списка складов, открыть FBS и убедиться, что виден единственный текст «Не удалось загрузить склады. Обновите страницу.» внутри `fbs-wms-warehouse-context`; внешнего `fbs-wms-warehouse-context-error` нет. При доступном ответе переключатель и список заказов по-прежнему работают.

### 2. Сократить подпись выгрузки остатков

Оператор в S-04 видит на главной кнопке короткое действие «Выгрузить остатки». Условие, что выгружаются только включённые склады, остаётся в существующем контексте экрана и не превращается в дополнительную кнопку или настройку; доступность кнопки и запуск выгрузки не меняются.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsStockSyncScreen.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/tests-e2e/ff-fbs-stock-sync.spec.ts`

Зависит от: нет.

Как проверить: в Playwright открыть S-04 с хотя бы одной включённой привязкой, увидеть кнопку `fbs-stock-sync-all` с подписью «Выгрузить остатки», нажать её и получить существующую видимую обратную связь `fbs-stock-sync-feedback`. При отсутствии строк для выгрузки кнопка остаётся недоступной.

## Порядок

1. Фичи 1 и 2 независимы: их можно выполнять параллельно разными frontend-исполнителями, потому что они меняют разные экраны, исходные файлы и e2e-спеки.
2. В каждой фиче сначала меняется подпись или единственный источник сообщения, затем в указанной e2e-спеке закрепляется пользовательский результат.

## Что осталось за бортом

- Рост размеров экранных компонентов, отмеченный `ui_guard.py`, не является отдельной незакрытой находкой `DESIGN-REVIEW.md`; декомпозиция этих экранов не входит в перепланирование ремонта.
- Изменение бизнес-логики склада, API, сканера, межскладских перемещений и ui-kit уже принято или не относится к двум незакрытым UX-находкам, поэтому сюда не включено.
