# Review · 04-warehouse-switch · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.tsx:1927` — ремонт S-14 безусловно рисует активный общий `WarehouseContextSwitch` и над очередью, и над уже открытым заданием, тогда как выбранное задание на строках 1948–1957 не закрывается и не меняется вместе с контекстом. Сценарий: оператор открывает задание склада «Север» по `/app/ff/packaging/:taskId`, затем в верхнем переключателе выбирает «Юг»; сверху экран сообщает `Склад Юг`, но панель продолжает показывать и исполнять задание и ячейку «Север». Это расходится с контрактом закреплённого документа: его склад должен быть показан как недоступный для смены с причиной, а контекст списка не должен выглядеть контекстом открытой операции. Цена — оператор получает два противоречащих друг другу склада в одном рабочем месте и может упаковывать документ не того склада, который считает активным. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.test.ts:194` проверяет только очередь без `selected`/`:taskId`, поэтому остаётся зелёным при этой поломке.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/tests/test_inbound_intake.py:1684` — единственная проверка ремонта конкурентной смены склада помечена `postgresql_concurrency` и на строках 1689–1691 безусловно пропускается не на PostgreSQL, однако штатный CI запускает обычный `pytest` на SQLite и PostgreSQL-контур для этого маркера не настраивает. Сценарий: из `patch_request_draft` или `submit_request` случайно удаляют `FOR UPDATE` либо условие `status='draft'`; все обязательные проверки остаются зелёными, потому что последовательные кейсы гонку не моделируют, а новый тест сообщает `skipped`. Цена — защита от заднего переписывания склада переданной заявки не является проверяемым инвариантом перед вливанием; локальный прогон подтвердил ровно `4 passed, 1 skipped`, а не работу гонки.

## Проверено и нормально

- Замороженный чек-лист предыдущего `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/night/volna-9-recovery/cards/04-warehouse-switch/REVIEW.md` проверен по каждому пункту. Исходная S-14-проблема с отдельным экземпляром хука закрыта: `App` передаёт странице общий список, выбранный склад и обработчик, очередь меняется «Север» → «Юг», а при `null` запрос не выполняется.
- Исходная гонка S-28 закрыта в реализации: обе операции захватывают одну строку заявки, смена склада дополнительно делает условный `UPDATE` только черновика, `null`, чужой и неоперационный склад по-прежнему дают `422`, а последовательная смена после передачи — `409`.
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/docs/blockers/S-28.md` содержит обе новые блокировки, у каждой заполнены все шесть полей и экранный запрет сопоставлен с серверным. Ремонтный diff входит в разрешённые границы; `night/` и реестр блокировок считались обязательными стадийными артефактами, а не выходом за экран.
- `ruff`, целевой `mypy`, TypeScript-проверка и frontend unit suite (`2 passed`) прошли; backend-выборка дала `4 passed, 1 skipped`, `git diff --check` чист. Секреты, ключи, токены, `.env`, кабинеты учётных данных, живой Wildberries и production `194.87.96.144` не открывались и не изменялись.
