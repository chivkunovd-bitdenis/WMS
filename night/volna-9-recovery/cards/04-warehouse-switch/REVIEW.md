# REVIEW · 04-warehouse-switch

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 13

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/inventory_service.py:561` — межскладская пара записывается без обязательных неизменяемых измерений `seller_id` и `warehouse_id`; более того, текущая модель `InventoryMovement` этих полей не содержит. Это нарушает обязательный порядок `ARCH-CROSS.md`, где 04-D разрешён только после 07-A. При подборе из склада A в сортировку склада B обе строки можно связать лишь с текущими ячейками, поэтому отчётность и хранение не получают зафиксированные склад исходящей/входящей стороны и селлера. Цена — непроверяемая история движений и неверные последующие отчёты/начисления.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/models/warehouse.py:31` — `barcode` стал `nullable=False`, но не получил Python/ORM-default. Любой прежний прямой конструктор `Warehouse(...)` без нового поля падает при flush с `NOT NULL constraint failed: warehouses.barcode`. Целевой прогон подтвердил это на тридцати тестах `test_fbs_stock_models.py` и `test_fbs_stock_sync.py`. Цена — красный backend CI и поломка всех внутренних seed/import-путей, которые создают модель не через `catalog_service.create_warehouse`.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/alembic/versions/20260822_0094_warehouse_operational_barcode.py:40` — если у tenant есть только legacy-склады `fbs-wb-*` / `FBS WB *`, миграция сначала помечает их служебными, а затем один из них снова делает `is_operational=true`. После обновления такой tenant увидит служебный склад в `/warehouses`, preflight и физических остатках, хотя контракт прямо требует скрывать все `FBS WB *` и при нуле рабочих складов показывать пустое состояние. Цена — возврат исходного дефекта со смешением WB-склада и физического склада WMS.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/fbs_supply_validator_service.py:189` — новый общий preflight считает доступность без `exclude_fbs_order_ids`, хотя выбранные заказы уже имеют собственные резервы. При остатке 1 и одном выбранном заказе с резервом 1 сервис получает доступность 0, затем ещё раз сравнивает её с требованием 1 и объявляет общую нехватку. Цена — валидная поставка блокируется ровно на остатке, достаточном для выбранных заказов.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/api/fbs_supplies.py:83` — API-контракт preflight не содержит `stock_preflight`, `warehouse_options` или `inventory`, а request-модели на строках 26–39 и 49–56 не принимают `selected_warehouse_id`. Pydantic отбрасывает рассчитанный `stock_preflight`, а поля, отправляемые из `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FbsSupplyCreateDialog.tsx:91` и `:118`, игнорируются. При двух складах оператор не увидит разбивку/предупреждение и его выбор не повлияет ни на повторный preflight, ни на созданную поставку. Цена — центральный сценарий выбора склада в FBS существует только в TypeScript-типах, но не в рабочем API.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/fbs_picking_service.py:287` — скан пустой ячейки не отклоняется: при `available < 1` код создаёт положительное движение `fbs_order_pick` на 1 штуку прямо в сортировке и отмечает заказ подобранным. После расширения выбора на любой операционный склад достаточно отправить `location_id` пустой ячейки другого склада, чтобы товар возник из ничего без `stock_transfer_out`. Цена — искусственное увеличение остатка и упаковка товара, которого физически нет.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/fbs_supply_service.py:240` — смена склада читает поставку без `FOR UPDATE` и блокирует её только по pick/task/trbx, не по финальному статусу. Поставку `in_delivery`/`done`, у которой нет этих связанных строк (например импортированную или legacy), можно перепривязать задним числом; при одновременном первом pick и PATCH обе транзакции могут увидеть «не начато» и оставить документ на одном складе, а движение — в сортировке другого. Цена — исторический документ расходится с уже совершённым складским фактом.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/App.tsx:3257` — `InboundScreen` и аналогично `OutboundScreen` на строке 3296 получают `selectedWarehouseId`, но не получают добавленный `onWarehouseChange`. При двух складах переключатель виден, однако его `onChange` в `InboundScreen.tsx:195` / `OutboundScreen.tsx:120` вызывает `undefined`, поэтому выбор не меняется и новая заявка создаётся на старом складе. Цена — оператор уверен, что сменил склад приёмки/отгрузки, но документ уходит на другой.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1539` — переключателю рабочего места передаётся ровно один option — уже записанный склад документа; `WarehouseContextSwitch` при одном option вообще не рендерится, а API-вызова PATCH в этом экране нет. Для нетронутой поставки с двумя операционными складами сменить склад консолидации невозможно. Цена — утверждённый сценарий «сменить до первого действия, затем заблокировать» недоступен из UI.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:519` — S-03 завёл собственный локальный `wmsWarehouseId`, а варианты строит только из строк текущей вкладки на строках 534–545, вместо единого сессионного контекста. После выбора склада B на S-04 переход на S-03 с заказами только склада A автоматически выберет A; пустой склад B выбрать и увидеть контрактный `EmptyState` нельзя. Цена — контекст не сохраняется между экранами и фильтр зависит от наличия данных, а не от списка операционных складов.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:376` — фильтр оставляет только строки, у которых уже есть `selectedWmsId`, равный текущему складу. У нового селлера все WB-склады ещё не сопоставлены и имеют `selectedWmsId=''`, поэтому таблица пустая и элементы настройки привязки на строках 823+ недоступны. Цена — с чистого состояния невозможно создать первую привязку WB → WMS через существующий экран.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/catalog_service.py:48` — создание склада не проверяет пересечение его `code`/`barcode` с кодами и баркодами ячеек. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/tests/test_warehouses.py:54` даже закрепляет обратное контракту: разрешает создать склад с кодом существующей ячейки и ждёт 409 только при каждом последующем скане. Цена — валидная конфигурация превращается в постоянную блокировку сканера вместо отклонения конфликтующей настройки.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/docs/blockers/S-03.md:36` — реестр блокировок описывает старый preflight, но не содержит две новые доменные блокировки карточки: `supply_warehouse_locked` и `insufficient_sorting_stock`. Для первой UI фактически не даёт действия вообще, для второй сервер блокирует упаковку, а экран заранее не предупреждает об отсутствии остатка в сортировке. Цена — обязательная сверка двух слоёв не выполнена, оператор упирается в новые запреты без согласованного описания и маршрута разблокировки.

## Проверено и нормально

- Полный diff реализации от базы карточки `d62f9afbef496916e0def30ba1c498ac31605a05` до `44665797b11fa634d364fc1407471de7bd3aefc8` прочитан; изменённые файлы реализации входят в переданный список, а `night/` и `tests/cases/` корректно признаны стадийными артефактами.
- Tenant-проверки в resolver, выборе ячейки, привязке WB и смене склада присутствуют; чужой tenant через новые маршруты не проходит.
- Межскладской transfer при достаточном остатке создаёт две стороны с общим `transfer_group_id`, а удаление упаковочного обхода больше не списывает товар молча из сортировки другого склада.
- `ruff check` по всем изменённым backend-файлам и тестам прошёл; `git diff --check` чист. Целевой pytest: `34 passed, 1 skipped, 31 failed`; тридцать падений относятся к находке 2, ещё одно — к существующему тесту с уже прошедшей фиксированной датой. Frontend build не запустился, потому что в рабочей копии отсутствует локальный `tsc`.
