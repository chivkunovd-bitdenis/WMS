# Review · 04-warehouse-switch · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 6

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/fbsApi.ts:289` — серверный ремонт теперь возвращает точную разбивку `source_warehouses[]` с количеством по каждому складу (`backend/app/services/fbs_supply_validator_service.py:462`), но frontend-тип и диалог на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FbsSupplyCreateDialog.tsx:43` по-прежнему читают только legacy `source_warehouse`. При потребности 10 шт. и остатках «Юг» 6 + «Север» 4 сервер обнулит одиночное поле и передаст два точных источника, а оператор увидит только `другие склады — 10 шт.` без имён и исполнимых количеств. Unit- и E2E-тесты подставляют невозможный для нового ответа старый `source_warehouse: Юг, available: 6`, поэтому остаются зелёными при этой поломке. Цена — прежняя находка №1 не закрыта: разрешённая поставка снова не даёт оператору инструкции, откуда физически взять товар.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/fbsApi.ts:608` — ремонт добавил серверный `warehouse_id` в список поставок, но клиентский wrapper не принимает и не передаёт этот параметр; вызов на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:584` загружает первые 500 поставок всех складов и лишь затем фильтрует их в памяти. Если у «Севера» 500 более свежих поставок, а у выбранного «Юга» есть более старая активная поставка, UI покажет пустой склад, хотя документ существует. Цена — оператор пропускает сборку или отгрузку выбранного склада; новый backend-фильтр фактически не используется.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.tsx:1879` — прежняя находка №7 не ремонтировалась: S-14 загружает очередь только по `status` и `search`, без общего складского контекста и без `warehouse_id`. При двух операционных складах упаковщик по-прежнему видит задания обеих зон, открывает чужое задание и только при подтверждении упирается в новый серверный запрет. Цена — контракт S-14 и назначенный `S-14-TC-001` не реализованы, а оператор получает позднюю стену вместо фильтрованной рабочей очереди.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/api/inbound_intake.py:64` — прежняя находка №9 не закрыта: PATCH-схема черновика всё ещё не содержит `warehouse_id`, хотя `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/SellerInboundDraftScreen.tsx:588` отправляет его. FastAPI игнорирует лишнее поле, возвращает документ со старым складом, после чего экран откатывает выбор с ошибкой. Добавленный E2E на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/tests-e2e/seller-cabinet.spec.ts:157` ожидает успешную смену от реального API и поэтому не может пройти; живой запуск в этой среде остановился раньше теста на запрете bind `127.0.0.1:18000`. Цена — селлер не может выполнить обязательный S-28 и сохранить склад черновика.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/api/fbs_supplies.py:876` — новые штатные блокировки упаковки `insufficient_sorting_stock` и `foreign_sorting_location` попадают в default-ветку как HTTP 500, причём `exc.message` отбрасывается. Поэтому экран получает технический код вместо понятного объяснения; запись B-14 в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/docs/blockers/S-03.md:264` ошибочно утверждает, что UI переводит код, хотя `readApiErrorMessage` сначала берёт непустое поле `message`, а там уже лежит сам код. B-15 на строке 280 прямо признаёт отсутствие UI-слоя, а `supply_warehouse_locked` и запрет работы без операционного склада в реестр так и не добавлены. Сценарий: упаковщик подтверждает строку без остатка или с чужой сортировкой и видит `insufficient_sorting_stock` / `foreign_sorting_location` после серверной ошибки 500. Цена — прежняя находка №11 закрыта формально, но оба слоя по-прежнему расходятся, а оператор не понимает, как продолжить работу.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/ui-kit/WarehouseContextSwitch.test.tsx:1` — обязательная frontend-сборка фактически не проходит: тест импортирует отсутствующий в `frontend/package.json` пакет `@testing-library/react` и использует незагруженные DOM-matchers (`toBeInTheDocument`, `toBeDisabled` и другие). Воспроизведено командой `npm run build`: TypeScript завершился с `TS2307` и серией `TS2339`. Целевой Vitest-запуск при этом сообщил только 5 прошедших файлов из 6 запрошенных и не исполнил этот suite, поэтому добавленные проверки пустого/ошибочного состояния не дают защиты. Цена — ветка не проходит обязательный build-gate и не может быть влита; это подтверждённый пропуск первого ревью сборки.

## Проверено и нормально

- Предыдущий `REVIEW.md` проверен как замороженный чек-лист; ремонтный diff `ce8c87f92e7e71f847e6264cc87ff41964ca149f..153068ec` и все 23 назначенных кейса прочитаны полностью. Продуктовые файлы входят в переданный пользователем список; `night/`, `tests/cases/` и обязательный реестр `docs/blockers/S-03.md` не считались выходом за границы.
- Находки про два одинаковых SKU, ложную смену склада консолидации при скане ячейки, единый сессионный контекст, ноль складов, количества S-01, раскрытие пары S-25 и обязательные измерения `InventoryMovement` закрыты. Повтор/undo движений остаются атомарными, а условное списание не позволяет остатку уйти в минус.
- Изменённый backend-набор дал `55 passed, 1 skipped, 1 failed`; единственный иной сбой — календарный тест с фиксированной датой 15.08.2026, просроченной на дату запуска. Целевые frontend unit-тесты дали `5 files / 18 tests passed`, но шестой запрошенный suite не был собран; отдельный `npm run build` подтвердил находку №6.
- `git diff --check` прошёл. Живой Playwright-сценарий смены склада черновика запустить не удалось: sandbox запретил webServer привязать `127.0.0.1:18000`; несоответствие PATCH-контракта подтверждено чтением реальной FastAPI-схемы и обработчика.
