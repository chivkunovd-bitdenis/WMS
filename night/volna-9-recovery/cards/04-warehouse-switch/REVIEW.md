# Review · 04-warehouse-switch · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.tsx:1866` — прежняя находка S-14 не закрыта: экран создаёт собственный экземпляр `useWarehouseContext('fulfillment')`, но не загружает в него склады, не получает состояние экземпляра из `App` и не рисует `WarehouseContextSwitch`. У хука локальные `useState`; начальное значение `selectedWarehouseId` равно `null`, а сохранённый выбор читается только внутри `setWarehouses`, которого этот экран не вызывает. Сценарий: оператор входит в систему или обновляет прямой URL `/app/ff/packaging`; общий экземпляр в `App` загружает «Север» и «Юг», но экземпляр страницы остаётся пустым, строка 1881 не отправляет запрос очереди, а строка 1961 показывает «Нет рабочего склада» с неисполняемой подсказкой выбрать его сверху. Цена — очередь упаковки полностью недоступна, `warehouse_id` фактически не используется и назначенный `S-14-TC-001` не проходит. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.test.ts:4` читает исходник как строку и остаётся зелёным при этой поломке, поэтому поведение не защищено.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/inbound_intake_service.py:440` — запрет смены склада после передачи проверяется неатомарно: `patch_request_draft` читает заявку обычным `SELECT` без блокировки строки, а затем отдельно фиксирует `warehouse_id` на строке 469; `submit_request` на строках 536–552 так же отдельно читает `draft` и фиксирует `status='submitted'`. Сценарий: в двух вкладках селлер одновременно выбирает «Юг» и нажимает «Передать на склад»; обе транзакции успевают прочитать `draft`, передача фиксирует статус, а запоздавший PATCH обновляет только склад и возвращает успех. В итоге уже переданная на «Север» заявка становится документом «Юга», хотя последовательный тест после передачи зелёный. Цена — склад принятого документа переписывается задним числом, очередь приёмки и физическое место расходятся; серверный запрет S-28 обходится обычной конкурентной правкой.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/services/inbound_intake_service.py:448` — ремонт добавил две новые блокировки сохранения склада (`status != draft` и отсутствующий, чужой либо неоперационный склад на строке 466), но канонического `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/docs/blockers/S-28.md` с обязательными шестью полями нет. Сейчас слои совпадают: экран скрывает выбор после `draft` и исключает неоперационные/служебные склады, сервер возвращает `409 not_draft` или `422 invalid_warehouse`; оператор видит закреплённый склад либо при устаревшем выборе общую ошибку смены, разблокируется обновлением карточки и выбором рабочего склада до передачи, а бизнес-смысл — не дать переписать маршрут переданного документа и не направить приёмку в служебную зону. Цена отсутствующего реестра — обе новые «нельзя»-ветки не входят в обязательную сверку экран/сервер и могут разойтись при следующей правке без заметного контрактного следа.

## Проверено и нормально

- Предыдущий `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/night/volna-9-recovery/cards/04-warehouse-switch/REVIEW.md` использован как замороженный чек-лист; прочитаны ремонтный diff `7fa3d629..a3710b7c`, контракт, `MAP.md`, обязательный `ARCH-CROSS.md` и все 23 назначенных кейса. Все десять продуктовых файлов ремонта входят в явно разрешённый список; артефакты `night/` не считались выходом за границы.
- Прежняя гонка S-03 закрыта: новый запуск прерывает старый `AbortController`, `signal` проходит во все worklist-запросы, включая страницы курсора, устаревший запрос не сбрасывает индикатор и `AbortError` не попадает оператору. Целевой тест действительно моделирует медленный «Север» и победу «Юга».
- Последовательные ветки S-28 работают: действующий операционный склад сохраняется, `null` и неоперационный склад дают `422`, смена после уже завершённой передачи даёт `409`; tenant-фильтр нового склада сохранён. Остатки и движения ремонтный diff не меняет.
- `ruff`, целевой `mypy`, `pytest -k warehouse` (`5 passed`), TypeScript-проверка и два frontend suite (`12 passed`) прошли; `git diff --check` чист. Секреты, ключи, токены, `.env`, кабинеты учётных данных и боевой production не открывались и не изменялись.
