# Review · 04-warehouse-switch · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/ff/FfPackagingPage.tsx:1879` — прежняя находка по S-14 не ремонтировалась: список заданий упаковки по-прежнему запрашивается только с `status` и `search`, без общего складского контекста и без `warehouse_id`, хотя серверный список уже умеет такой фильтр. При двух операционных складах упаковщик видит задания обеих зон, открывает чужое задание и лишь при подтверждении упирается в серверный запрет. Цена — контракт S-14 и назначенный `S-14-TC-001` остаются невыполненными; оператор получает позднюю блокировку вместо рабочей очереди своего склада.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/backend/app/api/inbound_intake.py:64` — прежняя находка по S-28 не закрыта: PATCH-схема `InboundIntakeRequestPlannedPatch` всё ещё не принимает `warehouse_id`, хотя `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/SellerInboundDraftScreen.tsx:588` отправляет его. FastAPI игнорирует лишнее поле и возвращает документ со старым складом; экран откатывает выбор с ошибкой. Цена — селлер не может сохранить склад черновика, обязательный сценарий S-28 и его E2E не проходят через реальный API.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-04-warehouse-switch/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:584` — ремонт серверной фильтрации поставок внёс гонку: при смене WMS-склада новый `load()` молча завершается, если запрос предыдущего склада ещё выполняется, а завершившийся старый запрос на строках 604–609 записывает уже отфильтрованные данные старого склада. Сценарий: медленно грузится «Север», оператор выбирает «Юг» до ответа; запрос «Юга» пропускается, ответ «Севера» сохраняется и затем отфильтровывается клиентом, поэтому вкладка выглядит пустой до следующего 30-секундного опроса или ручного обновления. Новый E2E отвечает мгновенно и эту гонку не проверяет. Цена — оператор временно теряет из списка существующие поставки выбранного склада и может пропустить сборку или отгрузку.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист; прочитаны ремонтный diff `153068ec..7fa3d629`, контракт, архитектурные решения и все 23 назначенных кейса. Продуктовые файлы ремонта входят в переданный список; стадийные артефакты `night/` и `docs/blockers/` не считались выходом за границы.
- Находка по точной разбивке источников preflight закрыта: frontend читает `source_warehouses[]`, агрегирует по ID склада и показывает имена с исполнимыми количествами. Находка по лимиту списка поставок закрыта серверным `warehouse_id`, но её ремонт породил отдельную гонку из пункта 3.
- Ошибки упаковочного списания теперь возвращаются как структурированный HTTP 409 с понятным сообщением; блокировки `B-14`–`B-17` описывают серверное правило и видимое состояние. Общий переключатель больше не ломает TypeScript-сборку, а его suite действительно исполняется.
- `npm run build` и 21 целевой frontend unit-тест прошли. Backend-набор дал `45 passed, 1 skipped, 1 failed`; единственный сбой — ранее известный календарный тест с фиксированной датой 15.08.2026, которая уже прошла на дату запуска. `git diff --check` до записи артефакта прошёл.
