# Ревью · 02-verdikt-screen · повторный проход

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 6

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:687-697,700-736` — защита от запоздалого ответа делает победителем первый завершившийся запрос, а не более новый запрос. Сценарий: запрос A стартует первым; запрос B стартует позже; A успевает сохранить старое положительное `filled`, пока B ещё ждёт WB; затем B получает актуальный отказ. Поскольку `metadata_last_checked_at` от A уже новее `started_at` запроса B, строки 694–696 ошибочно объявляют B устаревшим и отбрасывают отказ. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_marking.py:321-367` проверяет только обратный порядок завершения, когда B полностью сохраняется раньше A. Цена — прежний `WB: принято` и разрешение сдачи могут остаться после реально более новой проверки с отказом; пункт 1 прежнего ревью закрыт не для всех допустимых порядков двух одновременных запросов.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:660-683` — оба запроса с `populate_existing=True` перечитывают уже находящиеся в сессии ORM-объекты и сбрасывают заранее загруженные связи `order.markings` и `marking.marking_code`. Сценарий: оператор подтверждает замену уже привязанного ЧЗ; после успешной синхронизации новый код с WB обработчик продолжает замену и обращается к прежнему `marking.marking_code`, но SQLAlchemy пытается лениво читать связь вне допустимого async-контекста и выбрасывает `MissingGreenlet`. Это воспроизводится целевыми тестами `test_fbs_kiz_commit_confirmed_replaces_old_kiz_and_voids_code`, `test_fbs_kiz_pool_to_external_replacement_does_not_double_count_unit` и `test_fbs_marking_readers_prefer_active_row_over_newer_rejected_row`. Цена — запрос замены ЧЗ завершается 500 вместо штатного погашения прежнего кода; три регрессии уже красные.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_kiz.py:1348-1349` — добавленная проверка противоречит собственной фикстуре и контракту: `_patch_wb_acceptance` возвращает явный `decision="accepted"`, а тест ожидает `Нет ответа WB` и `delivery_allowed = false`. Фактический серверный ответ закономерно содержит `WB: принято` и `true`, поэтому тест падает. Цена — разрешённый backend-набор красный (`102 passed, 4 failed` вместе с находкой 2), а этот тест не может служить доказательством fail-closed поведения после сканирования.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:331-347,390-396` — у фоновых загрузок нет номера поколения или отмены предыдущего запроса, и любой успешный ответ безусловно снимает `refreshError`. Сценарий: медленный refresh A стартует первым; через 15 секунд refresh B стартует позже и быстро падает, включая безопасный запрет; затем A возвращает уже устаревший положительный workspace, очищает ошибку на строке 337 и снова активирует «Передать в WB». Цена — экран опять показывает ложный ранний допуск до того, как серверный гейт отклонит финальное действие, то есть возвращает исходный пользовательский дефект именно при медленной сети.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:265-281` — e2e-сценарий ожидания WB остался на старом контракте: фикстура передаёт `tone: 'stop'`, а tooltip ожидается как «Сдача пока недоступна». Реализация `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/utils/metaStatus.ts:81-87` теперь правильно выдаёт нейтральный тон и «WB ещё не подтвердил код», поэтому браузерная проверка упадёт на строке 281. Цена — frontend e2e не зелёный и не подтверждает отремонтированный пункт 3 прежнего ревью.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:429-511` — тест с именем `S-03-TC-018` проверяет другой сценарий: вкладка всё время видима, последовательно приходит 503, затем отказ, но пользователь ни разу не пытается передать поставку. Назначенный кейс `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/tests/cases/S-03.md:313-329` требует скрыть вкладку, сохранить отказ на сервере, вернуться и немедленно нажать сдачу, проверив отсутствие передачи и статуса «В доставке». Текущий тест останется зелёным, даже если скрытая вкладка отправит старое разрешение напрямую в deliver. Цена — пункт 4 прежнего ревью не закрыт назначенным пользовательским доказательством, а опасный путь обхода не защищён от регрессии.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист; просмотрен весь продуктовый diff после коммита прежнего вердикта `b8942efa`, а неизменённые части исходной реализации заново не переоткрывались. Все затронутые продуктовые файлы входят в переданный список карточки; стадийные артефакты ролей не считались выходом за границы.
- Последовательный сценарий ошибки фонового refresh теперь действительно показывает складской `Alert`, подменяет строки на `Нет ответа WB`, блокирует `PrimaryAction`, а следующий последовательный успешный refresh восстанавливает только свежий серверный вердикт. Серверный deliver-гейт повторяет запрет и не меняет остатки, резерв или складские движения.
- Словарь ожидания/требования/безответа соответствует контракту: `pending` нейтрален, `required` показывает «Пришлите ЧЗ», неизвестный ответ закрывается с «Ждём ответа Wildberries». Снята зелёная заливка строки по WB-вердикту; зелёным остаётся только хвост ЧЗ, а состояния печати и активного сканера не смешаны с вердиктом.
- В изолированном окружении без чтения `.env` прошли frontend unit-тесты (`15 passed`), TypeScript, ESLint, Ruff и целевой mypy. Backend-набор завершился `102 passed, 4 failed`; все четыре падения отражены в находках 2–3. Playwright обнаруживает назначенные тесты, но живой web-server не запускался, чтобы не читать проектный `.env`; секреты, токены, живой WB и production `194.87.96.144` не затрагивались.
