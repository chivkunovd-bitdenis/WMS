# Ревью · 02-verdikt-screen · повторная проверка ремонта

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 1

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:689-690` — `_record_wb_sync_started` теперь делает `session.commit()` внутри переданной вызывающим кодом сессии. Этот commit фиксирует не только маркер проверки и снимает не только его SQLite-блокировку: он завершает всю транзакцию и освобождает блокировку поставки, взятую через `_get_supply_for_update` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_shipment_service.py:972-986`, когда маршрут сдачи доходит до синхронизации маркировки через строки 619-630. Конкретный сценарий: два запроса `deliver` одной поставки с разными ключами идемпотентности приходят параллельно; первый берёт блокировку поставки, но освобождает её этим внутренним commit до вызова `deliver_marketplace_supply`, второй проходит тот же гейт, и оба запроса могут создать отдельные операции и отправить передачу в WB. Цена — повторная внешняя операция и две конкурирующие записи истории сдачи; кроме того, commit преждевременно фиксирует любые уже накопленные в вызывающей транзакции изменения статусов заказов. Тест ремонта вызывает `_sync_order_meta_from_wb` в изолированных сессиях и поэтому эту потерю границы транзакции не обнаруживает.

## Проверено и нормально

- Предыдущий `REVIEW.md` из коммита `c5a8c95d` использован как замороженный чек-лист; проверен только ремонтный diff `c5a8c95d..65779114`. Все четыре затронутых продуктовых файла входят в переданные границы карточки, а изменения в `night/` учтены как стадийные артефакты.
- Первая прежняя находка закрыта в исходном сценарии: новый WB-запрос стартует до завершения старого, поздний успех не перетирает свежий отказ; `ruff` прошёл, оба варианта `test_fbs_marking_sync_does_not_apply_stale_response` прошли (`2 passed`), тест пути автополла прошёл (`1 passed`). Новая регрессия границы транзакции вынесена отдельной находкой выше.
- Вторая прежняя находка закрыта: `visibilitychange` сразу ставит синхронный защитный ref, кнопка и сам `deliver()` учитывают его, а `S-03-TC-018` делает обычный клик без `force` до ответа refresh и проверяет отсутствие диалога и `/deliver`. TypeScript, три unit-теста и обнаружение двух Playwright-сценариев прошли; фактический Playwright не стартовал только из-за запрета среды на bind `127.0.0.1:18000`.
- Новая временная блокировка сдачи на время обновления записана шестью полями в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/docs/blockers/S-03.md`: экран показывает причину, а сервер независимо обновляет WB-вердикт и повторяет гейт. Формат API, остатки, резервы и складские движения ремонтный diff не меняет; секреты, `.env`, кабинеты учётных данных, живой WB и production `194.87.96.144` не читались и не затрагивались.

## Сохранность результата

Артефакт и дополнение реестра блокировок записаны в этой рабочей копии поверх `65779114`, но отдельный коммит создать нельзя: `git add` не смог создать `/Users/deniscivkunov/Projects/WMS/.git/worktrees/lane-2-02-verdikt-screen1/index.lock` из-за `Operation not permitted`. До снятия ограничения результат остаётся локальным незакоммиченным diff.
