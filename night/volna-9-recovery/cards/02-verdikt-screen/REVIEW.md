# Ревью · 02-verdikt-screen

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 4

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:641-690` — результат синхронизации применяется без блокировки заказа, номера поколения запроса или проверки актуальности кода. Если запрос A начался раньше, запрос B позже получил от WB отказ и сохранил его, а затем A вернулся с прежним положительным ответом, строки 662–690 перезапишут новый отказ старым `filled` и снова выставят `metadata_delivery_allowed = true`. Это прямо ломает назначенный S-03-TC-016: оператор увидит ложное «WB: принято», причина отказа потеряется, а серверный гейт сможет разрешить сдачу по устаревшему ответу. Цена — возврат исходного дефекта на реальной поставке и возможная попытка передачи заказа, который WB уже отклонил.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:330-343,389-393` — ошибка фонового обновления намеренно проглатывается при `load(true)`: не ставится `error`, не инвалидируется старый положительный `workspace`, и `deliveryBlocker` продолжает считать кнопку разрешённой. Сценарий: вкладка на этапе сдачи показывает ранее принятые коды, следующий 15-секундный запрос падает после изменения вердикта на сервере, но оператор по-прежнему видит активное «Передать в WB» без `Alert` и без «Нет ответа WB». Это нарушает K8 и S-03-TC-018; сервер может остановить финальный запрос, но оператор снова узнает о проблеме только в конце операции. Цена — опасный ложный допуск на экране и потеря раннего предупреждения, ради которого существует карточка.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:386-391` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/utils/metaStatus.ts:30-37,76-83` — состояние ожидания реализовано с тоном `stop`, а все блокирующие состояния без причины получают одинаковую подсказку «Сдача пока недоступна». Контракт требует для «WB: проверяет» нейтральный тон и объяснение «WB ещё не подтвердил код», для отсутствия ответа — «Ждём ответа Wildberries», а для требуемого кода — видимое «Пришлите ЧЗ». При `pending` или `required` оператор получает красный стоп-сигнал без указания, ждать ему или сканировать код; существующий unit-тест закрепляет это расхождение и остаётся зелёным. Цена — неверная срочность статуса и отсутствие конкретного следующего действия на заблокированной поставке.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:259-301` — регрессия, заявленная как доказательство нейтральной строки, не проходит сам сценарий S-03-TC-014. Хелпер открывает workspace со `stage: 'boxes'`, тогда как проверяемые `fbs-kiz-row-*` рендерятся только на вкладке упаковки; тест не переходит туда. Кроме того, фикстура содержит принятый и отклонённый заказ вместо требуемых принятого, полностью распечатанного и активного на сканере, не проверяет тон чипа и информационный фон активной строки. S-03-TC-015–018 в исполняемых тестах вообще не упомянуты, поэтому две найденные выше регрессии остаются зелёными. Цена — заявленное e2e-доказательство не подтверждает фичу и не защищает её от возврата зелёной заливки или устаревшего допуска.

## Проверено и нормально

- Полностью сверены CONTRACT, FEATURES, RESHENIYA, CASES, карта волны и продуктовый diff переданных backend/frontend-файлов; стадийные артефакты и унаследованная инфраструктура не считались выходом за границы.
- Сервер и экран используют один `metadata.verdict`; неизвестная подпись и отсутствующий вердикт на основном GET-пути сваливаются в безопасное «Нет ответа WB», а сервер повторно проверяет гейт перед передачей.
- Правка границ строки соответствует атому: WB-вердикт больше не включает `success.light`/`success.main`; зелёным остаётся только текст хвоста ЧЗ, а активная строка сканера сохраняет информационные цвета.
- Локально прошли 58 тестов `test_fbs_marking.py`, `test_fbs_shipment_deliver_gate_unit.py`, `test_fbs_worklist_query_count.py` и 16 unit-тестов `fbsApi.test.ts`/`metaStatus.test.ts`; секреты, `.env`, живой WB и production не читались и не затрагивались.
