# Ревью · 02-verdikt-screen · проверка ремонта

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:663-686,743-747` — маркер старта WB-проверки записывается через новый `SessionLocal`, то есть второй транзакцией, которая может ждать блокировку строки, уже удерживаемую транзакцией вызывающего кода. Конкретный штатный сценарий: автополл вызывает `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_autopoll_service.py:261-272`; перед синхронизацией маркировки `sync_seller_order_statuses` выбирает все активные заказы с `FOR UPDATE` (`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/wb_marketplace_orders_service.py:1014-1021`) и держит эти блокировки до общего commit. Затем второй connection на строках 672-686 пытается обновить один из тех же заказов, а первый connection ждёт завершения этого обновления: получается взаимное ожидание до DB timeout. Цена — автополл статусов и маркировки зависает на первой собираемой поставке, не сохраняет свежие отказы WB и удерживает блокировки остальных заказов; интерфейс продолжает показывать устаревший вердикт. Новый тест проверяет только две чистые независимые сессии и этот штатный составной вызов не покрывает. Комментарий на строках 880-882 уже признаёт тот же deadlock для KIZ-транзакции, но исключение сделано только для одного вызывающего пути.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:429-521` — прежняя находка по `S-03-TC-018` не закрыта: тест закрывает рабочее место целиком, при повторном открытии заранее получает свежий отказ, ждёт disabled-кнопку и только затем принудительно кликает уже отключённый HTML-элемент. Скрытая вкладка со старым разрешением на экране не моделируется, а мок `/deliver` на строках 489-493 вообще принял бы передачу — он не повторяет серверный гейт. Сценарий поломки: первая браузерная вкладка остаётся открытой со старым `WB: принято`, во второй вкладке сервер сохраняет отказ, после возврата оператор немедленно нажимает ещё активную кнопку, а серверный гейт по регрессии пропускает запрос. Этот тест останется зелёным, потому что сам доводит интерфейс до свежего disabled-состояния и ни разу не отправляет `/deliver`. Цена — назначенный `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/tests/cases/S-03.md:313-329` всё ещё не доказан, и опасный обход из скрытой вкладки не защищён.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:524-587` — новый тест гонки фоновых refresh ошибочно помечен `S-03-TC-007`. Канонический `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/tests/cases/S-03.md:112-125` под этим номером проверяет один заказ с несколькими метаданными и приоритет блокирующего вердикта, а новый тест проверяет два HTTP-refresh и ошибку 503. Сценарий поломки: агрегация нескольких метаданных снова разрешает сдачу, но тест гонки refresh остаётся зелёным, хотя отчётность считает `S-03-TC-007` покрытым. Цена — нарушена обязательная трассировка «кейс → пользовательский тест» и у ремонта устаревшего refresh нет собственного корректного TC-ID.

## Проверено и нормально

- Предыдущий `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/night/volna-9-recovery/cards/02-verdikt-screen/REVIEW.md` использован как замороженный чек-лист; проверен только продуктовый diff после вердикта `1955c5bd`. Все пять затронутых продуктовых файлов входят в переданный список карточки, а стадийные артефакты ролей не считались выходом за границы.
- Прежние находки 2-5 закрыты по существу: заранее загруженные `order.markings` и `marking_code` сохраняются после `populate_existing`; успешная WB-фикстура снова проверяет `WB: принято`; номер поколения не даёт старому refresh снять более свежий fail-closed запрет; pending-сценарий ожидает нейтральный тон и правильную подсказку.
- В изолированном запуске из `/private/tmp`, где проектный `.env` не читается, прошли шесть целевых backend-тестов (`6 passed`). TypeScript и ESLint для двух ремонтных frontend-файлов также прошли; `git diff --check` чистый.
- Формат API, остатки, резерв и складские движения ремонтный diff не меняет. Новых операторских запретов не добавлено, поэтому отдельного изменения реестра блокировок для этого ремонта не требуется. Полный Playwright не запускался, чтобы не поднимать процессы, которые могут прочитать проектный `.env`; живой WB и production `194.87.96.144` не затрагивались.
