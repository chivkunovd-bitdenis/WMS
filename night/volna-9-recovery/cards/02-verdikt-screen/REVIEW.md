# Ревью · 02-verdikt-screen

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/utils/metaStatus.ts:4` — словарь не знает реальный и прямо зафиксированный в контрактных материалах код WB `uinBadStatus`. `reasonLabel` на строке 26 не находит `uinbadstatus` и возвращает исходное техническое значение, поэтому при исходном боевом сценарии `filled + reason=uinBadStatus` оператор увидит под чипом `WB не принял` строку `uinBadStatus`, а не обещанное `неверный статус УИН`. Цена — главное объяснение ранней блокировки остаётся непонятным оператору; `S-03-TC-003` нарушен. E2E в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-orders.spec.ts:198` подставляет вымышленный словарный код `invalid_code`, поэтому остаётся зелёным при поломке реального кейса.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:603` — новый единый вердикт правильно сохраняет `metadata_delivery_allowed=False` для `filled + reason`, но серверный workspace продолжает считать тот же заказ готовым в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_workspace_service.py:339`: после ложного сохранённого `True` он сразу выходит, а после нового `False` проваливается в запасную проверку и принимает `meta_details_json.status == accepted`, не проверяя `reason` или `decision`. Конкретно для `filled + reason=uinBadStatus` `_wb_order_verdict` возвращает блокировку, а `_metadata_ready` возвращает `True`; это воспроизведено локально. Цена — `progress.metadata_ready` завышается и `_compute_stage` на строках 375–384 может перевести поставку к коробам/сдаче, пока видимый вердикт WB запрещает продолжение; пункт 1 контракта о едином серверном источнике готовности не выполнен.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:143` — `isOrderMarkingReady` по-прежнему вычисляет готовность локально из `metadata.states` и считает `accepted` достаточным, хотя серверный `verdict.delivery_allowed` может быть `false` из-за непустого `reason`. В реальном ответе `filled + reason=uinBadStatus` статус маркировки остаётся `accepted`; если стикер уже открыт или применён, `orderPrintDone` на строках 1235–1239 помечает заказ завершённым и строка получает галочку на строке 1965 рядом с красным `WB не принял`. Цена — оператор одновременно видит признак выполненной работы и запрет WB, вопреки пунктам 1 и 8 контракта. E2E блокирующих состояний в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:255` передаёт пустой `states`, поэтому этот конфликт не проверяет.

## Проверено и нормально

- Полный продуктовый diff `d62f9af..8605931` прочитан и сверён с контрактом, решениями, кейсами, картой волны и экраном `S-03`; заявленные файлы реализации входят в переданные владельцем границы, а артефакты стадий находками по границам не считались.
- Все шесть находок предыдущего ревью перепроверены: API и workspace теперь получают `verdict`, `compute_delivery_allowed` делегирует единому правилу, отсутствующий optional-ответ блокирует, приоритет «нет ответа» исправлен, ложная `Готова к сдаче` убрана, открытый диалог повторно проверяет `deliveryBlocker`.
- Финальная серверная сдача и кнопка workspace одинаково запрещают `reason`, `pending`, `required` и неизвестный ответ; новая блокировка B-14 описана в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/docs/blockers/S-03.md` шестью обязательными полями и присутствует на обоих слоях.
- Целевые backend-тесты запущены локально: `43 passed`; `git diff --check` и компиляция изменённых Python-модулей прошли. TypeScript и Playwright не запускались: в рабочей копии нет соответствующих бинарников в `frontend/node_modules`.
