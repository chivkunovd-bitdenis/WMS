# Ревью · 02-verdikt-screen · повторная проверка ремонта

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:675-687,747-754` — ремонт исходной взаимной блокировки перенёс запись маркера в вызывающую транзакцию, но теперь её `UPDATE` остаётся незакоммиченным на всё время внешнего запроса WB и блокирует второй запуск синхронизации того же заказа ещё до его запроса к WB. Конкретный сценарий: первый `_sync_order_meta_from_wb` дошёл до медленного WB, затем фоновый опрос или действие оператора запускает более свежую проверку с отказом; второй вызов ждёт блокировку строки и не может получить свежий вердикт. Назначенный тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_marking.py:373-376` больше не достигает старта второго WB-запроса: при точечном запуске он завис более чем на 20 секунд, а после прерывания показал `sqlite3.OperationalError: database is locked` на строке 687. Цена — `S-03-TC-016` регрессировал, свежий отказ задерживается за старым медленным ответом, а backend-набор тестов фактически не проходит.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:513-528` — прежняя находка по `S-03-TC-018` не закрыта: после возврата вкладки тест сначала переводит часы на 15 секунд, ждёт второй `/workspace`, получает отказ и проверяет уже отключённую кнопку; только после этого делает принудительный клик. Конкретный сценарий из `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/tests/cases/S-03.md:321-329` — оператор возвращается и сразу нажимает ещё зелёную кнопку до refresh — не выполняется. Добавленный мок `/deliver` с `409` не помогает: disabled HTML-кнопка не отправляет запрос, поэтому счётчик остаётся нулём даже при сломанном серверном гейте. Цена — опасный обход со старым разрешением может вернуться, а пользовательский тест останется зелёным.

## Проверено и нормально

- Предыдущий вердикт из коммита `c062e2b5` использован как замороженный чек-лист; проверен только ремонтный diff `c062e2b5..7dd2bc8e`. Все три затронутых продуктовых файла входят в переданные границы карточки, а `tests/cases/` и `night/` учтены как стадийные артефакты, а не выход за границы.
- Исходная находка с отдельной `SessionLocal` в пути автополла закрыта по существу: маркер использует вызывающую сессию, и новый составной тест `test_fbs_autopoll_marking_sync_uses_status_transaction_for_wb_marker` прошёл (`1 passed`). Внесённая этим ремонтом конкурентная регрессия вынесена отдельной находкой выше.
- Ошибка трассировки закрыта: гонка двух refresh получила собственные кейс и тест `S-03-TC-019`; `S-03-TC-007` снова ссылается только на агрегацию нескольких метаданных одного заказа. `git diff --check c062e2b5..HEAD` прошёл без замечаний.
- Формат API, остатки, резервы и складские движения ремонтный diff не меняет. Новых операторских блокировок в нём нет; секреты, `.env`, кабинеты учётных данных, живой WB и production `194.87.96.144` не читались и не затрагивались.
