# Ревью · 02-verdikt-screen

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 6

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1275` — рабочее место поставки падает при чтении настоящего ответа API, а список заказов подменяет любой фактический вердикт текстом `Нет ответа WB`. Компонент без проверки читает `order.metadata.verdict.delivery_allowed`, хотя используемая обоими экранами схема `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/api/fbs_orders.py:104` не содержит `verdict`, а сборщик worklist-ответа `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_worklist_service.py:846` его не формирует. При открытии любой живой поставки React вычисляет `undefined.delivery_allowed` и прекращает рендер до строки и причины; на списке `metaStatusView(undefined)` безопасно, но неверно показывает отсутствие ответа даже при сохранённом решении WB. Цена — обе основные UI-зоны карточки не работают с реальным серверным контрактом. Новые E2E в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:220` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-orders.spec.ts:189` вручную добавляют отсутствующее поле в заглушку, поэтому остаются зелёными при поломке настоящего API.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:552` — главный отрицательный ответ WB `filled + reason` превращается в разрешающий `WB: принято`. Код пытается взять `detail.reason`, но фактический `MarketplaceMetaDetail` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/wildberries_fbs_client.py:34` такого поля не имеет, а парсер на строке 167 отбрасывает входной `reason`; локальный репро с `{'decision': 'filled', 'reason': 'uinBadStatus'}` вернул объект без `reason`. Затем `filled` становится `accepted`, `_wb_order_verdict` не видит причину и разрешает передачу. Цена — воспроизводится исходный боевой дефект карточки: оператор снова видит положительный вердикт по коду, который WB отклонил. Тесты подставляют `reason` прямо в искусственный marking и потому не проверяют реальный путь парсинга ответа WB.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:311` — заказ, для которого WB не запросил ни обязательных, ни необязательных метаданных (`required_meta_json=[]`, `optional_meta_json=[]`), получает блокирующий агрегированный вердикт `delivery_allowed=false`. Фронт на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1275` запретит передачу обычной немаркируемой поставки, тогда как серверный гейт `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_shipment_service.py:500` для такого заказа проверку не запускает и прямой запрос пропускает. Цена — новый самодельный запрет только на экране и расхождение двух слоёв; оно уже корректно описано в B-14 реестра блокировок.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:139` — `pending` и `assigned` по-прежнему входят в `MARKING_ACCEPTED_STATUSES`, хотя контракт прямо требует убрать их из готовых состояний. Когда стикер уже применён, а WB ещё проверяет код либо код лишь назначен, `orderPrintDone` на строке 1238 считает заказ завершённым: счётчик на строке 1778 включает его в напечатанные, а печать на строке 1791 переводится в режим повторной. Цена — рядом с красным `WB: проверяет` интерфейс продолжает показывать локально завершённую маркировку, сохраняя ложноположительный сигнал, ради устранения которого заведена карточка.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:332` — агрегация нарушает пункт 6 контракта и не различает обязательное и необязательное отсутствующее значение. Если WB запросил только optional-метаданную, но marking ещё нет, общий `missing` выдаёт `WB: нужен код`, хотя необязательный код не требуется; если одно требование `pending`, а у второго отсутствует либо неизвестен `decision`, проверка `pending` на строке 339 срабатывает раньше отсутствующего ответа на строке 346, хотя контракт задаёт обратный приоритет. Цена — оператор получает неверную следующую работу: вводить необязательный код или ждать проверку вместо диагностики отсутствующего ответа.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1140` — на рабочей вкладке `В работе` поставка со статусом `packed` безусловно показывает `Готова к сдаче` по локальному статусу, даже если внутри есть заказ с `WB не принял`, `WB: проверяет` или без ответа. Это прямо запрещено пунктом 8 контракта и повторяет исходный оптимистичный сигнал до входа в workspace. Цена — оператор по-прежнему узнаёт о блокировке только после открытия поставки. E2E `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-orders.spec.ts:205` проверяет новые чипы только на вкладке `Просрочены`, а рабочую строку поставки с отрицательным заказом не покрывает.

## Проверено и нормально

- Полный продуктовый diff от `d62f9af` сверён с `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/night/volna-9-recovery/cards/02-verdikt-screen/CONTRACT.md`, кейсами, решениями, картой волны и `S-03` в реестре экранов; заявленные файлы реализации входят в переданный список, стадийные артефакты находками не считались.
- Маппинг одиночных `filled`, `optional`, `notRequired`, `pending`, `required` и неизвестного решения внутри нового агрегатора соответствует фиксированному словарю, если данные уже вручную представлены в ожидаемом внутреннем формате; непустой сохранённый `reason` имеет высший приоритет.
- Серверный delivery-check связывает отказ с UUID конкретного заказа и не доверяет одному старому `metadata_delivery_allowed`; прямой запрос с известным блокирующим вердиктом останавливается.
- `ruff` по пяти целевым backend-файлам прошёл; целевые `pytest` прошли, 37 тестов. Frontend-проверки не запускались: в рабочей копии отсутствуют локальные `tsc` и Playwright.
