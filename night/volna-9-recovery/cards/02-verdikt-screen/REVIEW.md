# Ревью · 02-verdikt-screen

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 6

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/fbsApi.ts:285` — единый серверный вердикт не доезжает ни в список заказов, ни в workspace: реальный сборщик ответа `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_worklist_service.py:846` по-прежнему возвращает `metadata` без `verdict`, а клиент лишь подменяет отсутствие поля константой `NO_WB_VERDICT`. Поэтому при открытии реальных данных любой сохранённый `WB: принято`, `WB не принял` или `WB: проверяет` отображается одинаково как `Нет ответа WB`. После первой изменяющей операции ситуация хуже: функции вроде `createFbsSupplyFromOrders` и `addFbsOrdersToSupply` на строках 599 и 628 возвращают ненормализованный workspace, `setWorkspace(next)` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:429` сохраняет его, а строка 1270 читает `undefined.delivery_allowed` и роняет React-рендер. Цена — основной экран либо скрывает настоящий ответ WB, либо падает после штатного действия. E2E на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts:220` вручную добавляет отсутствующее поле в заглушку и остаётся зелёным при этой поломке реального API.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:385` — старый `compute_delivery_allowed` остался вторым, более оптимистичным правилом и не учитывает ни `reason`, ни WB `decision`: `filled + reason=uinBadStatus` имеет внутренний статус `accepted` и возвращает `True`. На строке 623 это значение сохраняется в `order.metadata_delivery_allowed`; его затем читают worklist и расчёт прогресса workspace, тогда как новая серверная сдача на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_shipment_service.py:500` пересчитывает `_wb_order_verdict` и запрещает тот же заказ. Цена — воспроизводится исходное расхождение: список и совместимые старые клиенты получают «можно», а финальная ручка отвечает отказом; добавление нового поля API не сохраняет обратную совместимость существующего `metadata.delivery_allowed`.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:366` — отсутствие ответа по запрошенной optional-метаданной трактуется как подтверждённое `WB: код не требуется` и открывает сдачу. Сценарий: в `optional_meta_json` есть `imei`, но WB ещё не вернул `metaDetails` либо ответ был пустым; marking отсутствует, `decision` равен `None`, и ветка на строках 366–373 возвращает `delivery_allowed=True`. Это противоречит пункту 6 контракта, где положительный итог допустим только при явном `decision` из `{filled, optional, notRequired}`, а отсутствующий ответ блокирует. Цена — сервер может передать заказ до получения внешнего вердикта. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_marking.py:515` закрепляет ошибочное поведение и будет зелёным при нарушенном контракте.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:345` — при нескольких метаданных нарушен заданный приоритет агрегирования: `pending` проверяется раньше отсутствующего или неизвестного ответа. Сценарий: SGTIN уже имеет `decision=pending`, а обязательный IMEI имеет marking со статусом `assigned`, но без `decision`; агрегатор показывает `WB: проверяет`, хотя по пункту 6 контракта должен показать более приоритетное `Нет ответа WB`. Цена — оператор ждёт проверку не того кода вместо диагностики пропавшего ответа и не понимает, какое действие действительно разблокирует сдачу; параметрические тесты проверяют состояния только по одному и этот конфликт не ловят.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1140` — строка поставки со статусом `packed` безусловно показывает `Готова к сдаче`, не имея агрегированного вердикта её заказов. Сценарий: поставка упакована, после чего WB возвращает по одному заказу `filled + reason`; в списке «В работе» остаётся зелёный по смыслу текст готовности, а блокировка обнаруживается лишь после открытия workspace. Цена — пункт 8 контракта не выполнен и исходный поздний ложноположительный сигнал остаётся на основном списке; E2E проверяет вердикты только на заказных строках вкладки «Просрочены», а такую поставку не покрывает.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:2419` — подтверждающая кнопка в уже открытом диалоге не читает `deliveryBlocker`. Сценарий: оператор открывает подтверждение при проходных данных, затем 15-секундный опрос workspace приносит свежий `WB не принял`; основная кнопка за диалогом становится недоступной, но кнопка `Передать в WB` внутри диалога остаётся активной и отправляет запрос. Сервер его остановит, однако оператор упрётся в позднюю ошибку вместо обещанной ранней блокировки. Цена — новый UI-гейт обходится штатным браузерным действием, слои расходятся в момент обновления, а тесты не моделируют смену вердикта при открытом подтверждении.

## Проверено и нормально

- Полный продуктовый diff `d62f9af..HEAD` сверён с `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/night/volna-9-recovery/cards/02-verdikt-screen/CONTRACT.md`, кейсами, решениями, картой волны и экраном `S-03` в `frontend/screens.registry.json`; заявленные файлы реализации входят в переданные владельцем границы, а стадийные артефакты находками не считались.
- Парсер реального ответа WB теперь сохраняет `reason`, а `filled + reason` корректно получает блокирующий новый `_wb_order_verdict`; `pending` и `assigned` убраны из локально принятых статусов workspace.
- Серверная финальная проверка заново вычисляет вердикт по каждому заказу, связывает отказ с конкретным UUID и не разрешает известные `reason`, `pending`, `required` и неизвестный `decision`.
- Целевые backend-тесты запущены локально: `41 passed`. Frontend-тесты не запускались, потому что в рабочей копии отсутствует `frontend/node_modules`; их заглушки и трассировка прочитаны целиком по diff.
