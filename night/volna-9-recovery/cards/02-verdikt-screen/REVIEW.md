# Ревью · 02-verdikt-screen · повторная проверка ремонта

Вердикт: **CHANGES_REQUESTED**

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py:693-695` — ремонт убрал `commit()` из транзакции сдачи, но для всех остальных вызовов по умолчанию снова открывает отдельную `SessionLocal`. Штатный автополл вызывает синхронизацию маркировки без переопределения этого режима (`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_autopoll_service.py:250`) после того, как та же вызывающая сессия выбрала активные заказы с `FOR UPDATE` (`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/wb_marketplace_orders_service.py:1014-1021`). Конкретный сценарий: цикл статусов удерживает блокировку заказа, отдельная сессия на строке 694 пытается обновить этот же заказ, а первая сессия ждёт возврата `_record_wb_sync_started`; в рабочей БД это взаимное ожидание до таймаута, а защитный тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_kiz.py:2695-2843` уже падает на запрете конкурирующей сессии. Цена — автополл не сохраняет свежий отказ WB и удерживает блокировки остальных заказов; общий backend-набор тестов красный. Это регрессия, внесённая текущим ремонтом ранее принятого пути автополла.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_shipment_warehouse_sc.py:377-389` — новый конкурентный тест не устанавливает, что второй запрос действительно дошёл до защищаемой границы транзакции, а заменяет это фиксированным `sleep(0.1)`. Конкретный сценарий: на медленном CI второй task за 100 мс ещё не дошёл до `_get_supply_for_update`; тест освобождает первый запрос, тот успевает перевести поставку в `in_delivery`, после чего второй закономерно получает `supply_bad_status`. Все проверки строк 386-410 проходят даже при возвращённом внутреннем `commit()`, потому что два запроса фактически не пересеклись в опасной точке. Цена — тест, добавленный именно против двойной передачи в WB, может остаться зелёным при сломанной блокировке и не защищает ремонт от повторной регрессии.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист; проверен ремонтный продуктовый diff `65779114..35a4982e`. Изменения реализации ограничены разрешёнными файлами `fbs_marking_service.py`, `fbs_shipment_service.py` и `test_fbs_shipment_warehouse_sc.py`; `night/` и `docs/blockers/` учтены как стадийные артефакты.
- Исходная находка с преждевременным завершением транзакции сдачи закрыта в самом маршруте `deliver`: передача выставляет `persist_started_marker_outside_caller=False`, маркер делает только `flush()`, а блокировка поставки сохраняется до внешней операции. Новый конкурентный сценарий прошёл, оба варианта `S-03-TC-016` прошли (`3 passed`).
- Полностью прошёл изменённый файл `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_shipment_warehouse_sc.py` (`12 passed`), `ruff` для трёх изменённых продуктовых файлов также прошёл. Отдельно проверенный автополл-тест дал `1 failed` по находке 1.
- Формат API, остатки, резервы и складские движения ремонтный diff не меняет. Новых экранных блокировок он не вводит; ранее добавленная B-15 описана шестью полями и имеет серверный гейт. Секреты, `.env`, кабинеты учётных данных, живой Wildberries и production `194.87.96.144` не читались и не затрагивались.
