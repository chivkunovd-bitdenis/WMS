ФИЧ: 6

## Фичи

### 1. Более поздняя проверка WB побеждает при любом порядке ответов

Что меняется для оператора: если две проверки маркировки одного заказа идут одновременно, на экране и в гейте сдачи остаётся результат той проверки, которую начали позже. Запоздавший успешный ответ не может вернуть «WB: принято» поверх более нового отказа.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_marking.py`

Зависимости: нет. Это серверная защита данных, не меняет контракт API.

Как проверить: целевой async-тест удерживает ответ раннего запроса A, запускает более новый B, даёт A записать `filled` без причины, затем возвращает B с отказом. После завершения обоих запросов сохранены причина отказа и `metadata_delivery_allowed = false`.

### 2. Замена ЧЗ не теряет загруженные связи после блокировки строки

Что меняется для оператора: подтверждённая замена уже привязанного ЧЗ после ответа WB завершается штатно — прежний код погашается, а не возникает 500 из-за async-загрузки связи.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_kiz.py`

Зависимости: фича 1 должна быть завершена первой, так как обе меняют один серверный сервис; смысловой зависимости нет.

Как проверить: проходят три уже назначенных регрессии `test_fbs_kiz_commit_confirmed_replaces_old_kiz_and_voids_code`, `test_fbs_kiz_pool_to_external_replacement_does_not_double_count_unit` и `test_fbs_marking_readers_prefer_active_row_over_newer_rejected_row`; ни одна не получает `MissingGreenlet`, прежний код и счётчик единиц остаются в ожидаемом состоянии.

### 3. Фикстура успешного ответа WB проверяется как успех

Что меняется для оператора: нет нового поведения; тест закрепляет уже работающий контракт — явный ответ WB `accepted` показывает «WB: принято» и разрешает сдачу, а не имитирует отсутствие ответа.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_kiz.py`

Зависимости: нет. Фича может выполняться параллельно с фичей 1, но до фичи 2 не запускать полный затронутый файл как доказательство зелёного набора: в нём находятся регрессии фичи 2.

Как проверить: целевой тест с `_patch_wb_acceptance` проходит и утверждает `delivery_allowed = true` и подпись `WB: принято`; ожидание `Нет ответа WB` в этом сценарии отсутствует.

### 4. Устаревший ответ workspace не отменяет свежий fail-closed запрет

Что меняется для оператора: когда фоновая загрузка A запущена раньше, свежая загрузка B завершилась ошибкой, а A вернулась позже, экран сохраняет сообщение об ошибке, показывает «Нет ответа WB» и не активирует «Передать в WB».

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx`
- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts`

Зависимости: нет. Серверный гейт остаётся окончательной защитой, но экран должен fail-closed до нажатия кнопки.

Как проверить: Playwright-сценарий искусственно задерживает успешный refresh A, запускает более поздний refresh B с ошибкой и только потом отпускает A. На экране остаются складской Alert без HTTP-кода, `Нет ответа WB`, «Сдача пока недоступна» и неактивная кнопка передачи.

### 5. E2E ожидания для состояния «WB: проверяет» соответствуют словарю вердикта

Что меняется для оператора: нет нового поведения; браузерная проверка фиксирует нейтральный статус ожидания и человеческую причину блокировки, которые уже задаёт контракт.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts`

Зависимости: фича 4 завершена первой, поскольку обе редактируют один e2e-файл.

Как проверить: сценарий `S-03-TC-004` передаёт `WB: проверяет` с тоном `neutral`, видит этот чип, неактивную кнопку и tooltip «WB ещё не подтвердил код»; он не ожидает stop-тон и текст «Сдача пока недоступна» для pending-состояния.

### 6. E2E доказывает запрет передачи после возврата на скрытую вкладку

Что меняется для оператора: тест защищает опасный пользовательский путь — отказ WB, полученный пока вкладка рабочего места скрыта, сохраняется при возвращении и не позволяет отправить поставку.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/frontend/tests-e2e/ff-fbs-supply.spec.ts`

Зависимости: фича 5 завершена первой, поскольку обе редактируют один e2e-файл.

Как проверить: сценарий `S-03-TC-018` открывает поставку с разрешением, скрывает/закрывает рабочее место, получает и сохраняет серверный отказ, возвращается, сразу пытается передать поставку и подтверждает отсутствие запроса передачи и статуса «В доставке»; до попытки видны `WB не принял` и человеческая причина.

## Порядок

1. Выполнить фичу 1, затем фичу 2: они обе исправляют `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/app/services/fbs_marking_service.py`, поэтому последовательность исключает конфликт и сначала закрепляет порядок серверных ответов.
2. Фичу 3 можно сделать параллельно с фичей 1: она меняет только тестовую фикстуру. Полный затронутый backend-набор подтверждать после фичи 2.
3. Фичу 4 можно начинать параллельно с backend-цепочкой: она самостоятельна и работает только во frontend.
4. После фичи 4 последовательно выполнить фичи 5 и 6: все три правят один e2e-файл, а последние две дополняют уже выровненную проверку экрана.

## Что осталось за бортом

- В `REVIEW.md` нет незакрытых продуктовых находок вне шести перечисленных; расширение контрактных K1–K8, новые API, экраны и ui-kit в это перепланирование не входят.
- Секреты, ключи, токены, `.env`, кабинеты учётных данных, живой Wildberries и production `194.87.96.144` не читались и не затрагивались.
- Git-коммит артефакта не создан: среда не разрешила создать `/Users/deniscivkunov/Projects/WMS/.git/worktrees/lane-2-02-verdikt-screen1/index.lock`. `FEATURES.md` существует как локальное изменение рабочей копии без восстанавливаемого SHA.
