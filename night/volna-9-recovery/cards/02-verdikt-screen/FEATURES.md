ФИЧ: 1

## Фичи

### 1. Сделать конкурентный регрессионный барьер сдачи доказательным

Что меняется для оператора: если один сотрудник уже передаёт поставку в Wildberries, второй одновременный запрос не повторит внешнюю передачу. Он останется заблокирован до завершения первого запроса, после чего получит штатный ответ `supply_bad_status`.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-02-verdikt-screen/backend/tests/test_fbs_shipment_warehouse_sc.py`

Зависимости: нет. Экранный вердикт, автополл и производственная логика сдачи уже приняты повторным ревью и не меняются; этот атом исправляет только оставшуюся находку о недоказанной конкурентной границе.

Как проверить: переписать синхронизацию сценария `test_fbs_shipment_second_deliver_does_not_reach_wb_while_first_holds_supply_lock`, чтобы она наблюдала не только вход второго запроса в `before_cursor_execute`, но и отсутствие его завершения до освобождения первого вызова Wildberries (через `after_cursor_execute` либо другой управляемый барьер, работающий с SQLite). До освобождения первого запроса тест должен зафиксировать, что второй запрос не достиг `deliver_marketplace_supply`; после освобождения — ровно один вызов Wildberries, `200` у первого запроса, `400 supply_bad_status` у второго и одну подтверждённую операцию `supply_deliver`. Негативная проверка обязательна: временный возврат преждевременного `await session.commit()` при записи стартового маркера синхронизации должен делать сценарий красным, а не оставлять его зелёным.

## Порядок

1. Выполнить фичу 1. Других незакрытых находок в повторном `REVIEW.md` нет; параллельных работ в этой карточке нет.

## Что осталось за бортом

- Принятые изменения экранного вердикта, серверного гейта и автополла не входят в перепланирование: повторное ревью оставило только дефект доказательности конкурентного теста.
- Если доказательство настоящей SQL-блокировки требует отдельного PostgreSQL-интеграционного контура, а такого контура нет в контракте, его создание остаётся за рамками этой карточки; атом обязан получить эквивалентный управляемый барьер в существующем SQLite-сценарии.
- Секреты, ключи, токены, `.env`, кабинеты учётных данных, живой Wildberries и production `194.87.96.144` при разбиении не читались и не затрагивались.
