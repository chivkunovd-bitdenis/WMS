ВЕРДИКТ: НАХОДКИ 10

# Code Review · 07-reporting · повторный проход

Вердикт: **CHANGES_REQUESTED**.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/App.tsx:3077` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/apps/seller/SellerApp.tsx:477` — в отчёт передаётся весь общий список `/warehouses`, в котором нет признака `is_operational` и который включает служебные склады. Сценарий: у tenant есть один операционный склад и `FBS WB Архив`; экран вместо скрытого единственного физического склада покажет оба и даст выбрать служебный, после чего backend вернёт пустой срез; цена — нарушены `ARCH-CROSS` и `S-33-TC-003/014`, оператору открыта заведомо ложная область отчёта.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:117` — московская календарная граница всё ещё уходит на API как timestamp без offset (`T00:00:00`), а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:53` сравнивает его с `created_at` без нормализации в МСК. Сценарий: движение 1 августа в 01:30 МСК имеет UTC-время 31 июля 22:30 и при UTC-сессии БД не попадёт в срез «1 августа», хотя дневная группировка позже научилась показывать Москву; цена — сводка, таблица и CSV теряют факты на обеих границах дня. Новый test этого не ловит: он передаёт API уже готовые UTC-границы.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:80` — декабрь переключает месяц на январь, но не увеличивает год. Сценарий: 15 декабря 2026 года пресет «Текущий месяц» вычислит `date_from=2026-12-01`, `date_to=2025-12-31`, покажет ошибку обратного периода и не загрузит отчёт; цена — весь декабрь базовый сценарий `S-33-TC-001` неработоспособен.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:497` — после ремонта пустого графика `daily` содержит только дни, в которых было движение текущего или прошлого периода; нулевые календарные дни внутри непустого периода исчезают. Сценарий: расход был 1 и 31 августа, между ними 29 дней без движений; `MovementFlowChart` получит две соседние точки и изобразит непрерывный поток вместо двух разовых операций; цена — дневная динамика и наложение прошлого периода искажены.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:19` и `:224` — экран по-прежнему объявляет `warnings` как `string[]` и рисует каждый warning как React-текст, тогда как новый backend в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:524` возвращает массив объектов. Сценарий: в срезе есть legacy-строка или устаревший WB-источник; React получает `{code, ...}` как `children` и падает с «Objects are not valid as a React child» вместо предупреждения; цена — весь `S-33` недоступен ровно на тех данных, для которых добавлена диагностика.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:507` — `source_freshness` вычисляется из `FbsWarehouseBinding.last_sync_at`, но это время **исходящей публикации остатков WMS → WB**, а не свежесть входящего импорта, причём `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/fbs_stock_sync_service.py:595` обновляет его даже при ошибке. Сценарий: импорт WB-данных не работает сутки, но пять минут назад попытка отправить остатки упала; API вернёт `is_stale=false`; цена — неполные внешние данные выдаются за свежие, прямо вопреки контракту.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:29` и `:239` — в типе и таблице всё ещё нет возвращаемого backend-ом `integrity_error`; над таблицей нет ошибки, а в строке нет `StatusChip` «Ошибка». Сценарий: для выбранного склада записана только `stock_transfer_out`; API вернёт `integrity_error=true`, но оператор увидит обычный расход без единого предупреждения; цена — неполная пара маскируется под штатную операцию; не закрыты замороженный пункт 8 прошлого review и `S-33-TC-013`.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:226` — «Повторить» после независимого сбоя сводки вызывает общий `load()`, который на `:151` очищает успешно загруженную таблицу и заново запрашивает её. Сценарий: overview упал, таблица на второй странице доступна; нажатие retry стирает строки, сбрасывает страницу на 1 и может превратить частичный сбой в полный, если повторно упадёт табличный API; цена — нарушены контракт частичного отказа и `S-33-TC-012`.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:171` — проверка целостности пары сверяет товар, селлера, склады, знак и количество, но не требует ровно один `stock_transfer_out` и один `stock_transfer_in`, хотя `movement_type` уже загружен. Сценарий: из-за ошибки writer две стороны имеют тип `stock_transfer_out`, но противоположные знаки и равные модули; сервис признает пару полной и не поднимет `integrity_error`; цена — поврежденная история маскируется как целостная и не даёт оператору сигнал для разбора.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:197` — группировка переводит в человекопонятный вид только transfer-типы, а остальные возвращает как внутренние коды. Сценарий: оператор выбирает «По операциям» после приёмки и видит `inbound_intake` вместо «Приёмка»; цена — один из двух главных разрезов не соответствует контракту. Новый `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/tests/test_reports_inventory.py:167` закрепляет неверный `inbound_intake`, а Playwright в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/tests-e2e/ff-reports.spec.ts:74` подменяет реальный API моком с уже готовой «Приёмкой», поэтому оба тестовых слоя зелёны при сломанном поведении.

## Проверено и нормально

- Проверен каждый из 14 пунктов предыдущего `REVIEW.md` и только ремонтный diff после него. Закрыты передача складов, исключающая верхняя граница дня, legacy-маркировка backfill, нулевая база сравнения, предыдущая серия, пустой график, колонки CSV, серверная пагинация и потоковая выдача.
- Область seller принудительно задаётся на сервере для overview, таблицы и CSV; внутренние transfer-стороны исключены из общего среза, а при фильтре одного склада возвращаются как отдельный поток.
- Целевые backend-тесты пройдены: `14 passed in 11.59s`; `ruff` и `mypy` для изменённых reporting-файлов зелёны, `git diff --check` зелёный.
- Новый `docs/blockers/S-33.md` описывает оба запрета шестью полями, и оба правила дублируются на backend. Frontend build/unit/e2e в этой сессии не запущены: в рабочей копии нет локальных `tsc`/`vitest`/Playwright.

Секреты, ключи, токены, `.env` и кабинеты учётных данных не читались.
