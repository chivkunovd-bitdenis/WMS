ВЕРДИКТ: НАХОДКИ 14

# Code Review · 07-reporting

Вердикт: **CHANGES_REQUESTED**.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/alembic/versions/20260822_0094_inventory_movement_reporting_dimensions.py:61` — PostgreSQL не разрешает ссылаться на целевую таблицу `movement` внутри `ON` вложенного `JOIN` в секции `FROM` этого `UPDATE`; на реальной базе миграция остановится с `invalid reference to FROM-clause entry`, поэтому новые обязательные колонки не появятся и релиз приложения поверх старой схемы станет невозможен. Текстовый тест миграции этого не обнаруживает, потому что SQL не исполняет.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:40` — товарный отчёт ограничивает селлера через текущий `Product.seller_id`, хотя 07-A специально закрепляет неизменяемый `InventoryMovement.seller_id`. Если товар после движения переназначили с селлера А на селлера Б, история исчезнет из отчёта А и попадёт в отчёт Б; цена — искажение истории и раскрытие другому селлеру чужих количеств.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/api/reports.py:27` — все три ручки не принимают `seller_id` ФФ-фильтра, а зависимость `seller_line_product_scope` возвращает значение только для пользователя портала селлера. Когда администратор ФФ выбирает селлера, браузер отправляет параметр, FastAPI молча его игнорирует, и таблица, сводка и CSV продолжают показывать весь tenant; цена — оператор принимает общие числа за числа выбранного клиента.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:145` — `build_overview` не принимает ни `warehouse_id`, ни `search`, хотя контракт требует единый срез для показателей, графика, таблицы и CSV. При поиске товара таблица и CSV сужаются, а верхние цифры и график остаются по всем товарам; складской фильтр на клиенте вообще не реализован. Цена — один экран одновременно показывает несовместимые итоги.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:154` — верхняя сводка не исключает служебные склады ни из движений, ни из текущего остатка: у `movement_filter` нет соединения со складом, а `balance_stmt` соединяет `Warehouse`, но не добавляет даже применённое ниже legacy-условие по имени. При наличии `FBS WB *` верхние показатели завышены, тогда как таблица на строке 39 эти данные скрывает; это прямо нарушает обязательное решение `ARCH-CROSS` об операционных складах.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:68` — проверка полноты transfer-пары выполняется уже с фильтром выбранного склада. У корректной пары исходящая и входящая стороны находятся на разных складах, поэтому в каждом отдельном складском срезе запрос увидит одну строку и пометит любую исправную пару как неполную; цена — ложное сообщение об ошибке целостности в штатной операции.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:29` — клиент ожидает поля `sku_code`, `product_name`, `wb_vendor_code`, `wb_barcode`, `total_in` и `total_out`, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:80` возвращает `sku`, `name`, `vendor_code`, `barcode`, `in_qty` и `out_qty`. Приведение JSON к типу скрывает ошибку от TypeScript: при первой непустой выборке названия и SKU будут пустыми, количества — `undefined`, а «Остаток сейчас» всегда станет фиктивным нулём. Цена — основная таблица непригодна для работы.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/App.tsx:3071` — подключён только маршрут ФФ `/app/ff/reports`; заявленный `/app/seller/reports` отсутствует в отдельном `SellerApp`, хотя `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/screens.registry.json:2663` объявляет его существующим. Селлер, открыв пункт или прямую ссылку, попадёт под wildcard-переадресацию на другой раздел; цена — половина целевой аудитории не имеет доступа к фиче.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:146` — сравнение периода не работает по контракту: процент передаётся в общий `delta`, который `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/ui-kit/ReportMetricStrip.tsx:27` форматирует как штуки, а серия на строке 161 не содержит `previousOutbound`, из-за чего график никогда не показывает прошлый расход. Для разницы `+2` и роста `100%` экран покажет `+2 шт.` и `+100 шт.`, без пунктирной линии; цена — неверная управленческая интерпретация динамики.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:134` — вариант «Текущий год» есть в меню, но `choosePeriod` для `year` ничего не меняет. После выбора над таблицей написано «Текущий год», а запрос остаётся за прежний месяц или 7/30 дней; цена — отчёт уверенно подписывает неверный период.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx:155` — обратные даты и интервал длиннее 366 дней не проверяются у полей: `useEffect` отправляет запрос, сервер блокирует его, а прежние показатели и строки остаются на экране рядом с ошибкой. Это новый серверный запрет без парного предупреждения на экране и без шестипольного `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/docs/blockers/S-33.md`; цена — оператор упирается в необъяснённую стену и может принять старый срез за новый.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/app/services/reporting_service.py:124` — CSV не повторяет видимую таблицу: в выгрузке отсутствуют «Товар»/SKU и «Остаток сейчас», хотя контракт и кейс `S-33-TC-005` требуют обе колонки; при этом клиентская таблица их рисует. Цена — выгруженный файл нельзя сверить с экраном и использовать как обещанный полный текущий срез.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/backend/tests/test_reports_inventory.py:31` — тест неизвестной группировки вызывает ручку без авторизации и проверяет только `401`, поэтому останется зелёным даже при полном удалении проверки `group_by`. Остальные API-тесты создают пустой tenant и не проверяют ни одной формулы, seller/tenant scope, transfer-пару, операционный склад, форму непустой строки или совпадение CSV с таблицей; именно поэтому перечисленные выше поломки проходят как `9 passed`.

14. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/screens.registry.json:2665` — заявленный экран `S-33` разрешает только `src/App.tsx` и `src/layouts/AuthedAppLayout.tsx`, но продуктовый diff меняет `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-1-07-reporting/frontend/src/screens/ff/FfReportsPage.tsx`. По заданному правилу реестра этот файл находится вне границ экрана; реестр нужно исправить до повторного dev-прохода, иначе guard и следующий исполнитель получают ложную карту владения.

## Проверено и нормально

- Полный продуктовый diff атомов 1–11 сверён с UX-контрактом, `FEATURES.md`, `CASES.md`, картой волны и обязательным порядком `ARCH-CROSS`; финансовые показатели и иные соседние домены в реализацию не протекли.
- Центральный writer движения записывает `seller_id` и `warehouse_id` вместе с движением, запрещает нулевую дельту и оставляет изменение баланса в той же транзакции; новых путей списания в минус эта карточка не добавляет.
- Backend-ручки требуют аутентификацию и существующее право чтения inventory; seller-пользователь на уровне API получает собственный scope, а внутренние transfer-пары исключены из верхнего внешнего потока по `transfer_group_id`.
- Целевые backend-тесты запущены: `9 passed in 4.96s`. Фронтенд-сборка не стартовала, потому что в рабочей копии отсутствует локальный `tsc`; секреты, ключи, токены, `.env` и кабинеты учётных данных не читались.
