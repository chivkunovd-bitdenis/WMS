# REVIEW · 06-picking-list-order · повторный проход

Вердикт: CHANGES_REQUESTED

ВЕРДИКТ: НАХОДКИ 4

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1223` — ремонт сортирует общий массив `packingOrders`, поэтому меняется не только предпросмотр полной ленты, но и порядок всех видимых строк упаковки и сканирования КИЗ в строке 1905. При товарах «Альфа» с артикулом `ZZZ` и «Яблоко» с артикулом `AAA` основной список раньше шёл по названию `Альфа → Яблоко`, а после ремонта идёт `Яблоко → Альфа`. Цена: атом 2 из `FEATURES.md` разрешает привести к серверному порядку существующий предпросмотр и запрос, но не разрешает перестраивать соседний рабочий список; `CONTRACT.md` и `OWNER-SCOPE.md` дополнительно требуют нулевую визуальную дельту FBS. Оператор получает незаявленное изменение порядка строк в основном сценарии упаковки и привязки Честного знака.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1225` — клиентский ключ не равен серверному ключу листа. Сервер в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/services/fbs_supply_service.py:145` начинает с `FbsOrder.wb_article`, а поле `product.seller_article` рабочего места формируется из `Product.wb_vendor_code` раньше `FbsOrder.wb_article`; размер рабочего места также может прийти из карточки WB, тогда как сервер берёт только `Product.wb_size`. Сценарий: у заказа A `wb_article = AAA`, но у связанного товара сохранён `wb_vendor_code = ZZZ`, у заказа B оба значения равны `BBB`. Лист и серверная полная лента идут `A → B`, а предпросмотр и тело запроса — `B → A`; сервер затем независимо пересортирует полный набор обратно. Цена: предыдущая находка о расхождении предпросмотра с физической лентой не закрыта для допустимого рассинхрона заказа и связанной карточки, оператор снова может наклеить QR не на тот товар.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/tests/test_fbs_packaging_integration.py:227` — проверка составного ключа меняет у второго заказа `product_id` на `None`, поэтому одновременно расходятся `sku_code`, `size` и `product_name`. Реализация, которая выбросит из ключа только `size`, останется зелёной: группы всё равно разделят разные SKU и названия; аналогично тест не доказывает обязательность каждого из остальных компонентов по отдельности. Цена: прежняя находка 3 закрыта для числового порядка `8 → 12 → 100`, но не закрыта для полного контракта `article → sku_code → size → product_name`; регрессия одного компонента ключа пройдёт CI и снова разведёт лист с лентой.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/tests-e2e/ff-fbs-supply.spec.ts:492` — новый e2e перехватывает `/order-print-tape` и возвращает заказы ровно в присланном frontend-порядке, хотя реальный сервер для полного набора сортирует их заново своим ключом. Данные теста вдобавок направляют `seller_article` и `sku` в одну сторону, поэтому расхождение полей из находки 2 не моделируется. Сценарий из находки 2 оставляет тест зелёным: он доказывает только внутреннюю согласованность frontend с собственным mock, но не совпадение предпросмотра с фактическим результатом сервера. Цена: пользовательский риск неправильного порядка физической ленты не защищён Playwright-проверкой.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист. Находка 1 закрыта безопасным `picking_list_group_sort_key`; находка 4 закрыта различимыми PNG и чтением фактического asset по `orderId`. Числовая часть находки 3 закрыта отдельным тестом, оставшаяся часть вынесена выше. Находка 5 снята явной границей актуального `FEATURES.md`: унаследованный двойной partial-error не менялся этим ремонтом.
- Ремонтный продуктовый diff после предыдущего вердикта затрагивает только пять файлов двух атомов из `FEATURES.md`; `FfFbsSupplyWorkspace.tsx` принадлежит `S-03` по `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/screens.registry.json`. Стадийные артефакты, унаследованные `AGENTS.md`, `scripts/stand/restore.sh` и `frontend/deploy/Caddyfile.seller.local` выходом за границы не считались. Нового маршрута, API-поля, ui-kit-компонента или блокировки оператора ремонт не добавляет.
- Серверная сортировка применяется только к полному множеству заказов; выборочный запрос сохраняет входной порядок. Формат ответа API, остатки, резервы, списания и записи во внешние кабинеты не затронуты.
- `ruff check`, `pytest backend/tests/test_fbs_packaging_integration.py` (`19 passed`), `npx tsc --noEmit -p tsconfig.app.json`, `npm run test:unit` (`138 passed`) и `git diff --check` прошли. Целевой Playwright не стартовал: sandbox запретил тестовому webServer bind на `127.0.0.1:18000` (`Errno 1`).
