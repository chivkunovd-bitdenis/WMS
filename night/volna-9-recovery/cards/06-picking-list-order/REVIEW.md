ВЕРДИКТ: НАХОДКИ 7

# Ревью · 06-picking-list-order

Вердикт: CHANGES_REQUESTED.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:85` — кнопка `Печать стикеров` вызывает `order-print-tape` с `include_order_qr: false`, а затем на `:94-95` печатает самодельный HTML только с `№ K` и текстом номера заказа. При поставке с Честным знаком сервер успеет выдать коды, пометить их напечатанными и привязать к WB, но фронт не выведет ни коды ЧЗ, ни PNG-стикеры WB. Цена — коды расходуются без физической этикетки, а оператор получает ленту, которую WB не примет; центральный контракт «неизменённый стикер WB → служебная этикетка WMS» не выполнен.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/fbsApi.ts:1043` — тип `FbsPickingItem` не содержит добавленные сервером `number_start`, `number_end` и `order_ids`, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:25` перенумеровывает строки на клиенте из `quantity`. При добавлении заказа во второй вкладке после открытия листа на `FfFbsPickList.tsx:80-86` для печати берётся свежий состав, но на экране остаются старые диапазоны. Цена — кейс `S-03-TC-008` сломан: оператор сверяет свежую ленту с устаревшим листом; общая серверная последовательность фактически не является источником истины для модалки.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:118` — служебная этикетка попадает в физическую печать только если `asset.order_number` не `null`, но серверная схема `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/api/fbs_supplies.py:190` и `map_print_asset` не возвращают ни `order_number`, ни `wb_order_id`. При обычной печати `order_sticker` предпросмотр на `FbsPrintPreviewDialog.tsx:200-202` покажет `Стикер WB №—` и `Служебная этикетка WMS № —`, а на принтер уйдёт только PNG WB. Цена — предпросмотр обещает несуществующую пару этикеток, а физическая лента не связана с листом. Кроме того, этот новый блок показывается и для соседней построчной QR-печати, что выходит за границу «только полная печать из листа».

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:92` — фронт проверяет только `missing` и `failed`, но игнорирует `shortage` и `order_errors` от `order-print-tape`. Если для ЧЗ-заказа не хватает кода, сервис вернёт `orders=[]`, `shortage>0`, но из-за `include_order_qr:false` оставит `missing=0` и `failed=0`; экран откроет пустое окно печати без объяснения. При ошибке одного заказа готовые заказы будут уплотнены через `map`, а номер проблемного заказа из `order_errors` вообще не будет показан. Цена — нарушены явные состояния «стикеры получены не все» и «пропуск остаётся видимым»; оператор не знает, какой заказ допечатать.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:109` — во время печати в `PrintAction` передаётся только `disabledReason`, но не `busy={printing}`. Поэтому реализованный в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/ui-kit/Actions.tsx:119-138` индикатор никогда не включается в целевой модалке. При медленном ответе WB оператор видит обычную кнопку без прогресса. Цена — состояние «Подготовка печати» и кейс `S-03-TC-007` выполнены не полностью, хотя закрытие и повторный запуск после рендера заблокированы.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:113` — при пустом результате поиска или фильтра добавлена отдельная плашка `Фильтр скрывает строки, но печать охватывает всю поставку`, хотя контракт прямо требует «рядом с фильтром отдельное предупреждение не добавляется». Цена — изменён утверждённый UX и добавлен незаказанный блок в единственную меняемую зону экрана.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.test.ts:5` — фронтовые тесты проверяют только клиентский хелпер диапазонов и ключи localStorage; они останутся зелёными, если кнопка спишет ЧЗ без печати, покажет старые диапазоны, проигнорирует `shortage` или не напечатает PNG WB. В `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/tests/test_fbs_supply_assembly.py:172` каноническая лента тоже проверена на частных функциях, а не через `order-print-tape`; обещанных в `FEATURES.md` Playwright-сценариев `S-03-TC-001…007` в diff нет. Цена — находки 1–6 не ловятся заявленным набором проверок.

## Проверено и нормально

- Все файлы реализации входят в явно переданную границу карточки; `night/`, `tests/cases/` и `docs/evidence/` рассматривались как стадийные артефакты, а не как выход за экран.
- Серверный `get_picking_list` стабильно сортирует товарные группы и заказы внутри них; диапазоны непрерывны, а tenant-фильтр при загрузке поставки сохранён.
- Построчная печать снова разрешена и сохраняет сквозной номер заказа; чужой `order_id` отвергается. Добавление полей в API аддитивно и не ломает старых клиентов.
- `backend/tests/test_fbs_supply_assembly.py` прошёл: `17 passed, 1 skipped`; `git diff --check` прошёл. Frontend-зависимости в этой копии не установлены, поэтому unit/build фронта не запущены.
