# Ревью · 06-picking-list-order

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 9

## Находки

1. **Критично — печать из листа подбора делает неоговорённую необратимую операцию с маркировкой.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:924` передаёт `layout_json: null`, после чего `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/services/fbs_order_tape_print_service.py:95` подставляет макет с блоком `cz`, а строки 212–260 выпускают/перепечатывают код маркировки и синхронизируют его с Wildberries. Затем `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:95` добавляет третью страницу с маркировкой после пары WB → WMS. Сценарий: в поставке есть товар с обязательным «Честным знаком», оператор нажимает обычную контрактную кнопку `Печать стикеров` в листе подбора — вместо ровно двух этикеток на заказ система расходует/привязывает код маркировки во внешней системе и печатает лишний лист. Цена — необратимое изменение данных маркировки и физическая лента, не совпадающая с листом подбора. Тест `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.test.ts:86` закрепляет именно лишнюю третью страницу, поэтому остаётся зелёным при нарушенном контракте.

2. **Высокая — изменено соседнее действие упаковки, хотя контракт разрешает только модалку листа подбора и результат печати из неё.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:959` теперь отправляет существующую кнопку `Печать всего` напрямую в новую ленту с `layout_json: null`; прежний стандартный конструктор печати с выбранным макетом, `allow_partial`, проверкой доступности маркировки и подтверждением большой партии больше не открывается. Сценарий: упаковщик заранее выбрал рабочий макет либо печатает более 100 этикеток и нажимает `Печать всего` в зоне упаковки — настройка игнорируется, а защитное подтверждение не появляется. Цена — неожиданная массовая печать с неверным составом и выход за границу карточки, прямо запрещённый строками 10–14 и 89 контракта.

3. **Высокая — неполную ленту можно физически напечатать, а её счётчики скрывают проблему.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:247` берёт `ready/missing/failed` только из `batch`, хотя для полной ленты `batch` равен `null`, поэтому показывает `Готово 0` и не показывает числа пропусков. На строках 307–312 печать блокируется только при загрузке или полном отсутствии preview. Сценарий: WB вернул один готовый PNG и ошибку для следующего заказа — оператор видит `ErrorNotice`, но кнопка остаётся активной и печатает укороченную ленту. Цена — часть заказов останется без пары, а оператор не получит честного итога по количеству. E2E-кейс `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/tests-e2e/ff-fbs-supply.spec.ts:389` проверяет текст ошибки, но не проверяет счётчики и запрет печати, поэтому дефект не ловит.

4. **Высокая — сервер проверяет только набор ID, но не тот канонический снимок, который видел оператор.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/services/fbs_order_tape_print_service.py:318` заново вычисляет порядок по изменяемым полям товара, а проверка на строках 340–346 сравнивает лишь длину и множество `order.id`. Сценарий: пока лист открыт, во второй вкладке меняют артикул, SKU, размер или название товара так, что группа переезжает в другое место; состав ID остаётся прежним, запрос проходит, но сервер выдаёт номера уже в новом порядке, отличном от показанного листа. Цена — служебный номер на физической этикетке сопоставляется не с той строкой подбора. `S-03-TC-008` покрывает только добавление/удаление ID и при таком изменении атрибутов остаётся зелёным.

5. **Высокая — общий предпросмотр печати глобально превращён в предпросмотр полной ленты и ломает соседние виды печати.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/api/fbs_supplies.py:260` добавляет `order_number` всем assets ручки `/print-assets`, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:94` по одному наличию этого номера допечатывает WMS-этикетку. Поэтому существующая строковая кнопка `QR` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1919` теперь печатает лишний лист с номером. Одновременно строка 308 общего диалога всегда называет главное действие `Печать стикеров заказов`, даже для QR короба, грузоместа и поставки. Сценарий: оператор печатает один QR заказа или QR поставки через старое действие — получает изменённый физический комплект и/или неверную подпись кнопки. Цена — регресс соседних операций S-03, которых в контракте менять нельзя.

6. **Высокая — обязательный PNG Wildberries уменьшается и может быть перепечатан на выбранном пользователем размере.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:207` берёт общий сохранённый размер этикетки, строки 215–218 добавляют поле 1 мм и вписывают изображение через `max-width/max-height`, а строки 263–267 оставляют в полной ленте выбор другого формата. Сценарий: даже при номинальном листе 40×58 полезная область становится 38×56; при сохранённом формате 60×80 тот же PNG масштабируется ещё иначе. Цена — прямое нарушение требования «не уменьшает» и риск отказа сканирования/приёмки WB. Физический E2E считает только число DOM-секций и не проверяет миллиметры или CSS.

7. **Средняя — два запроса листа могут перепутать поставки.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:54` запускает загрузку без отмены и без номера актуального запроса, а строка 57 безусловно записывает ответ. Сценарий: при открытом компоненте быстро меняется `supplyId` с A на B; ответ B приходит первым, затем медленный ответ A затирает строки, но кнопка на строках 82–87 отправляет эти ID уже для поставки B. Цена — оператор видит чужой лист, а печать либо упирается в серверный отказ, либо готовит не тот документ при совпавшем составе. Назначенные тесты используют одну поставку и гонку ответов не моделируют.

8. **Средняя — состояния загрузки и ошибки не соответствуют контракту.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:58` выводит наружу произвольный `Error.message` вместо фиксированного складского текста; строки 99–103 одновременно с ошибкой показывают пустое состояние `В поставке нет позиций`, а строка 100 на загрузке полностью убирает счётчики вместо скелета. Сценарий: сервер отвечает 500 или браузер получает сетевой `Failed to fetch` — оператор видит техническую причину вместе с ложным сообщением о пустой поставке. Цена — неверная диагностика складской ситуации и прямое расхождение со строками 60–74 контракта.

9. **Средняя — после обновления пропадут уже сохранённые отметки `Собрал` / `Упаковал`.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:36` меняет ключ localStorage с прежнего `article::size` на `article::sku::size::product_name`, но строки 40–43 не мигрируют старые записи. Сценарий: оператор частично отметил действующую поставку до выкладки, затем снова открывает её после обновления — все галочки отображаются снятыми. Цена — потеря видимого прогресса и повторная сборка/упаковка, хотя контракт требует сохранить существующие локальные отметки. Новый unit-тест проверяет только новый формат ключа и не проверяет совместимость со старыми данными.

## Проверено и нормально

- Продуктовый diff целиком сопоставлен с `CONTRACT.md`, `FEATURES.md`, `CASES.md`, общей картой волны и всеми `S-03-TC-001` … `S-03-TC-013`; лишних продуктовых файлов вне переданного списка и границы S-03/ui-kit не обнаружено.
- Серверная группировка и нумерация в неизменном снимке детерминированы: сортировка заканчивается `wb_order_id` и `order.id`, диапазоны непрерывны, а пропуск готового WB-стикера не сдвигает последующие номера.
- Поиск, фильтры и локальные галочки не меняют серверные `number_start/number_end` и не сокращают массив `order_ids`, отправляемый полной печати; блокировка повторного нажатия и закрытия во время подготовки реализована на уровне UI.
- Tenant-фильтрация сохранена, списания, резервы и остатки карточка не меняет; добавленные поля API опциональны, поэтому структурного удаления полей ответа для старых клиентов не найдено. `git diff --check` проходит. Две ошибки `I001` полного `ruff check` воспроизводятся без изменений уже на базовом SHA `d62f9afb`, поэтому карточке не приписаны.
