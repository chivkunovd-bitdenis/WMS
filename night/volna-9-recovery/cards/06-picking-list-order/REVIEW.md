ВЕРДИКТ: НАХОДКИ 7

# Ревью · 06-picking-list-order

Вердикт: CHANGES_REQUESTED.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/services/fbs_order_tape_print_service.py:102` — новая проверка требует передавать весь состав поставки для любого вызова `order-print-tape`, но существующая строковая печать в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:968` отправляет один `order_id`. В поставке из двух и более заказов оператор нажмёт активную «Печать ЧЗ и ШК» у одного заказа и получит `order_not_in_supply`, хотя заказ входит в поставку. Цена — сломаны печать и перепечать отдельного заказа; это новая серверная блокировка без такого же правила и предупреждения на экране. Рассогласование описано как B-14 в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/docs/blockers/S-03.md`.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:647` — `requestPrintBatch` теперь игнорирует переданный набор `_orderIds` и всегда отправляет все заказы поставки. При нажатии кнопки `QR` в одной строке (`:1970`) оператор вместо одного выбранного стикера получает предпросмотр всей поставки. Цена — лишняя печать, риск наклеить стикер не на тот заказ и регресс существующего строкового действия, которого нет в контракте карточки.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:75` — главное действие модалки по-прежнему вызывает deprecated `/stickers`, а не канонический `order-print-tape`, не отправляет серверные `order_ids` листа и печатает только WB-изображения. Для поставки, где порядок `wb_order_id` расходится с `(article, sku_code, size, product_name)`, лист идёт по товарному канону, а лента — по `wb_order_id`; служебных этикеток `№ K` на этой ленте нет. Если состав изменён во второй вкладке после открытия листа, ручка молча возьмёт новый состав, тогда как на экране останутся старые диапазоны. Цена — центральный сценарий карточки и кейс S-03-TC-008 не выполнены, оператор снова сопоставляет документы глазами.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FbsPrintPreviewDialog.tsx:114` — фактическое окно печати создаёт страницы только из PNG WB; нарисованная рядом в preview «Служебная этикетка WMS № K» (`:197-200`) в печатный HTML не попадает. Кроме того, серверный `FbsPrintAssetOut` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/app/api/fbs_supplies.py:190` не содержит `order_number` и `wb_order_id`, поэтому preview обычного print-batch всегда показывает `№ —`. Этот блок рисуется также для QR короба, грузоместа и поставки, хотя контракт меняет только заказную ленту. Цена — интерфейс обещает несуществующую этикетку, а на физическую ленту номер не выходит; соседние виды печати получили посторонний блок вне контракта.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:29` — ключ локальной отметки и React-ключ строки учитывает только `article + size`, хотя сервер группирует ещё по `sku_code + product_name`. Если у двух товарных строк совпали артикул и размер, но отличаются SKU или название, отметка «Собрал/Упаковал» одной строки переключает обе, а таблица получает дублирующиеся ключи (`:100`). Цена — ложная отметка физической операции и возможность пропустить неподобранный товар.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:97` — при `printing=true` в `PrintAction` передаётся `disabledReason=undefined`, поэтому кнопка остаётся визуально и технически доступной; индикатора действия также нет. Два быстрых нажатия до применения нового render могут запустить два запроса и открыть два окна печати, а после render кнопка всё равно выглядит активной, хотя обработчик молча ничего не делает. Цена — дублирование печати и невыполнение явного состояния «подготовка печати»/кейса S-03-TC-007.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.test.ts:5` — добавленный тест проверяет только локальный расчёт диапазонов из `quantity`; он остаётся зелёным, если кнопка вызывает старую ручку, печатает ленту в другом порядке, не печатает WMS-номер, смешивает изменившийся состав или допускает двойной запуск. Backend-тест в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/backend/tests/test_fbs_supply_assembly.py:169` аналогично проверяет только приватную сортировку и не проходит реальный `order-print-tape`. Пользовательского Playwright-сценария с назначенными S-03-TC-001…007 в diff нет. Цена — все находки 1–6 проходят заявленные тесты незамеченными.

## Проверено и нормально

- Все файлы реализации в diff входят в явно переданную границу карточки; `night/` и `tests/cases/` рассматривались как стадийные артефакты, а не как выход за экран.
- Серверный `get_picking_list` детерминированно сортирует товарные группы, заказы внутри группы и возвращает непрерывные диапазоны; tenant-фильтр при загрузке поставки сохранён.
- Добавление `order_number` в ответ `order-print-tape` аддитивно и само по себе не ломает старых API-клиентов; схема базы и операции с остатками/резервами этой карточкой не меняются.
- `git diff --check` прошёл; целевые backend-тесты дали `3 passed`. Frontend unit-тест запустить не удалось: локальная команда завершилась `vitest: command not found`.
