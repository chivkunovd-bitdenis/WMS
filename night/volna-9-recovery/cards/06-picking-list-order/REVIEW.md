ВЕРДИКТ: НАХОДКИ 4

# Ревью · 06-picking-list-order

Вердикт: CHANGES_REQUESTED.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:1191-1210,1605-1607` — новая модалка `FfFbsPickList` не подключена к рабочему месту: в `frontend/src` нет ни одного её импорта или монтирования, а видимая кнопка «Печать листа подбора» по-прежнему сразу открывает старый печатный HTML через `buildFbsPickingListPrintHtml`. При обычном клике оператор не увидит контрактные фильтры, отметки, диапазоны `№`, состояния загрузки/пустоты и кнопку полной ленты. Цена — основной пользовательский сценарий карточки целиком недостижим, хотя изолированный компонент существует в коде.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:902-945,1769-1782` — доступная оператору полная печать «Печать всего» продолжает идти через существующий `MarkingPrintDialog`, а не через реализованный в `FfFbsPickList` построитель пары. Ответ сервера содержит `order_number`, но текущий рендерер этого маршрута печатает WB-стикер и затем этикетки маркировки, не добавляя служебную этикетку `WMS № K`. При печати реальной поставки лента останется без номера, совпадающего с листом. Цена — физически нельзя сопоставить позицию листа с заказом на ленте, то есть главная цель карточки не выполнена.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.tsx:41-52,113-132` — альтернативный путь печати внутри пока недоступной модалки также собирает неверную физическую ленту для товара с Честным знаком: вместо сохранённого PDF/DataMatrix-артефакта он выводит обычным текстом полный `cis_code`, причём ставит эту страницу перед WB-стикером, а затем добавляет отдельную страницу WMS. При заказе с `has_label_artifact=true` сервер пометит код напечатанным и привяжет его к заказу, но товар получит текст КИЗ без машиночитаемого DataMatrix. Цена — израсходованный код маркировки без пригодной этикетки и нарушение контрактной последовательности `WB → WMS № K`.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-06-picking-list-order/frontend/src/screens/v2/FfFbsPickList.test.ts:4-55` — тесты проверяют только чистые функции диапазонов, ключей и наличие строк в HTML; они не монтируют модалку и не проходят ни один обещанный пользовательский сценарий `S-03-TC-001…007`. Набор останется зелёным, если `FfFbsPickList` вообще не подключён, видимая кнопка открывает старый лист, полная лента не получает номер или вместо DataMatrix печатается текст КИЗ. Цена — регрессии из находок 1–3 не ловятся автоматическими проверками перед вливанием.

## Проверено и нормально

- Все продуктовые изменения сверены с переданным списком файлов; артефакты в `night/`, `tests/cases/` и `docs/blockers/` не считались выходом за границы экрана.
- Серверный лист и лента используют один ключ `(article, sku_code, size, product_name)` с развязкой по `wb_order_id`, затем `order.id`; диапазоны непрерывны и не зависят от порядка клиента.
- Полная лента на сервере принимает только актуальный полный набор ID без дублей, а старый построчный режим с `include_order_qr=false` сохраняет поддержку подмножества; изменение API выполнено добавочными nullable-полями.
- Целевые backend-проверки прошли: `5 passed` для листа/ленты и `1 passed` для интеграционного endpoint-сценария. Frontend unit-тест не запустился из-за отсутствующего локального `vitest`; TypeScript-проверка также не завершилась в доступной среде и была остановлена без результата.
