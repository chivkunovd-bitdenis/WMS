# 09-billing — повторное содержательное ревью ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 3

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:186` — ремонт научил read-model возвращать человекочитаемый номер исходного документа, но экран по-прежнему выводит его обычным `TextCell` без действия открытия; при сверке строки `ПР-101` администратор не может перейти к существующей приёмке или отгрузке, хотя пункт 5.2 контракта прямо требует клик по номеру — цена: замороженная находка 2 и пользовательский путь `S-31-TC-004` закрыты лишь наполовину, расследование начисления обрывается на финансовом реестре.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/marketplace_unload_service.py:1097` — ради сторно ремонт разрешил прежнему endpoint отмены принимать уже `shipped`, а на строках 1113–1114 переводит документ в `cancelled` без возврата физически списанного товара; штатный frontend при этом не показывает отмену для `shipped` (`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1720`, `:3179`). При прямом повторе существующего API-клиента отгруженный документ исчезает из статуса «Отгружено», остаток остаётся уменьшенным, а оператор не получает ни доступного рабочего действия, ни предупреждения о другой семантике поздней отмены — цена: складской документ и остатки начинают описывать разные факты, а `S-31-TC-016` недоступен через реальный операторский сценарий. Это также изменение поведения вне финансового контракта: он требует неизменяемое сторно, но не разрешает переопределять жизненный цикл уже отгруженной заявки без складской корректировки.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/tests/test_marketplace_unload_completion.py:172` — тест, помеченный `S-31-TC-016`, отгружает и отменяет документ в один текущий день и проверяет только наличие двух ledger-строк; он не создаёт уже выставленный счёт исходного месяца и не формирует счёт следующего месяца. Если сторно ошибочно попадёт в старый счёт, не войдёт в следующий или перепишет прошлую детализацию, тест останется зелёным — цена: ключевое правило неизменяемого закрытого счёта и переноса разницы, ради которого добавлен ремонт, не защищено от регрессии.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист, после чего прочитан весь ремонтный diff `dbea4075..5d15d3c8` и все назначенные кейсы `S-31-TC-001`–`S-31-TC-016`; стадийные артефакты в `night/`, `tests/cases/` и `docs/evidence/` выходом за границы не считались.
- Полностью закрыты прежние находки 1, 3, 4, 5 и 7: параметры месяца совместимы, код хранения канонический, устаревшие блокировки не выдаются за живые, барьер проверяет каждый операционный склад, миграции образуют линию `09a → 09b → 09c`. В находках 2, 6 и тестовой части 8 остались перечисленные выше разрывы.
- Tenant-фильтры ledger, счетов и исходных финансовых фактов сохранены; параллельное формирование защищено уникальностью, повторная отмена счёта и повторное сторно идемпотентны, отрицательная строка сохраняет снимок исходной ставки и не меняет выставленный счёт напрямую.
- Адресный backend-набор завершился `32 passed`; `ruff` по ремонтному diff прошёл. Обычный `mypy` не сообщил ошибок в изменённых файлах, но остановился на четырёх унаследованных ошибках соседних сервисов. Frontend-гейты в этой рабочей копии не подтверждены из-за отсутствующих локальных npm-зависимостей. Секреты, ключи, токены, `.env`, кабинеты учётных данных, живой Wildberries и боевой прод не читались и не затрагивались.
