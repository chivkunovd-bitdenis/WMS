# 09-billing — содержательное ревью

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 15

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/tasks/billing_tasks.py:6` — задача, поставленная в Celery beat на ежедневные 02:30, всегда возвращает `None` и не открывает сессию, не обходит tenant/селлеров и не вызывает `form_invoice` — после закрытия месяца ни один счёт автоматически не появится; цена: главное прямое требование владельца «выставлять автоматически» фактически отсутствует.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:80` — экран вызывает `GET /billing/ledger` и на строке 97 `GET /billing/invoices`, которых нет среди маршрутов `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:68-150`; кроме того, отмена на строке 156 не передаёт обязательный `Authorization: Bearer ...` — на реальном сервере обе таблицы уходят в ошибку загрузки, а существующая отмена отвечает 401; цена: весь новый экран непригоден вне замоканных e2e.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:33` — проверка 12-значного ИНН передаёт в `zip(..., strict=True)` одиннадцать цифр и десять весов, поэтому любой 12-значный ИНН индивидуального предпринимателя, например `500100732259`, вызывает необработанный `ValueError`, а не пользовательскую ошибку — сохранение реквизитов завершается HTTP 500; цена: целый законный тип плательщика нельзя завести в биллинг.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/models/billing.py:38` — частичный уникальный индекс профиля ФФ задан только через `postgresql_where`; в SQLite, который используется Playwright-сервером, он компилируется как безусловный `UNIQUE (tenant_id)` — после сохранения профиля одного селлера профиль ФФ или второго селлера падает по уникальности; цена: обязательный комплект реквизитов невозможно собрать на локальном/CI-контуре, а тест с одним профилем это скрывает.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:109` — версии ищутся и закрываются отдельно по `unit`, поэтому для одной услуги и селлера одновременно могут действовать тарифы `document` и `item`; выбор тарифа в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:39` единицу не фиксирует и при таком наборе получает произвольную строку — одна и та же приёмка может посчитаться то за документ, то поштучно; цена: сумма счёта недетерминирована и зависит от плана выполнения SQL.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSettingsScreen.tsx:394` — диалог ставки не содержит предусмотренных контрактом полей «Для кого» и «Расчёт»: `seller_id` всегда `null`, а `unit` навсегда остаётся начальным `document`; при выборе хранения сервер всегда отвергает ставку, а выбранная в UI отгрузка сохраняется с кодом `outbound`, тогда как журнал на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/marketplace_unload_service.py:1013` ищет `marketplace_outbound` — персональный тариф недоступен, хранение не сохраняется, отгрузка остаётся `unpriced`; цена: два из трёх видов услуг и тариф конкретного селлера нельзя настроить рабочим способом.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:38` — дата тарифа берётся по UTC, а границы счёта в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:32` также строятся в UTC, хотя контракт требует календарь МСК — операция 1 августа в 00:30 МСК попадёт в 31 июля и возьмёт июльский тариф; цена: документы около полуночи систематически начисляются не в тот договорный месяц.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:32` — сервис не проверяет, что `period` нормализован на первое число, уже закрыт и содержит начисления — администратор может сформировать нулевой счёт за текущий/будущий месяц или отдельные счета за `2026-08-01` и `2026-08-02`; первый вариант затем навсегда исключит поздние начисления из-за уникальности, второй создаст два счёта за один календарный месяц; цена: неизменяемая финансовая история становится неполной или дублированной.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:47` — закрытие хранения считается нарушенным только при наличии выдуманной ledger-строки `source_type == "storage_period_open"`, но ни один writer такой строки не создаёт; обязательный межкарточный барьер требует, наоборот, доказательства зафиксированного statement по каждому операционному складу — при действующем storage-тарифе и вообще отсутствующем statement сервис выпустит неполный неизменяемый счёт; цена: теряется выручка за хранение и исправить уже выставленный документ пересчётом нельзя.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:45` — строка без тарифа навсегда остаётся с `amount=None`, а создание тарифа не переоценивает её и повтор формирования снова возвращает `unpriced`; предусмотренный контрактом путь «исправить тариф → повторить формирование» невозможен — цена: любой пропущенный на момент операции тариф блокирует счёт за весь месяц без штатного восстановления.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:27` — идемпотентность реализована как `SELECT`, затем обычный `INSERT` без блокировки и обработки `IntegrityError`; два одновременных финальных запроса оба не увидят запись, а один упадёт на уникальном ограничении и откатит складскую операцию. Такой же check-then-insert находится в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:23`, поэтому параллельные ручной и Celery-запуски дают 500 вместо одного счёта; цена: повтор, который контракт объявляет безопасным, ломает операцию или ночной выпуск.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:110` — в снимок счёта записывается только агрегат услуги без ID и без связей с исходными ledger-записями/документами; UI на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:179` ожидает `line.id` и `documents`, поэтому раскрытие всегда заканчивается «Документы не переданы» — цена: требуемой владельцем подробной разбивки и аудиторского следа от счёта к работе нет.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:13` — сервис умеет создать только `charge`; логики создания единственного `reversal`, обработки отмены исходной операции и включения позднего сторно в следующий период нет нигде в реализации — после отмены/переделки уже начисленной приёмки или отгрузки следующий счёт не получит отрицательную корректировку; цена: селлер остаётся переплаченным, а заявленная неизменяемая история финансово неверна.

14. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSettingsScreen.tsx:377` — вкладка «Тарифы ФФ» показывается без проверки `isFulfillmentAdmin`, хотя маршрут настроек на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/App.tsx:3138` разрешён также сотруднику с правом settings, а сервер принимает финансовые изменения только от администратора — сотрудник видит финансовые формы и упирается в 403 лишь после сохранения; цена: расхождение двух слоёв создаёт самодельную стену без предупреждения и нарушает контракт доступа. Новый blocker для S-19 шестью полями в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/docs/blockers/S-19.md` не описан.

15. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-seller-profile.spec.ts:30` — обязательный негативный сценарий сохранности реквизитов безусловно пропущен, `billing-tariffs.spec.ts` вообще отсутствует, а тесты реестров на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-ledger.spec.ts:5` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-invoices.spec.ts:7` подменяют отсутствующие GET API успешными ответами — тесты остаются зелёными при неработающем реальном экране, 500 на 12-значном ИНН, гонках, неверной границе месяца и неполном storage-счёте; цена: заявленные `S-31-TC-002/003/006/009-016` не защищают финансовый контракт перед вливанием.

## Проверено и нормально

- Полный diff одиннадцати атомов от общего базового коммита `d62f9af` прочитан; реализационные файлы находятся в явном списке карточки, а `night/`, `tests/cases/` и прочие стадийные артефакты не считались выходом за границы.
- Серверные mutation-ручки используют `require_fulfillment_admin`, а запросы к профилям и тарифам проверяют принадлежность указанного селлера текущему tenant; прямой доступ к данным другого tenant через эти пути не найден.
- Фактическое количество приёмки берётся из `posted_qty`, marketplace-отгрузки — из распределённого факта, а отсутствие тарифа не откатывает физическую складскую операцию; это соответствует выбранному продуктовому правилу.
- Адресные backend-тесты прошли: `4 passed`; `ruff` для изменённых billing-модулей и `git diff --check` также зелёные. Полная frontend-сборка не стартовала, потому что в checkout отсутствует `tsc`; секреты, `.env`, кабинеты учётных данных и боевой прод не читались и не затрагивались.
