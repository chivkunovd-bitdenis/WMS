# 09-billing — повторное содержательное ревью ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/marketplace_unload_service.py:1097` — замороженная находка не закрыта: прежний endpoint отмены принимает статус `shipped`, а строки 1100–1117 записывают финансовое сторно и переводят отгрузку в `cancelled` без возврата уже списанного товара. Штатный экран разрешает отмену только для `submitted`, `confirmed` и `collecting` (`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1721`, `:3179`). При прямом вызове существующего API-клиента после физической отгрузки документ перестаёт числиться отгруженным, хотя остаток остаётся уменьшенным; через UI предусмотренного рабочего процесса позднего сторно нет — цена: складской статус расходится с фактом движения товара, а `S-31-TC-016` реализован недоступным оператору и не описанным контрактом способом.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/tests/test_marketplace_unload_completion.py:172` — замороженная находка не закрыта: тест с меткой `S-31-TC-016` по-прежнему отгружает и отменяет документ в одном текущем периоде и на строках 215–252 проверяет только две записи ledger. Он не выставляет исходный счёт, не переносит время сторно в следующий месяц, не формирует следующий счёт и не сверяет неизменность старой детализации. Если сторно попадёт в старый счёт, потеряется при следующем формировании либо изменит уже выставленный документ, тест останется зелёным — цена: главное правило кейса о неизменяемом закрытом счёте и учёте разницы только в следующем счёте не защищено от регрессии.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист. После его коммита `11cd9459` продуктового ремонта нет: `fc205a21`, `96cacc59` и `475a1db4` меняют только стадийные отчёты и журнал, поэтому новых ремонтных регрессий в продуктовом diff не появилось.
- Прежняя экранная находка закрыта в текущем состоянии: `FfBillingScreen.tsx:200–212` делает номера `inbound_intake` и `marketplace_unload` кликабельными, а `billing-ledger.spec.ts:22–24` проверяет открытие штатного диалога приёмки с сохранением маршрута расчётов.
- Границы переданных продуктовых файлов и стадийных артефактов соблюдены; новых односторонних UI/server-блокировок в ремонтном diff нет. Tenant-фильтры, идемпотентность одной reversal-записи и снимок исходной ставки в проверяемом пути не изменялись.
- Адресный backend-набор завершился `11 passed`, `ruff` для затронутых backend-файлов и `git diff --check` прошли. Секреты, ключи, токены, `.env`, кабинеты учётных данных, живой Wildberries и боевой production не читались и не затрагивались.
