# 09-billing — содержательное ревью

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 17

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:68` — router содержит только сохранение профилей и тарифов, формирование и отмену счёта, но не содержит `GET /billing/ledger`, `GET /billing/invoices`, чтения профилей и чтения тарифов; при любом реальном открытии `/app/ff/billing` запросы из `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:82` и `:100` получают 404/405, а после перезагрузки S-18/S-19 сохранённые реквизиты и ставки исчезают из формы — цена: основной экран карточки не работает без тестовых route-моков, администратор не может сверить начисления или открыть выставленный счёт.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/inbound_intake_service.py:1230` и `:1644` — два штатных пути `putaway_box` и `complete_distribution` переводят приёмку в `done`, но, в отличие от `receive_line`/`post_all_remaining`, не вызывают `record_operational_charge`; если оператор завершает ту же приёмку коробом или заранее сохранённым распределением, товар оприходуется, а оплачиваемая работа не попадает в ledger — цена: выручка по приёмке теряется в зависимости от выбранного оператором рабочего пути.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSettingsScreen.tsx:424` — форма сохраняет услугу отгрузки как `outbound`, тогда как финальная отгрузка пишет начисление с `service_code="marketplace_outbound"` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/marketplace_unload_service.py:1014`; при настроенной через UI ставке каждая реальная отгрузка всё равно становится `unpriced` — цена: счета за отгрузку невозможно выпустить штатной настройкой.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:39` — дата подбора тарифа вычисляется по UTC, хотя контракт относит финальный факт к календарному месяцу МСК; операция в 00:30 МСК первого числа получает дату предыдущего дня, предыдущую версию тарифа и может попасть в счёт с суммой не по договорному периоду — цена: систематически неверная ставка на границе каждого месяца.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:55` — строка, созданная без тарифа, навсегда сохраняет `rate=None` и `amount=None`, а создание покрывающей дату версии в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:87` не переоценивает такие строки; сценарий контракта «добавить тариф → повторить формирование» снова возвращает `unpriced` — цена: единственный пропуск тарифа бессрочно блокирует счёт за месяц без пути восстановления.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:62` — готовность хранения проверяется только по уже найденным ledger-строкам и вымышленному `source_type` `storage_statement`, хотя `ARCH-CROSS.md` требует проверять действующий тариф и зафиксированный `StorageStatement` по каждому операционному складу, включая нулевой; при действующем storage-тарифе и отсутствии публикации хранения `storage_entries` пуст, проверка пропускается и создаётся неполный счёт — цена: выручка за хранение остаётся за пределами неизменяемого документа.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:113` — пустой список начислений не останавливает формирование: при заполненных профилях создаётся счёт с `lines=[]` и итогом 0, хотя контракт для пустого месяца требует «Нет начислений для формирования»; ежедневный запуск выставит каждому селлеру бессодержательный коммерческий документ — цена: реестр и неизменяемая нумерация загрязняются ложными счетами.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:73` — объект добавляется в сессию до `begin_nested()`, а SQLAlchemy при открытии savepoint сначала автоматически flush-ит pending-объекты; при двух одновременных финализациях `IntegrityError` возникает на строке `:78`, вне `try` на `:79`, и откатывает складскую операцию вместо возврата уже созданного начисления — цена: заявленная идемпотентность не защищает повторный клик/параллельный запрос и может сорвать завершение приёмки или отгрузки.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/alembic/versions/20260822_0094_billing_financial_core.py:47` — уникальность версии тарифа включает nullable `seller_id` и `unit`; PostgreSQL допускает несколько строк с `seller_id IS NULL`, а две параллельные общие ставки разных единиц также не конфликтуют, хотя сервисная предварительная проверка обеих транзакций их не увидит — цена: двойное быстрое сохранение создаёт две действующие версии одной услуги, и выбор ставки в ledger становится недетерминированным.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:97` — закрытый доменный набор валидируется только по единице: сервер принимает `inbound + liter_day`, `marketplace_outbound + liter_day`, `storage` вместо `storage_liter_day` и произвольный `service_code`; при прямом API-запросе сохраняется тариф, который UI не способен корректно показать, а операционный writer никогда не выберет — цена: финансовая конфигурация содержит формально успешные, но неработающие ставки.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:177` — единственность сторно проверяется по переданному `source_id`, но на `reversal_of_id` нет уникального ограничения, и функция вообще не подключена ни к одному пути отмены/переделки; два события с разными ID могут дважды сторнировать исходную строку, а реальные отмены не создают сторно вовсе — цена: следующий счёт либо не вернёт переплату, либо вернёт её дважды.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/tasks/billing_tasks.py:23` — ежедневная задача обрабатывает только непосредственно предыдущий месяц, а не все пропущенные закрытые месяцы, и расписание в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/celery_app.py:18` не задаёт timezone Celery; после простоя дольше месяца старый счёт уже никогда не сформируется автоматически, а `02:30` интерпретируется стандартной UTC-зоной Celery вместо МСК — цена: прямое требование автоматического догоняющего выпуска не выполняется.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:149` — JSON-строки счёта создаются без `id`, а поля снимков называются `ff_profile_snapshot`/`seller_profile_snapshot`; UI ожидает обязательный `line.id` и `ff_profile`/`seller_profile` в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:38`–`:39`, поэтому у реального ответа ключи строк будут `undefined`, раскрытие документов не выберет строку, а реквизиты заменятся заглушками — цена: подробная разбивка и аудиторский след счёта не работают даже после добавления GET-ручки.

14. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:153` и `:184` — все исправляющие действия у проблем счёта и «Открыть тарифы» имеют `onClick={() => undefined}`, а предусмотренное контрактом состояние «Причины устранены — повторите формирование» с рабочей кнопкой отсутствует; администратор видит блокировку, но не может перейти к исправлению или повторить тот же алгоритм — цена: любой проблемный счёт оставляет оператора в тупике.

15. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:157` — печатный HTML не содержит снимков ИНН, банковских реквизитов и даты выставления, хотя контракт требует их в коммерческом счёте; кроме того, `seller_name` и остальные строки вставляются через `document.write` без экранирования, поэтому селлер с HTML в названии исполняет разметку/скриптовый обработчик в печатном окне — цена: распечатка не является требуемым счётом и создаёт stored-XSS в авторизованной сессии администратора.

16. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-ledger.spec.ts:5` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-invoices.spec.ts:7` — тесты подменяют отсутствующие GET API успешными ответами и потому остаются зелёными при полностью неработающем реальном экране; негативный тест профиля на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-seller-profile.spec.ts:61` требует одновременно старый success и новую ошибку, хотя экран сбрасывает success перед запросом, и проверяет только несохранённые значения текущей формы вместо повторного чтения из БД — цена: `S-31-TC-004/007/009/012` не ловят перечисленные регрессии и один из обязательных кейсов сам противоречит реализации.

17. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfSettingsScreen.tsx:409` — вкладка «Тарифы ФФ» показывается сотруднику с правом settings, потому что маршрут S-19 допускает его через `canSettingsOps`, но содержимое на строке `:412` скрывается условием `isFulfillmentAdmin`, а серверные mutation-ручки также требуют администратора; сотрудник нажимает видимое действие и получает пустой экран без объяснения — цена: новый UI-only blocker расходится с серверной границей и оставляет оператора в тупике. Обязательное описание шестью полями в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/docs/blockers/S-19.md` отсутствует.

## Проверено и нормально

- Полный актуальный diff карточки от общего базового коммита `d62f9af` и все перечисленные реализационные файлы сверены с `CONTRACT.md`, `RESHENIYA.md`, `CASES.md`, `MAP.md` и обязательным `ARCH-CROSS.md`; стадийные артефакты в `night/`, `tests/cases/` и `docs/evidence/` выходом за границы не считались.
- Mutation-ручки биллинга требуют роль администратора ФФ, а сохранение профиля/персонального тарифа проверяет принадлежность селлера текущему tenant; изменения существующих форматов API, способные отдельно сломать ТСД или портал селлера, не найдены.
- Фактическое количество приёмки в двух подключённых путях берётся из `posted_qty`, количество marketplace-отгрузки — из распределённого факта, а отсутствие тарифа не откатывает физическую операцию при обычном одиночном запросе.
- Адресные backend-проверки прошли: `8 passed`, адресный `ruff check` и `git diff --check` зелёные. Frontend build не подтверждён: локальная команда остановилась на `tsc: command not found`; секреты, ключи, токены, `.env`, кабинеты учётных данных и боевой прод не читались и не затрагивались.
