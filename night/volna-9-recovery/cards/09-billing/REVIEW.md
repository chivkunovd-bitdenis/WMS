# 09-billing — содержательное ревью после rework

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 14

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:88` — живой экран не совместим с обеими GET-ручками биллинга: он отправляет `period=YYYY-MM` в параметр `date`, а при значении фильтра по умолчанию ещё и `seller_id=all` в параметр `UUID`; `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:164` и `:191` отвечают 422. Даже после ручной замены периода на `YYYY-MM-DD` ledger возвращается JSON-массивом, тогда как экран ожидает объект `{entries|rows}`; у массива свойство `entries` является функцией `Array.prototype.entries`, и передача её в `setRows` приводит к ошибке React. При первом обычном открытии `/app/ff/billing` администратор получает ошибку вместо начислений и счетов — цена: основной экран карточки фактически не работает с реальным API, хотя route-моки e2e зелёные.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:172` — read-model начислений не реализует контракт экрана: границы месяца строятся как наивные даты, а не полуинтервал месяца МСК, параметры `service_code`, `mode` и `document_number` вообще отсутствуют, а ответ на `:182` не содержит `seller_name`, `document_number`, `performer_name` и `problem`. Операция в 00:30 МСК первого числа попадает не в тот месяц, поиск и фильтр услуги ничего не фильтруют, а даже успешно полученная строка не может показать селлера, исходный номер, исполнителя и «Нет тарифа» — цена: финансовая сверка по S-31-TC-004/005/012 недостоверна и неполна.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:191` — список счетов игнорирует переданные экраном `status` и `number`, не добавляет `seller_name`, а реальные поля снимков называются `ff_profile_snapshot`/`seller_profile_snapshot`; документы в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:139` содержат только технические `source_type/source_id/occurred_at`, тогда как `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:39` ожидает `seller_name`, `ff_profile`/`seller_profile` и документ с `date/number`. При открытии реального счета плательщик подменяется пустой заглушкой, исходный номер документа не показывается, а поиск и статусный фильтр не действуют — цена: детализация и печатный аудиторский след S-31-TC-007 не соответствуют неизменяемому счету.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:159` — экран ищет блокирующие причины только внутри `invoice.issues`, но при блокировке счет не создаётся, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:191` не возвращает `BillingRunIssue` ни отдельно, ни внутри счета. POST формирования отдаёт причину один раз, но `retryFormation` на `:167` её не сохраняет и после успеха очищает список без повторного GET; вдобавок он передаёт в date-путь `YYYY-MM`. При отсутствии тарифа, реквизитов или закрытого хранения администратор никогда не увидит реальную причину и исправляющее действие, а после устранения причины не увидит сформированный счет — цена: обязательный сценарий восстановления из блокировки S-31-TC-013 заканчивается тупиком.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:94` — создание покрывающей даты версии тарифа никак не переоценивает ранее записанные `BillingLedgerEntry` с `rate=None/amount=None`, а формирование счета на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:63` продолжает считать такую строку `unpriced`. Если склад завершил работу без тарифа, администратор затем добавил правильную версию и повторил формирование, счет останется заблокирован навсегда — цена: требуемый контрактом путь «добавить покрывающий тариф → ежедневный повтор выпуска» отсутствует, выручку за такой факт нельзя восстановить штатно.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:65` — барьер хранения проверяет вымышленные ledger-источники `storage_statement/storage_statement_closed`, хотя обязательный `ARCH-CROSS.md` задаёт строки `source_type='storage_measurement'` и отдельный зафиксированный `StorageStatement` по каждому операционному складу, включая нулевой. Любой реальный storage-entry поэтому блокирует счет навсегда; нулевой зафиксированный statement не распознаётся; тариф, начавшийся в середине месяца, вообще пропускает барьер из-за сравнения `valid_from <= period` на `:77`. В результате один сценарий выпускает неполный неизменяемый счет, а другой не выпускает полный — цена: общий счет 08+09 нельзя безопасно сформировать ни для обычного, ни для нулевого хранения.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:121` — проверка версий тарифа выполняется без блокировки и уникальность защищает только точное совпадение даты. Если две вкладки одновременно создают версии с 15 сентября и 1 октября, обе читают одну прежнюю версию, обе закрывают её и вставляют пересекающиеся интервалы; при одинаковой дате проигравший запрос получает необработанный `IntegrityError`, потому что `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:146` ловит только доменную ошибку. Цена: S-31-TC-010/011 нарушается, выбор ставки для будущей работы становится недетерминированным либо пользователь видит 500.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_ledger_service.py:17` — writer не проверяет явно заданную дату включения биллинга tenant и вызывается безусловно из всех финальных путей приёмки и отгрузки. Сразу после выкладки tenant без включённого модуля и без тарифов начинает накапливать `unpriced`-строки; ежедневная задача затем создаёт проблемы за эти месяцы. Цена: нарушено решение «учёт не раньше даты включения и начала тарифа», существующим клиентам молча появляется финансовая история, которую владелец прямо запретил считать автоматическим backfill.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:194` — функция сторно существует, но не вызывается ни одним production-путём; поиск по `backend/app` находит только её определение. Когда оплачиваемый факт отменён или переделан после выставления счета, отрицательная строка не появляется и разница не входит в следующий месяц — цена: S-31-TC-016 и продуктовое правило неизменяемой истории не работают, ФФ продолжает требовать оплату за отменённую работу.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:221` — ручка формирования принимает произвольный `seller_id`, но `form_invoice` не подтверждает принадлежность селлера tenant. Администратор tenant A, передав UUID селлера tenant B и закрытый период, получает `missing_profile`, после чего `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:107` создаёт `BillingRunIssue` с `tenant_id=A` и внешним `seller_id=B`; обычный FK это допускает. Цена: финансовые данные связывают два tenant, отчёты и последующая обработка получают логически чужого селлера.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_configuration_service.py:73` — обязательность банковских реквизитов ФФ проверяется до `strip()`: строки из пробелов проходят `all(...)`, затем на `:86`–`:89` превращаются в `None`. `form_invoice` проверяет лишь наличие объекта профиля, поэтому администратор может сохранить пробелы и получить выставленный коммерческий счет без банка, БИК и счетов — цена: неизменяемый документ выпускается без обязательных реквизитов получателя.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/alembic/versions/20260822_0094_billing_financial_core.py:8` — миграция ответвляется напрямую от `20260821_0093`, хотя обязательный порядок `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/night/volna-9-recovery/ARCH-CROSS.md:148` сначала вливает карточку 03 с собственной миграцией 0094, затем 07-A, и только потом 09-A. При сборке волны billing и уже обязательные миграции образуют несколько Alembic-head; стандартный `alembic upgrade head` не сможет выбрать единственную вершину — цена: выкладка общей волны останавливается до применения финансовой схемы.

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-ledger.spec.ts:6` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-invoices.spec.ts:7` — e2e подменяют сервер именно теми обёртками и полями, которых реальный API не возвращает, поэтому остаются зелёными при находках 1–4. Backend-тесты используют `AsyncMock` и не проверяют HTTP-схему, реальную БД, параллельную запись, позднюю тарификацию, storage-barrier, автоматический полный счет или сторно; назначенные S-31-TC-006/010/011/013–016 не имеют теста поведения. Цена: тестовый слой создаёт ложное подтверждение главного финансового пути и не ловит регрессии, ради которых эти кейсы заведены.

14. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/docs/blockers/S-31.md:1` — обязательного файла нет, хотя новый экран и сервер вводят три явных запрета на выпуск счета (`unpriced`, `missing_profile`, `storage_period_not_closed`) и UI на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:216` блокирует повтор. В `_kandidaty/S-31.md` есть только механическая заметка без шести полей, а слои расходятся: сервер запрещает выпуск, но живой UI не получает причины. Цена: оркестратор и утренняя проверка не видят введённые доменные блокировки, оператор упирается в серверную стену без предусмотренного предупреждения.

## Проверено и нормально

- Полный актуальный набор реализационных файлов карточки сверён с `CONTRACT.md`, `RESHENIYA.md`, `CASES.md`, `MAP.md` и обязательным `ARCH-CROSS.md`; файлы стадийных ролей в `night/`, `tests/cases/` и `docs/evidence/` выходом за границы не считались.
- Все четыре штатных пути завершения приёмки теперь передают исполнителя и создают один idempotency-key по документу; финальная marketplace-отгрузка использует согласованный код `marketplace_outbound`, а отсутствие тарифа не откатывает одиночную складскую операцию.
- Дата выбора операционного тарифа переведена на календарь МСК, общая ставка уступает персональной, нулевая ставка разрешена, а частичные уникальные индексы отдельно защищают общий тариф, профиль ФФ и одно сторно исходной строки.
- Адресные `ruff`, `mypy` и 11 billing-pytest прошли; `git diff --check` нашёл только лишнюю пустую строку в конце e2e-файла, что не является содержательной находкой. Frontend build не подтверждён из-за отсутствующего в рабочей копии `tsc`; секреты, ключи, токены, `.env`, кабинеты учётных данных и боевой прод не читались и не затрагивались.
