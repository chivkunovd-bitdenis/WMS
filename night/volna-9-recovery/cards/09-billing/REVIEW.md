# 09-billing — повторное содержательное ревью ремонта

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 8

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:106` — экран после ремонта запрашивает начисления с обязательным параметром `date=YYYY-MM-01`, тогда как `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:215` по-прежнему требует параметр `period`; при обычном открытии `/app/ff/billing` FastAPI отвечает 422 и администратор видит «Не удалось загрузить начисления» вместо журнала — цена: основной пользовательский путь S-31-TC-004/005/012 не работает с живым API.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:246` — read-model подставляет UUID исходной записи как `document_number`, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:146` навсегда сохраняет тот же UUID в детализацию счёта, хотя у приёмки и marketplace-отгрузки уже есть человекочитаемые `document_number`/`display_number`; клик для открытия существующего документа также не реализован. При сверке приёмки `ПР-101` администратор увидит технический UUID, поиск `ПР-101` ничего не найдёт, а выставленный счёт утратит требуемый аудиторский след — цена: S-31-TC-004/007/012 не позволяют найти и доказать исходный факт.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:244` — UI отправляет фильтр хранения как `service_code=storage` и распознаёт хранение в детализации только по коду `storage` на строке 251, но единый межкарточный контракт и backend используют `storage_liter_day`. После интеграции карточки 08 фильтр «Хранение» вернёт пустой список, услуга будет показана техническим кодом, а вместо «Расчёт хранения за {месяц}» в счёте появится UUID measurement — цена: оператор не может сверить хранение, а S-31-TC-006/007 дают неверный вид полного счёта.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:97` — `BillingRunIssue` только создаётся и никогда не закрывается или не удаляется после исправления причины; `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/api/billing.py:282` возвращает все старые issues, а `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/src/screens/ff/FfBillingScreen.tsx:250` при наличии issue всегда отключает «Повторить формирование». Если администратор добавил отсутствующий тариф или реквизиты и вернулся на экран, прежняя причина остаётся, повтор недоступен, автоматическое формирование может создать счёт, но UI продолжит показывать блокировку. Дополнительно ежедневный запуск создаёт `no_entries` на строке 68 сервиса, а новый API выставляет эту неблокирующую пустоту как неизвестную проблему «Требуется исправление» — цена: восстановление S-31-TC-013 заканчивается тупиком, пустые месяцы ложно объявляются поломкой.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:71` — storage-barrier считает период готовым при наличии хотя бы одной строки `storage_liter_day` и вообще не проверяет зафиксированные `StorageStatement` по каждому операционному складу, как требует `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/night/volna-9-recovery/ARCH-CROSS.md:43`. Если statement склада A зафиксирован, а склада B ещё нет, одна опубликованная строка снимает барьер и создаётся неизменяемый неполный счёт; нулевой statement и принадлежность строки конкретному statement/складу также не подтверждаются — цена: часть хранения теряется из счёта без возможности штатного пересчёта, S-31-TC-006/013 нарушены. Документ `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/docs/blockers/S-31.md:43` описывает правильное правило, но сервер ему не соответствует.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/app/services/billing_invoice_service.py:198` — `record_reversal` всё ещё не вызывается ни одним production-путём в `backend/app`; рабочие сервисы приёмки и marketplace-отгрузки только создают charge. При поздней отмене или переделке уже оплачиваемого факта отдельная отрицательная строка не появляется и разница не входит в следующий месяц — цена: S-31-TC-016 отсутствует, неизменяемый прошлый счёт остаётся без компенсирующей операции, ФФ продолжает требовать лишнюю сумму.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/alembic/versions/20260822_0096_billing_activation_date.py:9` — ремонт добавил новый head `20260822_0096`, но `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/backend/tests/test_billing_financial_core_migration.py:13` всё ещё требует head `20260822_0095`, поэтому назначенный billing pytest уже падает (`1 failed, 16 passed`). Одновременно соседняя обязательная карточка 08 публикует собственную миграцию с тем же revision `20260822_0096`, а карточки 03 и 07 используют собственные `0094`; при сборке волны это даёт коллизии revision/multiple heads вместо порядка из `ARCH-CROSS.md` — цена: текущий CI красный, а интеграционная миграция волны не может быть безопасно применена.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-09-billing/frontend/tests-e2e/billing-ledger.spec.ts:6` — тест ремонта закрепляет несовместимый с backend контракт: мок отвечает JSON-массивом и на строке 13 требует параметр `date`, поэтому остаётся зелёным именно при находке 1; `billing-invoices.spec.ts` также мокирует готовые снимки и документы, не проходя живой read-model. Backend-тесты не вызывают HTTP-ручки ledger/invoices и через `AsyncMock` не проверяют lifecycle issues, два склада хранения, параллельное формирование и production-сторно. При поломанном живом экране и незакрытых S-31-TC-006/010/011/013–016 тестовый слой сообщает ложный успех — цена: регрессии финансового пути доходят до интеграции незамеченными.

## Проверено и нормально

- Предыдущий `REVIEW.md` использован как замороженный чек-лист: полностью закрыты прежние пункты 5, 7, 8, 10 и 11 — покрывающая ставка переоценивает `unpriced`, тарифный поток блокируется на tenant, дата включения биллинга проверяется writer-ом, чужой seller отклоняется, пробельные банковские поля не проходят.
- Прочитан весь diff после прошлого вердикта и все назначенные кейсы `S-31-TC-001`–`S-31-TC-016`; стадийные артефакты в `night/`, `tests/cases/` и `docs/evidence/`, а также разрешённый документ блокировок выходом за границы не считались.
- Tenant-фильтры профилей, начислений, счетов и формирования теперь присутствуют; месячные границы ledger/invoice считаются полуинтервалом календарного месяца МСК; нулевая ставка и сохранение снимков реквизитов в уже созданном счёте реализованы без пересчёта прошлого.
- Адресные `ruff` и `mypy` прошли. Billing pytest воспроизводимо завершился `1 failed, 16 passed` из-за находки 7; frontend typecheck/e2e в этом проходе не подтвердились, потому что локальный `npx` попытался обратиться к недоступному registry. Секреты, ключи, токены, `.env`, кабинеты учётных данных и боевой прод не читались и не затрагивались.
