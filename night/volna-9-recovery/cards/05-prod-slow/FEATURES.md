ФИЧ: 2

## Фичи

### 1. Закрепить состояние «Готовим ленту…» пользовательским E2E-сценарием

Что меняется для оператора: пока лента готовится в фоне, в карточке состояния нет ни кнопки «Закрыть», ни другого действия; оператор по-прежнему может закрыть всю модалку кликом по затемнённой области. Это защищает правило R-31 и назначенный сценарий `S-03-TC-018`, не меняя уже верный компонент модалки.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-marking-print-constructor.spec.ts`

Зависимости: нет. Это самостоятельная проверка существующей реализации `TapePreparationStatus`; принятую логику экрана и прочие состояния ленты не менять.

Как проверить: сценарий с пометкой `S-03-TC-018` через UI доводит модалку до `marking-print-preparing`, подтверждает отсутствие кнопки «Закрыть» и любых интерактивных действий внутри карточки, затем закрывает диалог кликом по backdrop (затемнённой области вне содержимого). После этого повторный запуск и переход в `ready` сохраняют действующий путь «Открыть для печати». Адресный Playwright-тест проходит.

### 2. Убрать из карточки 05 глобальный снимок UI-инвентаря

Что меняется для оператора: никак — это изоляция границы поставки. Карточка 05 не становится владельцем подписей, чипов и предупреждений соседних экранов; в ней остаются только контрактные правки S-03 и общей модалки, для которых новый UI-kit не требовался.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/ui-kit/inventory.generated.ts`

Зависимости: нет; может выполняться параллельно с фичей 1. Не перегенерировать весь инвентарь и не переносить в эту карточку данные `MovementsScreen`, `FfFbsStockSyncScreen`, `SellerInboundDraftScreen`, `FfInboundRequestView` или других владельцев. Если для их актуализации нужен генератор, это отдельные карточки-владельцы, а не работа 05.

Как проверить: дифф карточки 05 по этому файлу не содержит глобальной перегенерации и не меняет записи соседних экранов; `git diff --check` проходит. После изоляции в содержимом и итоговом диффе не остаётся не принадлежащих S-03/`MarkingPrintDialog` данных, добавленных этой карточкой.

## Порядок

1. Выполнить фичу 1, чтобы обязательный пользовательский сценарий `S-03-TC-018` стал защитой от прямой регрессии R-31.
2. Выполнить фичу 2 как независимую очистку границы. Её можно делать параллельно с фичей 1, поскольку оба атома меняют разные frontend-файлы.
3. После обоих атомов повторить code review только по двум незакрытым находкам. Уже принятые ширины заголовков, отсутствие жёлтой заливки результата поиска и код `MarkingPrintDialog.tsx` не перерабатывать.

## Что осталось за бортом

- Принятые изменения таблицы S-03 и самого `MarkingPrintDialog.tsx` не входят в перепланирование: в повторном `REVIEW.md` они признаны корректными.
- Перенос или актуализация данных глобального UI-инвентаря у карточек-владельцев не относится к 05; эта карточка только убирает свою неоднозначную атрибуцию.
- Адресный браузерный прогон в этой среде может не стартовать из-за запрета bind `127.0.0.1:18000`; это ограничение фиксируется при выполнении, но не меняет объём атомов.
