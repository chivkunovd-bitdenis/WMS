ФИЧ: 1

## Фичи

### 1. Вернуть проверку закрытия подготовки ленты клавишей Esc

Оператор, пока лента ЧЗ готовится в фоне, должен по-прежнему суметь закрыть модалку клавишей Esc; после повторного открытия тот же сценарий отдельно проверяет закрытие кликом по затемнённой области. Уже принятые проверки отсутствия кнопки «Закрыть» и любых интерактивных действий внутри `marking-print-preparing` сохраняются без изменения.

Файлы:

- `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-marking-print-constructor.spec.ts`

Зависимости: нет. Это точечный ремонт только теста поверх принятого атома DEV-01; реализацию `MarkingPrintDialog` и уже принятые части карточки не менять.

Как проверить: запустить сценарий `S-03-TC-018` из `ff-marking-print-constructor.spec.ts`. После появления `marking-print-preparing` в первом цикле отправить Esc и убедиться, что `marking-print-dialog` скрыт; открыть модалку заново, сохранить проверку отсутствия действий внутри статуса, закрыть её кликом по backdrop и убедиться, что диалог снова скрыт. Тем самым оба штатных способа выхода из фоновой подготовки защищены от регрессии.

## Порядок

1. Выполнить фичу 1. Параллельных фич нет: это один независимый тестовый атом, который не требует повторной разработки уже принятой карточки.

## Что осталось за бортом

- Других незакрытых находок в `REVIEW.md` нет; фиксированные ширины таблицы, отсутствие жёлтой подсветки поиска, запрет действий в состоянии подготовки и закрытие по backdrop уже приняты и намеренно не возвращаются в разработку.
- Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод `194.87.96.144` и живой кабинет Wildberries в этом атоме не затрагиваются.
