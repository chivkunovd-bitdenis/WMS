ФИЧ: 6

# Разбиение незакрытых находок · 05-prod-slow

В `JUDGE.md` нет доказанного дефекта реализации: все шесть находок — отсутствие
браузерного доказательства уже принятых изменений. Поэтому это не повторная
разработка карточки, а шесть независимых атомов продуктовой проверки. В каждом
атоме исходный файл указан как граница проверяемого поведения; менять его можно
только если живая проверка покажет конкретное расхождение с контрактом.

## Фичи

### 1. Подтвердить шапку вкладки «Новые» на обычной и суженной ширине

Оператор открывает `/app/ff/fbs`, переключается на вкладку «Новые» и фиксирует
два снимка: обычной и суженной ширины. На обоих видны ровно четыре
информационных однострочных заголовка «Товар», «Селлер», «Маршрут сдачи»,
«Отгрузить до» с фиксированными ширинами; колонки «Статус» нет. Это закрывает
только непроверенность `S-03-TC-016` по R-09 и R-36, без изменения состава
таблицы.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx`.

Зависит от: нет; реализация этого места уже принята предыдущим циклом.

Как проверить: пройти `S-03-TC-016` в живом браузере, сохранить оба снимка в
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/` и в вердикте указать URL,
роль, действия и видимый результат.

### 2. Подтвердить, что поиск не окрашивает строку заказа

Оператор находит заказ через поиск и фиксирует обычное и hover-состояние
найденной строки. Результат поиска остаётся видимым, но без жёлтой заливки;
жёлтый цвет допустим только у независимого признака расхождения количества.
Это закрывает только непроверенность `S-03-TC-017` по R-11 и не меняет
скролл к найденной строке или логику поиска.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx`.

Зависит от: нет; можно выполнять параллельно с фичей 1.

Как проверить: пройти `S-03-TC-017` в живом браузере, сохранить два снимка в
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/`; отдельно указать, по какому
признаку в данных распознана строка с расхождением количества.

### 3. Подтвердить штатное закрытие модалки во время фоновой подготовки ленты

Оператор открывает печать ЧЗ и в состоянии «Готовим ленту…» фиксирует отсутствие
кнопок в карточке состояния. Затем в двух независимых запусках закрывает диалог
сначала клавишей Esc, затем кликом по затемнённой области; фоновая подготовка
не отменяется. Это закрывает только непроверенность `S-03-TC-018` по R-31;
состояния готовности и ошибки не меняются.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-marking-print-constructor.spec.ts`.

Зависит от: нет; можно выполнять параллельно с фичами 1 и 2.

Как проверить: пройти `S-03-TC-018` в живом браузере и сохранить три снимка
в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/`: подготовка без кнопок,
закрытие по Esc и закрытие по backdrop. Для каждого закрытия после возврата
проверить, что то же задание остаётся в подготовке либо переходит в готовность.

### 4. Подтвердить сохранение контекста поставки при скрытой вкладке

Оператор открывает рабочее место FBS-поставки, выбирает рабочий контекст,
скрывает вкладку и возвращается после интервала автообновления. Выбранные
поставка, вкладка и видимый контекст не сбрасываются невидимым тиком. Это
закрывает только непроверенность `S-04-TC-001`; нового правила интерфейса не
добавляется.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx`.

Зависит от: нет; можно выполнять параллельно с фичами 1–3.

Как проверить: пройти `S-04-TC-001` в живом браузере, сохранить снимок до
скрытия и снимок после возврата в
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/`; в вердикте записать
выбранные оператором значения и фактически выдержанный интервал.

### 5. Подтвердить отсутствие дубля ручного складского действия при автообновлении

Оператор выполняет одно ручное действие в рабочем месте FBS-поставки и остаётся
на этой вкладке до следующего 15-секундного обновления. После возврата ответа
и тика в интерфейсе нет второй операции, дублирующей строки или сброшенного
контекста. Это закрывает только непроверенность `S-04-TC-002` и не меняет
частоту опроса.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx`.

Зависит от: нет; можно выполнять параллельно с фичами 1–4.

Как проверить: пройти `S-04-TC-002` в живом браузере, сохранить снимок до
подтверждения действия и после следующего тика в
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/`; сверить, что операция и
строка представлены ровно один раз.

### 6. Зафиксировать визуальные инварианты трёх принятых зон

После прохождения пяти сценариев запускается проверка геометрии, обрезки,
окраски и высоты кнопок только для уже затронутых зон: таблицы «Новые»,
рабочего места поставки и модалки печати. Этот атом закрывает находку об
отсутствующем выводе `scripts/ui/invariants.js`, не расширяя контракт на
остальные экраны и не повышая baseline.

Файлы: `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx`.

Зависит от: фичи 1, 2, 3, 4 и 5 — вывод должен опираться на те же живые
состояния, а не заменять их.

Как проверить: запустить
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/scripts/ui/invariants.js` на созданных браузерных
снимках, приложить дословный вывод к продуктовой проверке и сохранить все
использованные снимки в
`/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/evidence/05-prod-slow/`. Любое нарушение
оформляется отдельной новой находкой, а не исправляется в этом атоме вслепую.

## Порядок

Фичи 1–5 независимы и могут идти параллельно: они проверяют разные состояния
и не предполагают изменения кода. Фича 6 идёт последней, потому что её вывод
должен быть привязан к тем же снимкам и живым состояниям. Если одна из фич
1–5 обнаружит реальное расхождение, её атом заканчивается артефактом
`PRODUCT_REWORK_REQUIRED`; новая dev-карточка создаётся только для этого
доказанного расхождения и не подменяет остальные проверки.

## Что осталось за бортом

- Переписывание или повторная реализация `FfFbsOrdersScreen.tsx`,
  `FfFbsSupplyWorkspace.tsx` и `MarkingPrintDialog.tsx`: `JUDGE.md` не
  доказал дефект, а контракт прямо запрещает возвращать принятую карточку к
  полной разработке.
- Превышения baseline `ui_guard.py`: они за границей контракта и не имеют
  доказанного нарушения канона в `JUDGE.md`.
- Производительность, инфраструктура, расписание импорта и marking metadata:
  в вердикте по ним нет новых незакрытых находок; границы карточки 05 из
  `ARCH-CROSS.md` не расширяются.
- Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод
  `194.87.96.144` и живой кабинет Wildberries не читались и не затрагивались.
