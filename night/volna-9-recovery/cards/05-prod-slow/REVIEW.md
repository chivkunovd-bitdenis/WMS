# Ревью · 05-prod-slow · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-marking-print-constructor.spec.ts:119` — пункт 3 предыдущего ревью не закрыт: тест состояния `preparing` по-прежнему закрывает диалог только клавишей Esc (строки 125–136), но не проверяет отсутствие кнопки «Закрыть» и вообще любых действий в карточке, не закрывает диалог кликом по затемнённому backdrop и не связывает сценарий с назначенным `S-03-TC-018`. Сценарий: вернуть в `TapePreparationStatus` запрещённую одиночную кнопку либо сломать обработку клика по backdrop, оставив Esc рабочим, — Playwright останется зелёным. Цена — обязательный пользовательский кейс и правило R-31 не защищены от прямой регрессии, хотя компонент сейчас реализован верно.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/ui-kit/inventory.generated.ts:14` — пункт 4 предыдущего ревью не закрыт: в ветке остаётся глобальная перегенерация 281 строки UI-инвентаря с данными `MovementsScreen`, `FfFbsStockSyncScreen`, `SellerInboundDraftScreen`, `FfInboundRequestView` и других соседних экранов. Текущие `CONTRACT.md` и `FEATURES.md` разрешают точечное изменение таблицы S-03 и общей модалки и прямо фиксируют, что нового ui-kit не требуется; ремонт после прошлого вердикта этот файл не менял и не отделял. Сценарий: принять карточку 05 владельцем глобального снимка — подписи, чипы и предупреждения соседних карточек попадут в showcase и последующие проверки без их контрактного и тестового прохода. Цена — нарушена граница контракта и потеряна однозначная атрибуция сгенерированного инвентаря; файл нужно вынести к карточкам-владельцам либо сузить до результата этой карточки.

## Проверено и нормально

- Замороженный чек-лист предыдущего `REVIEW.md` проверен полностью. Пункт 1 закрыт: тест поиска теперь требует отсутствие жёлтой заливки и в обычном состоянии, и при наведении; код сохраняет найденную строку видимой без цветового сигнала.
- Пункт 2 закрыт: таблица вкладки «Новые» получила `table-layout: fixed`, общую ширину 713 px и четыре фиксированные ширины 210/135/180/140 px; тест проверяет алгоритм раскладки, общую и фактические ширины, однострочность заголовков.
- Ремонтный продуктовый diff после прошлого вердикта прочитан целиком. Он меняет только проверки ширин в `ff-fbs-orders.spec.ts` и стадийные отчёты; новых операторских блокировок, серверных правил, изменений данных или формата API в нём нет. Границы `frontend/screens.registry.json` для исполняемого ремонта соблюдены.
- `npx tsc --noEmit -p tsconfig.app.json`, 142 frontend unit-теста и `git diff --check` прошли. Адресный Playwright-прогон двух ремонтных сценариев не стартовал из-за запрета среды на bind `127.0.0.1:18000`; это не считается доказательством прохождения браузерных тестов. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод и живой кабинет Wildberries не читались и не затрагивались.
