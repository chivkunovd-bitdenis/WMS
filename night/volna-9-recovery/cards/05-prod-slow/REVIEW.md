# Ревью · 05-prod-slow · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 4

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-fbs-orders.spec.ts:733` — существующий пользовательский тест после поиска по-прежнему требует от найденной строки фон `rgba(255, 214, 102, 0.24)`, хотя новый контракт и код эту жёлтую заливку удаляют. Сценарий: запустить `fbs orders: search keeps list, selected drawer stays stable and Excel downloads` после ремонта R-11 — вычисленный фон будет обычным, а ожидание на строке 735 упадёт. Цена — обязательный Playwright-набор карточки красный и одновременно закрепляет прямо противоположное продуктовому правилу поведение; такое изменение нельзя вливать до исправления проверки на отсутствие жёлтой заливки, включая hover.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1278` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1292` — значения `width` поставлены только на ячейки заголовка, но сама MUI-таблица остаётся с CSS-алгоритмом `table-layout: auto` и шириной 100%. В этом алгоритме `width` ячейки участвует как ограничение раскладки, но не запрещает колонке расширяться по содержимому или за счёт свободной ширины контейнера. Сценарий: открыть «Новые» на широком рабочем экране либо получить от WB длинный маршрут; браузер перераспределит свободное место/минимальную ширину содержимого, и фактические колонки перестанут быть 210/135/180/140 px, хотя заголовки останутся однострочными. Цена — S-03-TC-016 и прямой контракт «ширины остаются фиксированными и не расширяются из-за длинных данных» не выполнены; автоматической проверки фактических ширин в `ff-fbs-orders.spec.ts` также нет.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-marking-print-constructor.spec.ts:119` — ремонтный тест закрывает модалку только клавишей Esc (строки 126 и 135), но не проверяет отсутствие `marking-print-close-preparing`/любых действий в карточке и ни разу не закрывает её кликом по затемнённому backdrop. Сценарий: вернуть запрещённую одиночную кнопку «Закрыть» либо сломать обработку backdrop, оставив Esc рабочим, — этот тест останется зелёным, хотя назначенный S-03-TC-018 будет нарушен. Цена — тест проверяет лишь часть поведения и не защищает именно R-31 и второй обязательный штатный способ закрытия; кроме того, в заголовке теста/комментарии нет назначения `S-03-TC-018`, там остаются только старые S-03-TC-008/009.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/ui-kit/inventory.generated.ts:14` — ремонтный diff меняет 281 строку глобального сгенерированного инвентаря: добавляет и удаляет подписи и ссылки на `MovementsScreen`, `FfFbsStockSyncScreen`, `SellerInboundDraftScreen`, `FfInboundRequestView` и другие соседние экраны, хотя текущие `FEATURES.md` и `CONTRACT.md` разрешают только две точечные правки S-03/общей модалки и не требуют переинвентаризации остальных экранов. Сценарий: принять карточку 05 как источник этого файла — изменения кнопок, чипов и alert соседних карточек попадут в UI-showcase и последующие проверки как будто были рассмотрены в рамках 05, хотя их исходный код и кейсы в этом повторном проходе не относятся к ремонту. Цена — нарушена граница контракта и теряется однозначная атрибуция UI-инвентаря; глобальную регенерацию нужно вынести к карточке-владельцу либо сузить diff до данных, вызванных этой карточкой.

## Проверено и нормально

- Замороженный чек-лист предыдущего `REVIEW.md` проверен целиком. Потерявший lease worker теперь откатывается, а финальный статус пишет через условный `UPDATE` по `status=running` и собственному `started_at`; адресные lease/heartbeat-тесты прошли (`4 passed`). Дублирующий идентификатор блокировки устранён: в `docs/blockers/S-03.md` остался один `B-13`, блокировка повторной печати обозначена `B-14`.
- Прочитан только ремонтный продуктовый diff после прошлого вердикта: `MarkingPrintDialog.tsx`, `FfFbsOrdersScreen.tsx`, `inventory.generated.ts` и `ff-marking-print-constructor.spec.ts`, а также назначенные S-03-TC-016…022 и S-04-TC-002. `MarkingPrintDialog.tsx` действительно убирает одиночную кнопку в `preparing`; жёлтая ветка строки поиска удалена без поломки `scrollIntoView`.
- Границы `frontend/screens.registry.json` соблюдены для исполняемых файлов: `FfFbsOrdersScreen.tsx` принадлежит S-03, `MarkingPrintDialog.tsx` зарегистрирован как shared для S-03, а `frontend/src/ui-kit/` разрешён отдельно. Стадийные материалы `night/`, `tests/cases/` и инфраструктура конвейера не считались продуктовым выходом за границы. Новых операторских блокировок в ремонтном diff нет.
- `npx tsc --noEmit -p tsconfig.app.json`, 142 frontend unit-теста, адресные backend lease-тесты и `git diff --check` прошли. Адресный Playwright-прогон не стартовал из-за запрета локального bind на `127.0.0.1:18000`, но противоречащее контракту ожидание на строках 733–735 установлено чтением самого теста. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод и живой кабинет Wildberries не читались и не затрагивались.
