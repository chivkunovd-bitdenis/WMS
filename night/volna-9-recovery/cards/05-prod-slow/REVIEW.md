# Ревью · 05-prod-slow

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 10

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:374` — эффект сброса вызывает `setPreparedTapePdf(null)`, но такого состояния и setter в компоненте нет: объявлен только `setPreparedTapeAssetId`. Сценарий: любая production-сборка TypeScript компилирует этот файл и останавливается с `Cannot find name 'setPreparedTapePdf'`. Цена — frontend нельзя собрать и выпустить; локально `npm run build` дополнительно не стартовал из-за отсутствующего `tsc`, но неопределённое имя подтверждено чтением полного файла.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/api/marking_codes.py:1284` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:43` — активный job сначала коммитится в БД, а затем отдельно публикуется в Celery; повторный запрос намеренно не публикует найденный `pending/running` job. Сценарий: Redis недоступен в момент `apply_async` либо worker погиб после фиксации `running` на строках 398–403; клиент получает ошибку или таймаут, но все повторы тех же данных возвращают навсегда активную запись, которую никто больше не подхватит. Цена — конкретную ленту больше нельзя собрать без ручной правки БД; тест `test_marking_label_tape_worker_does_not_reclaim_running_job` закрепляет отказ от повторного подхвата, но восстановления зависших заданий нет.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/wb_marketplace_orders_service.py:1535` — полный обход больше не имеет произвольного лимита, но не проверяет повтор уже виденного `next_token`. Сценарий: WB из-за сбоя возвращает непустую страницу и тот же курсор два раза подряд; цикл бесконечно запрашивает и коммитит одну страницу, держит single-flight lock и расходует лимит WB. Цена — часовая сверка зависает, создаёт постоянную внешнюю нагрузку и больше не запускается для этого продавца; тесты проверяют только конечную последовательность разных курсоров.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:634` — polling не связан с контекстом, для которого был запущен, и после закрытия продолжает записывать общий `preparedTapeAssetId`/`tapePreparation`. Сценарий: оператор запускает ленту товара A, закрывает диалог и открывает товар B; завершившийся старый promise выставляет в новом диалоге `ready` и идентификатор PDF товара A, после чего «Открыть для печати» печатает чужую ленту. Цена — этикетки одного товара или документа наклеиваются на другой; теста на закрытие и быстрое открытие другого контекста нет.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:523` — любой запрос вкладки «Новые» при уже показанных строках считается фоновым тиком, после чего строки безусловно объединяются на строках 554–565. Сценарий: оператор меняет селлера или склад — новые 50 строк добавляются к заказам старого фильтра, а `nextCursor` остаётся от прежней выдачи; аналогично заказ, ушедший из `new`, после 30-секундного тика остаётся в таблице навсегда. Цена — экран смешивает склады/селлеров и позволяет выбрать уже неактуальный заказ, хотя контракт требует обновлять первую порцию, сохраняя только действительно догруженный хвост.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1412` — общее пустое тело таблицы для всех неснабженческих вкладок жёстко показывает «Новых заказов пока нет». Сценарий: вкладка «Просрочены» или «Отменённые» возвращает пустой список; оператор видит состояние вкладки «Новые» и обещание автоматической загрузки из WB вместо прежнего состояния текущей группы. Цена — прямое нарушение контракта «остальные вкладки не меняются» и неверное объяснение пользователю.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:661` — при открытии готового asset любой отказ, включая серверный `asset_expired`, переводится в `failed`; текст исключения из `fetchPreparedCzArtifactTapePdf` отбрасывается. Сценарий: PDF истёк между состоянием `ready` и кликом «Открыть для печати»; вместо контрактного «Срок хранения ленты истёк» оператор видит «Не удалось собрать ленту». Цена — отдельное состояние истечения фактически недостижимо в предусмотренном контрактом сценарии и причина нужного повтора скрыта.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-fbs-orders.spec.ts:178` — один happy-path тест помечен сразу `S-03-TC-001`–`005`, хотя не нажимает «Выбрать все», не наблюдает скелет и не получает пустой ответ; `S-03-TC-006`–`015` вообще не представлены в разрешённых E2E-файлах. Сценарий: оставить поломки смены фильтра, скрытого тика, ошибки/двойного клика догрузки, закрытия диалога, ошибки и истечения PDF — Playwright останется зелёным. Цена — машинная трассировка заявляет покрытие, которого нет, и не защищает основные результаты карточки.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:120` — экран S-03 разрешает собственные файлы из строк 120–126 и `frontend/src/ui-kit/`, но продуктовый diff меняет `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts`; реестр относит их соответственно к S-14/S-15 на строках 1119–1121 и S-09 на строках 629–637. Сценарий: общая печать меняется на соседних экранах без отдельного экранного dev/test-прохода, что уже проявляется находками 1, 4 и 7. Цена — нарушение обязательной машинной границы и непроверенная регрессия трёх экранов.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:1053` — появился новый операторский запрет повторной печати при `preparing/ready`, но в `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md` нет записи с шестью обязательными полями; для фактических владельцев общего диалога S-14/S-15 постоянных файлов реестра блокировок также нет. Серверный слой запрет повторного активного job поддерживает частичным уникальным индексом, то есть слои совпадают, но сценарий, видимое объяснение и способ разблокировки не зарегистрированы. Цена — новый запрет выпадает из обязательного аудита блокировок и утренний сборщик не сможет проверить его согласованность после следующих изменений.

## Проверено и нормально

- Весь продуктовый diff от базового `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow` commit `d62f9af` прочитан; изменённые продуктовые файлы совпадают с переданным списком, а `night/` и `tests/cases/` оценивались только как стадийные артефакты.
- Очередь `print` действительно вынесена в отдельный Celery worker с `--concurrency=1`; обычный worker слушает только `celery`. Частичный уникальный индекс активных job теперь одинаково задан для PostgreSQL и SQLite, завершённый `failed/done` job не блокирует новый запуск.
- Первая порция «Новых» ограничена 50 строками, четыре информационные колонки восстановлены, `TableLoadMore` блокирует локальный повторный клик и не удаляет строки при ошибке. Автоматическое открытие popup до готовности и провал серверной ошибки в HTML-fallback устранены.
- `git diff --check` прошёл; адресный backend-набор `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/tests/test_background_jobs.py`, `test_fbs_print_assets.py`, `test_marking_pdf_label_artifact.py`, `test_wb_marketplace_orders_service.py` — 44 passed. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод и живой WB не читались и не затрагивались.
