# Ревью · 05-prod-slow · повторный проход

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 2

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:558` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:578` — ремонт находки №2 поддерживает lease heartbeat-ом, но при доказанной потере lease старый worker всё равно без проверки владельца записывает `failed` через ранее загруженный ORM-объект `job`. Сценарий: первый worker зависает дольше 15 минут во время недоступности БД, второй worker захватывает протухший job и успевает собрать PDF, после чего первый worker оживает, видит `lease_lost` и коммитит `status=failed`, `result_json=None` поверх результата нового владельца. Цена — готовый asset остаётся без доступной ссылки, job ложно становится ошибочным, оператор запускает повторную тяжёлую сборку; добавленный тест на строке 326 проверяет только исправный heartbeat и не передаёт lease другому worker.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md:222` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md:257` — пункт №6 предыдущего `REVIEW.md` не исправлен: блокировка повторной печати ленты и блокировка передачи поставки в доставку по-прежнему имеют один идентификатор `B-13`. Сценарий: тестировщик, утренний разбор или автоматизация получает ссылку `S-03/B-13` и не может однозначно выбрать между проверкой фоновой печати и проверкой статуса `packed`. Цена — обязательная трассировка новой блокировки остаётся неоднозначной, поэтому замороженный пункт ремонта не закрыт.

## Проверено и нормально

- Замороженный чек-лист из шести пунктов проверен целиком, затем прочитан только ремонтный продуктовый diff после вердикта. Новые продуктовые файлы не вышли за переданный список; стадийные артефакты `night/` не считались выходом за границы.
- Находка №1 закрыта: `FbsBindingStockPool` читается с `FOR UPDATE`, поэтому разные заказы контуров `new` и `reconcile` последовательно уменьшают один пул; повтор одного заказа по-прежнему защищён уникальным журналом списаний.
- Находки №3–5 закрыты: устаревший обход `select all` отбрасывается по поколению и ключу фильтра; трекер сохраняет отдельные сессии A/B; только серверный `asset_expired` показывает истечение, а временная ошибка повторяет выдачу того же PDF без нового job.
- Адресные backend-наборы прошли: `59 passed`; frontend build, `4` unit-теста `TableLoadMore` и сборка списка из `19` Playwright-сценариев прошли. `git diff --check` зелёный. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой production и живой кабинет Wildberries не читались и не затрагивались.
