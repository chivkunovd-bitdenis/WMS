# Ревью · 05-prod-slow

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 15

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/api/marking_codes.py:1294` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docker-compose.prod.yml:66` — задание отправляется в очередь `print`, но единственный production-worker запущен без подписки `-Q print` и в Celery не объявлен маршрут/список потребляемых очередей. При настроенном брокере оператор получит `202`, а job навсегда останется `pending`; основная печатная функция карточки на стенде не работает.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/api/marking_codes.py:1291` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:375` — повторный POST для уже найденного `pending` job снова ставит тот же `job_id` в очередь, а worker без атомарного захвата безусловно переводит любую строку в `running` и создаёт новый asset. Два запроса до старта worker либо повторная доставка Celery одновременно соберут одну ленту дважды и дадут два пика CPU/RAM; последний commit затрёт `result_json` ссылкой на один из двух файлов. Предварительный `SELECT` в `create_pending_job` (`background_job_service.py:41`) также не обрабатывает гонку двух одновременных INSERT и вместо возврата существующего job способен отдать 500 по unique violation.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/celery_app.py:26` — старый `fbs-orders-autopoll` оставлен рядом с новым `wb-orders-new`, оба запускаются раз в 180 секунд. Старый путь всё ещё последовательно проходит продавцов и держит `wb_seller_lock` во время HTTP-чтения (`backend/app/services/fbs_autopoll_service.py:349`), поэтому каждый частый цикл дважды читает `/orders/new`, а ручная операция продавца по-прежнему может быть заблокирована. Это прямо сохраняет часть исходной нагрузки и нарушает решение «один независимый new-контур без общего лока».

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/fbs_autopoll_service.py:38` — single-flight хранится в `defaultdict` с `asyncio.Lock`, то есть только в памяти одного процесса. Два Celery prefork-worker или две ноды одновременно примут одинаковый `(seller_id, sync_kind)` и оба получат собственный lock; часовой reconcile задублирует полный внешний обход и конкурентные upsert. Тест на строках 51–63 проверяет лишь вложенные контексты в одном event loop и остаётся зелёным при этой поломке.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/wb_marketplace_orders_service.py:1536` — reconcile ограничен `MAX_ORDERS_PAGES`, но после десятой страницы возвращает успешный результат, даже если `next_token` не равен `None`. У продавца с более чем десятью страницами хвост заказов не будет сверен, хотя контракт требует пройти курсор до конца и не считать незавершённый проход успешным.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/wb_marketplace_orders_service.py:1467` — из обоих новых контуров удалён вызов `link_confirmed_orders_to_wb_supplies`, а других production-вызовов этой функции нет. Когда WB подтвердит заказ и назначит внешнюю поставку, локальная карточка поставки больше не создастся и заказ останется «Поставка создана в WB, недоступна в WMS»; это регресс старого `sync_seller_orders` и противоречит контракту локальной сверки связей после полного прохода.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/fbs_print_asset_service.py:697` — срок 12 часов только запрещает чтение, но файл не удаляется и задачи уборки в диффе нет. При регулярной печати лент по 155–500 кодов PDF будут бессрочно накапливаться в `wms_data_dir`; решение продукта прямо требует удалять файл после 12 часов, а исходная задача требует получить запас памяти/диска, а не новый неограниченный склад бинарников.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1280` — вкладка «Новые» оставлена с пятью информационными колонками (`Товар`, `Заказ и сканирование`, `Селлер`, `Склад селлера / WB`, `Создан WB / в сборке`) вместо четырёх эталонных `Товар`, `Селлер`, `Маршрут сдачи`, `Отгрузить до`. При открытии S-03 пользователь получает закреплённый в AGENTS/CONTRACT старый регресс, хотя карточка обязана была наложить пагинацию на актуальную JSX-строку `etalon`.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:553` — вкладки «В работе», «В доставке» и «Завершённые» всё ещё одновременно запрашивают два списка с `limit: 500`. При каждом 30-секундном обновлении рабочее место продолжит получать до тысячи строк, хотя контракт этой карточки ограничивает первую порцию этих вкладок ста строками.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:576` — фоновый тик правильно сохраняет уже догруженные строки, но строка 587 заменяет текущий `nextCursor` курсором первой страницы. После загрузки первых 100 строк и 30-секундного тика первый клик «Показать ещё» повторно загрузит вторую страницу, визуально не добавит ни одной строки из-за дедупликации и только вернёт курсор к третьей странице; оператор увидит неработающий клик и лишний сетевой запрос.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1428` — обязательные состояния первой порции не реализованы: вместо `TableSkeletonBody` показывается отдельный спиннер «Обновляем рабочий список…», пустая вкладка говорит «Заказов в этой группе нет / Измените фильтры…» вместо текста про автоматическую загрузку WB, а ошибка на строках 1116–1119 не даёт действия «Повторить». На медленном, пустом или временно упавшем API экран расходится с тремя явными пунктами UX-контракта.

12. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:628` — ошибка фоновой подготовки перехватывается внутри `deliverTape` и молча переводит сценарий в старый HTML fallback; состояния `failed`/`expired` не выставляются, поэтому оператор не увидит обещанные «Повторить» и «Закрыть». Если fallback тоже упадёт, внешний `handlePrint` читает устаревшее значение `tapePreparation` из текущего render и также может оставить диалог в вечном «Готовим к печати».

13. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:1706` — состояние `ready` рендерится дважды: сначала через `TapePreparationStatus`, затем отдельным вторым блоком. У второго блока обработчик `onClick={() => void openPreparedTape}` не вызывает функцию. После готовности оператор увидит две одинаковые главные кнопки, одна из которых ничего не делает, вместо ровно одного действия по контракту.

14. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-fbs-orders.spec.ts:180` — изменённый тест утверждает `limit=50`, но в ответ на этот запрос сразу отдаёт все 500 строк и проверяет строку №500; `next_cursor`, «Показать ещё», двойной клик, ошибка страницы, сохранение выбора и фоновый тик не участвуют. Такой тест остаётся зелёным, если курсорная логика полностью сломана. Для диалога печати ни один из назначенных `S-03-TC-008/009/014/015` также не добавлен, поэтому находки 10, 12 и 13 автоматикой не обнаруживаются.

15. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:119` — заявленный S-03 разрешает только файлы из массива строк 120–126 и `frontend/src/ui-kit/`, однако дифф меняет `frontend/src/components/MarkingPrintDialog.tsx` (реестр относит его к S-14/S-15 на строках 1119–1121) и `frontend/src/utils/printMarkingCodeLabel.ts` (принадлежит `HonestSignProductPage` на строках 627–637). Это выход за экранные границы карточки; конкретная цена уже проявилась: общий диалог S-14/S-15 изменён без их назначенных Playwright-сценариев. Новая блокировка повторной печати при `preparing/ready` (`MarkingPrintDialog.tsx:1053`) также не описана шестью полями в `docs/blockers/S-14.md`/`S-15.md`, а серверный слой ей не соответствует — повторный POST разрешён и повторно ставит job в очередь, как указано в находке 2.

## Проверено и нормально

- Частый сервис `sync_new_orders_for_seller` действительно не вызывает постраничный `/orders`, а сетевые ошибки reconcile не превращаются в обычный успешный return.
- Print asset проверяется по tenant, checksum и `expires_at`; PDF не кладётся в `BackgroundJob.result_json`, там хранится только `asset_id`.
- `TableLoadMore` скрывается без следующего курсора, блокирует повторный клик на время локального запроса и сохраняет действие после ошибки; догрузка дедуплицирует строки и не очищает выбранные ID.
- Целевой backend-прогон завершился успешно: 37 тестов passed. `npm run build` не состоялся, потому что в рабочей копии отсутствует бинарник `tsc` (`sh: tsc: command not found`); production, живой WB, секреты, ключи, токены и `.env` не открывались и не затрагивались.
