# Ревью · 05-prod-slow

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 10

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:120`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:629` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:1119` — заявленный экран S-03 разрешает собственные файлы строк 120–126, его `shared` и `frontend/src/ui-kit/`, но продуктовый diff меняет `frontend/src/utils/printMarkingCodeLabel.ts`, принадлежащий S-09, и `frontend/src/components/MarkingPrintDialog.tsx`, зарегистрированный для S-14/S-15. Сценарий: общий контракт печати меняется сразу на экранах ЧЗ и упаковки без их отдельного экранного dev/test-прохода. Цена — нарушение обязательной границы экрана и непроверенная регрессия соседних S-09/S-14/S-15.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docker-compose.prod.yml:26` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docker-compose.prod.yml:80` — API и новый `print_worker` не имеют общего `WMS_DATA_DIR` и общего volume, хотя worker сохраняет готовый PDF локально через `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:462`, а API читает его из своей файловой системы. Сценарий: в `docker-compose.prod.yml` worker успешно завершает job и записывает `asset_id`, оператор нажимает «Открыть для печати», но API-контейнер не видит файл из файловой системы worker-контейнера и отвечает `asset_not_ready`. Цена — вынесенная в очередь печать на продовой схеме не выдаёт ни одной готовой ленты; уборщик в другом контейнере дополнительно удалит строку БД, но оставит физический файл занимать диск worker-контейнера.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/wb_marketplace_orders_service.py:1448` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:321` — публично используемый `sync_seller_orders` превращён в alias только лёгкого импорта `new`, поэтому прежняя ручная ручка `/operations/fbs-orders/sync` больше не обновляет статусы, не освобождает резерв отменённого заказа и не привязывает подтверждённую WB-поставку. Сценарий подтверждён адресным запуском: `test_fbs_order_status_sync_supplier_confirm_moves_new_to_external_processing`, `test_fbs_order_status_sync_links_external_wb_supply` и `test_fbs_order_status_sync_releases_reserve_on_cancel` падают соответственно на неизменённом `wb_status`, отсутствующем `supply_link_candidates` и отсутствующем `statuses_updated`. Цена — оператор нажимает существующую синхронизацию, но отменённый заказ продолжает держать остаток, а принятый WB заказ остаётся в неверной вкладке и без поставки; это также ломает обратную совместимость результата job.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/tasks/background_jobs.py:95` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docker-compose.prod.yml:66` — диспетчер публикует задания всех продавцов подряд без равномерного распределения по трёхминутному/часовому интервалу, а обычный Celery worker не ограничен требуемыми двумя одновременными сетевыми sync. Сценарий: у 25 продавцов с токеном один beat-тик мгновенно ставит 25 `new` jobs, и worker запускает столько сетевых запросов, сколько даёт его стандартная concurrency. Цена — карточка «прод тормозит» сама создаёт пиковый залп к WB, конкуренцию за БД и новые 429 вместо контрактного сглаживания нагрузки.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:442` — новый worker вызывает прежний `build_label_artifact_tape_pdf`, который на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/marking_code_service.py:658` складывает все исходные BLOB в `parts`, а затем `merge_label_artifact_pdfs_for_print` создаёт второй полный список преобразованных PDF. Сценарий: лента из 500 крупных сохранённых этикеток одновременно держит в памяти исходные BLOB, преобразованные BLOB, документ PyMuPDF и итоговый PDF. Цена — API изолирован, но единственный print worker всё ещё может уйти в OOM и потерять всю очередь; прямой контракт «читать последовательно, не удерживая список всех BLOB» не выполнен.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:48`, `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:1063` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md:222` — сервер переиспользует одинаковый job только в `pending/running`, а после `done` создаёт новый, хотя архитектурный контракт требует вернуть и готовое задание; интерфейс при `ready` повтор запрещает. Сценарий: после успешной сборки тот же запрос повторяют через API или после закрытия и нового открытия диалога — сервер создаёт второй PDF и второй job, тогда как открытый диалог говорит, что повтор заблокирован. Цена — расхождение слоёв нового запрета, лишняя тяжёлая сборка и риск повторной печати этикеток; тест `test_finished_marking_job_can_be_retried_with_same_idempotency_key` закрепляет неверное для `done` поведение. Запись блокировок дополнительно неоднозначна: новый пункт назван `B-13`, хотя второй `B-13` уже начинается на строке 257.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:490` — фоновая уборка после удаления файла удаляет и сам `FbsPrintAsset`, хотя контракт требует оставить запись без бинарного содержимого для аудита. Сценарий: проходит 12 часов, beat удаляет asset-строку, а `BackgroundJob.result_json` продолжает ссылаться на исчезнувший `asset_id`. Цена — невозможно доказать, какой артефакт был собран, ссылка задания становится висячей, и расследование повторной/ошибочной печати теряет данные.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:613` — догрузка не имеет номера запроса или проверки актуальности фильтра и на строке 620 добавляет ответ к любому текущему списку. Сценарий: оператор нажимает «Показать ещё» у селлера A или склада X и, пока запрос летит, переключает фильтр на B/Y; если первая страница нового фильтра ответит раньше старой догрузки, старые строки A/X будут добавлены к новой таблице B/Y. Цена — на одном экране смешиваются продавцы/маршруты, после чего оператор может выбрать и отправить в поставку не тот заказ; существующий E2E проверяет только смену фильтра без одновременно летящей догрузки.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:586` — фоновое обновление первой порции намеренно не принимает новый `next_cursor`, даже когда хвост ещё не догружен. Сценарий: первоначально показаны заказы 1–50 с курсором после 50; перед тиком сверху приходит новый заказ, обновлённая первая страница содержит новый + 1–49, но код сохраняет старый курсор после 50, и последующий «Показать ещё» начинает с 51 — заказ 50 пропадает из доступного списка. Цена — оператор не видит и не может выбрать часть новых заказов; тест polling подменяет одну строку другой и поэтому не моделирует сдвиг курсора при вставке.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-fbs-orders.spec.ts:178` — в разрешённом браузерном наборе присутствуют `S-03-TC-001`–`007` и `010`–`012`, но отсутствуют назначенные `S-03-TC-008`, `009`, `013`, `014`, `015`: подготовка и явное открытие PDF, повторное открытие активной задачи, параллельная вкладка, безопасная ошибка и истечение. Сценарий: сломать polling/закрытие диалога, повтор готового job или состояние истёкшего asset — Playwright останется зелёным. Цена — главная пользовательская часть асинхронной печати и критические края карточки не имеют проверки видимого результата; backend happy-path не заменяет браузерный сценарий.

## Проверено и нормально

- Полный продуктовый diff карточки от базового commit `d62f9af` прочитан; изменённые продуктовые файлы совпадают с переданным списком, а `night/`, `tests/cases/` и иные стадийные материалы не считались выходом за границы реализации.
- Разделение `new`/`reconcile`, защита от циклического WB-курсора, PostgreSQL advisory lock по `(seller, sync_kind)`, частичный уникальный индекс активных print jobs и восстановление stale `running` job сами по себе реализованы последовательно; Alembic показывает единственную голову `20260822_0050`.
- Первая порция «Новых» ограничена 50 строками, остальные рабочие вкладки — 100; `TableLoadMore` блокирует локальный двойной клик, сохраняет строки при ошибке, а видимый 30-секундный тик прекращается при `document.hidden`. Отложенный результат закрытого диалога защищён generation-счётчиком, отдельное состояние истечения отображается.
- `git diff --check` прошёл. Адресные изменённые backend-наборы — 49 passed; три теста прежнего FBS intake-контракта отдельно запущены и упали по находке 3. `npm run build` не стартовал из-за отсутствующего `tsc` в рабочей копии. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод и живой WB не читались и не затрагивались.
