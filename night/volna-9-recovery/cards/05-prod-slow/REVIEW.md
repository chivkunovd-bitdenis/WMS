# Ревью · 05-prod-slow

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 11

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docker-compose.prod.yml:66` — очередь `print` подключена к общему Celery worker без отдельного процесса и без `--concurrency=1`. Celery по умолчанию поднимает несколько prefork-процессов, поэтому два разных задания ленты могут одновременно собирать PDF, а тяжёлая печать конкурирует с обычными задачами очереди `celery`. Сценарий: два оператора почти одновременно запускают ленты по 500 кодов; обе сборки идут параллельно и возвращают исходный пик CPU/RAM, хотя контракт требует один последовательный worker очереди `print`. Цена — повтор исходной остановки API и риск исчерпания памяти на стенде.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/models/background_job.py:21` — активная уникальность описана только через `postgresql_where`; для SQLite SQLAlchemy компилирует этот индекс как обычный уникальный индекс по `(tenant_id, job_type, idempotency_key)` без условия статуса. Сценарий: локальный/E2E job завершился `failed` или `done`, оператор нажимает «Повторить», `create_pending_job` пытается вставить новую строку с тем же ключом и получает `IntegrityError`, потому что завершённая строка навсегда занимает ключ. Цена — повтор печати в штатном локальном режиме и тестовом стенде отвечает 500; тесты зелёные, потому что проверяют только два активных `pending` job.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:631` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:1086` — на всё время polling родительский `busy=true`, а `Dialog.onClose` разрешает закрытие только при `!busy`; кнопка отмены также disabled. Сценарий: оператор запускает длинную ленту и хочет продолжить работу в WMS, как прямо обещает текст состояния, но модальный диалог нельзя закрыть ни кнопкой, ни Escape, ни кликом вне окна. Цена — рабочее место фактически заблокировано до конца фоновой задачи; контракт «закрытие не отменяет job» не реализован.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:942` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts:498` — первое нажатие «Печать» синхронно вызывает `window.open` ещё до создания фонового job. Сценарий: браузер разрешает popup, оператор нажимает «Печать» и сразу получает пустую вкладку «Загрузка PDF…», хотя PDF ещё не готов и контракт запрещает автоматическое открытие до явного действия «Открыть для печати». Цена — лишняя вкладка висит всё время сборки и пользовательский сценарий расходится с утверждённым UX.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:636` — ошибка `prepareCzArtifactTape` переводит состояние в `failed`, затем бросается во внешний `catch` той же функции; строки 643–652 трактуют её как отказ native PDF и запускают старый HTML-fallback. Сценарий: Celery job завершился `failed` либо GET артефакта вернул ошибку, но браузер успешно собрал HTML из исходных кодов; диалог вызывает `onPrinted()` и закрывается вместо показа «Не удалось собрать ленту / Повторить / Закрыть». Цена — серверная ошибка скрывается, оператор считает фоновую ленту успешно подготовленной, а заявленные состояния ошибки и истечения не работают.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts:478` — клиент скачивает PDF сразу при статусе `done` и хранит `Blob`, а действие «Открыть для печати» на строках `MarkingPrintDialog.tsx:660–664` больше не проверяет серверный срок. Сценарий: диалог остаётся открытым более 12 часов, сервер удаляет истёкший asset, после чего оператор нажимает «Открыть для печати» и печатает ранее скачанный blob. Цена — правило хранения 12 часов обходится на клиенте, состояние `expired` при открытии недостижимо.

7. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts:461` — polling не имеет таймаута, предела попыток или обработки зависшего `pending/running`. Сценарий: worker принял задачу, но погиб до завершения либо очередь недоступна после успешного `202`; статус остаётся `pending`/`running` бессрочно, цикл делает запрос каждые 500 мс, а из-за находки 3 диалог нельзя закрыть. Цена — бесконечная блокировка рабочего места и постоянная нагрузка на API вместо понятной ошибки с повтором.

8. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/tests-e2e/ff-fbs-orders.spec.ts:180` — изменённый тест требует `limit=50`, но мок в ответ на первую страницу отдаёт сразу 500 строк и тест выбирает строку №500. `next_cursor`, «Показать ещё», ошибка догрузки, повторный клик, сохранение выбора и фоновый тик не проверяются; назначенные сценарии диалога `S-03-TC-008/009/014/015` также не добавлены ни в этот, ни в другие разрешённые spec-файлы. Сценарий: удалить всю курсорную догрузку или оставить поломки из находок 3–7 — Playwright останется зелёным. Цена — основной пользовательский результат карточки не защищён регрессией.

9. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/screens.registry.json:120` — S-03 разрешает экранные изменения только в массиве строк 120–126 и `frontend/src/ui-kit/`, но дифф меняет `frontend/src/components/MarkingPrintDialog.tsx` (реестр относит его к S-14/S-15 на строках 1119–1121) и `frontend/src/utils/printMarkingCodeLabel.ts` (файл экрана Honest Sign на строках 628–637). Сценарий: общая печать на S-14/S-15 получает новую асинхронную семантику и блокировки без назначенного этим экранам dev/test-прохода. Цена — выход за машинно заданные границы экрана и непроверенная регрессия соседних сценариев.

10. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:1055` — появились новые блокировки повторной печати при `preparing/ready` и закрытия при `busy`, но они не описаны шестью обязательными полями ни в `docs/blockers/S-03.md`, ни в отсутствующих `docs/blockers/S-14.md`/`S-15.md`. Серверная дедупликация активного job соответствует запрету повторного запуска, но сервер не требует держать диалог открытым. Сценарий: оператор не может повторить или закрыть действие, а утренний сборщик блокировок не знает условия, видимого объяснения и способа разблокировки. Цена — новый операторский запрет прошёл мимо обязательного реестра, а UI- и server-слои описаны неполно.

11. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:576` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1416` — контрактные тексты первой ошибки и пустого состояния не зафиксированы: ошибка выводит произвольный `cause.message`, а пустое состояние называется «Новых заказов нет» вместо «Новых заказов пока нет» и использует другой hint. Сценарий: API отвечает техническим `invalid_cursor`/сетевой строкой либо список действительно пуст; оператор видит не утверждённые складские формулировки, а тест точный текст не проверяет. Цена — прямое несовпадение с контрактом состояний S-03 и возможность утечки технической причины на экран.

## Проверено и нормально

- Разделение WB-контуров в текущем диффе в основном корректно: `new` не обходит исторические страницы, `reconcile` идёт до конца, после полного прохода восстанавливает связи с WB-поставками; старое дублирующее Beat-расписание удалено, PostgreSQL advisory lock разделён по `sync_kind`.
- Активный print-job создаётся идемпотентно на PostgreSQL, повторно не публикуется и атомарно захватывается worker; `result_json` содержит только `asset_id`. Истёкшие `label_tape` удаляются плановой задачей и при попытке чтения.
- Таблица «Новые» восстановлена до четырёх информационных колонок; первая порция ограничена 50, соседние вкладки — 100, фоновый тик не заменяет таблицу скелетом и сохраняет текущий курсор догрузки. `TableLoadMore` блокирует локальный двойной клик и сохраняет действие после ошибки.
- Целевые backend-прогоны прошли: 12 тестов WB-сервиса и все выбранные тесты background jobs/print assets/marking PDF зелёные; `alembic heads` показывает один head `20260822_0050`, `git diff --check` чист. Frontend build не выполнен: локальный `npm run build` завершился `sh: tsc: command not found`. Секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой прод и живой WB не читались и не затрагивались.
