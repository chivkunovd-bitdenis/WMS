# Ревью · 05-prod-slow

Вердикт: CHANGES_REQUESTED.

ВЕРДИКТ: НАХОДКИ 6

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/fbs_autopoll_service.py:96` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/wb_marketplace_orders_service.py:617` — контуры `new` и `reconcile` намеренно имеют разные локи и могут одновременно списывать разные новые заказы из одной строки `FbsBindingStockPool`, но списание читает `quantity` без `FOR UPDATE` и затем записывает вычисленное значение. Сценарий: в пуле 10 единиц, оба контура одновременно читают 10 для двух разных заказов и оба сохраняют 9; при этом создаются две строки `FbsStockPoolDebit` по одной единице. Цена — одна операция списания теряется, доступный остаток завышается, а журнал списаний расходится с самим пулом; тесты покрывают дубль одного заказа, но не два разных заказа одного пула в двух разрешённых контурах.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:145` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/backend/app/services/background_job_service.py:455` — lease фоновой печати считается протухшим через 15 минут только по неизменяемому `started_at`, без heartbeat (периодического подтверждения, что worker жив). Сценарий: медленная сборка 500 тяжёлых PDF всё ещё работает после 15 минут, оператор после клиентского таймаута повторяет запрос, новая Celery-задача захватывает тот же `running` job и начинает вторую сборку параллельно первой. Цена — два тяжёлых процесса, два asset-файла и гонка за финальный `result_json/status`; именно на медленном production это возвращает риск OOM и дублирования ленты, который карточка должна убрать.

3. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:872` — асинхронный обход курсоров для чекбокса «выбрать все» не фиксирует `requestId/queryKey`, в отличие от обычного `loadMore`, и после завершения безусловно добавляет страницы и выделение в текущий экран. Сценарий: оператор запускает «выбрать все» у селлера A, пока обход идёт переключает фильтр на селлера B, после чего ответы A попадают в таблицу и выбор B. Цена — смешение заказов разных селлеров/маршрутов и риск создать поставку или выгрузку не по текущему фильтру; существующий `S-03-TC-003` меняет фильтр отдельно от летящего select-all и эту гонку не ловит.

4. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts:517` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/components/MarkingPrintDialog.tsx:276` — трекер хранит только одну, последнюю сессию подготовки на весь смонтированный диалог. Сценарий: оператор запускает ленту для строки A, закрывает диалог, запускает ленту для строки B, затем снова открывает A; `snapshot(A)` возвращает `idle`, хотя серверное задание A всё ещё `running` или уже `done`. Цена — нарушен прямой контракт «повторное открытие тех же данных показывает состояние уже созданного job»: оператор не видит готовую/активную ленту и вынужден снова нажимать «Печать»; E2E проверяет только закрытие и открытие одной и той же последней сессии.

5. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/frontend/src/utils/printMarkingCodeLabel.ts:448` — любой неуспешный ответ выдачи готового asset классифицируется как истечение срока. Сценарий: API отвечает 500 из-за временной ошибки хранилища, 401 после истечения сессии или 404 `asset_not_ready`, а диалог сообщает «Срок хранения ленты истёк» и предлагает собрать её заново. Цена — оператор получает ложную причину и запускает лишнюю тяжёлую операцию вместо повторного открытия/входа; контракт требует различать ошибку сборки/доступа и реальное истечение 12 часов, тогда как E2E подставляет только заранее известный expired-ответ.

6. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md:222` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/docs/blockers/S-03.md:257` — новая блокировка повторной печати описана шестью полями и подтверждена на обоих слоях, но получила идентификатор `B-13`, уже занятый блокировкой передачи поставки в доставку. Сценарий: тестировщик, утренний разбор или автоматизация ссылается на `S-03/B-13` и не может однозначно определить, проверять очередь ленты или статус `packed`. Цена — новая обязательная блокировка формально записана, но её трассировка и последующая проверка неоднозначны.

## Проверено и нормально

- Полный продуктовый diff от базового commit `d62f9af` прочитан по переданному списку. Изменённые файлы реализации входят в разрешённые границы; `night/`, `tests/cases/`, `docs/evidence/` и унаследованная инфраструктура ночного конвейера не считались выходом за экран.
- Исправления предыдущего ревью подтверждены: ручной `sync_seller_orders` снова сохраняет статусную сверку и привязку поставок; задания WB распределяются по интервалу и ограничены worker concurrency 2; API и print worker используют общий `wms_data`; PDF сливается по одному исходному BLOB; готовый job переиспользуется, а audit-строка asset после очистки остаётся.
- Обычная догрузка защищена от ответа старого фильтра, первая порция ограничена 50 строками, рабочие вкладки — 100, `TableLoadMore` блокирует повторный клик и сохраняет действие после ошибки. Реестр S-03 теперь объявляет оба общих файла печати, поэтому прежней находки по границе экрана нет.
- Адресные backend-наборы прошли: `62 passed`; `npm run build` и 4 unit-теста `TableLoadMore` прошли. Playwright не запустился из-за запрета среды открыть `127.0.0.1:18000`. `git diff --check` зелёный; секреты, ключи, токены, `.env`, кабинеты учётных данных, боевой production и живой кабинет Wildberries не читались и не затрагивались.
