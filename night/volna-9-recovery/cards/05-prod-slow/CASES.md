# Кейсы тестирования · 05-prod-slow

## Назначенные кейсы

- `S-03-TC-001` — первая порция новых FBS-заказов.
- `S-03-TC-002` — догрузка следующей порции без потери работы.
- `S-03-TC-003` — выбор всех новых заказов охватывает курсорный список.
- `S-03-TC-004` — начальная загрузка сохраняет каркас таблицы.
- `S-03-TC-005` — пустая вкладка новых заказов.
- `S-03-TC-006` — автообновление видимой вкладки не прерывает подбор.
- `S-03-TC-007` — остальные рабочие вкладки получают ограниченную первую порцию.
- `S-03-TC-008` — подготовка ленты ЧЗ в фоне и явное открытие PDF.
- `S-03-TC-009` — повторное открытие активной подготовки ленты.

## Добавлено

В общую базу `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-2-05-prod-slow/tests/cases/S-03.md` добавлены постоянные номера
`S-03-TC-001`–`S-03-TC-009` для карточки `05-prod-slow`.

## Находки

Секреты, ключи, токены, `.env`, боевой прод `194.87.96.144` и живой кабинет
Wildberries не открывались и не затрагивались.

## Ломающие кейсы

- `S-03-TC-010` — двойной клик по «Показать ещё» не даёт два запроса, дубль или
  пропуск курсорной порции.
- `S-03-TC-011` — ошибка следующей страницы сохраняет уже подобранные строки и
  возвращает понятный повтор.
- `S-03-TC-012` — скрытая вкладка не запускает видимую перерисовку и при возврате
  не теряет выбор или догруженные строки.
- `S-03-TC-013` — параллельная работа в двух вкладках не выполняет действие над
  устаревшим заказом повторно.
- `S-03-TC-014` — контролируемая ошибка фоновой сборки показывает безопасный
  повтор, без внутренних деталей.
- `S-03-TC-015` — истёкшая через 12 часов лента не открывает старый файл и требует
  осознанно собрать новую.

## Смежные кейсы

- `S-04-TC-001` — рабочее место FBS-поставки: скрытая вкладка не должна
  перерисовать или сбросить контекст оператора. Источник: строка карты
  `05 · prod-slow`, где назван `S-04` с тем же циклом автополлинга; также строка
  260–263 карты отмечает общую точку `FfFbsOrdersScreen.tsx` для 05 и 04.
- `S-03-TC-014` и `S-03-TC-015` дополнительно покрывают общий контракт ленты для
  `HonestSignScreen` и `HonestSignProductPage`: строка 319–321 карты требует
  перепройти печать после переноса `/marking-codes/label-artifact-tape`. Для этих
  экранов в базе пока нет отдельного постоянного номера, поэтому их проверка
  привязана к общему диалогу и его состояниям, без выдумывания нового номера экрана.
