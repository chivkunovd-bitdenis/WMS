ВЕРДИКТ: НАХОДКИ 2

# REVIEW · 03-no-distribution-mode

Вердикт: CHANGES_REQUESTED.

## Находки

1. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-03-no-distribution-mode/backend/app/services/fbs_packing_box_service.py:115-121,193-198,375-380,528-565` — явное выключение режима снимает `no-distribution:` с ключей уже созданных коробов, поэтому повтор исходного запроса создания коробов с тем же ключом перестаёт находить свой результат. Сценарий: `POST /boxes` с `without_distribution=true` и ключом `K` создал короб, затем оператор выключил режим, после чего отложенный повтор исходного POST приходит с теми же `K`, `count` и `without_distribution=true`; сервис ищет `no-distribution:K`, не находит существующий короб с очищенным ключом `K`, создаёт новый локальный короб и доходит до cargo-place операции, которая либо отвечает `idempotency_key_reused` из-за новых `packaging_box_id`, либо при иной реализации внешней идемпотентности создаёт дубль. Цена: штатный повтор операции после смены режима больше не возвращает исходный результат, оператор получает конфликт или дубли коробов/грузомест; добавленные тесты проверяют снятие префикса и обычный повтор по отдельности, но не эту последовательность.

2. `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-03-no-distribution-mode/frontend/tests-e2e/ff-fbs-supply.spec.ts:345-400` — единственный браузерный тест включает режим до создания первого короба и не реализует назначенные карточке `S-03-TC-001`, `S-03-TC-002` и `S-03-TC-003`. Сценарий: вернуть на `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-03-no-distribution-mode/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:2035` исходный запрет `workspace.boxes.length > 0` — тест останется зелёным, потому что кликает галку при пустом списке; он также не удаляет и не пересоздаёт короба, не переоткрывает workspace и не выключает режим. Цена: исходный тупик владельца и потеря состояния после удаления коробов могут вернуться при зелёном E2E-гейте; три заявленных пользовательских критерия не защищены тестом по поведению.

## Проверено и нормально

- Совокупный продуктовый дифф от базового commit `d62f9af` прочитан целиком. Все изменённые файлы реализации входят в разрешённый владельцем список; `night/`, `tests/cases/`, OpenAPI-экспорт и реестр блокировок считал стадийными артефактами, а не выходом за границы. Фронтовые файлы принадлежат экрану S-03 по `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/volna-9-recovery/lane-3-03-no-distribution-mode/frontend/screens.registry.json`.
- Серверное правило и экран согласованы: при назначенных заказах переключатель заблокирован с объяснением, а прямой или конкурентный запрос получает `409 boxes_already_distributed`. Блокировка строки `FbsSupply` сериализует переключение режима с созданием коробов и назначением заказов.
- Старые поставки с `no-distribution:` продолжают читаться как режим без распределения в workspace, readiness и назначении заказа; новое поле поставки переживает пустой список коробов. Формат workspace расширен аддитивно, без удаления прежних полей.
- Миграция добавляющая, её родитель `20260821_0093` существует, внешний ключ удаляется до колонок. `pytest -q tests/test_fbs_packing_box.py tests/test_fbs_openapi_contract.py` — `15 passed`; Playwright локально не запускался, потому что в рабочей копии отсутствуют установленные `playwright` и `vite`.
