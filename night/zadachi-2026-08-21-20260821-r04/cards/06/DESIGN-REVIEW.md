# UI-критика карточки 06

Проверены только зарегистрированный экран S-03 и связанные с карточкой файлы: `FfFbsPickList.tsx`, `FfFbsSupplyWorkspace.tsx`, `FbsPrintPreviewDialog.tsx`, `fbsApi.ts`. Браузер не использовался.

## Находки

| Правило | Файл:строка | Что теряет оператор | Как чинить |
|---|---|---|---|
| R-09 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-6-06/frontend/src/screens/v2/FfFbsPickList.tsx:202` | В таблице нет требуемой первой колонки `№ на ленте`, а значит лист не даёт постоянной точки соответствия с наклейкой; оператору приходится сопоставлять товар и стикер вручную. | Добавить первой колонку `№ на ленте` с фиксированной шириной и выводить в ней серверный номер либо непрерывный диапазон. Не менять состав остальных колонок. |
| R-21 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-6-06/frontend/src/screens/v2/FfFbsPickList.tsx:252` | Сообщение «Нет позиций по фильтру» не говорит, что делать, и одинаково маскирует пустую поставку и пустой результат поиска. Оператор не понимает, нужно ли менять фильтр или добавить заказы. | Разделить пустые состояния: для пустой поставки показать «В листе подбора нет товаров» и совет добавить заказы, для фильтра — «По выбранному фильтру позиций нет» и совет изменить фильтр или поиск. |
| R-22 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-6-06/frontend/src/screens/v2/FfFbsPickList.tsx:195` | Во время загрузки листа показывается маленький спиннер, а тело таблицы остаётся без строк. На длинном рабочем списке это выглядит как пустой или сломанный документ и не показывает, что именно загружается. | Пока ответ не получен, выводить скелет строк таблицы; печать держать недоступной до полной загрузки. |

## Проверено и нормально

- R-07: таблица листа подбора остаётся компактной (`size="small"`) и имеет липкую шапку.
- R-08: количество выровнено вправо.
- R-11: строки таблицы не получают статусную заливку.
- R-23: ошибка загрузки выводится внутри модального окна через `Alert severity="error"`.
- R-32 и R-33: существующая кнопка «Печать стикеров» укладывается в ограничение подписи и называет печатаемый результат.
- Инвентарь `docs/product/ui-inventory.json` не содержит выдуманных для этой карточки названий новых колонок или статусов: причина — сама колонка `№ на ленте` ещё не реализована, что отражено в находке R-09.

## ui_guard.py

Новые отступления есть. `python3 scripts/ui/ui_guard.py` завершился с кодом 1 и сообщил о трёх новых экран-монолитах: `src/components/WbProductPickerDialog.tsx` (0 → 646), `src/screens/v2/FfFbsSupplyWorkspace.tsx` (2493 → 2498) и `src/screens/v2/SellerInboundDraftScreen.tsx` (1111 → 1169). Это сигнал храповика; его строки не внесены в таблицу находок, потому что `ui_guard.py` не сопоставляет их с нумерованным правилом канона, а роль запрещает находки без R-XX.

`python3 scripts/ui/ui_inventory.py` завершился успешно.
