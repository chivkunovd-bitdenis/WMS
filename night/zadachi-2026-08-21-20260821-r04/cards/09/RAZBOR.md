## Дословно

> То же самое с выставлением счетов — вести цифровой учёт работы. Фикс на отгрузку и на
> приёмку. Возможность указывать стоимость либо за документ, либо поштучно по товарам —
> считаем все собранные штуки либо документы, кто сколько собрал всего за период, можем
> выставлять счета по ИНН клиента автоматически с подробной разбивкой. Требования не
> финальные, нужно продумать и проработать модуль.

Из постановки также следуют вопросы об источнике событий, защите от двойного учёта,
переделках и отменах, переходе операции через границу периода и истории тарифов.
Источник: `/Users/deniscivkunov/Projects/WMS/.worktrees/pipeline-etalon/night/zadachi-2026-08-21-20260821-r04/cards/09/ISTOCHNIK.md`.

## Что сейчас

Модуля счетов продавцу нет. В `backend/app/models/seller.py:22-34` нет ИНН и реквизитов
плательщика; в `backend/app/models/tenant.py:26-56` нет реквизитов фулфилмента. Таблицы
`invoices`, `invoice_items`, `tariffs`, миграции и API `/invoices`, `/tariffs`, `/billing`
не найдены. В `frontend/screens.registry.json` нет экранов счетов и тарифов среди S-01…S-32.

Потенциальные источники факта работы уже есть: `inbound_intake_requests.posted_at` и
количества строк (`backend/app/models/inbound_intake.py:31-77,248-270`), финальный статус
FBS-поставки (`backend/app/models/fbs_supply.py:36-55`), а также отгрузочные модели с
`posted_at` (`backend/app/models/outbound_shipment.py:58-65`).

Единственная существующая billing-логика — внутренняя оплата упаковщиков:
`backend/app/services/staff_packaging_billing_service.py:61-105` фиксирует сотрудника,
штуки, ставку и сумму в `PackagingTask` (`backend/app/models/packaging_task.py:84-95`),
а API ставки находится в `backend/app/api/staff_accounts.py:252-299`. Это не расчёт счёта
продавцу.

Проверены незакоммиченные изменения, история всех локальных и remote-веток, поиск по
`invoice`/`billing`/`tariff`/`ИНН` и тематические имена веток. Найден только `d21d7c5`
(`feat(billing): staff packaging pay rate and monthly earnings`) в
`origin/feat/staff-packaging-billing`; счетов продавцу нет в `origin/etalon`/`main`,
поэтому задача не сделана и не влита в бой.

Экран новый, поэтому отдельного `docs/blockers/S-xx.md` пока нет. После появления экрана
нужно внести блокировки реквизитов, финального статуса, периода и неизменяемости счёта
шестью полями реестра. Карта волны по указанному пути также отсутствует.

## Что должно быть

Нужна доменная проработка, после которой можно отдельно утвердить и реализовать срезы:

1. Хранить реквизиты плательщика-селлера и фулфилмента, включая ИНН, и понятно блокировать
   выпуск счёта при отсутствии обязательных данных.
2. Хранить тарифы с периодом действия и снимком в начислении; для приёмки и отгрузки
   поддержать цену за документ или за штуку. Изменение тарифа не меняет выставленные счета.
3. Считать только финализированные события (приёмка по `posted_at`, отгрузка по финальному
   статусу), учитывать каждую операцию один раз и отдельно утвердить правило отмены/сторно.
4. Утвердить период и часовой пояс, включая операцию, начатую в одном периоде и завершённую
   в другом.
5. Формировать черновик с услугой, документом-источником, количеством, тарифом и суммой;
   после выставления фиксировать номер, дату и итог.
6. Дать ФФ внутренний отчёт «сотрудник × период»; не смешивать его с внешним счётом.

Признак результата аналитики: владелец утвердил модель плательщика, события, правила
отмен/сторно, период, версионирование тарифов и жизненный цикл счёта. Затем новые экраны
и API можно добавить в реестр. Экранов по задаче сейчас нет — экран будет создан.

## Тип

`домен`

## Экраны

Экран будет создан. В `frontend/screens.registry.json` нет id для счетов, тарифов или
реквизитов; конкретные маршруты и id нельзя назначить до продуктового решения.

## Вопросы и допущения

- Один селлер — один плательщик или возможны несколько? Какие реквизиты обязательны кроме ИНН?
- Какие статусы являются финальными для приёмки, FBS и прочих отгрузок?
- Как учитывать отмену/откат после выставления и переделку операции: сторно или иначе?
- В какой период попадает операция, завершённая в другом месяце, и какой часовой пояс действует?
- Нужны ли автоматическая отправка селлеру, PDF и бухгалтерский формат? Кто выставляет счёт?
- Решено источником правды проекта: денежные суммы хранятся с минимальной точностью тарифа
  в RUB, границы расчётных суток и периода — МСК; НДС в MVP не рассчитывается
  (`docs/MVP_DECISIONS_RU.md`).
- Карта волны не найдена по пути `/Users/deniscivkunov/Projects/WMS/.worktrees/pipeline-etalon/night/zadachi-2026-08-21-20260821-r04/MAP.md`.
