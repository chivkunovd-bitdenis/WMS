# UI review — карточка 05, S-03

Проверены файлы S-03 из `frontend/screens.registry.json`, контракт карточки и разделы 1–3 канона. Это кодовая проверка; сценарий в браузере она не заменяет.

## Находки

| Правило | Файл:строка | Что теряет оператор | Как чинить |
|---|---|---|---|
| R-02 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:897` | В шапке есть только название `Заказы FBS`; нет обязательной строки `body2 color="text.secondary"` о назначении рабочего списка. При входе в раздел оператор не получает краткого контекста, что именно делать на этом экране. | Сразу под заголовком добавить одну вторичную строку о назначении списка, не добавляя в шапку счётчики или технические статусы. |
| R-09 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1216` | У рабочих колонок заданы только `minWidth`, а не фиксированные `width`; при новых строках и состояниях границы могут расширяться и данные визуально смещаются между столбцами. Оператор медленнее сопоставляет товар, селлера и срок. | Закрепить `width` для каждой колонки состава из контракта, сохранив состав таблицы и малую плотность. |
| R-22 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1380` | Загрузка рабочего списка показана спиннером `CircularProgress`, тогда как канон для таблицы требует строки-скелеты. При первой загрузке оператор видит пустую таблицу и не понимает её будущую структуру. | Заменить этот блок на пять строк `TableSkeletonBody`; при дозагрузке оставлять уже загруженные строки и показывать скелеты только снизу. |
| R-36 | `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1217` | Заголовки `Заказ и сканирование`, `Склад селлера / WB` и `Создан WB / в сборке` не защищены от переноса (`noWrap`/`whiteSpace: 'nowrap'` отсутствуют). Перенос рвёт связь заголовка с данными и меняет высоту шапки. | Для заголовков оставить короткие согласованные подписи из контракта и включить запрет переноса; ширины закрепить вместе с исправлением R-09. |

Карточка ещё не реализует согласованную дозагрузку: лимит первой выдачи остаётся 500 (`FfFbsOrdersScreen.tsx:82,566`), а состояние `next_cursor` не используется. Это блокирует проверку предложенного R-37, но R-37 пока помечен в контракте как новая «дыра» канона, поэтому в таблицу находок не включён как действующее правило.

### ui_guard.py

Новые отступления есть: `ui_guard.py` сообщил о трёх случаях. Из них S-03 затрагивает `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx` (экран-монолит 2493 → 2498). Он не относится к зонам карточки 05 — фильтрам, таблице и панели дозагрузки `FfFbsOrdersScreen` — поэтому не предлагаю менять соседний workspace в этой карточке. Два остальных случая относятся к другим экранам.

## Проверено и нормально

- R-03: фильтры находятся над таблицей в отдельном `Paper variant="outlined"` (`FfFbsOrdersScreen.tsx:942-1049`); пагинация не добавлена в фильтры.
- R-04, R-05, R-07: рабочая таблица использует `TableContainer component={Paper} variant="outlined"`, `stickyHeader` и `size="small"` (`FfFbsOrdersScreen.tsx:1188-1202`).
- R-14: видимые статусы остаются короткими `Chip`; новых технических чипов для cursor/job/poll в текущем коде нет.
- R-21 и R-23: пустое состояние объясняет следующий шаг, а ошибка показана внутри экрана как `Alert severity="error"` (`FfFbsOrdersScreen.tsx:1051-1055,1363-1377`). Текст ошибки нужно сохранить в складском языке при реализации карточки.
- R-13: в проверенных зонах нет новой локальной палитры или замены типографики.
- `python3 scripts/ui/ui_inventory.py` завершился успешно; сверены реальные подписи S-03 из `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-5-05/docs/product/ui-inventory.json`. Новых выдуманных названий колонок или статусов для карточки 05 не обнаружено.
