# Ревью карточки 03

Вердикт: **CHANGES_REQUESTED**.

## Находки

1. **P0 — основной операторский сценарий не реализован.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:2028-2030` — чекбокс по-прежнему меняет только локальный state и остаётся заблокированным при `workspace.boxes.length > 0`; вызова нового `POST .../boxes-without-distribution` во фронтенде нет. Сценарий поломки: оператор создаёт пустой короб и затем решает включить «Без распределения» — чекбокс серый, поэтому исходный тупик из контракта воспроизводится без изменений. Цена: карточка не исправляет заявленный производственный дефект для пользователя.

2. **P0 — состояние поставки теряется на экране после загрузки.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:356,1278` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/frontend/src/screens/v2/fbsApi.ts:453-475` — локальный флаг всегда сбрасывается в `false`, заголовок выводит режим только по коробам, а новое поле `supply.boxes_without_distribution` даже не описано в типе ответа. Сценарий поломки: режим включён на поставке без коробов, оператор обновляет workspace — чекбокс выключен, а шапка снова показывает «Распределено 0 из N»; это напрямую ломает S-03-TC-005 и требование контракта о корректной шапке. Цена: оператор видит противоположное сохранённому состоянию и может продолжить процесс в неверном режиме.

3. **P0 — миграция не переносит старый признак, поэтому исправляемый дефект остаётся у боевых поставок.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/alembic/versions/20260821_0094_fbs_supplies_boxes_without_distribution.py:41-59` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/app/services/fbs_packing_box_service.py:382-384,404-423` — старые `no-distribution:` значения читаются лишь с существующих коробов, но поле поставки не заполняется. Сценарий поломки: у старой поставки режим задан префиксом, оператор удаляет все короба и создаёт их заново — после удаления источник признака исчезает и режим слетает; если он отправит `enabled=false`, метод раньше времени вернётся как будто режим уже выключен, хотя workspace продолжит читать старый префикс как `true`. Цена: повторяется описанный в первоисточнике инцидент и старый режим нельзя надёжно переключить.

4. **P0 — нарушена идемпотентность запросов, начатых до выкладки.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/app/services/fbs_packing_box_service.py:106-118` — после изменения формат поиска всегда использует сырой ключ и больше не ищет сохранённый `no-distribution:<key>`. Сценарий поломки: первый запрос создания коробов успел выполниться на старой версии, клиент не получил ответ и повторяет тот же idempotency key после обновления — сервис не находит старые короба и создаёт дополнительный комплект с новыми номерами и грузоместами WB. Цена: физические и WB-грузоместа дублируются именно при штатном повторе после сетевого отказа/деплоя.

5. **P1 — проверка «короба ещё пустые» допускает гонку двух вкладок.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/app/services/fbs_packing_box_service.py:180-217,390-401,493-498` — переключатель считает назначения, а назначение отдельно читает флаг поставки; блокировки строки поставки или иной общей сериализации нет. Сценарий поломки: одна вкладка включает режим, одновременно другая назначает заказ; обе транзакции читают старое состояние, после коммитов остаются и `boxes_without_distribution_at`, и назначенный в короб заказ. Цена: сервер пропускает несовместимое состояние, а preflight может разрешить сдачу как «без распределения», скрыв уже существующую раскладку.

6. **P1 — тесты зелёные при сломанном пользовательском поведении.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/tests/test_fbs_packing_box.py:213-335` — добавлены только прямые API-тесты; ни один тест не нажимает чекбокс, не проверяет его доступность после создания пустого короба и не проверяет видимую шапку после перезагрузки. Сценарий поломки уже присутствует в текущем коде фронтенда, но весь новый набор тестов при этом может пройти. Цена: S-03-TC-001…005 формально описаны, однако регресс пользовательского пути не защищён.

7. **P1 — новая серверная блокировка не оформлена и не согласована с экраном.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/backend/app/services/fbs_packing_box_service.py:398-399` и `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/docs/blockers/S-03.md:162-179` — появился новый отказ `boxes_already_distributed`, но в реестре блокировок осталась старая B-09 про запрет при любом созданном коробе; новой записи с шестью обязательными полями нет, а UI не показывает соответствующее правило до действия. Сценарий поломки: после назначения заказа будущий подключённый переключатель упрётся в 409, тогда как документ и текущий экран описывают другую границу запрета. Цена: сервер и операторский слой расходятся, причина блокировки не контролируется продуктовым реестром.

8. **P1 — все файлы реализации находятся вне разрешённых границ экрана S-03.** `/Users/deniscivkunov/Projects/WMS/.worktrees/.night-worktrees/zadachi-2026-08-21-20260821-r04/lane-3-03/frontend/screens.registry.json:120-126` разрешает только перечисленные frontend-файлы экрана и `frontend/src/ui-kit/`, тогда как результат меняет `backend/alembic/versions/20260821_0094_fbs_supplies_boxes_without_distribution.py`, `backend/app/api/fbs_errors.py`, `backend/app/api/fbs_supplies.py`, `backend/app/models/fbs_supply.py`, `backend/app/services/fbs_packing_box_service.py`, `backend/app/services/fbs_shipment_service.py`, `backend/app/services/fbs_workspace_service.py`, `backend/tests/test_fbs_packing_box.py` и `tests/cases/S-03.md`. Сценарий поломки процесса: карточка проходит дальше с изменениями вне объявленного наряда, хотя правило роли требует считать любой такой файл находкой. Цена: отсутствует проверяемая изоляция карточки и нельзя подтвердить соблюдение границ ночной волны.

## Проверено и нормально

- Полностью прочитан рабочий diff, включая незатреканную миграцию, API-схему, модель, сервисы workspace/доставки, backend-тесты и каталог S-03; несвязанных изменений внутри этих файлов не обнаружено.
- Новый endpoint ограничен tenant-контекстом и ролью FBS-оператора, а попытка изменить уже переданную поставку по-прежнему получает серверный отказ; добавленное поле ответа является добавляющим, а не удаляет существующие поля API.
- Обычные последовательные случаи нового backend-пути покрыты содержательно: включение/выключение на пустых коробах, отказ после назначения, разблокировка после удаления назначения и состояние без коробов.
- `git diff --check` прошёл. Запуск pytest подтвердить не удалось: в рабочей копии нет `backend/.venv/bin/pytest`; секреты, `.env`, токены и кабинеты учётных данных не читались.
