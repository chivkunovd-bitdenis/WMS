.DS_Store
Thumbs.db

# Python
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
.python-version

# Envs
.env
.env.*

# Node
node_modules/
dist/
.vite/
frontend/test-results/
frontend/playwright-report/

# Local e2e sqlite
e2e.db
backend/e2e-*.db
backend/live-*.db
backend/wms_pytest.sqlite
backend/tests/wms_pytest.sqlite
backend/tests/wms_pytest_*.sqlite
backend/tests/wms_pytest_*.sqlite-*
backend/tests/wms_pytest_data/
backend/tests/wms_pytest_data_*/
backend/var/wms-data/
backend/dev.db
backend/dev-local.db
# База локального стенда для осмотра экранов, рецепт в docs/agent-orders/HANDOFF-POLISH.md
backend/manual.db
backend/live*.db
backend/*live*.db
backend/alembic_autogen.db

# IDE
.vscode/
.idea/
.cursor/.autopilot_armed.json

# Cursor queue autopilot (local only)
.cursor/state/
.cursor/wt/

# Мобильный ТСД — отдельный git-репозиторий, живёт локально и без remote
mobile/

# Спасённое из удалённых копий проекта (см. CLAUDE.md → Архив)
.archive/

# Изолированные рабочие копии веток — только здесь, никаких клонов рядом с проектом
.worktrees/

# Состояние наряда — локальное, у каждой ветки своё (scripts/naryad_state.py)
tasks/_current/

# Вывод генераторов печати и черновики агентов — не код
output/

# Ресурсы самого приложения остаются в репозитории — они часть продукта.
!frontend/public/*.png
!frontend/src/assets/*.png
scratchpad/

# Тяжёлые артефакты проверок в репозитории не хранятся: они раздували каждую
# рабочую копию на 170 МБ и попадали в историю навсегда. Вынесены 30.08.2026
# в .archive/media-20260830 (вне гита), указатель — docs/MEDIA-ARCHIVE.md.
docs/**/*.png
docs/**/*.jpg
docs/**/*.jpeg
docs/**/*.gif
docs/**/*.pdf
docs/**/*.zip
docs/**/*.sqlite
docs/**/*.db
docs/**/*.mp4
docs/**/*.webm
tasks/**/*.png
tasks/**/*.jpg
tasks/**/*.pdf
tasks/**/*.zip
output/
