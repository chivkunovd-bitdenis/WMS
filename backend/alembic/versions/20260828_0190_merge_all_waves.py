"""Свести головы всех волн 28.08 в одну.

Пустая сводная ревизия, своих изменений схемы не несёт.

Откуда взялись три головы. В один день параллельно работали шесть исполнителей,
и каждый честно взял свободный номер: складская структура — ``20260828_0130``,
документ инвентаризации — ``20260828_0160``, журнал карты склада —
``20260828_0170``. Плюс ``20260828_0120`` — сведение эталона с невлитой цепочкой.
Пока ветки жили порознь, у каждой была своя единственная голова; при сведении их
стало три, и ``alembic upgrade head`` перестал понимать, куда идти.

Расходиться им было нечем: разные таблицы, пересечений нет. Поэтому сведение
пустое — нужно только, чтобы у базы снова была одна голова.
"""

from __future__ import annotations

from collections.abc import Sequence

revision = "20260828_0190"
down_revision: str | Sequence[str] | None = (
    "20260828_0120",
    "20260828_0160",
    "20260828_0170",
)
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Схему не трогаем: ревизия существует ради единственной головы."""


def downgrade() -> None:
    """Обратно тоже нечего откатывать."""
