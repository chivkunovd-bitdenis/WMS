"""Свести две линии миграций в одну: эталон и невлитую цепочку 23–28.08.

Пустая сводная ревизия. Своих изменений схемы не несёт — её единственная работа
в том, чтобы у базы снова была одна голова.

Откуда взялись две. Прод собирается с ветки ``etalon``, и туда 27.08 уехала
ревизия ``20260827_0104`` (автор операции WB). Параллельно в невлитой цепочке
доросла своя линия до ``20260828_0116`` (доля остатка FBS). Пока ветки жили
порознь, каждая была единственной головой у себя; при сведении их стало две, и
``alembic upgrade head`` перестал понимать, куда идти.

Тем же сведением расшит дубль номера ``20260825_0101``: его занимали и уникальный
артикул продавца (он на бою), и счета маркетплейсов из линии Ozon. На бою остался
исходный номер, линия Ozon переехала на ``20260825_0111`` — иначе накатка на
боевую базу встала бы на неоднозначной ревизии.
"""

from __future__ import annotations

from collections.abc import Sequence

revision = "20260828_0120"
down_revision: str | Sequence[str] | None = ("20260827_0104", "20260828_0116")
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Схему не трогаем: ревизия существует ради единственной головы."""


def downgrade() -> None:
    """Обратно тоже нечего откатывать."""
