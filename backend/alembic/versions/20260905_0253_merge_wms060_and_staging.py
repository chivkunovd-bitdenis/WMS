"""Свести две линии миграций в одну: остатки WMS-060 и цепочку стенда.

Пустая сводная ревизия. Своих изменений схемы не несёт — её единственная работа
в том, чтобы у базы снова была одна голова.

Откуда взялись две. Ветка ``codex/wms-060-stock-operations`` и ветка стенда
``staging`` разошлись от общего предка и каждая доросла до своей головы поверх
``20260902_0248``: стенд ушёл в ``20260903_0249`` (уникальность привязки по
маркетплейсу) и ``20260903_0250`` (контейнеры инвентаризации), работа по
остаткам ФБС — в ``20260904_0251`` (режим поштучного выделения) и
``20260905_0252`` (свободный остаток ФБС). Пока ветки жили порознь, каждая была
единственной головой у себя; при сведении их стало две, и ``alembic upgrade
head`` перестал понимать, куда идти.

Revision ID: 20260905_0253
Revises: 20260903_0250, 20260905_0252
Create Date: 2026-09-05
"""

from __future__ import annotations

from collections.abc import Sequence

revision: str = "20260905_0253"
down_revision: str | Sequence[str] | None = ("20260903_0250", "20260905_0252")
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Схему не меняет — только сводит две головы в одну."""


def downgrade() -> None:
    """Схему не меняет — откатывать нечего."""
