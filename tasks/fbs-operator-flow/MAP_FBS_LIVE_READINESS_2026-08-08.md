# Карта готовности ФБС к боевой работе селлеров — 08.08.2026

Что должно сложиться, чтобы селлер подключил свой ключ и начал реально отгружать заказы.

## Как устроен обмен (важно понимать до правок)

**Остаток считает WMS, а не кабинет.** `fbs_stock_availability_service` берёт `inventory_balances`
(то, что физически принято приёмкой на склад ФФ), вычитает резервы `FbsOrderReservation` под другие
ФБС-заказы и исключает ячейку сортировки. Остаток ФБО/FBW из каталога здесь не участвует.

**Остаток уезжает из WMS в WB, а не наоборот.** `put_marketplace_stocks` делает `PUT /api/v3/stocks/{warehouseId}`.
То есть кабинет получает цифру от нас. Если товар не принят на склад ФФ — в кабинет уедет ноль,
и заказы по нему собирать нечем.

**Заказы приходят из WB в WMS** через `GET /api/v3/orders/new` + `POST /api/marketplace/v3/orders/meta`.

**Один ключ на всё.** После `7a96fc2` действует `token = marketplace_token_encrypted or content_token_encrypted`,
и при сохранении контентного токена он копируется в маркетплейсный слот.

## Что уже работает (проверено на живом кабинете Denmarcs 08.08.2026)

- Единый ключ проходит в Marketplace API: синхронизация вернула 14 заказов, создала 7.
- Автопривязка склада WB к складу WMS (`d22e348`): `1155120` привязался сам.
- Сопоставление заказов с карточками: «Резинки пружинки глянцевые (3 шт)», «Брюки летние льняные L».
- Вся операторская цепочка на эмуляторе, оба маршрута, включая короба, QR грузомест и передачу.
- Бэкенд: `ruff` чисто, `pytest` 633 passed.

## Чего не хватает — по приоритету

### P0. Без этого селлер не подключится и заказы не поедут

**1. Поле маркетплейс-токена в форме ключей.**
`frontend/src/screens/v2/WildberriesScreen.tsx` имеет только `wb_content_token` и `wb_supplies_token`.
Бэкенд третий токен принимает (`_parse_token_merge_patch` разбирает `marketplace_api_token`).
Сейчас, если в маркетплейсном слоте лежит мусор, перебить его через интерфейс невозможно —
именно это и случилось на стенде, пришлось чистить в базе.
*Объём: одно поле в форме + прокидка в существующий PATCH.*

**2. Автоопрос заказов — инфраструктура Celery.**
Код готов: `celery_app.py` уже содержит `beat_schedule` с задачами `wms.fbs_orders_autopoll`
(интервал `fbs_poll_interval_sec`) и `wms.fbs_order_statuses_autopoll` (`fbs_statuses_sync_interval_sec`).
В Railway проекта `loyal-wonder` сервисов всего три: `Postgres`, `WMS`, `web`. Нет Redis, нет worker,
нет beat, не задан `CELERY_BROKER_URL`. Пока их нет — заказы не приезжают вообще никак.
*Объём: добавить Redis + два сервиса из того же репозитория (worker и beat), прописать `CELERY_BROKER_URL`.
Разработки нет, только конфигурация. 30–40 минут с проверкой.*

**3. Кнопка ручной синхронизации заказов.**
Во фронте нет вызова `POST /operations/fbs-orders/sync`. Даже когда автоопрос заработает, оператору
нужна возможность дёрнуть обновление руками — иначе при сбое остаётся только ждать.
*Объём: одна кнопка на экране заказов ФБС.*

### P1. Без этого процесс поедет, но с граблями

**4. Приёмка товара на склад ФФ.** Живой прогон 08.08 упёрся именно сюда: в `inventory_balances`
ноль по резинкам и брюкам. Это не дефект — просто товар не принят. Для боевого запуска нужен
регламент: селлер везёт товар → приёмка в WMS → остаток публикуется в WB → заказы становятся собираемыми.

**5. Чистка сеяного мусора на стенде.** Селлеры `FBS Demo North` и `FBS Demo South`, товары
`FBS test product 1..5`, заказ `990001` с артикулом `E2E-MOCK`. Операторы будут путаться.

**6. Ветка деплоя.** Сервис `WMS` в Railway собирается из `fix/fbs-railway-test-emulator-20260806`,
а `.cursor/skills/railway-staging-deploy/SKILL.md` утверждает, что из `staging`. Расхождение надо
устранить, иначе выкладки будут уходить в пустоту.

**7. Мёртвая переменная.** `WMS_FBS_TEST_EMULATOR_ENABLED=1` в коде не используется — убрать,
чтобы не вводила в заблуждение.

### P2. Качество и защита от регрессов

**8. Браузерная e2e-спека устарела.** `frontend/tests-e2e/ff-fbs-full-flow.spec.ts` ищет
`ff-packaging-line` и кнопку «Стикер WB», а в эталоне на этом месте «Печать всего», «Всё упаковано»
и раздельная печать ЧЗ/ШК. Пока спека красная, регрессы фронта ловить нечем.

**9. Проверить поведение `pending` по Честному знаку.** `51be7da` убрал `META_STATUS_PENDING`
из `_META_DELIVERY_OK`: заказ с непройденной проверкой КИЗ больше не считается готовым к передаче.
По контракту WB строже и, вероятно, правильнее, но на живых заказах может тормозить отгрузку.

**10. Смержить PR [#105](https://github.com/chivkunovd-bitdenis/WMS/pull/105)** в `main`, чтобы
`main` перестал отставать на 33 коммита.

## Решения по остаткам и статусам (обсуждение 08.08.2026, к реализации)

### Как считается доступное под ФБС

```
доступно для ФБС = max(0, хранение + сортировка − резерв под отгрузку на МП − резерв под другие ФБС-заказы)
```

Склад **один**, отдельного «ФБС-склада» нет. Резерв под ФБО/FBW вычитается из того же пула,
поэтому «запланировал 1000 на ФБО из 1500» автоматически оставляет 500 под ФБС.
Ячейка сортировки **входит** в доступное.

### P0-11. Публикация остатка после каждого движения

`PUT /api/v3/stocks/{warehouseId}` — перезапись итогового количества по штрихкоду.
Реализовано в `fbs_stock_sync_service.sync_binding_stocks`, но дёргается **только вручную**
через `POST /operations/fbs-sellers/{id}/stocks/sync`. Автовызова после приёмки, отгрузки,
списания и резерва под ФБС-заказ нет — Кодекс отвязал публикацию от опроса заказов.
Следствие: списали товар — в кабинете висит старая цифра, WB продолжает продавать.
*Нужно: ставить публикацию в очередь после каждого события, меняющего остаток,
плюс фоновая сверка раз в несколько минут как страховка. Ручную кнопку оставить.*

### P0-12. Признак участия в ФБС + лимит на товар + предпросмотр

**Нельзя включать синхронизацию «в лоб»** — иначе весь склад ФФ мгновенно уедет в продажу по ФБС,
включая товар под будущие ФБО-отгрузки и всё, что продавать этим каналом не собирались.

Правило обратное: публикуем **только то, что явно разрешено**.
- Признак «участвует в ФБС» на товаре в связке с привязкой склада, по умолчанию **выключен**.
  Пока не включён — про товар в WB не пишем ничего, ни числа, ни нуля.
- Лимит на товар: публикуем `min(доступно, лимит)`. Закрывает и сценарий «оставить 500
  на подстраховку», и «выставить только часть». Одна механика вместо двух фич.
- Ноль публикуем только для товаров, которые ведём мы. Чужие позиции селлера на том же складе WB
  не трогаем — перезапись идёт по штрихкоду.
- Экран предпросмотра перед первой публикацией: товар, остаток, лимит, итоговая цифра.
  Публикуем только после подтверждения.

*Проверить перед работой: возможно, признак/лимит уже частично есть в модели товара или в привязке склада.*

#### Уточнение 09.08.2026: галочка живёт в кабинете селлера, по каждому товару

Проверка кода показала: признака нет нигде. Есть только `FbsWarehouseBinding.stock_sync_enabled` —
рубильник на весь склад целиком, а `_load_seller_products` тянет **все** товары селлера без фильтра.
То есть сегодня включение синхронизации выгружает в WB вообще весь каталог.

Как должно быть: селлер сам, у себя в кабинете, в списке товаров ставит переключатель
«Синхронизировать остаток ФБС» — против каждой позиции отдельно, плюс «включить всем» в шапке.
Пока переключатель выключен, товара для WB не существует. Как только включён, текущий остаток
на складе ФФ уезжает в кабинет и дальше сам едет следом за каждым движением товара, без ручных действий.

**Что править по файлам:**

*Бэкенд, данные.* Новая колонка `fbs_stock_sync_enabled` (bool, `default false`) в
`backend/app/models/product.py` + миграция `backend/alembic/versions/20260809_0075_product_fbs_stock_sync_flag.py`
(следующая после `..._0074_fbs_packing_boxes_pvz_link.py`). Флаг кладём на товар, а не на связку
со складом: у селлера один каталог, а решение «продавать это по ФБС» — про товар, не про склад.
Лимит `fbs_stock_limit` (nullable int) добавляем той же миграцией, чтобы не плодить вторую.

*Бэкенд, публикация.* В `backend/app/services/fbs_stock_sync_service.py` фильтр в
`_load_seller_products` (строка ~166) — брать только `Product.fbs_stock_sync_enabled.is_(True)`.
В `_build_publish_plan` (~182) считать `amount = min(доступно, лимит)`, если лимит задан.
Важная тонкость: товар, который **только что выключили**, обязан один раз уехать нулём и лишь
потом выпасть из выгрузки, иначе в кабинете навсегда зависнет последняя цифра. Значит выключение
ставит `FbsStockSyncItem` в `pending` с `last_target_amount = 0`, а из плана позиция уходит уже
после подтверждённого нуля.

*Бэкенд, API.* В `backend/app/api/products.py` рядом с
`PATCH /{product_id}/packaging-instructions` (строка 465) — `PATCH /{product_id}/fbs-stock-sync`
на один-два поля, и батч-ручка для «включить всем». Ответы `SellerWbCatalogOut` и `FfCatalogOut`
(ручки на строках 242 и 293) дополняем полями флага, лимита и текущей опубликованной цифры,
чтобы фронт рисовал состояние без второго запроса.

*Бэкенд, реакция на движения.* Тот же хвост, что и в P0-11: включение флага и любое движение
по товару ставят публикацию в очередь. Точка входа — `fbs_stock_sync_service.sync_binding_stocks`,
дёргать её из `celery_app.py` отложенной задачей, а не синхронно в транзакции приёмки.

*Фронт, кабинет селлера.* `frontend/src/screens/v2/SellerProductsStockScreen.tsx` — колонка со
`Switch` в таблице каталога, чекбокс-«всем» в шапке, поле лимита и подпись «опубликовано в WB: N».
Оптимистичное переключение с откатом по ошибке; `MUI Switch` там ещё не импортирован, `Checkbox` уже есть.

*Фронт, сторона ФФ.* `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx` показывает те же флаги
только на чтение — оператору ФФ важно видеть, почему товар не публикуется, но переключать
чужой канал продаж он не должен. Типы и вызовы — в `frontend/src/screens/v2/fbsApi.ts`.

*Тесты.* `backend/tests/` — на фильтр в плане публикации, на `min(доступно, лимит)` и на
«выключили → уехал ноль → выпал из выгрузки».

### P1-13. Момент возникновения резерва под ФБО

Проверить, появляется ли `MarketplaceUnloadReservation` уже при **планировании** отгрузки на МП
или только при подтверждении. Если только при подтверждении — между планированием и сборкой
ФБС успеет съесть запланированный объём. Тогда резервировать надо с момента планирования.

### P1-14. Неснижаемый остаток под ФБС

Сейчас приоритет у того, кто первым зарезервировал. Гарантированной брони под ФБС нет.
Решить: нужна ли настройка «ниже N штук ФБО опускать нельзя», или достаточно порядка поступления.

### P0-15. Единый ключ — не заставлять вводить повторно

Селлер вводит ключ **один раз**. Все категории прав в нём уже есть, значит этот же ключ
используется и для маркетплейса, и для контента, и для поставок. Никаких отдельных полей
под каждый токен быть не должно — поле маркетплейс-токена из P0-1 нужно только как
аварийная перезапись, а основной путь — один ключ на всё (`7a96fc2` это уже умеет).

### P0-16. Карта соответствия статусов WMS ↔ WB, в обе стороны

Опрос статусов — **раз в минуту** плюс ручная кнопка.
Недостаточно «просто считать» статус: нужна явная таблица соответствия. Если заказ собран
в кабинете — у нас он должен уйти из «Новых», и наоборот: наши переходы должны отражаться в WB.
*Нужно: составить и зафиксировать таблицу соответствия, реализовать применение в обе стороны.*

### Границы ответственности по складам

У селлера может быть несколько складов ФБС, и не все обслуживает наш ФФ. Остаток в WB задаётся
**на конкретный склад** (`PUT /api/v3/stocks/{warehouseId}`), у нас этому соответствует
`fbs_warehouse_bindings`. Публикуем только в привязанные к нашим складам ФФ; чужие склады селлера
не трогаем вообще. WB сам суммирует остатки по складам и маршрутизирует заказ туда, где товар есть.

Брать за истину цифру, которую селлер проставил в кабинете руками, **нельзя**: товар физически
у нас, движение знаем мы, а `PUT /stocks` — перезапись, двух хозяев у одной цифры быть не может.

## Порядок запуска для одного селлера (целевой сценарий)

1. Селлер создаёт в кабинете WB токен с категориями **«Контент» и «Маркетплейс»**.
2. Вставляет его в WMS — в поле контентного токена; он же уезжает в маркетплейсный слот.
3. WMS импортирует карточки (уже работает) и автоматически привязывает склад WB к складу WMS (уже работает).
4. Автоопрос притягивает новые заказы каждые `fbs_poll_interval_sec` — **нужен пункт 2 из P0**.
5. Товар физически принимается на склад ФФ через «Приёмку», остаток публикуется в WB.
6. Оператор собирает поставку, подбирает, упаковывает, печатает ЧЗ и ШК, раскладывает по коробам,
   печатает QR, передаёт в WB.

Сейчас разорваны шаги 2 и 4.
