# 00 — Триаж

- **Задача (одной строкой):** Приём сборочных заданий WB FBS в WMS: модели, опрос API, маппинг товара, резерв остатка, синк статусов.
- **Тип:** `feature`
- **Размер:** `L`
- **GitHub Issue:** нет (эпик `tasks/fbs-marketplace-orders/`, под-задача из `_COMPOSER_TASKS.md`)
- **Стоит ли делать сейчас / зачем:** Ядро FBS; без него остальные под-задачи (сборка, отгрузка, фронт) не стартуют. Гейт 1 эпика ✅ 30.07.2026.

## Маршрут (feature L)
- [x] 0 триаж (этот файл)
- [x] 1 анализ + арх-коучинг (наследует эпик + уточнение резерва)
- [x] 🔒 ГЕЙТ 1: арх + стек — подтверждён на эпике; уточнение резерва — модератор (см. `02-arch-decision.md`)
- [x] 2 контракт
- [x] 3 тест-дизайн
- [ ] 4 код
- [ ] 5 независимое ревью + прогон гейтов
- [ ] 🔒 ГЕЙТ 2 приёмка (опц.)
- [ ] 6 док → `docs/`

## Что затрагивает
backend: `models` (новые `fbs_*`), alembic, `services` (WB Marketplace client + sync + reserve), `api` (минимум: ручной sync / list), `tasks`/`background_job`, `core/settings` (base URL Marketplace API). Frontend/mobile — out of scope.
