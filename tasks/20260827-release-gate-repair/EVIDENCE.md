# Доказательства · восстановление релизного гейта

Дата проверки: 27.08.2026.

## Backend

- `cd backend && ruff check .` — успешно.
- `cd backend && mypy .` — успешно, проверено 306 файлов.
- `cd backend && pytest` — `1031 passed, 5 skipped`.

## Frontend

- `cd frontend && npm run build` — успешно.
- `cd frontend && npm run test:e2e` — исходный полный прогон: `201 passed, 7 skipped`,
  0 падений.
- После замечаний reviewer и устранения transient SQLite lock выполнен чистый повторный
  `CI=1 npm run test:e2e`: `201 passed, 7 skipped`, 0 падений.

Полный браузерный прогон отдельно подтвердил действующие FBS-сценарии: получение заказов,
создание поставки, привязку склада, получение QR коробов и QR поставки. Все пропуски заданы
самими тестами для сценариев, требующих внешнего живого кабинета или специального окружения.

## Целостность изменения

- production-файлы `frontend/src` не менялись;
- автоматически перезаписанные Playwright-скриншоты восстановлены из `HEAD` и в diff не входят;
- `git diff --check` проходит;
- отдельный reviewer сначала нашёл два ослабления Playwright, после исправления подтвердил
  `CODE_REVIEW_PASSED`; P0/P1/P2-блокеров нет.
- test-only SQLite helper получил ограниченный `.timeout 10000`: временный `SQLITE_BUSY`
  дожидается освобождения, длительная блокировка по-прежнему роняет тест; reviewer подтвердил
  `CODE_REVIEW_PASSED` повторно.

## Живой браузер

Отдельный ux-judge в локальном изолированном окружении (`http://127.0.0.1:5190`, API `18101`,
отдельная временная SQLite-база) открыл реальную видимую вкладку браузера.
Вручную пройдены вход администратора, «Заказы FBS», «Остатки WB», «Упаковка» и очередь маркировки.
Проверены фильтры, таблицы, вкладки и пустые состояния. Seller-shell не подменяет FF-маршруты,
ошибок консоли, наложений и горизонтального переполнения нет (viewport 1280×720,
`scrollWidth == clientWidth == 1280`).

- `docs/evidence/20260827-release-gate-repair/fbs-live.png`
- `docs/evidence/20260827-release-gate-repair/packaging-live.png`
- независимый вердикт: `PRODUCT_BROWSER_APPROVED` для локальной release-gate ветки.
