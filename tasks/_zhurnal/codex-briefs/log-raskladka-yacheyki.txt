Reading additional input from stdin...
2026-08-29T10:47:40.893332Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/deniscivkunov/Projects/WMS/.git]
reasoning effort: high
reasoning summaries: none
session id: 01a04d21-e30c-77f2-b0b0-bb78331d0bb4
--------
user
## Разрешения для этой сессии (читать первым)

1. Ты работаешь в git worktree внутри `~/Projects/WMS/.worktrees/`. Это РАЗРЕШЕНО
   правилами проекта (см. CLAUDE.md, пункт про `git worktree` внутри `.worktrees/`).
   Не останавливайся на проверке `pwd` — ты там, где надо.
2. НЕ гоняй полный `pytest`. Только адресные файлы тестов, которые названы ниже.
   Полный прогон в этом дереве даёт ошибки сбора из-за соседних worktree.
3. Продуктовый гейт (BA / Product / Browser QA) для этой задачи ОТМЕНЁН владельцем
   25.08.2026. Не заводи карточки, не жди вердиктов, не пиши файлы гейта.
4. Фронт не трогай вообще. Только `backend/`. Фронт делает владелец сам.
5. Пиши по-русски в комментариях и коммитах, как принято в этом репозитории.

---

# Задача: раскладка не видит того, что уже разложено

## Что сломано (проверено по коду и базе стенда)

Экран раскладки в документе сортировки берёт данные из
`GET /warehouses/{warehouse_id}/sorting-objects`.

В `backend/app/services/warehouse_map_service.py`, функция `get_sorting_objects`
(около строки 1092) собирает список так:

```python
_sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
```

То есть **только из зоны «Без ячеек»**. А ячейки в ответе отдаются голыми:

```python
"cells": [{"id": row["id"], "code": row["code"], "barcode": row["barcode"]} for row in data["cells"]]
```

**без содержимого**.

Из-за этого: оператор кладёт короб в ячейку, запись в базе появляется (проверено:
на стенде 16 коробов из 63 имеют `storage_location_id`), но при следующем чтении
короб исчезает с экрана — он уже не в «Без ячеек», а в ячейке содержимого нет.
Для оператора это выглядит как «закрепил за ячейкой, и оно сбросилось».

## Ещё два бага в той же ручке размещения

Файл `backend/app/api/warehouses.py`, роут
`POST /{warehouse_id}/sorting-objects/place` (около строки 367):

```python
cell_id = body.cell_id or body.to_id
if cell_id is None:
    raise HTTPException(422, detail="cell_id_required")
```

и дальше `warehouse_map_service.place_sorting_object`, который **всегда** зовёт
`move_object(..., to_kind="cell", to_id=cell_id, ...)`.

**Баг 1 — «снять с ячейки» не работает.** Фронт при снятии шлёт
`{cell_id: null, to_id: null}` и в коде рядом стоит комментарий, что сервер
понимает пустую цель как «Без ячеек». Сервер её не понимает — отвечает 422.
Нужно: пустая цель = переместить объект в зону «Без ячеек»
(ячейка с кодом `SORTING_LOCATION_CODE`), а не отказ.

**Баг 2 — вложить короб в палету нельзя.** Фронт шлёт id тары в `to_id`
(`{cell_id: null, to_id: "<uuid палеты/короба/грузоместа>"}`), а ручка кладёт
это значение в `cell_id` и передаёт как перемещение в ЯЧЕЙКУ. Нужно различать:
`cell_id` — цель-ячейка, `to_id` — цель-тара, и звать `move_object` с
правильным `to_kind`.

## Что сделать

1. `get_sorting_objects` должен возвращать **и** содержимое ячеек: для каждой
   ячейки — какая тара и какой товар в ней лежит, в том же формате, в каком
   сейчас отдаётся содержимое «Без ячеек» (`objects` с полем holder и `lines`).
   Смотри, как устроен `_sorting_tree_rows` и что кладёт `get_warehouse_map`
   в `data["cells"]` — переиспользуй, не пиши второй обход дерева.
   Формат ответа расширяй **добавлением**, ничего не убирая: фронт живой.
2. Роут `place`: пустая цель — снятие в «Без ячеек», без 422.
3. Роут `place`: цель-тара через `to_id` — вложение в тару, а не в ячейку.
4. Схема ответа `SortingObjectsOut` — обнови под новые поля.

## Тесты (обязательно, новые роуты/поведение без теста не принимаются)

Заведи `backend/tests/test_sorting_objects_cells.py`:
- объект положили в ячейку → он виден в ответе `GET sorting-objects` с этой ячейкой;
- «снять с ячейки» (пустая цель) → 200, объект вернулся в «Без ячеек»;
- вложить короб в палету через `to_id` → 200, короб внутри палеты, а не в ячейке;
- товар положили в ячейку → виден в содержимом ячейки с количеством.

## Ворота перед сдачей

```
cd backend && ruff check . && mypy .
python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q
python3 ../scripts/ci/back_guard.py
```

Закоммить осмысленным сообщением по-русски. Итог запиши в
`tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md`: что изменил, какие
поля добавил в ответ (это нужно владельцу для фронта), какие тесты прошли.
warning: Under-development features enabled: chronicle. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/deniscivkunov/.codex/config.toml.
codex
Сначала сверю состояние worktree, актуальные правила и прошлый контекст по этой части WMS. Затем точечно прочитаю сервис, схемы и существующие тесты; фронт и полный `pytest` трогать не буду. Продуктовый гейт считаю явно отменённым владельцем для этой задачи.
exec
/bin/zsh -lc 'pwd && git status --short --branch && rg -n "раскладк|sorting-objects|warehouse_map_service|fix-raskladka" /Users/deniscivkunov/.codex/memories/MEMORY.md' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 300ms:
## codex/fix-raskladka-20260829
?? ZADANIE.md
34:- PR #139, `codex/wms-wave-storage-category-inbound-split-20260828`, `integration/vse-v-etalon-20260828`, `/app/ff/stocktaking`, `/app/ff/fbs-stock`, `/app/ff/sorting-objects`, `ff/unload-pick/:requestId`, `address_storage_enabled`, `WHB-CBA098DA9BDC`, staging, browser QA
301:- `Экран «Карта склада» WMS`, `Раскладка объектами · макет`, `sorting-objects.html`, `move-lab.html`, `localhost:5179`, drag-and-drop, create cell, sticker print, `WB-TRBX`, `Сортировка`

exec
/bin/zsh -lc "sed -n '20,48p' /Users/deniscivkunov/.codex/memories/MEMORY.md && sed -n '286,318p' /Users/deniscivkunov/.codex/memories/MEMORY.md && wc -l CLAUDE.md docs/MVP_DECISIONS_RU.md && sed -n '1,240p' CLAUDE.md && sed -n '1,260p' docs/MVP_DECISIONS_RU.md" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
- extensions/skysight/resources/2026-08-29T03-00-00-jSZW-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-29T03-00-00-jSZW-10min-memory-summary.md, updated_at=2026-08-29T03:00:00+00:00, thread_id=None, observed PR #140/review handoff; staging and merge claims require direct recheck) [skysight memory]
- extensions/skysight/resources/2026-08-29T02-50-00-hiXd-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-29T02-50-00-hiXd-10min-memory-summary.md, updated_at=2026-08-29T02:50:00+00:00, thread_id=None, observed later validation/status report) [skysight memory]
- extensions/skysight/resources/2026-08-29T02-40-00-AEND-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-29T02-40-00-AEND-10min-memory-summary.md, updated_at=2026-08-29T02:40:00+00:00, thread_id=None, observed validation/status report and build state) [skysight memory]
- extensions/skysight/resources/2026-08-28T22-10-00-sFDG-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T22-10-00-sFDG-10min-memory-summary.md, updated_at=2026-08-28T22:10:00+00:00, thread_id=None, observed mockup-leak/server-action sweep and reminder request) [skysight memory]
- extensions/skysight/resources/2026-08-28T22-00-00-wkbc-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T22-00-00-wkbc-10min-memory-summary.md, updated_at=2026-08-28T22:00:00+00:00, thread_id=None, observed screen-by-screen staging status) [skysight memory]
- extensions/skysight/resources/2026-08-28T21-50-00-yTbc-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T21-50-00-yTbc-10min-memory-summary.md, updated_at=2026-08-28T21:50:00+00:00, thread_id=None, observed FBS load-loop/bulk-read defects and remaining manual checks) [skysight memory]
- extensions/skysight/resources/2026-08-28T18-00-00-wpqW-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T18-00-00-wpqW-6h-memory-summary.md, updated_at=2026-08-28T18:00:00+00:00, thread_id=None, six-hour WMS/FBS rollout, mockup reconciliation, and validation arc) [skysight memory]
- extensions/skysight/resources/2026-08-28T20-00-00-Syhh-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T20-00-00-Syhh-10min-memory-summary.md, updated_at=2026-08-28T20:00:00+00:00, thread_id=None, observed PR #139 staging rollout and authentication blocker) [skysight memory]
- extensions/skysight/resources/2026-08-28T20-30-00-bajH-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T20-30-00-bajH-10min-memory-summary.md, updated_at=2026-08-28T20:30:00+00:00, thread_id=None, observed warehouse-map validation continuing) [skysight memory]
- extensions/skysight/resources/2026-08-28T21-10-00-tKJK-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T21-10-00-tKJK-10min-memory-summary.md, updated_at=2026-08-28T21:10:00+00:00, thread_id=None, observed FBS-stock/browser validation report and remaining flows) [skysight memory]
- extensions/skysight/resources/2026-08-28T12-00-00-mgyl-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T12-00-00-mgyl-6h-memory-summary.md, updated_at=2026-08-28T12:00:00+00:00, thread_id=None, six-hour consolidation/staging and mockup-versus-live arc) [skysight memory]

### keywords

- PR #139, `codex/wms-wave-storage-category-inbound-split-20260828`, `integration/vse-v-etalon-20260828`, `/app/ff/stocktaking`, `/app/ff/fbs-stock`, `/app/ff/sorting-objects`, `ff/unload-pick/:requestId`, `address_storage_enabled`, `WHB-CBA098DA9BDC`, staging, browser QA

## User preferences

- for the WB sheet, the user required: “не придумываешь, а берёшь коды по апи вб, а ШК из нашей базы” -> never fill a missing QR or barcode; stop with the missing evidence. [Task 1]
- when testing release readiness, the user repeatedly asked what is actually connected, on staging, and testable -> distinguish accepted mockup, backend support, menu/app connection, deployment, and browser acceptance. [Task 2] [skysight memory]
- for a replacement UI, the user chose the new outbound picking screen but required the old embedded panel to remain restorable -> replace the navigation target without deleting the fallback implementation. [Task 2] [skysight memory]
- while long WMS work is running, the user asked for a 20-minute check whether tasks had actually been pushed through/completed -> use repeated checks to seek new evidence, then stop them once they only repeat the same status. [Task 2] [skysight memory]

## Reusable knowledge

- Correct label provenance is strict `orderId → file` from WB `POST /api/v3/orders/stickers`; use original returned PNG, never generate a QR from order number or `sticker_barcode`. Group picking rows by article + size + confirmed WMS barcode. [Task 1]
- The correct live scope in this case was `https://app.sellerfocus.pro`, seller `ИП Чжоу`; `loyal-wonder / production` was actually a staging branch with different data. Environment name alone is insufficient evidence. [Task 1]
- The approved print format was two A4 pages: 5 pick rows with `5 позиций · 8 единиц · 8 этикеток WB`, then 8 original PNG in a 2×4 grid labelled by `orderId`. Before delivery, verify 8 assets, 5 EAN-13 values, A4 dimensions, and no cropped/distorted PNG. [Task 1]
- Observed rollout reported inventory, FBS residual distribution, and sorting-object placement connected to live routes, while standalone mockup pages remained. Do not treat that report as browser acceptance; the staged login/browser walkthrough was still in progress. [Task 2] [skysight memory]

# Task Group: WMS August 27–28 warehouse-map/sorting prototype acceptance and movement-author contract
scope: Use when converting the warehouse-map/sorting prototype into bounded implementation, reviewing its operator UX, or preserving movement authorship and container-stock safety.
applies_to: cwd=/Users/deniscivkunov/Projects/WMS; reuse_rule=product rules and acceptance checks are reusable; local prototypes, branches, tests, commit/push state, and all production inventory must be rechecked

## Task 1: Review the warehouse-map and object-sorting prototype, outcome rework pending

### rollout_summary_files

- extensions/skysight/resources/2026-08-28T00-00-00-bFKp-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T00-00-00-bFKp-6h-memory-summary.md, updated_at=2026-08-28T00:00:00+00:00, thread_id=None, observed manual prototype acceptance and regressions) [skysight memory]
- extensions/skysight/resources/2026-08-28T06-00-00-TCJs-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T06-00-00-TCJs-10min-memory-summary.md, updated_at=2026-08-28T06:00:00+00:00, thread_id=None, observed narrow layout correction request; no final acceptance) [skysight memory]
- extensions/skysight/resources/2026-08-28T07-30-00-EthU-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T07-30-00-EthU-10min-memory-summary.md, updated_at=2026-08-28T07:30:00+00:00, thread_id=None, observed whole-object removal retest through completed mock state) [skysight memory]

### keywords

- `Экран «Карта склада» WMS`, `Раскладка объектами · макет`, `sorting-objects.html`, `move-lab.html`, `localhost:5179`, drag-and-drop, create cell, sticker print, `WB-TRBX`, `Сортировка`

## Task 2: Define the real warehouse storage/movement contract and review movement author, outcome implementation boundary set; merge/deploy not shown

### rollout_summary_files

- rollout_summaries/2026-08-27T20-34-17-2SbJ-wms_process_change_and_pr_139.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=/Users/deniscivkunov/.codex/sessions/2026/08/27/rollout-2026-08-27T23-34-17-01a044ee-3574-7062-ac1b-865127ac8e6f.jsonl, updated_at=2026-08-28T12:04:58+00:00, thread_id=01a044ee-3574-7062-ac1b-865127ac8e6f, process change, movement-author audit, and PR #139 status)
- extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md, updated_at=2026-08-27T21:30:00+00:00, thread_id=None, observed container-stock and tenant-safety contract) [skysight memory]
- extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md, updated_at=2026-08-27T23:20:00+00:00, thread_id=None, observed `actor_user_id` correction and unrelated branch-gate noise) [skysight memory]

### keywords

- `InventoryMovement.actor_user_id`, `actor_user_id`, `codex/inventory-movement-author`, `CONTRACT.md`, `WAVE-1-CODEX.md`, container references, `Сортировка`, tenant ownership, row locking, `48972cef`

## User preferences

- when reviewing the local screen, the user asked to change only the red-marked width issue and not reinterpret the whole layout -> make narrow visual corrections without moving unrelated controls or redesigning accepted interactions. [Task 1] [skysight memory]
- when testing warehouse movement, the user requires a product inside a box to be taken out before plus/drag movement; preserve container hierarchy rather than allowing direct child relocation. [Task 1] [skysight memory]
      94 CLAUDE.md
     123 docs/MVP_DECISIONS_RU.md
     217 total
# ⛔️⛔️⛔️ ГЛАВНОЕ ПРАВИЛО. НАРУШЕНИЕ = УВОЛЬНЕНИЕ ⛔️⛔️⛔️

## Никаких гипотез. Ни одной. Никогда.

Владелец — суровый технический директор, который спрашивает за функционал.
Сотрудник, который вбросил догадку, не разобравшись, — вылетает. В серьёзных
системах иначе нельзя: неверный ответ про склад стоит денег и товара.

**Каждое утверждение в ответе должно опираться на то, что ты только что
посмотрел.** Не на память, не на «скорее всего», не на «судя по коду должно
быть», не на вчерашний отчёт и не на чужую сводку.

- **Про код** — открой файл и прочитай. Про ветки — выполни `git`.
  Про базу — сделай запрос.
- **Про фронт и экраны — ОТКРОЙ БРАУЗЕР И ПОСМОТРИ.** Чтение кода экрана,
  запросы к API и зелёные тесты **не считаются проверкой интерфейса**.
  Нажми кнопку сам. Если не нажимал — так и скажи, не выдавай за проверенное.
- **Не знаешь — скажи «не знаю, сейчас проверю»** и проверь. Это нормальный
  ответ. Догадка вместо проверки — нет.
- **Не уверен в цифре — не называй цифру.** Сначала посчитай.

## Не экономить контекст на ответах

Запрещено сокращать проверку, чтобы сэкономить контекст или время. Лучше
потратить сто вызовов инструментов и ответить точно, чем ответить быстро и
неверно. Экономия контекста ценой достоверности — то же самое увольнение.

## Проверять себя на перегруз перед каждым ответом

Перед ответом оцени своё состояние. Если контекст перегружен и есть риск начать
нести чушь — **скажи об этом прямо и попроси пересоздать чат**. Признать
«я поплыл, начнём новый чат» — правильное поведение. Продолжать отвечать
наугад — недопустимое.

Признаки, при которых надо остановиться и попросить новый чат: путаешься в том,
что уже сделал; повторяешь одно и то же; отвечаешь по памяти вместо проверки;
не помнишь, проверял ли экран руками.

---

@AGENTS.md

# ⛔️ ЕДИНСТВЕННЫЙ ПРАВИЛЬНЫЙ ПУТЬ ПРОЕКТА

```
/Users/deniscivkunov/Projects/WMS
```

**Это единственная рабочая копия WMS. Другой нет и быть не должно.**

Правила, обязательные к исполнению любой моделью/агентом (Claude, Codex, Cursor, Composer):

1. **Работать только в `~/Projects/WMS`.** Перед первой правкой убедись: `pwd` заканчивается на `/Projects/WMS`. Если ты оказался в другом месте — остановись и скажи об этом пользователю, не правь файлы.
2. **Никогда не создавать вторую копию проекта.** Запрещены новые клоны на Рабочем столе, во «Загрузках», в `/tmp`, а также папки вида `WMS-<что-то>`, `WMS 2`, `WMS copy`. Нужна изоляция — используй `git worktree` **внутри** `~/Projects/WMS/.worktrees/`, и убирай её за собой.
3. **Проект не должен жить на Рабочем столе.** Десктоп синхронизируется с iCloud: файлы выгружаются в «плейсхолдеры», `git status` виснет минутами, а отдельные файлы (был случай с `frontend/package.json`) физически пропадают с диска, оставаясь в гите. Все зависания git в этом проекте до 08.08.2026 были именно из-за этого.
4. **Если что-то «висит» на git-командах — это не Docker и не CI.** Проверь, что ты не в старой папке на Десктопе.

> История вопроса: до 08.08.2026 на Десктопе лежали пять копий (`WMS`, `WMS ` с пробелом в конце имени, `WMS-fbs-qa-move-button-20260807`, `WMS-hotfix-unified-token`, `WMS-iCloud-placeholder-backup-20260805`). Агенты правили бэк в одной, фронт в другой, ветки разъезжались, а пользователь видел на экране результат третьей. Все они удалены. Не воссоздавай их.

## Эталонное состояние кода

Актуальная ветка — **`etalon`**. Она собрана 08.08.2026 из `qa/fbs-move-wb-transfer-button-20260807` (весь операторский флоу ФБС, 30 коммитов) плюс три бэковых коммита из `hotfix/wb-unified-token-fbs-intake`: единый WB-токен для приёмки заказов, устойчивость к сбоям WB и автопривязка складов WB.

**Эталон фронта — таблица FBS-заказов ровно из четырёх колонок:** `Товар`, `Селлер`, `Маршрут сдачи`, `Отгрузить до` (плюс `Статус` для вкладок, кроме «Новые»). Если видишь шесть колонок с `Товар и заказ`, `Селлер и склады`, `Остаток`, `Маршрут и данные` — ты на **устаревшей** ветке (`main`, `feat/fbs-stock-sync`, `hotfix/wb-unified-token-fbs-intake`). Это регресс, а не улучшение.

## Границы правок фронта

**Фронт не трогаем, если задача про бэк.** Нельзя «заодно» добавлять колонки, чипы, статусы, подсказки, блоки «Подробнее» и технические пояснения вида «Следующий шаг — серверная проверка». Любая правка фронта — только по явному запросу пользователя и строго по утверждённому макету. Расширение таблицы заказов уже дважды приводило к откатам.

## Что за проект

WMS фулфилмент-центра (3PL для маркетплейсов). Backend — FastAPI, frontend — React + MUI + Playwright, mobile — Android ТСД (терминал сбора данных).

**Слои backend (не смешивать):** роуты — `backend/app/api`, логика — `backend/app/services`, модели — `backend/app/models`, БД — `backend/app/db`, фоновые задачи (Celery/BackgroundTasks) — `backend/app/tasks`.

**Мобильный ТСД** — отдельный git-репозиторий внутри `mobile/` (у него свой `.git` и **нет remote**, он существует только локально). Состояние — в `mobile/docs/PROGRESS.md`, читать первым при работе по мобилке.

## Гейт перед push

- backend: `cd backend && ruff check . && mypy . && pytest` (есть `backend/.venv`)
- frontend (при изменениях): `cd frontend && npm run build && npm run test:e2e`; юнит — `npm run test:unit` (vitest)

## Процесс

- **Ветвление:** фича-ветка (`feat/…`, `fix/…`, `chore/…`) → PR → зелёный CI → merge. Прямо в `main` не коммитим.
- **CI-контракт:** PR, трогающий `backend/app/api|services` или `frontend/src`, обязан нести в описании блок `### Test coverage` с `TC-ID` и маркерами Given/When/Then (см. AGENTS.md), иначе CI красный. Таблицу берём из `tasks/<slug>/04-test-cases.md`.
- **Регламент задачи** — [`.dev/PROCESS.md`](.dev/PROCESS.md), читать перед работой. Арх-карта — [`.dev/ARCHITECTURE.md`](.dev/ARCHITECTURE.md).
- **На каждый ответ:** [`.dev/COMMUNICATION.md`](.dev/COMMUNICATION.md) (по-человечески, пояснять термины) и [`.dev/LEARNING.md`](.dev/LEARNING.md).
- **Терминология (UI и общение):** «приёмка» = селлер→ФФ (`inbound`), «отгрузка» = ФФ→маркетплейс (`marketplace_unload`). Слово «поставка» в пользовательском UI для селлер→ФФ не используем. Источник — `docs/MVP_DECISIONS_RU.md`.
- **Ритм:** маленькие связные коммиты после каждой единицы; не оставлять грязное дерево; не тащить несвязанный рефактор в задачу.

## Архив

`.archive/` (вне гита) — спасённое из удалённых папок: полная копия `.git` старого чекаута со всеми ветками и 24 стешами, снапшоты `.tar.gz` от 05.08.2026, старые session-handoff'ы. Трогать не нужно; удалить можно, когда убедишься, что ничего не потеряно.
# Решения для старта разработки WMS (зафиксировано)

Документ — **единый источник правды** для автопилота: что строим в MVP, какие интеграции, какие границы. Обновляется только осознанно (через PR).

## Продукт и тенантность

- **Модель**: SaaS — один инстанс → много фулфилментов (тенантов) → у каждого много селлеров.
- **MVP по контуру**: операции склада + **кабинет селлера** + **биллинг v1**.
- **Язык интерфейса**: русский (аккаунт/продукт ориентированы на RU).

## Биллинг v1

- **Хранение**: **литр‑день** — тариф «рубль за литр в сутки» (или копейки; точность в настройках тарифа).
- **Объём**:
  - если у SKU заданы габариты — \(Д \times Ш \times В\) → литры на штуку × количество;
  - иначе — объём **тары/места** (короб/пакет/лот);
  - допускается **ручной override** объёма на позицию/поставку на период онбординга.
- **Граница суток и валюта**: **МСК**, **RUB**, **НДС в MVP не считаем** (отдельный эпик позже).

## Аутентификация

- **Email + пароль** (+ восстановление пароля по плану реализации).
- Роли и изоляция данных обязательны с первых релизов кабинета и мультитенантности.

## Wildberries (первая интеграция)

### Принцип

- **Только импорт (read/sync в нашу систему)** для **FBW** (карточки, FBW-поставки) и прочих read-only интеграций. Создание **FBW**-поставок и карточек **в WB через наш продукт в MVP не делаем** (нет исходящих «create» в сторону WB для FBW).

### Исключение: FBS operator flow (осознанное, FBSFLOW-130)

- **FBS (Wildberries «Маркетплейс»)** — полный операторский цикл **с исходящими вызовами WB**: создание поставки, batch-добавление заказов, metadata/КИЗ, стикеры, грузоместа ПВЗ, передача в доставку, post-delivery sync.
- Контракт, ошибки и тест-кейсы: `tasks/fbs-operator-flow/` (`BACKEND_CONTRACT.md`, `ERROR_CATALOG_RU.md`, `OPENAPI_NOTES.md`, `TEST_CASES.md` → `docs/IMPLEMENTED_PRODUCT_SCENARIOS_TEST_CASES_EN.md` §S17).
- Эмулятор WB в CI **не заменяет** live smoke (`TC-S17-024`); секреты — только env / manual job.
- Старые задачи `tasks/fbs-frontend-*` и `tasks/fbs-marketplace-orders/SPEC.md` описывают **ранний** контракт (`sticker_file`, bind order→trbx) и **deprecated** для нового frontend.

### Методы API (ориентир документации WB)

| Назначение | Метод | Базовый хост (по документации) |
|------------|--------|--------------------------------|
| Список карточек товаров | `POST /content/v2/get/cards/list` | `https://content-api.wildberries.ru` |
| Список поставок FBW | `POST /api/v1/supplies` | `https://supplies-api.wildberries.ru` |
| Детали поставки | `GET /api/v1/supplies/{ID}` | `https://supplies-api.wildberries.ru` |
| Товары поставки | `GET /api/v1/supplies/{ID}/goods` | `https://supplies-api.wildberries.ru` |

Ссылки на портал WB API:

- [Карточки товаров — список](https://dev.wildberries.ru/docs/openapi/work-with-products#tag/Kartochki-tovarov/paths/~1content~1v2~1get~1cards~1list/post)
- [Поставки FBW — детали поставки](https://dev.wildberries.ru/docs/openapi/orders-fbw#tag/Informaciya-o-postavkah/paths/~1api~1v1~1supplies~1%7BID%7D/get)
- [Поставки FBW — товары поставки](https://dev.wildberries.ru/docs/openapi/orders-fbw#tag/Informaciya-o-postavkah/paths/~1api~1v1~1supplies~1%7BID%7D~1goods/get)

### Токены и лимиты

- У WB разные категории доступа (**Контент**, **Поставки** и т.д.) — в продукте хранить секреты **только** в env / secrets, раздельно по тенанту/селлеру.
- Соблюдать лимиты запросов из документации (очередь, backoff, идемпотентность по `requestId` где применимо).

## Терминология (фулфилмент ↔ маркетплейс, RU)

Закреплено для продукта и UI; в коде/API могут оставаться исторические имена (`marketplace_unload`, `outbound` и т.д.) — смысл в интерфейсе и документации **только** по таблице ниже.

| Термин (RU) | Смысл | Кто создаёт / инициатор |
|-------------|--------|-------------------------|
| **Приёмка** (ранее «поставка» в UI) | Товар идёт **от селлера на фулфилмент** (входящий поток на склад ФФ). В портале ФФ — раздел **«Приёмка»**; в коде/API — `inbound` / `inbound_intake`. | **Селлер** создаёт заявку; **ФФ** ведёт приёмку в очереди «Приёмка» → «Сортировка». |
| **Отгрузка** (в смысле «на МП») | Фулфилмент **отгружает на маркетплейс** (склад МП / вывоз на площадку). **Не** называем это «выгрузкой» или *download* в пользовательских текстах. | **ФФ** (операции; документ в системе может называться технически `marketplace_unload` и т.п.). |

Если в сообщении без контекста неясно, о какой цепочке речь — **уточнять у автора**: приёмка (селлер→ФФ) или отгрузка (ФФ→МП). Термин «поставка» в пользовательском UI **не используем** для селлер→ФФ (кроме импорта WB FBW, если явно про склад МП).

## Онбординг селлера (кто и как)

- **Добавляет только админ FF** (раздел «Селлеры» в портале): название бренда + **email**.
- Цепочка API: `POST /sellers` → `POST /auth/seller-accounts` **без пароля** → в БД случайный hash и `must_set_password=true`.
- Админ передаёт селлеру **email** и URL портала селлера; **пароль по API не возвращается**.
- **Первый вход селлера:** email + **пустой пароль** → экран задания пароля → дальше обычный вход.
- UI FF: только MUI, см. `docs/UI_DESIGN_SYSTEM_RU.md`.

## Кабинет селлера (MVP — функции)

Селлер в кабинете должен иметь как минимум:

1. **Создание заявок** (входящие процессы: приёмка / отгрузка — формулировка бизнес‑сущности «заявка» в терминах WMS).
2. **Приём** — сценарий участия/контроля приёмки со стороны селлера (в объёме MVP: статусы, состав, расхождения — по мере готовности бэкенда).
3. **Отгрузка** — создание/отслеживание отгрузки на стороне фулфилмента; синхронизация с импортом WB для FBW — **только отображение импортированных поставок**, без создания в WB из UI в MVP.

## Печать

- **Накладная поставки (приёмка на ФФ):** в документе поставки (приёмка) — кнопка «Печать накладной», A4 в браузере; для сотрудника склада перед приёмкой по составу и коробам.
- **Накладная отгрузки:** отгрузка на МП и operational outbound — отдельные кнопки в соответствующих документах.
- **Этикетки коробов:** термо **58×40**, CODE128 — в блоке коробов приёмки (не путать с накладной).
- Принтер этикеток: **термо 58 мм**, лента **40 мм** (детали ESC/POS/ZPL — в задаче на модуль печати).

## Упаковка и задание на упаковку

Полное описание процесса: **[docs/PACKAGING_RU.md](PACKAGING_RU.md)**.

- **Документ «Задание на упаковку»** — один тип, одна форма; раздел **«Упаковка»** в операциях ФФ. Не этап жизненного цикла приёмки.
- **Остаток в месте** делится на **не упаковано** и **упаковано**; задание переводит количество между ними без смены ячейки.
- **Три входа** (приёмка/сортировка, ручное задание, отгрузка на МП) — один и тот же документ; из сортировки можно упаковать **часть** товара **без** отгрузки.
- **Отгрузка на МП:** задание на упаковку создаётся при **«Утверждено»** (`confirmed`), на **весь план** (1:1 с документом отгрузки). Сборка в короба и упаковка идут **параллельно** — незавершённая упаковка **не блокирует** создание и наполнение коробов. **«Отгружено»** (ship) — только при **завершённом** задании упаковки + распределении по коробам + ЧЗ где нужно. UI документа: **2 вкладки** («Товары», «Упаковка»); короба и ship — на «Товарах» / в footer. Прогресс упаковки для MP — **счётчик в задании** (без колонки «На полке упак.» и без `apply_packaging_convert` по ячейкам). Печать с вкладки упаковки — **MarkingPrintDialog** (конструктор ленты).
- Строка задания (общий тип «Упаковка»): для заданий **не** из MP — всего / упаковано на полке / …; для MP-unload — только прогресс «упаковано в работе» по строкам.

## Адресное хранение (отгрузка на МП и приёмка)

- **DEC-009:** для нового ФФ адресное хранение **включено** по умолчанию (`tenants.address_storage_enabled`).
- **DEC-018:** «зона сортировки» = виртуальная `StorageLocation` приёмки; источник списания, пока товар не разложен по ячейкам.
- **DEC-019:** при выключении адресного хранения (`true → false`) система **атомарно переносит** все ненулевые остатки с адресных ячеек на виртуальную ячейку зоны сортировки (DEC-018). Toggle **не блокируется**; обратной автомиграции при включении нет. В настройках ФФ после успешного выключения — info «Остатки с ячеек перенесены на зону сортировки».

## Резерв под внутреннюю отгрузку (outbound)

- В `draft` / `submitted`: резерв по строке — **по ячейке**, если ячейка назначена; иначе **по складу ФФ** (суммарно по SKU, без привязки к ячейке).
- **Submit** разрешён без ячейки: предварительный резерв держит доступное на складе.
- **Списание / фактический подбор** — только с назначенными ячейками (`lines_missing_storage` при проведении, если ячейки нет).
- Ячейку можно назначить при добавлении строки, в черновике или при подборе после submit.

## E2E и автопилот

- Каждая фича с UI → минимум один Playwright‑сценарий **пользовательского** пути + стабильные `data-testid`.
- Внешний WB API в CI **мокируется** (фикстуры/Wiremock/записанные ответы); отдельные «живые» смоук‑тесты — вручную или в ночном job с секретами.

## Что сознательно не входит в MVP (по текущим решениям)

- Создание поставок и карточек в WB через API из продукта.
- НДС и бухгалтерия полного цикла.

  → Прочитать обязательные правила и текущую реализацию sorting-objects/place/схем
  • Добавить адресные тесты для содержимого ячеек, снятия и вложения в тару
  • Исправить сервис, роут и схему ответа только в backend
  • Прогнать заданные ruff, mypy, pytest и back_guard
  • Записать итоговый brief, проверить diff и создать отдельный коммит
exec
/bin/zsh -lc "sed -n '1,260p' .dev/PROCESS.md && sed -n '1,260p' .dev/ARCHITECTURE.md && sed -n '1,220p' .dev/COMMUNICATION.md && sed -n '1,220p' .dev/LEARNING.md && git branch --show-current && git log -1 --oneline --decorate" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
# PROCESS.md — регламент разработки WMS (ИСТОЧНИК ПРАВДЫ)

> Этот файл — единый регламент для **любого** агента (Claude Code, Cursor, Codex) и человека.
> `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/process.mdc` — только указатели сюда.
> **Прочитай этот файл ПЕРЕД тем, как что-либо трогать. Правишь процесс — правь здесь, в одном месте.**

Ниже — как в WMS ведётся любая задача. Правила окружения и CI из [`AGENTS.md`](../AGENTS.md)
(вертикальный срез, разделение слоёв, обязательная таблица `### Test coverage`, зелёный CI, ветка→PR→main)
остаются в силе и здесь не дублируются — этот файл описывает **последовательность стадий и гейты** поверх них.

**Глобальные правила на каждый ответ** (действуют всегда, не только внутри задач):
[`COMMUNICATION.md`](COMMUNICATION.md) — по-человечески + пояснять термины; [`LEARNING.md`](LEARNING.md) —
обучать по продукту (разбор на гейте + добивка по дырам из [`ARCHITECTURE.md`](ARCHITECTURE.md)).

---

## 0. Хребет задачи — папка `tasks/<slug>/`

Любая работа = одна задача = одна папка `tasks/<slug>/` (slug — короткий kebab-case, напр.
`tasks/fbs-orders-intake/`). Агент, взявшийся за задачу, **обязан** складывать туда артефакты по
стадиям. Дисциплину держит не запрет, а **след**: нет артефакта стадии — стадия не пройдена, это видно.

Обязательные файлы (по мере прохождения; какие именно — зависит от маршрута, см. §1):
```
tasks/<slug>/
  00-triage.md        # тип, размер, маршрут
  01-analysis.md      # разбор + вопросы человеку (арх-коучинг)
  02-arch-decision.md # арх-решение + СТЕК + явное «ок» человека (ГЕЙТ 1)
  03-contract.md      # критерии приёмки (BA); можно пропустить для S
  04-test-cases.md    # тест-кейсы + маппинг на TC-ID (см. §5); по ним валидируется код
  05-review.md        # вердикт независимого ревью + прогон тестов
  06-doc.md           # конфа: бизнес + технически + почему (→ внешний док)
```

Артефакты заполняются **из шаблонов** в [`templates/`](templates/) (`00-triage.md … 06-doc.md`):
копируешь шаблон в `tasks/<slug>/` и заполняешь — форму не выдумываешь.

Активный агент в начале работы **читает всё, что уже есть в `tasks/<slug>/`**, и продолжает с той
стадии, где след обрывается. Не переделывает пройденное молча.

Связь с GitHub Issue (по [`AGENTS.md`](../AGENTS.md)): одна задача `tasks/<slug>/` ↔ одна Issue с
меткой `ready`. Slug и номер Issue вписываются в `00-triage.md`, чтобы след в репо и трекер не расходились.

---

## 1. Типы задач и маршруты

Первое действие по любой задаче — **классифицировать** (стадия 0, `00-triage.md`):

- **Тип:** `bug` | `feature` | `доработка` | `рефакторинг`.
- **Размер:** `S` (мелочь, часы) | `M` | `L`.
- **Маршрут** — из типа+размера:

| Тип / размер | Маршрут (стадии) |
|---|---|
| **bug** | 0 → 1(причина, кратко) → [ГЕЙТ 1 только если меняется архитектура] → 4(фикс) → 3(регресс-кейс) → 5 → 6 |
| **feature S** | 0 → 1 → **ГЕЙТ 1** → 4 → 3(тест) → 5 → 6 (контракт §2 пропускаем) |
| **feature/доработка M–L** | 0 → 1 → **ГЕЙТ 1** → 2 → 3 → 4 → 5 → [ГЕЙТ 2 опц.] → 6 (полный цикл) |
| **рефакторинг** | 0 → 1 → **ГЕЙТ 1** → 4 → 5(тесты-регресс зелёные) → 6 |

Маршрут масштабируется под размер: мелкое не гоняем через весь конвейер, крупное — через весь.

---

## 2. Конвейер стадий

Пайплайн едет **сам**: стадии идут по порядку, каждая читает артефакт предыдущей и дописывает свой.
Человека дёргаем **только на гейтах** (§3). Остальное агент делает автономно.

**Что именно делает каждая роль — в [`roles/`](roles/) (по файлу на роль: миссия, вход, выход,
что обязан сделать, DoD, что запрещено).** Ниже — сводка.

| # | Стадия | Роль | Что делает | Выход |
|---|--------|------|-----------|-------|
| 0 | **Триаж** | [Триажёр](roles/triage.md) | классифицирует (тип/размер/маршрут) | `00-triage.md` |
| 1 | **Анализ + арх-коучинг** | [Аналитик](roles/analyst.md) | разбирает по продукту и технике; **качает человека вопросами**; предлагает стек | `01-analysis.md` |
| — | **🔒 ГЕЙТ 1** | человек | подтверждает архитектуру и стек | `02-arch-decision.md` |
| 2 | **Контракт** | [BA](roles/ba.md) | критерии приёмки, границы (для S — пропуск) | `03-contract.md` |
| 3 | **Тест-дизайн** | [Тест-дизайнер](roles/test-designer.md) | тест-кейсы из контракта + маппинг на TC-ID | `04-test-cases.md` |
| 4 | **Код** | [Кодер](roles/coder.md) | пишет вертикальный срез по стандарту §4 | код + зелёные тесты |
| 5 | **Ревью + прогон** | [Ревьюер](roles/reviewer.md) | **независимый** (не автор кода) ищет, где плохо и где контракт не выполнен; прогоняет гейты | `05-review.md` |
| — | **🔒 ГЕЙТ 2 (опц.)** | человек | приёмка до merge | отметка в `05-review.md` |
| 6 | **Док** | [Доккер](roles/doccer.md) | конфа: что/зачем, бизнес+технически, **почему** → `docs/` | `06-doc.md` |

**Ревью (стадия 5) обязано быть независимым:** автор кода не оценивает свою работу сам. В Claude Code —
через отдельного субагента-ревьюера (или `/code-review`); в Cursor/Codex — отдельным проходом с явной
установкой «ищи, где плохо и где контракт не выполнен», не «подтверди, что всё хорошо».

---

## 3. Гейты — где пайплайн ОБЯЗАН позвать человека

Гейты только ключевые. На гейте пайплайн **встаёт и спрашивает человека, ждёт явного ответа**, потом едет.

- **🔒 ГЕЙТ 1 — Архитектура + стек (жёсткий, обязателен для feature/доработка/рефакторинг).**
  Прежде чем писать код, агент фиксирует в `02-arch-decision.md`: как решаем, каким **стеком**, какие
  затрагиваются части системы, есть ли миграции — и **явно спрашивает человека**. Без записанного «ок»
  человека — **кодить нельзя**. Для бага — только если фикс меняет архитектуру.
- **🔒 ГЕЙТ 2 — Приёмка (мягкий/опциональный).** Перед merge показать результат и критерии приёмки;
  если человек не возразил — двигаемся. Человек может сделать его жёстким для конкретной задачи.

Вне гейтов агент работает сам и человека не дёргает.

---

## 4. Стандарт кода

Код должен быть **человекочитаемым, не мусорным**:
- Читается как окружающий код: те же имена, идиомы, плотность комментариев, что вокруг.
- **Слои не смешиваются** ([`AGENTS.md`](../AGENTS.md)): роуты — `backend/app/api`, бизнес-логика —
  `backend/app/services`, модели — `backend/app/models`, доступ к БД — `backend/app/db`,
  фоновые задачи — `backend/app/tasks`.
- **Новые/правимые экраны ФФ — единый MUI** (эталон `FfProductsCatalogScreen.tsx`), без legacy `Card`/`Input`.
- Понятные имена; без мёртвого кода, закомментированных блоков, отладочного мусора.
- Маленькие связные коммиты (одна единица — один коммит), а не один большой в конце.
- Меняешь минимум для задачи; не тащишь несвязанный рефактор в тот же коммит.
- **Гейт перед push** (обязателен):
  - backend: `cd backend && ruff check . && mypy . && pytest`
  - frontend (при изменениях): `cd frontend && npm run build && npm run test:e2e`
- **Ветвление:** работаем в фича-ветке (`feat/…`, `fix/…`, `chore/…`), merge в `main` — только через PR
  с зелёным CI. Долгоживущих веток не держим.

---

## 5. Тесты и трассируемость (стык с CI-контрактом)

WMS уже требует ([`AGENTS.md`](../AGENTS.md)): каждый PR, трогающий `backend/app/api|services` или
`frontend/src`, несёт в описании блок **`### Test coverage`** — таблицу с `TC-ID` (`TC-Sxx-yyy` из
[`docs/IMPLEMENTED_PRODUCT_SCENARIOS_TEST_CASES_EN.md`](../docs/IMPLEMENTED_PRODUCT_SCENARIOS_TEST_CASES_EN.md)
или новые `TC-NEW-*`), со строкой `Y` и маркерами Given/When/Then; иначе CI красный.

Поэтому стадия 3 (`04-test-cases.md`) — **не параллельная бюрократия, а тот же спек**: каждый тест-кейс
маппится на `TC-ID`, а Playwright-сценарий проверяет **видимый результат** через `data-testid`, а не
просто HTTP 200. Таблица из `04-test-cases.md` переносится в описание PR как блок `### Test coverage`.

---

## 6. Документация по фиче → `docs/`

Каждая задача завершается артефактом `06-doc.md` — конфа-страницей:
- **что и зачем** сделали; для бага — **суть бага**;
- что поменяли **технически** и что это значит **бизнесово**;
- **почему** так, какие решения приняты (ссылка на `02-arch-decision.md`).

Дев-дока WMS живёт в репо, в папке [`docs/`](../docs/) (там же — MVP-решения, сценарии, бэклоги).
Куда класть конфу и как связывать со сценариями — в [`doc-store.md`](doc-store.md). Исходник конфы
остаётся в `tasks/<slug>/06-doc.md` (след задачи), а долгоживущее правило/сценарий — в `docs/`.

**RAG/внешняя вики отложены** — сейчас хватает `docs/` и поиска по репо. Навесим позже поверх тех же
markdown-страниц, переделывать доку не придётся.

---

## Итог одной строкой

Задача → `tasks/<slug>/` → триаж → анализ+коучинг → **[ГЕЙТ: арх+стек]** → (контракт) → тесты →
код (вертикальный срез) → независимое ревью → (приёмка) → PR с зелёным CI → док в `docs/`.
Пайплайн едет сам, человека зовёт только на гейтах.
# ARCHITECTURE.md — живая арх-карта WMS + реестр знаний

> Наполняется по [`LEARNING.md`](LEARNING.md): на гейтах задач (человек объясняет → сверка с кодом)
> и на добивках. Здесь фиксируется, **как система устроена на самом деле** (подтверждённо, с `file:line`),
> и что человек ещё не знает (реестр дыр). Ничего не пишем «по памяти модели» — только подтверждённое кодом.
>
> Пути в колонке «Где в коде» — **первичная прикидка**, статус у всех `не подтверждено`, пока не сверили
> на гейте. Уточняем и проставляем `file:line` при первом разборе соответствующей подсистемы.

## Подсистемы (статус понимания человеком)
Статусы: `ok` (объяснил, сверено с кодом) · `плаваю` (объяснял с ошибками) · `не подтверждено` (ещё не проверяли).

| Подсистема | Где в коде (прикидка, сверить на гейте) | Статус |
|---|---|---|
| Мультитенантность / изоляция данных | `backend/app/models/tenant.py`, `seller.py`, `deps.py` | не подтверждено |
| Аутентификация / роли / порталы | `backend/app/api/auth.py`, `services/auth_service.py` | не подтверждено |
| Приёмка (селлер → ФФ) | `backend/app/api/inbound_intake.py`, `services/inbound_intake_service.py` | не подтверждено |
| Отгрузка на МП (ФФ → маркетплейс) | `backend/app/models/marketplace_unload.py`, `services/marketplace_unload_*` | не подтверждено |
| Остатки / движения / резервы | `backend/app/models/inventory_*`, `services/inventory_service.py` | не подтверждено |
| Адресное хранение (ячейки/стеллажи/короба) | `backend/app/models/storage_location.py`, `warehouse_*`, `services/sorting_location_service.py` | не подтверждено |
| Упаковка (задания, биллинг) | `backend/app/models/packaging_task.py`, `services/packaging_task_service.py` | не подтверждено |
| Честный Знак (коды маркировки, печать) | `backend/app/models/marking_code.py`, `services/marking_*` | не подтверждено |
| Печать / конструктор шаблонов | `backend/app/models/print_template.py`, `services/print_template_service.py` | не подтверждено |
| Интеграция Wildberries (import-only) | `backend/app/services/wildberries_*`, `api/wildberries_integration.py` | не подтверждено |
| Фоновые задачи (Celery / BackgroundTasks) | `backend/app/tasks/`, `services/background_job_service.py` | не подтверждено |
| Фронтенд-портал ФФ (MUI) | `frontend/src`, эталон `FfProductsCatalogScreen.tsx` | не подтверждено |
| Мобильный ТСД (Android) | `mobile/`, состояние в `mobile/docs/PROGRESS.md` | не подтверждено |

## Реестр дыр (что подтянуть)
Порядок разбора — сверху вниз (сверху самое давнее/важное). Закрытая дыра → статус `закрыто`, тема в таблице выше → `ok`.

| # | Подсистема | Что именно неясно (со слов человека) | Заведено | Статус |
|---|---|---|---|---|
| _пусто_ | | заполняется на гейтах и добивках | | |

## Подтверждённые решения (арх-карта)
Сюда добавляются `02-arch-decision.md` из закрытых задач — как принятые архитектурные решения.
- _(пока пусто — первое решение появится после первой задачи через конвейер)_
# COMMUNICATION.md — как агент разговаривает (ГЛОБАЛЬНО, каждый ответ)

> Эти правила обязательны для **любого** агента (Claude Code, Cursor, Codex) в **каждом** ответе —
> не только внутри задач конвейера. Указатель на этот файл — в `AGENTS.md`, `CLAUDE.md`, `.cursor/rules`.

## Правило 1 — отвечай нормальным человеческим языком, повествовательно

Ответ — это **объяснение для человека, а не заметка разработчика самому себе.** Пиши связными
предложениями, разворачивай мысль, объясняй **почему**, а не только **что**. Веди читателя за руку.

**Так НЕЛЬЗЯ (стиль «заметка в блокноте», который бесит):**
- рубленые огрызки без связок; `file:line` в каждой строке вместо объяснения;
- таблица/список вываленных фактов ВМЕСТО того, чтобы объяснить суть словами;
- жаргон и сокращения, понятные только автору; телеграфный «сделал X, поправил Y, см. Z».

**Так НАДО:**
- сначала суть простыми словами, потом детали; причинно-следственные связи проговорены;
- структура (заголовки/списки) допустима, но как каркас поверх прозы, а не замена ей;
- ссылки на код и таблицы — в поддержку объяснения, а не вместо него.

Важно: «по-человечески» ≠ «многословно». Не лей воду и не раздувай. Ясно, полно, по делу — но
живым языком. Короткий связный абзац лучше длинного, и лучше вываленного списка огрызков.

## Правило 2 — незнакомые термины объясняй сразу, инлайн, всегда

Как только в ответе появляется термин, сленг, аббревиатура или название инструмента, которое человек
может не знать, — **тут же поясни его прямо в тексте**, коротко и по-русски, не прерывая мысль и не
заставляя переспрашивать. Цель — человек учится по ходу, а не спотыкается о жаргон.

Форма: первое употребление — с кратким пояснением в скобках или соседним предложением. Например:
«…создаём поставку ФБС (сборочное задание маркетплейса на конкретный заказ покупателя)…».
Не превращай это в лекцию — одного точного пояснения достаточно; заведомо известное не разжёвывай.

## Почему это правило вообще есть
Пользователь десятки раз (проверено по истории чатов) просил «ответь по-человечески» — потому что
дефолтный стиль ИИ съезжает в телеграфную заметку. Это не косметика: непонятный ответ = потраченное
время и потеря контроля над продуктом. Нарушение этих двух правил считается плохим ответом.
# LEARNING.md — обучение по продукту (ГЛОБАЛЬНО)

> Обязательно для любого агента (Claude Code, Cursor, Codex). Цель — чтобы человек **знал свой продукт
> и мог объяснить, как он устроен, без ИИ.** Разработка через нейронки это знание атрофирует; здесь — противоядие.

## Три жёстких принципа
1. **Правда — из кода, не из памяти модели.** Каждый вопрос и «правильный ответ» агент выводит из
   реального файла и показывает `file:line`. Никаких «по памяти» — так человека можно научить галлюцинации.
2. **Сначала человек, потом ответ (active recall).** Агент спрашивает и ждёт версию человека (пусть с
   ошибкой), и только потом показывает, как на самом деле, и где было расхождение. Вспоминание до
   подсказки — это то, что строит память; лекция без вопроса не оседает.
3. **Понимание, а не факты.** Спрашивай «проведи меня по шагам от X к Y», «почему так, что сломается,
   если поменять», «где искать, если Z упал» — а не «сколько потоков» (это гуглится в конфиге).

## Две передачи

### A. Разбор в момент работы (основной, event-driven)
Привязан к конвейеру. **На ГЕЙТЕ 1 каждой задачи**, прежде чем менять подсистему, человек своими
словами объясняет, как она работает сейчас; агент сверяет с кодом и подсвечивает промахи. Здесь оседает
большая часть знания — оно контекстное и сразу полезное. Промахи и незакрытые темы → в реестр (ниже).

### B. Добивка по остывающим темам (вспомогательный, **по дырам**)
**На старте сессии** агент смотрит реестр дыр в [`ARCHITECTURE.md`](ARCHITECTURE.md):
- если **есть открытые дыры** — предлагает короткую добивку: **одна подсистема, 2–3 вопроса, ~3 минуты**,
  берётся самая «остывшая»/давняя дыра; после — обновляет её статус;
- если **дыр нет** — молчит, не мешает;
- по команде человека «погоняй меня» — запускает добивку принудительно, даже если реестр пуст.

Не глобальный экзамен. Одна тема за раз, бьём по дырам, а не наугад.

## Реестр знаний
Живёт в [`ARCHITECTURE.md`](ARCHITECTURE.md): список подсистем WMS + статус понимания (`ok` / `плаваю` /
`не подтверждено`) + таблица открытых дыр. Наполняется на гейтах (передача A) и добивках (передача B).
Это же — живая арх-карта: подтверждённые объяснения фиксируются как «как оно устроено на самом деле».

## Порог
Обучение не должно тормозить работу: добивка B — это предложение на старте сессии, человек может сказать
«не сейчас». Разбор A на гейте — часть процесса и не пропускается (гейт и так обязателен).
codex/fix-raskladka-20260829
14111d2d (HEAD -> codex/fix-raskladka-20260829, origin/staging, integration/vse-v-etalon-20260828, codex/fix-p2-20260829, codex/fix-ozon-20260829) chore(audit): сверка остатка продавца с кабинетом WB

codex
Worktree чист по отслеживаемым файлам; единственный посторонний файл — уже существующий `ZADANIE.md`, его не затрагиваю. Ветка `codex/fix-raskladka-20260829` стоит на проверенном SHA `14111d2d`. Теперь разбираю фактический контракт данных: как карта склада строит дерево ячеек, как `_sorting_tree_rows` его разворачивает и что именно принимает `move_object`.
exec
/bin/zsh -lc "rg -n \"def get_sorting_objects|def _sorting_tree_rows|def get_warehouse_map|def place_sorting_object|SORTING_LOCATION_CODE|class SortingObjects|SortingObjectPlace|sorting-objects/place|sorting-objects\" backend/app/services/warehouse_map_service.py backend/app/api/warehouses.py backend/app/schemas backend/tests -g '*.py'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
backend/app/api/warehouses.py:223:class SortingObjectsOut(BaseModel):
backend/app/api/warehouses.py:264:async def get_warehouse_map_route(
backend/app/api/warehouses.py:328:@router.get("/{warehouse_id}/sorting-objects", response_model=SortingObjectsOut)
backend/app/api/warehouses.py:329:async def get_sorting_objects_route(
backend/app/api/warehouses.py:344:    "/{warehouse_id}/sorting-objects",
backend/app/api/warehouses.py:367:@router.post("/{warehouse_id}/sorting-objects/place", response_model=WarehouseMapMoveOut)
backend/app/api/warehouses.py:368:async def place_sorting_object_route(
backend/app/services/warehouse_map_service.py:30:    SORTING_LOCATION_CODE,
backend/app/services/warehouse_map_service.py:318:async def get_warehouse_map(
backend/app/services/warehouse_map_service.py:451:            and root_location.code != SORTING_LOCATION_CODE
backend/app/services/warehouse_map_service.py:460:            if location.code == SORTING_LOCATION_CODE:
backend/app/services/warehouse_map_service.py:612:            or location.code == SORTING_LOCATION_CODE
backend/app/services/warehouse_map_service.py:688:    if location is None or location.code == SORTING_LOCATION_CODE:
backend/app/services/warehouse_map_service.py:1061:def _sorting_tree_rows(
backend/app/services/warehouse_map_service.py:1092:async def get_sorting_objects(
backend/app/services/warehouse_map_service.py:1128:                        StorageLocation.code != SORTING_LOCATION_CODE,
backend/app/services/warehouse_map_service.py:1181:async def place_sorting_object(
backend/tests/test_warehouse_map_api.py:24:    SORTING_LOCATION_CODE,
backend/tests/test_warehouse_map_api.py:281:        f"/warehouses/{warehouse.id}/sorting-objects", headers=headers
backend/tests/test_warehouse_map_api.py:288:        f"/warehouses/{warehouse.id}/sorting-objects/place",
backend/tests/test_warehouse_map_api.py:394:                f"/warehouses/{warehouse.id}/sorting-objects", headers=headers
backend/tests/test_warehouse_map_api.py:400:        f"/warehouses/{warehouse.id}/sorting-objects/place",
backend/tests/test_warehouse_map_api.py:418:                        StorageLocation.code == SORTING_LOCATION_CODE,
backend/tests/test_sorting_object_creation_api.py:67:        f"/warehouses/{warehouse.id}/sorting-objects",
backend/tests/test_sorting_object_creation_api.py:79:        f"/warehouses/{warehouse.id}/sorting-objects",
backend/tests/test_sorting_object_creation_api.py:137:        f"/warehouses/{owner_warehouse.id}/sorting-objects",
backend/tests/test_sorting_object_creation_api.py:144:        f"/warehouses/{foreign_warehouse.id}/sorting-objects",
backend/tests/test_sorting_object_creation_api.py:153:        f"/warehouses/{owner_warehouse.id}/sorting-objects",
backend/tests/test_sorting_object_creation_api.py:167:        f"/warehouses/{uuid.uuid4()}/sorting-objects",
backend/tests/test_ownership_transfer_service.py:33:from app.services.sorting_location_service import SORTING_LOCATION_CODE
backend/tests/test_ownership_transfer_service.py:218:                StorageLocation.code == SORTING_LOCATION_CODE,
backend/tests/test_inventory_counts.py:22:    SORTING_LOCATION_CODE,
backend/tests/test_inventory_counts.py:307:    assert cells[0]["label"] != SORTING_LOCATION_CODE
backend/tests/test_marketplace_unload_pick_from_container.py:123:        f"/warehouses/{wid}/sorting-objects", headers=h, json={"kind": "box"}
backend/tests/test_warehouse_structure_wave1.py:37:from app.services.sorting_location_service import SORTING_LOCATION_CODE
backend/tests/test_warehouse_structure_wave1.py:564:                StorageLocation.code == SORTING_LOCATION_CODE,

exec
/bin/zsh -lc "sed -n '180,410p' backend/app/api/warehouses.py && sed -n '300,520p' backend/app/services/warehouse_map_service.py && sed -n '1020,1225p' backend/app/services/warehouse_map_service.py && sed -n '1,470p' backend/tests/test_warehouse_map_api.py && sed -n '1,220p' backend/tests/test_sorting_object_creation_api.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
class WarehouseMapDisbandOut(BaseModel):
    id: str
    disbanded: bool


class SortingObjectOut(BaseModel):
    id: str
    kind: Literal["pallet", "box", "cargo_place"]
    code: str
    barcode: str
    holder: str | None


class SortingGoodsLineOut(BaseModel):
    id: str
    productId: str
    qty: int
    holder: str | None


class SortingAlreadyAtOut(BaseModel):
    cellId: str
    code: str
    qty: int


class SortingProductOut(BaseModel):
    id: str
    name: str
    sku: str
    seller: str
    barcode: str
    photo: str
    size: str | None
    alreadyAt: list[SortingAlreadyAtOut]


class SortingCellOut(BaseModel):
    id: str
    code: str
    barcode: str


class SortingObjectsOut(BaseModel):
    objects: list[SortingObjectOut]
    lines: list[SortingGoodsLineOut]
    products: list[SortingProductOut]
    cells: list[SortingCellOut]


class SortingObjectCreateIn(BaseModel):
    kind: Literal["pallet", "box", "cargo_place"]


class SortingPlaceIn(BaseModel):
    kind: Literal["product", "pallet", "box", "cargo_place"]
    id: uuid.UUID
    cell_id: uuid.UUID | None = None
    to_id: uuid.UUID | None = None
    qty: int = Field(gt=0)


def _map_error(exc: WarehouseMapError) -> HTTPException:
    if exc.code in {
        "warehouse_not_found",
        "object_not_found",
        "destination_not_found",
        "cell_not_found",
        "pallet_not_found",
    }:
        return HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=exc.code)
    if exc.code in {
        "address_storage_disabled",
        "container_cycle",
        "invalid_container_destination",
        "insufficient_stock",
        "pallet_disbanded",
        "pallet_identifier_conflict",
    }:
        return HTTPException(status_code=status.HTTP_409_CONFLICT, detail=exc.code)
    return HTTPException(status_code=status.HTTP_422_UNPROCESSABLE_ENTITY, detail=exc.code)


@router.get("/{warehouse_id}/map", response_model=WarehouseMapOut)
async def get_warehouse_map_route(
    warehouse_id: uuid.UUID,
    user: Annotated[User, Depends(require_catalog_cells_read_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> WarehouseMapOut:
    try:
        data = await warehouse_map_service.get_warehouse_map(session, user.tenant_id, warehouse_id)
    except WarehouseMapError as exc:
        raise _map_error(exc) from None
    return WarehouseMapOut.model_validate(data)


@router.post("/{warehouse_id}/map/move", response_model=WarehouseMapMoveOut)
async def move_warehouse_map_object_route(
    warehouse_id: uuid.UUID,
    body: WarehouseMapMoveIn,
    user: Annotated[User, Depends(require_cells_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> WarehouseMapMoveOut:
    try:
        result = await warehouse_map_service.move_object(
            session,
            tenant_id=user.tenant_id,
            warehouse_id=warehouse_id,
            actor_user_id=user.id,
            kind=body.kind,
            object_id=body.id,
            to_kind=body.to_kind,
            to_id=body.to_id,
            quantity=body.qty,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return WarehouseMapMoveOut.model_validate(result)


@router.post("/{warehouse_id}/map/disband", response_model=WarehouseMapDisbandOut)
async def disband_warehouse_map_pallet_route(
    warehouse_id: uuid.UUID,
    body: WarehouseMapDisbandIn,
    user: Annotated[User, Depends(require_cells_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> WarehouseMapDisbandOut:
    pallet_id = body.id or body.pallet_id
    if pallet_id is None:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail="pallet_id_required",
        )
    try:
        result = await warehouse_map_service.disband_pallet(
            session,
            tenant_id=user.tenant_id,
            warehouse_id=warehouse_id,
            actor_user_id=user.id,
            pallet_id=pallet_id,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return WarehouseMapDisbandOut.model_validate(result)


@router.get("/{warehouse_id}/sorting-objects", response_model=SortingObjectsOut)
async def get_sorting_objects_route(
    warehouse_id: uuid.UUID,
    user: Annotated[User, Depends(require_catalog_cells_read_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> SortingObjectsOut:
    try:
        data = await warehouse_map_service.get_sorting_objects(
            session, user.tenant_id, warehouse_id
        )
    except WarehouseMapError as exc:
        raise _map_error(exc) from None
    return SortingObjectsOut.model_validate(data)


@router.post(
    "/{warehouse_id}/sorting-objects",
    response_model=SortingObjectOut,
    status_code=status.HTTP_201_CREATED,
)
async def create_sorting_object_route(
    warehouse_id: uuid.UUID,
    body: SortingObjectCreateIn,
    user: Annotated[User, Depends(require_cells_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> SortingObjectOut:
    try:
        data = await warehouse_map_service.create_sorting_object(
            session,
            user.tenant_id,
            warehouse_id,
            kind=body.kind,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return SortingObjectOut.model_validate(data)


@router.post("/{warehouse_id}/sorting-objects/place", response_model=WarehouseMapMoveOut)
async def place_sorting_object_route(
    warehouse_id: uuid.UUID,
    body: SortingPlaceIn,
    user: Annotated[User, Depends(require_cells_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> WarehouseMapMoveOut:
    cell_id = body.cell_id or body.to_id
    if cell_id is None:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail="cell_id_required",
        )
    try:
        result = await warehouse_map_service.place_sorting_object(
            session,
            tenant_id=user.tenant_id,
            warehouse_id=warehouse_id,
            actor_user_id=user.id,
            kind=body.kind,
            object_id=body.id,
            cell_id=cell_id,
            quantity=body.qty,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return WarehouseMapMoveOut.model_validate(result)


@router.get("", response_model=list[WarehouseOut])
async def list_warehouses(
    user: Annotated[User, Depends(get_current_user)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> list[WarehouseOut]:
    rows = await list_wh_svc(session, user.tenant_id)
    return [
        WarehouseOut(
            id=str(w.id),
            name=w.name,
            code=w.code,
            barcode=w.barcode,
            is_operational=w.is_operational,
        )
    def collect(child: dict[str, Any]) -> None:
        if child["kind"] == "product":
            if child["seller_name"]:
                sellers.add(child["seller_name"])
            return
        for nested in child["children"]:
            collect(nested)

    for child in children:
        collect(child)
    return {
        **node,
        "children": children,
        "qty": sum(int(child["qty"]) for child in children),
        "seller_name": next(iter(sellers)) if len(sellers) == 1 else None,
    }


async def get_warehouse_map(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> dict[str, Any]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    address_enabled = await is_address_storage_enabled(session, tenant_id)
    (
        rows,
        locations,
        pallets,
        warehouse_boxes,
        inbound_boxes,
        cargo_places,
        cards,
    ) = await _load_map_rows(session, tenant_id, warehouse_id)

    location_by_id = {location.id: location for location in locations}
    balance_location: dict[tuple[str, uuid.UUID], uuid.UUID] = {}
    for balance, location, _product, _seller in rows:
        if balance.container_kind and balance.container_id:
            balance_location.setdefault((balance.container_kind, balance.container_id), location.id)

    nodes: dict[tuple[str, uuid.UUID], dict[str, Any]] = {}
    holders: dict[tuple[str, uuid.UUID], tuple[str, uuid.UUID] | uuid.UUID | None] = {}
    pallet_ids = {pallet.id for pallet in pallets}
    for pallet in pallets:
        key = ("pallet", pallet.id)
        nodes[key] = {
            "kind": "pallet",
            "id": str(pallet.id),
            "code": pallet.code,
            "barcode": pallet.barcode,
            "seller_name": None,
            "qty": 0,
            "children": [],
        }
        holders[key] = pallet.storage_location_id
    for box in warehouse_boxes:
        key = (box.container_kind, box.id)
        nodes[key] = {
            "kind": box.container_kind,
            "id": str(box.id),
            "code": box.internal_barcode,
            "barcode": box.internal_barcode,
            "seller_name": None,
            "qty": 0,
            "children": [],
        }
        holders[key] = (
            ("pallet", box.pallet_id)
            if box.pallet_id in pallet_ids
            else box.storage_location_id or balance_location.get(key)
        )
    for inbound_box in inbound_boxes:
        key = ("box", inbound_box.id)
        nodes[key] = {
            "kind": "box",
            "id": str(inbound_box.id),
            "code": f"КР-{inbound_box.box_number:06d}",
            "barcode": inbound_box.internal_barcode,
            "seller_name": None,
            "qty": 0,
            "children": [],
        }
        holders[key] = (
            ("pallet", inbound_box.pallet_id)
            if inbound_box.pallet_id in pallet_ids
            else balance_location.get(key)
        )
    for place in cargo_places:
        key = ("cargo_place", place.id)
        nodes[key] = {
            "kind": "cargo_place",
            "id": str(place.id),
            "code": f"ГМ-{place.place_number:06d}",
            "barcode": place.internal_barcode,
            "seller_name": None,
            "qty": 0,
            "children": [],
        }
        holders[key] = (
            ("pallet", place.pallet_id)
            if place.pallet_id in pallet_ids
            else balance_location.get(key)
        )

    loose_by_location: dict[uuid.UUID, list[dict[str, Any]]] = defaultdict(list)
    sellers: set[str] = set()
    categories: set[str] = set()
    for balance, location, product, seller in rows:
        card = (
            cards.get((product.seller_id, product.wb_nm_id))
            if (product.seller_id is not None and product.wb_nm_id is not None)
            else None
        )
        category, photo_url = _card_data(card)
        seller_name = seller.name if seller is not None else None
        if seller_name:
            sellers.add(seller_name)
        if category:
            categories.add(category)
        product_node = {
            "kind": "product",
            "id": str(balance.id),
            "product_id": str(product.id),
            "name": product.name,
            "seller_name": seller_name,
            "category": category,
            "barcode": product.wb_barcode,
            "photo_url": photo_url,
            "qty": int(balance.quantity),
        }
        container_key = (
            (balance.container_kind, balance.container_id)
            if balance.container_kind and balance.container_id
            else None
        )
        if container_key in nodes:
            assert container_key is not None
            nodes[container_key]["children"].append(product_node)
        else:
            loose_by_location[location.id].append(product_node)

    root_by_location: dict[uuid.UUID, list[dict[str, Any]]] = defaultdict(list)
    unassigned: list[dict[str, Any]] = []
    for key, node in nodes.items():
        holder = holders.get(key)
        if isinstance(holder, tuple) and holder in nodes:
            nodes[holder]["children"].append(node)
            continue
        root_location = location_by_id.get(holder) if isinstance(holder, uuid.UUID) else None
        if (
            address_enabled
            and root_location is not None
            and root_location.code != SORTING_LOCATION_CODE
        ):
            root_by_location[root_location.id].append(node)
        else:
            unassigned.append(node)

    cells: list[dict[str, Any]] = []
    if address_enabled:
        for location in locations:
            if location.code == SORTING_LOCATION_CODE:
                unassigned.extend(loose_by_location.pop(location.id, []))
                continue
            children = [
                *root_by_location.get(location.id, []),
                *loose_by_location.pop(location.id, []),
            ]
            normalized = [
                _normalize_container(child) if child["kind"] != "product" else child
                for child in children
            ]
            cells.append(
                {
                    "id": str(location.id),
                    "code": location.code,
                    "barcode": location.barcode,
                    "qty": sum(int(child["qty"]) for child in normalized),
                    "children": normalized,
                }
            )
    else:
        for loose in loose_by_location.values():
            unassigned.extend(loose)
    normalized_unassigned = [
        _normalize_container(node) if node["kind"] != "product" else node for node in unassigned
    ]

    warehouses = list(
        (
            await session.scalars(
                select(Warehouse)
                .where(
                    Warehouse.tenant_id == tenant_id,
                    Warehouse.is_operational.is_(True),
                )
                .order_by(Warehouse.name)
            )
        ).all()
    )
    event_rows = list(
        (
            await session.execute(
                select(WarehouseMapEvent, User)
                .outerjoin(User, User.id == WarehouseMapEvent.actor_user_id)
                .where(
                    WarehouseMapEvent.tenant_id == tenant_id,
                    WarehouseMapEvent.warehouse_id == warehouse_id,
                )
                .order_by(WarehouseMapEvent.created_at.desc(), WarehouseMapEvent.id.desc())
                .limit(100)
            )
        ).all()
    )
    journal = [
        {
            "id": str(event.id),
            "at": event.created_at.astimezone(UTC).isoformat().replace("+00:00", "Z"),
            "actor_name": actor.email if actor is not None else "Система",
            "subject": event.subject,
            "qty": event.quantity,
            "from_label": event.from_label,
async def disband_pallet(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    pallet_id: uuid.UUID,
) -> dict[str, Any]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    pallet = await session.get(Pallet, pallet_id)
    if (
        pallet is None
        or pallet.tenant_id != tenant_id
        or pallet.warehouse_id != warehouse_id
        or pallet.disbanded_at is not None
    ):
        raise WarehouseMapError("pallet_not_found")
    from_label = (
        await _location_label(session, pallet.storage_location_id)
        if pallet.storage_location_id is not None
        else UNASSIGNED_LABEL
    )
    code = pallet.code
    try:
        await pallet_service.disband_pallet(session, tenant_id, pallet_id)
    except pallet_service.PalletServiceError as exc:
        raise WarehouseMapError(exc.code) from exc
    event = WarehouseMapEvent(
        tenant_id=tenant_id,
        warehouse_id=warehouse_id,
        actor_user_id=actor_user_id,
        subject=_container_title("pallet", code),
        quantity=None,
        from_label=from_label,
        to_label=UNASSIGNED_LABEL,
    )
    session.add(event)
    await session.commit()
    return {"id": str(pallet_id), "disbanded": True}


def _sorting_tree_rows(
    nodes: list[dict[str, Any]],
    *,
    holder: str | None,
    objects: list[dict[str, Any]],
    lines: list[dict[str, Any]],
) -> None:
    for node in nodes:
        if node["kind"] == "product":
            lines.append(
                {
                    "id": node["id"],
                    "productId": node["product_id"],
                    "qty": node["qty"],
                    "holder": holder,
                }
            )
            continue
        object_holder = f"obj:{node['id']}"
        objects.append(
            {
                "id": node["id"],
                "kind": node["kind"],
                "code": node["code"],
                "barcode": node["barcode"] or "",
                "holder": holder,
            }
        )
        _sorting_tree_rows(node["children"], holder=object_holder, objects=objects, lines=lines)


async def get_sorting_objects(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> dict[str, Any]:
    data = await get_warehouse_map(session, tenant_id, warehouse_id)
    objects: list[dict[str, Any]] = []
    lines: list[dict[str, Any]] = []
    _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
    product_ids = {uuid.UUID(row["productId"]) for row in lines}
    products = (
        list(
            (
                await session.execute(
                    select(Product, Seller)
                    .outerjoin(Seller, Seller.id == Product.seller_id)
                    .where(Product.tenant_id == tenant_id, Product.id.in_(product_ids))
                )
            ).all()
        )
        if product_ids
        else []
    )
    location_rows = (
        list(
            (
                await session.execute(
                    select(
                        InventoryBalance.product_id,
                        StorageLocation,
                        func.sum(InventoryBalance.quantity),
                    )
                    .join(StorageLocation)
                    .where(
                        InventoryBalance.tenant_id == tenant_id,
                        InventoryBalance.product_id.in_(product_ids),
                        InventoryBalance.quantity > 0,
                        StorageLocation.warehouse_id == warehouse_id,
                        StorageLocation.code != SORTING_LOCATION_CODE,
                        StorageLocation.deleted_at.is_(None),
                    )
                    .group_by(InventoryBalance.product_id, StorageLocation.id)
                )
            ).all()
        )
        if product_ids
        else []
    )
    already: dict[uuid.UUID, dict[uuid.UUID, tuple[str, int]]] = defaultdict(dict)
    for product_id, location, quantity in location_rows:
        already[product_id][location.id] = (location.code, int(quantity or 0))

    product_nodes: dict[str, dict[str, Any]] = {}

    def collect_product_nodes(nodes: list[dict[str, Any]]) -> None:
        for node in nodes:
            if node["kind"] == "product":
                product_nodes[node["product_id"]] = node
            else:
                collect_product_nodes(node["children"])

    for cell in data["cells"]:
        collect_product_nodes(cell["children"])
    collect_product_nodes(data["unassigned"])
    product_data = [
        {
            "id": str(product.id),
            "name": product.name,
            "sku": product.sku_code,
            "seller": seller.name if seller is not None else "",
            "barcode": product.wb_barcode or "",
            "photo": product_nodes.get(str(product.id), {}).get("photo_url") or "",
            "size": product.wb_size,
            "alreadyAt": [
                {"cellId": str(cell_id), "code": code, "qty": qty}
                for cell_id, (code, qty) in already.get(product.id, {}).items()
            ],
        }
        for product, seller in products
    ]
    return {
        "objects": objects,
        "lines": lines,
        "products": product_data,
        "cells": [
            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
            for row in data["cells"]
        ],
    }


async def place_sorting_object(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    kind: ObjectKind,
    object_id: uuid.UUID,
    cell_id: uuid.UUID,
    quantity: int,
) -> dict[str, Any]:
    return await move_object(
        session,
        tenant_id=tenant_id,
        warehouse_id=warehouse_id,
        actor_user_id=actor_user_id,
        kind=kind,
        object_id=object_id,
        to_kind="cell",
        to_id=cell_id,
        quantity=quantity,
    )
from __future__ import annotations

import time
import uuid

import pytest
from httpx import AsyncClient
from sqlalchemy import func, select

from app.db.session import SessionLocal
from app.models.inbound_intake import (
    InboundIntakeBox,
    InboundIntakeRequest,
)
from app.models.inventory_balance import InventoryBalance
from app.models.pallet import Pallet
from app.models.product import Product
from app.models.seller import Seller
from app.models.storage_location import StorageLocation
from app.models.tenant import Tenant
from app.models.user import User
from app.models.warehouse import Warehouse
from app.services.sorting_location_service import (
    SORTING_LOCATION_CODE,
    get_or_create_sorting_location,
)


async def _register(
    client: AsyncClient, label: str
) -> tuple[dict[str, str], User, Tenant]:
    suffix = f"{label}-{time.time_ns()}"
    email = f"{suffix}@example.com"
    response = await client.post(
        "/auth/register",
        json={
            "organization_name": f"Склад {label}",
            "slug": suffix,
            "admin_email": email,
            "password": "password123",
        },
    )
    assert response.status_code == 200, response.text
    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
    async with SessionLocal() as session:
        user = await session.scalar(select(User).where(User.email == email))
        assert user is not None
        tenant = await session.get(Tenant, user.tenant_id)
        assert tenant is not None
        session.expunge(user)
        session.expunge(tenant)
    return headers, user, tenant


async def _seed_map(
    tenant_id: uuid.UUID,
) -> tuple[
    Warehouse,
    StorageLocation,
    StorageLocation,
    Product,
    Product,
    InventoryBalance,
    Pallet,
    InboundIntakeBox,
]:
    async with SessionLocal() as session:
        suffix = uuid.uuid4().hex[:10]
        warehouse = Warehouse(
            tenant_id=tenant_id,
            name="Ярцево",
            code=f"map-{suffix}",
            barcode=f"WH-MAP-{suffix}",
        )
        session.add(warehouse)
        await session.flush()
        cell = StorageLocation(
            tenant_id=tenant_id,
            warehouse_id=warehouse.id,
            code="А-01-01",
            barcode=f"CELL-{suffix}",
        )
        session.add(cell)
        await session.flush()
        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
        seller = Seller(tenant_id=tenant_id, name="ИП Тестовый")
        session.add(seller)
        await session.flush()
        product = Product(
            tenant_id=tenant_id,
            seller_id=seller.id,
            name="Футболка белая",
            sku_code=f"TS-{suffix}",
            wb_barcode=f"4600{suffix[:8]}",
        )
        sorting_product = Product(
            tenant_id=tenant_id,
            seller_id=seller.id,
            name="Носки",
            sku_code=f"SK-{suffix}",
            wb_barcode=f"4700{suffix[:8]}",
        )
        session.add_all([product, sorting_product])
        await session.flush()
        request = InboundIntakeRequest(
            tenant_id=tenant_id,
            warehouse_id=warehouse.id,
            status="receiving",
        )
        session.add(request)
        await session.flush()
        box = InboundIntakeBox(
            tenant_id=tenant_id,
            request_id=request.id,
            box_number=41,
            internal_barcode=f"BOX-{suffix}",
        )
        pallet = Pallet(
            tenant_id=tenant_id,
            warehouse_id=warehouse.id,
            code="П-000131",
            barcode=f"PLT-{suffix}",
            storage_location_id=cell.id,
        )
        session.add_all([box, pallet])
        await session.flush()
        box.pallet_id = pallet.id
        loose = InventoryBalance(
            tenant_id=tenant_id,
            storage_location_id=cell.id,
            product_id=product.id,
            quantity=5,
            quantity_unpacked=5,
            quantity_packed=0,
        )
        boxed = InventoryBalance(
            tenant_id=tenant_id,
            storage_location_id=cell.id,
            product_id=product.id,
            container_kind="box",
            container_id=box.id,
            quantity=7,
            quantity_unpacked=7,
            quantity_packed=0,
        )
        loose_sorting = InventoryBalance(
            tenant_id=tenant_id,
            storage_location_id=sorting.id,
            product_id=sorting_product.id,
            quantity=3,
            quantity_unpacked=3,
            quantity_packed=0,
        )
        session.add_all([loose, boxed, loose_sorting])
        await session.commit()
        for row in (
            warehouse,
            cell,
            sorting,
            product,
            sorting_product,
            loose,
            pallet,
            box,
        ):
            session.expunge(row)
        return (
            warehouse,
            cell,
            sorting,
            product,
            sorting_product,
            loose,
            pallet,
            box,
        )


def _tree_qty(data: dict[str, object]) -> int:
    cells = data["cells"]
    unassigned = data["unassigned"]
    assert isinstance(cells, list)
    assert isinstance(unassigned, list)
    return sum(int(cell["qty"]) for cell in cells) + sum(
        int(node["qty"]) for node in unassigned
    )


@pytest.mark.asyncio
async def test_map_totals_moves_sorting_disband_and_tenant_scope(
    async_client: AsyncClient,
) -> None:
    headers, user, tenant = await _register(async_client, "map")
    (
        warehouse,
        cell,
        sorting,
        _product,
        _sorting_product,
        loose,
        pallet,
        box,
    ) = await _seed_map(tenant.id)

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/map", headers=headers
    )
    assert response.status_code == 200, response.text
    data = response.json()
    assert data["sellers"] == ["ИП Тестовый"]
    assert data["cells"][0]["qty"] == 12
    pallet_node = next(
        node for node in data["cells"][0]["children"] if node["kind"] == "pallet"
    )
    assert pallet_node["qty"] == 7
    assert pallet_node["children"][0]["kind"] == "box"
    assert sum(node["qty"] for node in data["unassigned"]) == 3
    async with SessionLocal() as session:
        db_total = int(
            await session.scalar(
                select(func.sum(InventoryBalance.quantity))
                .join(StorageLocation)
                .where(
                    InventoryBalance.tenant_id == tenant.id,
                    StorageLocation.warehouse_id == warehouse.id,
                )
            )
            or 0
        )
    assert _tree_qty(data) == db_total == 15

    moved = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "product",
            "id": str(loose.id),
            "to_kind": "unassigned",
            "to_id": None,
            "qty": 2,
        },
    )
    assert moved.status_code == 200, moved.text
    after_move = (
        await async_client.get(f"/warehouses/{warehouse.id}/map", headers=headers)
    ).json()
    assert after_move["cells"][0]["qty"] == 10
    assert sum(node["qty"] for node in after_move["unassigned"]) == 5
    assert after_move["journal"][0]["actor_name"] == user.email
    assert after_move["journal"][0]["from_label"] == "Ячейка А-01-01"
    assert after_move["journal"][0]["to_label"] == "Без ячеек"
    assert _tree_qty(after_move) == db_total

    zero = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "product",
            "id": str(loose.id),
            "to_kind": "unassigned",
            "qty": 0,
        },
    )
    assert zero.status_code == 422

    self_move = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "pallet",
            "id": str(pallet.id),
            "to_kind": "pallet",
            "to_id": str(pallet.id),
            "qty": 7,
        },
    )
    assert self_move.status_code == 409
    assert self_move.json()["detail"] == "container_cycle"

    sorting_objects = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects", headers=headers
    )
    assert sorting_objects.status_code == 200, sorting_objects.text
    sorting_line = next(
        row for row in sorting_objects.json()["lines"] if row["qty"] == 3
    )
    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "product",
            "id": sorting_line["id"],
            "cell_id": str(cell.id),
            "qty": 3,
        },
    )
    assert placed.status_code == 200, placed.text

    moved_pallet = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "pallet",
            "id": str(pallet.id),
            "to_kind": "unassigned",
            "qty": 7,
        },
    )
    assert moved_pallet.status_code == 200, moved_pallet.text
    assert moved_pallet.json()["moved_qty"] == 7
    after_pallet_move = (
        await async_client.get(f"/warehouses/{warehouse.id}/map", headers=headers)
    ).json()
    moved_pallet_node = next(
        node
        for node in after_pallet_move["unassigned"]
        if node["kind"] == "pallet" and node["id"] == str(pallet.id)
    )
    assert moved_pallet_node["qty"] == 7

    disbanded = await async_client.post(
        f"/warehouses/{warehouse.id}/map/disband",
        headers=headers,
        json={"pallet_id": str(pallet.id)},
    )
    assert disbanded.status_code == 200, disbanded.text
    async with SessionLocal() as session:
        stored_pallet = await session.get(Pallet, pallet.id)
        stored_box = await session.get(InboundIntakeBox, box.id)
        assert stored_pallet is not None and stored_pallet.disbanded_at is not None
        assert stored_box is not None and stored_box.pallet_id is None
        box_location = await session.scalar(
            select(InventoryBalance.storage_location_id).where(
                InventoryBalance.tenant_id == tenant.id,
                InventoryBalance.container_kind == "box",
                InventoryBalance.container_id == box.id,
                InventoryBalance.quantity > 0,
            )
        )
        assert box_location == sorting.id

    other_headers, _other_user, other_tenant = await _register(async_client, "other")
    del other_headers
    (
        other_warehouse,
        _other_cell,
        _other_sorting,
        _other_product,
        _other_sorting_product,
        foreign_balance,
        _other_pallet,
        _other_box,
    ) = await _seed_map(other_tenant.id)
    denied = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "product",
            "id": str(foreign_balance.id),
            "to_kind": "unassigned",
            "qty": 1,
        },
    )
    assert denied.status_code == 404
    assert denied.json()["detail"] == "object_not_found"
    assert other_warehouse.tenant_id == other_tenant.id


@pytest.mark.asyncio
async def test_address_storage_disabled_hides_cells_and_rejects_place(
    async_client: AsyncClient,
) -> None:
    headers, _user, tenant = await _register(async_client, "no-address")
    warehouse, cell, _sorting, *_rest = await _seed_map(tenant.id)
    switched = await async_client.patch(
        "/tenant/settings",
        headers=headers,
        json={"address_storage_enabled": False},
    )
    assert switched.status_code == 200, switched.text

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/map", headers=headers
    )
    assert response.status_code == 200, response.text
    data = response.json()
    assert data["cells"] == []
    assert sum(node["qty"] for node in data["unassigned"]) == 15

    line = next(
        row
        for row in (
            await async_client.get(
                f"/warehouses/{warehouse.id}/sorting-objects", headers=headers
            )
        ).json()["lines"]
        if row["qty"] > 0
    )
    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "product",
            "id": line["id"],
            "cell_id": str(cell.id),
            "qty": 1,
        },
    )
    assert placed.status_code == 409
    assert placed.json()["detail"] == "address_storage_disabled"

    async with SessionLocal() as session:
        locations = list(
            (
                await session.scalars(
                    select(StorageLocation).where(
                        StorageLocation.warehouse_id == warehouse.id,
                        StorageLocation.code == SORTING_LOCATION_CODE,
                    )
                )
            ).all()
        )
    assert len(locations) == 1
from __future__ import annotations

import time
import uuid

import pytest
from httpx import AsyncClient
from sqlalchemy import select

from app.db.session import SessionLocal
from app.models.pallet import Pallet
from app.models.tenant import Tenant
from app.models.user import User
from app.models.warehouse import Warehouse
from app.models.warehouse_box import WarehouseBox


async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
    suffix = f"{label.replace('_', '-')}-{time.time_ns()}"
    email = f"{suffix}@example.com"
    response = await client.post(
        "/auth/register",
        json={
            "organization_name": f"Склад {label}",
            "slug": suffix,
            "admin_email": email,
            "password": "password123",
        },
    )
    assert response.status_code == 200, response.text
    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
    async with SessionLocal() as session:
        user = await session.scalar(select(User).where(User.email == email))
        assert user is not None
        tenant = await session.get(Tenant, user.tenant_id)
        assert tenant is not None
        session.expunge(tenant)
    return headers, tenant


async def _create_warehouse(tenant_id: uuid.UUID, label: str) -> Warehouse:
    suffix = uuid.uuid4().hex[:10]
    async with SessionLocal() as session:
        warehouse = Warehouse(
            tenant_id=tenant_id,
            name=f"Склад {label}",
            code=f"sorting-{suffix}",
            barcode=f"WH-SORTING-{suffix}",
        )
        session.add(warehouse)
        await session.commit()
        await session.refresh(warehouse)
        session.expunge(warehouse)
    return warehouse


@pytest.mark.asyncio
@pytest.mark.parametrize("kind", ["pallet", "box", "cargo_place"])
async def test_created_sorting_object_is_unassigned_on_map_and_resolvable_by_barcode(
    async_client: AsyncClient,
    kind: str,
) -> None:
    headers, tenant = await _register(async_client, f"create-{kind}")
    warehouse = await _create_warehouse(tenant.id, kind)

    created = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
        json={"kind": kind},
    )

    assert created.status_code == 201, created.text
    payload = created.json()
    assert payload["kind"] == kind
    assert payload["barcode"]
    assert payload["holder"] is None

    sorting_objects = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
    )
    assert sorting_objects.status_code == 200, sorting_objects.text
    sorting_row = next(
        row for row in sorting_objects.json()["objects"] if row["id"] == payload["id"]
    )
    assert sorting_row == payload

    warehouse_map = await async_client.get(
        f"/warehouses/{warehouse.id}/map",
        headers=headers,
    )
    assert warehouse_map.status_code == 200, warehouse_map.text
    map_row = next(
        row for row in warehouse_map.json()["unassigned"] if row["id"] == payload["id"]
    )
    assert map_row["kind"] == kind
    assert map_row["barcode"] == payload["barcode"]
    assert map_row["qty"] == 0
    assert map_row["children"] == []

    resolved = await async_client.get(
        "/operations/scan/resolve",
        headers=headers,
        params={"code": payload["barcode"], "warehouse_id": str(warehouse.id)},
    )
    assert resolved.status_code == 200, resolved.text
    assert resolved.json()["type"] == kind
    assert resolved.json()["id"] == payload["id"]

    async with SessionLocal() as session:
        if kind == "pallet":
            stored = await session.get(Pallet, uuid.UUID(payload["id"]))
            assert stored is not None
            assert stored.tenant_id == tenant.id
            assert stored.warehouse_id == warehouse.id
            assert stored.storage_location_id is None
            assert stored.disbanded_at is None
        else:
            stored_box = await session.get(WarehouseBox, uuid.UUID(payload["id"]))
            assert stored_box is not None
            assert stored_box.tenant_id == tenant.id
            assert stored_box.warehouse_id == warehouse.id
            assert stored_box.container_kind == kind
            assert stored_box.storage_location_id is None
            assert stored_box.pallet_id is None


@pytest.mark.asyncio
async def test_sorting_object_creation_and_listing_are_tenant_scoped(
    async_client: AsyncClient,
) -> None:
    owner_headers, owner_tenant = await _register(async_client, "owner")
    foreign_headers, foreign_tenant = await _register(async_client, "foreign")
    owner_warehouse = await _create_warehouse(owner_tenant.id, "owner")
    foreign_warehouse = await _create_warehouse(foreign_tenant.id, "foreign")
    created = await async_client.post(
        f"/warehouses/{owner_warehouse.id}/sorting-objects",
        headers=owner_headers,
        json={"kind": "box"},
    )
    assert created.status_code == 201, created.text

    foreign_list = await async_client.get(
        f"/warehouses/{foreign_warehouse.id}/sorting-objects",
        headers=foreign_headers,
    )
    assert foreign_list.status_code == 200, foreign_list.text
    assert created.json()["id"] not in {
        row["id"] for row in foreign_list.json()["objects"]
    }

    denied = await async_client.get(
        f"/warehouses/{owner_warehouse.id}/sorting-objects",
        headers=foreign_headers,
    )
    assert denied.status_code == 404
    assert denied.json()["detail"] == "warehouse_not_found"


@pytest.mark.asyncio
async def test_sorting_object_creation_rejects_missing_warehouse(
    async_client: AsyncClient,
) -> None:
    headers, _tenant = await _register(async_client, "missing")

    response = await async_client.post(
        f"/warehouses/{uuid.uuid4()}/sorting-objects",
        headers=headers,
        json={"kind": "cargo_place"},
    )

    assert response.status_code == 404
    assert response.json()["detail"] == "warehouse_not_found"

exec
/bin/zsh -lc "sed -n '1,180p' backend/app/services/warehouse_map_service.py && sed -n '520,930p' backend/app/services/warehouse_map_service.py && sed -n '1090,1210p' backend/app/services/warehouse_map_service.py && sed -n '1,180p' backend/app/api/warehouses.py && rg -n \"to_kind=.*(pallet|box|cargo_place)|\\\"to_kind\\\": \\\"(pallet|box|cargo_place)\\\"|place_sorting_object\\(\" backend/tests backend/app -g '*.py'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
from __future__ import annotations

import uuid
from collections import defaultdict
from dataclasses import dataclass
from datetime import UTC
from typing import Any, Literal, cast

from sqlalchemy import func, or_, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.inbound_intake import (
    InboundIntakeBox,
    InboundIntakeCargoPlace,
    InboundIntakeRequest,
)
from app.models.inventory_balance import InventoryBalance
from app.models.pallet import Pallet
from app.models.product import Product
from app.models.seller import Seller
from app.models.seller_wildberries_imported_card import SellerWildberriesImportedCard
from app.models.storage_location import StorageLocation
from app.models.user import User
from app.models.warehouse import Warehouse
from app.models.warehouse_box import WarehouseBox
from app.models.warehouse_map_event import WarehouseMapEvent
from app.services import inventory_service, pallet_service, warehouse_box_service
from app.services.inventory_container_service import ContainerKind, validate_container
from app.services.sorting_location_service import (
    SORTING_LOCATION_CODE,
    UNASSIGNED_LABEL,
    get_or_create_sorting_location,
)
from app.services.tenant_settings_service import is_address_storage_enabled
from app.services.wb_card_enrichment import first_photo_url_from_card, subject_name_from_card

ObjectKind = Literal["product", "pallet", "box", "cargo_place"]
DestinationKind = Literal["cell", "unassigned", "sorting", "pallet", "box", "cargo_place"]
MOVEMENT_TYPE_WAREHOUSE_MAP = "warehouse_map_move"


class WarehouseMapError(Exception):
    def __init__(self, code: str) -> None:
        self.code = code
        super().__init__(code)


@dataclass(frozen=True)
class WarehouseContainerPathItem:
    kind: ContainerKind
    id: uuid.UUID
    code: str
    label: str


def _container_title(kind: str, code: str) -> str:
    title = {"pallet": "Палета", "box": "Короб", "cargo_place": "Грузоместо"}[kind]
    return f"{title} {code}"


def _container_path_item(
    kind: ContainerKind,
    container_id: uuid.UUID,
    code: str,
) -> WarehouseContainerPathItem:
    return WarehouseContainerPathItem(
        kind=kind,
        id=container_id,
        code=code,
        label=_container_title(kind, code),
    )


async def resolve_container_paths(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    container_refs: set[tuple[ContainerKind, uuid.UUID]],
) -> dict[tuple[ContainerKind, uuid.UUID], tuple[WarehouseContainerPathItem, ...]]:
    """Return map-compatible human container paths within one tenant warehouse."""
    if not container_refs:
        return {}
    await _assert_warehouse(session, tenant_id, warehouse_id)
    pallets = list(
        (
            await session.scalars(
                select(Pallet).where(
                    Pallet.tenant_id == tenant_id,
                    Pallet.warehouse_id == warehouse_id,
                    Pallet.disbanded_at.is_(None),
                )
            )
        ).all()
    )
    warehouse_boxes, inbound_boxes, cargo_places = await _load_boxes(
        session, tenant_id, warehouse_id
    )
    pallet_by_id = {pallet.id: pallet for pallet in pallets}
    warehouse_box_by_key: dict[tuple[ContainerKind, uuid.UUID], WarehouseBox] = {
        (box.container_kind, box.id): box for box in warehouse_boxes
    }
    inbound_box_by_id = {box.id: box for box in inbound_boxes}
    cargo_place_by_id = {place.id: place for place in cargo_places}

    paths: dict[
        tuple[ContainerKind, uuid.UUID], tuple[WarehouseContainerPathItem, ...]
    ] = {}
    for ref in container_refs:
        kind, container_id = ref
        if kind == "pallet":
            pallet = pallet_by_id.get(container_id)
            if pallet is None:
                raise WarehouseMapError("container_not_found")
            paths[ref] = (_container_path_item("pallet", pallet.id, pallet.code),)
            continue

        code: str
        parent_pallet_id: uuid.UUID | None
        warehouse_box = warehouse_box_by_key.get(ref)
        if warehouse_box is not None:
            code = warehouse_box.internal_barcode
            parent_pallet_id = warehouse_box.pallet_id
        elif kind == "box" and container_id in inbound_box_by_id:
            inbound_box = inbound_box_by_id[container_id]
            code = f"КР-{inbound_box.box_number:06d}"
            parent_pallet_id = inbound_box.pallet_id
        elif kind == "cargo_place" and container_id in cargo_place_by_id:
            cargo_place = cargo_place_by_id[container_id]
            code = f"ГМ-{cargo_place.place_number:06d}"
            parent_pallet_id = cargo_place.pallet_id
        else:
            raise WarehouseMapError("container_not_found")

        path: list[WarehouseContainerPathItem] = []
        if parent_pallet_id is not None:
            parent = pallet_by_id.get(parent_pallet_id)
            if parent is None:
                raise WarehouseMapError("container_not_found")
            path.append(_container_path_item("pallet", parent.id, parent.code))
        path.append(_container_path_item(kind, container_id, code))
        paths[ref] = tuple(path)
    return paths


def _card_data(card: SellerWildberriesImportedCard | None) -> tuple[str | None, str | None]:
    raw = card.raw_json if card is not None else None
    if not isinstance(raw, dict):
        return None, None
    return subject_name_from_card(raw), first_photo_url_from_card(raw)


async def _assert_warehouse(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> Warehouse:
    warehouse = await session.get(Warehouse, warehouse_id)
    if warehouse is None or warehouse.tenant_id != tenant_id:
        raise WarehouseMapError("warehouse_not_found")
    return warehouse


async def _load_boxes(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> tuple[list[WarehouseBox], list[InboundIntakeBox], list[InboundIntakeCargoPlace]]:
    warehouse_boxes = list(
        (
            await session.scalars(
                select(WarehouseBox).where(
                    WarehouseBox.tenant_id == tenant_id,
                    WarehouseBox.warehouse_id == warehouse_id,
                )
            )
        ).all()
    )
    inbound_boxes = list(
        (
            await session.scalars(
                select(InboundIntakeBox)
                .join(InboundIntakeRequest)
                .where(
                    InboundIntakeBox.tenant_id == tenant_id,
            "from_label": event.from_label,
            "to_label": event.to_label,
        }
        for event, actor in event_rows
    ]
    return {
        "warehouses": [{"id": str(row.id), "name": row.name} for row in warehouses],
        "sellers": sorted(sellers),
        "categories": sorted(categories),
        "cells": cells,
        "unassigned": normalized_unassigned,
        "journal": journal,
    }


async def _container_location_id(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
) -> uuid.UUID:
    await validate_container(session, tenant_id, warehouse_id, kind, container_id)
    if kind == "pallet":
        pallet = await session.get(Pallet, container_id)
        assert pallet is not None
        if pallet.storage_location_id is not None:
            return pallet.storage_location_id
    if kind == "box":
        box = await session.get(WarehouseBox, container_id)
        if (
            box is not None
            and box.tenant_id == tenant_id
            and box.warehouse_id == warehouse_id
            and box.container_kind == kind
        ):
            if box.pallet_id is not None:
                pallet = await session.get(Pallet, box.pallet_id)
                if pallet is not None and pallet.storage_location_id is not None:
                    return pallet.storage_location_id
            if box.storage_location_id is not None:
                return box.storage_location_id
    if kind == "cargo_place":
        cargo_place = await session.get(WarehouseBox, container_id)
        if (
            cargo_place is not None
            and cargo_place.tenant_id == tenant_id
            and cargo_place.warehouse_id == warehouse_id
            and cargo_place.container_kind == kind
        ):
            if cargo_place.pallet_id is not None:
                pallet = await session.get(Pallet, cargo_place.pallet_id)
                if pallet is not None and pallet.storage_location_id is not None:
                    return pallet.storage_location_id
            if cargo_place.storage_location_id is not None:
                return cargo_place.storage_location_id
    balance_location = await session.scalar(
        select(InventoryBalance.storage_location_id)
        .where(
            InventoryBalance.tenant_id == tenant_id,
            InventoryBalance.container_kind == kind,
            InventoryBalance.container_id == container_id,
            InventoryBalance.quantity > 0,
        )
        .limit(1)
    )
    if balance_location is not None:
        location = await session.get(StorageLocation, balance_location)
        if location is not None and location.warehouse_id == warehouse_id:
            return balance_location
    sorting = await get_or_create_sorting_location(session, tenant_id, warehouse_id)
    return sorting.id


async def _destination(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    to_kind: DestinationKind,
    to_id: uuid.UUID | None,
) -> tuple[uuid.UUID, ContainerKind | None, uuid.UUID | None, str]:
    if to_kind == "cell":
        if not await is_address_storage_enabled(session, tenant_id):
            raise WarehouseMapError("address_storage_disabled")
        if to_id is None:
            raise WarehouseMapError("destination_required")
        location = await session.get(StorageLocation, to_id)
        if (
            location is None
            or location.tenant_id != tenant_id
            or location.warehouse_id != warehouse_id
            or location.deleted_at is not None
            or location.code == SORTING_LOCATION_CODE
        ):
            raise WarehouseMapError("cell_not_found")
        return location.id, None, None, f"Ячейка {location.code}"
    if to_kind in {"unassigned", "sorting"}:
        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse_id)
        return sorting.id, None, None, UNASSIGNED_LABEL
    if to_id is None:
        raise WarehouseMapError("destination_required")
    container_kind = cast(ContainerKind, to_kind)
    try:
        location_id = await _container_location_id(
            session, tenant_id, warehouse_id, container_kind, to_id
        )
    except ValueError as exc:
        raise WarehouseMapError("destination_not_found") from exc
    code = await _container_code(session, tenant_id, warehouse_id, container_kind, to_id)
    return location_id, container_kind, to_id, _container_title(container_kind, code)


async def _container_code(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
) -> str:
    if kind == "pallet":
        row = await session.get(Pallet, container_id)
        if row is not None and row.tenant_id == tenant_id and row.warehouse_id == warehouse_id:
            return row.code
    elif kind == "box":
        warehouse_box = await session.get(WarehouseBox, container_id)
        if (
            warehouse_box is not None
            and warehouse_box.tenant_id == tenant_id
            and warehouse_box.warehouse_id == warehouse_id
            and warehouse_box.container_kind == "box"
        ):
            return warehouse_box.internal_barcode
        inbound = await session.scalar(
            select(InboundIntakeBox)
            .join(InboundIntakeRequest)
            .where(
                InboundIntakeBox.id == container_id,
                InboundIntakeBox.tenant_id == tenant_id,
                InboundIntakeRequest.warehouse_id == warehouse_id,
            )
        )
        if inbound is not None:
            return f"КР-{inbound.box_number:06d}"
    else:
        warehouse_cargo_place = await session.get(WarehouseBox, container_id)
        if (
            warehouse_cargo_place is not None
            and warehouse_cargo_place.tenant_id == tenant_id
            and warehouse_cargo_place.warehouse_id == warehouse_id
            and warehouse_cargo_place.container_kind == "cargo_place"
        ):
            return warehouse_cargo_place.internal_barcode
        cargo = await session.scalar(
            select(InboundIntakeCargoPlace)
            .join(InboundIntakeRequest)
            .where(
                InboundIntakeCargoPlace.id == container_id,
                InboundIntakeCargoPlace.tenant_id == tenant_id,
                InboundIntakeRequest.warehouse_id == warehouse_id,
            )
        )
        if cargo is not None:
            return f"ГМ-{cargo.place_number:06d}"
    raise WarehouseMapError("object_not_found")


async def _location_label(session: AsyncSession, location_id: uuid.UUID) -> str:
    location = await session.get(StorageLocation, location_id)
    if location is None or location.code == SORTING_LOCATION_CODE:
        return UNASSIGNED_LABEL
    return f"Ячейка {location.code}"


async def _transfer_balance(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    balance: InventoryBalance,
    quantity: int,
    destination_location_id: uuid.UUID,
    destination_container_kind: ContainerKind | None,
    destination_container_id: uuid.UUID | None,
    transfer_group_id: uuid.UUID,
    actor_user_id: uuid.UUID,
) -> None:
    source_kind = cast(ContainerKind | None, balance.container_kind)
    await inventory_service.record_movement_and_adjust_balance(
        session,
        tenant_id=tenant_id,
        product_id=balance.product_id,
        storage_location_id=balance.storage_location_id,
        quantity_delta=-quantity,
        movement_type=MOVEMENT_TYPE_WAREHOUSE_MAP,
        transfer_group_id=transfer_group_id,
        container_kind=source_kind,
        container_id=balance.container_id,
        actor_user_id=actor_user_id,
    )
    await inventory_service.record_movement_and_adjust_balance(
        session,
        tenant_id=tenant_id,
        product_id=balance.product_id,
        storage_location_id=destination_location_id,
        quantity_delta=quantity,
        movement_type=MOVEMENT_TYPE_WAREHOUSE_MAP,
        transfer_group_id=transfer_group_id,
        container_kind=destination_container_kind,
        container_id=destination_container_id,
        actor_user_id=actor_user_id,
    )


async def _container_balances(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
) -> list[InventoryBalance]:
    refs: list[tuple[str, uuid.UUID]] = [(kind, container_id)]
    if kind == "pallet":
        warehouse_boxes, inbound_boxes, cargo_places = await _load_boxes(
            session, tenant_id, warehouse_id
        )
        refs.extend(
            (row.container_kind, row.id)
            for row in warehouse_boxes
            if row.pallet_id == container_id
        )
        refs.extend(("box", row.id) for row in inbound_boxes if row.pallet_id == container_id)
        refs.extend(
            ("cargo_place", row.id) for row in cargo_places if row.pallet_id == container_id
        )
    predicates = [
        (InventoryBalance.container_kind == ref_kind) & (InventoryBalance.container_id == ref_id)
        for ref_kind, ref_id in refs
    ]
    rows = list(
        (
            await session.scalars(
                select(InventoryBalance)
                .join(StorageLocation)
                .where(
                    InventoryBalance.tenant_id == tenant_id,
                    InventoryBalance.quantity > 0,
                    StorageLocation.warehouse_id == warehouse_id,
                    or_(*predicates),
                )
                .with_for_update()
            )
        ).all()
    )
    return rows


async def _place_container(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
    to_kind: DestinationKind,
    to_id: uuid.UUID | None,
    destination_location_id: uuid.UUID,
) -> None:
    if kind == "pallet":
        if to_kind not in {"cell", "unassigned", "sorting"}:
            raise WarehouseMapError("invalid_container_destination")
        pallet = await session.get(Pallet, container_id)
        if (
            pallet is None
            or pallet.tenant_id != tenant_id
            or pallet.warehouse_id != warehouse_id
            or pallet.disbanded_at is not None
        ):
            raise WarehouseMapError("object_not_found")
        pallet.storage_location_id = destination_location_id if to_kind == "cell" else None
        return
    if to_kind not in {"cell", "unassigned", "sorting", "pallet"}:
        raise WarehouseMapError("invalid_container_destination")
    pallet_id = to_id if to_kind == "pallet" else None
    if pallet_id == container_id:
        raise WarehouseMapError("container_cycle")
    if kind == "box":
        warehouse_box = await session.get(WarehouseBox, container_id)
        if (
            warehouse_box is not None
            and warehouse_box.tenant_id == tenant_id
            and warehouse_box.warehouse_id == warehouse_id
            and warehouse_box.container_kind == "box"
        ):
            warehouse_box.pallet_id = pallet_id
            warehouse_box.storage_location_id = (
                destination_location_id if to_kind == "cell" else None
            )
            return
        inbound = await session.scalar(
            select(InboundIntakeBox)
            .join(InboundIntakeRequest)
            .where(
                InboundIntakeBox.id == container_id,
                InboundIntakeBox.tenant_id == tenant_id,
                InboundIntakeRequest.warehouse_id == warehouse_id,
            )
        )
        if inbound is None:
            raise WarehouseMapError("object_not_found")
        inbound.pallet_id = pallet_id
        return
    warehouse_cargo_place = await session.get(WarehouseBox, container_id)
    if (
        warehouse_cargo_place is not None
        and warehouse_cargo_place.tenant_id == tenant_id
        and warehouse_cargo_place.warehouse_id == warehouse_id
        and warehouse_cargo_place.container_kind == "cargo_place"
    ):
        warehouse_cargo_place.pallet_id = pallet_id
        warehouse_cargo_place.storage_location_id = (
            destination_location_id if to_kind == "cell" else None
        )
        return
    cargo = await session.scalar(
        select(InboundIntakeCargoPlace)
        .join(InboundIntakeRequest)
        .where(
            InboundIntakeCargoPlace.id == container_id,
            InboundIntakeCargoPlace.tenant_id == tenant_id,
            InboundIntakeRequest.warehouse_id == warehouse_id,
        )
    )
    if cargo is None:
        raise WarehouseMapError("object_not_found")
    cargo.pallet_id = pallet_id


async def move_object(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    kind: ObjectKind,
    object_id: uuid.UUID,
    to_kind: DestinationKind,
    to_id: uuid.UUID | None,
    quantity: int,
) -> dict[str, Any]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    if quantity <= 0:
        raise WarehouseMapError("quantity_must_be_positive")
    if kind == "pallet" and to_kind == "pallet" and object_id == to_id:
        raise WarehouseMapError("container_cycle")
    destination_location_id, destination_kind, destination_id, to_label = await _destination(
        session, tenant_id, warehouse_id, to_kind, to_id
    )
    transfer_group_id = uuid.uuid4()

    if kind == "product":
        balance = await session.scalar(
            select(InventoryBalance)
            .join(StorageLocation)
            .where(
                InventoryBalance.id == object_id,
                InventoryBalance.tenant_id == tenant_id,
                InventoryBalance.quantity > 0,
                StorageLocation.warehouse_id == warehouse_id,
            )
            .with_for_update()
        )
        if balance is None:
            raise WarehouseMapError("object_not_found")
        if quantity > balance.quantity:
            raise WarehouseMapError("insufficient_stock")
        product = await session.get(Product, balance.product_id)
        assert product is not None
        from_label = await _location_label(session, balance.storage_location_id)
        if balance.container_kind and balance.container_id:
            code = await _container_code(
                session,
                tenant_id,
                warehouse_id,
                cast(ContainerKind, balance.container_kind),
                balance.container_id,
            )
            from_label = _container_title(balance.container_kind, code)
        await _transfer_balance(
            session,
            tenant_id=tenant_id,
            balance=balance,
            quantity=quantity,
            destination_location_id=destination_location_id,
            destination_container_kind=destination_kind,
            destination_container_id=destination_id,
            transfer_group_id=transfer_group_id,
            actor_user_id=actor_user_id,
        )
        subject = product.name
        moved_quantity: int | None = quantity
    else:
        container_kind: ContainerKind = kind
        try:
            await validate_container(session, tenant_id, warehouse_id, container_kind, object_id)
        except ValueError as exc:
            raise WarehouseMapError("object_not_found") from exc
        if to_kind in {"box", "cargo_place"}:
            raise WarehouseMapError("invalid_container_destination")
        source_location_id = await _container_location_id(
            session, tenant_id, warehouse_id, container_kind, object_id
        )
        from_label = await _location_label(session, source_location_id)
        balances = await _container_balances(


async def get_sorting_objects(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> dict[str, Any]:
    data = await get_warehouse_map(session, tenant_id, warehouse_id)
    objects: list[dict[str, Any]] = []
    lines: list[dict[str, Any]] = []
    _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
    product_ids = {uuid.UUID(row["productId"]) for row in lines}
    products = (
        list(
            (
                await session.execute(
                    select(Product, Seller)
                    .outerjoin(Seller, Seller.id == Product.seller_id)
                    .where(Product.tenant_id == tenant_id, Product.id.in_(product_ids))
                )
            ).all()
        )
        if product_ids
        else []
    )
    location_rows = (
        list(
            (
                await session.execute(
                    select(
                        InventoryBalance.product_id,
                        StorageLocation,
                        func.sum(InventoryBalance.quantity),
                    )
                    .join(StorageLocation)
                    .where(
                        InventoryBalance.tenant_id == tenant_id,
                        InventoryBalance.product_id.in_(product_ids),
                        InventoryBalance.quantity > 0,
                        StorageLocation.warehouse_id == warehouse_id,
                        StorageLocation.code != SORTING_LOCATION_CODE,
                        StorageLocation.deleted_at.is_(None),
                    )
                    .group_by(InventoryBalance.product_id, StorageLocation.id)
                )
            ).all()
        )
        if product_ids
        else []
    )
    already: dict[uuid.UUID, dict[uuid.UUID, tuple[str, int]]] = defaultdict(dict)
    for product_id, location, quantity in location_rows:
        already[product_id][location.id] = (location.code, int(quantity or 0))

    product_nodes: dict[str, dict[str, Any]] = {}

    def collect_product_nodes(nodes: list[dict[str, Any]]) -> None:
        for node in nodes:
            if node["kind"] == "product":
                product_nodes[node["product_id"]] = node
            else:
                collect_product_nodes(node["children"])

    for cell in data["cells"]:
        collect_product_nodes(cell["children"])
    collect_product_nodes(data["unassigned"])
    product_data = [
        {
            "id": str(product.id),
            "name": product.name,
            "sku": product.sku_code,
            "seller": seller.name if seller is not None else "",
            "barcode": product.wb_barcode or "",
            "photo": product_nodes.get(str(product.id), {}).get("photo_url") or "",
            "size": product.wb_size,
            "alreadyAt": [
                {"cellId": str(cell_id), "code": code, "qty": qty}
                for cell_id, (code, qty) in already.get(product.id, {}).items()
            ],
        }
        for product, seller in products
    ]
    return {
        "objects": objects,
        "lines": lines,
        "products": product_data,
        "cells": [
            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
            for row in data["cells"]
        ],
    }


async def place_sorting_object(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    kind: ObjectKind,
    object_id: uuid.UUID,
    cell_id: uuid.UUID,
    quantity: int,
) -> dict[str, Any]:
    return await move_object(
        session,
        tenant_id=tenant_id,
        warehouse_id=warehouse_id,
        actor_user_id=actor_user_id,
        kind=kind,
        object_id=object_id,
        to_kind="cell",
        to_id=cell_id,
        quantity=quantity,
    )
from __future__ import annotations

import uuid
from typing import Annotated, Literal

from fastapi import APIRouter, Depends, HTTPException, Query, status
from pydantic import BaseModel, Field
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import (
    get_current_user,
    require_catalog_cells_read_access,
    require_cells_access,
    require_fulfillment_admin,
)
from app.db.session import get_db
from app.models.storage_location import StorageLocation
from app.models.user import User
from app.models.warehouse import Warehouse
from app.services import tenant_settings_service as tenant_settings_svc
from app.services import warehouse_map_service
from app.services.catalog_service import (
    CatalogError,
    create_location,
    create_location_from_rack,
    create_warehouse,
    delete_location,
    delete_warehouse,
    get_warehouse,
    list_racks,
    rename_location,
    rename_warehouse,
    resolve_warehouse_scan,
    suggest_next_location_for_rack,
)
from app.services.catalog_service import (
    list_locations as list_locs_svc,
)
from app.services.catalog_service import (
    list_warehouses as list_wh_svc,
)
from app.services.sorting_location_service import (
    SORTING_LOCATION_LABEL,
    get_or_create_sorting_location,
)
from app.services.warehouse_map_service import WarehouseMapError

router = APIRouter(prefix="/warehouses", tags=["warehouses"])


async def _require_address_storage(
    session: AsyncSession,
    tenant_id: uuid.UUID,
) -> None:
    if not await tenant_settings_svc.is_address_storage_enabled(session, tenant_id):
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="address_storage_disabled",
        )


class WarehouseCreate(BaseModel):
    name: str = Field(min_length=1, max_length=255)
    code: str = Field(min_length=1, max_length=64, pattern=r"^[a-zA-Z0-9_-]+$")


class WarehousePatch(BaseModel):
    name: str = Field(min_length=1, max_length=255)


class WarehouseOut(BaseModel):
    id: str
    name: str
    code: str
    barcode: str
    is_operational: bool

    model_config = {"from_attributes": False}


class LocationCreate(BaseModel):
    code: str | None = Field(default=None, min_length=1, max_length=64)
    rack_name: str | None = Field(default=None, min_length=1, max_length=32)
    side: int | None = Field(default=None, ge=1, le=2)
    position: int | None = Field(default=None, ge=1, le=9999)


class LocationPatch(BaseModel):
    code: str = Field(min_length=1, max_length=64)


class RackOut(BaseModel):
    name: str


class LocationSuggestOut(BaseModel):
    position: int
    code: str


class LocationOut(BaseModel):
    id: str
    code: str
    warehouse_id: str
    barcode: str


class WarehouseMapProductOut(BaseModel):
    kind: Literal["product"]
    id: str
    product_id: str
    name: str
    seller_name: str | None
    category: str | None
    barcode: str | None
    photo_url: str | None
    qty: int


class WarehouseMapContainerOut(BaseModel):
    kind: Literal["pallet", "box", "cargo_place"]
    id: str
    code: str
    barcode: str | None
    seller_name: str | None
    qty: int
    children: list[WarehouseMapProductOut | WarehouseMapContainerOut]


class WarehouseMapCellOut(BaseModel):
    id: str
    code: str
    barcode: str | None
    qty: int
    children: list[WarehouseMapProductOut | WarehouseMapContainerOut]


class WarehouseMapJournalOut(BaseModel):
    id: str
    at: str
    actor_name: str
    subject: str
    qty: int | None
    from_label: str
    to_label: str


class WarehouseOptionOut(BaseModel):
    id: str
    name: str


class WarehouseMapOut(BaseModel):
    warehouses: list[WarehouseOptionOut]
    sellers: list[str]
    categories: list[str]
    cells: list[WarehouseMapCellOut]
    unassigned: list[WarehouseMapProductOut | WarehouseMapContainerOut]
    journal: list[WarehouseMapJournalOut]


class WarehouseMapMoveIn(BaseModel):
    kind: Literal["product", "pallet", "box", "cargo_place"]
    id: uuid.UUID
    to_kind: Literal["cell", "unassigned", "sorting", "pallet", "box", "cargo_place"]
    to_id: uuid.UUID | None = None
    qty: int = Field(gt=0)


class WarehouseMapMoveOut(BaseModel):
    id: str
    moved_qty: int | None


class WarehouseMapDisbandIn(BaseModel):
    id: uuid.UUID | None = None
    pallet_id: uuid.UUID | None = None


class WarehouseMapDisbandOut(BaseModel):
backend/app/services/warehouse_map_service.py:1181:async def place_sorting_object(
backend/app/api/warehouses.py:381:        result = await warehouse_map_service.place_sorting_object(
backend/tests/test_warehouse_map_api.py:272:            "to_kind": "pallet",
backend/tests/test_marketplace_unload_pick_from_container.py:143:            "to_kind": "box",

exec
/bin/zsh -lc "sed -n '730,1045p' backend/app/services/warehouse_map_service.py && sed -n '230,325p' backend/tests/test_warehouse_map_api.py && sed -n '100,175p' backend/tests/test_marketplace_unload_pick_from_container.py && rg -n \"class WarehouseBox|storage_location_id|pallet_id\" backend/app/models/warehouse_box.py backend/app/models/pallet.py backend/app/models/inbound_intake.py backend/app/models/inventory_balance.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:


async def _container_balances(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
) -> list[InventoryBalance]:
    refs: list[tuple[str, uuid.UUID]] = [(kind, container_id)]
    if kind == "pallet":
        warehouse_boxes, inbound_boxes, cargo_places = await _load_boxes(
            session, tenant_id, warehouse_id
        )
        refs.extend(
            (row.container_kind, row.id)
            for row in warehouse_boxes
            if row.pallet_id == container_id
        )
        refs.extend(("box", row.id) for row in inbound_boxes if row.pallet_id == container_id)
        refs.extend(
            ("cargo_place", row.id) for row in cargo_places if row.pallet_id == container_id
        )
    predicates = [
        (InventoryBalance.container_kind == ref_kind) & (InventoryBalance.container_id == ref_id)
        for ref_kind, ref_id in refs
    ]
    rows = list(
        (
            await session.scalars(
                select(InventoryBalance)
                .join(StorageLocation)
                .where(
                    InventoryBalance.tenant_id == tenant_id,
                    InventoryBalance.quantity > 0,
                    StorageLocation.warehouse_id == warehouse_id,
                    or_(*predicates),
                )
                .with_for_update()
            )
        ).all()
    )
    return rows


async def _place_container(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    kind: ContainerKind,
    container_id: uuid.UUID,
    to_kind: DestinationKind,
    to_id: uuid.UUID | None,
    destination_location_id: uuid.UUID,
) -> None:
    if kind == "pallet":
        if to_kind not in {"cell", "unassigned", "sorting"}:
            raise WarehouseMapError("invalid_container_destination")
        pallet = await session.get(Pallet, container_id)
        if (
            pallet is None
            or pallet.tenant_id != tenant_id
            or pallet.warehouse_id != warehouse_id
            or pallet.disbanded_at is not None
        ):
            raise WarehouseMapError("object_not_found")
        pallet.storage_location_id = destination_location_id if to_kind == "cell" else None
        return
    if to_kind not in {"cell", "unassigned", "sorting", "pallet"}:
        raise WarehouseMapError("invalid_container_destination")
    pallet_id = to_id if to_kind == "pallet" else None
    if pallet_id == container_id:
        raise WarehouseMapError("container_cycle")
    if kind == "box":
        warehouse_box = await session.get(WarehouseBox, container_id)
        if (
            warehouse_box is not None
            and warehouse_box.tenant_id == tenant_id
            and warehouse_box.warehouse_id == warehouse_id
            and warehouse_box.container_kind == "box"
        ):
            warehouse_box.pallet_id = pallet_id
            warehouse_box.storage_location_id = (
                destination_location_id if to_kind == "cell" else None
            )
            return
        inbound = await session.scalar(
            select(InboundIntakeBox)
            .join(InboundIntakeRequest)
            .where(
                InboundIntakeBox.id == container_id,
                InboundIntakeBox.tenant_id == tenant_id,
                InboundIntakeRequest.warehouse_id == warehouse_id,
            )
        )
        if inbound is None:
            raise WarehouseMapError("object_not_found")
        inbound.pallet_id = pallet_id
        return
    warehouse_cargo_place = await session.get(WarehouseBox, container_id)
    if (
        warehouse_cargo_place is not None
        and warehouse_cargo_place.tenant_id == tenant_id
        and warehouse_cargo_place.warehouse_id == warehouse_id
        and warehouse_cargo_place.container_kind == "cargo_place"
    ):
        warehouse_cargo_place.pallet_id = pallet_id
        warehouse_cargo_place.storage_location_id = (
            destination_location_id if to_kind == "cell" else None
        )
        return
    cargo = await session.scalar(
        select(InboundIntakeCargoPlace)
        .join(InboundIntakeRequest)
        .where(
            InboundIntakeCargoPlace.id == container_id,
            InboundIntakeCargoPlace.tenant_id == tenant_id,
            InboundIntakeRequest.warehouse_id == warehouse_id,
        )
    )
    if cargo is None:
        raise WarehouseMapError("object_not_found")
    cargo.pallet_id = pallet_id


async def move_object(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    kind: ObjectKind,
    object_id: uuid.UUID,
    to_kind: DestinationKind,
    to_id: uuid.UUID | None,
    quantity: int,
) -> dict[str, Any]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    if quantity <= 0:
        raise WarehouseMapError("quantity_must_be_positive")
    if kind == "pallet" and to_kind == "pallet" and object_id == to_id:
        raise WarehouseMapError("container_cycle")
    destination_location_id, destination_kind, destination_id, to_label = await _destination(
        session, tenant_id, warehouse_id, to_kind, to_id
    )
    transfer_group_id = uuid.uuid4()

    if kind == "product":
        balance = await session.scalar(
            select(InventoryBalance)
            .join(StorageLocation)
            .where(
                InventoryBalance.id == object_id,
                InventoryBalance.tenant_id == tenant_id,
                InventoryBalance.quantity > 0,
                StorageLocation.warehouse_id == warehouse_id,
            )
            .with_for_update()
        )
        if balance is None:
            raise WarehouseMapError("object_not_found")
        if quantity > balance.quantity:
            raise WarehouseMapError("insufficient_stock")
        product = await session.get(Product, balance.product_id)
        assert product is not None
        from_label = await _location_label(session, balance.storage_location_id)
        if balance.container_kind and balance.container_id:
            code = await _container_code(
                session,
                tenant_id,
                warehouse_id,
                cast(ContainerKind, balance.container_kind),
                balance.container_id,
            )
            from_label = _container_title(balance.container_kind, code)
        await _transfer_balance(
            session,
            tenant_id=tenant_id,
            balance=balance,
            quantity=quantity,
            destination_location_id=destination_location_id,
            destination_container_kind=destination_kind,
            destination_container_id=destination_id,
            transfer_group_id=transfer_group_id,
            actor_user_id=actor_user_id,
        )
        subject = product.name
        moved_quantity: int | None = quantity
    else:
        container_kind: ContainerKind = kind
        try:
            await validate_container(session, tenant_id, warehouse_id, container_kind, object_id)
        except ValueError as exc:
            raise WarehouseMapError("object_not_found") from exc
        if to_kind in {"box", "cargo_place"}:
            raise WarehouseMapError("invalid_container_destination")
        source_location_id = await _container_location_id(
            session, tenant_id, warehouse_id, container_kind, object_id
        )
        from_label = await _location_label(session, source_location_id)
        balances = await _container_balances(
            session, tenant_id, warehouse_id, container_kind, object_id
        )
        moved_total = sum(int(row.quantity) for row in balances)
        for balance in balances:
            await _transfer_balance(
                session,
                tenant_id=tenant_id,
                balance=balance,
                quantity=int(balance.quantity),
                destination_location_id=destination_location_id,
                destination_container_kind=cast(ContainerKind, balance.container_kind),
                destination_container_id=balance.container_id,
                transfer_group_id=transfer_group_id,
                actor_user_id=actor_user_id,
            )
        await _place_container(
            session,
            tenant_id,
            warehouse_id,
            container_kind,
            object_id,
            to_kind,
            to_id,
            destination_location_id,
        )
        code = await _container_code(session, tenant_id, warehouse_id, container_kind, object_id)
        subject = _container_title(container_kind, code)
        moved_quantity = moved_total

    event = WarehouseMapEvent(
        tenant_id=tenant_id,
        warehouse_id=warehouse_id,
        actor_user_id=actor_user_id,
        subject=subject,
        quantity=moved_quantity,
        from_label=from_label,
        to_label=to_label,
    )
    session.add(event)
    await session.commit()
    return {"id": str(event.id), "moved_qty": moved_quantity}


async def create_sorting_object(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    *,
    kind: Literal["pallet", "box", "cargo_place"],
) -> dict[str, str | None]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    if kind == "pallet":
        try:
            pallet = await pallet_service.create_pallet(
                session,
                tenant_id,
                warehouse_id=warehouse_id,
            )
        except pallet_service.PalletServiceError as exc:
            raise WarehouseMapError(exc.code) from exc
        return {
            "id": str(pallet.id),
            "kind": kind,
            "code": pallet.code,
            "barcode": pallet.barcode,
            "holder": None,
        }

    try:
        container = await warehouse_box_service.create_warehouse_box(
            session,
            tenant_id,
            warehouse_id=warehouse_id,
            container_kind=kind,
        )
        await session.commit()
        await session.refresh(container)
    except warehouse_box_service.WarehouseBoxError as exc:
        await session.rollback()
        raise WarehouseMapError(exc.code) from exc
    return {
        "id": str(container.id),
        "kind": kind,
        "code": container.internal_barcode,
        "barcode": container.internal_barcode,
        "holder": None,
    }


async def disband_pallet(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    pallet_id: uuid.UUID,
) -> dict[str, Any]:
    await _assert_warehouse(session, tenant_id, warehouse_id)
    pallet = await session.get(Pallet, pallet_id)
    if (
        pallet is None
        or pallet.tenant_id != tenant_id
        or pallet.warehouse_id != warehouse_id
        or pallet.disbanded_at is not None
    ):
        raise WarehouseMapError("pallet_not_found")
    from_label = (
        await _location_label(session, pallet.storage_location_id)
        if pallet.storage_location_id is not None
        else UNASSIGNED_LABEL
    )
    code = pallet.code
    try:
        await pallet_service.disband_pallet(session, tenant_id, pallet_id)
    except pallet_service.PalletServiceError as exc:
    assert _tree_qty(data) == db_total == 15

    moved = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "product",
            "id": str(loose.id),
            "to_kind": "unassigned",
            "to_id": None,
            "qty": 2,
        },
    )
    assert moved.status_code == 200, moved.text
    after_move = (
        await async_client.get(f"/warehouses/{warehouse.id}/map", headers=headers)
    ).json()
    assert after_move["cells"][0]["qty"] == 10
    assert sum(node["qty"] for node in after_move["unassigned"]) == 5
    assert after_move["journal"][0]["actor_name"] == user.email
    assert after_move["journal"][0]["from_label"] == "Ячейка А-01-01"
    assert after_move["journal"][0]["to_label"] == "Без ячеек"
    assert _tree_qty(after_move) == db_total

    zero = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "product",
            "id": str(loose.id),
            "to_kind": "unassigned",
            "qty": 0,
        },
    )
    assert zero.status_code == 422

    self_move = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "pallet",
            "id": str(pallet.id),
            "to_kind": "pallet",
            "to_id": str(pallet.id),
            "qty": 7,
        },
    )
    assert self_move.status_code == 409
    assert self_move.json()["detail"] == "container_cycle"

    sorting_objects = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects", headers=headers
    )
    assert sorting_objects.status_code == 200, sorting_objects.text
    sorting_line = next(
        row for row in sorting_objects.json()["lines"] if row["qty"] == 3
    )
    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "product",
            "id": sorting_line["id"],
            "cell_id": str(cell.id),
            "qty": 3,
        },
    )
    assert placed.status_code == 200, placed.text

    moved_pallet = await async_client.post(
        f"/warehouses/{warehouse.id}/map/move",
        headers=headers,
        json={
            "kind": "pallet",
            "id": str(pallet.id),
            "to_kind": "unassigned",
            "qty": 7,
        },
    )
    assert moved_pallet.status_code == 200, moved_pallet.text
    assert moved_pallet.json()["moved_qty"] == 7
    after_pallet_move = (
        await async_client.get(f"/warehouses/{warehouse.id}/map", headers=headers)
    ).json()
    moved_pallet_node = next(
        node
        for node in after_pallet_move["unassigned"]
        if node["kind"] == "pallet" and node["id"] == str(pallet.id)
    )
    assert moved_pallet_node["qty"] == 7

    disbanded = await async_client.post(
        f"/warehouses/{warehouse.id}/map/disband",
        headers=headers,
        json={"pallet_id": str(pallet.id)},
    )
            "length_mm": 1,
            "width_mm": 1,
            "height_mm": 1,
            "seller_id": sid,
        },
    )
    pid = str(pr.json()["id"])
    await _link_product_wb_barcode(
        async_client, h, seller_id=sid, product_id=pid, monkeypatch=monkeypatch
    )

    # Пять штук приходят россыпью в ячейку.
    loc_id = await _post_inventory(
        async_client,
        h,
        warehouse_id=wid,
        product_id=pid,
        qty=5,
        location_code="PICK-BOX",
    )

    # Заводим короб, ставим его в ту же ячейку и перекладываем в него три штуки.
    box = await async_client.post(
        f"/warehouses/{wid}/sorting-objects", headers=h, json={"kind": "box"}
    )
    assert box.status_code == 201, box.text
    box_id = str(box.json()["id"])

    place = await async_client.post(
        f"/warehouses/{wid}/map/move",
        headers=h,
        json={"kind": "box", "id": box_id, "to_kind": "cell", "to_id": loc_id, "qty": 1},
    )
    assert place.status_code == 200, place.text

    balance_id = await _loose_balance_id(loc_id, pid)

    into_box = await async_client.post(
        f"/warehouses/{wid}/map/move",
        headers=h,
        json={
            "kind": "product",
            "id": balance_id,
            "to_kind": "box",
            "to_id": box_id,
            "qty": 3,
        },
    )
    assert into_box.status_code == 200, into_box.text

    mu = await async_client.post(
        "/operations/marketplace-unload-requests",
        headers=h,
        json={"warehouse_id": wid, "seller_id": sid, "wb_mp_warehouse_id": wb_wid},
    )
    mid = str(mu.json()["id"])
    await async_client.post(
        f"/operations/marketplace-unload-requests/{mid}/lines",
        headers=h,
        json={"product_id": pid, "quantity": 5},
    )
    await _patch_mp_planned_date(async_client, h, mid)
    await _patch_packaging_instructions(async_client, h, pid)
    sub = await async_client.post(
        f"/operations/marketplace-unload-requests/{mid}/submit", headers=h
    )
    assert sub.status_code == 200, sub.text

    # Подбор видит два источника: россыпь и короб.
    options = await async_client.get(
        f"/operations/marketplace-unload-requests/{mid}/pick-options", headers=h
    )
    assert options.status_code == 200, options.text
    product_row = next(o for o in options.json() if o["product_id"] == pid)
    loc_row = next(
        loc for loc in product_row["locations"] if loc["storage_location_id"] == loc_id
backend/app/models/pallet.py:43:    storage_location_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/warehouse_box.py:19:class WarehouseBox(Base):
backend/app/models/warehouse_box.py:50:    storage_location_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/warehouse_box.py:56:    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/inventory_balance.py:57:            "storage_location_id",
backend/app/models/inventory_balance.py:70:    storage_location_id: Mapped[uuid.UUID] = mapped_column(
backend/app/models/inbound_intake.py:157:    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/inbound_intake.py:258:    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/inbound_intake.py:345:    storage_location_id: Mapped[uuid.UUID | None] = mapped_column(
backend/app/models/inbound_intake.py:382:    storage_location_id: Mapped[uuid.UUID] = mapped_column(

exec
/bin/zsh -lc "rg -n \"resolve.*container|container.*kind|WarehouseBox.*id ==|Pallet.*id ==.*to|to_id.*container|destination_kind\" backend/app/services backend/app/api -g '*.py' | head -250 && sed -n '1,180p' backend/app/services/inventory_container_service.py && sed -n '1,95p' backend/app/models/warehouse_box.py && sed -n '1,75p' backend/app/models/pallet.py && sed -n '130,285p' backend/app/models/inbound_intake.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
backend/app/services/pick_option_location_service.py:75:            InventoryBalance.container_kind,
backend/app/services/pick_option_location_service.py:94:        container_refs.add((cast(ContainerKind, raw_kind), container_id))
backend/app/services/pick_option_location_service.py:97:        container_paths = await warehouse_map_service.resolve_container_paths(
backend/app/services/marketplace_unload_pick_service.py:236:    container_kind: ContainerKind | None = None,
backend/app/services/marketplace_unload_pick_service.py:249:        container_kind=container_kind,
backend/app/api/fbs_supplies.py:238:    container_kind: Literal["pallet", "box", "cargo_place"] | None = None
backend/app/api/fbs_supplies.py:243:        if (self.container_kind is None) != (self.container_id is None):
backend/app/api/fbs_supplies.py:244:            msg = "container_kind and container_id must be set together"
backend/app/api/fbs_supplies.py:1284:            container_kind=body.container_kind,
backend/app/api/marketplace_unload_requests.py:286:    container_kind: Literal["pallet", "box", "cargo_place"] | None = None
backend/app/api/marketplace_unload_requests.py:291:        if (self.container_kind is None) != (self.container_id is None):
backend/app/api/marketplace_unload_requests.py:292:            msg = "container_kind and container_id must be set together"
backend/app/api/marketplace_unload_requests.py:1358:            container_kind=body.container_kind,
backend/app/services/inventory_balance_upsert.py:28:    container_kind: ContainerKind | None = None,
backend/app/services/inventory_balance_upsert.py:36:        "container_kind": container_kind,
backend/app/services/fbs_cancelled_after_pack_service.py:267:            .join(WarehouseBox, WarehouseBox.id == FbsPackingBox.warehouse_box_id)
backend/app/api/inventory_counts.py:104:    container_kind: str | None
backend/app/api/inventory_counts.py:313:                container_kind=line.container_kind,
backend/app/services/pallet_service.py:189:            WarehouseBox.tenant_id == tenant_id,
backend/app/services/pallet_service.py:190:            WarehouseBox.warehouse_id == warehouse_id,
backend/app/services/pallet_service.py:213:            InventoryBalance.container_kind == kind,
backend/app/services/pallet_service.py:236:        if balance.container_kind is None or balance.container_id is None:
backend/app/services/pallet_service.py:238:        key = (balance.product_id, balance.container_kind, balance.container_id)
backend/app/services/pallet_service.py:277:                    InventoryBalance.container_kind == "pallet",
backend/app/services/pallet_service.py:294:                    InventoryBalance.container_kind.is_(None),
backend/app/services/pallet_service.py:302:            source.container_kind = None
backend/app/services/pallet_service.py:362:            *((box.container_kind, box.id) for box in warehouse_boxes),
backend/app/services/pallet_service.py:411:                    WarehouseBox.tenant_id == tenant_id,
backend/app/services/pallet_service.py:412:                    WarehouseBox.pallet_id == pallet.id,
backend/app/services/pallet_service.py:425:            *((box.container_kind, box.id) for box in warehouse_boxes),
backend/app/services/inventory_service.py:521:    container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:528:    if (container_kind is None) != (container_id is None):
backend/app/services/inventory_service.py:529:        msg = "container kind and id must be set together"
backend/app/services/inventory_service.py:541:    if container_kind is not None and container_id is not None:
backend/app/services/inventory_service.py:546:            container_kind,
backend/app/services/inventory_service.py:586:                container_kind=container_kind,
backend/app/services/inventory_service.py:612:            InventoryBalance.container_kind == container_kind,
backend/app/services/inventory_service.py:641:                container_kind=container_kind,
backend/app/services/inventory_service.py:698:                InventoryBalance.container_kind.is_(None),
backend/app/services/inventory_service.py:717:                InventoryBalance.container_kind.is_(None),
backend/app/services/inventory_service.py:764:            InventoryBalance.container_kind.is_(None),
backend/app/services/inventory_service.py:1080:    container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:1094:            InventoryBalance.container_kind.is_(None)
backend/app/services/inventory_service.py:1095:            if container_kind is None
backend/app/services/inventory_service.py:1096:            else InventoryBalance.container_kind == container_kind,
backend/app/services/inventory_service.py:1111:    container_kind: ContainerKind | None,
backend/app/services/inventory_service.py:1119:        InventoryBalance.container_kind.is_(None)
backend/app/services/inventory_service.py:1120:        if container_kind is None
backend/app/services/inventory_service.py:1121:        else InventoryBalance.container_kind == container_kind,
backend/app/services/inventory_service.py:1183:    container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:1194:        container_kind,
backend/app/services/inventory_service.py:1210:        container_kind=container_kind,
backend/app/services/inventory_service.py:1224:    container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:1243:        container_kind=container_kind,
backend/app/services/inventory_service.py:1334:    from_container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:1336:    to_container_kind: ContainerKind | None = None,
backend/app/services/inventory_service.py:1373:        from_container_kind,
backend/app/services/inventory_service.py:1390:        container_kind=from_container_kind,
backend/app/services/inventory_service.py:1402:        container_kind=to_container_kind,
backend/app/services/inventory_service.py:1442:            InventoryBalance.container_kind.is_(None),
backend/app/services/scan_resolver_service.py:152:        WarehouseBox.tenant_id == tenant_id,
backend/app/services/scan_resolver_service.py:154:        WarehouseBox.container_kind == "box",
backend/app/services/scan_resolver_service.py:157:        warehouse_stmt = warehouse_stmt.where(WarehouseBox.warehouse_id == warehouse_id)
backend/app/services/scan_resolver_service.py:201:        WarehouseBox.tenant_id == tenant_id,
backend/app/services/scan_resolver_service.py:203:        WarehouseBox.container_kind == "cargo_place",
backend/app/services/scan_resolver_service.py:206:        warehouse_stmt = warehouse_stmt.where(WarehouseBox.warehouse_id == warehouse_id)
backend/app/services/fbs_picking_service.py:316:    container_kind: ContainerKind | None = None,
backend/app/services/fbs_picking_service.py:406:                container_kind=container_kind,
backend/app/services/fbs_picking_service.py:703:    container_kind: ContainerKind | None = None,
backend/app/services/fbs_picking_service.py:861:            container_kind,
backend/app/services/fbs_picking_service.py:878:                from_container_kind=container_kind,
backend/app/services/fbs_picking_service.py:957:        source_container_kind=container_kind,
backend/app/services/fbs_picking_service.py:1005:    container_kind: ContainerKind | None = None,
backend/app/services/fbs_picking_service.py:1032:        container_kind=container_kind,
backend/app/services/fbs_picking_service.py:1228:                to_container_kind=cast("ContainerKind | None", pick.source_container_kind),
backend/app/services/inventory_container_service.py:26:    container_kind: ContainerKind,
backend/app/services/inventory_container_service.py:29:    if container_kind == "pallet":
backend/app/services/inventory_container_service.py:38:    elif container_kind == "cargo_place":
backend/app/services/inventory_container_service.py:41:                WarehouseBox.id == container_id,
backend/app/services/inventory_container_service.py:42:                WarehouseBox.tenant_id == tenant_id,
backend/app/services/inventory_container_service.py:43:                WarehouseBox.warehouse_id == warehouse_id,
backend/app/services/inventory_container_service.py:44:                WarehouseBox.container_kind == "cargo_place",
backend/app/services/inventory_container_service.py:64:                WarehouseBox.id == container_id,
backend/app/services/inventory_container_service.py:65:                WarehouseBox.tenant_id == tenant_id,
backend/app/services/inventory_container_service.py:66:                WarehouseBox.warehouse_id == warehouse_id,
backend/app/services/inventory_container_service.py:67:                WarehouseBox.container_kind == "box",
backend/app/services/fbs_packaging_integration_service.py:822:            WarehouseBox.id == packaging_box_id,
backend/app/services/fbs_packaging_integration_service.py:823:            WarehouseBox.tenant_id == tenant_id,
backend/app/services/fbs_packaging_integration_service.py:824:            WarehouseBox.warehouse_id == supply.warehouse_id,
backend/app/services/warehouse_box_service.py:33:    container_kind: Literal["box", "cargo_place"] = "box",
backend/app/services/warehouse_box_service.py:40:            container_kind=container_kind,
backend/app/services/warehouse_box_service.py:62:        WarehouseBox.tenant_id == tenant_id,
backend/app/services/warehouse_map_service.py:56:def _container_title(kind: str, code: str) -> str:
backend/app/services/warehouse_map_service.py:70:        label=_container_title(kind, code),
backend/app/services/warehouse_map_service.py:74:async def resolve_container_paths(
backend/app/services/warehouse_map_service.py:100:        (box.container_kind, box.id): box for box in warehouse_boxes
backend/app/services/warehouse_map_service.py:140:        path.append(_container_path_item(kind, container_id, code))
backend/app/services/warehouse_map_service.py:168:                    WarehouseBox.tenant_id == tenant_id,
backend/app/services/warehouse_map_service.py:169:                    WarehouseBox.warehouse_id == warehouse_id,
backend/app/services/warehouse_map_service.py:295:        _normalize_container(child) if child["kind"] != "product" else child
backend/app/services/warehouse_map_service.py:336:        if balance.container_kind and balance.container_id:
backend/app/services/warehouse_map_service.py:337:            balance_location.setdefault((balance.container_kind, balance.container_id), location.id)
backend/app/services/warehouse_map_service.py:355:        key = (box.container_kind, box.id)
backend/app/services/warehouse_map_service.py:357:            "kind": box.container_kind,
backend/app/services/warehouse_map_service.py:430:            (balance.container_kind, balance.container_id)
backend/app/services/warehouse_map_service.py:431:            if balance.container_kind and balance.container_id
backend/app/services/warehouse_map_service.py:468:                _normalize_container(child) if child["kind"] != "product" else child
backend/app/services/warehouse_map_service.py:484:        _normalize_container(node) if node["kind"] != "product" else node for node in unassigned
backend/app/services/warehouse_map_service.py:542:    await validate_container(session, tenant_id, warehouse_id, kind, container_id)
backend/app/services/warehouse_map_service.py:554:            and box.container_kind == kind
backend/app/services/warehouse_map_service.py:568:            and cargo_place.container_kind == kind
backend/app/services/warehouse_map_service.py:580:            InventoryBalance.container_kind == kind,
backend/app/services/warehouse_map_service.py:621:    container_kind = cast(ContainerKind, to_kind)
backend/app/services/warehouse_map_service.py:624:            session, tenant_id, warehouse_id, container_kind, to_id
backend/app/services/warehouse_map_service.py:628:    code = await _container_code(session, tenant_id, warehouse_id, container_kind, to_id)
backend/app/services/warehouse_map_service.py:629:    return location_id, container_kind, to_id, _container_title(container_kind, code)
backend/app/services/warehouse_map_service.py:649:            and warehouse_box.container_kind == "box"
backend/app/services/warehouse_map_service.py:669:            and warehouse_cargo_place.container_kind == "cargo_place"
backend/app/services/warehouse_map_service.py:700:    destination_container_kind: ContainerKind | None,
backend/app/services/warehouse_map_service.py:705:    source_kind = cast(ContainerKind | None, balance.container_kind)
backend/app/services/warehouse_map_service.py:714:        container_kind=source_kind,
backend/app/services/warehouse_map_service.py:726:        container_kind=destination_container_kind,
backend/app/services/warehouse_map_service.py:745:            (row.container_kind, row.id)
backend/app/services/warehouse_map_service.py:754:        (InventoryBalance.container_kind == ref_kind) & (InventoryBalance.container_id == ref_id)
backend/app/services/warehouse_map_service.py:809:            and warehouse_box.container_kind == "box"
backend/app/services/warehouse_map_service.py:834:        and warehouse_cargo_place.container_kind == "cargo_place"
backend/app/services/warehouse_map_service.py:872:    destination_location_id, destination_kind, destination_id, to_label = await _destination(
backend/app/services/warehouse_map_service.py:896:        if balance.container_kind and balance.container_id:
backend/app/services/warehouse_map_service.py:901:                cast(ContainerKind, balance.container_kind),
backend/app/services/warehouse_map_service.py:904:            from_label = _container_title(balance.container_kind, code)
backend/app/services/warehouse_map_service.py:911:            destination_container_kind=destination_kind,
backend/app/services/warehouse_map_service.py:919:        container_kind: ContainerKind = kind
backend/app/services/warehouse_map_service.py:921:            await validate_container(session, tenant_id, warehouse_id, container_kind, object_id)
backend/app/services/warehouse_map_service.py:927:            session, tenant_id, warehouse_id, container_kind, object_id
backend/app/services/warehouse_map_service.py:931:            session, tenant_id, warehouse_id, container_kind, object_id
backend/app/services/warehouse_map_service.py:941:                destination_container_kind=cast(ContainerKind, balance.container_kind),
backend/app/services/warehouse_map_service.py:950:            container_kind,
backend/app/services/warehouse_map_service.py:956:        code = await _container_code(session, tenant_id, warehouse_id, container_kind, object_id)
backend/app/services/warehouse_map_service.py:957:        subject = _container_title(container_kind, code)
backend/app/services/warehouse_map_service.py:1004:            container_kind=kind,
backend/app/services/marketplace_unload_collect_service.py:433:    container_kind: ContainerKind | None = None,
backend/app/services/marketplace_unload_collect_service.py:517:            container_kind,
backend/app/services/marketplace_unload_collect_service.py:532:            container_kind=container_kind,
backend/app/services/marketplace_unload_collect_service.py:549:            container_kind=container_kind,
backend/app/services/marketplace_unload_collect_service.py:594:    container_kind: ContainerKind | None = None,
backend/app/services/marketplace_unload_collect_service.py:659:            container_kind=container_kind,
backend/app/services/marketplace_unload_collect_service.py:700:        container_kind=container_kind,
backend/app/services/inventory_count_service.py:128:                WarehouseBox.tenant_id == tenant_id,
backend/app/services/inventory_count_service.py:129:                WarehouseBox.warehouse_id == warehouse_id,
backend/app/services/inventory_count_service.py:130:                WarehouseBox.pallet_id == pallet.id,
backend/app/services/inventory_count_service.py:153:        refs.extend((row.container_kind, row.id) for row in warehouse_containers.all())
backend/app/services/inventory_count_service.py:161:                WarehouseBox.id == container_id,
backend/app/services/inventory_count_service.py:162:                WarehouseBox.tenant_id == tenant_id,
backend/app/services/inventory_count_service.py:163:                WarehouseBox.container_kind == "box",
backend/app/services/inventory_count_service.py:180:                WarehouseBox.id == container_id,
backend/app/services/inventory_count_service.py:181:                WarehouseBox.tenant_id == tenant_id,
backend/app/services/inventory_count_service.py:182:                WarehouseBox.container_kind == "cargo_place",
backend/app/services/inventory_count_service.py:314:            container_kind = cast(ContainerKind, object_scope.type)
backend/app/services/inventory_count_service.py:318:                container_kind,
backend/app/services/inventory_count_service.py:323:                    InventoryBalance.container_kind == ref_kind,
backend/app/services/inventory_count_service.py:364:                container_kind=balance.container_kind,
backend/app/services/inventory_count_service.py:493:        InventoryBalance.container_kind == line.container_kind,
backend/app/services/inventory_count_service.py:587:                container_kind=cast(ContainerKind | None, line.container_kind),
"""Resolve polymorphic balance containers inside tenant and warehouse bounds."""

from __future__ import annotations

import uuid
from typing import Literal

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.inbound_intake import (
    InboundIntakeBox,
    InboundIntakeCargoPlace,
    InboundIntakeRequest,
)
from app.models.pallet import Pallet
from app.models.warehouse_box import WarehouseBox

ContainerKind = Literal["pallet", "box", "cargo_place"]


async def validate_container(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    container_kind: ContainerKind,
    container_id: uuid.UUID,
) -> None:
    if container_kind == "pallet":
        found_id = await session.scalar(
            select(Pallet.id).where(
                Pallet.id == container_id,
                Pallet.tenant_id == tenant_id,
                Pallet.warehouse_id == warehouse_id,
                Pallet.disbanded_at.is_(None),
            )
        )
    elif container_kind == "cargo_place":
        found_id = await session.scalar(
            select(WarehouseBox.id).where(
                WarehouseBox.id == container_id,
                WarehouseBox.tenant_id == tenant_id,
                WarehouseBox.warehouse_id == warehouse_id,
                WarehouseBox.container_kind == "cargo_place",
            )
        )
        if found_id is None:
            found_id = await session.scalar(
                select(InboundIntakeCargoPlace.id)
                .join(
                    InboundIntakeRequest,
                    InboundIntakeRequest.id == InboundIntakeCargoPlace.request_id,
                )
                .where(
                    InboundIntakeCargoPlace.id == container_id,
                    InboundIntakeCargoPlace.tenant_id == tenant_id,
                    InboundIntakeRequest.tenant_id == tenant_id,
                    InboundIntakeRequest.warehouse_id == warehouse_id,
                )
            )
    else:
        found_id = await session.scalar(
            select(WarehouseBox.id).where(
                WarehouseBox.id == container_id,
                WarehouseBox.tenant_id == tenant_id,
                WarehouseBox.warehouse_id == warehouse_id,
                WarehouseBox.container_kind == "box",
            )
        )
        if found_id is None:
            found_id = await session.scalar(
                select(InboundIntakeBox.id)
                .join(
                    InboundIntakeRequest,
                    InboundIntakeRequest.id == InboundIntakeBox.request_id,
                )
                .where(
                    InboundIntakeBox.id == container_id,
                    InboundIntakeBox.tenant_id == tenant_id,
                    InboundIntakeRequest.tenant_id == tenant_id,
                    InboundIntakeRequest.warehouse_id == warehouse_id,
                )
            )
    if found_id is None:
        msg = "container not found"
        raise ValueError(msg)
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING, Literal

from sqlalchemy import CheckConstraint, DateTime, ForeignKey, String, UniqueConstraint, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.pallet import Pallet
    from app.models.storage_location import StorageLocation
    from app.models.tenant import Tenant
    from app.models.warehouse import Warehouse


class WarehouseBox(Base):
    """Сквозной физический короб склада (ШК печатается на этикетке 58x40)."""

    __tablename__ = "warehouse_boxes"
    __table_args__ = (
        CheckConstraint(
            "container_kind IN ('box', 'cargo_place')",
            name="ck_warehouse_boxes_container_kind",
        ),
        UniqueConstraint(
            "tenant_id",
            "internal_barcode",
            name="uq_warehouse_boxes_tenant_barcode",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    warehouse_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("warehouses.id", ondelete="CASCADE"),
        index=True,
    )
    internal_barcode: Mapped[str] = mapped_column(String(64), nullable=False)
    container_kind: Mapped[Literal["box", "cargo_place"]] = mapped_column(
        String(32), nullable=False, default="box", server_default="box"
    )
    storage_location_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("storage_locations.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("pallets.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant")
    warehouse: Mapped[Warehouse] = relationship("Warehouse")
    storage_location: Mapped[StorageLocation | None] = relationship("StorageLocation")
    pallet: Mapped[Pallet | None] = relationship(
        "Pallet", back_populates="warehouse_boxes"
    )
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import DateTime, ForeignKey, String, Text, UniqueConstraint, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.inbound_intake import InboundIntakeBox, InboundIntakeCargoPlace
    from app.models.storage_location import StorageLocation
    from app.models.tenant import Tenant
    from app.models.warehouse import Warehouse
    from app.models.warehouse_box import WarehouseBox


class Pallet(Base):
    """Tenant-owned warehouse pallet grouping boxes and cargo places."""

    __tablename__ = "pallets"
    __table_args__ = (
        UniqueConstraint("tenant_id", "code", name="uq_pallets_tenant_code"),
        UniqueConstraint("tenant_id", "barcode", name="uq_pallets_tenant_barcode"),
    )

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    warehouse_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("warehouses.id", ondelete="CASCADE"),
        index=True,
    )
    code: Mapped[str] = mapped_column(String(32), nullable=False)
    barcode: Mapped[str] = mapped_column(String(64), nullable=False)
    free_text: Mapped[str | None] = mapped_column(Text, nullable=True)
    storage_location_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("storage_locations.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    disbanded_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant")
    warehouse: Mapped[Warehouse] = relationship("Warehouse")
    storage_location: Mapped[StorageLocation | None] = relationship("StorageLocation")
    boxes: Mapped[list[InboundIntakeBox]] = relationship(
        "InboundIntakeBox", back_populates="pallet"
    )
    cargo_places: Mapped[list[InboundIntakeCargoPlace]] = relationship(
        "InboundIntakeCargoPlace", back_populates="pallet"
    )
    warehouse_boxes: Mapped[list[WarehouseBox]] = relationship(
        "WarehouseBox", back_populates="pallet"
    )

    __tablename__ = "inbound_intake_cargo_places"
    __table_args__ = (
        UniqueConstraint(
            "request_id",
            "place_number",
            name="uq_inbound_intake_cargo_place_req_num",
        ),
        UniqueConstraint(
            "tenant_id",
            "internal_barcode",
            name="uq_inbound_intake_cargo_place_tenant_barcode",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    request_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("inbound_intake_requests.id", ondelete="CASCADE"),
        index=True,
    )
    place_number: Mapped[int] = mapped_column(Integer, nullable=False)
    internal_barcode: Mapped[str] = mapped_column(String(64), nullable=False)
    free_text: Mapped[str | None] = mapped_column(Text, nullable=True)
    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("pallets.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    label_printed_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    request: Mapped[InboundIntakeRequest] = relationship(
        "InboundIntakeRequest", back_populates="cargo_places"
    )
    pallet: Mapped[Pallet | None] = relationship(
        "Pallet", back_populates="cargo_places"
    )
    lines: Mapped[list[InboundIntakeCargoPlaceLine]] = relationship(
        "InboundIntakeCargoPlaceLine",
        back_populates="cargo_place",
        cascade="all, delete-orphan",
    )


class InboundIntakeCargoPlaceLine(Base):
    __tablename__ = "inbound_intake_cargo_place_lines"
    __table_args__ = (
        UniqueConstraint(
            "cargo_place_id",
            "product_id",
            name="uq_inbound_intake_cargo_place_line_place_product",
        ),
        CheckConstraint(
            "quantity >= 0",
            name="ck_inbound_cargo_place_line_quantity_nonnegative",
        ),
        CheckConstraint(
            "posted_qty >= 0",
            name="ck_inbound_cargo_place_line_posted_nonnegative",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    cargo_place_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("inbound_intake_cargo_places.id", ondelete="CASCADE"),
        index=True,
    )
    product_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("products.id", ondelete="CASCADE"),
        index=True,
    )
    quantity: Mapped[int] = mapped_column(Integer, nullable=False)
    posted_qty: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    cargo_place: Mapped[InboundIntakeCargoPlace] = relationship(
        "InboundIntakeCargoPlace", back_populates="lines"
    )
    product: Mapped[Product] = relationship("Product")


class InboundIntakeBox(Base):
    """Physical inbound box with internal barcode for later piece-by-piece intake."""

    __tablename__ = "inbound_intake_boxes"
    __table_args__ = (
        UniqueConstraint(
            "request_id",
            "box_number",
            name="uq_inbound_intake_box_req_num",
        ),
        UniqueConstraint(
            "tenant_id",
            "internal_barcode",
            name="uq_inbound_intake_box_tenant_barcode",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    request_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("inbound_intake_requests.id", ondelete="CASCADE"),
        index=True,
    )
    box_number: Mapped[int] = mapped_column(Integer, nullable=False)
    internal_barcode: Mapped[str] = mapped_column(String(64), nullable=False)
    free_text: Mapped[str | None] = mapped_column(Text, nullable=True)
    pallet_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("pallets.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )
    label_printed_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    intake_opened_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    intake_closed_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )

    request: Mapped[InboundIntakeRequest] = relationship(
        "InboundIntakeRequest", back_populates="boxes"
    )
    pallet: Mapped[Pallet | None] = relationship("Pallet", back_populates="boxes")
    lines: Mapped[list[InboundIntakeBoxLine]] = relationship(
        "InboundIntakeBoxLine",
        back_populates="box",
        cascade="all, delete-orphan",
    )

exec
/bin/zsh -lc "rg -n \"sorting-objects/place|cell_id|to_id\" frontend/src frontend/tests-e2e -g '*.{ts,tsx}' | head -200 && rg -n \"sorting-objects\" docs tasks -g '*.md' | head -200 && rg -n \"class SortingCellOut|SortingObjectsOut\" -S . --glob '"'!frontend/**'"' --glob '"'!ZADANIE.md'"'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 41ms:
exec
/bin/zsh -lc "sed -n '60,120p' frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx && rg -n \"onPlace|PlacePayload|cellId|toId\" frontend/src/screens/ff/sorting-objects -g '*.{ts,tsx}' && sed -n '1,220p' frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx && sed -n '1,180p' frontend/src/screens/ff/sorting-objects/types.ts 2>/dev/null || true" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
  // работу оператора под руками. Но приехавшие позже данные он бы и не увидел.
  // Смена ключа пересобирает экран заново — ровно тогда, когда пришёл новый
  // состав, и ни разу между.
  const [version, setVersion] = useState(0)

  const load = useCallback(async () => {
    if (!warehouseId) return
    setError(null)
    try {
      const res = await fetch(apiUrl(`/warehouses/${warehouseId}/sorting-objects`), {
        headers: headers(token),
      })
      if (!res.ok) throw new Error(await readApiErrorMessage(res))
      setData((await res.json()) as ApiSorting)
      setVersion((current) => current + 1)
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Не удалось загрузить раскладку')
    }
  }, [token, warehouseId])

  useEffect(() => {
    void load()
  }, [load])

  async function place(payload: {
    kind: ObjKind | 'product'
    id: string
    cellId: string | null
    toId: string | null
    qty: number
  }) {
    if (!warehouseId) return
    setError(null)
    try {
      const res = await fetch(apiUrl(`/warehouses/${warehouseId}/sorting-objects/place`), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', ...headers(token) },
        body: JSON.stringify({
          kind: payload.kind,
          id: payload.id,
          cell_id: payload.cellId,
          to_id: payload.toId,
          qty: payload.qty,
        }),
      })
      if (!res.ok) throw new Error(await readApiErrorMessage(res))
    } catch (err) {
      // Экран уже переставил строку у себя. Показываем отказ и перечитываем
      // склад: иначе на экране будет одно, а в системе другое, и оператор
      // узнает об этом на инвентаризации.
      setError(err instanceof Error ? err.message : 'Не удалось поставить объект')
      await load()
    }
  }

  if (!warehouseId) {
    return (
      <EmptyState
        title="Нет складов"
        hint="Раскладывать некуда: заведите склад в настройках."
      />
frontend/src/screens/ff/sorting-objects/objectsRows.ts:46:      alreadyAt: Array<{ cellId: string; code: string; qty: number }>
frontend/src/screens/ff/sorting-objects/objectsStub.ts:33:export type AlreadyAt = { cellId: string; code: string; qty: number }
frontend/src/screens/ff/sorting-objects/objectsStub.ts:66:  { id: 'p-tshirt', name: 'Футболка хлопок белая, M', sku: 'TS-WHT-M', seller: 'ИП Горячкина', barcode: '4680123456789', photo: photo('ФБ'), size: 'M', alreadyAt: [{ cellId: 'c-a11', code: 'А 1.1', qty: 24 }] },
frontend/src/screens/ff/sorting-objects/objectsStub.ts:68:  { id: 'p-sneakers', name: 'Кроссовки беговые, 42', sku: 'SN-RUN-42', seller: 'ООО Ситипак', barcode: '4600987654321', photo: photo('КР'), size: '42', alreadyAt: [{ cellId: 'c-b11', code: 'Б 1.1', qty: 36 }] },
frontend/src/screens/ff/sorting-objects/objectsStub.ts:69:  { id: 'p-socks', name: 'Носки спортивные, 3 пары', sku: 'SK-SPT-3', seller: 'ООО Ситипак', barcode: '4600987654338', photo: photo('НС'), size: null, alreadyAt: [{ cellId: 'c-a12', code: 'А 1.2', qty: 60 }] },
frontend/src/screens/ff/sorting-objects/objectsStub.ts:71:  { id: 'p-belt', name: 'Ремень кожаный, 110 см', sku: 'BL-110', seller: 'ИП Ларин', barcode: '4601122334462', photo: photo('РМ'), size: '110', alreadyAt: [{ cellId: 'c-a12', code: 'А 1.2', qty: 8 }] },
frontend/src/screens/ff/sorting-objects/objectsStub.ts:151:export function cellQty(cellId: string, objects: WarehouseObject[], lines: GoodsLine[]): number {
frontend/src/screens/ff/sorting-objects/objectsStub.ts:153:    .filter((line) => line.holder === cellRef(cellId))
frontend/src/screens/ff/sorting-objects/objectsStub.ts:156:    .filter((one) => one.holder === cellRef(cellId))
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:55:  onPlace,
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:69:  onPlace: (row: ObjectRow) => void
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:77:  onPickCell: (cellId: string) => void
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:104:                key={place.cellId}
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:105:                onClick={() => onPickCell(place.cellId)}
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:107:                data-testid={`objects-already-${place.cellId}`}
frontend/src/screens/ff/sorting-objects/ObjectsTree.tsx:325:                onClick={() => onPlace(row)}
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:92:  onPlace?: (payload: {
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:95:    cellId: string | null
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:96:    toId: string | null
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:107:  onPlace,
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:153:  function targetParts(target: Holder): { cellId: string | null; toId: string | null } {
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:154:    if (!target) return { cellId: null, toId: null }
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:155:    if (target.startsWith('cell:')) return { cellId: target.slice(5), toId: null }
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:156:    return { cellId: null, toId: target.slice(4) }
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:162:    onPlace?.({ kind: 'product', id: line.productId, qty, ...targetParts(target) })
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:175:    onPlace?.({ kind: object.kind, id: object.id, qty: 1, ...targetParts(target) })
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:379:            onPlace={(row: ObjectRow) =>
frontend/src/screens/ff/sorting-objects/SortingObjectsScreen.tsx:506:            onPlace={(row: ObjectRow) =>
frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx:87:    cellId: string | null
frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx:88:    toId: string | null
frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx:100:          cell_id: payload.cellId,
frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx:101:          to_id: payload.toId,
frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx:207:          onPlace={(payload) => void place(payload)}
import { Box, Paper, Stack, Typography } from '@mui/material'
import { alpha, useTheme } from '@mui/material/styles'
import { useState } from 'react'
import {
  ActionGroup,
  AppDialog,
  NumberInput,
  PrimaryAction,
  PrintAction,
  ScannerField,
  ScreenHeader,
  SecondaryAction,
  SelectInput,
} from '../../../ui-kit'
import { CreateCellDialog } from '../warehouse-map/WarehouseMapToolbar'
import type { LabelSize } from '../../../utils/labelSize'
import { BoxLabelPrintDialog } from '../../../components/BoxLabelPrintDialog'
import { LinearProgress } from '@mui/material'
import { ObjectsTree } from './ObjectsTree'
import {
  CELLS,
  INITIAL_LINES,
  INITIAL_OBJECTS,
  PRODUCTS,
  KIND_TITLE,
  cellQty,
  cellRef,
  productById,
  type Cell,
  type GoodsLine,
  type Holder,
  type Product,
  whereIs,
  type ObjKind,
  type WarehouseObject,
} from './objectsStub'
import {
  canPut,
  cellRows,
  destinationsFor,
  objectTitle,
  unplacedRows,
  type Carried,
  type ObjectRow,
} from './objectsRows'

// Раскладка объектами.
//
// Порядок работы обратный привычному: сначала собираем объект — товар в короб,
// короб на палету, — и только готовый объект ставим на полку. Где лежит товар,
// отдельно не хранится: это вычисляется по цепочке держателей, поэтому «что в
// коробе» и «где короб» не могут разъехаться. Это одно знание.
//
// Список один. Короба, палеты и грузоместа стоят в нём как агрегаты со своим
// содержимым внутри, товар россыпью — такими же строками верхнего уровня, а
// разница между «ещё не поставлено» и «стоит на полке» видна колонкой «Где».

/**
 * Экран работает и от сервера, и от заглушки.
 *
 * Данные приходят пропсами, без них берутся выдуманные: превью макета обязано
 * открываться без сервера, иначе посмотреть на экран можно будет только после
 * готового бэка, а смотреть надо раньше.
 */
type SortingScreenProps = {
  onNote: (note: string) => void
  initialObjects?: WarehouseObject[]
  initialLines?: GoodsLine[]
  products?: Product[]
  initialCells?: Cell[]
  /** Подпись под заголовком: что за документ раскладываем. */
  purpose?: string
  /** Имя склада для диалога создания ячейки. */
  warehouseName?: string
  /** Завести ячейку на сервере. Без него экран заводит её только у себя. */
  onCreateCell?: (code: string) => void
  /** Напечатать штрихкод. Без него печать остаётся заглушкой превью. */
  onPrint?: (title: string, barcode: string, size: LabelSize) => void
  /** Уйти с экрана. Без него «Закрыть» остаётся заглушкой превью. */
  onClose?: () => void
  /** Завести тару на сервере. Без него тара появляется только в превью. */
  onCreateObject?: (kind: ObjKind) => void
  /**
   * Каждая постановка на полку уходит на сервер сразу.
   *
   * Тогда «Сохранить» и «Завершить» сохранять нечего: они бы только делали вид.
   * Экран честно говорит об этом вместо того, чтобы показывать живую кнопку,
   * которая ничего не делает.
   */
  savedImmediately?: boolean
  /** Поставить объект или товар на ячейку. Без него экран двигает только себя. */
  onPlace?: (payload: {
    kind: ObjKind | 'product'
    id: string
    cellId: string | null
    toId: string | null
    qty: number
  }) => void
}

export function SortingObjectsScreen({
  onNote,
  initialObjects,
  initialLines,
  products: productsProp,
  initialCells,
  onPlace,
  purpose,
  warehouseName,
  onCreateCell,
  onPrint,
  onClose,
  onCreateObject,
  savedImmediately,
}: SortingScreenProps) {
  const theme = useTheme()
  const products = productsProp ?? PRODUCTS
  const [objects, setObjects] = useState<WarehouseObject[]>(initialObjects ?? INITIAL_OBJECTS)
  const [lines, setLines] = useState<GoodsLine[]>(initialLines ?? INITIAL_LINES)
  const [collapsed, setCollapsed] = useState<Set<string>>(new Set())
  const [carried, setCarried] = useState<Carried | null>(null)
  const [activeCellId, setActiveCellId] = useState<string | null>(null)
  const [scanValue, setScanValue] = useState('')
  const [scanError, setScanError] = useState<string | null>(null)
  const [scanNotice, setScanNotice] = useState<string | null>(null)
  const [asking, setAsking] = useState<Carried | null>(null)
  const [askTarget, setAskTarget] = useState('')
  const [askQty, setAskQty] = useState<number | null>(null)
  const [created, setCreated] = useState(0)
  const [printing, setPrinting] = useState<string | null>(null)
  const [cellDialogOpen, setCellDialogOpen] = useState(false)
  const [extraCells, setExtraCells] = useState<typeof CELLS>([])

  const cells = [...(initialCells ?? CELLS), ...extraCells]
  const activeCell = cells.find((one) => one.id === activeCellId) ?? null
  const loose = lines.filter((line) => line.holder === null)
  const unplaced = objects.filter((one) => one.holder === null)
  const totalQty = lines.reduce((sum, line) => sum + line.qty, 0)
  const leftQty = lines
    .filter((line) => !whereIs(line.holder, objects, cells).cell)
    .reduce((sum, line) => sum + line.qty, 0)

  function toggle(objectId: string) {
    setCollapsed((current) => {
      const next = new Set(current)
      if (next.has(objectId)) next.delete(objectId)
      else next.add(objectId)
      return next
    })
  }

  /** Куда именно поставили: ячейка или другой объект. Сервер различает их. */
  function targetParts(target: Holder): { cellId: string | null; toId: string | null } {
    if (!target) return { cellId: null, toId: null }
    if (target.startsWith('cell:')) return { cellId: target.slice(5), toId: null }
    return { cellId: null, toId: target.slice(4) }
  }

  function moveGoods(line: GoodsLine, qty: number, target: Holder) {
    // Экран двигает у себя сразу, не дожидаясь сервера: оператор ставит короба
    // подряд, и пауза на каждый ответ превращает раскладку в ожидание.
    onPlace?.({ kind: 'product', id: line.productId, qty, ...targetParts(target) })
    setLines((current) => {
      const rest = current.filter((one) => one.id !== line.id)
      const left = line.qty - qty
      const twin = rest.find((one) => one.productId === line.productId && one.holder === target)
      const withTarget = twin
        ? rest.map((one) => (one === twin ? { ...one, qty: one.qty + qty } : one))
        : [...rest, { id: `l-${Date.now()}-${line.id}`, productId: line.productId, qty, holder: target }]
      return left > 0 ? [...withTarget, { ...line, qty: left }] : withTarget
    })
  }

  function moveObject(object: WarehouseObject, target: Holder, label: string) {
    onPlace?.({ kind: object.kind, id: object.id, qty: 1, ...targetParts(target) })
    setObjects((current) => current.map((one) => (one.id === object.id ? { ...one, holder: target } : one)))
    onNote(`${KIND_TITLE[object.kind]} ${object.code} → ${label}`)
  }

  function labelOf(target: Holder): string {
    if (!target) return 'россыпь'
    if (target.startsWith('cell:')) {
      const cell = cells.find((one) => cellRef(one.id) === target)
      return cell ? `ячейку ${cell.code}` : 'ячейку'
    }
    const object = objects.find((one) => `obj:${one.id}` === target)
    return object ? objectTitle(object) : 'объект'
  }

  /** Перетащили: контейнер едет целиком, у товара спрашиваем количество. */
  function drop(target: Holder) {
    if (!carried) return
    if (!canPut(carried, target, objects)) {
      setCarried(null)
      return
    }
    if (carried.kind === 'object') {
      moveObject(carried.object, target, labelOf(target))
    } else {
      openDialog(carried, target)
    }
    setCarried(null)
  }

  /** Снять с ячейки: уезжает целиком и вместе с содержимым, без вопросов. */
  function takeOffCell(row: ObjectRow) {
    if (row.kind === 'goods') {
      moveGoods(row.line, row.line.qty, null)
      return
    }
    // Снятие — это то же перемещение, только цель пустая: сервер понимает
    // пустую цель как «Без ячеек». Раньше здесь менялось только состояние
    // вкладки: экран говорил «снят», а после обновления страницы тара
    // возвращалась в старую ячейку, потому что на сервер ничего не уходило.
    moveObject(row.object, null, 'россыпь')
  }

  /** Вынуть наружу: то же окно, но место уже выбрано — россыпь. */
  function takeOut(row: ObjectRow) {
    openDialog(

exec
/bin/zsh -lc "sed -n '1,260p' ZADANIE.md && sed -n '1,210p' frontend/src/screens/ff/sorting-objects/objectsStub.ts && sed -n '1,130p' frontend/src/screens/ff/sorting-objects/objectsRows.ts && sed -n '1,125p' frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
## Разрешения для этой сессии (читать первым)

1. Ты работаешь в git worktree внутри `~/Projects/WMS/.worktrees/`. Это РАЗРЕШЕНО
   правилами проекта (см. CLAUDE.md, пункт про `git worktree` внутри `.worktrees/`).
   Не останавливайся на проверке `pwd` — ты там, где надо.
2. НЕ гоняй полный `pytest`. Только адресные файлы тестов, которые названы ниже.
   Полный прогон в этом дереве даёт ошибки сбора из-за соседних worktree.
3. Продуктовый гейт (BA / Product / Browser QA) для этой задачи ОТМЕНЁН владельцем
   25.08.2026. Не заводи карточки, не жди вердиктов, не пиши файлы гейта.
4. Фронт не трогай вообще. Только `backend/`. Фронт делает владелец сам.
5. Пиши по-русски в комментариях и коммитах, как принято в этом репозитории.

---

# Задача: раскладка не видит того, что уже разложено

## Что сломано (проверено по коду и базе стенда)

Экран раскладки в документе сортировки берёт данные из
`GET /warehouses/{warehouse_id}/sorting-objects`.

В `backend/app/services/warehouse_map_service.py`, функция `get_sorting_objects`
(около строки 1092) собирает список так:

```python
_sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
```

То есть **только из зоны «Без ячеек»**. А ячейки в ответе отдаются голыми:

```python
"cells": [{"id": row["id"], "code": row["code"], "barcode": row["barcode"]} for row in data["cells"]]
```

**без содержимого**.

Из-за этого: оператор кладёт короб в ячейку, запись в базе появляется (проверено:
на стенде 16 коробов из 63 имеют `storage_location_id`), но при следующем чтении
короб исчезает с экрана — он уже не в «Без ячеек», а в ячейке содержимого нет.
Для оператора это выглядит как «закрепил за ячейкой, и оно сбросилось».

## Ещё два бага в той же ручке размещения

Файл `backend/app/api/warehouses.py`, роут
`POST /{warehouse_id}/sorting-objects/place` (около строки 367):

```python
cell_id = body.cell_id or body.to_id
if cell_id is None:
    raise HTTPException(422, detail="cell_id_required")
```

и дальше `warehouse_map_service.place_sorting_object`, который **всегда** зовёт
`move_object(..., to_kind="cell", to_id=cell_id, ...)`.

**Баг 1 — «снять с ячейки» не работает.** Фронт при снятии шлёт
`{cell_id: null, to_id: null}` и в коде рядом стоит комментарий, что сервер
понимает пустую цель как «Без ячеек». Сервер её не понимает — отвечает 422.
Нужно: пустая цель = переместить объект в зону «Без ячеек»
(ячейка с кодом `SORTING_LOCATION_CODE`), а не отказ.

**Баг 2 — вложить короб в палету нельзя.** Фронт шлёт id тары в `to_id`
(`{cell_id: null, to_id: "<uuid палеты/короба/грузоместа>"}`), а ручка кладёт
это значение в `cell_id` и передаёт как перемещение в ЯЧЕЙКУ. Нужно различать:
`cell_id` — цель-ячейка, `to_id` — цель-тара, и звать `move_object` с
правильным `to_kind`.

## Что сделать

1. `get_sorting_objects` должен возвращать **и** содержимое ячеек: для каждой
   ячейки — какая тара и какой товар в ней лежит, в том же формате, в каком
   сейчас отдаётся содержимое «Без ячеек» (`objects` с полем holder и `lines`).
   Смотри, как устроен `_sorting_tree_rows` и что кладёт `get_warehouse_map`
   в `data["cells"]` — переиспользуй, не пиши второй обход дерева.
   Формат ответа расширяй **добавлением**, ничего не убирая: фронт живой.
2. Роут `place`: пустая цель — снятие в «Без ячеек», без 422.
3. Роут `place`: цель-тара через `to_id` — вложение в тару, а не в ячейку.
4. Схема ответа `SortingObjectsOut` — обнови под новые поля.

## Тесты (обязательно, новые роуты/поведение без теста не принимаются)

Заведи `backend/tests/test_sorting_objects_cells.py`:
- объект положили в ячейку → он виден в ответе `GET sorting-objects` с этой ячейкой;
- «снять с ячейки» (пустая цель) → 200, объект вернулся в «Без ячеек»;
- вложить короб в палету через `to_id` → 200, короб внутри палеты, а не в ячейке;
- товар положили в ячейку → виден в содержимом ячейки с количеством.

## Ворота перед сдачей

```
cd backend && ruff check . && mypy .
python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q
python3 ../scripts/ci/back_guard.py
```

Закоммить осмысленным сообщением по-русски. Итог запиши в
`tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md`: что изменил, какие
поля добавил в ответ (это нужно владельцу для фронта), какие тесты прошли.
// Раскладка объектами: товар лежит в коробе, короб на палете, палета в ячейке.
//
// Ключевое отличие от «раскладки по товарам»: место товара нигде не хранится —
// оно вычисляется по цепочке держателей. Поэтому нельзя рассинхронизировать
// «что в коробе» и «где короб»: это одно и то же знание, записанное один раз.

export type ObjKind = 'pallet' | 'box' | 'cargo_place'

/** Кто держит: объект, ячейка или никто (собрано, но ещё не поставлено). */
export type Holder = string | null

export const KIND_TITLE: Record<ObjKind, string> = {
  pallet: 'Палета',
  box: 'Короб',
  cargo_place: 'Грузоместо',
}

export type WarehouseObject = {
  id: string
  kind: ObjKind
  code: string
  barcode: string
  holder: Holder
}

export type GoodsLine = {
  id: string
  productId: string
  qty: number
  holder: Holder
}

export type AlreadyAt = { cellId: string; code: string; qty: number }

export type Product = {
  id: string
  name: string
  sku: string
  seller: string
  barcode: string
  photo: string
  /** Размер с карточки маркетплейса. Есть не у всякого товара. */
  size: string | null
  /** Где этот же товар уже лежит на складе — подсказка «положи туда же». */
  alreadyAt: AlreadyAt[]
}

export type Cell = { id: string; code: string; barcode: string }

export const objRef = (id: string) => `obj:${id}`
export const cellRef = (id: string) => `cell:${id}`
export const isCellRef = (holder: Holder) => Boolean(holder && holder.startsWith('cell:'))
export const refId = (holder: string) => holder.slice(holder.indexOf(':') + 1)

function photo(letters: string): string {
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="240" height="240">
    <rect width="240" height="240" fill="#eef1f6"/>
    <rect x="48" y="60" width="144" height="120" rx="10" fill="#c7cedb"/>
    <text x="120" y="141" font-family="Inter, sans-serif" font-size="46" font-weight="700"
      fill="#5a6478" text-anchor="middle">${letters}</text>
  </svg>`
  return `data:image/svg+xml;utf8,${encodeURIComponent(svg.replace(/\s+/g, ' '))}`
}

export const PRODUCTS: Product[] = [
  { id: 'p-tshirt', name: 'Футболка хлопок белая, M', sku: 'TS-WHT-M', seller: 'ИП Горячкина', barcode: '4680123456789', photo: photo('ФБ'), size: 'M', alreadyAt: [{ cellId: 'c-a11', code: 'А 1.1', qty: 24 }] },
  { id: 'p-hoodie', name: 'Худи оверсайз серое, L', sku: 'HD-GRY-L', seller: 'ИП Горячкина', barcode: '4680123456796', photo: photo('ХД'), size: 'L', alreadyAt: [] },
  { id: 'p-sneakers', name: 'Кроссовки беговые, 42', sku: 'SN-RUN-42', seller: 'ООО Ситипак', barcode: '4600987654321', photo: photo('КР'), size: '42', alreadyAt: [{ cellId: 'c-b11', code: 'Б 1.1', qty: 36 }] },
  { id: 'p-socks', name: 'Носки спортивные, 3 пары', sku: 'SK-SPT-3', seller: 'ООО Ситипак', barcode: '4600987654338', photo: photo('НС'), size: null, alreadyAt: [{ cellId: 'c-a12', code: 'А 1.2', qty: 60 }] },
  { id: 'p-mug', name: 'Термокружка 450 мл', sku: 'MG-450', seller: 'ИП Ларин', barcode: '4601122334455', photo: photo('ТК'), size: null, alreadyAt: [] },
  { id: 'p-belt', name: 'Ремень кожаный, 110 см', sku: 'BL-110', seller: 'ИП Ларин', barcode: '4601122334462', photo: photo('РМ'), size: '110', alreadyAt: [{ cellId: 'c-a12', code: 'А 1.2', qty: 8 }] },
]

export const CELLS: Cell[] = [
  { id: 'c-a11', code: 'А 1.1', barcode: '2000000000114' },
  { id: 'c-a12', code: 'А 1.2', barcode: '2000000000121' },
  { id: 'c-a13', code: 'А 1.3', barcode: '2000000000138' },
  { id: 'c-a21', code: 'А 2.1', barcode: '2000000000213' },
  { id: 'c-b11', code: 'Б 1.1', barcode: '2000000000411' },
  { id: 'c-b12', code: 'Б 1.2', barcode: '2000000000428' },
]

// Номера наши собственные и сквозные: короб, заведённый на приёмке, дальше
// фигурирует под этим же номером везде — в раскладке, на карте склада, в
// упаковке, в отгрузке. Приёмка идёт от селлера на наш склад, маркетплейс в ней
// не участвует, и назначать коробу чужой формат номера незачем.
//
// Отдельно: по FBS идентификатор грузоместа выдаёт сам Wildberries ответом на
// POST .../trbx, и на стикер для WB идёт именно он — но это другая история и
// другой документ (см. docs/research/20260827-wb-box-barcodes.md).
//
// Что пришло с шага упаковки: палета с двумя коробами уже собрана, один короб и
// одно грузоместо стоят отдельно, часть товара приехала россыпью.
export const INITIAL_OBJECTS: WarehouseObject[] = [
  { id: 'plt-131', kind: 'pallet', code: 'П-000131', barcode: '2100000001311', holder: null },
  { id: 'box-472', kind: 'box', code: 'КР-000472', barcode: '2200000004723', holder: objRef('plt-131') },
  { id: 'box-473', kind: 'box', code: 'КР-000473', barcode: '2200000004730', holder: objRef('plt-131') },
  { id: 'box-480', kind: 'box', code: 'КР-000480', barcode: '2200000004807', holder: null },
  { id: 'cp-318', kind: 'cargo_place', code: 'ГМ-000318', barcode: '2300000003185', holder: null },
]

export const INITIAL_LINES: GoodsLine[] = [
  { id: 'l-1', productId: 'p-tshirt', qty: 24, holder: objRef('box-472') },
  { id: 'l-2', productId: 'p-hoodie', qty: 16, holder: objRef('box-472') },
  { id: 'l-3', productId: 'p-sneakers', qty: 30, holder: objRef('box-473') },
  { id: 'l-4', productId: 'p-socks', qty: 90, holder: objRef('box-480') },
  { id: 'l-5', productId: 'p-belt', qty: 14, holder: objRef('cp-318') },
  { id: 'l-6', productId: 'p-mug', qty: 12, holder: null },
  { id: 'l-7', productId: 'p-tshirt', qty: 18, holder: null },
  { id: 'l-8', productId: 'p-socks', qty: 40, holder: null },
]

/**
 * Товар по идентификатору. Список передаётся снаружи, а не берётся из заглушки:
 * иначе экран навсегда привязан к выдуманным данным и подключить его к серверу
 * можно только переписав.
 */
export function productById(products: Product[], id: string): Product {
  return (
    products.find((one) => one.id === id) ?? {
      // Товар, которого нет в списке, на экране всё равно называем: пустая
      // строка вместо названия читается как поломка, а это просто рассинхрон.
      id,
      name: 'Товар не найден',
      sku: '—',
      seller: '—',
      barcode: '',
      photo: '',
      size: null,
      alreadyAt: [],
    }
  )
}

/** Сколько штук внутри объекта — вместе со всем вложенным. */
export function objectQty(
  objectId: string,
  objects: WarehouseObject[],
  lines: GoodsLine[],
): number {
  const own = lines
    .filter((line) => line.holder === objRef(objectId))
    .reduce((sum, line) => sum + line.qty, 0)
  const inner = objects
    .filter((one) => one.holder === objRef(objectId))
    .reduce((sum, one) => sum + objectQty(one.id, objects, lines), 0)
  return own + inner
}

/** Сколько штук стоит на ячейке — считается по составу, а не хранится. */
export function cellQty(cellId: string, objects: WarehouseObject[], lines: GoodsLine[]): number {
  const loose = lines
    .filter((line) => line.holder === cellRef(cellId))
    .reduce((sum, line) => sum + line.qty, 0)
  const inside = objects
    .filter((one) => one.holder === cellRef(cellId))
    .reduce((sum, one) => sum + objectQty(one.id, objects, lines), 0)
  return loose + inside
}

/** Цепочка держателей до ячейки: где физически лежит эта строка товара. */
export function whereIs(
  holder: Holder,
  objects: WarehouseObject[],
  cells: Cell[],
): { cell: Cell | null; path: string[] } {
  const path: string[] = []
  let cursor = holder
  while (cursor) {
    if (isCellRef(cursor)) {
      const cell = cells.find((one) => one.id === refId(cursor!)) ?? null
      return { cell, path }
    }
    const object = objects.find((one) => one.id === refId(cursor!))
    if (!object) return { cell: null, path }
    path.unshift(`${KIND_TITLE[object.kind]} ${object.code}`)
    cursor = object.holder
  }
  return { cell: null, path }
}
import {
  KIND_TITLE,
  cellRef,
  whereIs,
  isCellRef,
  objRef,
  objectQty,
  productById,
  refId,
  type Cell,
  type GoodsLine,
  type Holder,
  type ObjKind,
  type Product,
  type WarehouseObject,
} from './objectsStub'

// Плоские строки для дерева объектов. Одна таблица с одной шапкой: вложенность
// видна отступом, а не второй таблицей внутри строки.

export type ObjectRow =
  | {
      key: string
      depth: number
      kind: 'object'
      object: WarehouseObject
      qty: number
      expandable: boolean
      expanded: boolean
      empty: boolean
      /** Сколько строк внутри — для подписи под названием. */
      inside: number
    }
  | {
      key: string
      depth: number
      kind: 'goods'
      line: GoodsLine
      name: string
      sku: string
      seller: string
      barcode: string
      photo: string
      size: string | null
      qty: number
      alreadyAt: Array<{ cellId: string; code: string; qty: number }>
      inside: number
    }

export type Carried =
  | { kind: 'object'; object: WarehouseObject }
  | { kind: 'goods'; line: GoodsLine }

/**
 * Куда можно положить то, что несут.
 *
 * Правила складские: товар ложится в любой объект и на любую ячейку; короб и
 * грузоместо — на палету или на ячейку; палета — только на ячейку. Внутрь себя
 * ничего не кладётся, и туда, где вещь и так лежит, — тоже: такой перенос
 * ничего не менял бы, а в журнале появилась бы работа, которой не было.
 */
export function canPut(
  carried: Carried,
  target: Holder,
  objects: WarehouseObject[],
): boolean {
  if (!target) return true // «вынуть наружу» разрешено всему
  if (carried.kind === 'goods') return carried.line.holder !== target
  const moving = carried.object
  if (moving.holder === target) return false
  if (target === objRef(moving.id)) return false
  if (isCellRef(target)) return true
  const byId = new Map(objects.map((one) => [one.id, one]))
  // Цикл: нельзя положить палету в короб, который стоит на ней самой.
  let cursor: Holder = target
  while (cursor && !isCellRef(cursor)) {
    const id = refId(cursor)
    if (id === moving.id) return false
    cursor = byId.get(id)?.holder ?? null
  }
  const host = byId.get(refId(target))
  if (!host) return false
  if (moving.kind === 'pallet') return false
  return host.kind === 'pallet'
}

/** Что можно создать внутри этого держателя. */
export function creatableIn(target: Holder, objects: WarehouseObject[]): ObjKind[] {
  if (!target) return ['pallet', 'box', 'cargo_place']
  if (isCellRef(target)) return ['pallet', 'box', 'cargo_place']
  const host = objects.find((one) => one.id === refId(target))
  if (!host) return []
  return host.kind === 'pallet' ? ['box', 'cargo_place'] : []
}

export function objectTitle(object: WarehouseObject): string {
  return `${KIND_TITLE[object.kind]} ${object.code}`
}

function walk(
  holder: Holder,
  depth: number,
  objects: WarehouseObject[],
  lines: GoodsLine[],
  products: Product[],
  collapsed: Set<string>,
  out: ObjectRow[],
) {
  for (const object of objects.filter((one) => one.holder === holder)) {
    const qty = objectQty(object.id, objects, lines)
    const children =
      objects.filter((one) => one.holder === objRef(object.id)).length +
      lines.filter((one) => one.holder === objRef(object.id)).length
    const expanded = !collapsed.has(object.id)
    out.push({
      key: `o-${object.id}`,
      depth,
      kind: 'object',
      object,
      qty,
      expandable: children > 0,
      expanded: children > 0 && expanded,
      empty: children === 0,
      inside: children,
    })
    if (children > 0 && expanded) {
      walk(objRef(object.id), depth + 1, objects, lines, products, collapsed, out)
    }
  }
  for (const line of lines.filter((one) => one.holder === holder)) {
import { useCallback, useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Box, Stack, ToggleButton, ToggleButtonGroup } from '@mui/material'
import { apiUrl } from '../../../api'
import { readApiErrorMessage } from '../../../utils/readApiErrorMessage'
import { renderBarcodeDataUrl } from '../../../utils/renderBarcodeDataUrl'
import { printBarcodeLabel } from '../../../utils/printBarcodeLabel'
import type { LabelSize } from '../../../utils/labelSize'
import { EmptyState, ErrorNotice } from '../../../ui-kit'
import { SortingObjectsScreen } from './SortingObjectsScreen'
import type { Cell, GoodsLine, ObjKind, Product, WarehouseObject } from './objectsStub'

// Раскладка по объектам, подключённая к серверу.
//
// Экран приняли по макету, и он не знает про сеть. Здесь только загрузка склада
// и отправка того, что оператор поставил на полку.

function headers(token: string): Record<string, string> {
  return { Authorization: `Bearer ${token}` }
}

type ApiSorting = {
  objects: WarehouseObject[]
  lines: GoodsLine[]
  products: Product[]
  cells: Cell[]
}

type Props = {
  token: string
  warehouses: Array<{ id: string; name: string }>
  /**
   * Внутри документа сортировки склад уже известен, а заголовок документа стоит
   * выше — переключатель складов и собственная шапка здесь только мешают.
   */
  embedded?: boolean
}

export function FfSortingObjectsPage({ token, warehouses, embedded }: Props) {
  const navigate = useNavigate()
  // Склад выбирается руками, как на карте: раскладка идёт на конкретном складе,
  // и молча показывать первый попавшийся значит врать оператору.
  const [warehouseId, setWarehouseId] = useState<string | null>(warehouses[0]?.id ?? null)

  // Список складов грузится в App отдельно и приезжает после первого рендера.
  // Состояние, заведённое пустым списком, так бы и осталось пустым, и экран
  // навсегда показывал бы «Нет складов» при живых складах.
  useEffect(() => {
    setWarehouseId((current) => {
      if (current && warehouses.some((one) => one.id === current)) return current
      return warehouses[0]?.id ?? null
    })
  }, [warehouses])
  const [data, setData] = useState<ApiSorting | null>(null)
  const [error, setError] = useState<string | null>(null)
  // Счётчик загрузок нужен как ключ экрана.
  //
  // Экран принимает состав склада НАЧАЛЬНЫМ состоянием: он им дальше двигает
  // сам, и перезаписывать его на каждый ответ сервера значило бы отменять
  // работу оператора под руками. Но приехавшие позже данные он бы и не увидел.
  // Смена ключа пересобирает экран заново — ровно тогда, когда пришёл новый
  // состав, и ни разу между.
  const [version, setVersion] = useState(0)

  const load = useCallback(async () => {
    if (!warehouseId) return
    setError(null)
    try {
      const res = await fetch(apiUrl(`/warehouses/${warehouseId}/sorting-objects`), {
        headers: headers(token),
      })
      if (!res.ok) throw new Error(await readApiErrorMessage(res))
      setData((await res.json()) as ApiSorting)
      setVersion((current) => current + 1)
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Не удалось загрузить раскладку')
    }
  }, [token, warehouseId])

  useEffect(() => {
    void load()
  }, [load])

  async function place(payload: {
    kind: ObjKind | 'product'
    id: string
    cellId: string | null
    toId: string | null
    qty: number
  }) {
    if (!warehouseId) return
    setError(null)
    try {
      const res = await fetch(apiUrl(`/warehouses/${warehouseId}/sorting-objects/place`), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', ...headers(token) },
        body: JSON.stringify({
          kind: payload.kind,
          id: payload.id,
          cell_id: payload.cellId,
          to_id: payload.toId,
          qty: payload.qty,
        }),
      })
      if (!res.ok) throw new Error(await readApiErrorMessage(res))
    } catch (err) {
      // Экран уже переставил строку у себя. Показываем отказ и перечитываем
      // склад: иначе на экране будет одно, а в системе другое, и оператор
      // узнает об этом на инвентаризации.
      setError(err instanceof Error ? err.message : 'Не удалось поставить объект')
      await load()
    }
  }

  if (!warehouseId) {
    return (
      <EmptyState
        title="Нет складов"
        hint="Раскладывать некуда: заведите склад в настройках."
      />
    )
  }

  const warehouseName = warehouses.find((one) => one.id === warehouseId)?.name ?? ''


2026-08-29T10:49:13.995068Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Проверка показала важную деталь контракта: живой экран уже умеет отображать содержимое ячейки через существующие верхнеуровневые `objects` и `lines`, если у корневой строки `holder="cell:<uuid>"`; вложенные строки сохраняют `holder="obj:<uuid>"`. Поэтому исправление должно наполнить именно эти массивы из `cell["children"]`, не вводя второй независимый источник содержимого. Отдельно сейчас проверяю историю схемы и тестовые фикстуры, чтобы расширить ответ ровно так, как ожидает эта ветка.
exec
/bin/zsh -lc 'git blame -L 205,235 -- backend/app/api/warehouses.py && git blame -L 1055,1205 -- backend/app/services/warehouse_map_service.py && git log --oneline --all -- backend/app/services/warehouse_map_service.py backend/app/api/warehouses.py | head -40 && git show --stat --oneline $(git log -1 --format=%H -- backend/app/services/warehouse_map_service.py)' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 123ms:
f8855609 merge: тара в местах подбора — поле sources в pick-options
ae9b407e feat(picking): expose container source paths
250fb180 fix backend review defects for stock shipments and pallets
3ede3b99 feat(sorting-objects): create warehouse containers
8382ab16 fix(inventory): скрыть код зоны сортировки в пересчёте
bab8017f merge: автор у движения, категории WB и ведомость хранения по матрице
4fdb1b13 Merge branch 'codex/address-storage-thread-20260828' into integration/vse-v-etalon-20260828
b7c194df Реализовать API карты склада и раскладки
28badb7c fix: убрать ячейки из безадресных складских операций
d59a7b5e feat(inventory): record movement actor
02052fce fix(warehouses): mark operational warehouses
302cace9 fix(warehouses): mark operational warehouses
57c2cf9c night(07-reporting): atom 3/7
3670eb27 night(04-warehouse-switch): atom 1/13
29cca9ed checkpoint: sorting cells wave1 updates
cffef35b feat(packaging): E7 packaging tasks, split stock, MP ship gate
eb73025b feat(ff): staff users with permission blocks in settings
c6f3ab61 feat(inbound): split reception and sorting with sorting-zone stock
260aa34b fix(ci): ruff — imports, line length, unused vars in tests
8c5a1c69 feat(mp-unload): seller plan flow, FF confirm, reservations, and e2e
dd0269ed Все правки разом
a4c9847d feat: seller inbound/outbound drafts, isolation, jobs, WB scaffold
a7ab9f03 feat(catalog): warehouses, locations, products API and UI with e2e journey
ae9b407e feat(picking): expose container source paths
 backend/app/api/fbs_supplies.py                    |  32 ++
 backend/app/api/marketplace_unload_requests.py     |  39 ++-
 backend/app/services/fbs_picking_service.py        |  81 +----
 .../services/marketplace_unload_pick_service.py    |  68 +---
 .../app/services/pick_option_location_service.py   | 198 +++++++++++
 backend/app/services/warehouse_map_service.py      |  93 ++++++
 backend/tests/test_fbs_pick_options.py             | 126 ++++++-
 .../test_marketplace_unload_address_storage.py     | 117 +++++++
 .../tests/test_pick_option_container_sources.py    | 370 +++++++++++++++++++++
 .../2026-08-29-pick-containers/CODE_REVIEW_RU.md   | 100 ++++++
 .../2026-08-29-pick-containers/FEATURE_CARDS_RU.md | 169 ++++++++++
 .../PRODUCT_BEFORE_DEV_RU.md                       | 224 +++++++++++++
 .../PRODUCT_BROWSER_REVIEW_RU.md                   | 109 ++++++
 13 files changed, 1600 insertions(+), 126 deletions(-)

2026-08-29T10:49:28.956977Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc "sed -n '205,250p' tasks/_zhurnal/PEREDACHA-29-08-DEN.md && sed -n '1,180p' tasks/20260828-vernut-v-maket-raskladki-to-chto-rastery/CONTRACT.md && git show b7c194df8:frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx | sed -n '1,120p' && git show b7c194df8:backend/app/services/warehouse_map_service.py | sed -n '1055,1190p'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
`cd backend && ruff check . && mypy . && pytest tests/test_pick_option_container_sources.py tests/test_fbs_pick_options.py tests/test_marketplace_unload_address_storage.py`,
влить в integration, выкатить, **открыть подбор в браузере и проверить руками**.

---

## Открытые дефекты

### 1. Каталог поехал вёрсткой — владелец прислал снимок, это последнее, что он видел

Заголовок «В Wildberries» ужимается до «В Wildberr» и переносится; действия
строки уехали к правому краю, посередине пустой провал. Появилось после того,
как я добавил колонку «В Wildberries» и значок настройки FBS в строку.
Файл — `frontend/src/screens/v2/FfProductsCatalogScreen.tsx`.
**Эталон — макет `/products.html`, открыть рядом и сравнить колонка в колонку.**
**Не чинил: владелец остановил словами «не чини а делай передачу подробную».**
Это первое, что нужно поправить, и поправить руками, глядя в браузер.

### 2. Раскладка в документе показывает чужую тару

Проверено фактом: у приёмки `0863964f-579d-454f-a566-e64ab9c8f610` один короб и
три грузоместа, а экран показал 24 складских короба `WHB-…`. Причина: раскладка
ходит в `GET /warehouses/{id}/sorting-objects` — метод по складу, про документ он
не знает. Согласованный макет `/sorting-objects.html` показывает шапку
«Приёмка №1284» и тару только этого документа. **Отдано в волну `codex-tara`.**

### 3. `__SORTING__` утекает в подбор

Доказано ответом стенда: `GET /operations/fbs-supplies/{id}/pick-options`
возвращает `"location_code":"__SORTING__"`. Карта склада ту же зону зовёт
«Без ячеек» — и это **одна и та же сущность**, я проверял по коду:
в `warehouse_map_service.py` ячейка с кодом `SORTING_LOCATION_CODE` исключается
из списка ячеек, а её содержимое кладётся в `unassigned`; на фронте
`UNASSIGNED_LABEL = 'Без ячеек'`. Дублирования сущностей нет — нужно только имя.
**Отдано в волну `codex-tara`.**

### 4. Подбор не показывает тару

Место в выдаче описано только полями про ячейку — `PickOptionLocation`,
`backend/app/services/marketplace_unload_pick_service.py:41`: ни вида тары, ни
номера. В самом экране намерение подписано — «снимаем с палет, коробов,
грузомест». **Отдано в волну `codex-tara`.**

### 5. Двойное списание остатка у Ozon

`backend/app/services/fbs_ozon_packaging_service.py:192` списывает остаток при
упаковке и создаёт расписку **без** `shipment_movement_id` — присвоения этого
# Контракт экрана «Раскладка по ячейкам»

Дата: 28.08.2026. Основание — макет `/sorting-objects.html`, принятый владельцем
28.08 («всё супер, фиксируем»). Здесь ровно то, что стоит на экране.

**Фронт написан, переписывать не нужно.** Код в
`frontend/src/screens/ff/sorting-objects/`. Не хватает единственного: чтобы
данные приходили с сервера, а действия туда уходили.

Общие требования по тенантности и надёжности — в
[контракте карты склада](../20260827-zadacha-ekran-karta-sklada-shest-zadach-/CONTRACT.md),
раздел «4а». Они действуют здесь без изменений и не повторяются.

---

## 0. Что это за экран

Это шаг **раскладки внутри документа приёмки** (селлер → ФФ), а не отдельный
раздел. Отсюда следуют две вещи, которые не надо изобретать заново.

Во-первых, **заканчивается он как документ**: «Закрыть», «Сохранить»,
«Завершить». Отдельной «записать ячейку» нет — записывается документ целиком,
как и в сегодняшней приёмке.

Во-вторых, **задание идёт по одной поставке одного селлера**, поэтому колонки
селлера на экране нет: она повторяла бы одно и то же в каждой строке.

## 1. Модель: место вычисляется, а не хранится

Порядок работы обратный привычному: сначала собирается объект — товар в короб,
короб на палету, — и только готовый объект ставится на полку.

**Место товара нигде не хранится.** Оно вычисляется по цепочке держателей: товар
в коробе, короб на палете, палета в ячейке. Поэтому «что в коробе» и «где короб»
не могут разъехаться — это одно знание, записанное один раз, а не два числа,
которые кто-то должен сверять.

У каждого объекта и каждой строки товара есть ровно один **держатель**:

- другой объект (короб на палете, товар в коробе);
- ячейка (объект стоит на полке);
- никто — собрано или принято, но ещё не поставлено.

Это та же самая ссылка на контейнер, которую владелец утвердил для карты склада
(её раздел 0). Второй раз её заводить не нужно — экраны обязаны читать одну и ту
же правду, иначе остаток на карте и остаток в раскладке разойдутся.

## 2. Что сервер обязан отдавать

`GET /inbound-intakes/{id}/sorting`

```json
{
  "warehouse_id": "uuid",
  "cells": [
    { "id": "uuid", "code": "А 1.1", "barcode": "2000000000114" }
  ],
  "objects": [
    {
      "id": "uuid",
      "kind": "pallet | box | cargo_place",
      "code": "П-000131",
      "barcode": "2100000001311",
      "holder": { "kind": "object | cell | none", "id": "uuid | null" }
    }
  ],
  "lines": [
    {
      "id": "uuid",
      "product_id": "uuid",
      "name": "Футболка хлопок белая, M",
      "size": "M",
      "barcode": "4680123456789",
      "photo_url": "https://...",
      "qty": 24,
      "holder": { "kind": "object | cell | none", "id": "uuid | null" },
      "already_at": [{ "cell_id": "uuid", "code": "А 1.1", "qty": 24 }]
    }
  ]
}
```

Правила, которые обязан соблюдать сервер:

- **`already_at` — это остаток того же товара на этом складе**, посчитанный по
  той же таблице остатков, что читает карта склада. Не отдельный расчёт.
- **`size`** — размер с карточки маркетплейса, может быть `null`. Экран
  показывает колонку «Размер», только если размер есть хотя бы у одной строки.
- **`qty` объекта сервер не отдаёт**: экран считает его сам по составу. Если
  сервер начнёт отдавать своё число, появится второй источник правды.
- Глубже трёх уровней вложенности не бывает: палета → короб/грузоместо → товар.
- `photo_url` может быть `null` — экран покажет силуэт, как везде в системе.

## 3. Что сервер обязан принимать

### 3.1 Переложить

`POST /inbound-intakes/{id}/sorting/move`

```json
{
  "subject": { "kind": "line | object", "id": "uuid" },
  "to": { "kind": "object | cell | none", "id": "uuid | null" },
  "qty": 12
}
```

- `qty` только для `kind: "line"`; у объекта его нет — объект едет целиком со
  всем содержимым.
- Ответ — свежее состояние раскладки в формате раздела 2. Экран показывает
  перемещение сразу, не дожидаясь ответа, и сверяется с ним; разошлось —
  показывает серверную правду и говорит об этом.

Что обязан проверить сервер, потому что фронт этого гарантировать не может:

- **Товар внутри короба или грузоместа нельзя переложить напрямую** — сначала
  «вынуть». Короб и грузоместо закрыты. С палеты брать напрямую можно: там вещи
  лежат открыто.
- Палета кладётся только на ячейку. Короб и грузоместо — на палету или на
  ячейку. Товар — куда угодно.
- Внутрь самого себя не кладётся ничего, в том числе через цепочку.
- Количество не больше того, что есть в строке; остаток не уходит в минус.
- Всё принадлежит тому же арендатору: и предмет, и цель, и документ.

На каждый отказ — сообщение на языке склада, без кодов и стектрейсов.

### 3.2 Снять с ячейки

Отдельного эндпоинта не нужно: это тот же `move` с `to: { kind: "none" }`.
Снимается **целиком**, вместе с содержимым — снимают полку, а не пересчитывают.

### 3.3 Создать объект

`POST /inbound-intakes/{id}/sorting/objects` → `{ "kind": "pallet | box | cargo_place" }`

Номер и штрихкод присваивает сервер и возвращает в ответе. Экран их не
придумывает.

### 3.4 Сохранить и завершить

`POST /inbound-intakes/{id}/sorting/save` — сохранить как есть, документ
остаётся в работе.

`POST /inbound-intakes/{id}/sorting/complete` — завершить распределение.
Отказывает, пока осталось непоставленное, и говорит сколько.

### 3.5 Ячейки и печать

Новых эндпоинтов не нужно. Создание ячейки — существующий
`api/warehouses.py`, той же модалкой, что на карте склада. Печать стикера
ячейки, короба, палеты и грузоместа — стандартный диалог выбора размера
этикетки, сервер для этого ничего не отдаёт.

## 4. Чего нет в данных и что придётся завести

| Что | Сейчас | Нужно |
|---|---|---|
| Палета | модели нет | заводится волной 1 карты склада |
| Товары внутри грузоместа | строк нет вообще | заводится волной 1 карты склада |
| Держатель у строки раскладки | `InboundIntakeDistributionLine` знает только ячейку | ссылка на держатель — объект или ячейка |
| Размер товара | `wb_size` у товара есть | отдать в ответе; ничего заводить не надо |
| `already_at` | остаток по товару и ячейке есть | собрать в ответ, отдельной модели не нужно |

## 5. Порядок

1. Волна 1 карты склада (палета, ссылка на контейнер, наполнение грузоместа) —
   без неё этому экрану не на чем стоять.
2. `GET .../sorting` — чтение.
3. `move`, создание объектов, `save`, `complete`.
4. Подключение экрана к API вместо заглушек — это делаю я.

## 6. Решения и границы

Открытых вопросов не осталось.

**Решено 28.08 — нумерация наша и сквозная.** Короб, палета и грузоместо
получают наш собственный номер (`КР-000472`, `П-000131`, `ГМ-000318`), и дальше
эта единица фигурирует под ним **везде**: в раскладке, на карте склада, в
упаковке, в отгрузке. Приёмка идёт от селлера на наш склад, маркетплейс в ней не
участвует, и назначать коробу чужой формат номера незачем.
fatal: path 'frontend/src/screens/ff/sorting-objects/FfSortingObjectsPage.tsx' exists on disk, but not in 'b7c194df8'

2026-08-29T10:49:47.624922Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc "sed -n '1,300p' backend/tests/conftest.py && sed -n '1,120p' backend/pyproject.toml && rg -n \"async_session|SessionLocal\" backend/tests/conftest.py backend/tests -g '*.py' | head -100" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
from __future__ import annotations

import os
from collections.abc import AsyncIterator
from pathlib import Path

import pytest_asyncio
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import AsyncSession

# Before importing app.db.session: same DATABASE_URL for routes and BackgroundTasks.
os.environ.setdefault(
    "JWT_SECRET_KEY", "test-jwt-secret-key-at-least-32-characters-long"
)
_TEST_RUN_ID = os.environ.get("PYTEST_XDIST_WORKER", str(os.getpid()))
_TEST_DB_PATH = Path(__file__).resolve().parent / f"wms_pytest_{_TEST_RUN_ID}.sqlite"
_TEST_DATA_DIR = Path(__file__).resolve().parent / f"wms_pytest_data_{_TEST_RUN_ID}"
os.environ["DATABASE_URL"] = os.environ.get(
    "WMS_TEST_DATABASE_URL",
    f"sqlite+aiosqlite:///{_TEST_DB_PATH}",
)
os.environ["WMS_DATA_DIR"] = os.environ.get("WMS_TEST_DATA_DIR", str(_TEST_DATA_DIR))

from app.db.session import SessionLocal, engine, get_db
from app.main import create_app
from app.models import Base
from app.services.fbs_stock_publish_service import drain_background_stock_publish_tasks
from app.services.fbs_stock_sync_service import drain_zero_publish_background_tasks


@pytest_asyncio.fixture
async def async_client() -> AsyncIterator[AsyncClient]:
    await drain_background_stock_publish_tasks()
    await drain_zero_publish_background_tasks()
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)

    async def override_get_db() -> AsyncIterator[AsyncSession]:
        async with SessionLocal() as session:
            yield session

    app = create_app()
    app.dependency_overrides[get_db] = override_get_db

    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        yield client

    app.dependency_overrides.clear()
    await drain_background_stock_publish_tasks()
    await drain_zero_publish_background_tasks()
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[project]
name = "wms-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "python-multipart>=0.0.9",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "sqlalchemy>=2.0",
  "greenlet>=3.0",
  "alembic>=1.13",
  "psycopg[binary]>=3.1",
  "bcrypt>=4.1",
  "pyjwt>=2.8",
  "email-validator>=2.1",
  "celery[redis]>=5.3",
  "httpx>=0.27",
  "cryptography>=42.0",
  "pymupdf>=1.24",
  "openpyxl>=3.1",
  "boto3>=1.34",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.4",
  "mypy>=1.10",
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "types-requests>=2.32.0.20240622",
  "aiosqlite>=0.20",
  # wb_emulator sticker/barcode deps for tests/test_fbs_stock_emulator_integration.py
  "qrcode[pil]>=7.4",
  "pypng>=0.20220615",
  "python-barcode>=0.15",
]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["alembic/versions", "alembic/env.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]
# Комментарии и сообщения оператору в этом проекте написаны по-русски, и линтер
# принимает кириллические буквы, похожие на латинские, за опечатку. Из-за этого
# правки начинали глушить проверку целыми файлами. Разрешаем эти буквы явно.
allowed-confusables = ["а", "б", "в", "г", "е", "и", "к", "м", "н", "о", "п", "р", "с", "у", "х", "з",
                       "А", "Б", "В", "Е", "З", "И", "К", "М", "Н", "О", "Р", "С", "Т", "У", "Х"]

[tool.ruff.lint.per-file-ignores]
# DATABASE_URL must be set before importing app.db.session (shared engine for HTTP + BackgroundTasks).
"tests/conftest.py" = ["E402"]
# Repo root on sys.path before wb_emulator imports (STOCK-100 integration test).
"tests/test_fbs_stock_emulator_integration.py" = ["E402"]
"tests/test_fbs_operator_flow_handoff.py" = ["E402"]
# Direct-run maintenance scripts add backend/ to sys.path before importing app modules.
"scripts/cleanup_fbs_stub_test_orders.py" = ["E402"]
"scripts/cleanup_fbs_wb_stub_warehouses.py" = ["E402"]

[tool.mypy]
python_version = "3.11"
explicit_package_bases = true
exclude = ["^tests/"]
strict = true
warn_return_any = true
warn_unused_ignores = true
no_implicit_optional = true
disallow_any_generics = true
plugins = []

[[tool.mypy.overrides]]
module = "celery.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "celery"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "app.tasks.*"
disable_error_code = ["untyped-decorator"]

[[tool.mypy.overrides]]
module = "app.celery_app"
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
module = "fitz"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "boto3"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "botocore.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
markers = [
    "postgresql_concurrency: overlap tests to rerun against isolated PostgreSQL via WMS_TEST_DATABASE_URL",
]
backend/tests/conftest.py:24:from app.db.session import SessionLocal, engine, get_db
backend/tests/conftest.py:41:        async with SessionLocal() as session:
backend/tests/test_notification_triggers.py:10:from app.db.session import SessionLocal
backend/tests/test_notification_triggers.py:73:    async with SessionLocal() as session:
backend/tests/test_notification_triggers.py:111:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:15:from app.db.session import SessionLocal
backend/tests/test_fbs_picking.py:127:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:428:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:449:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:576:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:621:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:689:    async with SessionLocal() as session:
backend/tests/test_fbs_picking.py:736:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:17:from app.db.session import SessionLocal
backend/tests/test_fbs_supply_from_orders.py:129:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:183:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:291:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:654:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:728:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:785:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:884:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:906:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:912:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:979:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:1077:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:1191:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:1247:    async with SessionLocal() as session:
backend/tests/test_fbs_supply_from_orders.py:1356:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:8:from app.db.session import SessionLocal
backend/tests/test_print_templates.py:78:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:100:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:221:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:241:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:309:    async with SessionLocal() as session:
backend/tests/test_print_templates.py:399:    async with SessionLocal() as session:
backend/tests/test_fbs_contract_error_envelope.py:13:from app.db.session import SessionLocal
backend/tests/test_fbs_contract_error_envelope.py:165:    async with SessionLocal() as session:
backend/tests/test_marking_verify_pair.py:11:from app.db.session import SessionLocal
backend/tests/test_marking_verify_pair.py:103:    async with SessionLocal() as session:
backend/tests/test_marking_verify_pair.py:130:    async with SessionLocal() as session:
backend/tests/test_reports_inventory.py:10:from app.db.session import SessionLocal
backend/tests/test_reports_inventory.py:51:    async with SessionLocal() as session:
backend/tests/test_reports_inventory.py:300:    async with SessionLocal() as session:
backend/tests/test_cleanup_fbs_stub_test_orders.py:12:from app.db.session import SessionLocal
backend/tests/test_cleanup_fbs_stub_test_orders.py:80:    async with SessionLocal() as session:
backend/tests/test_cleanup_fbs_stub_test_orders.py:143:    async with SessionLocal() as session:
backend/tests/test_cleanup_fbs_stub_test_orders.py:194:    async with SessionLocal() as session:
backend/tests/test_reports_overview.py:10:from app.db.session import SessionLocal
backend/tests/test_reports_overview.py:26:    async with SessionLocal() as session:
backend/tests/test_reports_overview.py:118:    async with SessionLocal() as session:
backend/tests/test_operation_facts.py:11:from app.db.session import SessionLocal
backend/tests/test_operation_facts.py:297:    async with SessionLocal() as session:
backend/tests/test_operation_facts.py:395:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:12:from app.db.session import SessionLocal
backend/tests/test_document_number_service.py:49:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:52:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:65:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:79:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:103:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:108:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:124:        async with SessionLocal() as session:
backend/tests/test_document_number_service.py:143:    async with SessionLocal() as session:
backend/tests/test_document_number_service.py:150:    async with SessionLocal() as session:
backend/tests/test_marking_code_events.py:11:from app.db.session import SessionLocal
backend/tests/test_marking_code_events.py:66:    async with SessionLocal() as session:
backend/tests/test_marking_code_events.py:119:    async with SessionLocal() as session:
backend/tests/test_marking_code_events.py:231:    async with SessionLocal() as session:
backend/tests/test_marking_code_events.py:252:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:21:from app.db.session import SessionLocal
backend/tests/test_wb_import_dimensions.py:70:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:92:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:116:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:138:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:161:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:182:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:202:    async with SessionLocal() as session:
backend/tests/test_wb_import_dimensions.py:234:    async with SessionLocal() as session:
backend/tests/test_marking_import_pools.py:11:from app.db.session import SessionLocal
backend/tests/test_marking_import_pools.py:91:    async with SessionLocal() as session:
backend/tests/test_marking_import_pools.py:208:    async with SessionLocal() as session:
backend/tests/test_marking_import_pools.py:279:    async with SessionLocal() as session:
backend/tests/fbs_operator_emulator_seed.py:15:from app.db.session import SessionLocal
backend/tests/fbs_operator_emulator_seed.py:124:    async with SessionLocal() as session:
backend/tests/fbs_operator_emulator_seed.py:154:        async with SessionLocal() as session:
backend/tests/fbs_operator_emulator_seed.py:183:            async with SessionLocal() as session:
backend/tests/fbs_operator_emulator_seed.py:237:    async with SessionLocal() as session:
backend/tests/inbound_box_intake_helpers.py:7:from app.db.session import SessionLocal
backend/tests/inbound_box_intake_helpers.py:50:    async with SessionLocal() as session:
backend/tests/test_fbs_stock_publish_on_movement.py:17:from app.db.session import SessionLocal
backend/tests/test_fbs_stock_publish_on_movement.py:85:    async with SessionLocal() as session:
backend/tests/test_fbs_stock_publish_on_movement.py:121:    async with SessionLocal() as session:
backend/tests/test_fbs_stock_publish_on_movement.py:134:    async with SessionLocal() as session:
backend/tests/test_fbs_stock_publish_on_movement.py:219:    async with SessionLocal() as session:
backend/tests/test_fbs_operation_actor.py:23:from app.db.session import SessionLocal
backend/tests/test_fbs_operation_actor.py:83:    async with SessionLocal() as session:
backend/tests/test_fbs_operation_actor.py:128:    async with SessionLocal() as session:
backend/tests/test_fbs_operation_actor.py:158:    async with SessionLocal() as session:
backend/tests/test_fbs_operation_actor.py:170:    async with SessionLocal() as session:
backend/tests/test_marking_pools_read.py:14:from app.db.session import SessionLocal
backend/tests/test_marking_pools_read.py:81:    async with SessionLocal() as session:

2026-08-29T10:50:10.862305Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc "sed -n '1,180p' backend/app/models/product.py && sed -n '1,130p' backend/app/models/seller.py && sed -n '1,130p' backend/app/models/warehouse.py && sed -n '1,120p' backend/app/models/storage_location.py && sed -n '1,125p' backend/app/models/inventory_balance.py && sed -n '1,140p' backend/app/models/tenant.py && sed -n '1,120p' backend/app/services/tenant_settings_service.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import (
    BigInteger,
    Boolean,
    DateTime,
    Float,
    ForeignKey,
    Integer,
    String,
    Text,
    UniqueConstraint,
    Uuid,
    func,
)
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.inbound_intake import InboundIntakeLine
    from app.models.inventory_balance import InventoryBalance
    from app.models.inventory_movement import InventoryMovement
    from app.models.outbound_shipment import OutboundShipmentLine
    from app.models.product_dimension_event import ProductDimensionEvent
    from app.models.seller import Seller
    from app.models.stock_direction import StockDirection
    from app.models.tenant import Tenant


class Product(Base):
    __tablename__ = "products"
    __table_args__ = (
        # Артикул с размером уникален внутри продавца, а не на весь тенант: один и тот же
        # товар (например J308-24/36) может числиться и за Loviana, и за ООО «Фэшн» — это
        # разные юрлица и разные остатки. Тенантное ограничение молча резало импорт карточек
        # WB второму продавцу: товар «уже есть», хотя есть он у чужого.
        UniqueConstraint(
            "tenant_id", "seller_id", "sku_code", name="uq_products_tenant_seller_sku"
        ),
        UniqueConstraint(
            "tenant_id",
            "seller_id",
            "wb_barcode",
            name="uq_products_tenant_wb_barcode",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    seller_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("sellers.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    sku_code: Mapped[str] = mapped_column(String(128), nullable=False)
    category: Mapped[str | None] = mapped_column(String(255), nullable=True)
    wb_nm_id: Mapped[int | None] = mapped_column(BigInteger, nullable=True, index=True)
    wb_vendor_code: Mapped[str | None] = mapped_column(String(255), nullable=True)
    wb_chrt_id: Mapped[int | None] = mapped_column(BigInteger, nullable=True, index=True)
    wb_barcode: Mapped[str | None] = mapped_column(String(64), nullable=True, index=True)
    wb_size: Mapped[str | None] = mapped_column(String(64), nullable=True)
    wb_country_of_origin: Mapped[str | None] = mapped_column(String(255), nullable=True)
    country_of_origin_iso_code: Mapped[str | None] = mapped_column(String(2), nullable=True)
    wb_shelf_life: Mapped[str | None] = mapped_column(String(255), nullable=True)
    length_mm: Mapped[int | None] = mapped_column(Integer, nullable=True)
    width_mm: Mapped[int | None] = mapped_column(Integer, nullable=True)
    height_mm: Mapped[int | None] = mapped_column(Integer, nullable=True)
    weight_g: Mapped[int | None] = mapped_column(Integer, nullable=True)
    volume_liters: Mapped[float | None] = mapped_column(Float, nullable=True)
    dimensions_source: Mapped[str | None] = mapped_column(String(32), nullable=True)
    dimensions_updated_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    dimensions_updated_by_user_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("users.id", ondelete="SET NULL"), nullable=True
    )
    packaging_instructions: Mapped[str | None] = mapped_column(Text, nullable=True)
    requires_honest_sign: Mapped[bool] = mapped_column(
        Boolean, nullable=False, default=False, server_default="false"
    )
    fbs_stock_sync_enabled: Mapped[bool] = mapped_column(
        Boolean, nullable=False, default=False, server_default="false"
    )
    fbs_stock_limit: Mapped[int | None] = mapped_column(Integer, nullable=True)
    # Правило вместо снимка: доля свободного остатка, которую отдаём в FBS.
    # Само число считается на момент публикации, поэтому не устаревает от прихода
    # новой партии. Старая `fbs_stock_limit` пока остаётся ради безопасного отката.
    fbs_percent: Mapped[int | None] = mapped_column(Integer, nullable=True)
    fbs_same_everywhere: Mapped[bool] = mapped_column(
        Boolean, nullable=False, default=True, server_default="true"
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant", back_populates="products")
    seller: Mapped[Seller | None] = relationship("Seller", back_populates="products")
    inbound_intake_lines: Mapped[list[InboundIntakeLine]] = relationship(
        "InboundIntakeLine",
        back_populates="product",
    )
    inventory_balances: Mapped[list[InventoryBalance]] = relationship(
        "InventoryBalance",
        back_populates="product",
    )
    inventory_movements: Mapped[list[InventoryMovement]] = relationship(
        "InventoryMovement",
        back_populates="product",
    )
    outbound_shipment_lines: Mapped[list[OutboundShipmentLine]] = relationship(
        "OutboundShipmentLine",
        back_populates="product",
    )
    stock_directions: Mapped[list[StockDirection]] = relationship(
        "StockDirection",
        back_populates="product",
        cascade="all, delete-orphan",
    )
    dimension_events: Mapped[list[ProductDimensionEvent]] = relationship(
        "ProductDimensionEvent",
        back_populates="product",
        cascade="all, delete-orphan",
        order_by="ProductDimensionEvent.observed_at",
    )
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import DateTime, ForeignKey, String, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.marketplace_account import MarketplaceAccount
    from app.models.product import Product
    from app.models.seller_marking_credentials import SellerMarkingCredentials
    from app.models.seller_wildberries_credentials import SellerWildberriesCredentials
    from app.models.seller_wildberries_imported_card import SellerWildberriesImportedCard
    from app.models.seller_wildberries_imported_supply import SellerWildberriesImportedSupply
    from app.models.tenant import Tenant
    from app.models.user import User


class Seller(Base):
    __tablename__ = "sellers"

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), nullable=False, index=True
    )
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant", back_populates="sellers")
    users: Mapped[list[User]] = relationship("User", back_populates="seller")
    products: Mapped[list[Product]] = relationship("Product", back_populates="seller")
    wildberries_credentials: Mapped[SellerWildberriesCredentials | None] = relationship(
        "SellerWildberriesCredentials",
        back_populates="seller",
        uselist=False,
        cascade="all, delete-orphan",
    )
    marketplace_accounts: Mapped[list[MarketplaceAccount]] = relationship(
        "MarketplaceAccount",
        back_populates="seller",
        cascade="all, delete-orphan",
    )
    marking_credentials: Mapped[SellerMarkingCredentials | None] = relationship(
        "SellerMarkingCredentials",
        back_populates="seller",
        uselist=False,
        cascade="all, delete-orphan",
    )
    wildberries_imported_cards: Mapped[list[SellerWildberriesImportedCard]] = relationship(
        "SellerWildberriesImportedCard",
        back_populates="seller",
        cascade="all, delete-orphan",
    )
    wildberries_imported_supplies: Mapped[list[SellerWildberriesImportedSupply]] = relationship(
        "SellerWildberriesImportedSupply",
        back_populates="seller",
        cascade="all, delete-orphan",
    )
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import Boolean, DateTime, ForeignKey, String, UniqueConstraint, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.inbound_intake import InboundIntakeRequest
    from app.models.outbound_shipment import OutboundShipmentRequest
    from app.models.storage_location import StorageLocation
    from app.models.tenant import Tenant
    from app.models.warehouse_storage_rack import WarehouseStorageRack


class Warehouse(Base):
    __tablename__ = "warehouses"
    __table_args__ = (UniqueConstraint("tenant_id", "code", name="uq_warehouses_tenant_code"),)

    id: Mapped[uuid.UUID] = mapped_column(Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    code: Mapped[str] = mapped_column(String(64), nullable=False)
    is_operational: Mapped[bool] = mapped_column(
        Boolean, nullable=False, default=True, server_default="true"
    )
    barcode: Mapped[str] = mapped_column(
        String(64),
        nullable=False,
        unique=True,
        index=True,
        default=lambda: f"WH-{uuid.uuid4().hex[:12].upper()}",
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant", back_populates="warehouses")
    locations: Mapped[list[StorageLocation]] = relationship(
        "StorageLocation", back_populates="warehouse"
    )
    storage_racks: Mapped[list[WarehouseStorageRack]] = relationship(
        "WarehouseStorageRack", back_populates="warehouse"
    )
    inbound_intake_requests: Mapped[list[InboundIntakeRequest]] = relationship(
        "InboundIntakeRequest",
        back_populates="warehouse",
    )
    outbound_shipment_requests: Mapped[list[OutboundShipmentRequest]] = relationship(
        "OutboundShipmentRequest",
        back_populates="warehouse",
    )
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import DateTime, ForeignKey, Integer, String, UniqueConstraint, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.inbound_intake import InboundIntakeLine
    from app.models.inventory_balance import InventoryBalance
    from app.models.inventory_movement import InventoryMovement
    from app.models.outbound_shipment import OutboundShipmentLine
    from app.models.tenant import Tenant
    from app.models.warehouse import Warehouse
    from app.models.warehouse_storage_rack import WarehouseStorageRack


class StorageLocation(Base):
    __tablename__ = "storage_locations"
    __table_args__ = (
        UniqueConstraint(
            "warehouse_id", "code", name="uq_storage_locations_wh_code"
        ),
        UniqueConstraint(
            "tenant_id", "barcode", name="uq_storage_locations_tenant_barcode"
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    warehouse_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("warehouses.id", ondelete="CASCADE"), index=True
    )
    code: Mapped[str] = mapped_column(String(64), nullable=False)
    rack_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("warehouse_storage_racks.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    side: Mapped[int | None] = mapped_column(Integer, nullable=True)
    position: Mapped[int | None] = mapped_column(Integer, nullable=True)
    barcode: Mapped[str] = mapped_column(String(64), nullable=False, index=True)
    deleted_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant", back_populates="locations")
    warehouse: Mapped[Warehouse] = relationship(
        "Warehouse", back_populates="locations"
    )
    rack: Mapped[WarehouseStorageRack | None] = relationship(
        "WarehouseStorageRack", back_populates="locations"
    )
    inventory_balances: Mapped[list[InventoryBalance]] = relationship(
        "InventoryBalance",
        back_populates="storage_location",
    )
    inventory_movements: Mapped[list[InventoryMovement]] = relationship(
        "InventoryMovement",
        back_populates="storage_location",
    )
    inbound_intake_lines: Mapped[list[InboundIntakeLine]] = relationship(
        "InboundIntakeLine",
        back_populates="storage_location",
    )
    outbound_shipment_lines: Mapped[list[OutboundShipmentLine]] = relationship(
        "OutboundShipmentLine",
        back_populates="storage_location",
    )
from __future__ import annotations

import uuid
from datetime import datetime
from typing import TYPE_CHECKING

from sqlalchemy import (
    CheckConstraint,
    DateTime,
    ForeignKey,
    Index,
    Integer,
    String,
    Uuid,
    func,
    text,
)
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.product import Product
    from app.models.storage_location import StorageLocation
    from app.models.tenant import Tenant


class InventoryBalance(Base):
    """Остаток SKU в ячейке. Меняется только проведёнными операциями."""

    __tablename__ = "inventory_balances"
    __table_args__ = (
        CheckConstraint(
            "(container_kind IS NULL AND container_id IS NULL) OR "
            "(container_kind IS NOT NULL AND container_id IS NOT NULL)",
            name="ck_inventory_balance_container_pair",
        ),
        CheckConstraint(
            "container_kind IS NULL OR container_kind IN ('pallet', 'box', 'cargo_place')",
            name="ck_inventory_balance_container_kind",
        ),
        # Запрета на отрицательный остаток здесь НЕТ, и это решение владельца.
        #
        # Минус на полке бессмыслен, и защита от него нужна — но проверка в базе
        # не умеет спрашивать, кто её вызвал, а ровно одному пути минус нужен:
        # подтверждённой доставке FBS. Маркетплейс сказал, что товар уехал —
        # значит на складе его нет, что бы ни думал учёт. Отказать значит
        # подвесить поставку навсегда и оставить призрачный остаток.
        #
        # Поэтому защита переехала в сервис: `record_movement_and_adjust_balance`
        # по умолчанию в минус не пускает, и делает это условием внутри самого
        # запроса — то есть остаётся устойчивой к одновременным списаниям.
        # Обойти её можно только явным `allow_negative=True`, и такой вызов в
        # коде один.
        Index(
            "uq_inventory_balance_loc_product_container",
            "storage_location_id",
            "product_id",
            text("coalesce(container_id, '00000000-0000-0000-0000-000000000000')"),
            unique=True,
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    storage_location_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("storage_locations.id", ondelete="CASCADE"),
        index=True,
    )
    product_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("products.id", ondelete="CASCADE"),
        index=True,
    )
    container_kind: Mapped[str | None] = mapped_column(
        String(32), nullable=True, index=True
    )
    container_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True), nullable=True, index=True
    )
    quantity: Mapped[int] = mapped_column(Integer, nullable=False)
    quantity_unpacked: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    quantity_packed: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    tenant: Mapped[Tenant] = relationship("Tenant", back_populates="inventory_balances")
    storage_location: Mapped[StorageLocation] = relationship(
        "StorageLocation", back_populates="inventory_balances"
    )
    product: Mapped[Product] = relationship(
        "Product", back_populates="inventory_balances"
    )
from __future__ import annotations

import uuid
from datetime import date, datetime, time
from typing import TYPE_CHECKING

from sqlalchemy import Boolean, Date, DateTime, String, Time, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.background_job import BackgroundJob
    from app.models.inbound_intake import InboundIntakeRequest
    from app.models.inventory_balance import InventoryBalance
    from app.models.inventory_movement import InventoryMovement
    from app.models.notification import Notification
    from app.models.outbound_shipment import OutboundShipmentRequest
    from app.models.product import Product
    from app.models.seller import Seller
    from app.models.storage_location import StorageLocation
    from app.models.user import User
    from app.models.warehouse import Warehouse


class Tenant(Base):
    __tablename__ = "tenants"

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    slug: Mapped[str] = mapped_column(String(64), unique=True, nullable=False, index=True)
    address_storage_enabled: Mapped[bool] = mapped_column(
        Boolean,
        nullable=False,
        server_default="true",
    )
    separate_marking_print_enabled: Mapped[bool] = mapped_column(
        Boolean,
        nullable=False,
        server_default="false",
        default=False,
    )
    fbs_shipment_cutoff_time: Mapped[time | None] = mapped_column(
        Time(timezone=False),
        nullable=True,
    )
    # None deliberately keeps billing off for existing tenants.  It is set
    # when the tenant explicitly creates its first tariff version, rather
    # than backfilling already-finalised warehouse documents.
    billing_enabled_from: Mapped[date | None] = mapped_column(Date, nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )

    users: Mapped[list[User]] = relationship("User", back_populates="tenant")
    sellers: Mapped[list[Seller]] = relationship("Seller", back_populates="tenant")
    warehouses: Mapped[list[Warehouse]] = relationship(
        "Warehouse", back_populates="tenant"
    )
    locations: Mapped[list[StorageLocation]] = relationship(
        "StorageLocation", back_populates="tenant"
    )
    products: Mapped[list[Product]] = relationship("Product", back_populates="tenant")
    inbound_intake_requests: Mapped[list[InboundIntakeRequest]] = relationship(
        "InboundIntakeRequest",
        back_populates="tenant",
    )
    inventory_balances: Mapped[list[InventoryBalance]] = relationship(
        "InventoryBalance",
        back_populates="tenant",
    )
    inventory_movements: Mapped[list[InventoryMovement]] = relationship(
        "InventoryMovement",
        back_populates="tenant",
    )
    outbound_shipment_requests: Mapped[list[OutboundShipmentRequest]] = relationship(
        "OutboundShipmentRequest",
        back_populates="tenant",
    )
    background_jobs: Mapped[list[BackgroundJob]] = relationship(
        "BackgroundJob",
        back_populates="tenant",
    )
    notifications: Mapped[list[Notification]] = relationship(
        "Notification",
        back_populates="tenant",
    )
from __future__ import annotations

import uuid
from datetime import time
from typing import TypedDict

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.tenant import Tenant
from app.services import inventory_service as inv_svc


class TenantSettingsData(TypedDict):
    address_storage_enabled: bool
    separate_marking_print_enabled: bool
    fbs_shipment_cutoff_time: str | None


async def get_tenant(session: AsyncSession, tenant_id: uuid.UUID) -> Tenant:
    tenant = await session.get(Tenant, tenant_id)
    if tenant is None:
        raise LookupError("tenant_not_found")
    return tenant


async def is_address_storage_enabled(
    session: AsyncSession,
    tenant_id: uuid.UUID,
) -> bool:
    """Включено ли адресное хранение у арендатора.

    Читаем одну колонку запросом, а не через объект арендатора. После записи
    сессия сбрасывает загруженные объекты, и обращение к полю у такого объекта
    пытается втихую сходить в базу — в асинхронном коде это падает жалобой на
    контекст, причём далеко от места настоящей причины. Этот флаг спрашивают
    почти из каждого ответа API, поэтому цена ошибки высокая, а запрос дешёвый.
    """
    value = await session.scalar(
        select(Tenant.address_storage_enabled).where(Tenant.id == tenant_id)
    )
    return bool(value)


async def get_tenant_settings(
    session: AsyncSession,
    tenant_id: uuid.UUID,
) -> TenantSettingsData:
    tenant = await get_tenant(session, tenant_id)
    return {
        "address_storage_enabled": tenant.address_storage_enabled,
        "separate_marking_print_enabled": tenant.separate_marking_print_enabled,
        "fbs_shipment_cutoff_time": (
            tenant.fbs_shipment_cutoff_time.strftime("%H:%M")
            if tenant.fbs_shipment_cutoff_time is not None
            else None
        ),
    }


async def update_tenant_settings(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    *,
    address_storage_enabled: bool | None = None,
    separate_marking_print_enabled: bool | None = None,
    fbs_shipment_cutoff_time: time | None = None,
    set_fbs_shipment_cutoff_time: bool = False,
    actor_user_id: uuid.UUID | None,
) -> TenantSettingsData:
    tenant = await get_tenant(session, tenant_id)
    if address_storage_enabled is not None:
        if tenant.address_storage_enabled and not address_storage_enabled:
            await inv_svc.migrate_all_address_balances_to_sorting(
                session,
                tenant_id,
                actor_user_id=actor_user_id,
            )
        tenant.address_storage_enabled = address_storage_enabled
    if separate_marking_print_enabled is not None:
        tenant.separate_marking_print_enabled = separate_marking_print_enabled
    if set_fbs_shipment_cutoff_time:
        tenant.fbs_shipment_cutoff_time = fbs_shipment_cutoff_time
    await session.commit()
    await session.refresh(tenant)
    return {
        "address_storage_enabled": tenant.address_storage_enabled,
        "separate_marking_print_enabled": tenant.separate_marking_print_enabled,
        "fbs_shipment_cutoff_time": (
            tenant.fbs_shipment_cutoff_time.strftime("%H:%M")
            if tenant.fbs_shipment_cutoff_time is not None
            else None
        ),
    }

2026-08-29T10:50:22.064250Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend/tests/test_sorting_objects_cells.py
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

exec
/bin/zsh -lc 'git status --short && .venv/bin/python -m pytest tests/test_sorting_objects_cells.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 exited 127 in 0ms:
?? ../ZADANIE.md
?? tests/test_sorting_objects_cells.py
zsh:1: no such file or directory: .venv/bin/python

diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

codex
Новый адресный тестовый файл добавлен. Первый запуск не дошёл до тестов: в этом worktree нет `backend/.venv`, поэтому локальный путь из общих правил отсутствует. Сейчас нахожу фактически используемый интерпретатор проекта и продолжаю тем же точечным набором, не переходя к полному `pytest`.
exec
/bin/zsh -lc "command -v python && python --version && command -v python3 && python3 --version && ls -la backend | sed -n '1,80p' && ls -la /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python 2>/dev/null || true && /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python --version 2>/dev/null || true" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
Python 3.14.3

diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

exec
/bin/zsh -lc 'python -m pytest tests/test_sorting_objects_cells.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 exited 127 in 0ms:
zsh:1: command not found: python

exec
/bin/zsh -lc 'ls -la /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python /Users/deniscivkunov/Projects/WMS/.venv/bin/python 2>/dev/null' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 exited 1 in 0ms:
lrwxr-xr-x@ 1 deniscivkunov  staff  65 Aug  8 08:56 /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -> /Library/Frameworks/Python.framework/Versions/3.14/bin/python3.14

exec
/bin/zsh -lc "find /Users/deniscivkunov/Projects/WMS -maxdepth 4 -path '*/bin/python' -type l -print" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 195ms:
/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python

diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -m pytest tests/test_sorting_objects_cells.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 exited 1 in 7565ms:
FFFF                                                                     [100%]
=================================== FAILURES ===================================
_______________ test_object_in_cell_is_returned_with_cell_holder _______________

async_client = <httpx.AsyncClient object at 0x10d4a42f0>

    @pytest.mark.asyncio
    async def test_object_in_cell_is_returned_with_cell_holder(
        async_client: AsyncClient,
    ) -> None:
        headers, tenant = await _register(async_client, "object-in-cell")
        warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
        box = await _create_object(async_client, headers, warehouse.id, "box")
    
        placed = await async_client.post(
            f"/warehouses/{warehouse.id}/sorting-objects/place",
            headers=headers,
            json={
                "kind": "box",
                "id": box["id"],
                "cell_id": str(cell.id),
                "to_id": None,
                "qty": 1,
            },
        )
        assert placed.status_code == 200, placed.text
    
        response = await async_client.get(
            f"/warehouses/{warehouse.id}/sorting-objects",
            headers=headers,
        )
        assert response.status_code == 200, response.text
        data = response.json()
        assert any(row["id"] == str(cell.id) for row in data["cells"])
>       stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       StopIteration

tests/test_sorting_objects_cells.py:135: StopIteration

The above exception was the direct cause of the following exception:

self = <Coroutine test_object_in_cell_is_returned_with_cell_holder>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

../../../backend/.venv/lib/python3.14/site-packages/pytest_asyncio/plugin.py:569: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../../../backend/.venv/lib/python3.14/site-packages/pytest_asyncio/plugin.py:905: in inner
    runner.run(coro, context=context)
/Library/Frameworks/Python.framework/Versions/3.14/lib/python3.14/asyncio/runners.py:127: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <_UnixSelectorEventLoop running=False closed=False debug=False>
future = <Task finished name='Task-4' coro=<test_object_in_cell_is_returned_with_cell_holder() done, defined at /Users/denisciv...ix-raskladka/backend/tests/test_sorting_objects_cells.py:107> exception=RuntimeError('coroutine raised StopIteration')>

    def run_until_complete(self, future):
        """Run until the Future is done.
    
        If the argument is a coroutine, it is wrapped in a Task.
    
        WARNING: It would be disastrous to call run_until_complete()
        with the same coroutine twice -- it would wrap it in two
        different Tasks and that can't be good.
    
        Return the Future's result, or raise its exception.
        """
        self._check_closed()
        self._check_running()
    
        new_task = not futures.isfuture(future)
        future = tasks.ensure_future(future, loop=self)
        if new_task:
            # An exception is raised if the future didn't complete, so there
            # is no need to log the "destroy pending task" message
            future._log_destroy_pending = False
    
        future.add_done_callback(_run_until_complete_cb)
        try:
            self.run_forever()
        except:
            if new_task and future.done() and not future.cancelled():
                # The coroutine raised a BaseException. Consume the exception
                # to not log a warning, the caller doesn't have access to the
                # local task.
                future.exception()
            raise
        finally:
            future.remove_done_callback(_run_until_complete_cb)
        if not future.done():
            raise RuntimeError('Event loop stopped before Future completed.')
    
>       return future.result()
               ^^^^^^^^^^^^^^^
E       RuntimeError: coroutine raised StopIteration

/Library/Frameworks/Python.framework/Versions/3.14/lib/python3.14/asyncio/base_events.py:719: RuntimeError
________________ test_empty_target_returns_object_to_unassigned ________________

async_client = <httpx.AsyncClient object at 0x10e3f82d0>

    @pytest.mark.asyncio
    async def test_empty_target_returns_object_to_unassigned(
        async_client: AsyncClient,
    ) -> None:
        headers, tenant = await _register(async_client, "take-off-cell")
        warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
        box = await _create_object(async_client, headers, warehouse.id, "box")
        placed = await async_client.post(
            f"/warehouses/{warehouse.id}/sorting-objects/place",
            headers=headers,
            json={
                "kind": "box",
                "id": box["id"],
                "cell_id": str(cell.id),
                "to_id": None,
                "qty": 1,
            },
        )
        assert placed.status_code == 200, placed.text
    
        removed = await async_client.post(
            f"/warehouses/{warehouse.id}/sorting-objects/place",
            headers=headers,
            json={
                "kind": "box",
                "id": box["id"],
                "cell_id": None,
                "to_id": None,
                "qty": 1,
            },
        )
>       assert removed.status_code == 200, removed.text
E       AssertionError: {"detail":"cell_id_required"}
E       assert 422 == 200
E        +  where 422 = <Response [422 Unprocessable Entity]>.status_code

tests/test_sorting_objects_cells.py:170: AssertionError
_____________________ test_to_id_places_box_inside_pallet ______________________

async_client = <httpx.AsyncClient object at 0x10e3c1450>

    @pytest.mark.asyncio
    async def test_to_id_places_box_inside_pallet(
        async_client: AsyncClient,
    ) -> None:
        headers, tenant = await _register(async_client, "box-in-pallet")
        warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
        pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
        box = await _create_object(async_client, headers, warehouse.id, "box")
    
        placed = await async_client.post(
            f"/warehouses/{warehouse.id}/sorting-objects/place",
            headers=headers,
            json={
                "kind": "box",
                "id": box["id"],
                "cell_id": None,
                "to_id": pallet["id"],
                "qty": 1,
            },
        )
>       assert placed.status_code == 200, placed.text
E       AssertionError: {"detail":"cell_not_found"}
E       assert 404 == 200
E        +  where 404 = <Response [404 Not Found]>.status_code

tests/test_sorting_objects_cells.py:203: AssertionError
________________ test_product_in_cell_is_returned_with_quantity ________________

async_client = <httpx.AsyncClient object at 0x10e2988a0>

    @pytest.mark.asyncio
    async def test_product_in_cell_is_returned_with_quantity(
        async_client: AsyncClient,
    ) -> None:
        headers, tenant = await _register(async_client, "product-in-cell")
        warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
    
        placed = await async_client.post(
            f"/warehouses/{warehouse.id}/sorting-objects/place",
            headers=headers,
            json={
                "kind": "product",
                "id": str(balance.id),
                "cell_id": str(cell.id),
                "to_id": None,
                "qty": 4,
            },
        )
        assert placed.status_code == 200, placed.text
    
        response = await async_client.get(
            f"/warehouses/{warehouse.id}/sorting-objects",
            headers=headers,
        )
        assert response.status_code == 200, response.text
>       stored_line = next(
            row
            for row in response.json()["lines"]
            if row["productId"] == str(product.id)
        )
E       StopIteration

tests/test_sorting_objects_cells.py:247: StopIteration

The above exception was the direct cause of the following exception:

self = <Coroutine test_product_in_cell_is_returned_with_quantity>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

../../../backend/.venv/lib/python3.14/site-packages/pytest_asyncio/plugin.py:569: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../../../backend/.venv/lib/python3.14/site-packages/pytest_asyncio/plugin.py:905: in inner
    runner.run(coro, context=context)
/Library/Frameworks/Python.framework/Versions/3.14/lib/python3.14/asyncio/runners.py:127: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <_UnixSelectorEventLoop running=False closed=False debug=False>
future = <Task finished name='Task-46' coro=<test_product_in_cell_is_returned_with_quantity() done, defined at /Users/deniscivk...ix-raskladka/backend/tests/test_sorting_objects_cells.py:222> exception=RuntimeError('coroutine raised StopIteration')>

    def run_until_complete(self, future):
        """Run until the Future is done.
    
        If the argument is a coroutine, it is wrapped in a Task.
    
        WARNING: It would be disastrous to call run_until_complete()
        with the same coroutine twice -- it would wrap it in two
        different Tasks and that can't be good.
    
        Return the Future's result, or raise its exception.
        """
        self._check_closed()
        self._check_running()
    
        new_task = not futures.isfuture(future)
        future = tasks.ensure_future(future, loop=self)
        if new_task:
            # An exception is raised if the future didn't complete, so there
            # is no need to log the "destroy pending task" message
            future._log_destroy_pending = False
    
        future.add_done_callback(_run_until_complete_cb)
        try:
            self.run_forever()
        except:
            if new_task and future.done() and not future.cancelled():
                # The coroutine raised a BaseException. Consume the exception
                # to not log a warning, the caller doesn't have access to the
                # local task.
                future.exception()
            raise
        finally:
            future.remove_done_callback(_run_until_complete_cb)
        if not future.done():
            raise RuntimeError('Event loop stopped before Future completed.')
    
>       return future.result()
               ^^^^^^^^^^^^^^^
E       RuntimeError: coroutine raised StopIteration

/Library/Frameworks/Python.framework/Versions/3.14/lib/python3.14/asyncio/base_events.py:719: RuntimeError
=============================== warnings summary ===============================
tests/test_sorting_objects_cells.py::test_empty_target_returns_object_to_unassigned
  /Users/deniscivkunov/Projects/WMS/backend/.venv/lib/python3.14/site-packages/fastapi/routing.py:352: StarletteDeprecationWarning: 'HTTP_422_UNPROCESSABLE_ENTITY' is deprecated. Use 'HTTP_422_UNPROCESSABLE_CONTENT' instead.
    return await dependant.call(**values)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/test_sorting_objects_cells.py::test_object_in_cell_is_returned_with_cell_holder
FAILED tests/test_sorting_objects_cells.py::test_empty_target_returns_object_to_unassigned
FAILED tests/test_sorting_objects_cells.py::test_to_id_places_box_inside_pallet
FAILED tests/test_sorting_objects_cells.py::test_product_in_cell_is_returned_with_quantity
4 failed, 1 warning in 4.56s

diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

Новые четыре регрессионных теста воспроизводят заявленные дефекты: содержимое ячеек отсутствует, пустая цель даёт 422, to_id ошибочно трактуется как ячейка.
  ✓ Прочитать обязательные правила и текущую реализацию sorting-objects/place/схем
  ✓ Добавить адресные тесты для содержимого ячеек, снятия и вложения в тару
  → Исправить сервис, роут и схему ответа только в backend
  • Прогнать заданные ruff, mypy, pytest и back_guard
  • Записать итоговый brief, проверить diff и создать отдельный коммит
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend/app/api/warehouses.py
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend/app/services/warehouse_map_service.py
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

2026-08-29T10:52:44.218256Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -m pytest tests/test_sorting_objects_cells.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 8445ms:
....                                                                     [100%]
4 passed in 5.45s

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..8f2d6e795f5758d592eeec1caa0b5ba18b09bab7
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,253 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    assert any(row["id"] == str(cell.id) for row in data["cells"])
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_line = next(
+        row
+        for row in response.json()["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4

2026-08-29T10:52:57.809560Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Четыре регрессионных теста теперь зелёные. Исправление делает две вещи одновременно: старый фронтовый контракт получает полный плоский список с `holder="cell:<uuid>"`, а в `cells[]` добавлены собственные `objects` и `lines` — это расширение не удаляет прежние поля. Для `place` цель теперь выбирается однозначно: `cell_id` — ячейка, `to_id` — фактически найденный контейнер в том же тенанте и складе, обе пустые — зона «Без ячеек».
apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend/tests/test_sorting_objects_cells.py
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

2026-08-29T10:53:18.097974Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'git diff --check && git diff --stat && git status --short' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
 backend/app/api/warehouses.py                 | 11 ++--
 backend/app/services/warehouse_map_service.py | 74 ++++++++++++++++++++++++---
 2 files changed, 71 insertions(+), 14 deletions(-)
 M backend/app/api/warehouses.py
 M backend/app/services/warehouse_map_service.py
?? ZADANIE.md
?? backend/tests/test_sorting_objects_cells.py

exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/ruff check app/api/warehouses.py app/services/warehouse_map_service.py tests/test_sorting_objects_cells.py' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 3ms:
All checks passed!

exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/mypy app/api/warehouses.py app/services/warehouse_map_service.py' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 9078ms:
Success: no issues found in 2 source files

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

2026-08-29T10:53:34.078161Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc "rg --files tests | rg '"'^tests/test_warehouse_map.*'"\\.py"'$'"' && sed -n '200,245p' app/api/warehouses.py && sed -n '355,400p' app/api/warehouses.py && sed -n '1055,1245p' app/services/warehouse_map_service.py && sed -n '1,310p' tests/test_sorting_objects_cells.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 0ms:
tests/test_warehouse_map_api.py
class SortingAlreadyAtOut(BaseModel):
    cellId: str
    code: str
    qty: int


class SortingProductOut(BaseModel):
    id: str
    name: str
    sku: str
    seller: str
    barcode: str
    photo: str
    size: str | None
    alreadyAt: list[SortingAlreadyAtOut]


class SortingCellOut(BaseModel):
    id: str
    code: str
    barcode: str
    objects: list[SortingObjectOut]
    lines: list[SortingGoodsLineOut]


class SortingObjectsOut(BaseModel):
    objects: list[SortingObjectOut]
    lines: list[SortingGoodsLineOut]
    products: list[SortingProductOut]
    cells: list[SortingCellOut]


class SortingObjectCreateIn(BaseModel):
    kind: Literal["pallet", "box", "cargo_place"]


class SortingPlaceIn(BaseModel):
    kind: Literal["product", "pallet", "box", "cargo_place"]
    id: uuid.UUID
    cell_id: uuid.UUID | None = None
    to_id: uuid.UUID | None = None
    qty: int = Field(gt=0)


def _map_error(exc: WarehouseMapError) -> HTTPException:
    if exc.code in {
) -> SortingObjectOut:
    try:
        data = await warehouse_map_service.create_sorting_object(
            session,
            user.tenant_id,
            warehouse_id,
            kind=body.kind,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return SortingObjectOut.model_validate(data)


@router.post("/{warehouse_id}/sorting-objects/place", response_model=WarehouseMapMoveOut)
async def place_sorting_object_route(
    warehouse_id: uuid.UUID,
    body: SortingPlaceIn,
    user: Annotated[User, Depends(require_cells_access)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> WarehouseMapMoveOut:
    try:
        result = await warehouse_map_service.place_sorting_object(
            session,
            tenant_id=user.tenant_id,
            warehouse_id=warehouse_id,
            actor_user_id=user.id,
            kind=body.kind,
            object_id=body.id,
            cell_id=body.cell_id,
            to_id=body.to_id,
            quantity=body.qty,
        )
    except WarehouseMapError as exc:
        await session.rollback()
        raise _map_error(exc) from None
    return WarehouseMapMoveOut.model_validate(result)


@router.get("", response_model=list[WarehouseOut])
async def list_warehouses(
    user: Annotated[User, Depends(get_current_user)],
    session: Annotated[AsyncSession, Depends(get_db)],
) -> list[WarehouseOut]:
    rows = await list_wh_svc(session, user.tenant_id)
    return [
    )
    session.add(event)
    await session.commit()
    return {"id": str(pallet_id), "disbanded": True}


def _sorting_tree_rows(
    nodes: list[dict[str, Any]],
    *,
    holder: str | None,
    objects: list[dict[str, Any]],
    lines: list[dict[str, Any]],
) -> None:
    for node in nodes:
        if node["kind"] == "product":
            lines.append(
                {
                    "id": node["id"],
                    "productId": node["product_id"],
                    "qty": node["qty"],
                    "holder": holder,
                }
            )
            continue
        object_holder = f"obj:{node['id']}"
        objects.append(
            {
                "id": node["id"],
                "kind": node["kind"],
                "code": node["code"],
                "barcode": node["barcode"] or "",
                "holder": holder,
            }
        )
        _sorting_tree_rows(node["children"], holder=object_holder, objects=objects, lines=lines)


async def get_sorting_objects(
    session: AsyncSession, tenant_id: uuid.UUID, warehouse_id: uuid.UUID
) -> dict[str, Any]:
    data = await get_warehouse_map(session, tenant_id, warehouse_id)
    objects: list[dict[str, Any]] = []
    lines: list[dict[str, Any]] = []
    _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
    cells: list[dict[str, Any]] = []
    for row in data["cells"]:
        cell_objects: list[dict[str, Any]] = []
        cell_lines: list[dict[str, Any]] = []
        _sorting_tree_rows(
            row["children"],
            holder=f"cell:{row['id']}",
            objects=cell_objects,
            lines=cell_lines,
        )
        objects.extend(cell_objects)
        lines.extend(cell_lines)
        cells.append(
            {
                "id": row["id"],
                "code": row["code"],
                "barcode": row["barcode"],
                "objects": cell_objects,
                "lines": cell_lines,
            }
        )
    product_ids = {uuid.UUID(row["productId"]) for row in lines}
    products = (
        list(
            (
                await session.execute(
                    select(Product, Seller)
                    .outerjoin(Seller, Seller.id == Product.seller_id)
                    .where(Product.tenant_id == tenant_id, Product.id.in_(product_ids))
                )
            ).all()
        )
        if product_ids
        else []
    )
    location_rows = (
        list(
            (
                await session.execute(
                    select(
                        InventoryBalance.product_id,
                        StorageLocation,
                        func.sum(InventoryBalance.quantity),
                    )
                    .join(StorageLocation)
                    .where(
                        InventoryBalance.tenant_id == tenant_id,
                        InventoryBalance.product_id.in_(product_ids),
                        InventoryBalance.quantity > 0,
                        StorageLocation.warehouse_id == warehouse_id,
                        StorageLocation.code != SORTING_LOCATION_CODE,
                        StorageLocation.deleted_at.is_(None),
                    )
                    .group_by(InventoryBalance.product_id, StorageLocation.id)
                )
            ).all()
        )
        if product_ids
        else []
    )
    already: dict[uuid.UUID, dict[uuid.UUID, tuple[str, int]]] = defaultdict(dict)
    for product_id, location, quantity in location_rows:
        already[product_id][location.id] = (location.code, int(quantity or 0))

    product_nodes: dict[str, dict[str, Any]] = {}

    def collect_product_nodes(nodes: list[dict[str, Any]]) -> None:
        for node in nodes:
            if node["kind"] == "product":
                product_nodes[node["product_id"]] = node
            else:
                collect_product_nodes(node["children"])

    for cell in data["cells"]:
        collect_product_nodes(cell["children"])
    collect_product_nodes(data["unassigned"])
    product_data = [
        {
            "id": str(product.id),
            "name": product.name,
            "sku": product.sku_code,
            "seller": seller.name if seller is not None else "",
            "barcode": product.wb_barcode or "",
            "photo": product_nodes.get(str(product.id), {}).get("photo_url") or "",
            "size": product.wb_size,
            "alreadyAt": [
                {"cellId": str(cell_id), "code": code, "qty": qty}
                for cell_id, (code, qty) in already.get(product.id, {}).items()
            ],
        }
        for product, seller in products
    ]
    return {
        "objects": objects,
        "lines": lines,
        "products": product_data,
        "cells": cells,
    }


async def _sorting_destination_kind(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    container_id: uuid.UUID,
) -> ContainerKind:
    found: list[ContainerKind] = []
    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
    for candidate in candidates:
        try:
            await validate_container(
                session,
                tenant_id,
                warehouse_id,
                candidate,
                container_id,
            )
        except ValueError:
            continue
        found.append(candidate)
    if len(found) != 1:
        raise WarehouseMapError("destination_not_found")
    return found[0]


async def place_sorting_object(
    session: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    warehouse_id: uuid.UUID,
    actor_user_id: uuid.UUID,
    kind: ObjectKind,
    object_id: uuid.UUID,
    cell_id: uuid.UUID | None,
    to_id: uuid.UUID | None,
    quantity: int,
) -> dict[str, Any]:
    if cell_id is not None and to_id is not None:
        raise WarehouseMapError("destination_conflict")
    if cell_id is not None:
        destination_kind: DestinationKind = "cell"
        destination_id = cell_id
    elif to_id is not None:
        destination_kind = await _sorting_destination_kind(
            session,
            tenant_id,
            warehouse_id,
from __future__ import annotations

import time
import uuid

import pytest
from httpx import AsyncClient
from sqlalchemy import select

from app.db.session import SessionLocal
from app.models.inventory_balance import InventoryBalance
from app.models.product import Product
from app.models.seller import Seller
from app.models.storage_location import StorageLocation
from app.models.tenant import Tenant
from app.models.user import User
from app.models.warehouse import Warehouse
from app.models.warehouse_box import WarehouseBox
from app.services.sorting_location_service import get_or_create_sorting_location


async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
    suffix = f"{label}-{time.time_ns()}"
    email = f"{suffix}@example.com"
    response = await client.post(
        "/auth/register",
        json={
            "organization_name": f"Склад {label}",
            "slug": suffix,
            "admin_email": email,
            "password": "password123",
        },
    )
    assert response.status_code == 200, response.text
    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
    async with SessionLocal() as session:
        user = await session.scalar(select(User).where(User.email == email))
        assert user is not None
        tenant = await session.get(Tenant, user.tenant_id)
        assert tenant is not None
        session.expunge(tenant)
    return headers, tenant


async def _seed_warehouse(
    tenant_id: uuid.UUID,
) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
    suffix = uuid.uuid4().hex[:10]
    async with SessionLocal() as session:
        warehouse = Warehouse(
            tenant_id=tenant_id,
            name="Склад раскладки",
            code=f"sorting-cells-{suffix}",
            barcode=f"WH-SORTING-CELLS-{suffix}",
        )
        session.add(warehouse)
        await session.flush()
        cell = StorageLocation(
            tenant_id=tenant_id,
            warehouse_id=warehouse.id,
            code="А-01-01",
            barcode=f"CELL-{suffix}",
        )
        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
        session.add_all([cell, seller])
        await session.flush()
        product = Product(
            tenant_id=tenant_id,
            seller_id=seller.id,
            name="Товар для раскладки",
            sku_code=f"SORT-{suffix}",
            wb_barcode=f"4600{suffix[:8]}",
        )
        session.add(product)
        await session.flush()
        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
        balance = InventoryBalance(
            tenant_id=tenant_id,
            storage_location_id=sorting.id,
            product_id=product.id,
            quantity=4,
            quantity_unpacked=4,
            quantity_packed=0,
        )
        session.add(balance)
        await session.commit()
        for row in (warehouse, cell, product, balance):
            session.expunge(row)
    return warehouse, cell, product, balance


async def _create_object(
    client: AsyncClient,
    headers: dict[str, str],
    warehouse_id: uuid.UUID,
    kind: str,
) -> dict[str, object]:
    response = await client.post(
        f"/warehouses/{warehouse_id}/sorting-objects",
        headers=headers,
        json={"kind": kind},
    )
    assert response.status_code == 201, response.text
    return response.json()


@pytest.mark.asyncio
async def test_object_in_cell_is_returned_with_cell_holder(
    async_client: AsyncClient,
) -> None:
    headers, tenant = await _register(async_client, "object-in-cell")
    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
    box = await _create_object(async_client, headers, warehouse.id, "box")

    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "box",
            "id": box["id"],
            "cell_id": str(cell.id),
            "to_id": None,
            "qty": 1,
        },
    )
    assert placed.status_code == 200, placed.text

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
    )
    assert response.status_code == 200, response.text
    data = response.json()
    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
    assert stored_box["holder"] == f"cell:{cell.id}"
    assert stored_cell["objects"] == [stored_box]
    assert stored_cell["lines"] == []


@pytest.mark.asyncio
async def test_empty_target_returns_object_to_unassigned(
    async_client: AsyncClient,
) -> None:
    headers, tenant = await _register(async_client, "take-off-cell")
    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
    box = await _create_object(async_client, headers, warehouse.id, "box")
    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "box",
            "id": box["id"],
            "cell_id": str(cell.id),
            "to_id": None,
            "qty": 1,
        },
    )
    assert placed.status_code == 200, placed.text

    removed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "box",
            "id": box["id"],
            "cell_id": None,
            "to_id": None,
            "qty": 1,
        },
    )
    assert removed.status_code == 200, removed.text

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
    )
    assert response.status_code == 200, response.text
    unassigned_box = next(
        row for row in response.json()["objects"] if row["id"] == box["id"]
    )
    assert unassigned_box["holder"] is None


@pytest.mark.asyncio
async def test_to_id_places_box_inside_pallet(
    async_client: AsyncClient,
) -> None:
    headers, tenant = await _register(async_client, "box-in-pallet")
    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
    box = await _create_object(async_client, headers, warehouse.id, "box")

    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "box",
            "id": box["id"],
            "cell_id": None,
            "to_id": pallet["id"],
            "qty": 1,
        },
    )
    assert placed.status_code == 200, placed.text

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
    )
    assert response.status_code == 200, response.text
    stored_box = next(
        row for row in response.json()["objects"] if row["id"] == box["id"]
    )
    assert stored_box["holder"] == f"obj:{pallet['id']}"
    assert not stored_box["holder"].startswith("cell:")
    async with SessionLocal() as session:
        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
        assert db_box is not None
        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
        assert db_box.storage_location_id is None


@pytest.mark.asyncio
async def test_product_in_cell_is_returned_with_quantity(
    async_client: AsyncClient,
) -> None:
    headers, tenant = await _register(async_client, "product-in-cell")
    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)

    placed = await async_client.post(
        f"/warehouses/{warehouse.id}/sorting-objects/place",
        headers=headers,
        json={
            "kind": "product",
            "id": str(balance.id),
            "cell_id": str(cell.id),
            "to_id": None,
            "qty": 4,
        },
    )
    assert placed.status_code == 200, placed.text

    response = await async_client.get(
        f"/warehouses/{warehouse.id}/sorting-objects",
        headers=headers,
    )
    assert response.status_code == 200, response.text
    data = response.json()
    stored_line = next(
        row
        for row in data["lines"]
        if row["productId"] == str(product.id)
    )
    assert stored_line["holder"] == f"cell:{cell.id}"
    assert stored_line["qty"] == 4
    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
    assert stored_cell["objects"] == []
    assert stored_cell["lines"] == [stored_line]

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

2026-08-29T10:53:41.064886Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/ruff check .' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 0ms:
All checks passed!

exec
/bin/zsh -lc 'python3 ../scripts/ci/back_guard.py' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/mypy .' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 exited 1 in 331ms:
НОВОЕ НАРУШЕНИЕ  backend/app/api/fbs_supplies.py: файл-монолит 2049 → 2296
НОВОЕ НАРУШЕНИЕ  backend/app/api/fbs_supplies.py: роут-без-теста 2 → 3
НОВОЕ НАРУШЕНИЕ  backend/app/api/inbound_intake.py: файл-монолит 2202 → 2333
НОВОЕ НАРУШЕНИЕ  backend/app/api/inbound_intake.py: роут-без-теста 19 → 21
НОВОЕ НАРУШЕНИЕ  backend/app/api/inventory_counts.py: роут-без-теста 0 → 2
НОВОЕ НАРУШЕНИЕ  backend/app/api/marketplace_unload_requests.py: файл-монолит 1744 → 1874
НОВОЕ НАРУШЕНИЕ  backend/app/api/marking_codes.py: файл-монолит 1782 → 1788
НОВОЕ НАРУШЕНИЕ  backend/app/api/products.py: файл-монолит 1271 → 1667
НОВОЕ НАРУШЕНИЕ  backend/app/services/catalog_service.py: файл-монолит 1278 → 1433
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_autopoll_service.py: файл-монолит 0 → 825
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_kiz_service.py: файл-монолит 1102 → 1129
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_marking_service.py: файл-монолит 891 → 925
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_picking_service.py: файл-монолит 893 → 1497
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_print_asset_service.py: файл-монолит 883 → 959
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_shipment_pvz_service.py: файл-монолит 1065 → 1069
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_shipment_service.py: файл-монолит 1384 → 1571
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_stock_sync_service.py: файл-монолит 868 → 900
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_supply_service.py: файл-монолит 1762 → 2021
НОВОЕ НАРУШЕНИЕ  backend/app/services/fbs_worklist_service.py: файл-монолит 897 → 916
НОВОЕ НАРУШЕНИЕ  backend/app/services/inbound_intake_service.py: файл-монолит 1902 → 2057
НОВОЕ НАРУШЕНИЕ  backend/app/services/inventory_service.py: файл-монолит 1328 → 1468
НОВОЕ НАРУШЕНИЕ  backend/app/services/marketplace_unload_collect_service.py: файл-монолит 858 → 905
НОВОЕ НАРУШЕНИЕ  backend/app/services/marketplace_unload_service.py: файл-монолит 1235 → 1243
НОВОЕ НАРУШЕНИЕ  backend/app/services/packaging_task_service.py: файл-монолит 1270 → 1271
НОВОЕ НАРУШЕНИЕ  backend/app/services/product_tz_import_service.py: файл-монолит 872 → 879
НОВОЕ НАРУШЕНИЕ  backend/app/services/storage_statement_service.py: файл-монолит 0 → 1028
НОВОЕ НАРУШЕНИЕ  backend/app/services/warehouse_map_service.py: файл-монолит 0 → 1263
НОВОЕ НАРУШЕНИЕ  backend/app/services/wb_marketplace_orders_service.py: файл-монолит 1697 → 1759
НОВОЕ НАРУШЕНИЕ  backend/app/services/wildberries_client.py: файл-монолит 1178 → 1186
НОВОЕ НАРУШЕНИЕ  backend/app/services/wildberries_fbs_client.py: файл-монолит 884 → 911

Осознанное отступление? Обнови линию флагом --update, объясни причину в PR.
стало лучше  backend/app/services/fbs_packaging_integration_service.py: файл-монолит 904 → 846
сводка по типам: голый-except: 8, роут-без-теста: 42, файл-монолит: 43101

 succeeded in 2778ms:
Success: no issues found in 390 source files

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

codex
Статические проверки прошли: `ruff` — без замечаний, `mypy` — 390 файлов без ошибок. `back_guard` запустился, но остановился на уже накопленном расхождении этой интеграционной базы с его baseline (контрольной линией): он перечислил 30 нарушений в несвязанных модулях и отдельно считает весь уже существующий `warehouse_map_service.py` новым монолитом `0 → 1263`. Baseline флагом `--update` не меняю — это затронуло бы общую политику репозитория вне задачи. Сейчас запускаю ровно заказанный набор pytest.
exec
/bin/zsh -lc '/Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/backend
 succeeded in 8800ms:
......                                                                   [100%]
6 passed in 5.78s

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

exec
/bin/zsh -lc "sed -n '1,280p' scripts/ci/back_guard.py && rg -n \"warehouse_map_service|fbs_supplies.py|baseline\" scripts/ci -g '*' && git diff -- backend/app/api/warehouses.py backend/app/services/warehouse_map_service.py backend/tests/test_sorting_objects_cells.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
#!/usr/bin/env python3
"""Сторож канона для бэкенда — храповик по образцу scripts/ui/ui_guard.py.

Прямой запрет отступлений покрасил бы CI сразу на сотнях мест, и его бы выключили.
Базовая линия фиксирует текущий уровень, CI падает только когда нарушений стало больше.

Запуск: без флага — проверить, --update — пересобрать базовую линию.
"""
from __future__ import annotations

import json
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
APP = ROOT / "backend" / "app"
API_DIR = APP / "api"
SERVICES_DIR = APP / "services"
TESTS_DIR = ROOT / "backend" / "tests"
BASELINE = ROOT / "docs" / "backend-guard-baseline.json"
MONOLITH_LINES = 800

ROUTE_RE = re.compile(r'@router\.(?:get|post|put|patch|delete)\(\s*"([^"]*)"')
PREFIX_RE = re.compile(r'prefix\s*=\s*"([^"]*)"')
DEF_RE = re.compile(r"^[ \t]*(?:async\s+)?def\s+\w+\s*\(", re.MULTILINE)
EXCEPT_RE = re.compile(r"^[ \t]*except(?:\s+Exception)?:\s*$", re.MULTILINE)
LOG_RE = re.compile(r"\blog\w*\.")
TODO_RE = re.compile(r"\b(TODO|FIXME)\b")
REF_RE = re.compile(r"tasks/|#\d+")

def _route_regex(full_path: str) -> re.Pattern[str]:
    """Ищем в тестах ПОЛНЫЙ путь роута, а не его последний сегмент.

    Сначала здесь сравнивался последний статический сегмент, и роут «/status»
    считался покрытым, потому что слово «status» встречается в любом наборе тестов.
    Проверка, которая почти никогда не срабатывает, бесполезна: подставной роут
    прошёл мимо неё на первом же испытании. Теперь параметры заменяются на любой
    сегмент, а остальные части пути обязаны совпасть по порядку.
    """
    parts = []
    for seg in full_path.strip("/").split("/"):
        parts.append(r"[^/\"']+" if seg.startswith("{") and seg.endswith("}") else re.escape(seg))
    return re.compile("/" + "/".join(parts))

def _untested_routes(text: str, tests_text: str) -> int:
    prefix = (m.group(1) if (m := PREFIX_RE.search(text)) else "").rstrip("/")
    count = 0
    for m in ROUTE_RE.finditer(text):
        full = (prefix + m.group(1)) or "/"
        if not _route_regex(full).search(tests_text):
            count += 1
    return count

def _defs_without_return_type(text: str) -> int:
    # Сигнатура часто многострочная: ищем "->" в заголовке между скобками и двоеточием.
    count = 0
    for m in DEF_RE.finditer(text):
        i, depth = m.end() - 1, 0
        while i < len(text):
            depth += text[i] == "("
            depth -= text[i] == ")"
            if text[i] == ")" and depth == 0:
                break
            i += 1
        colon = text.find(":", i)
        if colon != -1 and "->" not in text[i + 1 : colon]:
            count += 1
    return count

def _bare_except(text: str) -> int:
    lines = text.splitlines()
    starts = (text.count("\n", 0, m.start()) for m in EXCEPT_RE.finditer(text))
    return sum(not any(LOG_RE.search(x) for x in lines[i : i + 3]) for i in starts)

def _todo_without_task(text: str) -> int:
    return sum(1 for line in text.splitlines() if TODO_RE.search(line) and not REF_RE.search(line))

def scan() -> dict[str, dict[str, int]]:
    tests_text = "\n".join(p.read_text(encoding="utf-8", errors="replace") for p in TESTS_DIR.rglob("*.py"))
    found: dict[str, dict[str, int]] = {}
    for path in sorted(APP.rglob("*.py")):
        if "__pycache__" in path.parts:
            continue
        text = path.read_text(encoding="utf-8", errors="replace")
        counts: dict[str, int] = {}
        lines = text.count("\n") + 1
        if lines > MONOLITH_LINES:
            counts["файл-монолит"] = lines
        if path.is_relative_to(API_DIR) and (n := _untested_routes(text, tests_text)):
            counts["роут-без-теста"] = n
        if path.is_relative_to(SERVICES_DIR) and (n := _defs_without_return_type(text)):
            counts["функция-без-типа"] = n
        if n := _bare_except(text):
            counts["голый-except"] = n
        if n := _todo_without_task(text):
            counts["todo-без-задачи"] = n
        if counts:
            found[str(path.relative_to(ROOT))] = counts
    return found

def _summary(current: dict[str, dict[str, int]]) -> str:
    totals: dict[str, int] = {}
    for counts in current.values():
        for rule, value in counts.items():
            totals[rule] = totals.get(rule, 0) + value
    return ", ".join(f"{rule}: {value}" for rule, value in sorted(totals.items())) or "нет отступлений"

def main() -> int:
    current = scan()
    if "--update" in sys.argv or not BASELINE.exists():
        BASELINE.parent.mkdir(parents=True, exist_ok=True)
        BASELINE.write_text(json.dumps(current, ensure_ascii=False, indent=2, sort_keys=True), encoding="utf-8")
        print(f"базовая линия записана: {len(current)} файлов ({_summary(current)})")
        return 0
    baseline = json.loads(BASELINE.read_text(encoding="utf-8"))
    regressions, improved = [], []
    for rel, counts in current.items():
        for rule, value in counts.items():
            was = baseline.get(rel, {}).get(rule, 0)
            if value > was:
                regressions.append(f"{rel}: {rule} {was} → {value}")
    for rel, counts in baseline.items():
        for rule, was in counts.items():
            value = current.get(rel, {}).get(rule, 0)
            if value < was:
                improved.append(f"{rel}: {rule} {was} → {value}")
    for line in improved:
        print(f"стало лучше  {line}")
    for line in regressions:
        print(f"НОВОЕ НАРУШЕНИЕ  {line}", file=sys.stderr)
    print(f"сводка по типам: {_summary(current)}")
    if regressions:
        print("\nОсознанное отступление? Обнови линию флагом --update, объясни причину в PR.", file=sys.stderr)
        return 1
    print("новых отступлений нет")
    return 0

if __name__ == "__main__":
    raise SystemExit(main())
scripts/ci/back_guard.py:21:BASELINE = ROOT / "docs" / "backend-guard-baseline.json"
scripts/ci/back_guard.py:116:    baseline = json.loads(BASELINE.read_text(encoding="utf-8"))
scripts/ci/back_guard.py:120:            was = baseline.get(rel, {}).get(rule, 0)
scripts/ci/back_guard.py:123:    for rel, counts in baseline.items():
scripts/ci/check_sol_pipeline.py:40:        self.git("commit", "-m", "baseline")
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c3..d8615063 100644
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@ class SortingCellOut(BaseModel):
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@ async def place_sorting_object_route(
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@ async def place_sorting_object_route(
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46..56a4dcf8 100644
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@ async def get_sorting_objects(
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@ async def get_sorting_objects(
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@ async def place_sorting_object(
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@ async def place_sorting_object(
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

exec
/bin/zsh -lc "ls -la tasks/_zhurnal/codex-briefs | sed -n '1,120p' && for file in "'$(rg --files tasks/_zhurnal/codex-briefs | tail -3); do sed -n '"'1,180p' \""'$file"; done' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
ls: tasks/_zhurnal/codex-briefs: No such file or directory
rg: tasks/_zhurnal/codex-briefs: IO error for operation on tasks/_zhurnal/codex-briefs: No such file or directory (os error 2)

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

exec
/bin/zsh -lc 'mkdir -p tasks/_zhurnal/codex-briefs' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]

apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..fa0ed2993587eb1474345f714d8a598b7ec01b4d
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,52 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. `GET
+/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline относительно текущей интеграционной ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..fa0ed2993587eb1474345f714d8a598b7ec01b4d
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,52 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. `GET
+/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline относительно текущей интеграционной ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

codex
Код и адресные тесты закончены; итоговый brief записан с точным контрактом ответа и честным статусом всех ворот. Перед коммитом делаю последний независимый проход по diff: проверяю, что изменены только сервис, роут/схема, новый тест и требуемый журнал, а `ZADANIE.md` остаётся чужим незатронутым файлом.
exec
/bin/zsh -lc "git diff --check && git diff --name-only && git status --short && git diff --stat && sed -n '1,240p' tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
backend/app/api/warehouses.py
backend/app/services/warehouse_map_service.py
 M backend/app/api/warehouses.py
 M backend/app/services/warehouse_map_service.py
?? ZADANIE.md
?? backend/tests/test_sorting_objects_cells.py
?? tasks/_zhurnal/codex-briefs/
 backend/app/api/warehouses.py                 | 11 ++--
 backend/app/services/warehouse_map_service.py | 74 ++++++++++++++++++++++++---
 2 files changed, 71 insertions(+), 14 deletions(-)
# Итог: раскладка видит содержимое ячеек

Исправлен backend-контракт экрана раскладки. `GET
/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
ответа при следующем чтении.

Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.

Ответ расширен добавлением двух полей в каждый элемент `cells`:

- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.

Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
`objects` и `lines`.

В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
различаются без подмены `to_id` идентификатором ячейки:

- заполненный `cell_id` означает перемещение в адресную ячейку;
- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
  склада (`pallet`, `box` или `cargo_place`);
- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
  цель.

Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
возвращается с правильным количеством.

Проверки на текущем worktree:

- `ruff check .` — прошёл;
- `mypy .` — прошёл, проверено 390 исходных файлов;
- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
  — прошёл: 6 тестов;
- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
  устаревшей baseline относительно текущей интеграционной ветки. Сторож показал
  уже накопленные нарушения в несвязанных модулях и считает весь существующий
  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
  вне этой задачи.

Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
владельца для этого worktree.

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..fa0ed2993587eb1474345f714d8a598b7ec01b4d
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,52 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. `GET
+/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline относительно текущей интеграционной ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:23.365689Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..2336473c49f4bf683dc0baf49bd2ef5e8b36bf05
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка `GET
+/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..2336473c49f4bf683dc0baf49bd2ef5e8b36bf05
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка `GET
+/warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:33.004401Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:39.133981Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'git add backend/app/api/warehouses.py backend/app/services/warehouse_map_service.py backend/tests/test_sorting_objects_cells.py tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md && git diff --cached --check && git status --short && git diff --cached --stat && git diff --cached --name-only' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
M  backend/app/api/warehouses.py
M  backend/app/services/warehouse_map_service.py
A  backend/tests/test_sorting_objects_cells.py
A  tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
?? ZADANIE.md
 backend/app/api/warehouses.py                      |  11 +-
 backend/app/services/warehouse_map_service.py      |  74 +++++-
 backend/tests/test_sorting_objects_cells.py        | 259 +++++++++++++++++++++
 .../codex-briefs/ITOG-raskladka-yacheyki.md        |  53 +++++
 4 files changed, 383 insertions(+), 14 deletions(-)
backend/app/api/warehouses.py
backend/app/services/warehouse_map_service.py
backend/tests/test_sorting_objects_cells.py
tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:43.950012Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
Backend исправлен и проверен адресным набором: ruff, mypy и 6 pytest прошли. back_guard выполнен, но падает на несинхронизированной общей baseline ветки; baseline не менялась.
  ✓ Прочитать обязательные правила и текущую реализацию sorting-objects/place/схем
  ✓ Добавить адресные тесты для содержимого ячеек, снятия и вложения в тару
  ✓ Исправить сервис, роут и схему ответа только в backend
  ✓ Прогнать заданные ruff, mypy, pytest и back_guard
  → Записать итоговый brief, проверить diff и создать отдельный коммит
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:52.246277Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'git commit -m "Исправить содержимое ячеек в раскладке"' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
[codex/fix-raskladka-20260829 bc7ef1a2] Исправить содержимое ячеек в раскладке
 4 files changed, 383 insertions(+), 14 deletions(-)
 create mode 100644 backend/tests/test_sorting_objects_cells.py
 create mode 100644 tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:55:56.020323Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'git rev-parse HEAD && git status --short --branch && git show --stat --oneline --decorate --no-renames HEAD && git diff HEAD''^ HEAD --check' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 15ms:
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:56:02.848241Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
Итоговый brief записан, в коммит вошли ровно четыре относящихся к задаче файла. Коммит проверен по SHA; посторонний ZADANIE.md остался неотслеживаемым и не затронут.
  ✓ Прочитать обязательные правила и текущую реализацию sorting-objects/place/схем
  ✓ Добавить адресные тесты для содержимого ячеек, снятия и вложения в тару
  ✓ Исправить сервис, роут и схему ответа только в backend
  ✓ Прогнать заданные ruff, mypy, pytest и back_guard
  ✓ Записать итоговый brief, проверить diff и создать отдельный коммит
diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:56:08.271905Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc "nl -ba /Users/deniscivkunov/.codex/memories/MEMORY.md | sed -n '294,330p'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka
 succeeded in 0ms:
   294	
   295	- extensions/skysight/resources/2026-08-28T00-00-00-bFKp-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T00-00-00-bFKp-6h-memory-summary.md, updated_at=2026-08-28T00:00:00+00:00, thread_id=None, observed manual prototype acceptance and regressions) [skysight memory]
   296	- extensions/skysight/resources/2026-08-28T06-00-00-TCJs-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T06-00-00-TCJs-10min-memory-summary.md, updated_at=2026-08-28T06:00:00+00:00, thread_id=None, observed narrow layout correction request; no final acceptance) [skysight memory]
   297	- extensions/skysight/resources/2026-08-28T07-30-00-EthU-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-28T07-30-00-EthU-10min-memory-summary.md, updated_at=2026-08-28T07:30:00+00:00, thread_id=None, observed whole-object removal retest through completed mock state) [skysight memory]
   298	
   299	### keywords
   300	
   301	- `Экран «Карта склада» WMS`, `Раскладка объектами · макет`, `sorting-objects.html`, `move-lab.html`, `localhost:5179`, drag-and-drop, create cell, sticker print, `WB-TRBX`, `Сортировка`
   302	
   303	## Task 2: Define the real warehouse storage/movement contract and review movement author, outcome implementation boundary set; merge/deploy not shown
   304	
   305	### rollout_summary_files
   306	
   307	- rollout_summaries/2026-08-27T20-34-17-2SbJ-wms_process_change_and_pr_139.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=/Users/deniscivkunov/.codex/sessions/2026/08/27/rollout-2026-08-27T23-34-17-01a044ee-3574-7062-ac1b-865127ac8e6f.jsonl, updated_at=2026-08-28T12:04:58+00:00, thread_id=01a044ee-3574-7062-ac1b-865127ac8e6f, process change, movement-author audit, and PR #139 status)
   308	- extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md, updated_at=2026-08-27T21:30:00+00:00, thread_id=None, observed container-stock and tenant-safety contract) [skysight memory]
   309	- extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md, updated_at=2026-08-27T23:20:00+00:00, thread_id=None, observed `actor_user_id` correction and unrelated branch-gate noise) [skysight memory]
   310	
   311	### keywords
   312	
   313	- `InventoryMovement.actor_user_id`, `actor_user_id`, `codex/inventory-movement-author`, `CONTRACT.md`, `WAVE-1-CODEX.md`, container references, `Сортировка`, tenant ownership, row locking, `48972cef`
   314	
   315	## User preferences
   316	
   317	- when reviewing the local screen, the user asked to change only the red-marked width issue and not reinterpret the whole layout -> make narrow visual corrections without moving unrelated controls or redesigning accepted interactions. [Task 1] [skysight memory]
   318	- when testing warehouse movement, the user requires a product inside a box to be taken out before plus/drag movement; preserve container hierarchy rather than allowing direct child relocation. [Task 1] [skysight memory]
   319	- for user-initiated inventory actions, preserve the authenticated user; `None` is only for truly userless system paths such as reconciliation CLI or marketplace polling. [Task 2] [skysight memory]
   320	- for this WMS development wave, the user said “никаких гейтов и никаких правил… просто делай сам разработку” and then required an independent coding agent plus one separate breaking tester -> when current repository rules permit, do not reintroduce BA/product gates; keep tasks sequential and leave frontend untouched for backend-only work. [Task 2]
   321	
   322	## Reusable knowledge
   323	
   324	- The prototype remains a mockup, not a production screen. Earlier manual testing found broken dragging, WB-returned box-name mismatch, missing create-cell/sticker-print controls, and a left-pane/right-cell-panel geometry regression; a later local retest did confirm whole-object removal/return for a box and completed placement state. Recheck all operator flows before claiming product acceptance. [Task 1] [skysight memory]
   325	- Make live storage derive from stock rows with optional, paired container type/identifier references, not a second container-contents truth. Preserve loose stock uniqueness while allowing the same product/cell in different boxes; map “no cells” to virtual `Сортировка`. [Task 2] [skysight memory]
   326	- Server contract: authenticated tenant identity, source/destination/product ownership checks, one transaction per movement, non-negative-stock protection, and locking for concurrent deductions. Cargo places need contents across receiving, shipping, and packing, not only on the map. [Task 2] [skysight memory]
   327	- `record_movement_and_adjust_balance` in `backend/app/services/inventory_service.py` is the central movement write path; propagate actor through inbound, transfer, outbound, marketplace-unload, FBS write-off, discrepancy, and picking wrappers, but retain nullable actor for true background/CLI historical rows. The only canonical checkout is `/Users/deniscivkunov/Projects/WMS`; preserve unrelated untracked `tasks/` directories. [Task 2]
   328	- PR #139 evidence: branch `codex/wms-wave-storage-category-inbound-split-20260828`, final observed SHA `a384779c9d7e3d5492ecdc718aef96de9eef3fb3`; local report was 1218 backend passed/10 skipped and 223 Playwright passed/10 skipped with ruff/mypy/frontend build/UI guard green. It was not shown merged/deployed, and final repeated backend/e2e observation stopped while running. [Task 2]
   329	
   330	## Failures and how to do differently

diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

2026-08-29T10:56:16.139426Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Исправление реализовано и сохранено в Git.

Коммит: `bc7ef1a2ca72f8773f31c3f450f6d0d00560335a`  
Ветка: `codex/fix-raskladka-20260829`

Что изменено:

- `GET sorting-objects` теперь возвращает содержимое ячеек в общих `objects` и `lines` с `holder="cell:<uuid>"`.
- В `cells[]` добавлены поля `objects` и `lines`; прежние поля сохранены.
- Пустая цель переносит объект в «Без ячеек».
- `to_id` распознаётся как палета, короб или грузоместо в пределах текущего тенанта и склада.
- Добавлены четыре регрессионных теста.

Проверки:

- `ruff check .` — прошёл.
- `mypy .` — прошёл, 390 файлов.
- Адресный `pytest` — 6 тестов прошли.
- `back_guard` остаётся красным из-за устаревшей общей контрольной линии ветки, включая несвязанные файлы. Контрольную линию не обновлял.
- Полный `pytest` и фронтенд не запускались согласно вашему ограничению.

Подробный итог: [ITOG-raskladka-yacheyki.md](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md)

Коммит не отправлялся в remote. Посторонний `ZADANIE.md` в коммит не включён.


diff --git a/backend/app/api/warehouses.py b/backend/app/api/warehouses.py
index eeb2c3c350511eb55e0e3380a9fa6a4740bda586..d861506324858eca3fe381e464aafd16a4b951a6
--- a/backend/app/api/warehouses.py
+++ b/backend/app/api/warehouses.py
@@ -218,6 +218,8 @@
     id: str
     code: str
     barcode: str
+    objects: list[SortingObjectOut]
+    lines: list[SortingGoodsLineOut]
 
 
 class SortingObjectsOut(BaseModel):
@@ -371,12 +373,6 @@
     user: Annotated[User, Depends(require_cells_access)],
     session: Annotated[AsyncSession, Depends(get_db)],
 ) -> WarehouseMapMoveOut:
-    cell_id = body.cell_id or body.to_id
-    if cell_id is None:
-        raise HTTPException(
-            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
-            detail="cell_id_required",
-        )
     try:
         result = await warehouse_map_service.place_sorting_object(
             session,
@@ -385,7 +381,8 @@
             actor_user_id=user.id,
             kind=body.kind,
             object_id=body.id,
-            cell_id=cell_id,
+            cell_id=body.cell_id,
+            to_id=body.to_id,
             quantity=body.qty,
         )
     except WarehouseMapError as exc:
diff --git a/backend/app/services/warehouse_map_service.py b/backend/app/services/warehouse_map_service.py
index 38a95a46fc236326ece1898dff34e091e4952a65..56a4dcf88847767a2dde969c09b5d1dc2e3face2
--- a/backend/app/services/warehouse_map_service.py
+++ b/backend/app/services/warehouse_map_service.py
@@ -1096,6 +1096,27 @@
     objects: list[dict[str, Any]] = []
     lines: list[dict[str, Any]] = []
     _sorting_tree_rows(data["unassigned"], holder=None, objects=objects, lines=lines)
+    cells: list[dict[str, Any]] = []
+    for row in data["cells"]:
+        cell_objects: list[dict[str, Any]] = []
+        cell_lines: list[dict[str, Any]] = []
+        _sorting_tree_rows(
+            row["children"],
+            holder=f"cell:{row['id']}",
+            objects=cell_objects,
+            lines=cell_lines,
+        )
+        objects.extend(cell_objects)
+        lines.extend(cell_lines)
+        cells.append(
+            {
+                "id": row["id"],
+                "code": row["code"],
+                "barcode": row["barcode"],
+                "objects": cell_objects,
+                "lines": cell_lines,
+            }
+        )
     product_ids = {uuid.UUID(row["productId"]) for row in lines}
     products = (
         list(
@@ -1171,13 +1192,35 @@
         "objects": objects,
         "lines": lines,
         "products": product_data,
-        "cells": [
-            {"id": row["id"], "code": row["code"], "barcode": row["barcode"]}
-            for row in data["cells"]
-        ],
+        "cells": cells,
     }
 
 
+async def _sorting_destination_kind(
+    session: AsyncSession,
+    tenant_id: uuid.UUID,
+    warehouse_id: uuid.UUID,
+    container_id: uuid.UUID,
+) -> ContainerKind:
+    found: list[ContainerKind] = []
+    candidates: tuple[ContainerKind, ...] = ("pallet", "box", "cargo_place")
+    for candidate in candidates:
+        try:
+            await validate_container(
+                session,
+                tenant_id,
+                warehouse_id,
+                candidate,
+                container_id,
+            )
+        except ValueError:
+            continue
+        found.append(candidate)
+    if len(found) != 1:
+        raise WarehouseMapError("destination_not_found")
+    return found[0]
+
+
 async def place_sorting_object(
     session: AsyncSession,
     *,
@@ -1186,9 +1229,26 @@
     actor_user_id: uuid.UUID,
     kind: ObjectKind,
     object_id: uuid.UUID,
-    cell_id: uuid.UUID,
+    cell_id: uuid.UUID | None,
+    to_id: uuid.UUID | None,
     quantity: int,
 ) -> dict[str, Any]:
+    if cell_id is not None and to_id is not None:
+        raise WarehouseMapError("destination_conflict")
+    if cell_id is not None:
+        destination_kind: DestinationKind = "cell"
+        destination_id = cell_id
+    elif to_id is not None:
+        destination_kind = await _sorting_destination_kind(
+            session,
+            tenant_id,
+            warehouse_id,
+            to_id,
+        )
+        destination_id = to_id
+    else:
+        destination_kind = "unassigned"
+        destination_id = None
     return await move_object(
         session,
         tenant_id=tenant_id,
@@ -1196,7 +1256,7 @@
         actor_user_id=actor_user_id,
         kind=kind,
         object_id=object_id,
-        to_kind="cell",
-        to_id=cell_id,
+        to_kind=destination_kind,
+        to_id=destination_id,
         quantity=quantity,
     )
diff --git a/backend/tests/test_sorting_objects_cells.py b/backend/tests/test_sorting_objects_cells.py
new file mode 100644
index 0000000000000000000000000000000000000000..0a04ebddf99ba332ea1890b6c0baaa1c4bdc0fb6
--- /dev/null
+++ b/backend/tests/test_sorting_objects_cells.py
@@ -0,0 +1,259 @@
+from __future__ import annotations
+
+import time
+import uuid
+
+import pytest
+from httpx import AsyncClient
+from sqlalchemy import select
+
+from app.db.session import SessionLocal
+from app.models.inventory_balance import InventoryBalance
+from app.models.product import Product
+from app.models.seller import Seller
+from app.models.storage_location import StorageLocation
+from app.models.tenant import Tenant
+from app.models.user import User
+from app.models.warehouse import Warehouse
+from app.models.warehouse_box import WarehouseBox
+from app.services.sorting_location_service import get_or_create_sorting_location
+
+
+async def _register(client: AsyncClient, label: str) -> tuple[dict[str, str], Tenant]:
+    suffix = f"{label}-{time.time_ns()}"
+    email = f"{suffix}@example.com"
+    response = await client.post(
+        "/auth/register",
+        json={
+            "organization_name": f"Склад {label}",
+            "slug": suffix,
+            "admin_email": email,
+            "password": "password123",
+        },
+    )
+    assert response.status_code == 200, response.text
+    headers = {"Authorization": f"Bearer {response.json()['access_token']}"}
+    async with SessionLocal() as session:
+        user = await session.scalar(select(User).where(User.email == email))
+        assert user is not None
+        tenant = await session.get(Tenant, user.tenant_id)
+        assert tenant is not None
+        session.expunge(tenant)
+    return headers, tenant
+
+
+async def _seed_warehouse(
+    tenant_id: uuid.UUID,
+) -> tuple[Warehouse, StorageLocation, Product, InventoryBalance]:
+    suffix = uuid.uuid4().hex[:10]
+    async with SessionLocal() as session:
+        warehouse = Warehouse(
+            tenant_id=tenant_id,
+            name="Склад раскладки",
+            code=f"sorting-cells-{suffix}",
+            barcode=f"WH-SORTING-CELLS-{suffix}",
+        )
+        session.add(warehouse)
+        await session.flush()
+        cell = StorageLocation(
+            tenant_id=tenant_id,
+            warehouse_id=warehouse.id,
+            code="А-01-01",
+            barcode=f"CELL-{suffix}",
+        )
+        seller = Seller(tenant_id=tenant_id, name="ИП Раскладка")
+        session.add_all([cell, seller])
+        await session.flush()
+        product = Product(
+            tenant_id=tenant_id,
+            seller_id=seller.id,
+            name="Товар для раскладки",
+            sku_code=f"SORT-{suffix}",
+            wb_barcode=f"4600{suffix[:8]}",
+        )
+        session.add(product)
+        await session.flush()
+        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse.id)
+        balance = InventoryBalance(
+            tenant_id=tenant_id,
+            storage_location_id=sorting.id,
+            product_id=product.id,
+            quantity=4,
+            quantity_unpacked=4,
+            quantity_packed=0,
+        )
+        session.add(balance)
+        await session.commit()
+        for row in (warehouse, cell, product, balance):
+            session.expunge(row)
+    return warehouse, cell, product, balance
+
+
+async def _create_object(
+    client: AsyncClient,
+    headers: dict[str, str],
+    warehouse_id: uuid.UUID,
+    kind: str,
+) -> dict[str, object]:
+    response = await client.post(
+        f"/warehouses/{warehouse_id}/sorting-objects",
+        headers=headers,
+        json={"kind": kind},
+    )
+    assert response.status_code == 201, response.text
+    return response.json()
+
+
+@pytest.mark.asyncio
+async def test_object_in_cell_is_returned_with_cell_holder(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "object-in-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    stored_box = next(row for row in data["objects"] if row["id"] == box["id"])
+    assert stored_box["holder"] == f"cell:{cell.id}"
+    assert stored_cell["objects"] == [stored_box]
+    assert stored_cell["lines"] == []
+
+
+@pytest.mark.asyncio
+async def test_empty_target_returns_object_to_unassigned(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "take-off-cell")
+    warehouse, cell, _product, _balance = await _seed_warehouse(tenant.id)
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    removed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": None,
+            "qty": 1,
+        },
+    )
+    assert removed.status_code == 200, removed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    unassigned_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert unassigned_box["holder"] is None
+
+
+@pytest.mark.asyncio
+async def test_to_id_places_box_inside_pallet(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "box-in-pallet")
+    warehouse, _cell, _product, _balance = await _seed_warehouse(tenant.id)
+    pallet = await _create_object(async_client, headers, warehouse.id, "pallet")
+    box = await _create_object(async_client, headers, warehouse.id, "box")
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "box",
+            "id": box["id"],
+            "cell_id": None,
+            "to_id": pallet["id"],
+            "qty": 1,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    stored_box = next(
+        row for row in response.json()["objects"] if row["id"] == box["id"]
+    )
+    assert stored_box["holder"] == f"obj:{pallet['id']}"
+    assert not stored_box["holder"].startswith("cell:")
+    async with SessionLocal() as session:
+        db_box = await session.get(WarehouseBox, uuid.UUID(str(box["id"])))
+        assert db_box is not None
+        assert db_box.pallet_id == uuid.UUID(str(pallet["id"]))
+        assert db_box.storage_location_id is None
+
+
+@pytest.mark.asyncio
+async def test_product_in_cell_is_returned_with_quantity(
+    async_client: AsyncClient,
+) -> None:
+    headers, tenant = await _register(async_client, "product-in-cell")
+    warehouse, cell, product, balance = await _seed_warehouse(tenant.id)
+
+    placed = await async_client.post(
+        f"/warehouses/{warehouse.id}/sorting-objects/place",
+        headers=headers,
+        json={
+            "kind": "product",
+            "id": str(balance.id),
+            "cell_id": str(cell.id),
+            "to_id": None,
+            "qty": 4,
+        },
+    )
+    assert placed.status_code == 200, placed.text
+
+    response = await async_client.get(
+        f"/warehouses/{warehouse.id}/sorting-objects",
+        headers=headers,
+    )
+    assert response.status_code == 200, response.text
+    data = response.json()
+    stored_line = next(
+        row
+        for row in data["lines"]
+        if row["productId"] == str(product.id)
+    )
+    assert stored_line["holder"] == f"cell:{cell.id}"
+    assert stored_line["qty"] == 4
+    stored_cell = next(row for row in data["cells"] if row["id"] == str(cell.id))
+    assert stored_cell["objects"] == []
+    assert stored_cell["lines"] == [stored_line]
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
new file mode 100644
index 0000000000000000000000000000000000000000..1548aa789726dccdb2080cf603aac4329ca3b3e5
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md
@@ -0,0 +1,53 @@
+# Итог: раскладка видит содержимое ячеек
+
+Исправлен backend-контракт экрана раскладки. Ручка
+`GET /warehouses/{warehouse_id}/sorting-objects` теперь разворачивает через уже
+существующий `_sorting_tree_rows` не только зону «Без ячеек», но и дерево каждой
+адресной ячейки. Поэтому тара и товар после размещения больше не исчезают из
+ответа при следующем чтении.
+
+Существующие верхнеуровневые массивы `objects` и `lines` сохранены и теперь
+содержат все строки склада. У корневого содержимого ячейки `holder` имеет вид
+`cell:<uuid ячейки>`, у вложенного содержимого по-прежнему используется
+`obj:<uuid тары>`, а у зоны «Без ячеек» — `null`.
+
+Ответ расширен добавлением двух полей в каждый элемент `cells`:
+
+- `cells[].objects` — плоский список тары этой ячейки в формате `objects`;
+- `cells[].lines` — плоский список товарных строк этой ячейки в формате `lines`.
+
+Прежние поля `cells[].id`, `cells[].code` и `cells[].barcode` не изменены.
+Схема `SortingObjectsOut` обновлена: `SortingCellOut` явно описывает новые
+`objects` и `lines`.
+
+В `POST /warehouses/{warehouse_id}/sorting-objects/place` цели теперь
+различаются без подмены `to_id` идентификатором ячейки:
+
+- заполненный `cell_id` означает перемещение в адресную ячейку;
+- заполненный `to_id` означает перемещение в найденную тару того же тенанта и
+  склада (`pallet`, `box` или `cargo_place`);
+- обе пустые цели означают перемещение в виртуальную зону «Без ячеек»;
+- одновременное заполнение `cell_id` и `to_id` отклоняется как неоднозначная
+  цель.
+
+Добавлен файл `backend/tests/test_sorting_objects_cells.py` с четырьмя
+регрессионными сценариями: короб в ячейке виден после повторного GET, пустая
+цель снимает короб с ячейки, `to_id` вкладывает короб в палету, товар в ячейке
+возвращается с правильным количеством.
+
+Проверки на текущем worktree:
+
+- `ruff check .` — прошёл;
+- `mypy .` — прошёл, проверено 390 исходных файлов;
+- `python -m pytest tests/test_sorting_objects_cells.py tests/test_warehouse_map*.py -q`
+  — прошёл: 6 тестов;
+- `python3 ../scripts/ci/back_guard.py` — запущен, но не прошёл из-за общей
+  устаревшей baseline (контрольной линии) относительно текущей интеграционной
+  ветки. Сторож показал
+  уже накопленные нарушения в несвязанных модулях и считает весь существующий
+  `backend/app/services/warehouse_map_service.py` новым монолитом `0 → 1263`.
+  Baseline не обновлялась, потому что это изменило бы общую политику репозитория
+  вне этой задачи.
+
+Фронтенд не изменялся. Полный `pytest` не запускался по прямому ограничению
+владельца для этого worktree.

tokens used
175 918
Исправление реализовано и сохранено в Git.

Коммит: `bc7ef1a2ca72f8773f31c3f450f6d0d00560335a`  
Ветка: `codex/fix-raskladka-20260829`

Что изменено:

- `GET sorting-objects` теперь возвращает содержимое ячеек в общих `objects` и `lines` с `holder="cell:<uuid>"`.
- В `cells[]` добавлены поля `objects` и `lines`; прежние поля сохранены.
- Пустая цель переносит объект в «Без ячеек».
- `to_id` распознаётся как палета, короб или грузоместо в пределах текущего тенанта и склада.
- Добавлены четыре регрессионных теста.

Проверки:

- `ruff check .` — прошёл.
- `mypy .` — прошёл, 390 файлов.
- Адресный `pytest` — 6 тестов прошли.
- `back_guard` остаётся красным из-за устаревшей общей контрольной линии ветки, включая несвязанные файлы. Контрольную линию не обновлял.
- Полный `pytest` и фронтенд не запускались согласно вашему ограничению.

Подробный итог: [ITOG-raskladka-yacheyki.md](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-raskladka/tasks/_zhurnal/codex-briefs/ITOG-raskladka-yacheyki.md)

Коммит не отправлялся в remote. Посторонний `ZADANIE.md` в коммит не включён.


