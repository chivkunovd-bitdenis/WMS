Reading additional input from stdin...
2026-08-29T15:21:17.693994Z ERROR codex_models_manager::manager: failed to load models cache: missing field `supports_parallel_tool_calls` at line 97 column 5
OpenAI Codex v0.147.0
--------
workdir: /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/deniscivkunov/Projects/WMS/.git]
reasoning effort: high
reasoning summaries: none
session id: 01a04e1c-63d4-7a40-9ca7-a72da692dfb4
--------
user
## Разрешения для этой сессии (читать первым)

1. Ты в git worktree внутри `~/Projects/WMS/.worktrees/` — это РАЗРЕШЕНО правилами
   проекта (CLAUDE.md, пункт про `git worktree`). Проверку `pwd` не устраивай.
2. НЕ гоняй полный `pytest`. Только адресные файлы, названные ниже.
3. Продуктовый гейт отменён владельцем 25.08.2026.
4. Фронт не трогай. Только `backend/`.
5. Комментарии и коммиты — по-русски.

---

# Задача: сторож возврата слепнет после передачи поставки

**Полная фактура уже собрана и лежит в репозитории:**
`docs/evidence/20260829-fiks-vozvrata-posle-peredachi/README.md`
Прочитай его целиком ПЕРВЫМ ДЕЛОМ — там хронология по поставке
`WB-GI-267347087`, текущий код, предлагаемый код, оба списка ущерба и SQL для
проверки после выкатки. Владелец правку согласовал.

## Суть в одну строку

В `backend/app/services/fbs_cancellation_service.py`, функция
`reverse_fbs_shipment_if_needed`, сторож проверяет `order.supplier_status ==
"complete"`. Это ненадёжно: WB после передачи перекручивает статус в `cancel`
или `confirm`, условие перестаёт срабатывать, и товар возвращается на остаток,
хотя физически уехал.

Проверять надо **факт передачи поставки** — `fbs_supplies.delivered_at`. Он
заполняется в момент передачи и задним числом не снимается.

## Правило, согласованное владельцем

> Отгрузку ещё не провели, пришла отмена — приходуем: товар лежит у нас на полке.
> Поставку уже передали WB — не приходуем: товара на складе нет.

## Что сделать

Реализуй правку ровно так, как описано в разделе «Фикс» того документа. Важные
детали оттуда, не потеряй:

- связь заказа с поставкой искать по ТЕКСТОВОМУ полю `order.wb_supply_id`, а НЕ
  по `order.supply_id`: у отменённых заказов `supply_id` пустой;
- проверку `supplier_status == complete` не выкидывать, а оставить как
  дополнительную: `already_handed_over = supply_delivered or supplier_status == complete`;
- при срабатывании сторожа ставить `ledger.reversed_at`, чтобы запись журнала не
  пыталась вернуться на каждом следующем обходе, и писать предупреждение в лог;
- параметр `skip_if_supplier_complete` сохранить, он уже есть в сигнатуре.

ВНИМАНИЕ: в этой ветке у функции есть ещё параметр `actor_user_id` и разбор
многопозиционных заказов Ozon через `ledger.ozon_positions_json`. Ничего из
этого не ломай — правка касается только условия сторожа.

## Тесты (обязательны, из документа)

- заказ списан, поставка передана (`delivered_at` заполнен), `supplier_status`
  пришёл как `cancel` → движения по складу нет, `reversed_at` проставлен,
  функция вернула `False`;
- заказ списан, поставка НЕ передана (`delivered_at` пустой) → товар возвращается
  на остаток (негативный контроль: сторож не должен блокировать этот случай);
- регресс: прежний случай со `supplier_status == complete` продолжает
  отсекаться.

Клади их рядом с существующими в `backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py`.

## Ворота

```
cd backend && ruff check . && mypy .
python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
```

Итог — в `tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md`.
warning: Under-development features enabled: chronicle. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/deniscivkunov/.codex/config.toml.
codex
Сначала целиком читаю согласованную фактуру, затем сверю текущую реализацию и только после этого внесу узкую backend-правку. Полный `pytest` запускать не буду; проверю ровно два указанных тестовых файла, а итог сохраню в отдельном русском коммите.
exec
/bin/zsh -lc "sed -n '1,260p' docs/evidence/20260829-fiks-vozvrata-posle-peredachi/README.md" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
# Возврат на остаток после передачи поставки WB — дефект сторожа

**Дата:** 29.08.2026
**Статус:** дефект найден, фикс описан, **на бой не выкачен** — ждёт команды владельца.
**Затронуто:** `backend/app/services/fbs_cancellation_service.py`

---

## Коротко

Когда WB отменяет заказ, WMS возвращает товар на остаток. Это верно только
если товар ещё лежит у нас. Если поставку уже передали WB и коробки уехали —
возвращать нечего, остаток становится завышенным.

Сторож, поставленный 29.08 в 16:58, должен был это отсекать. Он отсекает не
всё: проверяет статус **заказа**, а надо проверять факт передачи **поставки**.

---

## Как проявилось

Поставка `WB-GI-267347087`:

| Событие | Когда |
|---|---|
| Товар списан со склада (проведена отгрузка) | 21.08.2026 12:32 UTC |
| Поставка передана WB (`delivered_at`, статус `done`) | 21.08.2026 12:01 UTC |
| WB прислал отмену по трём заказам | 29.08.2026 14:59 UTC |
| **WMS вернула 3 пары на остаток** | 29.08.2026 14:59 UTC |

Заказы: `5532200010`, `5532200011` (`6720-8A ( 6731-11A )/37`),
`5532205474` (`6720-8A ( 6731-11A )/34`), продавец ИП Чжоу.

Товар физически уехал восемь дней назад. На складе его нет.

---

## Почему сторож пропустил

Текущее условие в `reverse_fbs_shipment_if_needed`:

```python
supplier_status = (order.supplier_status or "").strip().lower()
if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
    # не возвращаем товар на остаток
```

Проверяется поле `fbs_orders.supplier_status`. Оно приходит из WB и **меняется
после передачи**: у отменённых заказов WB ставит `cancel`, у части — оставляет
`confirm`. Как только значение перестаёт быть `complete`, условие ложно и
возврат проводится.

`fbs_supplies.delivered_at` таким свойством не обладает: он заполняется в момент
передачи и задним числом не снимается. Это и есть надёжный признак того, что
товар покинул склад.

---

## Правило, которое нужно (согласовано с владельцем 29.08)

> Если отгрузку ещё не провели, а отмена пришла — приходуем: товар всё равно
> никуда не повезут, он лежит у нас на полке.
> Если поставку уже передали WB — не приходуем: товара на складе нет.

---

## Фикс

В `backend/app/services/fbs_cancellation_service.py`, в
`reverse_fbs_shipment_if_needed`: вместо проверки статуса заказа проверять факт
передачи поставки, оставив проверку статуса как дополнительную.

```python
# Факт передачи поставки — единственный надёжный признак того, что товар
# физически покинул склад. `order.supplier_status` для этого не годится:
# WB после передачи меняет его на `cancel`/`confirm`, и проверка на
# `complete` перестаёт срабатывать (дефект 29.08.2026, см. этот док).
supply_delivered = False
if order.wb_supply_id:
    supply = (await session.execute(
        select(FbsSupply).where(
            FbsSupply.tenant_id == order.tenant_id,
            FbsSupply.wb_supply_id == order.wb_supply_id,
        )
    )).scalar_one_or_none()
    supply_delivered = supply is not None and supply.delivered_at is not None

supplier_status = (order.supplier_status or "").strip().lower()
already_handed_over = supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE

if skip_if_supplier_complete and already_handed_over:
    logger.warning(
        "fbs_reversal_skipped_after_handover order=%s supply=%s delivered=%s status=%s",
        order.wb_order_id, order.wb_supply_id, supply_delivered, supplier_status,
    )
    ledger.reversed_at = datetime.now(UTC)   # отметка есть, движения нет
    return False
```

Связь заказа с поставкой — по тексту `wb_supply_id`, **не** по `supply_id`:
у отменённых заказов `fbs_orders.supply_id` пустой, заполнено только текстовое поле.

### Тесты (обязательны, без них не катим)

- `TC-NEW-001` — Given заказ списан и его поставка передана (`delivered_at`
  заполнен), а `supplier_status` пришёл как `cancel`, When приходит отмена,
  Then движения по складу нет, `reversed_at` проставлен, функция вернула `False`.
- `TC-NEW-002` — Given заказ списан, поставка НЕ передана (`delivered_at` пустой),
  When приходит отмена, Then товар возвращается на остаток (ожидаемое поведение,
  негативный контроль — сторож не должен блокировать этот случай).
- `TC-NEW-003` — Given у заказа нет `wb_supply_id` вовсе, When приходит отмена,
  Then возврат проводится (нет поставки — нечего было передавать).

---

## Ущерб на 29.08.2026 18:20

### Уже вернули ошибочно — 5 записей

| Продавец | Заказ | Артикул | Поставка передана | Вернули на остаток |
|---|---|---|---|---|
| Denmarcs | 5445244563 | wb6n9771yd | 13.08 | 15.08 (две записи по одному заказу) |
| ИП Чжоу | 5532200010 | 6720-8A ( 6731-11A )/37 | 21.08 | 29.08 14:59 |
| ИП Чжоу | 5532200011 | 6720-8A ( 6731-11A )/37 | 21.08 | 29.08 14:59 |
| ИП Чжоу | 5532205474 | 6720-8A ( 6731-11A )/34 | 21.08 | 29.08 14:59 |

Остаток ИП Чжоу вырос из-за этого с 1649 до 1652.

### Ещё 4 сработают при следующей синхронизации

| Продавец | Заказ | Артикул | Поставка передана | supplier_status |
|---|---|---|---|---|
| ИП Чжоу | 5539581010 | 6720-8T (6731-11Т)/31 | 27.08 | confirm |
| ИП Чжоу | 5532437696 | 6720-8A ( 6731-11A )/37 | 21.08 | confirm |
| ИП Чжоу | 5590698457 | 6020-4R/31 | 28.08 | confirm |
| Loviana | 5549156595 | J308-24/38 | 22.08 | confirm |

---

## Что сторож при этом ловит правильно

За период с 13:58 до 15:05 UTC 29.08 обработано 10 отмен:

| supplier_status | Поведение | Штук |
|---|---|---|
| `complete` | возврат заблокирован | 7 |
| `cancel` | возврат проведён (дефект — поставка была передана) | 3 |

То есть основной путь работает; течёт именно на смене статуса после передачи.

---

## Порядок работ

1. Правка `fbs_cancellation_service.py` по образцу выше.
2. Три теста из раздела «Тесты».
3. Гейт: `ruff check . && mypy . && pytest` + `python3 scripts/ci/back_guard.py`.
4. Выкатка на бой **только по явной команде владельца**.
5. После выкатки — снять с остатка 3 пары ИП Чжоу, вернувшиеся ошибочно
   29.08 (движение `correction_phantom_return`, как в
   `docs/evidence/20260829-spisanie-fantomnyh-vozvratov/`).
6. Проверить, что 4 «ждущих» заказа после фикса не дали движений.

## Как проверить, что дефект ушёл

```sql
-- должно быть 0 после фикса: возвраты по заказам, чья поставка передана
select count(*)
from fbs_shipment_reversal_ledger l
join fbs_orders o    on o.id = l.fbs_order_id
join fbs_supplies sp on sp.wb_supply_id = o.wb_supply_id
where l.reversed_at > '<момент выкатки>'
  and sp.delivered_at is not null;
```

exec
/bin/zsh -lc 'rg -n -C 3 "fbs_cancellation_service|reverse_fbs_shipment_if_needed|reversal guard|возврат.*передач|WB-GI-267347087|actor_user_id|ozon_positions_json" /Users/deniscivkunov/.codex/memories/MEMORY.md' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
343-
344-- rollout_summaries/2026-08-27T20-34-17-2SbJ-wms_process_change_and_pr_139.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=/Users/deniscivkunov/.codex/sessions/2026/08/27/rollout-2026-08-27T23-34-17-01a044ee-3574-7062-ac1b-865127ac8e6f.jsonl, updated_at=2026-08-28T12:04:58+00:00, thread_id=01a044ee-3574-7062-ac1b-865127ac8e6f, process change, movement-author audit, and PR #139 status)
345-- extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T21-30-00-qdiC-10min-memory-summary.md, updated_at=2026-08-27T21:30:00+00:00, thread_id=None, observed container-stock and tenant-safety contract) [skysight memory]
346:- extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-27T23-20-00-yOBk-10min-memory-summary.md, updated_at=2026-08-27T23:20:00+00:00, thread_id=None, observed `actor_user_id` correction and unrelated branch-gate noise) [skysight memory]
347-
348-### keywords
349-
350:- `InventoryMovement.actor_user_id`, `actor_user_id`, `codex/inventory-movement-author`, `CONTRACT.md`, `WAVE-1-CODEX.md`, container references, `Сортировка`, tenant ownership, row locking, `48972cef`
351-
352-## User preferences
353-
--
1503-
1504-### keywords
1505-
1506:- `5532200010`, `5532200011`, `WB-GI-267347087`, `supplierStatus=complete`, `wbStatus=waiting`, `FBS-91FBDA87-001`, `WB-MP-50687347`, `FBS-91FBDA87-023`, `WB-MP-50687369`, `Отсортировано`, `Отгрузите поставку`
1507-
1508-## Task 4: Check whether final catalog boxes work is integrated on stage, outcome incomplete version observed; redeploy completion not captured
1509-

exec
/bin/zsh -lc "sed -n '1460,1512p' /Users/deniscivkunov/.codex/memories/MEMORY.md" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
- Candidate `e7a1cbd4` was reported published after storage `30/30` and a combined browser gate `55/55`, with a clean worktree and one migration head. This is local candidate evidence, not staging proof: the next visible step was merge into `etalon`, deploy to `staging`, confirm the deployed SHA, and open all three sections. [Task 1] [skysight memory]

## Failures and how to do differently

- Symptom: a coordinated stage release becomes an accidental broad production deploy. Cause: reports/billing/storage and catalog boxes are discussed in one thread. Fix: retain the explicit stage-only versus production-only split and verify the exact branch/SHA plus each screen before promotion. [Task 1] [skysight memory]
- Symptom: agents duplicate finance/storage logic while parallelizing. Cause: dependency ownership was not made concrete. Fix: establish the single `backend-core` and shared UI boundary before storage integration; stop and resolve incompatible tariff ownership before deploy. [Task 1] [skysight memory]
- Symptom: a green local browser gate is reported as staging completion. Cause: candidate publication and environment deployment were conflated. Fix: confirm merge/push, deployed SHA, and all three stage screens; only then stop background resources as requested. [Task 1] [skysight memory]

# Task Group: WMS catalog boxes release, scan responsiveness, and WB FBS customer-status explanation
scope: Use for the 2026-08-23 box/catalog follow-up, whether it is actually deployed, production scan-latency evidence, or a customer-facing explanation of WB orders still awaiting physical acceptance.
applies_to: cwd=/Users/deniscivkunov/Projects/WMS and `/Users/deniscivkunov/Projects/WMS/.worktrees/pipeline-etalon`; reuse_rule=product requirements and diagnostic distinctions are reusable; commits, deployed SHAs, metrics, live order statuses, and monitoring state are time-specific and must be rechecked

## Task 1: Review and publish the `Короба` catalog work, outcome committed/pushed but not production-verified

### rollout_summary_files

- extensions/skysight/resources/2026-08-23T11-40-00-VKFS-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T11-40-00-VKFS-10min-memory-summary.md, updated_at=2026-08-23T11:40:00+00:00, thread_id=None, observed checked-branch-only state and remaining integration/stage verification)
- extensions/skysight/resources/2026-08-23T11-20-00-kDSA-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T11-20-00-kDSA-10min-memory-summary.md, updated_at=2026-08-23T11:20:00+00:00, thread_id=None, observed PASS evidence and real-volume caveat)
- extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md, updated_at=2026-08-23T06:00:00+00:00, thread_id=None, broad release and UI-decision context)

### keywords

- `Короба`, `Короба и грузоместа`, `Товары`, `36fc18d0966c8d65861fdac3a0ba9862e99654f5`, `source document`, `barcode echo`, `highlight`, `duplicate scan`, `etalon`, `stage verification`

## Task 2: Diagnose production box/receiving scan latency, outcome materially improved in observed production sample

### rollout_summary_files

- extensions/skysight/resources/2026-08-23T11-40-00-VKFS-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T11-40-00-VKFS-10min-memory-summary.md, updated_at=2026-08-23T11:40:00+00:00, thread_id=None, observed one-hour monitoring request and no new activity)
- extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md, updated_at=2026-08-23T10:50:00+00:00, observed 13 real scans/HTTP 200 and interval comparison)
- extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md, updated_at=2026-08-23T06:00:00+00:00, thread_id=None, backend-vs-frontend diagnosis and deploy/rollback context)

### keywords

- `scan latency`, `15-20 seconds`, `276 rows`, `116 ms`, `React.memo`, `FfInboundBoxAddDialog.tsx`, `FfInboundRequestView.tsx`, `hotfix/scan-render-20260823`, `4.9 seconds`, `15.5 seconds`, `E2E_WEB_MODE=preview`

## Task 3: Explain WB FBS orders `5532200010` and `5532200011` to a client, outcome evidence-based wording prepared

### rollout_summary_files

- extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md, updated_at=2026-08-23T10:50:00+00:00, thread_id=None, observed box-level WB acceptance evidence)
- extensions/skysight/resources/2026-08-23T10-40-00-rzZE-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T10-40-00-rzZE-10min-memory-summary.md, updated_at=2026-08-23T10:40:00+00:00, thread_id=None, observed request for a short non-technical explanation)
- extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md, updated_at=2026-08-23T06:00:00+00:00, thread_id=None, supply/status context)

### keywords

- `5532200010`, `5532200011`, `WB-GI-267347087`, `supplierStatus=complete`, `wbStatus=waiting`, `FBS-91FBDA87-001`, `WB-MP-50687347`, `FBS-91FBDA87-023`, `WB-MP-50687369`, `Отсортировано`, `Отгрузите поставку`

## Task 4: Check whether final catalog boxes work is integrated on stage, outcome incomplete version observed; redeploy completion not captured

### rollout_summary_files

- extensions/skysight/resources/2026-08-23T17-50-00-nUCS-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T17-50-00-nUCS-10min-memory-summary.md, updated_at=2026-08-23T17:50:00+00:00, thread_id=None, observed hands-on stage checks of reports, boxes, FBS reprint, cells, and sorting)

codex
Документ подтверждает узкую причину: сторож должен опираться на неизменяемый `fbs_supplies.delivered_at`, связывая поставку через текстовый `wb_supply_id`, но сохранить старую отсечку по `complete`. Сейчас сверяю модели, точную сигнатуру функции и существующие тестовые фикстуры, чтобы не задеть `actor_user_id` и многопозиционный путь Ozon.
exec
/bin/zsh -lc "sed -n '1,320p' backend/app/services/fbs_cancellation_service.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
"""FBS order cancellation and status sync helpers."""

from __future__ import annotations

import logging
import uuid
from datetime import UTC, datetime

import httpx
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.fbs_order import (
    FBS_ORDER_STATUS_CANCELLED,
    FBS_ORDER_STATUS_DEFECT,
    FBS_ORDER_STATUS_DONE,
    FBS_ORDER_STATUS_IN_DELIVERY,
    FBS_ORDER_STATUS_SORTED,
    FbsOrder,
)
from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
from app.services import inventory_service as inv_svc
from app.services.wb_marketplace_orders_service import (
    WbMarketplaceOrdersError,
    _release_reservation,
    _resolve_marketplace_api_token,
    sync_order_statuses,
)
from app.services.wildberries_client import (
    WildberriesClientError,
    cancel_marketplace_order,
)
from app.services.wildberries_errors import (
    log_wb_client_error,
    wb_error_context,
    wb_error_ref,
    wb_operator_message,
)

logger = logging.getLogger(__name__)

NON_CANCELLABLE_STATUSES = frozenset(
    {
        FBS_ORDER_STATUS_IN_DELIVERY,
        FBS_ORDER_STATUS_DONE,
        FBS_ORDER_STATUS_SORTED,
        FBS_ORDER_STATUS_DEFECT,
    }
)

# WB-статус поставщика "собрано и передано". Как только он появился на заказе,
# посылка физически уехала на приёмку WB — вернуться она может только через
# отдельный документ возврата (в системе пока нет), а не автоматическим
# сторнированием списания.
SUPPLIER_STATUS_COMPLETE = "complete"


class FbsCancellationError(Exception):
    def __init__(
        self,
        code: str,
        *,
        message: str | None = None,
        context: dict[str, object] | None = None,
        retryable: bool = False,
    ) -> None:
        self.code = code
        self.message = message or code
        self.context = context or {}
        self.retryable = retryable
        super().__init__(code)


async def reverse_fbs_shipment_if_needed(
    session: AsyncSession,
    order: FbsOrder,
    *,
    actor_user_id: uuid.UUID | None = None,
    skip_if_supplier_complete: bool = True,
) -> bool:
    """Reverse one packed physical unit exactly once; caller owns the order lock.

    `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
    потому что у функции два вызывающих (ручная отмена оператором и обработка
    статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
    оба вызова используют безопасное значение по умолчанию: если WB уже
    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
    пришли позже чем через двое суток после упаковки, то есть посылка была уже
    у WB.
    """
    stmt = (
        select(FbsShipmentReversalLedger)
        .where(
            FbsShipmentReversalLedger.tenant_id == order.tenant_id,
            FbsShipmentReversalLedger.fbs_order_id == order.id,
        )
        .with_for_update()
    )
    ledger = (await session.execute(stmt)).scalar_one_or_none()
    if (
        ledger is None
        or ledger.shipment_movement_id is None
        or ledger.reversed_at is not None
    ):
        return False

    supplier_status = (order.supplier_status or "").strip().lower()
    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
        # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
        # обработанной без движения по складу (reversal_movement_id остаётся
        # пустым), иначе она будет пытаться вернуться на каждом следующем
        # обходе синка статусов.
        logger.warning(
            "fbs shipment reversal skipped: parcel already handed to WB "
            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
            order.id,
            order.wb_order_id,
            order.seller_id,
            ledger.product_id,
        )
        ledger.reversed_at = datetime.now(UTC)
        await session.flush()
        return False

    from app.services import stock_direction_service

    positions = list(ledger.ozon_positions_json or [])
    if not positions:
        positions = [
            {
                "product_id": str(ledger.product_id),
                "storage_location_id": str(ledger.storage_location_id),
                "quantity": int(ledger.quantity),
            }
        ]
    reversal_movement = None
    for position in positions:
        product_id = uuid.UUID(str(position["product_id"]))
        storage_location_id = uuid.UUID(str(position["storage_location_id"]))
        quantity = int(str(position["quantity"]))
        movement = await inv_svc.record_movement_and_adjust_balance(
            session,
            tenant_id=order.tenant_id,
            product_id=product_id,
            storage_location_id=storage_location_id,
            quantity_delta=quantity,
            movement_type=MOVEMENT_TYPE_FBS_SHIPMENT,
            actor_user_id=actor_user_id,
        )
        if reversal_movement is None:
            reversal_movement = movement
        await stock_direction_service.restore_fbs_pool(
            session,
            order.tenant_id,
            product_id,
            quantity,
        )
    ledger.reversed_at = datetime.now(UTC)
    await session.flush()
    ledger.reversal_movement_id = reversal_movement.id if reversal_movement is not None else None
    await session.flush()
    return True


def penalty_band_for_order(created_at_wb: datetime) -> str:
    """WB cancel penalty window band (logged only in MVP)."""
    now = datetime.now(tz=UTC)
    created = (
        created_at_wb if created_at_wb.tzinfo is not None else created_at_wb.replace(tzinfo=UTC)
    )
    hours = (now - created).total_seconds() / 3600.0
    if hours < 13:
        return "lt13"
    if hours < 18:
        return "13_18"
    if hours < 120:
        return "18_120"
    return "gt120"


async def cancel_order(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    order_id: uuid.UUID,
    http_client: httpx.AsyncClient,
    *,
    actor_user_id: uuid.UUID | None,
) -> FbsOrder:
    stmt = (
        select(FbsOrder)
        .where(FbsOrder.id == order_id, FbsOrder.tenant_id == tenant_id)
        .with_for_update()
    )
    res = await session.execute(stmt)
    order = res.scalar_one_or_none()
    if order is None:
        raise FbsCancellationError("order_not_found")

    if order.status == FBS_ORDER_STATUS_CANCELLED:
        return order

    if order.status in NON_CANCELLABLE_STATUSES:
        raise FbsCancellationError("order_not_cancellable")

    band = penalty_band_for_order(order.created_at_wb)
    logger.info(
        "fbs_cancel_penalty_band order_id=%s wb_order_id=%s band=%s",
        order.id,
        order.wb_order_id,
        band,
    )

    try:
        api_token = await _resolve_marketplace_api_token(session, tenant_id, order.seller_id)
    except WbMarketplaceOrdersError as exc:
        raise FbsCancellationError(
            exc.code,
            message=exc.message,
            context=exc.context,
            retryable=exc.retryable,
        ) from exc

    try:
        await cancel_marketplace_order(
            http_client,
            api_token=api_token,
            order_id=int(order.wb_order_id),
        )
    except WildberriesClientError as exc:
        suffix = f"_{exc.status_code}" if exc.status_code else ""
        ref = wb_error_ref()
        log_wb_client_error(
            logger,
            "fbs order WB cancel failed",
            exc,
            tenant_id=tenant_id,
            seller_id=order.seller_id,
            local_entity_id=order.id,
            wb_object_id=order.wb_order_id,
            ref=ref,
        )
        raise FbsCancellationError(
            f"wb_{exc.code}{suffix}",
            message=wb_operator_message(exc),
            context=wb_error_context(
                exc,
                ref=ref,
                extra={"order_id": str(order.id), "wb_order_id": order.wb_order_id},
            ),
            retryable=exc.code == "transport_error",
        ) from exc

    order.status = FBS_ORDER_STATUS_CANCELLED
    order.wb_status = "cancelled"
    await reverse_fbs_shipment_if_needed(
        session,
        order,
        actor_user_id=actor_user_id,
    )
    from app.services.fbs_packaging_integration_service import (
        detach_cancelled_order_from_supply,
    )

    await detach_cancelled_order_from_supply(
        session,
        tenant_id,
        order,
        actor_user_id=actor_user_id,
    )
    await _release_reservation(session, order)
    await session.flush()
    return order


async def sync_seller_order_statuses(
    session: AsyncSession,
    tenant_id: uuid.UUID,
    seller_id: uuid.UUID,
    http_client: httpx.AsyncClient,
    *,
    actor_user_id: uuid.UUID | None,
) -> int:
    try:
        api_token = await _resolve_marketplace_api_token(session, tenant_id, seller_id)
    except WbMarketplaceOrdersError as exc:
        raise FbsCancellationError(
            exc.code,
            message=exc.message,
            context=exc.context,
            retryable=exc.retryable,
        ) from exc
    try:
        updated = await sync_order_statuses(
            session,
            tenant_id,
            seller_id,
            http_client,
            api_token,
            actor_user_id=actor_user_id,
        )
        await session.flush()
        return updated
    except WbMarketplaceOrdersError as exc:
        raise FbsCancellationError(
            exc.code,
            message=exc.message,
            context=exc.context,
            retryable=exc.retryable,
        ) from exc

exec
/bin/zsh -lc "sed -n '1,360p' backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
"""Хотфикс 29.08.2026 — учёт товара по заказам FBS.

Найдено на боевых данных 29.08.2026:

Дефект 1 (`_write_off_sold_order`): списание со склада жило только в
завершении упаковки поставки. Заказ, закрывшийся статусом WB "sold" в обход
упаковки, физически уезжал, а в учёте оставался — на бою нашли 14 таких.

Дефект 2 (`reverse_fbs_shipment_if_needed`): при отмене остаток возвращался на
склад по факту "было списание и его ещё не отменяли", без проверки, что
посылку уже забрал WB. На бою — 275 ложных возвратов, все по заказам со
статусом поставщика "complete" (собрано и передано).

TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
"""

from __future__ import annotations

import time
import uuid
from datetime import UTC, datetime, timedelta
from typing import Any

import pytest
from httpx import AsyncClient
from sqlalchemy import func, select

from app.db.session import SessionLocal
from app.models.fbs_order import (
    FBS_ORDER_STATUS_ASSEMBLING,
    FBS_ORDER_STATUS_CANCELLED,
    FBS_ORDER_STATUS_DONE,
    FBS_ORDER_STATUS_PACKED,
    MAPPING_STATUS_MAPPED,
    RESERVE_STATUS_NO_STOCK,
    FbsOrder,
)
from app.models.fbs_order_pick import FbsOrderPick
from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
from app.models.fbs_supply import FbsSupply
from app.models.inventory_balance import InventoryBalance
from app.models.inventory_movement import (
    MOVEMENT_TYPE_FBS_SHIPMENT,
    MOVEMENT_TYPE_INBOUND_INTAKE,
    InventoryMovement,
)
from app.models.product import Product
from app.services import inventory_service
from app.services.fbs_cancellation_service import reverse_fbs_shipment_if_needed
from app.services.sorting_location_service import get_or_create_sorting_location
from app.services.wb_marketplace_orders_service import (
    _apply_wb_status_to_order,
    upsert_order_from_wb_row,
)
from tests.fbs_seed_helpers import DEFAULT_WB_WAREHOUSE_ID, seed_fbs_warehouse_binding


async def _register_ff_admin(async_client: AsyncClient) -> tuple[dict[str, str], str, uuid.UUID]:
    suffix = str(time.time_ns())
    reg = await async_client.post(
        "/auth/register",
        json={
            "organization_name": f"FBS writeoff {suffix}",
            "slug": f"fbs-writeoff-{suffix}",
            "admin_email": f"fbs-writeoff-{suffix}@example.com",
            "password": "password123",
        },
    )
    assert reg.status_code == 200, reg.text
    headers = {"Authorization": f"Bearer {reg.json()['access_token']}"}
    me = await async_client.get("/auth/me", headers=headers)
    tenant_id = uuid.UUID(me.json()["tenant_id"])
    return headers, suffix, tenant_id


async def _setup_seller_with_token(
    async_client: AsyncClient,
    headers: dict[str, str],
    suffix: str,
) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID]:
    seller = await async_client.post(
        "/sellers",
        headers=headers,
        json={"name": f"Seller {suffix}"},
    )
    assert seller.status_code in (200, 201), seller.text
    seller_id = uuid.UUID(seller.json()["id"])
    tok = await async_client.patch(
        f"/integrations/wildberries/sellers/{seller_id}/tokens",
        headers=headers,
        json={"marketplace_api_token": "wb-marketplace-token"},
    )
    assert tok.status_code == 200, tok.text
    warehouse = await async_client.post(
        "/warehouses",
        headers=headers,
        json={"name": "WH", "code": f"wh-{suffix[-8:]}"},
    )
    assert warehouse.status_code in (200, 201), warehouse.text
    warehouse_id = uuid.UUID(warehouse.json()["id"])
    location = await async_client.post(
        f"/warehouses/{warehouse_id}/locations",
        headers=headers,
        json={"code": f"A-{suffix[-6:]}"},
    )
    assert location.status_code in (200, 201), location.text
    source_location_id = uuid.UUID(location.json()["id"])
    return seller_id, warehouse_id, source_location_id


def _wb_order_row(*, order_id: int, article: str) -> dict[str, Any]:
    return {
        "id": order_id,
        "rid": f"rid-{order_id}",
        "createdAt": "2026-07-01T12:00:00+03:00",
        "nmId": 900001,
        "chrtId": 555,
        "article": article,
        "skus": [f"BAR-{order_id}"],
        "price": 199900,
        "cargoType": 1,
        "officeId": 42,
        "isLegal": False,
        "warehouseId": DEFAULT_WB_WAREHOUSE_ID,
    }


# TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается ровно один раз
@pytest.mark.asyncio
async def test_write_off_sold_order_that_skipped_packaging_happens_once(
    async_client: AsyncClient,
) -> None:
    """Дефект 1: заказ подобран (лежит в ячейке сортировки), но так и не был
    упакован (в системе нет `FbsShipmentReversalLedger`, как это бывает у
    заказов, закрытых WB напрямую).

    Given: у заказа есть подбор (`FbsOrderPick`) в ячейку сортировки с 1 штукой
           остатка, но списания по нему ещё не было.
    When: приходит статус WB "sold" (обход синка статусов) — дважды подряд,
          как это происходит при повторном опросе WB.
    Then: остаток списывается один раз (движение `fbs_shipment` на -1,
          `FbsShipmentReversalLedger` с ровно одной строкой на заказ);
          повторный обход находит уже существующую запись в журнале и
          повторного списания не делает — остаток и число движений не меняются.
    """
    headers, suffix, tenant_id = await _register_ff_admin(async_client)
    seller_id, warehouse_id, source_location_id = await _setup_seller_with_token(
        async_client, headers, suffix
    )

    async with SessionLocal() as session:
        await seed_fbs_warehouse_binding(
            session,
            tenant_id=tenant_id,
            seller_id=seller_id,
            wms_warehouse_id=warehouse_id,
        )
        product = Product(
            tenant_id=tenant_id,
            seller_id=seller_id,
            name="Sold-bypass product",
            sku_code=f"SOLD-{suffix[-8:]}",
            wb_barcode=f"SOLD-BAR-{suffix[-8:]}",
        )
        session.add(product)
        await session.flush()

        order, _created = await upsert_order_from_wb_row(
            session,
            tenant_id,
            seller_id,
            _wb_order_row(order_id=970001, article=f"ART-{suffix}"),
        )
        order.product_id = product.id
        order.status = FBS_ORDER_STATUS_ASSEMBLING

        supply = FbsSupply(
            tenant_id=tenant_id,
            seller_id=seller_id,
            warehouse_id=warehouse_id,
            wb_supply_id=f"skip-pack-{suffix[-8:]}",
            name="Supply that never got packaged",
            delivery_type="warehouse_sc",
        )
        session.add(supply)
        await session.flush()
        order.supply_id = supply.id

        # Товар пришёл на склад и был подобран в сортировку — обычное
        # состояние перед упаковкой. В найденных на бою 14 случаях на этом
        # шаге всё и заканчивалось: упаковку заказ не проходил.
        await inventory_service.record_movement_and_adjust_balance(
            session,
            tenant_id=tenant_id,
            product_id=product.id,
            storage_location_id=source_location_id,
            quantity_delta=1,
            movement_type=MOVEMENT_TYPE_INBOUND_INTAKE,
            actor_user_id=None,
        )
        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse_id)
        await inventory_service.transfer_on_hand_between_locations(
            session,
            tenant_id=tenant_id,
            from_storage_location_id=source_location_id,
            to_storage_location_id=sorting.id,
            product_id=product.id,
            quantity=1,
            actor_user_id=None,
        )
        session.add(
            FbsOrderPick(
                tenant_id=tenant_id,
                fbs_order_id=order.id,
                fbs_supply_id=supply.id,
                source_storage_location_id=source_location_id,
                sorting_storage_location_id=sorting.id,
                product_id=product.id,
                picked_at=datetime.now(UTC),
                scan_idempotency_key=f"pick-{order.id}",
            )
        )
        await session.commit()
        order_id = order.id
        product_id = product.id
        sorting_id = sorting.id

    # Первый обход синка статусов: заказ выкуплен.
    async with SessionLocal() as session:
        order = await session.get(FbsOrder, order_id)
        assert order is not None
        await _apply_wb_status_to_order(
            session, order, "sold", supplier_status="complete", actor_user_id=None
        )
        await session.commit()

    async with SessionLocal() as session:
        order = await session.get(FbsOrder, order_id)
        assert order is not None
        assert order.status == FBS_ORDER_STATUS_DONE

        ledgers = list(
            (
                await session.execute(
                    select(FbsShipmentReversalLedger).where(
                        FbsShipmentReversalLedger.fbs_order_id == order_id
                    )
                )
            ).scalars()
        )
        assert len(ledgers) == 1
        assert ledgers[0].quantity == 1
        assert ledgers[0].storage_location_id == sorting_id
        assert ledgers[0].reversed_at is None

        write_off_count = await session.scalar(
            select(func.count(InventoryMovement.id)).where(
                InventoryMovement.tenant_id == tenant_id,
                InventoryMovement.product_id == product_id,
                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
            )
        )
        assert write_off_count == 1

        balance = await session.scalar(
            select(InventoryBalance).where(
                InventoryBalance.tenant_id == tenant_id,
                InventoryBalance.product_id == product_id,
                InventoryBalance.storage_location_id == sorting_id,
            )
        )
        assert balance is not None
        assert int(balance.quantity) == 0

    # Второй обход синка статусов с тем же статусом — второго списания быть не должно.
    async with SessionLocal() as session:
        order = await session.get(FbsOrder, order_id)
        assert order is not None
        await _apply_wb_status_to_order(
            session, order, "sold", supplier_status="complete", actor_user_id=None
        )
        await session.commit()

    async with SessionLocal() as session:
        ledgers = list(
            (
                await session.execute(
                    select(FbsShipmentReversalLedger).where(
                        FbsShipmentReversalLedger.fbs_order_id == order_id
                    )
                )
            ).scalars()
        )
        assert len(ledgers) == 1, "повторный обход не должен создавать вторую запись"

        write_off_count = await session.scalar(
            select(func.count(InventoryMovement.id)).where(
                InventoryMovement.tenant_id == tenant_id,
                InventoryMovement.product_id == product_id,
                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
            )
        )
        assert write_off_count == 1, "повторный обход не должен списывать второй раз"

        balance = await session.scalar(
            select(InventoryBalance).where(
                InventoryBalance.tenant_id == tenant_id,
                InventoryBalance.product_id == product_id,
                InventoryBalance.storage_location_id == sorting_id,
            )
        )
        assert balance is not None
        assert int(balance.quantity) == 0


async def _seed_order_with_existing_write_off(
    async_client: AsyncClient,
    *,
    supplier_status: str,
) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
    """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.

    Возвращает (order_id, tenant_id, product_id, storage_location_id).
    """
    headers, suffix, tenant_id = await _register_ff_admin(async_client)
    seller_id, warehouse_id, location_id = await _setup_seller_with_token(
        async_client, headers, suffix
    )

    async with SessionLocal() as session:
        product = Product(
            tenant_id=tenant_id,
            seller_id=seller_id,
            name="Already shipped product",
            sku_code=f"SHIP-{supplier_status}-{suffix[-8:]}",
            wb_barcode=f"SHIP-BAR-{supplier_status}-{suffix[-8:]}",
        )
        session.add(product)
        await session.flush()

        now = datetime.now(UTC)
        order = FbsOrder(
            tenant_id=tenant_id,
            seller_id=seller_id,
            warehouse_id=warehouse_id,
            product_id=product.id,
            wb_order_id=int(f"98{suffix[-6:]}"),
            status=FBS_ORDER_STATUS_PACKED,
            supplier_status=supplier_status,
            created_at_wb=now,
            deadline_at=now + timedelta(hours=24),
            mapping_status=MAPPING_STATUS_MAPPED,
            reserve_status=RESERVE_STATUS_NO_STOCK,
        )
        session.add(order)
        await session.flush()

        # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,

exec
/bin/zsh -lc 'rg -n "class FbsSupply|wb_supply_id|delivered_at|reverse_fbs_shipment_if_needed" backend/app backend/tests/test_fbs_ozon_lane.py | head -240' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
backend/tests/test_fbs_ozon_lane.py:197:        wb_supply_id="ozon-supply-1",
backend/tests/test_fbs_ozon_lane.py:1120:            wb_supply_id=None,
backend/tests/test_fbs_ozon_lane.py:1212:    assert str(workspace["supply"]["wb_supply_id"]).startswith("PENDING-")
backend/app/cli/reconcile_fbs_unlinked_shipments.py:69:                        FbsSupply.delivered_at.is_not(None),
backend/app/cli/reconcile_fbs_shipped_stock.py:32:        WHERE fs.delivered_at IS NOT NULL
backend/app/cli/reconcile_fbs_shipped_stock.py:55:        WHERE fs.delivered_at IS NOT NULL
backend/app/services/fbs_cancelled_after_pack_service.py:113:    if order.wb_supply_id and order.wb_supply_id == supply.wb_supply_id:
backend/app/services/fbs_cancelled_after_pack_service.py:176:    if supply.wb_supply_id:
backend/app/services/fbs_cancelled_after_pack_service.py:180:                FbsOrder.wb_supply_id == supply.wb_supply_id,
backend/app/services/fbs_cancelled_after_pack_service.py:326:        and order.wb_supply_id
backend/app/services/fbs_cancelled_after_pack_service.py:332:                FbsSupply.wb_supply_id == order.wb_supply_id,
backend/app/services/fbs_cancelled_after_pack_service.py:363:        if current_supply is None and order.wb_supply_id:
backend/app/services/fbs_cancelled_after_pack_service.py:368:                    if item.seller_id == order.seller_id and item.wb_supply_id == order.wb_supply_id
backend/app/services/fbs_cancelled_after_pack_service.py:458:                        "wb_supply_id": supply.wb_supply_id,
backend/app/services/fbs_cancelled_after_pack_service.py:465:                        "wb_supply_id": order.wb_supply_id,
backend/app/services/fbs_cancelled_after_pack_service.py:481:                    supply.delivered_at is not None
backend/app/models/seller_wildberries_imported_supply.py:44:    wb_supply_id: Mapped[int | None] = mapped_column(BigInteger, nullable=True)
backend/app/services/fbs_shipment_service.py:181:    wb_supply_id: str | None,
backend/app/services/fbs_shipment_service.py:195:        wb_object_id=wb_supply_id,
backend/app/services/fbs_shipment_service.py:199:    if wb_supply_id is not None:
backend/app/services/fbs_shipment_service.py:200:        extra["wb_supply_id"] = wb_supply_id
backend/app/services/fbs_shipment_service.py:356:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:786:    supply.delivered_at = now
backend/app/services/fbs_shipment_service.py:952:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:960:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:988:        wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1022:    external_supply_id = supply.external_supply_id or supply.wb_supply_id
backend/app/services/fbs_shipment_service.py:1256:                wb_supply_id=supply_read.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1280:                    supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1290:                    wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1310:                        wb_object_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1316:                        wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1332:                    wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1342:                    wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1393:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1403:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1423:                wb_object_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1429:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1445:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1455:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1520:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_service.py:1529:            wb_supply_id=supply.wb_supply_id,
backend/app/models/fbs_order.py:209:    wb_supply_id: Mapped[str | None] = mapped_column(String(64), nullable=True, index=True)
backend/app/services/fbs_supply_reconcile_service.py:149:    wb_supply_id: str | None,
backend/app/services/fbs_supply_reconcile_service.py:154:    operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:155:    operation.wb_object_kind = "supply" if wb_supply_id is not None else None
backend/app/services/fbs_supply_reconcile_service.py:226:    wb_supply_id: str | None,
backend/app/services/fbs_supply_reconcile_service.py:230:    operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:231:    operation.wb_object_kind = "supply" if wb_supply_id is not None else None
backend/app/services/fbs_supply_reconcile_service.py:272:    wb_supply_id: str,
backend/app/services/fbs_supply_reconcile_service.py:277:    operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:317:    wb_supply_id: str | None,
backend/app/services/fbs_supply_reconcile_service.py:322:    operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:323:    operation.wb_object_kind = "supply" if wb_supply_id is not None else None
backend/app/services/fbs_supply_reconcile_service.py:339:    wb_supply_id: str | None = None,
backend/app/services/fbs_supply_reconcile_service.py:346:    if wb_supply_id is not None:
backend/app/services/fbs_supply_reconcile_service.py:347:        operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:359:    wb_supply_id: str,
backend/app/services/fbs_supply_reconcile_service.py:365:    operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_reconcile_service.py:378:    wb_supply_id: str,
backend/app/services/fbs_supply_reconcile_service.py:390:        supply_id=wb_supply_id,
backend/app/services/fbs_supply_reconcile_service.py:398:    wb_supply_id: str,
backend/app/services/fbs_supply_reconcile_service.py:406:            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_reconcile_service.py:425:    wb_supply_id: str,
backend/app/services/fbs_supply_reconcile_service.py:434:            supply_id=wb_supply_id,
backend/app/models/fbs_supply.py:36:class FbsSupply(Base):
backend/app/models/fbs_supply.py:63:    wb_supply_id: Mapped[str] = mapped_column(String(64), nullable=True, index=True)
backend/app/models/fbs_supply.py:110:    delivered_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
backend/app/services/fbs_supply_validator_service.py:25:class FbsSupplyValidationError(Exception):
backend/app/api/wildberries_integration.py:87:    wb_supply_id: int | None
backend/app/api/wildberries_integration.py:273:            wb_supply_id=int(r.wb_supply_id) if r.wb_supply_id is not None else None,
backend/app/services/fbs_shipment_pvz_service.py:508:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:620:                supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:647:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:655:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:697:        wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:734:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:770:        wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:796:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:818:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:826:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:845:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:942:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_shipment_pvz_service.py:950:                wb_supply_id=supply.wb_supply_id,
backend/app/api/fbs_orders.py:93:    wb_supply_id: str | None
backend/app/api/fbs_supplies.py:41:class FbsSupplyPreflightBody(BaseModel):
backend/app/api/fbs_supplies.py:46:class FbsSupplyPlannedDestinationBody(BaseModel):
backend/app/api/fbs_supplies.py:52:class FbsSupplyFromOrdersBody(BaseModel):
backend/app/api/fbs_supplies.py:60:class FbsSupplyAddOrdersBody(BaseModel):
backend/app/api/fbs_supplies.py:65:class FbsSupplyPreflightIssueOut(BaseModel):
backend/app/api/fbs_supplies.py:71:class FbsSupplyPreflightSummaryOut(BaseModel):
backend/app/api/fbs_supplies.py:84:class FbsSupplyPreflightOut(BaseModel):
backend/app/api/fbs_supplies.py:91:class FbsSupplyCreateBody(BaseModel):
backend/app/api/fbs_supplies.py:100:class FbsSupplyAddOrderBody(BaseModel):
backend/app/api/fbs_supplies.py:104:class FbsSupplyStickersBody(BaseModel):
backend/app/api/fbs_supplies.py:108:class FbsSupplyOrderOut(BaseModel):
backend/app/api/fbs_supplies.py:117:class FbsSupplyOut(BaseModel):
backend/app/api/fbs_supplies.py:121:    wb_supply_id: str
backend/app/api/fbs_supplies.py:133:    delivered_at: str | None
backend/app/api/fbs_supplies.py:139:class FbsSupplyWorklistItemOut(BaseModel):
backend/app/api/fbs_supplies.py:142:    wb_supply_id: str
backend/app/api/fbs_supplies.py:155:class FbsSupplyWorklistOut(BaseModel):
backend/app/api/fbs_supplies.py:357:class FbsSupplyStatusBody(BaseModel):
backend/app/api/fbs_supplies.py:361:class FbsSupplyPlannedShipmentDateBody(BaseModel):
backend/app/api/fbs_supplies.py:365:class FbsSupplyBoxesWithoutDistributionBody(BaseModel):
backend/app/api/fbs_supplies.py:404:    wb_supply_id: str | None
backend/app/api/fbs_supplies.py:529:class FbsSupplyDeliverBody(BaseModel):
backend/app/api/fbs_supplies.py:634:        wb_supply_id=supply.wb_supply_id,
backend/app/api/fbs_supplies.py:652:        delivered_at=supply.delivered_at.isoformat() if supply.delivered_at else None,
backend/app/api/fbs_supplies.py:1009:                exc.code.startswith("wb_") and exc.context.get("wb_supply_id")
backend/app/services/fbs_cancellation_service.py:75:async def reverse_fbs_shipment_if_needed(
backend/app/services/fbs_cancellation_service.py:258:    await reverse_fbs_shipment_if_needed(
backend/app/services/ozon_fbs_sync_service.py:333:                reverse_fbs_shipment_if_needed,
backend/app/services/ozon_fbs_sync_service.py:336:            await reverse_fbs_shipment_if_needed(
backend/app/services/fbs_supply_service.py:128:class FbsSupplyError(Exception):
backend/app/services/fbs_supply_service.py:184:    wb_supply_id: str | None = None,
backend/app/services/fbs_supply_service.py:198:        wb_object_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:202:    if wb_supply_id is not None:
backend/app/services/fbs_supply_service.py:203:        extra["wb_supply_id"] = wb_supply_id
backend/app/services/fbs_supply_service.py:287:    wb_supply_id: str | None = None,
backend/app/services/fbs_supply_service.py:300:    if wb_supply_id is not None:
backend/app/services/fbs_supply_service.py:301:        context["wb_supply_id"] = wb_supply_id
backend/app/services/fbs_supply_service.py:426:    wb_supply_id: str,
backend/app/services/fbs_supply_service.py:442:        wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:465:    wb_supply_id: str,
backend/app/services/fbs_supply_service.py:472:            supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:649:            wb_supply_id=f"PENDING-{operation.id}",
backend/app/services/fbs_supply_service.py:676:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:704:        wb_supply_id_raw = wb_row.get("id")
backend/app/services/fbs_supply_service.py:705:        if wb_supply_id_raw is None:
backend/app/services/fbs_supply_service.py:708:        wb_supply_id = str(wb_supply_id_raw)
backend/app/services/fbs_supply_service.py:709:        supply.wb_supply_id = wb_supply_id
backend/app/services/fbs_supply_service.py:710:        operation.wb_object_id = wb_supply_id
backend/app/services/fbs_supply_service.py:728:                wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:736:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:746:                        wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:753:                            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:761:                        wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:771:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:783:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:792:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:798:                        wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:806:                        "wb_supply_id": wb_supply_id,
backend/app/services/fbs_supply_service.py:817:                wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:824:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:833:                wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:841:            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:852:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:866:                wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:871:                    wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:878:                context={"wb_supply_id": wb_supply_id, "supply_id": str(supply.id)},
backend/app/services/fbs_supply_service.py:887:            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:926:            wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:937:                    wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:946:                    wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:964:                        context={"wb_supply_id": operation.wb_object_id},
backend/app/services/fbs_supply_service.py:973:                    wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:981:                wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:992:                    wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:1000:                context={"wb_supply_id": operation.wb_object_id},
backend/app/services/fbs_supply_service.py:1008:            wb_supply_id=operation.wb_object_id,
backend/app/services/fbs_supply_service.py:1220:    wb_supply_id_raw = wb_row.get("id")
backend/app/services/fbs_supply_service.py:1221:    if wb_supply_id_raw is None:
backend/app/services/fbs_supply_service.py:1223:    wb_supply_id = str(wb_supply_id_raw)
backend/app/services/fbs_supply_service.py:1229:        wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:1338:                "wb_supply_id": supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1420:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1444:    wb_supply_id = (supply.wb_supply_id or "").strip()
backend/app/services/fbs_supply_service.py:1445:    if not wb_supply_id or wb_supply_id.startswith("PENDING-"):
backend/app/services/fbs_supply_service.py:1466:            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:1474:            wb_supply_id=wb_supply_id,
backend/app/services/fbs_supply_service.py:1483:            "wb_supply_id": wb_supply_id,
backend/app/services/fbs_supply_service.py:1512:        order.wb_supply_id = wb_supply_id
backend/app/services/fbs_supply_service.py:1548:            "fbs supply repair from WB: supply=%s wb_supply_id=%s wb_orders=%s "
backend/app/services/fbs_supply_service.py:1551:            wb_supply_id,
backend/app/services/fbs_supply_service.py:1559:        "wb_supply_id": wb_supply_id,
backend/app/services/fbs_supply_service.py:1703:            supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1712:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1834:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1844:                    wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1854:            wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_supply_service.py:1862:            context={"wb_supply_id": supply.wb_supply_id, "supply_id": str(supply.id)},
backend/app/services/fbs_supply_service.py:1964:                wb_supply_id=supply.wb_supply_id,
backend/app/services/fbs_print_asset_service.py:387:                supply_id=supply.wb_supply_id,
backend/app/services/fbs_workspace_service.py:150:            "wb_supply_id": supply.wb_supply_id,
backend/app/services/fbs_tracking_service.py:249:                    supply_id=supply.wb_supply_id,
backend/app/services/fbs_tracking_service.py:397:        .order_by(FbsSupply.delivered_at.asc())
backend/app/services/fbs_tracking_service.py:421:        wb_entry = wb_supplies.get(supply.wb_supply_id)
backend/app/services/fbs_tracking_service.py:437:            skipped.append(f"{supply.wb_supply_id}:{exc.code}")
backend/app/services/fbs_assembly_time_service.py:54:        select(FbsOrder.created_at_wb, FbsSupply.delivered_at)
backend/app/services/fbs_assembly_time_service.py:59:            FbsSupply.delivered_at.is_not(None),
backend/app/services/fbs_assembly_time_service.py:71:        (delivered_at - created_at_wb).total_seconds()
backend/app/services/fbs_assembly_time_service.py:72:        for created_at_wb, delivered_at in rows
backend/app/services/fbs_assembly_time_service.py:73:        if delivered_at is not None and delivered_at >= created_at_wb
backend/app/services/wb_marketplace_orders_service.py:811:    wb_supply_id = _wb_supply_id_from_order_row(row)
backend/app/services/wb_marketplace_orders_service.py:812:    if wb_supply_id is not None:
backend/app/services/wb_marketplace_orders_service.py:813:        existing.wb_supply_id = wb_supply_id
backend/app/services/wb_marketplace_orders_service.py:941:        wb_supply_id=_wb_supply_id_from_order_row(row),
backend/app/services/wb_marketplace_orders_service.py:1087:        from app.services.fbs_cancellation_service import reverse_fbs_shipment_if_needed
backend/app/services/wb_marketplace_orders_service.py:1093:        await reverse_fbs_shipment_if_needed(
backend/app/services/wb_marketplace_orders_service.py:1306:def _wb_supply_id_from_order_row(row: dict[str, Any]) -> str | None:
backend/app/services/wb_marketplace_orders_service.py:1307:    for key in ("supplyId", "supplyID", "supply_id", "wb_supply_id"):
backend/app/services/wb_marketplace_orders_service.py:1317:def _wb_supply_name_from_row(row: dict[str, Any], wb_supply_id: str) -> str:
backend/app/services/wb_marketplace_orders_service.py:1322:    return f"WB supply {wb_supply_id}"
backend/app/services/wb_marketplace_orders_service.py:1344:            FbsOrder.wb_supply_id.is_not(None),
backend/app/services/wb_marketplace_orders_service.py:1358:    wb_supply_id: str,
backend/app/services/wb_marketplace_orders_service.py:1363:        FbsSupply.wb_supply_id == wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1397:    wb_supply_id: str,
backend/app/services/wb_marketplace_orders_service.py:1404:    existing = await _get_existing_supply_by_wb_id(session, tenant_id, seller_id, wb_supply_id)
backend/app/services/wb_marketplace_orders_service.py:1413:            "wb supply link skipped: seller=%s wb_supply_id=%s reason=no_local_warehouse",
backend/app/services/wb_marketplace_orders_service.py:1415:            wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1422:    supply_name = _wb_supply_name_from_row(row, wb_supply_id)
backend/app/services/wb_marketplace_orders_service.py:1425:    if supplies_dict is not None and wb_supply_id in supplies_dict:
backend/app/services/wb_marketplace_orders_service.py:1427:        wb_name, wb_done = supplies_dict[wb_supply_id]
backend/app/services/wb_marketplace_orders_service.py:1438:                supply_id=wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1446:                "wb supply details fetch failed: seller=%s wb_supply_id=%s error=%s",
backend/app/services/wb_marketplace_orders_service.py:1448:                wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1457:        wb_supply_id=wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1496:    """Все поставки селлера одним списком: `{wb_supply_id: (имя, закрыта ли)}`.
backend/app/services/wb_marketplace_orders_service.py:1568:        if local_supply.wb_supply_id in supplies_dict:
backend/app/services/wb_marketplace_orders_service.py:1569:            wb_name, wb_done = supplies_dict[local_supply.wb_supply_id]
backend/app/services/wb_marketplace_orders_service.py:1595:            if order.wb_supply_id is None:
backend/app/services/wb_marketplace_orders_service.py:1597:            wb_supply_id = order.wb_supply_id.strip()
backend/app/services/wb_marketplace_orders_service.py:1598:            if wb_supply_id:
backend/app/services/wb_marketplace_orders_service.py:1599:                candidates_by_supply_id.setdefault(wb_supply_id, []).append(order)
backend/app/services/wb_marketplace_orders_service.py:1610:        for wb_supply_id, matching_orders in candidates_by_supply_id.items():
backend/app/services/wb_marketplace_orders_service.py:1612:                await _get_existing_supply_by_wb_id(session, tenant_id, seller_id, wb_supply_id)
backend/app/services/wb_marketplace_orders_service.py:1619:                wb_supply_id,
backend/app/services/wb_marketplace_orders_service.py:1620:                {"supplyId": wb_supply_id},
backend/app/services/wb_marketplace_orders_service.py:1631:                skipped_unmapped_warehouse_supply_ids.append(wb_supply_id)
backend/app/services/wb_marketplace_orders_service.py:1645:                        "wb supply link skipped: seller=%s wb_supply_id=%s wb_order_id=%s "
backend/app/services/wb_marketplace_orders_service.py:1648:                        wb_supply_id,
backend/app/services/wildberries_import_supplies_service.py:79:                    wb_supply_id=supply_id,
backend/app/services/wildberries_import_supplies_service.py:87:            row.wb_supply_id = supply_id

exec
/bin/zsh -lc 'git status --short --branch && git log -1 --oneline' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
## codex/fix-vozvrat-peredacha-20260829
?? ZADANIE.md
?? docs/evidence/20260829-fiks-vozvrata-posle-peredachi/
7da1b51c fix(catalog): убрать рудиментарную кнопку «Весь остаток на FBS»

exec
/bin/zsh -lc "rg -n \"REVERSAL|reverse_fbs|supplier_status|complete\" backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py && nl -ba backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py | sed -n '200,520p' && sed -n '1,150p' backend/app/models/fbs_supply.py" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
9:Дефект 2 (`reverse_fbs_shipment_if_needed`): при отмене остаток возвращался на
12:статусом поставщика "complete" (собрано и передано).
15:TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
16:TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
51:from app.services.fbs_cancellation_service import reverse_fbs_shipment_if_needed
235:            session, order, "sold", supplier_status="complete", actor_user_id=None
282:            session, order, "sold", supplier_status="complete", actor_user_id=None
321:    supplier_status: str,
337:            sku_code=f"SHIP-{supplier_status}-{suffix[-8:]}",
338:            wb_barcode=f"SHIP-BAR-{supplier_status}-{suffix[-8:]}",
351:            supplier_status=supplier_status,
400:# TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
402:async def test_reversal_blocked_when_supplier_status_complete(
406:    """Дефект 2: `supplier_status == "complete"` значит посылку уже забрал WB.
409:           `reversed_at` пуст), `supplier_status == "complete"` — ровно
421:        async_client, supplier_status="complete"
428:            session, order, "cancel", supplier_status="complete", actor_user_id=None
469:# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
471:@pytest.mark.parametrize("supplier_status", ["new", "confirm"])
472:async def test_reversal_still_happens_when_supplier_status_not_complete(
474:    supplier_status: str,
476:    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
485:        async_client, supplier_status=supplier_status
492:            session, order, "cancel", supplier_status=supplier_status,
522:# TC-NEW-FBS-REVERSAL-COMPLETE-003 — параметр по умолчанию защищает вызов без явного флага
524:async def test_reverse_fbs_shipment_if_needed_default_blocks_complete(
527:    """Прямой вызов `reverse_fbs_shipment_if_needed` без явного параметра
530:    если `supplier_status == "complete"`.
533:        async_client, supplier_status="complete"
539:        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
   200	            movement_type=MOVEMENT_TYPE_INBOUND_INTAKE,
   201	            actor_user_id=None,
   202	        )
   203	        sorting = await get_or_create_sorting_location(session, tenant_id, warehouse_id)
   204	        await inventory_service.transfer_on_hand_between_locations(
   205	            session,
   206	            tenant_id=tenant_id,
   207	            from_storage_location_id=source_location_id,
   208	            to_storage_location_id=sorting.id,
   209	            product_id=product.id,
   210	            quantity=1,
   211	            actor_user_id=None,
   212	        )
   213	        session.add(
   214	            FbsOrderPick(
   215	                tenant_id=tenant_id,
   216	                fbs_order_id=order.id,
   217	                fbs_supply_id=supply.id,
   218	                source_storage_location_id=source_location_id,
   219	                sorting_storage_location_id=sorting.id,
   220	                product_id=product.id,
   221	                picked_at=datetime.now(UTC),
   222	                scan_idempotency_key=f"pick-{order.id}",
   223	            )
   224	        )
   225	        await session.commit()
   226	        order_id = order.id
   227	        product_id = product.id
   228	        sorting_id = sorting.id
   229	
   230	    # Первый обход синка статусов: заказ выкуплен.
   231	    async with SessionLocal() as session:
   232	        order = await session.get(FbsOrder, order_id)
   233	        assert order is not None
   234	        await _apply_wb_status_to_order(
   235	            session, order, "sold", supplier_status="complete", actor_user_id=None
   236	        )
   237	        await session.commit()
   238	
   239	    async with SessionLocal() as session:
   240	        order = await session.get(FbsOrder, order_id)
   241	        assert order is not None
   242	        assert order.status == FBS_ORDER_STATUS_DONE
   243	
   244	        ledgers = list(
   245	            (
   246	                await session.execute(
   247	                    select(FbsShipmentReversalLedger).where(
   248	                        FbsShipmentReversalLedger.fbs_order_id == order_id
   249	                    )
   250	                )
   251	            ).scalars()
   252	        )
   253	        assert len(ledgers) == 1
   254	        assert ledgers[0].quantity == 1
   255	        assert ledgers[0].storage_location_id == sorting_id
   256	        assert ledgers[0].reversed_at is None
   257	
   258	        write_off_count = await session.scalar(
   259	            select(func.count(InventoryMovement.id)).where(
   260	                InventoryMovement.tenant_id == tenant_id,
   261	                InventoryMovement.product_id == product_id,
   262	                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
   263	            )
   264	        )
   265	        assert write_off_count == 1
   266	
   267	        balance = await session.scalar(
   268	            select(InventoryBalance).where(
   269	                InventoryBalance.tenant_id == tenant_id,
   270	                InventoryBalance.product_id == product_id,
   271	                InventoryBalance.storage_location_id == sorting_id,
   272	            )
   273	        )
   274	        assert balance is not None
   275	        assert int(balance.quantity) == 0
   276	
   277	    # Второй обход синка статусов с тем же статусом — второго списания быть не должно.
   278	    async with SessionLocal() as session:
   279	        order = await session.get(FbsOrder, order_id)
   280	        assert order is not None
   281	        await _apply_wb_status_to_order(
   282	            session, order, "sold", supplier_status="complete", actor_user_id=None
   283	        )
   284	        await session.commit()
   285	
   286	    async with SessionLocal() as session:
   287	        ledgers = list(
   288	            (
   289	                await session.execute(
   290	                    select(FbsShipmentReversalLedger).where(
   291	                        FbsShipmentReversalLedger.fbs_order_id == order_id
   292	                    )
   293	                )
   294	            ).scalars()
   295	        )
   296	        assert len(ledgers) == 1, "повторный обход не должен создавать вторую запись"
   297	
   298	        write_off_count = await session.scalar(
   299	            select(func.count(InventoryMovement.id)).where(
   300	                InventoryMovement.tenant_id == tenant_id,
   301	                InventoryMovement.product_id == product_id,
   302	                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
   303	            )
   304	        )
   305	        assert write_off_count == 1, "повторный обход не должен списывать второй раз"
   306	
   307	        balance = await session.scalar(
   308	            select(InventoryBalance).where(
   309	                InventoryBalance.tenant_id == tenant_id,
   310	                InventoryBalance.product_id == product_id,
   311	                InventoryBalance.storage_location_id == sorting_id,
   312	            )
   313	        )
   314	        assert balance is not None
   315	        assert int(balance.quantity) == 0
   316	
   317	
   318	async def _seed_order_with_existing_write_off(
   319	    async_client: AsyncClient,
   320	    *,
   321	    supplier_status: str,
   322	) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
   323	    """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
   324	
   325	    Возвращает (order_id, tenant_id, product_id, storage_location_id).
   326	    """
   327	    headers, suffix, tenant_id = await _register_ff_admin(async_client)
   328	    seller_id, warehouse_id, location_id = await _setup_seller_with_token(
   329	        async_client, headers, suffix
   330	    )
   331	
   332	    async with SessionLocal() as session:
   333	        product = Product(
   334	            tenant_id=tenant_id,
   335	            seller_id=seller_id,
   336	            name="Already shipped product",
   337	            sku_code=f"SHIP-{supplier_status}-{suffix[-8:]}",
   338	            wb_barcode=f"SHIP-BAR-{supplier_status}-{suffix[-8:]}",
   339	        )
   340	        session.add(product)
   341	        await session.flush()
   342	
   343	        now = datetime.now(UTC)
   344	        order = FbsOrder(
   345	            tenant_id=tenant_id,
   346	            seller_id=seller_id,
   347	            warehouse_id=warehouse_id,
   348	            product_id=product.id,
   349	            wb_order_id=int(f"98{suffix[-6:]}"),
   350	            status=FBS_ORDER_STATUS_PACKED,
   351	            supplier_status=supplier_status,
   352	            created_at_wb=now,
   353	            deadline_at=now + timedelta(hours=24),
   354	            mapping_status=MAPPING_STATUS_MAPPED,
   355	            reserve_status=RESERVE_STATUS_NO_STOCK,
   356	        )
   357	        session.add(order)
   358	        await session.flush()
   359	
   360	        # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
   361	        # что делает `apply_fbs_supply_write_off` при завершении поставки.
   362	        await inventory_service.record_movement_and_adjust_balance(
   363	            session,
   364	            tenant_id=tenant_id,
   365	            product_id=product.id,
   366	            storage_location_id=location_id,
   367	            quantity_delta=1,
   368	            movement_type=MOVEMENT_TYPE_INBOUND_INTAKE,
   369	            actor_user_id=None,
   370	        )
   371	        write_off = await inventory_service.apply_fbs_supply_write_off(
   372	            session,
   373	            tenant_id=tenant_id,
   374	            product_id=product.id,
   375	            storage_location_id=location_id,
   376	            quantity=1,
   377	            actor_user_id=None,
   378	        )
   379	        await session.flush()
   380	        shipment_movement_id = write_off.id
   381	        session.add(
   382	            FbsShipmentReversalLedger(
   383	                tenant_id=tenant_id,
   384	                fbs_order_id=order.id,
   385	                product_id=product.id,
   386	                storage_location_id=location_id,
   387	                quantity=1,
   388	                # Возврат делается только по записи, которая помнит движение
   389	                # списания: без него сторнировать нечего.
   390	                shipment_movement_id=shipment_movement_id,
   391	            )
   392	        )
   393	        await session.commit()
   394	        order_id = order.id
   395	        product_id = product.id
   396	
   397	    return order_id, tenant_id, product_id, location_id
   398	
   399	
   400	# TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
   401	@pytest.mark.asyncio
   402	async def test_reversal_blocked_when_supplier_status_complete(
   403	    async_client: AsyncClient,
   404	    caplog: pytest.LogCaptureFixture,
   405	) -> None:
   406	    """Дефект 2: `supplier_status == "complete"` значит посылку уже забрал WB.
   407	
   408	    Given: заказ уже списан (запись в `fbs_shipment_reversal_ledger` есть,
   409	           `reversed_at` пуст), `supplier_status == "complete"` — ровно
   410	           признак, по которому на бою нашли 275 ложных возвратов.
   411	    When: WB присылает по этому заказу статус отмены.
   412	    Then: остаток на складе НЕ возвращается (баланс и число движений
   413	          `fbs_shipment` не меняются), запись журнала помечается обработанной
   414	          (`reversed_at` проставлен), но без движения (`reversal_movement_id`
   415	          пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
   416	          ограничение: в лог пишется предупреждение с номером заказа, продавцом
   417	          и товаром, чтобы такие случаи были видны.
   418	    """
   419	    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
   420	    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
   421	        async_client, supplier_status="complete"
   422	    )
   423	
   424	    async with SessionLocal() as session:
   425	        order = await session.get(FbsOrder, order_id)
   426	        assert order is not None
   427	        await _apply_wb_status_to_order(
   428	            session, order, "cancel", supplier_status="complete", actor_user_id=None
   429	        )
   430	        await session.commit()
   431	
   432	    assert "already handed to WB" in caplog.text
   433	    assert str(order_id) in caplog.text
   434	
   435	    async with SessionLocal() as session:
   436	        order = await session.get(FbsOrder, order_id)
   437	        assert order is not None
   438	        assert order.status == FBS_ORDER_STATUS_CANCELLED
   439	
   440	        ledger = await session.scalar(
   441	            select(FbsShipmentReversalLedger).where(
   442	                FbsShipmentReversalLedger.fbs_order_id == order_id
   443	            )
   444	        )
   445	        assert ledger is not None
   446	        assert ledger.reversed_at is not None, "запись должна быть помечена обработанной"
   447	        assert ledger.reversal_movement_id is None, "движения по складу быть не должно"
   448	
   449	        movement_count = await session.scalar(
   450	            select(func.count(InventoryMovement.id)).where(
   451	                InventoryMovement.tenant_id == tenant_id,
   452	                InventoryMovement.product_id == product_id,
   453	                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
   454	            )
   455	        )
   456	        assert movement_count == 1, "должно остаться только исходное списание"
   457	
   458	        balance = await session.scalar(
   459	            select(InventoryBalance).where(
   460	                InventoryBalance.tenant_id == tenant_id,
   461	                InventoryBalance.product_id == product_id,
   462	                InventoryBalance.storage_location_id == location_id,
   463	            )
   464	        )
   465	        assert balance is not None
   466	        assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
   467	
   468	
   469	# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
   470	@pytest.mark.asyncio
   471	@pytest.mark.parametrize("supplier_status", ["new", "confirm"])
   472	async def test_reversal_still_happens_when_supplier_status_not_complete(
   473	    async_client: AsyncClient,
   474	    supplier_status: str,
   475	) -> None:
   476	    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
   477	    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
   478	    When: WB присылает статус отмены.
   479	    Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
   480	          растёт на 1), запись журнала помечена обработанной с заполненным
   481	          `reversal_movement_id` — старое поведение для этого случая не
   482	          меняется.
   483	    """
   484	    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
   485	        async_client, supplier_status=supplier_status
   486	    )
   487	
   488	    async with SessionLocal() as session:
   489	        order = await session.get(FbsOrder, order_id)
   490	        assert order is not None
   491	        await _apply_wb_status_to_order(
   492	            session, order, "cancel", supplier_status=supplier_status,
   493	            actor_user_id=None,
   494	        )
   495	        await session.commit()
   496	
   497	    async with SessionLocal() as session:
   498	        order = await session.get(FbsOrder, order_id)
   499	        assert order is not None
   500	        assert order.status == FBS_ORDER_STATUS_CANCELLED
   501	
   502	        ledger = await session.scalar(
   503	            select(FbsShipmentReversalLedger).where(
   504	                FbsShipmentReversalLedger.fbs_order_id == order_id
   505	            )
   506	        )
   507	        assert ledger is not None
   508	        assert ledger.reversed_at is not None
   509	        assert ledger.reversal_movement_id is not None
   510	
   511	        balance = await session.scalar(
   512	            select(InventoryBalance).where(
   513	                InventoryBalance.tenant_id == tenant_id,
   514	                InventoryBalance.product_id == product_id,
   515	                InventoryBalance.storage_location_id == location_id,
   516	            )
   517	        )
   518	        assert balance is not None
   519	        assert int(balance.quantity) == 1, "остаток должен вернуться — товар ещё у нас"
   520	
from __future__ import annotations

import uuid
from datetime import date, datetime
from typing import TYPE_CHECKING

from sqlalchemy import Date, DateTime, ForeignKey, Integer, String, UniqueConstraint, Uuid, func
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base

if TYPE_CHECKING:
    from app.models.fbs_order import FbsOrder
    from app.models.fbs_print_asset import FbsPrintAsset
    from app.models.fbs_trbx import FbsTrbx
    from app.models.seller import Seller
    from app.models.tenant import Tenant
    from app.models.warehouse import Warehouse

FBS_SUPPLY_STATUS_DRAFT = "draft"
FBS_SUPPLY_STATUS_ASSEMBLING = "assembling"
FBS_SUPPLY_STATUS_PACKED = "packed"
FBS_SUPPLY_STATUS_IN_DELIVERY = "in_delivery"
FBS_SUPPLY_STATUS_DONE = "done"

FBS_SUPPLY_SOURCE_WMS = "wms"
FBS_SUPPLY_SOURCE_WB = "wb"

FBS_DELIVERY_TYPE_WAREHOUSE_SC = "warehouse_sc"
FBS_DELIVERY_TYPE_PVZ = "pvz"

FBS_SUPPLY_SOURCE_WMS = "wms"
FBS_SUPPLY_SOURCE_WB = "wb"


class FbsSupply(Base):
    __tablename__ = "fbs_supplies"
    __table_args__ = (
        UniqueConstraint(
            "seller_id",
            "marketplace",
            "external_supply_id",
            name="uq_fbs_supplies_seller_marketplace_external_supply",
        ),
    )

    id: Mapped[uuid.UUID] = mapped_column(Uuid(as_uuid=True), primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("tenants.id", ondelete="CASCADE"), index=True
    )
    seller_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("sellers.id", ondelete="CASCADE"), index=True
    )
    warehouse_id: Mapped[uuid.UUID] = mapped_column(
        Uuid(as_uuid=True), ForeignKey("warehouses.id", ondelete="CASCADE"), index=True
    )
    marketplace: Mapped[str] = mapped_column(
        String(32), nullable=False, default="wb", server_default="wb", index=True
    )
    external_supply_id: Mapped[str | None] = mapped_column(String(255), nullable=True, index=True)
    # Nullable in storage for Ozon; WB service paths establish a non-empty value
    # before calling WB-specific helpers.
    wb_supply_id: Mapped[str] = mapped_column(String(64), nullable=True, index=True)
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    source: Mapped[str] = mapped_column(
        String(16),
        nullable=False,
        default=FBS_SUPPLY_SOURCE_WMS,
        server_default=FBS_SUPPLY_SOURCE_WMS,
    )
    status: Mapped[str] = mapped_column(
        String(32),
        nullable=False,
        default=FBS_SUPPLY_STATUS_DRAFT,
        server_default=FBS_SUPPLY_STATUS_DRAFT,
    )
    delivery_type: Mapped[str] = mapped_column(String(32), nullable=False)
    cargo_type: Mapped[str | None] = mapped_column(String(16), nullable=True)
    wb_office_id: Mapped[int | None] = mapped_column(Integer, nullable=True)
    barcode_file: Mapped[str | None] = mapped_column(String(512), nullable=True)
    document_number: Mapped[str | None] = mapped_column(String(64), nullable=True)
    display_number: Mapped[str | None] = mapped_column(String(64), nullable=True)
    planned_destination_office_id: Mapped[int | None] = mapped_column(Integer, nullable=True)
    planned_destination_name: Mapped[str | None] = mapped_column(String(255), nullable=True)
    planned_destination_zone: Mapped[str | None] = mapped_column(String(128), nullable=True)
    planned_shipment_date: Mapped[date | None] = mapped_column(Date, nullable=True)
    last_wb_sync_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    barcode_asset_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        # The database FK is created in migration 0069 under this name.  Keep
        # it deferred in SQLAlchemy metadata: print assets point back to
        # supplies, so PostgreSQL needs an ALTER TABLE step when dropping the
        # test schema.
        ForeignKey(
            "fbs_print_assets.id",
            name="fk_fbs_supplies_barcode_asset_id",
            ondelete="SET NULL",
            use_alter=True,
        ),
        nullable=True,
        index=True,
    )
    packaging_task_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("packaging_tasks.id", ondelete="SET NULL"),
        nullable=True,
        index=True,
    )
    created_at_wb: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    delivered_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        server_default=func.now(),
        onupdate=func.now(),
        nullable=False,
    )
    honest_sign_skipped_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    honest_sign_skipped_by_user_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("users.id", ondelete="SET NULL"),
        nullable=True,
    )
    boxes_without_distribution_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True), nullable=True
    )
    boxes_without_distribution_by_user_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid(as_uuid=True),
        ForeignKey("users.id", ondelete="SET NULL"),
        nullable=True,
    )

    tenant: Mapped[Tenant] = relationship("Tenant")
    seller: Mapped[Seller] = relationship("Seller")
    warehouse: Mapped[Warehouse] = relationship("Warehouse")
    orders: Mapped[list[FbsOrder]] = relationship(
        "FbsOrder",
        back_populates="supply",
    )
    trbxes: Mapped[list[FbsTrbx]] = relationship(
        "FbsTrbx",
        back_populates="supply",
        cascade="all, delete-orphan",
    )
    barcode_asset: Mapped[FbsPrintAsset | None] = relationship(
        "FbsPrintAsset",

exec
/bin/zsh -lc "nl -ba backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py | sed -n '518,610p' && rg -n \"_seed_order_with_existing_write_off\" backend/tests" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
   518	        assert balance is not None
   519	        assert int(balance.quantity) == 1, "остаток должен вернуться — товар ещё у нас"
   520	
   521	
   522	# TC-NEW-FBS-REVERSAL-COMPLETE-003 — параметр по умолчанию защищает вызов без явного флага
   523	@pytest.mark.asyncio
   524	async def test_reverse_fbs_shipment_if_needed_default_blocks_complete(
   525	    async_client: AsyncClient,
   526	) -> None:
   527	    """Прямой вызов `reverse_fbs_shipment_if_needed` без явного параметра
   528	    (как это делает ручная отмена оператором в `cancel_order`) обязан
   529	    использовать безопасное значение по умолчанию и не возвращать остаток,
   530	    если `supplier_status == "complete"`.
   531	    """
   532	    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
   533	        async_client, supplier_status="complete"
   534	    )
   535	
   536	    async with SessionLocal() as session:
   537	        order = await session.get(FbsOrder, order_id)
   538	        assert order is not None
   539	        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
   540	        await session.commit()
   541	
   542	    assert reversed_flag is False
   543	
   544	    async with SessionLocal() as session:
   545	        balance = await session.scalar(
   546	            select(InventoryBalance).where(
   547	                InventoryBalance.tenant_id == tenant_id,
   548	                InventoryBalance.product_id == product_id,
   549	                InventoryBalance.storage_location_id == location_id,
   550	            )
   551	        )
   552	        assert balance is not None
   553	        assert int(balance.quantity) == 0
backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:318:async def _seed_order_with_existing_write_off(
backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:420:    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:484:    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:532:    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(

apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/app/services/fbs_cancellation_service.py
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

exec
/bin/zsh -lc 'git diff -- backend/app/services/fbs_cancellation_service.py backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py && python3 -m compileall -q backend/app/services/fbs_cancellation_service.py backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3c..601a2b3e 100644
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@ from app.models.fbs_order import (
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@ async def reverse_fbs_shipment_if_needed(
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@ async def reverse_fbs_shipment_if_needed(
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36bea..154465a1 100644
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@ async def _seed_order_with_existing_write_off(
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@ async def _seed_order_with_existing_write_off(
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@ async def _seed_order_with_existing_write_off(
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@ async def test_reversal_blocked_when_supplier_status_complete(
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@ async def test_reversal_still_happens_when_supplier_status_not_complete(
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

codex
Правка внесена без изменения Ozon-ветки и передачи `actor_user_id`. Новый тест специально оставляет `order.supply_id` пустым, связывает заказ только через `order.wb_supply_id` и проверяет одновременно результат `False`, отсутствие нового движения, `reversed_at` и предупреждение в логе; негативный контроль теперь создаёт реальную непереданную поставку с пустым `delivered_at`. Запускаю адресные тесты.
exec
/bin/zsh -lc 'python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:23:42.336694Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
 exited 1 in 37908ms:
..F....................................                                  [100%]
=================================== FAILURES ===================================
_____________ test_reversal_blocked_when_supplier_status_complete ______________

async_client = <httpx.AsyncClient object at 0x11813ac30>
caplog = <_pytest.logging.LogCaptureFixture object at 0x1181e8cd0>

    @pytest.mark.asyncio
    async def test_reversal_blocked_when_supplier_status_complete(
        async_client: AsyncClient,
        caplog: pytest.LogCaptureFixture,
    ) -> None:
        """Дефект 2: `supplier_status == "complete"` значит посылку уже забрал WB.
    
        Given: заказ уже списан (запись в `fbs_shipment_reversal_ledger` есть,
               `reversed_at` пуст), `supplier_status == "complete"` — ровно
               признак, по которому на бою нашли 275 ложных возвратов.
        When: WB присылает по этому заказу статус отмены.
        Then: остаток на складе НЕ возвращается (баланс и число движений
              `fbs_shipment` не меняются), запись журнала помечается обработанной
              (`reversed_at` проставлен), но без движения (`reversal_movement_id`
              пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
              ограничение: в лог пишется предупреждение с номером заказа, продавцом
              и товаром, чтобы такие случаи были видны.
        """
        caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
        order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
            async_client, supplier_status="complete"
        )
    
        async with SessionLocal() as session:
            order = await session.get(FbsOrder, order_id)
            assert order is not None
            await _apply_wb_status_to_order(
                session, order, "cancel", supplier_status="complete", actor_user_id=None
            )
            await session.commit()
    
>       assert "already handed to WB" in caplog.text
E       AssertionError: assert 'already handed to WB' in 'WARNING  app.services.fbs_cancellation_service:fbs_cancellation_service.py:134 fbs_reversal_skipped_after_handover order=98723000 supply=None delivered=False status=complete\n'
E        +  where 'WARNING  app.services.fbs_cancellation_service:fbs_cancellation_service.py:134 fbs_reversal_skipped_after_handover order=98723000 supply=None delivered=False status=complete\n' = <_pytest.logging.LogCaptureFixture object at 0x1181e8cd0>.text

tests/test_fbs_writeoff_sold_and_reversal_guard.py:515: AssertionError
------------------------------ Captured log call -------------------------------
WARNING  app.services.fbs_cancellation_service:fbs_cancellation_service.py:134 fbs_reversal_skipped_after_handover order=98723000 supply=None delivered=False status=complete
=========================== short test summary info ============================
FAILED tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_blocked_when_supplier_status_complete
1 failed, 38 passed in 31.71s

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..154465a1b891f1d13cad5e7e758ef3cbd8a2ba6c
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -466,15 +549,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +565,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:23:45.813836Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Адресный прогон нашёл один ожидаемый разрыв в старом тесте: лог теперь имеет согласованный машиночитаемый текст `fbs_reversal_skipped_after_handover`, а тест всё ещё искал прежнюю английскую фразу. Сам сторож сработал правильно; обновляю только это устаревшее ожидание и описание состава лога, затем повторю оба тестовых файла.
apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:23:55.388899Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:23:59.949210Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:24:13.703329Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

codex
Повторный прогон идёт заметно дольше первого и пока не выдал новой ошибки. Процесс не прерываю: дождусь фактического результата, после чего отдельно выполню `ruff` и `mypy`.
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:26:37.578277Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:26:56.590659Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:27:04.234633Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

2026-08-29T15:27:14.065082Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
 exited 2 in 202379ms:
....
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! KeyboardInterrupt !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
/Library/Frameworks/Python.framework/Versions/3.14/lib/python3.14/asyncio/runners.py:166: KeyboardInterrupt
(to show a full traceback on KeyboardInterrupt use --full-trace)
4 passed in 196.68s (0:03:16)

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

codex
Повторный полный адресный прогон завис после четырёх успешных тестов без traceback; я остановил его через 3:16, чтобы не выдавать зависание за результат. Первый прогон завершился штатно за 31,71 секунды и показал только уже исправленное ожидание текста лога. Сейчас локализую зависший тест отдельным запуском — это позволит отличить дефект правки от нестабильности общей тестовой фикстуры.
exec
/bin/zsh -lc 'python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py --collect-only -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
 succeeded in 4866ms:
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_write_off_sold_order_that_skipped_packaging_happens_once
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_blocked_when_supplier_status_complete
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_still_happens_when_supplier_status_not_complete[new]
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_still_happens_when_supplier_status_not_complete[confirm]
tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reverse_fbs_shipment_if_needed_default_blocks_complete

6 tests collected in 0.03s

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

exec
/bin/zsh -lc "python3 -m pytest 'tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_still_happens_when_supplier_status_not_complete[confirm]' -q -vv" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

 succeeded in 7487ms:
============================= test session starts ==============================
platform darwin -- Python 3.14.3, pytest-9.0.2, pluggy-1.6.0 -- /Library/Frameworks/Python.framework/Versions/3.14/bin/python3
cachedir: .pytest_cache
rootdir: /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
configfile: pyproject.toml
plugins: anyio-4.12.1, cov-7.1.0, asyncio-1.3.0
asyncio: mode=Mode.AUTO, debug=False, asyncio_default_fixture_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 1 item

tests/test_fbs_writeoff_sold_and_reversal_guard.py::test_reversal_still_happens_when_supplier_status_not_complete[confirm] PASSED [100%]

============================== 1 passed in 2.04s ===============================

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

exec
/bin/zsh -lc 'ruff check . && mypy .' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

 succeeded in 20905ms:
All checks passed!
Success: no issues found in 390 source files

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

codex
Локальный запуск конкретно зависшего негативного контроля прошёл: `1 passed in 2.04s`. Статические ворота тоже зелёные: `ruff` без замечаний, `mypy` — без ошибок по 390 файлам. Повторяю требуемую команду двух адресных файлов целиком, чтобы итог опирался именно на полный указанный набор.
exec
/bin/zsh -lc 'python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

 succeeded in 35648ms:
.......................................                                  [100%]
39 passed in 30.54s

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

codex
Требуемый адресный набор завершился штатно: `39 passed in 30.54s`; Ozon-регрессии входят в этот результат. Теперь оформляю итоговый журнал, затем ещё раз проверю только свой diff и сохраню его отдельным коммитом, не добавляя пользовательские `ZADANIE.md` и каталог с исходной фактурой.
exec
/bin/zsh -lc 'ls -ld tasks/_zhurnal/codex-briefs && git diff --check && git status --short' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
drwxr-xr-x@ 7 deniscivkunov  staff  224 Aug 29 18:21 tasks/_zhurnal/codex-briefs
 M backend/app/services/fbs_cancellation_service.py
 M backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
?? ZADANIE.md
?? docs/evidence/20260829-fiks-vozvrata-posle-peredachi/

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

apply patch
patch: completed
/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

exec
/bin/zsh -lc 'git diff --check && git diff --stat && git diff -- backend/app/services/fbs_cancellation_service.py backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
 backend/app/services/fbs_cancellation_service.py   |  42 ++++++---
 .../test_fbs_writeoff_sold_and_reversal_guard.py   | 102 +++++++++++++++++++--
 2 files changed, 124 insertions(+), 20 deletions(-)
diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3c..601a2b3e 100644
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@ from app.models.fbs_order import (
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@ async def reverse_fbs_shipment_if_needed(
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@ async def reverse_fbs_shipment_if_needed(
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36bea..981c04e8 100644
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@ async def _seed_order_with_existing_write_off(
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@ async def _seed_order_with_existing_write_off(
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@ async def _seed_order_with_existing_write_off(
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@ async def test_reversal_blocked_when_supplier_status_complete(
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@ async def test_reversal_blocked_when_supplier_status_complete(
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@ async def test_reversal_blocked_when_supplier_status_complete(
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@ async def test_reversal_still_happens_when_supplier_status_not_complete(
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

exec
/bin/zsh -lc 'git add backend/app/services/fbs_cancellation_service.py backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md && git diff --cached --check && git diff --cached --stat && git status --short' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 7ms:
M  backend/app/services/fbs_cancellation_service.py
M  backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
A  tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
?? ZADANIE.md
?? docs/evidence/20260829-fiks-vozvrata-posle-peredachi/

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

exec
/bin/zsh -lc 'git commit -m "Исправить возврат после передачи поставки WB"' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
[codex/fix-vozvrat-peredacha-20260829 ccb76693] Исправить возврат после передачи поставки WB
 3 files changed, 172 insertions(+), 20 deletions(-)
 create mode 100644 tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

2026-08-29T15:29:36.193721Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Изменение сохранено отдельным коммитом `ccb76693`; в него вошли ровно сервис, адресный тест и итоговый журнал. Пользовательские неотслеживаемые `ZADANIE.md` и папка с фактурой в коммит не попали. Сейчас проверяю SHA и публикую именованную ветку в remote; merge и выкатку не выполняю.
exec
/bin/zsh -lc 'git status --short --branch && git rev-parse HEAD && git show --stat --oneline --decorate --no-renames HEAD' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 21ms:
ccb76693 (HEAD -> codex/fix-vozvrat-peredacha-20260829) Исправить возврат после передачи поставки WB
 backend/app/services/fbs_cancellation_service.py   |  42 ++++++---
 .../test_fbs_writeoff_sold_and_reversal_guard.py   | 102 +++++++++++++++++++--
 .../codex-briefs/ITOG-vozvrat-peredacha.md         |  48 ++++++++++
 3 files changed, 172 insertions(+), 20 deletions(-)

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

2026-08-29T15:29:45.433911Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
exec
/bin/zsh -lc 'git push -u origin codex/fix-vozvrat-peredacha-20260829' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 exited 128 in 0ms:
fatal: unable to access 'https://github.com/chivkunovd-bitdenis/WMS.git/': Could not resolve host: github.com

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

2026-08-29T15:29:49.017203Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Публикация ветки не состоялась: окружение не может разрешить `github.com` (`Could not resolve host`). Коммит сохранён локально и восстановим по полному SHA, но ветка не опубликована, поэтому статус не называю «готово». Финально проверяю, что после коммита нет незакоммиченного diff в наших файлах и что остались только исходные пользовательские материалы.
exec
/bin/zsh -lc "nl -ba /Users/deniscivkunov/.codex/memories/MEMORY.md | sed -n '1490,1508p'" in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
  1490	- extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md, updated_at=2026-08-23T06:00:00+00:00, thread_id=None, backend-vs-frontend diagnosis and deploy/rollback context)
  1491	
  1492	### keywords
  1493	
  1494	- `scan latency`, `15-20 seconds`, `276 rows`, `116 ms`, `React.memo`, `FfInboundBoxAddDialog.tsx`, `FfInboundRequestView.tsx`, `hotfix/scan-render-20260823`, `4.9 seconds`, `15.5 seconds`, `E2E_WEB_MODE=preview`
  1495	
  1496	## Task 3: Explain WB FBS orders `5532200010` and `5532200011` to a client, outcome evidence-based wording prepared
  1497	
  1498	### rollout_summary_files
  1499	
  1500	- extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T10-50-00-pMPZ-10min-memory-summary.md, updated_at=2026-08-23T10:50:00+00:00, thread_id=None, observed box-level WB acceptance evidence)
  1501	- extensions/skysight/resources/2026-08-23T10-40-00-rzZE-10min-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T10-40-00-rzZE-10min-memory-summary.md, updated_at=2026-08-23T10:40:00+00:00, thread_id=None, observed request for a short non-technical explanation)
  1502	- extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md (cwd=/Users/deniscivkunov/Projects/WMS, rollout_path=extensions/skysight/resources/2026-08-23T06-00-00-GhdT-6h-memory-summary.md, updated_at=2026-08-23T06:00:00+00:00, thread_id=None, supply/status context)
  1503	
  1504	### keywords
  1505	
  1506	- `5532200010`, `5532200011`, `WB-GI-267347087`, `supplierStatus=complete`, `wbStatus=waiting`, `FBS-91FBDA87-001`, `WB-MP-50687347`, `FBS-91FBDA87-023`, `WB-MP-50687369`, `Отсортировано`, `Отгрузите поставку`
  1507	
  1508	## Task 4: Check whether final catalog boxes work is integrated on stage, outcome incomplete version observed; redeploy completion not captured

exec
/bin/zsh -lc 'git status --short --branch && git diff HEAD -- backend/app/services/fbs_cancellation_service.py backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md && git rev-parse HEAD' in /Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat
 succeeded in 0ms:
## codex/fix-vozvrat-peredacha-20260829
?? ZADANIE.md
?? docs/evidence/20260829-fiks-vozvrata-posle-peredachi/
ccb7669354ad21c2396f71e90dbcbbcd3d19ee09

diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

2026-08-29T15:30:01.224679Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `supports_parallel_tool_calls` at line 97 column 5
codex
Правка локально реализована, проверена и сохранена в Git:

- Ветка: `codex/fix-vozvrat-peredacha-20260829`
- Коммит: `ccb7669354ad21c2396f71e90dbcbbcd3d19ee09`
- [Сервис](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/app/services/fbs_cancellation_service.py:75)
- [Тесты](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:419)
- [Итоговый журнал](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md:1)

Проверки:

- `ruff check .` — успешно.
- `mypy .` — успешно, 390 файлов.
- Два указанных тестовых файла — `39 passed in 30.54s`.
- Полный `pytest` не запускался.
- Фронт, Ozon-логика и `actor_user_id` не изменялись.

Ветка не опубликована: `git push` завершился ошибкой DNS — окружение не смогло разрешить `github.com`. Merge и выкатка не выполнялись. `ZADANIE.md` и каталог с исходной фактурой остались вне коммита.


diff --git a/backend/app/services/fbs_cancellation_service.py b/backend/app/services/fbs_cancellation_service.py
index 9e07cb3cc12af06588e4a06c07f93d5480a493f6..601a2b3ed079c4f657d61e1e2f4323cc62bf46ca
--- a/backend/app/services/fbs_cancellation_service.py
+++ b/backend/app/services/fbs_cancellation_service.py
@@ -19,6 +19,7 @@
     FbsOrder,
 )
 from app.models.fbs_shipment_reversal_ledger import FbsShipmentReversalLedger
+from app.models.fbs_supply import FbsSupply
 from app.models.inventory_movement import MOVEMENT_TYPE_FBS_SHIPMENT
 from app.services import inventory_service as inv_svc
 from app.services.wb_marketplace_orders_service import (
@@ -84,12 +85,10 @@
     `skip_if_supplier_complete` — параметр, а не жёстко зашитая проверка внутри,
     потому что у функции два вызывающих (ручная отмена оператором и обработка
     статусов от WB) и им может понадобиться разное поведение в будущем. Сейчас
-    оба вызова используют безопасное значение по умолчанию: если WB уже
-    подтвердил `supplier_status == "complete"`, посылка физически передана WB,
-    и возвращать штуку на склад нельзя, иначе остаток завышается на товар,
-    которого на складе нет. На бою так нашли 275 ложных возвратов — 257 из них
-    пришли позже чем через двое суток после упаковки, то есть посылка была уже
-    у WB.
+    оба вызова используют безопасное значение по умолчанию: если поставка уже
+    передана WB (`FbsSupply.delivered_at` заполнен) либо WB ещё сообщает
+    `supplier_status == "complete"`, возвращать штуку на склад нельзя, иначе
+    остаток завышается на товар, которого на складе нет.
     """
     stmt = (
         select(FbsShipmentReversalLedger)
@@ -107,19 +106,38 @@
     ):
         return False
 
+    # Факт передачи поставки — надёжный признак того, что товар физически
+    # покинул склад. `order.supplier_status` для этого недостаточен: WB после
+    # передачи меняет его на `cancel`/`confirm`, и проверка только на
+    # `complete` перестаёт срабатывать.
+    supply_delivered = False
+    if order.wb_supply_id:
+        supply = (
+            await session.execute(
+                select(FbsSupply).where(
+                    FbsSupply.tenant_id == order.tenant_id,
+                    FbsSupply.wb_supply_id == order.wb_supply_id,
+                )
+            )
+        ).scalar_one_or_none()
+        supply_delivered = supply is not None and supply.delivered_at is not None
+
     supplier_status = (order.supplier_status or "").strip().lower()
-    if skip_if_supplier_complete and supplier_status == SUPPLIER_STATUS_COMPLETE:
+    already_handed_over = (
+        supply_delivered or supplier_status == SUPPLIER_STATUS_COMPLETE
+    )
+    if skip_if_supplier_complete and already_handed_over:
         # Посылка уже у WB — трогать склад нельзя. Помечаем запись журнала
         # обработанной без движения по складу (reversal_movement_id остаётся
         # пустым), иначе она будет пытаться вернуться на каждом следующем
         # обходе синка статусов.
         logger.warning(
-            "fbs shipment reversal skipped: parcel already handed to WB "
-            "order_id=%s wb_order_id=%s seller_id=%s product_id=%s",
-            order.id,
+            "fbs_reversal_skipped_after_handover order=%s supply=%s "
+            "delivered=%s status=%s",
             order.wb_order_id,
-            order.seller_id,
-            ledger.product_id,
+            order.wb_supply_id,
+            supply_delivered,
+            supplier_status,
         )
         ledger.reversed_at = datetime.now(UTC)
         await session.flush()
diff --git a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
index 47c36beae47277b7eda9d9019e7054addabba531..981c04e82ac436b5ab27397e4ecbf46bff6028e9
--- a/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
+++ b/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py
@@ -14,6 +14,7 @@
 TC-NEW-FBS-WRITEOFF-SOLD-001 — sold-заказ в обход упаковки списывается один раз
 TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
 """
 
 from __future__ import annotations
@@ -319,6 +320,7 @@
     async_client: AsyncClient,
     *,
     supplier_status: str,
+    supply_delivered: bool | None = None,
 ) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID, uuid.UUID]:
     """Заказ, уже списанный (как при упаковке поставки), готовый к отмене.
 
@@ -357,6 +359,23 @@
         session.add(order)
         await session.flush()
 
+        if supply_delivered is not None:
+            supply = FbsSupply(
+                tenant_id=tenant_id,
+                seller_id=seller_id,
+                warehouse_id=warehouse_id,
+                wb_supply_id=f"reversal-{suffix[-8:]}",
+                name="Поставка для проверки сторожа возврата",
+                delivery_type="warehouse_sc",
+                delivered_at=now if supply_delivered else None,
+            )
+            session.add(supply)
+            await session.flush()
+            # У отменённых заказов FK `supply_id` пустой: связь с переданной
+            # поставкой должна находиться именно по текстовому `wb_supply_id`.
+            order.wb_supply_id = supply.wb_supply_id
+            assert order.supply_id is None
+
         # Один экземпляр лежал в ячейке, упаковка списала его — то же самое,
         # что делает `apply_fbs_supply_write_off` при завершении поставки.
         await inventory_service.record_movement_and_adjust_balance(
@@ -397,6 +416,70 @@
     return order_id, tenant_id, product_id, location_id
 
 
+# TC-NEW-FBS-REVERSAL-DELIVERED-001 — переданная поставка блокирует возврат после cancel
+@pytest.mark.asyncio
+async def test_reversal_blocked_for_delivered_supply_after_supplier_status_cancel(
+    async_client: AsyncClient,
+    caplog: pytest.LogCaptureFixture,
+) -> None:
+    """Given: заказ списан, связанная по текстовому `wb_supply_id` поставка
+    передана (`delivered_at` заполнен), но WB уже сменил supplier status на
+    `cancel`; FK `order.supply_id` при этом пустой.
+    When: сторож обрабатывает отмену.
+    Then: функция возвращает False, складского движения нет, а журнал помечен
+    обработанным через `reversed_at`. Ограничение: связь через пустой FK не
+    используется, решение принимается по текстовому идентификатору поставки.
+    """
+    caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
+    order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
+        async_client,
+        supplier_status="cancel",
+        supply_delivered=True,
+    )
+
+    async with SessionLocal() as session:
+        order = await session.get(FbsOrder, order_id)
+        assert order is not None
+        assert order.supply_id is None
+        assert order.wb_supply_id is not None
+        reversed_flag = await reverse_fbs_shipment_if_needed(session, order)
+        await session.commit()
+
+    assert reversed_flag is False
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=True" in caplog.text
+    assert "status=cancel" in caplog.text
+
+    async with SessionLocal() as session:
+        ledger = await session.scalar(
+            select(FbsShipmentReversalLedger).where(
+                FbsShipmentReversalLedger.fbs_order_id == order_id
+            )
+        )
+        assert ledger is not None
+        assert ledger.reversed_at is not None
+        assert ledger.reversal_movement_id is None
+
+        movement_count = await session.scalar(
+            select(func.count(InventoryMovement.id)).where(
+                InventoryMovement.tenant_id == tenant_id,
+                InventoryMovement.product_id == product_id,
+                InventoryMovement.movement_type == MOVEMENT_TYPE_FBS_SHIPMENT,
+            )
+        )
+        assert movement_count == 1, "должно остаться только исходное списание"
+
+        balance = await session.scalar(
+            select(InventoryBalance).where(
+                InventoryBalance.tenant_id == tenant_id,
+                InventoryBalance.product_id == product_id,
+                InventoryBalance.storage_location_id == location_id,
+            )
+        )
+        assert balance is not None
+        assert int(balance.quantity) == 0, "товар после передачи не должен вернуться"
+
+
 # TC-NEW-FBS-REVERSAL-COMPLETE-001 — отмена по complete-заказу не возвращает остаток
 @pytest.mark.asyncio
 async def test_reversal_blocked_when_supplier_status_complete(
@@ -413,8 +496,8 @@
           `fbs_shipment` не меняются), запись журнала помечается обработанной
           (`reversed_at` проставлен), но без движения (`reversal_movement_id`
           пуст) — иначе она пыталась бы вернуться на каждом следующем обходе;
-          ограничение: в лог пишется предупреждение с номером заказа, продавцом
-          и товаром, чтобы такие случаи были видны.
+          ограничение: в лог пишется предупреждение с номером заказа, поставкой,
+          признаком передачи и статусом, чтобы такие случаи были видны.
     """
     caplog.set_level("WARNING", logger="app.services.fbs_cancellation_service")
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
@@ -429,8 +512,9 @@
         )
         await session.commit()
 
-    assert "already handed to WB" in caplog.text
-    assert str(order_id) in caplog.text
+    assert "fbs_reversal_skipped_after_handover" in caplog.text
+    assert "delivered=False" in caplog.text
+    assert "status=complete" in caplog.text
 
     async with SessionLocal() as session:
         order = await session.get(FbsOrder, order_id)
@@ -466,15 +550,15 @@
         assert int(balance.quantity) == 0, "остаток не должен вырасти — товара на складе нет"
 
 
-# TC-NEW-FBS-REVERSAL-COMPLETE-002 — отмена по не-complete заказу остаток возвращает
+# TC-NEW-FBS-REVERSAL-COMPLETE-002 — непереданная поставка возвращается на остаток
 @pytest.mark.asyncio
 @pytest.mark.parametrize("supplier_status", ["new", "confirm"])
 async def test_reversal_still_happens_when_supplier_status_not_complete(
     async_client: AsyncClient,
     supplier_status: str,
 ) -> None:
-    """Given: заказ уже списан, но `supplier_status` ещё не "complete"
-    (`new`/`confirm`) — посылка ещё у нас, WB её не забирал.
+    """Given: заказ уже списан, его поставка ещё не передана (`delivered_at`
+    пустой), а `supplier_status` равен `new`/`confirm`.
     When: WB присылает статус отмены.
     Then: остаток возвращается на склад (движение `fbs_shipment` +1, баланс
           растёт на 1), запись журнала помечена обработанной с заполненным
@@ -482,7 +566,9 @@
           меняется.
     """
     order_id, tenant_id, product_id, location_id = await _seed_order_with_existing_write_off(
-        async_client, supplier_status=supplier_status
+        async_client,
+        supplier_status=supplier_status,
+        supply_delivered=False,
     )
 
     async with SessionLocal() as session:
diff --git a/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
new file mode 100644
index 0000000000000000000000000000000000000000..5588e6197fa6033ebabfbe75f0a448460bd3bc8e
--- /dev/null
+++ b/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md
@@ -0,0 +1,48 @@
+# Итог: сторож возврата после передачи поставки WB
+
+Дата проверки: 29.08.2026.
+
+## Что изменено
+
+В `reverse_fbs_shipment_if_needed` добавлена проверка факта передачи поставки по
+`fbs_supplies.delivered_at`. Поставка ищется по текстовому
+`order.wb_supply_id` в пределах арендатора; пустой `order.supply_id` для этого
+не используется.
+
+Старое условие `supplier_status == "complete"` сохранено как дополнительное.
+Сторож блокирует возврат, если поставка уже передана либо статус поставщика всё
+ещё равен `complete`. При блокировке он пишет предупреждение в лог, проставляет
+`ledger.reversed_at`, не создаёт складского движения и возвращает `False`.
+
+Параметр `skip_if_supplier_complete`, передача `actor_user_id` в складское
+движение и разбор многопозиционных заказов Ozon через
+`ledger.ozon_positions_json` не изменялись.
+
+## Проверенные сценарии
+
+- Переданная поставка, `supplier_status == "cancel"`, пустой `order.supply_id`:
+  возврат заблокирован по текстовому `wb_supply_id`, функция вернула `False`,
+  новое движение не создано, `reversed_at` заполнен.
+- Непереданная поставка с пустым `delivered_at`: товар вернулся на остаток для
+  статусов `new` и `confirm`.
+- Прежний случай `supplier_status == "complete"`: возврат по-прежнему
+  блокируется даже без связанной поставки.
+- Адресный файл Ozon прошёл без регрессии.
+
+## Ворота
+
+Из каталога `backend/` выполнено:
+
+```text
+ruff check .
+All checks passed!
+
+mypy .
+Success: no issues found in 390 source files
+
+python3 -m pytest tests/test_fbs_writeoff_sold_and_reversal_guard.py tests/test_fbs_ozon_lane.py -q
+39 passed in 30.54s
+```
+
+Полный `pytest` не запускался согласно ограничению задачи. Выкатка и изменение
+боевых данных не выполнялись.

tokens used
100 832
Правка локально реализована, проверена и сохранена в Git:

- Ветка: `codex/fix-vozvrat-peredacha-20260829`
- Коммит: `ccb7669354ad21c2396f71e90dbcbbcd3d19ee09`
- [Сервис](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/app/services/fbs_cancellation_service.py:75)
- [Тесты](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/backend/tests/test_fbs_writeoff_sold_and_reversal_guard.py:419)
- [Итоговый журнал](/Users/deniscivkunov/Projects/WMS/.worktrees/fix-vozvrat/tasks/_zhurnal/codex-briefs/ITOG-vozvrat-peredacha.md:1)

Проверки:

- `ruff check .` — успешно.
- `mypy .` — успешно, 390 файлов.
- Два указанных тестовых файла — `39 passed in 30.54s`.
- Полный `pytest` не запускался.
- Фронт, Ozon-логика и `actor_user_id` не изменялись.

Ветка не опубликована: `git push` завершился ошибкой DNS — окружение не смогло разрешить `github.com`. Merge и выкатка не выполнялись. `ZADANIE.md` и каталог с исходной фактурой остались вне коммита.


