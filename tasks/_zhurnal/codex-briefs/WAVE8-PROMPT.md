Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave8
на ветке codex/warehouse-map-screen-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: подключить принятый владельцем экран карты склада к боевому API.

СНАЧАЛА ПРОЧИТАЙ ОБРАЗЕЦ — там правила и три готовых примера:
  tasks/_zhurnal/codex-briefs/OBRAZEC-PODKLYUCHENIYA.md

Экран: frontend/src/screens/ff/warehouse-map/ (FfWarehouseMapScreen и соседи).
Сейчас он живёт на выдуманных данных из stub.ts, превью — /warehouse-map.html.

Сервер уже готов, роуты в backend/app/api/warehouses.py:
- GET  /warehouses/{warehouse_id}/map          — весь состав склада
- POST /warehouses/{warehouse_id}/map/move     — переместить объект
- POST /warehouses/{warehouse_id}/map/disband  — расформировать палету

Форма ответа совпадает с типами экрана: смотри
frontend/src/screens/ff/warehouse-map/WarehouseMapTypes.ts — сервер писали,
читая этот файл как контракт.

СДЕЛАТЬ:
1. FfWarehouseMapPage.tsx — связка: загрузка карты по выбранному складу,
   перемещение, расформирование. Экран сделать управляемым пропсами, сохранив
   работу превью.
2. Маршрут ff/warehouse-map в App.tsx, доступ по тому же признаку, что у
   ff/sorting-objects.
3. Значок пересчёта на строках уже есть (onInventory) — сейчас он в превью
   показывает надпись. В боевом экране он должен вести на инвентаризацию:
   переход на /app/ff/stocktaking. Модалку пересчёта в боевой экран НЕ тащи,
   это следующий шаг.

ОСОБОЕ ВНИМАНИЕ: перетаскивание меняет состав склада. Экран должен менять своё
состояние сразу, а при отказе сервера — показать причину и перечитать карту.
Молча разъехавшиеся экран и база на складе означают, что человек идёт к полке и
не находит товар.

## ПРО ТЕСТЫ
Полный pytest НЕ ЗАПУСКАЙ ВООБЩЕ. npm run build и Playwright НЕ ЗАПУСКАЙ.
Только `npx tsc --noEmit -p tsconfig.app.json` из frontend/ и
`python3 scripts/ui/ui_guard.py` из корня. Они быстрые.

## ГРАНИЦЫ
frontend/src/App.tsx правь ТОЛЬКО добавлением своего маршрута рядом с
существующими. Соседние маршруты и импорты не трогай — над этим файлом работают
параллельно, и лишняя правка станет конфликтом.
backend/ НЕ ТРОГАТЬ ВООБЩЕ: сервер уже готов.

## ПРОДУКТОВЫЙ ГЕЙТ ОТМЕНЁН
Протокола BA / Product / Code Review / Browser QA в репозитории нет.
Не запускай агентов-приёмщиков, не выписывай feature cards и вердикты.
Уложись в час. Коммить маленькими связными коммитами по-русски.
