Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave6
на ветке codex/scan-resolver-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: один общий распознаватель отсканированного кода на весь склад.

ЗАЧЕМ. Владелец требует, чтобы сканер работал на всех складских экранах и
распознавал ЛЮБОЙ объект: ячейку, палету, короб, грузоместо, товар, заказ.
Сейчас единого распознавателя нет — восемь разных функций, каждая ищет ровно
одно, что нужно ей на своём шаге. Пока так, экраны будут по-разному решать, что
им пикнули, и разъедутся. Сначала общий распознаватель, потом экраны.

Существующие частные поиски (посмотри их, оттуда брать логику, а не выдумывать):
- `catalog_service.resolve_warehouse_scan()` — склад или ячейка
- `warehouse_box_service.resolve_barcode()` — короб склада или коробка приёмки
- `inbound_intake_service.scan_distribution_barcode()` — ячейка по коду и ШК
- `fbs_supplies.scan_fbs_pick_location()` / `scan_fbs_pick_product()`
- `inbound_intake_service.scan_barcode_to_loose_intake()` — товар

СДЕЛАТЬ:

1. **`backend/app/services/scan_resolver_service.py`** — функция
   `resolve_any_scan(session, tenant_id, code, *, warehouse_id=None)`,
   возвращающая, что это за объект: тип, идентификатор, человеческое название,
   и склад, если он определяется. Типы: `cell`, `pallet`, `box`, `cargo_place`,
   `product`, `fbs_order`, `warehouse`.
   - Порядок поиска задай осознанно и объясни его комментарием: коды разных
     сущностей могут пересечься, и порядок — это решение, а не случайность.
   - Если код подходит под несколько сущностей, верни неоднозначность явно, а не
     первое попавшееся: молча выбранный не тот объект на складе означает, что
     человек положит товар не туда.
   - Ничего не найдено — понятный отказ, а не пустой ответ.

2. **`GET /operations/scan/resolve?code=...&warehouse_id=...`** — роут поверх неё.
   Обязательно с тестом, это правило репозитория.

3. **Нормализация кода — в одном месте, внутри распознавателя.** Сейчас на бэке
   везде только `.strip()`. Добавь: обрезку, отсечение невидимых управляющих
   символов, регистронезависимое сравнение для кодов ячеек (как уже делает
   `scan_distribution_barcode`). Русскую раскладку НЕ трогай — она чинится во
   фронте, в `useBarcodeScanner.ts`, и дублировать её на бэке не нужно.

4. **Частные поиски не ломать.** Существующие функции оставь работающими: их
   зовут живые экраны. Твоя задача — дать общий вход, а не переписать всё.
   Если внутри частной функции логика точно та же — можешь позвать общую, но
   только там, где поведение не меняется, и это должно быть покрыто тестом.

ЧЕМ ЗАКРЫВАЕТСЯ (тесты поимённо):
- каждый из типов находится по своему штрихкоду;
- арендатор Б не находит объект арендатора А;
- неоднозначный код возвращает неоднозначность, а не первый попавшийся объект;
- неизвестный код возвращает понятный отказ;
- код с пробелами по краям и в другом регистре находит ту же ячейку;
- фильтр по складу сужает поиск и не находит объект чужого склада.

ЖЁСТКИЕ ГРАНИЦЫ:
- Только backend/app/**, backend/tests/**.
- frontend/ НЕ ТРОГАТЬ ВООБЩЕ.
- Миграции не нужны; если вдруг понадобится — номер 20260828_0180 и выше.

ПРО ТЕСТЫ:
ПОЛНЫЙ pytest НЕ ЗАПУСКАЙ ВООБЩЕ, ни разу. Полный прогон делают централизованно
перед вливанием. Гоняй ТОЛЬКО свои файлы точечно. Playwright и npm не запускай.
ruff и mypy дешёвые — гоняй сколько нужно.
Питон: /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python

Уложись в час. Коммить маленькими связными коммитами по-русски.

## ПРОДУКТОВЫЙ ГЕЙТ ОТМЕНЁН
Протокола BA / Product / Code Review / Browser QA в репозитории больше нет.
Не запускай агентов-приёмщиков, не выписывай feature cards и вердикты.
