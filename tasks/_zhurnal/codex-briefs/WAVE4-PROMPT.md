Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave4
на ветке codex/inventory-count-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: бэк документа инвентаризации, ШАГ 1 (по ячейкам, без контейнеров).

Контракт читать ПОЛНОСТЬЮ перед первой строкой кода:
  tasks/20260828-sdelat-dokument-inventarizacii-ego-znach/CONTRACT.md
Раздел 7 объясняет, почему шаг 1 и шаг 2 разделены. Ты делаешь только ШАГ 1.

Макет экрана уже сделан и принят владельцем, он лежит в
frontend/src/screens/ff/inventory/ — ЧИТАЙ его как источник правды по форме
данных (какие числа нужны строке, что показывает тара, что такое «остаток
изменился»), но НЕ ПРАВЬ там ни одного файла.

ЧТО ДЕЛАТЬ

1. Модели `InventoryCount` и `InventoryCountLine` — состав полей в разделе 5
   контракта. Строка шага 1 привязана к (ячейка, товар); поля `container_kind`
   и `container_id` ЗАВЕДИ СРАЗУ nullable, но не используй — их наполнит волна 1
   складской структуры, которую делает другой исполнитель.
2. Новый тип движения `MOVEMENT_TYPE_INVENTORY_COUNT = "inventory_count"` в
   inventory_movement.py и поле `inventory_count_line_id` рядом с уже
   существующими `inbound_intake_line_id` и `outbound_shipment_line_id`.
3. Сервис: наполнение документа (весь склад либо отбор по селлеру и категории),
   сохранение введённых фактов пачкой, проведение.
   Проводки делать ТОЛЬКО через существующую
   `inventory_service.record_movement_and_adjust_balance` — своей арифметики
   остатка не писать.
4. Роуты из раздела 5 контракта. Каждый — вместе с тестом, это правило репозитория.

ГЛАВНОЕ ПРАВИЛО ПРОВЕДЕНИЯ, не переоткрывать:
дельта считается В МОМЕНТ ПРОВЕДЕНИЯ от актуального остатка, а не от того, что
показывалось при вводе. После проведения в системе лежит ровно то, что человек
насчитал руками. Строки, где числящееся успело измениться, возвращаются отдельным
списком в ответе — экран покажет предупреждение.

АДРЕСНОЕ ХРАНЕНИЕ. Если у арендатора `Tenant.address_storage_enabled` выключено,
ячеек в его картине мира нет: документ наполняется без разбиения по ячейкам, и
ответ не должен требовать ячейку от клиента. Читай флаг через
`tenant_settings_service.is_address_storage_enabled`.

ЧЕМ ЗАКРЫВАЕТСЯ — эти проверки поимённо (раздел 6 контракта):
- документ по отбору селлера не содержит товаров другого селлера;
- арендатор Б не видит документ арендатора А и не может его провести;
- введённый ноль даёт расхождение на весь числящийся остаток;
- пустое поле в проведение не попадает и движения не создаёт;
- проведение создаёт ровно одно движение на строку с расхождением и ни одного там, где сошлось;
- после проведения остаток равен введённому факту;
- повторное проведение отбивается;
- правка проведённого документа отбивается;
- остаток, изменившийся между вводом и проведением, попадает в предупреждение,
  и итоговый остаток всё равно равен факту;
- проведение пустого документа отбивается.

ЖЁСТКИЕ ГРАНИЦЫ
- Только backend/app/**, backend/tests/**, backend/alembic/versions/**.
- frontend/ НЕ ТРОГАТЬ ВООБЩЕ.
- Миграции только добавляющие. Номер бери 20260828_0160 и выше, проверив
  реальную последнюю ревизию командой, а не на глаз. Номера 0116, 0117 заняты,
  соседние исполнители берут 0130, 0140, 0150 — не залезай на них.

ПРО ТЕСТЫ
НЕ ГОНЯЙ ПОЛНЫЙ pytest по ходу — это тысяча тестов и пятнадцать минут.
Точечно свои файлы. Полный прогон РОВНО ОДИН РАЗ в конце. Упало — только упавшее.
Playwright и npm НЕ ЗАПУСКАЙ ВООБЩЕ.
Питон: /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python

Не больше часа. Упёрся дольше двадцати минут — коммить что есть и напиши, где встал.
Коммить маленькими связными коммитами по-русски.

## ПРОДУКТОВЫЙ ГЕЙТ ОТМЕНЁН
В этом репозитории больше нет протокола BA / Product / Code Review / Browser QA.
Не запускай агентов-приёмщиков, не выписывай feature cards и вердикты. Ты пишешь
код, приёмку делает владелец. Каждая минута на церемонию украдена у дела.
