Бэкенд-пересчёт по таре реализован и сохранён в Git.

Добавлены канонические типы:

- `pallet` — палета;
- `box` — короб;
- `cargo_place` — грузоместо.

Состав берётся из фактических `InventoryBalance`, как на карте склада. Палета включает собственный товар и товар из вложенных коробов и грузомест. Строки сохраняют `container_kind` и `container_id`, поэтому остатки в таре не смешиваются с тем же SKU россыпью.

Проверки:

- короб: ровно две строки с количеством `18` и `7`;
- грузоместо: только его остатки;
- палета: собственный товар плюс вложенная тара;
- пустая тара: `409 container_has_no_stock`;
- существующие `product` и `location`;
- `11 passed`;
- `ruff check .` — пройден;
- `mypy .` — пройден.

`frontend/` не изменялся. Полный pytest, npm и Playwright не запускались по вашему указанию.

Коммит: `9b30c95c518a7788977a11d2f4dbaf8a04d9ad17`  
Ветка: `codex/count-containers-20260828`

Незакрыто: ветка не отправлялась в `origin`, PR и деплой не создавались.

