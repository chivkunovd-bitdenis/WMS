Локально реализовано и сохранено в коммите `3ede3b99c37dc7069ae4ac14123d2eccc72ec059`.

Добавлена ручка:

`POST /warehouses/{warehouse_id}/sorting-objects`

Тело:

```json
{"kind": "pallet"}
```

Допустимы `pallet`, `box`, `cargo_place`. Ответ `201` содержит `id`, `kind`, `code`, серверный `barcode` и `holder: null`.

Добавочная миграция вводит вид складской тары; существующие записи остаются коробами. Созданная тара:

- принадлежит складу и арендатору;
- пуста, не привязана к палете или ячейке;
- видна в «Без ячеек» через `GET /sorting-objects` и `GET /map`;
- находится сканером по выданному ШК.

Проверки:

- 5 новых API-тестов — зелёные;
- 12 точечных регрессионных тестов — зелёные;
- `ruff check .` — зелёный;
- `mypy .` — зелёный;
- полный pytest, npm и Playwright не запускались;
- `frontend/` не изменён.

Незакрыта только публикация: `git push` не прошёл из-за недоступного DNS GitHub (`Could not resolve host: github.com`). Рабочее дерево чистое, коммит восстановим локально.

