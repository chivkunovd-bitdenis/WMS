Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave2
на ветке codex/fbs-stock-share-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: волна 2 — FBS-остатки как доля, склады продавца, время сборки.
Полное задание с границами и приёмкой:
  tasks/20260828-seychas-zadacha-eto-blok-pro-fbs-ego-nad/WAVE-2-CODEX.md
Контракт (форма данных, раздел 7 про изолированную выкатку):
  tasks/20260828-ekran-tovary-i-fbs-bolshaya-modalka-osta/CONTRACT.md
Прочитай оба ПОЛНОСТЬЮ перед первой строкой кода.

Половина работы уже сделана и лежит в этой же ветке (миграция 20260828_0116,
fbs_stock_rule_service.py, fbs_stock_breakdown_by_product, дополненный триггер
публикации, роуты fbs-rule). НЕ ПЕРЕПИСЫВАЙ ЭТО. Твоя часть — четыре пункта из
раздела «Что делать в этой волне»:

1. Склады продавца: GET /fbs-sellers/{id}/warehouses и
   PUT /fbs-sellers/{id}/warehouses/{wb_warehouse_id}. Колонка `served` уже
   заведена в базе, её сейчас никто не выставляет — научи.
2. Отсев чужих заказов при импорте: заказ на склад без served=true в базу не
   попадает вообще. Фильтровать на стороне WB нельзя, склад приходит внутри
   заказа полем warehouseId. Заказ с НЕПРИВЯЗАННЫМ складом не прячем — для него
   уже есть статус warehouse_unmapped.
3. Среднее время сборки: GET /fbs/assembly-time?from=&to=&seller_id= →
   {hours, orders}. Период по умолчанию в сервер НЕ зашивать, его задаёт экран.
4. Обнуление старых абсолютных лимитов — ОТДЕЛЬНЫМ шагом после настройки
   правила, НЕ в миграции.

ЖЁСТКИЕ ГРАНИЦЫ:
- Только backend/app/models, backend/app/services, backend/app/api,
  backend/alembic/versions, backend/tests.
- frontend/ НЕ ТРОГАТЬ ВООБЩЕ. Макет /products.html уже согласован владельцем.
- Миграции только добавляющие, удаление колонок запрещено.
- Новый роут — только вместе с тестом.
- Номера 20260828_0116 и 20260828_0117 заняты. Если нужна миграция, бери номер
  позже, например 20260828_0140, и проверь реальную последнюю ревизию командой,
  не угадывай.

ПРО ТЕСТЫ — ВАЖНО, ЧИТАЙ ВНИМАТЕЛЬНО:
НЕ ГОНЯЙ ПОЛНЫЙ pytest по ходу работы. Полный набор — тысяча с лишним тестов и
пятнадцать минут; за проход ты сожжёшь час впустую.
- По ходу запускай ТОЛЬКО свои файлы тестов: `pytest backend/tests/test_<файл>.py -q`
- Полный pytest — РОВНО ОДИН РАЗ в самом конце, когда всё закоммичено.
- Если полный прогон нашёл падение — перезапускай ТОЛЬКО упавшие тесты по имени.
- Playwright / npm НЕ ЗАПУСКАЙ ВООБЩЕ, ты не трогаешь фронт.
- ruff и mypy дешёвые, гоняй сколько нужно.

Гейт: ruff, mypy, точечный pytest, один полный pytest в конце,
python3 scripts/ci/back_guard.py (он красный из-за старых монолитов —
базовую линию НЕ поднимай, просто не ухудшай).
Питон бери отсюда: /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python

Не больше часа. Упёрся дольше двадцати минут — коммить что есть и напиши, где встал.
Коммить маленькими связными коммитами по-русски, дерево не оставляй грязным.
В конце коротко: что сделано, какие роуты появились, имена новых тестов,
результат каждого гейта числами.


## ПРОДУКТОВЫЙ ГЕЙТ УЖЕ ПРОЙДЕН — НЕ ЗАПУСКАЙ ЕГО ЗАНОВО

В AGENTS.md этого репозитория описан протокол BA -> Product -> Atomic Dev ->
Code Review -> Browser QA. По ЭТОЙ задаче он УЖЕ ПРОЙДЕН: владелец лично
утвердил макет и контракт, задание перед тобой — это и есть результат гейта.

Поэтому:
- НЕ запускай BA-агента и не выписывай feature cards.
- НЕ запускай Product Before Dev.
- НЕ запускай Code Review и Browser QA.
- Не пиши вердикты, статусы PRODUCT_APPROVED и прочую отчётность гейта.

Ты — Atomic Dev и только он. Садись и пиши код прямо сейчас. Ревью и живую
проверку в браузере сделает владелец и я, это не твоя работа и на неё у тебя нет
ни экранов, ни доступа. Каждая минута на церемонию — минута, украденная у дела.
