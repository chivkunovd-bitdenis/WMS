Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave10
на ветке codex/address-storage-frontend-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: закрыть фронтовые дыры адресного хранения.

ПРАВИЛО ВЛАДЕЛЬЦА ДОСЛОВНО: «настройка адресного хранения должна красной нитью
проходить через всю систему. Если она не стоит, то мы нигде и ячейки не
показываем — просто тару и товары».

Бэк это уже соблюдает: ячейки скрыты, ячеечные операции отбиваются
`address_storage_disabled`. Отчёт с полным разбором и, главное, с ПЕРЕЧНЕМ
ОСТАВШИХСЯ ФРОНТОВЫХ ДЫР лежит здесь:
  docs/research/20260828-address-storage-audit.md
Читай раздел «ЧТО ОСТАЛОСЬ ВО ФРОНТЕ» — это твой список работ.

Флаг во фронте уже есть: `me.address_storage_enabled`, прокидывается пропсом
`addressStorageEnabled` (смотри, как это сделано в
`frontend/src/screens/ff/FfInboundRequestView.tsx` и
`FfMarketplaceUnloadBoxAddDialog.tsx` — там уже правильно).

СДЕЛАТЬ: пройти по перечню из отчёта и в каждом месте убрать ячейку из вида,
когда флаг выключен — колонку, поле выбора, подпись, пункт меню, переход на
ячеечный экран. Не «спрятать текст», а не показывать то, чего в этой картине
мира не существует.

ОСОБО ВАЖНО: не менять поведение для арендатора, у которого адресное хранение
ВКЛЮЧЕНО. Это главный риск — так работает прод.

Где сомневаешься, показывать или нет, — оставь как есть и перечисли такие места
в итоге отдельным списком. Лучше честно назвать спорное, чем молча спрятать
нужное.

## ПРО ТЕСТЫ
Полный pytest НЕ ЗАПУСКАЙ. npm run build и Playwright НЕ ЗАПУСКАЙ.
Только `npx tsc --noEmit -p tsconfig.app.json` из frontend/ и
`python3 scripts/ui/ui_guard.py` из корня.

## ГРАНИЦЫ
backend/ НЕ ТРОГАТЬ — там уже сделано.
frontend/src/App.tsx правь по минимуму: над ним работают параллельно.
Файлы `preview.tsx` и папки макетов (warehouse-map, sorting-objects,
unload-pick, unload-pick-2, inventory, products-fbs) НЕ ТРОГАТЬ — их подключают
другие исполнители прямо сейчас.

## ПРОДУКТОВЫЙ ГЕЙТ ОТМЕНЁН
Не запускай агентов-приёмщиков, не выписывай feature cards и вердикты.
Уложись в час. Коммить маленькими связными коммитами по-русски.
