Ты работаешь в /Users/deniscivkunov/Projects/WMS/.worktrees/codex-wave3
на ветке codex/address-storage-thread-20260828. Это git worktree, работай только здесь.

ЗАДАНИЕ: настройка адресного хранения должна проходить красной нитью через всю
систему. Владелец сформулировал дословно: «если она не стоит, то мы нигде и
ячейки не показываем — просто тару и товары».

Флаг: `Tenant.address_storage_enabled` (backend/app/models/tenant.py),
читается через `tenant_settings_service.is_address_storage_enabled`.

Сейчас его соблюдают только четыре места:
- marketplace_unload_collect_service.py
- marketplace_unload_box_service.py
- fbs_supply_service.py
- fbs_workspace_service.py

ЧТО НАДО СДЕЛАТЬ

1. Пройди по backend/app/services и backend/app/api и найди КАЖДОЕ место, где
   ячейка (storage_location) обязательна, подставляется по умолчанию, требуется
   в запросе или возвращается в ответе. Для каждого ответь: что должно
   происходить, когда адресное хранение выключено.
2. Почини те, где выключенный флаг ломает работу или заставляет пользователя
   выбирать ячейку, которой у него нет. Общее правило: без адресного хранения
   ячейка не спрашивается и не показывается, товар живёт в таре или сам по себе,
   а движения пишутся в служебную зону (посмотри, как это уже сделано в
   `sorting_location_service.get_or_create_sorting_location` — не изобретай своё).
3. Каждое исправление закрой тестом: «арендатор без адресного хранения проходит
   этот сценарий и ни разу не упирается в ячейку».
4. Отдельным файлом `docs/research/20260828-address-storage-audit.md` напиши
   отчёт: таблица «место — что было — что стало» плюс раздел «ЧТО ОСТАЛОСЬ ВО
   ФРОНТЕ» с перечнем файлов frontend/src, где ячейка показывается без оглядки
   на флаг. Фронт САМ НЕ ПРАВЬ — его правит владелец, тебе нужен только перечень.

ЖЁСТКИЕ ГРАНИЦЫ
- Только backend/app/**, backend/tests/**, backend/alembic/versions/**, docs/research/**.
- frontend/ НЕ ТРОГАТЬ ВООБЩЕ. Ни одного файла.
- Миграции только добавляющие; если миграция нужна, бери номер 20260828_0150 и
  выше, проверив реальную последнюю ревизию командой, а не на глаз.
- Не меняй поведение для арендатора, у которого адресное хранение ВКЛЮЧЕНО.
  Это главный риск: сейчас так работает прод.

ПРО ТЕСТЫ
НЕ ГОНЯЙ ПОЛНЫЙ pytest по ходу работы — это тысяча тестов и пятнадцать минут.
Точечно свои файлы: `pytest backend/tests/test_<файл>.py -q`. Полный прогон РОВНО
ОДИН РАЗ в конце. Упало — перезапускай только упавшее по имени.
Playwright и npm НЕ ЗАПУСКАЙ ВООБЩЕ.
ruff и mypy дешёвые, гоняй сколько нужно.
Питон: /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python

Не больше часа. Упёрся дольше двадцати минут — коммить что есть и напиши, где встал.
Коммить маленькими связными коммитами по-русски.

## ПРОДУКТОВЫЙ ГЕЙТ ОТМЕНЁН
В этом репозитории больше нет протокола BA / Product / Code Review / Browser QA.
Не запускай агентов-приёмщиков, не выписывай feature cards и вердикты. Ты пишешь
код, приёмку делает владелец. Каждая минута на церемонию украдена у дела.
