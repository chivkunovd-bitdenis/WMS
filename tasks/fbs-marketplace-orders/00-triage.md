# 00 — Триаж

> Заполняет: первый агент, взявшийся за задачу. Первое действие по любой задаче.

- **Задача (одной строкой):** сделать работу с ФБС (схема «Маркетплейс» Wildberries) — заказ-ориентированная отгрузка со склада ФФ.
- **Тип:** `feature`
- **Размер:** `L` (новая доменная сущность «заказ» + интеграция с Marketplace API + фронт + мобилка; режем на под-задачи после ГЕЙТА 1)
- **GitHub Issue:** нет (завести при старте реализации, метка `ready`)
- **Стоит ли делать сейчас / зачем:** да — прямой запрос человека. ФБС = отдельная выручка/поток для селлеров, которого в системе нет вообще (`fbs` не встречается в коде).

## Маршрут (из типа+размера, см. `.dev/PROCESS.md` §1)
- [x] 0 триаж (этот файл)
- [x] 1 анализ + арх-коучинг
- [ ] 🔒 ГЕЙТ 1: арх + стек  ← **сейчас здесь, решение за человеком**
- [ ] 2 контракт
- [ ] 3 тест-дизайн (+ маппинг на TC-ID)
- [ ] 4 код (вертикальный срез)
- [ ] 5 независимое ревью + прогон гейтов
- [ ] 🔒 ГЕЙТ 2 приёмка (опц.)
- [ ] 6 док → `docs/`

> Размер `L`: после ГЕЙТА 1 разбиваем на под-задачи (`tasks/fbs-*`), каждая — свой вертикальный срез и PR.
> Первая под-задача-кандидат: `fbs-orders-intake` (модели + автоопрос заказов + резерв), она несёт основной риск.

## Что затрагивает (первая прикидка)
- backend `models`: новые `fbs_order`, `fbs_order_line`, `fbs_supply` + alembic-миграция.
- backend `services`: новый WB Marketplace-клиент (расширение `wildberries_client.py`), сервис синка заказов, резерв через `inventory_service`.
- backend `api`: новые роуты под `wildberries_integration.py` / отдельный роутер ФБС.
- backend `tasks`: фоновый job автоопроса заказов (тип задачи в Celery/BackgroundTasks).
- frontend `frontend/src`: экран очереди заказов ФБС, карточка заказа, группировка в отгрузку, «передать курьеру».
- mobile `mobile/`: сборка заказа сканером + печать стикера (отдельный трек, `mobile/docs/PROGRESS.md`).
- Пересечение с существующим: остатки/резервы (`inventory_*`), упаковка (`packaging_task`), печать (`print_template`), креды WB (`seller_wildberries_credentials`).
- **Конфликт скоупа:** `docs/MVP_DECISIONS_RU.md:29` — «WB только импорт, без create в WB». Полный цикл ФБС требует записи в WB → выносится на ГЕЙТ 1.
