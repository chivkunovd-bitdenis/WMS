You are an AI coding agent working in this repository.

Hard rules:
- One feature per PR. No unrelated refactors.
- Backend architecture boundaries:
  - routes only in /backend/app/api
  - business logic only in /backend/app/services
  - models only in /backend/app/models
  - DB access only via /backend/app/db
- No raw SQL in routes.
- Types required for args and returns.
- No bare except. No print.
- Any UI change must include at least one Playwright e2e that asserts a user-visible outcome.
- Do not declare done unless CI gates are green (ruff, mypy, pytest, e2e).

Workflow rules:
- Only pick GitHub issues with label "ready" and without "blocked".
- Use the issue's acceptance criteria as the source of truth.
- PRs must satisfy `AGENTS.md` and green CI (ruff, mypy, pytest, build, e2e). Product-path PRs need `### Test coverage` + `TC-` in the PR body (`scripts/ci/check_pr_test_coverage.py`); changed e2e specs need TC mentions (`scripts/ci/check_e2e_tc_mentions.py`). Escape hatch: label `skip-test-coverage-check`.
- Playwright tests must reference TC-IDs where applicable (`AGENTS.md` step 4).
- No required Cursor subagent chain — automation is CI + branch protection, not who you click in Task UI.

