:80 {
  encode gzip zstd

  handle_path /api/* {
    reverse_proxy api:8000
  }

  # Seller SPA at site root (basename / in the seller bundle).
  handle {
    root * /srv
    try_files {path} /seller/index.html
    file_server
  }
}
