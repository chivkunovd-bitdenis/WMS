# WMS-484 · Фронт не умирает целиком и никогда не показывает служебный текст

Постановка ведущего по прямому поручению владельца 20.09.2026 (аналитик не вызывался — владелец
велел экономить ресурсы и отдать задачу Codex по описанию). Исполнитель — Codex (Astra).
Ветка `fix/wms484-frontend-error-recovery` от `origin/etalon` с ревертом мержа WMS-477 (#228):
код ветки равен боевому d5903838 плюс эта задача.

## 1. Исходный запрос владельца (дословно)

20.09.2026, после падения экрана поставки FBS у пользователей на проде:

- «это что нахуй за сообщение? какой нахуй разработчик? это откуда вообще взялось»
- «вообще никогда такого быть не должно в принципе»
- «не должно быть сломанного куска экрана»
- «никогда в жизни не смей нигде показывать это говно техническое»
- «ты понял как фронт починить? чтобы он с ошибкой не падал никогда больше?»
- «делай. опиши задачу подробно и отдай по cli кодексу, сам экономь ресурсы»

## 2. Что произошло и что известно (контекст для исполнителя)

- 20.09 11:16 МСК на бой выкачен WMS-477 (кнопка «Проверить в WB» и тихое автообновление
  вкладки «Упаковка и маркировка» рабочего места поставки FBS раз в 15 с). В 13:2x МСК
  пользователи увидели вместо приложения страницу ошибки библиотеки react-router: английский
  «Unexpected Application Error!», стек и «💿 Hey developer 👋 …» (браузер владельца перевёл её на
  русский). Бой откачен на d5903838 в 13:28.
- Трассировка со скриншота сверена с файлом сборки `client-BPdQZMHG.js`: кадры bl/vl —
  `commitMutationEffectsOnFiber` / `recursivelyTraverseMutationEffects`, то есть исключение
  при применении изменений к DOM (insertBefore/removeChild/обновление узла или очистка
  layout-эффекта). Первая строка ошибки не сохранилась. Воспроизвести на стенде с боевыми
  данными не удалось. Точная причина неизвестна — задача обязана сделать так, чтобы
  **любая** такая ошибка (а) не была видна пользователю, (б) попадала в журнал сервера с
  текстом и стеком.
- Сейчас в приложении нет ни одного перехватчика ошибок: `frontend/src/App.tsx`,
  `frontend/src/apps/seller/SellerApp.tsx`, `frontend/src/main.tsx` — ни `errorElement`, ни
  `ErrorBoundary`. Ошибки фронта нигде не журналируются. `frontend/index.html` объявляет
  `lang="en"` (Vite по умолчанию), портал селлера `frontend/seller/index.html` — `lang="ru"`.
- Стек: React 19.2, react-router-dom 7.14, MUI; сборка Vite; тесты — vitest (`npm run
  test:unit`, среда node, без jsdom/testing-library — не добавлять jsdom ради этой задачи:
  логику перехватчика вынести в чистые функции и тестировать их).
- Бэкенд: FastAPI, слои `backend/app/api` (роуты), `backend/app/services` (логика),
  `backend/app/models`, `backend/app/db`. Авторизация — `get_current_user` из
  `backend/app/api/deps.py`; ответы об ошибках — как у соседних ручек.

## 3. Требования

**R1. Ни при какой ошибке фронта пользователь не видит служебного текста.** Страница
react-router по умолчанию, стек, английские сообщения, коды и имена функций — никогда, ни в
портале фулфилмента, ни в портале селлера. Проверяется и на ошибке при отрисовке, и на ошибке
в эффекте, и на ошибке в ленивой подгрузке кода маршрута (`Failed to fetch dynamically
imported module` после выкатки).

**R2. Ошибка одного блока не убивает приложение.** Перехватчики (React ErrorBoundary,
классовый компонент — иначе в React их не сделать) стоят: (а) на корневом маршруте каждого
портала как `errorElement`/обёртка — последний рубеж; (б) вокруг содержимого каждого
раздела (элемент каждого `Route` в `App.tsx` и `SellerApp.tsx`); (в) вокруг содержимого
рабочего места поставки FBS `FfFbsSupplyWorkspace` (место падения) и вокруг тела остальных
крупных окон-диалогов, где это делается одной обёрткой без переписывания окна. Меню, шапка и
соседние разделы при ошибке одного блока продолжают работать.

**R3. Самовосстановление до показа чего-либо.** Поймав ошибку, перехватчик молча
перемонтирует упавшее поддерево (сброс ключа) — один раз сразу и ещё раз через короткую
паузу (порядка 1–2 с), потому что для рассинхрона DOM (translate/расширения) перемонтирование
лечит. Только если тот же перехватчик упал третий раз за короткое окно (порядка 30 с) —
показывается запасной вид.

**R4. Запасной вид — одна человеческая фраза в стиле приложения.** MUI `Alert` (или
существующий компонент состояния ошибки, если такой есть в `docs/UI_DESIGN_SYSTEM_RU.md`)
с текстом вида «Не удалось показать этот раздел. Обновите страницу.» и кнопкой «Обновить»
(перезагрузка страницы). Для корневого рубежа — та же фраза на пустой странице с логотипом,
как у существующих экранов входа/загрузки (`ProfileLoadingScreen`, `SubscriptionBlockedScreen`
— взять за образец оформления). Никаких стеков, кодов, ссылок на документацию, английских слов.
Для ошибки подгрузки чанка после выкатки — сразу автоматическая перезагрузка страницы один
раз (с защитой от бесконечного цикла через `sessionStorage`), без показа чего-либо.

**R5. Журнал ошибок фронта на сервере.** Новая ручка `POST /client-errors`
(`backend/app/api/client_errors.py`, логика в `backend/app/services/client_error_service.py`):
принимает JSON `{message, stack, url, screen, component, action, user_agent, app_build}`
(строки, лимит на размер каждого поля и всего тела ~16 КБ, лишнее обрезать, не отвергать),
требует обычную авторизацию, пишет одну запись в серверный журнал уровня ERROR с
префиксом `client error` и полями `tenant_id`, `user_id`, `role`, `url`, `screen`,
`component`, `action`, `message`, первые ~40 строк `stack`. **Без новой таблицы** — журнал
контейнера уже собирается (`docker logs`), этого достаточно, чтобы увидеть причину;
таблицу заводить запрещено. Ограничение частоты: не более ~20 записей в минуту на пользователя
(простой счётчик в памяти процесса), сверх — 204 без записи. Ответ 204. Ошибки самой ручки
никогда не 500 наружу.

**R6. Фронт отправляет всё пойманное.** Один модуль `frontend/src/utils/clientErrorReport.ts`:
`reportClientError(error, {screen, component, action})` — fire-and-forget `fetch` с текущим
токеном (`apiUrl`, `authHeaders` как у соседей), с дедупликацией одинаковых сообщений в
пределах минуты и без исключений наружу (сбой отправки глотается). Вызывается: из каждого
перехватчика (при каждой поимке, включая успешно восстановленные — иначе причина падения
17-го числа так и останется неизвестной), из `window.addEventListener('error')` и
`window.addEventListener('unhandledrejection')` (регистрация в `main.tsx` обоих порталов).
Никакого UI у этих обработчиков нет.

**R7. Запрет автоперевода.** `frontend/index.html`: `lang="ru"` (как у селлера),
`translate="no"` на `<html>` и `<meta name="google" content="notranslate">` — в обоих
`index.html`. Это гигиена, не «причина» — владелец прямо сказал, что причина — само
появление ошибки; поэтому R1–R6 обязательны независимо от R7.

**R8. Ничего лишнего.** Не менять поведение экранов, не трогать WMS-477 (в ветке он
отреверчен и возвращается отдельно), не заводить новые таблицы, статусы, настройки в UI. Не
переписывать существующие компоненты ради обёртки — обёртка снаружи. Тексты — по-русски,
человеческие, без техножаргона.

## 4. Проверки (заполняет исполнитель для того, что проверил сам; ведущий — остальное)

| № | Проверка | Как | Вердикт |
|---|---|---|---|
| C1 | Ошибка при отрисовке компонента внутри раздела: раздел перемонтируется молча, если ошибка одноразовая — пользователь ничего не видит; меню и шапка живы | vitest на чистую логику перехватчика (счётчик попыток, окно 30 с, решение «перемонтировать/показать запасной вид»); ручная проверка на стенде через временный компонент, бросающий ошибку N раз | |
| C2 | Ошибка повторяется ≥3 раз за 30 с в одном блоке: показан запасной вид с фразой и кнопкой «Обновить», без служебного текста; остальное приложение работает | стенд, ручная проверка; vitest логики | |
| C3 | Корневой рубеж: ошибка вне разделов (например, в шапке) даёт страницу с фразой и кнопкой в стиле экранов входа, не страницу react-router | стенд | |
| C4 | Ошибка подгрузки чанка после выкатки: одна автоматическая перезагрузка, без зацикливания, без текста | vitest логики; стенд — имитация `Failed to fetch dynamically imported module` | |
| C5 | Каждая поимка (в т.ч. восстановленная) уходит в `POST /client-errors` с экраном, компонентом, текстом и стеком; сбой сети не даёт исключений | vitest модуля отправки (подмена fetch); стенд — запись видна в журнале API | |
| C6 | Обработчики `error`/`unhandledrejection` зарегистрированы в обоих порталах и шлют отчёт | код + стенд | |
| C7 | Ручка: 204 при валидном теле, обрезка длинных полей, 401 без токена, ограничение частоты (21-я запись за минуту — 204 без строки в журнале), строка журнала содержит tenant/user/screen/message/stack | pytest `backend/tests/test_wms484_client_errors.py` | |
| C8 | Портал селлера: те же перехватчики и отправка; `SellerApp.tsx` без служебной страницы | код + стенд | |
| C9 | `index.html` обоих порталов: `lang="ru"`, `translate="no"`, meta notranslate; сборка проходит | код + `npm run build` | |
| C10 | Гейты: `cd backend && ruff check . && mypy . && pytest -n auto tests/test_wms484_client_errors.py`; `cd frontend && npx tsc --noEmit -p tsconfig.app.json && npm run build && npm run test:unit` | исполнитель | |
| C11 | Соседи не задеты: экраны входа, подписки, загрузки профиля, рабочее место FBS без ошибок рендерятся как раньше (снимки до/после) | ведущий, стенд | |
| C12 | На бою после выкатки: в журнале API появляются записи `client error` при реальных падениях (если будут); страница react-router не встречается | после выкатки | |

## 5. Заключение

Ожидает реализации.
