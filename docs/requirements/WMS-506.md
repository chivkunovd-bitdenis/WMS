# WMS-506 — автоматическая перепечатка КИЗ при упаковке FBS

Дата: 22.09.2026. Статус: **реализовано в ветке; R4 исправлено и перепроверено; полная приёмка не завершена**.

## 1. Исходный запрос и уточнения

Первоначальный запрос:

> смотри что важно сделать на проде сейчас
> мы вчера добавляли функицонал перепечатки ЧЗ
> вот надо чтобы при сканировании он сразу печатался, без всякой хуйни
> не открывал диалог печати а моментально сразу печатал
> можно так сделать надёжлно?

Уточнение владельца, определяющее текущий объём:

> Я уверен, что это можно сделать без каких-либо дополнительных уёбищных программ, чтобы и не нужно было ничего устанавливать и заморачиваться. Наверняка позволяет, блять, винда, да или Mac, ну сразу отправлять на печать. Нужно это проанализировать. И ещё процесс будет такой: на FBS, на вкладке Упаковка сделать сверху аккуратную галочку Перепечатывать KIS. Процесс будет выглядеть следующим образом: когда мы сканируем QR-код и затем сканируем KIS, то всё так же, как и сейчас, KIS привязывается, всё это нормально работает, да, он подсвечивается, но ещё вылазит дубль точно такого же KIS. То есть он же прямо сразу вылазит из принтера. Это в точности та же технология, которую мы делали для, собственно, перепечатки, но просто нужно, чтобы она была встроена при этой галочке, да, при каждом сканировании упаковки. Вот так.

В интерфейсе использовать принятое в продукте написание «КИЗ». Объект перепечатки — полный код маркировки ЧЗ, считанный оператором и привязанный к заказу, а не QR стикера заказа.

## 2. Существующий процесс и цель

Исследована кодовая база `3e125074d5cb4c38e9107f864c3075f4f1f224cd`. Оператор ФФ работает в поставке FBS на вкладке «Упаковка». В `frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx` уже есть встроенная строка сканирования:

1. `scanKizSticker` вызывает `lookupFbsOrderBySticker`, выбирает заказ и показывает его строку.
2. `scanKizCode` вызывает `validateFbsKiz`, затем `commitFbsKiz` с парой заказ/исходное значение и ключом запроса.
3. Серверный `commit_kiz_pairs` в `backend/app/services/fbs_kiz_service.py` фиксирует транзакцию перед ответом `status=ok`. При ошибке некоторые состояния неопределённого внешнего исхода тоже сохраняются, но ответ имеет `status=error`: наличие записи само по себе не доказывает успешную привязку.
4. Фронт проверяет `outcome.status`, сбрасывает выбор заказа и перечитывает поставку. Подсветка и текст вердикта площадки берутся из актуальных метаданных. У WB последующая проверка может закончиться позже локального сохранения.
5. На этой точке автоматической перепечатки сейчас нет. Тот же КИЗ можно перепечатать отдельным существующим действием.

WMS-489 уже использует `printMarkingCodeLabels(codes, { duplicateCopies: 1 })`: строит DataMatrix и вызывает `frameWindow.print()` через скрытый iframe. Значение по умолчанию этой функции — **две копии**, поэтому новый путь обязан явно передать одну. Существующий механизм `startAutoKizReprintPrint` имеет захват попытки печати и отметку запуска; он не подтверждает физический выход бумаги.

Полный анализ штатной тихой печати Chrome/Edge на Windows/macOS, первичные источники и пределы надёжности: [BROWSER-PRINT-ANALYSIS.md](../evidence/WMS-506/BROWSER-PRINT-ANALYSIS.md).

**Граница визуального исследования:** отдельная вкладка `https://wms.sellerfocus.pro/` открыта 22.09.2026 и показала `401 invalid_token` / форму входа. Авторизованный экран упаковки в этом проходе не просмотрен; новую авторизацию не выполняли. В уже открытой пользовательской вкладке был незавершённый диалог остатков — его не изменяли. Размещение ниже определено по коду и прямому запросу; визуальное сравнение с упаковкой и текущим эталоном «Ячейки» остаётся обязательным перед реализацией UI.

## 3. Требования

| ID | Условие и действие | Наблюдаемый результат | Основание |
|---|---|---|---|
| R1 | Оператор открывает FBS → поставка → «Упаковка» с внесением КИЗ | Вверху существующей области сканирования находится компактная галочка «Перепечатывать КИЗ», в действующей теме; без отдельного окна или изменения структуры таблицы | Прямое уточнение владельца |
| R2 | Галочка выключена; оператор сканирует стикер и КИЗ | Сохраняются штатная привязка, подсветка, ошибки и фокус. Автоматических вызовов печати нет | «Всё так же, как и сейчас»; решение D1 |
| R3 | Галочка включена; штатный commit подтвердил успешную привязку именно сканируемого кода к выбранному заказу | Запускается одна автоматическая перепечатка сразу после подтверждения. Она не ждёт фонового позеленения строки WB, отдельного открытия предпросмотра или кнопки «Печатать» | «При каждом сканировании упаковки»; сохранение существующего процесса |
| R4 | Выполняется R3 | Генератор WMS-489 получает полный тот же нормализованный КИЗ, включая разделители GS и криптографическую часть, и явно одну копию. Новый КИЗ из пула не выделяется; повторно отсканированная этикетка соответствует исходной маркировке | «Дубль точно такого же KIS», «та же технология» |
| R5 | Рабочее место использует поддерживаемый Chrome/Edge и штатную тихую печать | Печать отправляется на настроенный принтер по умолчанию без подтверждения каждого задания. WMS Print, дополнительные приложения и расширения не требуются. Галочка WMS не меняет настройки браузера самостоятельно | Прямой запрет дополнительных программ; подтверждённые возможности браузера |
| R6 | Повтор ответа, повтор доставки одного запроса, перерисовка, обновление страницы, фоновый вердикт WB или возврат в поставку | Эти события не создают ещё одну автоматическую копию. Привязка и печать связаны с одним конкретным действием сканирования, а не с фактом наличия КИЗ в списке. Намеренная новая перепечатка существующим действием остаётся возможна | Защита от повторов из AGENTS.md; B07/B09 |
| R7 | Валидация/привязка отклонена либо исход сохранения пока неизвестен | Код не печатается как успешно привязанный. Существующая ошибка/восстановление работают. После потерянного ответа сначала выясняется сохранённый результат той же попытки; повтор не порождает новую привязку и слепую печать | AGENTS.md; B02/B09 |
| R8 | Привязка сохранена, но генерация этикетки или запуск браузерной печати явно завершились ошибкой | Привязка сохраняется; ошибка печати показана в контексте скана отдельно от ошибки КИЗ. Можно продолжить упаковку и воспользоваться существующей перепечаткой. Не объявлять физическую печать успешной по возврату `print()` | «Всё так же»; ограничения web API, B05/B07 |
| R9 | Печать предыдущего успешно сохранённого скана ещё обрабатывается; оператор начинает следующий | Дополнительный этап печати не теряет следующий скан и не подменяет заказ/КИЗ данными последнего активного заказа. Задания отправляются последовательно; фокус возвращается в поле, ошибки одного задания не ломают следующий заказ | Непрерывный процесс упаковки; B07 |
| R10 | Оператор переключает галочку или поставку во время запроса | Решение печатать относится к состоянию галочки на момент этого скана. Уже подтверждённое задание не переназначается новой поставке/селлеру. Выключение прекращает создание заданий для последующих сканов | Решение D2; границы клиентов AGENTS.md |
| R11 | Изменяется общий генератор или механизм печати | Существующая ручная перепечатка КИЗ в упаковке, приёмке возвратов и «Честном знаке» сохраняет своё поведение; соседние документы и данные других продавцов не меняются | Повторное использование WMS-489, AGENTS.md |

## 4. Принятые решения и технические границы

**D1 — управление на рабочем месте.** Галочка изначально выключена; её явный выбор сохраняется локально для текущего пользователя и ФФ на данном браузере. Это настройка оператора/рабочего места, а не глобальное свойство поставки для всех сотрудников. Использовать существующий механизм локальных настроек, если он имеется; новая серверная таблица для галочки не требуется. Основание: печатает конкретный рабочий компьютер, включение другим сотрудником не должно неожиданно менять его работу. Не менять сам механизм привязки.

**D2 — событие успешного скана.** Снимок заказа, полного кода и выбранной галочки фиксируется для конкретной попытки. Точка интеграции — успешный результат commit, до независимого `load(true)` и без ожидания `meta_status=accepted`. `status=error` с `wb_pending_confirmation` не приравнивать к успеху; восстановление должно подтвердить именно сохранённый результат. Текущий ответ `ok` не содержит самостоятельного признака «новая операция», поэтому просто добавленный вызов `print()` после каждого `ok` не обеспечивает защиту от повторов.

**D3 — общий генератор, отдельная обработка сбоя печати.** Переиспользовать существующий генератор и применимые механизмы WMS-489. Не отправлять копию в сторонний сервис, не создавать новый пул маркировки. Потеря ответа после фактического вызова печати не разрешает автоматически отправить её повторно. Текущий `releaseClaim` после любой ошибки нельзя переносить без различения ошибки до запуска и неопределённого исхода после него. Реализация обязана показать эту границу в тестах, а не обещать «ровно один физический отпечаток» через браузер.

**D4 — тихая печать без дополнительной программы.** Выбран штатный `window.print()` и разовая настройка поддерживаемого браузера/принтера. Политика браузера действует шире страницы WMS, поэтому влияет и на другие печатные документы; она не является настройкой одной галочки. Нельзя гарантировать, что окно предпросмотра даже кратко не появится: политика автоматически закрывает его. Принтер должен уже работать в ОС; неподдерживаемый/ненастроенный браузер покажет обычный диалог. Сайт не может скрытно включить эту политику. Никакие настройки компьютера в рамках анализа не менялись.

**D5 — область площадок.** Галочка относится к существующей упаковке FBS с рабочим сканированием маркировки. Не добавлять отдельное требование или блокировку по WB/Ozon; сохранить текущую специфику выбора заказа и подтверждений каждой площадки. QR-сценарий владельца проверяется прежде всего на WB; общий компонент требует проверки Ozon, если он затронут.

Решения основаны на прямом запросе, текущем коде, навыке `wms-product-decision` и случаях библиотеки: действие в контексте документа, отсутствие самодельных запретов, B02/B05/B07/B09. Авторизованный экран и физическое оборудование ещё предстоит посмотреть; это не основание объявить приёмку.

## 5. Проверки

Приёмка 22.09.2026: прочитан точный итоговый diff `55ca079c..c2640d89`, код ответов WB/Ozon, генератор и целевые тесты. Ниже отделены подтверждения чтением кода от ещё не выполненных браузерных и физических сценариев. Исходные ожидания не менялись.

| Проверка | Требования | Подготовка → действие → ожидаемый результат и проверка | Вердикт |
|---|---|---|---|
| C1 | R1, R2 | Открыть реальную упаковку с маркируемыми товарами, обычным и длинным названием → сверить верхнюю галочку с текущим UI, затем сканировать при выключенной → прежняя привязка/подсветка, ноль вызовов печати; проверить сохранение после открытия | Не проверено полностью — код помещает стандартный Checkbox над полем скана; выключенный snapshot не ставится в очередь. Авторизованный экран, длинные названия, реальный фокус не проверены. |
| C2 | R3, R4, R5 | Настроенный браузер и принтер, полный тестовый КИЗ с GS → QR заказа, КИЗ при включённой галочке → сохранена прежняя привязка, одна этикетка без ручного подтверждения; считать её DataMatrix и побайтно сопоставить нормализованный код, проверить размер/поля и отсутствие второй страницы | Не проверено полностью — нарушение R4 версии e2f95046 исправлено в c2640d89: печатается bound_kiz из успешного серверного ответа, а не raw. Серверный тест с ]d2 и &lt;GS&gt; подтверждает ответ с реальными GS; одна копия задана явно. Выход бумаги и повторное считывание этикетки не проверены. |
| C3 | R3 | WB вернул успешное сохранение, метаданные ещё проверяются → скан → печать запускается до accepted; последующее позеленение не запускает новую копию | Подтверждено чтением кода — enqueue стоит после status=ok и newly_bound=true, до load(true), без проверки accepted. При refresh и обновлении метаданных вызова печати нет. Реальная задержка WB не проверена. |
| C4 | R6, R7 | На тестовой среде повторить один ответ/запрос, затем обновить и открыть поставку; отдельно потерять ответ после commit → подтвердить ту же привязку, отсутствие новой автоматической копии от повтора/refresh; проверить явную ручную перепечатку | Подтверждено в программной части — WB и Ozon повтор существующего кода возвращают newly_bound=false; очередь помнит attemptId в sessionStorage и не повторяет его. Целевой серверный тест проверяет true/false первого и повторного WB-ответа. Сетевая потеря ответа и ручной путь в браузере не проверены. |
| C5 | R7 | Неверный код, отказ validate, ошибка commit, pending_confirmation → печать не вызывается на неподтверждённый успех; после штатного сброса QR другого заказа работает, данные прежних заказов сохранены | Подтверждено чтением кода — ветка status!=ok возвращается до enqueue, включая pending_confirmation; исключения validate/commit попадают в прежний catch. Сброс сохранён. Реальный переход после ошибки в браузере не проверен. |
| C6 | R8, R9 | Успешная привязка; вызвать ошибку генератора до print, затем неопределённость после запуска → привязка остаётся, ошибка читается в скане, нет слепого автоматического повтора; продолжить следующий заказ и отдельно ручную перепечатку | Подтверждено в программной части — Promise печати не awaited в транзакционном пути, ошибка имеет отдельное состояние, claim попытки остаётся при ошибке, tail после ошибки продолжает очередь. Физическая неопределённость и ручное восстановление не проверены. |
| C7 | R9, R10 | Серия трёх разных заказов и КИЗ, медленная печать → сканировать подряд, переключить галочку/поставку при активном запросе → нет потерь и подмены кодов, порядок заданий соответствует сканам, фокус восстановлен, следующий скан учитывает новое состояние галочки | Не проверено полностью — snapshot фиксирует orderId/enabled; очередь получает bound_kiz из ответа конкретного запроса, очередь последовательна и не ждёт физическую печать; изменение workspaceGeneration защищает показ старой ошибки. Серия реальных сканов, взаимодействие iframe.focus и фокуса ввода, переключение поставки при запросе не проверены. |
| C8 | R5, R8 | Прогон Chrome/Edge Windows и macOS: отключить принтер/бумагу, отправить задание, восстановить → проверить штатную очередь ОС; WMS не заявляет подтверждённый физический выход. Ненастроенный браузер не выдавать за тихую печать | Не проверено — политики Chrome/Edge исследованы по документации, но настройки компьютеров, ОС-очередь, принтер и бумага не проверялись. Код не показывает положительного подтверждения физической печати. |
| C9 | R11, D5 | Ручная перепечатка в FBS, возвратах и ЧЗ; второй продавец и Ozon в общем компоненте → прежние права/подтверждения и код печати сохраняются; проверить отсутствие изменения остатков/резервов и передачи чужого кода | Подтверждено в границах diff — общий генератор, ручная перепечатка, остатки и резервы не изменялись; Ozon возвращает newly_bound=false для существующего и true для нового кода. Операторские сценарии второго продавца/Ozon и ручная печать не проверены. |
| C10 | R1, R10, D1 | Два пользователя/ФФ в одном браузере и два рабочих места → выбор галочки сохраняется в своём контексте, не включает печать другому оператору; визуально проверить узкий экран и длинные названия | Не проверено полностью — default=false и локальное разделение по tenant/sub подтверждены кодом и целевым тестом; warehouseId из ключа удалён в c2640d89, область соответствует D1. Узкий экран и поведение двух реальных рабочих мест не проверены. |

## 6. Сверка с запросом

- «Без дополнительных программ» → R5, D4 и отдельный отчёт браузеров.
- «Сверху аккуратная галочка» → R1, D1, C1/C10.
- «QR-код и затем КИЗ», «всё как сейчас, привязывается, подсвечивается» → R2/R3/R7, существующий pipeline.
- «Дубль точно такого же» → R4/C2: полный код, одна копия, не новый код из пула.
- «Сразу вылазит из принтера» → R3/R5/C2/C8; физический результат требует проверки реального принтера, web API не подтверждает бумагу.
- «Та же технология перепечатки», «при каждом сканировании упаковки» → R4/R6/R9/R11; генератор WMS-489 и последовательность самостоятельных сканов.
- Защиты R6–R11 не вводят новых запретов упаковки; они сохраняют данные и отделяют новый скан от повторного ответа.

## Заключение

Повторная приёмка программной части выполнена на `c2640d8981255a4d7c83d27f4728998f503cb4f8` чтением точного итогового diff и целевых тестов. Обнаруженное на `e2f95046` нарушение R4 исправлено: сервер возвращает `bound_kiz`, совпадающий с фактически сохранённым полным нормализованным КИЗ; фронт печатает только его. Исходный ввод с AIM-префиксом `]d2` и заменителями GS больше не передаётся напрямую в генератор. Если сохранённого кода нет в ответе, автоматическая печать не запускается. Предпочтение галочки также приведено к D1: текущие ФФ и пользователь, без дополнительного разделения по складам.

Программная часть содержит требуемые основные условия: галочка выключена по умолчанию, успешное новое сохранение отличается от повтора через `newly_bound` и для WB, и для Ozon; ошибка/pending не печатаются; используется одна копия и последовательная очередь без автоматического повтора после ошибки. Привязка остаётся сохранённой при сбое печати. Перечитывание списка не запускает печать. Подтверждённых оставшихся технических нарушений в проверенном диапазоне не найдено.

Ведущий сообщил результаты проверок исполнителя для финальной коррекции: `pytest -q tests/test_fbs_kiz.py -k 'same_pair_is_idempotent_without_second_wb_call'` — 1 passed, 119 deselected; `ruff check app/api/fbs_kiz.py app/services/fbs_kiz_service.py app/services/ozon_kiz_service.py tests/test_fbs_kiz.py` — pass; `npx vitest run src/screens/v2/fbsKizAutoReprint.test.ts src/screens/v2/FfFbsSupplyWorkspace.wms506.test.ts` — 2 файла, 8 passed; `npx tsc --noEmit -p tsconfig.app.json` — pass. Аналитик прочитал соответствующие тесты и итоговый код; повторно широкие наборы не запускал. Серверный тест проверяет первый и повторный WB-ответ и нормализацию `]d2` / заменителей GS. Тесты с чтением исходника не выдаются за браузерную приёмку.

**Полная задача не принята:** авторизованный экран, работа сканера во время печати, применение штатной тихой политики на Windows/macOS и физическая этикетка с повторным считыванием **не проверены**. Перекрёстное ревью **не завершено**: по сообщению ведущего два запуска Opus CLI прерваны без заключения; PASS отсутствует. Production в рамках этой приёмки не менялся. Для завершения нужны заключение перекрёстного ревью и оставшиеся реальные сценарии C1/C2/C7/C8/C9/C10. Отсутствие физического подтверждения браузерного API нельзя скрывать формулировкой «успешно напечатано».
