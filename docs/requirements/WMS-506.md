# WMS-506 · Подключение Ozon без лишнего требования роли seller/info

## Исходный запрос и уточнения

22.09.2026. Ведущий передал обращение: клиент не может подключить Ozon. После диагностики владелец потребовал дословно:

> убирай нахуй это из вмс

Контекст приказа — убрать препятствие подключения, связанное с проверкой через `/v1/seller/info`. Это не поручение удалить интеграцию Ozon, отключить проверку действительности ключа или менять ключ в кабинете Ozon.

Ведущий передал установленные диагностикой факты: исходная пара Client-Id/Api-Key действительна; `POST /v1/roles` отвечает200, доступны роли Returns/Warehouse/Product/Supply order/Posting FBS, expires_at —2026-12-21. Нужные текущим операциям read-only методы складов, товаров и FBS postings отвечают200. `POST /v1/seller/info` отвечает403, code7, `Api-Key is missing a required role for a method`. Значения Client-Id и Api-Key в документ не включаются. Аналитик не повторял внешние запросы с клиентским ключом; эти факты — результат диагностики ведущего.

Уточнение ведущего при постановке: сохранить существующий публичный контракт ошибок. Недействительная пара отличается от временной недоступности; transport/429/5xx могут оставаться одной публичной категорией. Новый UI-код/текст не нужен. Проверка полного списка ролей как условие подключения запрещена: владелец потребовал убрать лишний барьер, а не заменить его другим.

## Существующий процесс и цель

База ветки `codex/wms506-ozon-role-validation` — `origin/etalon`, продуктовый SHA `3e125074`. Селлер вводит два значения в существующем разделе настроек Ozon. `SellerSettingsScreen.tsx` отправляет `PUT /integrations/ozon/self/account`; повторная проверка сохранённого подключения использует `POST /integrations/ozon/self/account/test-connection`.

По прочитанному `backend/app/services/ozon_client.py` обе операции вызывают `validate_ozon_credentials`, которая делает один read-only POST к фиксированному Ozon host, сейчас `/v1/seller/info`, с пустым JSON и запретом redirect. API в `backend/app/api/ozon_integration.py` превращает401/403 в `ozon_credentials_invalid` и прекращает сохранение. Отсюда действующий ключ с правами рабочих операций ошибочно выглядит недействительным. После успешной проверки кандидат сохраняется существующим `MarketplaceAccountService`, затем запускается существующий импорт каталога; неудача этого импорта не должна отменять уже подтверждённое подключение.

Цель — действующая пара подключается и повторно проверяется через `/v1/roles`, не требуя роли для seller/info. Сохраняются существующие данные, права, границы селлеров и понятное различие между отказом аутентификации и временной проблемой проверки.

## Требования

| ID | Условие и действие | Проверяемое поведение | Основание |
|---|---|---|---|
| R1 | Селлер сохраняет новую пару или заменяет существующую; Ozon подтверждает её успешным ответом `/v1/roles`. | Проверка использует `POST /v1/roles` с этой парой. `/v1/seller/info` не вызывается как обязательная проверка и его недоступность не мешает подключению. Успех2xx проходит в существующее сохранение; GET статуса после сохранения показывает connected/valid. | Приказ убрать установленное препятствие и результаты диагностики. |
| R2 | Селлер повторно проверяет уже сохранённое подключение. | Используется та же новая граница проверки `/v1/roles`; успешная проверка обновляет статус valid. Не остаётся второй живой путь, требующий seller/info. | Исправление общей функции должно охватить обоих callers; B04. |
| R3 | `/v1/roles` отвечает401/403 либо проверка временно невозможна. | Публичный контракт сохраняется:401/403 →422 `ozon_credentials_invalid`; transport/timeout/429/5xx →503 `ozon_validation_unavailable`; прочий неожиданный статус →502 `ozon_validation_failed`. Существующий UI отличает неверную пару от временной проблемы и позволяет повторить действие. Разделять transport и429 новым UI не требуется; существующие внутренние причины при test-connection сохраняются. | Уточнение ведущего; человекочитаемый отказ без ложного обвинения в неверном ключе. |
| R4 | Проверка кандидата завершилась ошибкой или ответ не получен. | Кандидат не сохраняется; прежнее подключение и его зашифрованные credentials не заменяются. Для первой настройки подключение не возникает. Повтор с успешной проверкой сохраняет кандидат обычным путём. При test-connection допустимо существующее обновление validation metadata, но сами credentials не заменяются. | Действующий контракт и сохранность данных; B03/B05. |
| R5 | Ozon вернул успех проверки, но набор ролей не включает seller/info либо произвольную роль из расширенного списка. | WMS не вводит локальный список обязательных ролей, проверку полного доступа, дополнительный запрос seller/info или новую форму/шаг. Проверка подлинности не превращается в обещание доступности всех операций Ozon. | Прямое уточнение ведущего; плохой случай «Ozon стал самодельным запретом работать». |
| R6 | При замене метода проверки выполняется внешний запрос. | Сохраняются фиксированный Ozon host, корректные заголовки Client-Id/Api-Key, POST с пустым JSON, запрет redirect и ограниченное ожидание. Ключ не появляется в публичном ответе, исключении или диагностических сообщениях. Проверка остаётся read-only; ключи в кабинете Ozon не изменяются. | Минимальный объём исправления и правила сохранности секретов. |
| R7 | Операция сохранения/проверки выполняется повторно или другим пользователем. | Существующие tenant/effective-seller/Settings guards, шифрование, единственность marketplace account и соседние подключения WB/другого селлера сохраняются. Повтор одной успешной пары не создаёт второе подключение. Исправление не меняет рабочие методы товаров/FBS и политику импорта после сохранения. | Границы клиента, существующий процесс; B08 и применимые повторные вызовы. |

## Решения аналитика

D1. Меняется read-only метод общей валидации, а не набор разрешённых действий пользователя. Это устранение подтверждённого технического дефекта. Нет оснований требовать выдачу новой роли, выпуск другого ключа или весь каталог ролей.

D2. Сохраняется проверка по успешному HTTP-статусу, как в исходной границе транспорта. В этой задаче не вводится анализ тела roles, локальное толкование срока действия или обязательных полей ролей. Поэтому отсутствующее необязательное поле в успешном ответе не становится новым запретом. Неуспешный статус не принимается за успех.

D3. Нового интерфейсного решения нет. Прочитан существующий экран настроек и его error mapping; визуальный просмотр на этапе постановки не выполнялся. Приёмка пользовательского пути должна отличать наблюдение экрана от проверки API. Искусственные ошибки выполняются только на изолированной среде с синтетическими ключами.

D4. Навыки аналитика и продуктового решения, owner-cases/failure-cases прочитаны из основного checkout по `/Users/deniscivkunov/Projects/WMS/docs/reviews/2026-09-11-analyst-draft/`: их нет в baseline этой ветки, копии не добавляются. Применимы запрет самодельного барьера, B03 о границе ошибки/успеха, B04 о всех путях общей функции, B05 о повторе после ошибки и B08 о соседнем scope. Актуальное прямое правило владельца о продолжении работы при обнаружении секретов имеет приоритет над старой stop-формулировкой baseline AGENTS.

## Проверки

Вердикты до реализации и приёмки намеренно пусты. Числа требований не заменяют наблюдение сценария.

| ID | Требования | Проверка | Вердикт |
|---|---|---|---|
| C1 | R1, R5, R6 | Подготовить fake transport: roles→200, seller/info→403 missing role. Сохранить синтетическую пару. Проверить запрос POST `/v1/roles`, фиксированный host, заголовки, `{}`, follow_redirects=False; обращения seller/info нет. Проверить сохранённую запись и GET connected/valid. Никакие роли из ответа не становятся дополнительным условием. | |
| C2 | R2, R6 | Для уже сохранённой пары выполнить test-connection при roles→200 и запрете seller/info. Проверить новый path и статус valid/last_validated_at; credentials не изменились. | |
| C3 | R3, R4 | Для401 и403 roles выполнить PUT первой настройки и замены существующей. Ожидать422/ozon_credentials_invalid; новой записи/замены ciphertext нет. Для test-connection проверить invalid metadata без смены credentials. | |
| C4 | R3, R4 | По отдельности вернуть transport exception, timeout,429 и500. PUT даёт503/ozon_validation_unavailable и не заменяет прежние credentials; test-connection сохраняет прежние различимые внутренние причины. Вернуть roles200 и повторить: подключение сохраняется. | |
| C5 | R3, R4, R6 | Вернуть неожиданный неуспешный статус, включая redirect. Проверить502/ozon_validation_failed, redirect не выполнен и кандидат не сохранён; успешный повтор работает. | |
| C6 | R7 | Повторить успешный PUT одинаковой пары, включая существующую проверку двух параллельных сохранений. Проверить одну запись подключения; другой seller/tenant и WB credentials прежние. Подтвердить действующие отказы без Settings/чужого scope и отсутствие секретов в публичном ответе. | |
| C7 | R1–R4 | На идентифицированном изолированном приложении пройти существующий экран настройки Ozon с синтетической парой: сохранить, повторно открыть статус, проверить подключение; отдельно получить отказ проверки и успешно повторить. Привязать видимые результаты к серверному состоянию. Не выдавать API-only или mock-host тест за внешний результат Ozon. | |
| C8 | R1, R2, R5–R7 | Просмотреть итоговый diff и все callers общей валидации; убедиться, что нет product dependency на seller/info, нового role whitelist/формы, изменений соседних рабочих методов и политики импорта после сохранения. Запустить целевые transport/account/API регрессии и применимые проверки backend; отделить тесты, review, browser, production. | |

## Сверка исходного запроса

«Клиент не может подключить Ozon» → R1/R2 и сохранённый результат C1/C2/C7. «Убирай ... это» в установленном контексте → удаление зависимости seller/info, R1/R5, без удаления интеграции и без обхода аутентификации. Сохранность ключа и честная ошибка → R3/R4/R6. Соседние селлеры/площадки и повторы → существующие гарантии R7, а не новый объём функциональности. У каждого требования есть основание в запросе, уточнении или действующем контракте. Новый blocker и новая схема ролей не добавлены.

## Заключение

Постановка передана разработчику; реализация, независимое review и приёмка ещё не выполнены. Пустые вердикты означают отсутствие приёмки, а не подтверждение результата. Подключение клиента в production и деплой по этому документу не заявлены.
