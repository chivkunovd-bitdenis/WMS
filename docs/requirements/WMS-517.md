# WMS-517 · Селлер: отчёт и локально подписанный вывод FBS-КИЗ из оборота

## Исходный запрос

> «Окей, тогда процесс будет такой. Система, соответственно, должна фиксировать те кИзы по товарам, которые мы передали, причем на сракте, которые мы печатали, или те, которые мы просто сканировали — наплевать. То есть если они привязаны к товарам, которые мы передали, нам их нужно вывести в оборот. Соответственно, у селлера должен появиться отчет, типа там кИзы выведенные из оборота, или что-то там вывести из оборота, короче, что-то такое. Вот, попадая в него, он автоматически видит, ну, может выбрать период, он может выбрать там товары. Соответственно, период — это дата, когда было передано на VB, да, отгрузка. Он там может видеть FBO-шный заказ со ссылкой на сам, собственно... ну, окей, нет, номер, короче, просто FBO-шного, FBS-ного, точнее, заказа, да, ну, либо отгрузки, соответственно, FBO. Вот, он может чекбоксами выбрать эти коды, либо вообще выбрать всё, нажать... ну, он видит галку там выведено из оборота или не выведено, у него есть фильтр типа только не выведенные, да, он может прожать всё и нажать кнопку вывести из оборота. В этот момент система формирует, ну, соответственно, обращение, да, копию честного знака, проверяет его подпись, как бы дает ему выбрать сертификат, и с этой подписью отправляет просто, соответственно, запрос.»

Уточнение владельца после просмотра первой версии прототипа:

> «Сука, что я тебя просил? Я просил только то, что, блять, передано на ВБ, то есть то, что мы нажали отгрузить. Нахуй ты мне туда статусы эти въё, блять, готов, не готов, там чё-то ВБ. Блять, нахуй эта каша нужна, я не понимаю. Источник? Что, блять, за источник? Что это за столбец? Нахуй здесь, блять, информация, что он отсканировал, блять, дебил? Сука, это уже то, что нужно вывести, оно уже передано, всё. Убирай нахуй этот источник. Нахуй убирай этот статус. Точнее статус. Чё там у тебя? Блять, статус простой. Либо выведен, либо не выведен. Всё, не нужен там приписок, кроме ошибки. Ошибка окей, ЧСЗ не принял документ. Но ты думаешь, он тебе вернёт реальный, блять, какой-то этот статус? Здесь только статус не вывода, то есть они уже все введены. Здесь статус, здесь только ошибка, если мы не смогли его вывести. Если он возвращает детализацию ошибок, хорошо, тогда их здесь нужно показывать. В остальном случае нет. Дальше, блять, эти строки должны быть узкими, группа нахуй оттуда убирай. Наименование и артикул разнеси в отдельные столбцы, сделай так, чтобы это было узко. Заказ ВБ номер, блять... Короче, сделай, чтобы эти строки были узкими, блять, они сейчас выглядят уёбищно. Ты понимаешь, их вводят там, блять, тысячами просто, сотнями, блять, тысячами, заебёшься листать. Должна быть такая, блять, более плоская хуйня.»

Уточнение владельца после просмотра второго экрана прототипа:

> «Господи, что это за каша на втором экране? Нахуй здесь эти, блять, шаги? Нахуй здесь это уведомление: будут отправлены коды? Нахуй здесь эти причины: передана продажа, блять, дистанционная продажа? Нахуй вообще этот блок? Нахуй здесь предварительная проверка? Нахуй здесь, блять, вот это уведомление? Ёб твою мать, да что ты, блять, за дебил-то нахуй, я не понимаю, блять. Нахуя это всё здесь нужно? Там просто должен быть, дай бог, список кизов и кнопка выбрать, блять, сертификат. Или вообще нахуй ничего не надо. Просто, блять, ты нажимаешь, и у тебя сразу выскакивает просто сертификат. Ты нажимаешь OK, передать. Всё. И уже если только случилась какая-то ошибка, то появляется модалка с конкретными кизами и с причиной, что не так. Всё, нахуй здесь больше ничего, блять, не нужно.»

Уточнение владельца для новой постановочной волны:

> «Код должен быть реально готов к тому, что он даст реквизиты ЧЗ и вставит сертификат,
> без следующего раунда разработки; реальные production withdrawal не выполнять.»

## Уточнение боевой фазы и источники

После приёмки кликабельного прототипа WMS-517 расширена до постановки реальной
интеграции. UX последнего уточнения владельца сохраняется без новых экранов и
технических шагов, но результатом боевой фазы должны стать юридически значимый
документ Честного знака, сохраняемое состояние операции и восстановление после сбоев.

Протокол подтверждён по официальной live-документации True API версии 731.0 от
23.09.2026: [запрос auth challenge](https://docs.crpt.ru/gismt/True_API/#_запрос_авторизации_при_единой_аутентификации),
[получение ключа сессии](https://docs.crpt.ru/gismt/True_API/#_получение_ключа_сессии_при_единой_аутентификации),
[создание документов](https://docs.crpt.ru/gismt/True_API/#_единый_метод_создания_документов),
[вывод из оборота](https://docs.crpt.ru/gismt/True_API/#_вывод_из_оборота),
[причины выбытия](https://docs.crpt.ru/gismt/True_API/#_справочник_причины_выбытия),
[список документов](https://docs.crpt.ru/gismt/True_API/#_метод_получения_списка_загруженных_документов_в_гис_мт),
[документ по идентификатору](https://docs.crpt.ru/gismt/True_API/#_метод_получения_содержимого_документа_по_идентификатору)
[статусы документов](https://docs.crpt.ru/gismt/True_API/#_справочник_статусы_документов),
[матрица МОД](https://docs.crpt.ru/gismt/True_API/#proverka_mod_pri_vyvode_iz_oborota)
и [получение МОД участника](https://docs.crpt.ru/gismt/True_API/#_метод_получения_списка_мод_по_участнику_оборота_товаров).
Поля фактической цены WB подтверждены официальными release notes
[№ 284](https://dev.wildberries.ru/release-notes?id=284) и
[№ 276](https://dev.wildberries.ru/release-notes?id=276). Браузерный контракт основан
на официальной документации
[CryptoPro CAdES Browser Plug-in](https://cryptopro.ru/products/cades/plugin/developer),
[активации](https://docs.cryptopro.ru/cades/plugin/plugin-activation),
[отсоединённой подписи](https://docs.cryptopro.ru/cades/plugin/plugin-samples/plugin-samples-sign-detached)
и [кодировании точных байтов](https://docs.cryptopro.ru/cades/plugin/content-encoding).
Официальное приложение 2 True API рекомендует браузерный пакет
[`@crpt/cades-pluginer`](https://www.npmjs.com/package/@crpt/cades-pluginer) и вызов
`CadesPluginer.signMessage(data, certificate)`. Закреплённый архив версии `0.0.1`
доступен в [официальном npm registry](https://registry.npmjs.org/@crpt/cades-pluginer/-/cades-pluginer-0.0.1.tgz):
реализация подписывает исходную JS-строку challenge через стандартный
`CADESCOM_STRING_TO_UCS2LE`, `CADESCOM_CADES_BES` и `detached=false`. Этого достаточно,
чтобы закрыть прежний B3 как неизвестность протокола; sandbox остаётся доказательством
работоспособности конкретного сертификата и участника, а не способом додумать профиль.
Кабинеты секретов не открывались, пользовательские сертификаты и ключевые контейнеры
не перечислялись. Позднее выполнена только локальная независимая OpenSSL-подпись
одноразовым RSA-сертификатом; КриптоПро/ГОСТ/USB-токен не использовались.

## Уточнённый бизнес-контекст

- Для WB FBS продавец остаётся владельцем товара и выводит КИЗ из оборота после
  фактической передачи товара Wildberries с причиной «Дистанционная продажа».
- Для FBO/FBW после УПД собственником становится маркетплейс и выводит код сам. По
  последнему уточнению владельца такие строки вообще не показываются в этом реестре.
- В реестр и операцию попадают только КИЗ FBS-заказов, по которым в WMS уже нажали
  «Отгрузить» и подтверждена передача WB. Отменённые до передачи заказы, FBO/FBW и
  возвраты в этом экране не показываются.
- Способ появления КИЗ в WMS — печать из пула или сканирование внешнего кода — не
  влияет на включение: важны привязка к товару и заказу и подтверждённая передача.
- Отчёт WB используется для последующей сверки, но не является первичным триггером.
  Передача SGTIN в заказ через WB API сама по себе не означает вывод из оборота.
- Браузер подписывает через КриптоПро CSP/CAdES Browser Plug-in. Приватный ключ и PIN
  остаются в CSP/криптоносителе селлера; WMS получает публичные сведения сертификата,
  точные данные для подписи и готовые подписи.
- Целевой auth-путь — `GET /auth/key`, локальная присоединённая подпись challenge и
  `POST /auth/simpleSignIn` с `unitedToken=true`. Старый сохранённый JWT не является
  целевой боевой архитектурой: JWT поддерживается только до конца 2026 года, а UUID
  без challenge — только до 30.09.2026.
- Для FBS создаётся `LK_RECEIPT` с `action=DISTANCE`. Цена по каждому КИЗ обязательна
  в целых копейках с НДС; один документ содержит только одну товарную группу.
- Принятый React-прототип остаётся визуальным эталоном. Реальная интеграция, хранение
  операций и production-деплой в нём отсутствуют и принимаются отдельно.

## Подтверждённый текущий процесс

Селлер уже открывает раздел `/seller/honest-sign`, но сейчас это дашборд остатков
кодов: личный запас, общие корзины, напечатанные коды, загрузка КМ и лента расхода.
Отчёта по переданным КИЗ и действия вывода из оборота в нём нет. В FBS-рабочей области
сейчас прямо указано, что вывод из оборота остаётся на продавце.

WMS уже хранит полное значение FBS-маркировки, источник `pool`/внешний скан, связь с
заказом, товарной строкой и `MarkingCode`. Поставка хранит `delivered_at`; действующий
сервис проставляет это время только при подтверждённой операции передачи и переводит
поставку в `in_delivery`. Это существующая основа для периода отчёта, но её пригодность
для всех исторических заказов нужно отдельно подтвердить на данных перед боевым запуском.

На момент первой постановки клиент True API выполнял только чтение `cises/info` и
`cises/check` с заранее полученным JWT. Текущий local slice добавил v3/v4 transport,
ledger, builder, MOD-проверку, recovery на fixtures, browser attached auth и закреплённый
vendor script. Эти части приняты только по code/fake-adapter/emulator/OpenSSL evidence:
физическая КриптоПро, ГОСТ-провайдер, USB-токен/PIN и live-вызовы не проверялись.
Приватный ключ, контейнер сертификата и PIN в WMS не передаются и храниться не должны.

`MarkingCode` уже хранит tenant, seller, product, полный `cis_code` и источник.
`FbsOrderMarking` связывает код с FBS-заказом, а `FbsSupply.delivered_at` выставляется
после подтверждённой передачи WB. `BillingProfile` хранит ИНН/КПП селлера. Текущий
`FbsOrder.price` заполняется полем WB `price`; это не подтверждённая фактическая сумма
покупателя и для `product_cost` использоваться не должно. Local slice уже добавил
append-only price observations и модели операции; товарная группа и МОД по-прежнему
берутся свежими внешними проверками, а не хранятся в Product/warehouse mapping.

Текущий экран в production пытались открыть визуально, но браузерный коннектор дважды
завершил навигацию по таймауту. Поэтому существующий UI подтверждён чтением React-кода,
а не визуальной проверкой; макет нужно принять отдельно в браузере.

## Цель и граница результата

В кабинете селлера экран «Честный знак → Вывод из оборота» показывает плоский плотный
реестр только уже переданных WB FBS-КИЗ. Селлер выбирает строки, нажимает основное
действие и получает единственное компактное окно локального сертификата с действиями
«Отмена» и «Подписать и отправить». После одной пользовательской команды WMS внутри
одной сохраняемой операции получает auth-token, проверяет КИЗ, формирует и подписывает
документы, отправляет их и дожидается окончательного результата. В интерфейс не
выносятся challenge, документы, товарные группы, polling или reconciliation.

Прототип этого пути уже принят. Боевая реализация считается принятой только после
серверного хранения, sandbox end-to-end и разрешённой production-проверки по матрице
ниже; локальный макет и mock-ответы этого не доказывают.

## Требования принятого UX-прототипа

R1–R13 ниже фиксируют уже принятую пользовательскую границу. Технические упоминания
в них уточняются обязательными боевыми требованиями BR1–BR22; при расхождении точный
протокол BR имеет приоритет, а последний UX-запрет владельца сохраняется.

### R1. Экран в контексте раздела «Честный знак»

В seller-портале раздел «Честный знак» содержит переход «Вывод из оборота». Он открывает
отдельный экран отчёта внутри этого раздела, сохраняет существующий стиль кабинета и
не переносит действие в настройки или FBS-рабочую область. На экране прототипа явно
видно, что это демонстрация без реальной отправки в Честный знак.

### R2. Период и состав отчёта

По умолчанию и после выбора периода отчёт использует подтверждённую дату фактической
передачи WB, а не дату заказа, плановую отгрузку или дату получения отчёта WB. В реестре
есть только колонки «Передано WB», «Заказ WB», «Артикул», «Наименование», «КИЗ» и
«Статус». Артикул и наименование разделены. Схема работы, источник КИЗ, товарная группа
и служебные подробности обычного состояния в таблице не показываются.

### R3. Источник КИЗ не меняет результат

Два КИЗ одного селлера, товара и подтверждённой FBS-передачи показываются и выбираются
одинаково независимо от того, были они напечатаны из пула WMS или отсканированы извне.
Источник не создаёт отдельный режим, не является основанием скрыть код и вообще не
показывается пользователю в реестре.

### R4. В реестре только уже переданные FBS-КИЗ

Строка появляется только после подтверждённого действия «Отгрузить» по FBS-заказу
текущего селлера. FBO/FBW, ещё не переданные FBS-заказы, отменённые до передачи заказы
и возвраты не показываются ни как кандидаты, ни как информационные строки.

### R5. Выбор кодов и массовое действие

Доступны только фильтры периода, товара/поиска и «Только не выведенные»; отдельного
сложного фильтра статусов нет. Селлер может выбрать строки чекбоксами либо выбрать все
доступные строки только на текущей видимой странице после применения фильтров. Размер
страницы переключается между 50, 100 и 250 строками. Переход на другую страницу или
изменение фильтра не подписывает невидимые строки. Кнопка «Вывести из оборота» находится
на этом же экране и активна только при непустом допустимом выборе.

### R6. Прямое действие без промежуточного экрана

По клику «Вывести из оборота» сразу открывается единственное компактное окно выбора
сертификата. До него и внутри него нет предпросмотра состава, stepper, уведомления о
будущей отправке, причины «Дистанционная продажа», периода, числа КИЗ или документов,
списка КИЗ, товарных групп, `LK_RECEIPT`, предварительной проверки либо иных технических
стадий. Коды разных продавцов никогда не смешиваются, а разделение по товарным группам
выполняется внутри интеграции и не выносится в пользовательский путь.

### R7. Внутренняя проверка КИЗ

Будущая интеграция перед формированием документа повторно читает состояние выбранных
КИЗ через True API и допускает только коды, для которых вывод с выбранной причиной
разрешён. Проверка идёт внутри одного действия без отдельного экрана, шага или успешного
уведомления. Код с уже выведенным, заблокированным, отсутствующим или изменившимся
статусом не отправляется повторно. Если Честный знак фактически возвращает ошибку по
коду, она попадает в итоговую модалку ошибки по R9; WMS не придумывает свою причину.

### R8. Единственное окно сертификата и внутренние подписи

Единственное окно предлагает выбрать локальный сертификат текущего селлера и содержит
только действия «Отмена» и «Подписать и отправить». За этой кнопкой браузер через
КриптоПро CSP/CAdES Browser Plugin выполняет подпись авторизации True API и отдельную
отсоединённую подпись точных байтов `LK_RECEIPT`, но challenge, подпись документа и
прочие технические стадии не показываются как отдельные шаги. Приватный ключ, контейнер
и PIN не отправляются и не сохраняются в WMS. «Отмена» закрывает окно, не меняет строки
реестра и позволяет запустить действие повторно.

### R9. Только успешный результат или фактическая ошибка

В обычном реестре есть только «Не выведен», «Выведен» и «Ошибка». Для первых двух
состояний не показываются пояснения, готовность, статус WB или иной служебный текст.
Только финальный успешный статус True API переводит строки в «Выведен»; окно выбора
сертификата закрывается, допускается краткий toast об успехе. Ожидание, проверка,
разделение документов и иные технические стадии отдельными окнами или шагами не
показываются. Только при фактической ошибке Честного знака открывается отдельная простая
модалка со списком конкретных КИЗ и конкретной возвращённой причиной по каждому. Если
детализации нет, WMS её не выдумывает. Будущая рабочая реализация сохраняет результат
между открытиями; прототип показывает состояния локальными данными без серверной
сохранности.

### R10. Идемпотентность и потеря ответа

Одна подтверждённая попытка имеет устойчивый ключ и неизменяемый хэш точного payload.
Повторный клик, доставка того же запроса или два одновременных вызова не создают второй
документ. При потере ответа после отправки WMS сначала сверяет ранее начатую операцию и
её внешний идентификатор/статус и не отправляет новый документ вслепую. Это внутренний
контракт интеграции: прототип не показывает «Сверяем результат», повторную отправку,
технические стадии или отдельную модалку потери ответа.

### R11. Аудит без приватного ключа

Будущая реализация сохраняет tenant, продавца, пользователя, время, выбранные КИЗ и их
источники, связь с FBS-заказами/поставками, причину, товарную группу, payload/hash,
несекретные сведения сертификата, внешний идентификатор документа, попытки и ответы
True API. Приватный ключ, контейнер сертификата и PIN в аудит не попадают. Изоляция
продавцов остаётся обязательным backend-контрактом будущей интеграции, но переключение
seller-контекста и отдельный интерфейс аудита в прототип WMS-517 не входят.

### R12. Обязательная интеграционная проверка до боевой реализации

До реализации реального вызова необходимо по актуальной схеме True API для каждой
поддерживаемой товарной группы подтвердить точный состав `LK_RECEIPT` для причины
«Дистанционная продажа», включая требование фактической цены и других обязательных
полей, единицы и формат значений. Цена не выводится из плановой цены, карточки товара
или тарифа WMS без подтверждённого источника. Пока источник обязательного поля не
определён, интеграция не объявляется готовой и реальный документ не отправляется.

### R13. Возврат после передачи не маскируется повтором вывода

Отмена до передачи и возврат не показываются в этом реестре. Физически полученный
возврат уже переданного и выведенного товара требует отдельного обратного ввода в
оборот; WMS-517 не добавляет это действие и не меняет подтверждённый статус вывода.

## Продуктовые решения

1. Экран размещён в «Честный знак → Вывод из оборота», потому что это отчёт по КИЗ и
   юридически значимое действие над выбранными строками. Настройки нужны для интеграции,
   а FBS-рабочая область — для складской передачи, поэтому там новое массовое действие
   теряет контекст отчёта.
2. Основная временная ось — `delivered_at` подтверждённой передачи WB. WB-отчёт остаётся
   сверкой: ожидание отчёта откладывало бы обязательное действие и делало внешний отчёт
   вторым источником истины.
3. FBO/FBW, отмены до передачи и возвраты полностью исключены из экрана по последнему
   прямому уточнению владельца. Реестр отвечает на один вопрос: какие уже переданные
   FBS-КИЗ нужно вывести или уже вывели.
4. Источник, схема, товарная группа, верхние метрики и подробности штатного статуса
   убраны как служебная каша. Товарная группа остаётся только внутренним основанием
   разделить документы True API и нигде не показывается в штатном пользовательском пути.
5. Выбор действует только по текущей видимой странице отфильтрованной выборки. Так
   «Выбрать всё» предсказуемо при сотнях тысяч строк и не подписывает невидимые записи.
6. Один пользовательский запуск может создать несколько документов по товарным группам,
   но результат ведётся отдельно по каждому документу. Продавцы не смешиваются вообще.
7. Статус «Выведен» принадлежит подтверждённому результату Честного знака, а не клику,
   подписи, ответу HTTP или локальному складскому статусу. Вне ошибки статус не получает
   поясняющий текст.
8. Плотные одноуровневые строки, sticky header и страницы 50/100/250 выбраны из-за
   ожидаемых сотен тысяч записей; вложенные карточки и верхние status-карточки не нужны.
9. После основного действия пользователь видит только выбор сертификата. Внутренние
   проверки, документы и две подписи не превращаются в мастер шагов; отдельная модалка
   появляется лишь для фактической ошибки Честного знака с КИЗ и причиной по каждому.

## Gap-analysis перед боевой реализацией

| Область | Что подтверждено сейчас | Текущая реализация или пробел | Статус |
|---|---|---|---|
| UX | Прототип принят: FBS-only реестр, одна модалка сертификата, успех либо конкретная ошибка. | Seller UI и same-origin API связаны локально; live CRPT результата нет. | Local browser slice принят; production-submit запрещён этой волной, sandbox закрыт B2. |
| Факт передачи | `FbsSupply.delivered_at` и `in_delivery` ставятся после подтверждённой передачи WB; КИЗ связан с заказом. | Не проверена полнота исторических `delivered_at` и ценовых полей. | Для новых данных база есть; исторические строки проходят отдельный data gate. |
| Цена | WB определяет `finalPrice`/`convertedFinalPrice` как фактическую сумму со скидками ×100 и передаёт валюты. CRPT требует целые копейки с НДС. | Append-only snapshot финальных сумм и валют реализован; полнота старых данных ограничена окном WB backfill. | Local data path принят; строки без однозначной RUB-суммы остаются non-submittable. |
| Товарная группа и владелец | `cises/info` возвращает `productGroup`, `productGroupId`, `ownerInn`, status/statusEx и GTIN. | Группа CRPT в карточке товара не хранится. | Ручная категория не нужна: группа и владелец повторно читаются сервером перед документом. |
| МОД | True API публикует точную матрицу обязательности и `GET /mods/list`, который возвращает неудалённые МОД участника с ИНН, товарными группами, КПП/ФИАС и пагинацией. | Техническая проверка `/mods/list` перед builder реализована в local/emulator slice; live ответ CRPT не проверялся. | Не глобальный blocker: группы вне матрицы идут без МОД; для обязательной группы 0 или несколько валидных внешних МОД блокируют только её документ. Новая WMS-сущность/настройка не нужна. |
| Локальная УКЭП | Публично подтверждены CryptoPro API readiness, Store, проверка сертификата и механика подписи document exact bytes. На одном Mac найдены CSP 5.0.13003 и Plug-in 2.0.15003. | Adapter проверен fake-тестами, а auth CMS/UCS-2LE — независимым OpenSSL-runner с одноразовым RSA-сертификатом; наличие файлов и OpenSSL не доказывают enabled extension, ГОСТ, пользовательский сертификат или физическую подпись. | Программная граница принята; нужна физическая CryptoPro/browser/token-матрица. |
| Auth | True API v731.0 задаёт challenge `/auth/key`, присоединённую УКЭП, `/auth/simpleSignIn`, UUID-token и срок; приложение 2 задаёт браузерный helper. | Adapter, vendor asset и локальная независимая OpenSSL-проверка attached CMS/UCS-2LE реализованы. OpenSSL использует одноразовый RSA-сертификат и не доказывает поведение КриптоПро/ГОСТ/USB-токена. | **B3 закрыт официальным профилем и local code evidence; физическая матрица и sandbox остаются отдельными gate.** |
| `LK_RECEIPT` | Известны inner/outer поля, `DISTANCE`, подпись, лимиты и группировка. | Builder, immutable bytes и create-client реализованы на fixtures; реальные подпись/create ещё не предъявлены. | Internal/emulator контракт принят; sandbox evidence остаётся B2. |
| Состояние операции | В FBS-передаче уже есть образец `pending_confirmation` без слепого повтора. | Ledger операций, документов, попыток и per-KIZ результата реализован локально. | State machine принята без live-provider доказательства. |
| Idempotency | В create True API нет idempotency key или client document id. | Внутренний request key, claims и submit intent реализованы; исполнитель предъявил отдельный PostgreSQL 16 concurrency-прогон, который reviewer независимо не воспроизвёл. | Local/emulator/PostgreSQL защита принята в границе предъявленного evidence; внешняя дедупликация только сверкой. |
| Итог и восстановление | v4 `/doc/list` и `/doc/{id}/info` дают статус, body и ошибки. | Polling/reconciliation и scheduler реализованы на emulator; live неизвестный исход не воспроизводился. | После неопределённого submit повтор запрещён до доказанного исхода. |
| Прослеживаемость | Официальный публичный раздел публикует текущий режим по товарным группам; `statusEx=EMPTY` означает отсутствующее значение. | Реализован versioned code snapshot официального источника без ручного owner config; `EMPTY` нормализуется как отсутствие, неизвестная группа fail-closed только для своего документа. | Local code/fixture evidence принято; live `cises/info` не проверялся. |
| КИЗ для API | В БД может храниться полный GS1 с GS/AI91/AI92; True API принимает идентификационный код 18–74 символа. | Реализована отдельная provider-проекция AI01+AI21 с сохранением исходного `FbsOrderMarking.value` и fail-closed при неоднозначности. | Local code/fixture и DB-constraint evidence принято; live CRPT не проверялся. |
| Контуры | Опубликованы sandbox и production v3/v4 URL. | Нет назначенного sandbox-участника, сертификата и тестовых КИЗ/групп. | **BLOCKER B2** для live end-to-end и выпуска, не для реализации/эмулятора. |

Публичная True API v731.0 и её официальный browser-helper достаточны для endpoint,
browser auth profile, auth request/response, `LK_RECEIPT`, create, MOD и status flow.
**B3 закрыт постановкой и больше не является vendor/sandbox blocker.** Sandbox evidence
требуется для реального end-to-end результата с назначенными реквизитами (B2), но не
для реализации real adapter и internal orchestration. Отсутствующая цена, недопустимая группа или
неразрешённый обязательный МОД являются ошибками конкретных данных, а не глобальными
blocker задачи и не заменяются догадкой.

## Обязательные боевые требования: данные и подпись

### BR1. Реестр и серверная принадлежность строк

`GET /operations/marking-codes/self/withdrawals` работает только в текущей seller-сессии
с правом «Честный знак». Он возвращает строки текущих `tenant_id` и `seller_id`, где
WB FBS-заказ связан с КИЗ и поставкой с непустым `delivered_at` после подтверждённой
передачи. FBO/FBW, ещё не переданные и отменённые до передачи заказы, а также возвраты
не возвращаются даже при подмене query-параметров. Источник `pool`/scan не влияет.

Единственный состав отчёта задаёт серверный `eligible_rows` SQL по существующей цепочке
`FbsOrderMarking → FbsOrder → FbsSupply`, где `FbsSupply.delivered_at IS NOT NULL`, с
tenant/seller и исключениями выше. Выбранные `row_ids` повторно разрешаются тем же
предикатом и тем же составом связей при создании operation; только эти строки становятся
items и затем входят в документы. Отдельный источник строк, повторная ручная сборка
списка или расширение состава между отчётом и operation запрещены.

Фильтры и пагинация соответствуют R2–R5. Клиент получает непрозрачный `row_id`, а не
право прислать произвольный CIS. Каждый mutation повторно выбирает и блокирует строки
по tenant/seller на сервере; данные из UI не считаются источником истины.

### BR2. Неизменяемый ценовой snapshot WB

Синхронизация `/api/v3/orders` сохраняет отдельно `finalPrice`, `currencyCode`,
`convertedFinalPrice`, `convertedCurrencyCode` и источник/время snapshot. Поля WB уже
выражены ×100: дополнительное умножение запрещено.

Для `product_cost` используется только:

1. `finalPrice`, когда `currencyCode=643` (RUB);
2. иначе `convertedFinalPrice`, когда `convertedCurrencyCode=643`.

Если обе RUB-суммы присутствуют и различаются, RUB-суммы нет, значение отсутствует или
выходит за диапазон CRPT `0..99999999999999999`, строка получает фактическую ошибку
данных и не попадает в документ. `FbsOrder.price`, цена карточки, тариф WMS и ручная
догадка не используются. Сумма отправляется целым числом копеек с НДС.

Существующий paginated sync с `dateFrom` используется для доступного WB периода не
более 30 дней. Более старые заказы или строки, которые WB уже не возвращает с финальной
RUB-ценой, остаются неотправляемыми; молчаливого backfill из `price` нет.

### BR3. Повторная проверка КИЗ, группы и владельца

После локальной auth и перед формированием документа backend повторно вызывает True API
`cises/info` для каждого выбранного КИЗ. В provider-запрос передаётся не обязательно
полное сохранённое scanner-значение, а его детерминированная проекция: существующий
GS1-разбор извлекает идентификационный код `01<GTIN>21<serial>` длиной 18–74 символа и
отбрасывает GS/AI91/AI92. Исходный `FbsOrderMarking.value`, связь строки отчёта и audit
не изменяются. Тот же извлечённый `cis` используется в `cises/info`, `LK_RECEIPT` и
reconciliation; неоднозначный или невалидный GS1 блокирует только строку до обращения
к CRPT. Только ответ текущей auth-сессии определяет
`productGroup`/`productGroupId`, `ownerInn`, status/statusEx и GTIN. Категория WMS и
ручное сопоставление не подменяют этот ответ.

КИЗ допускается дальше, только если группа однозначна, поддерживает `LK_RECEIPT` с
`DISTANCE`, `ownerInn` совпадает с проверенным ИНН seller `BillingProfile`, а фактический
статус разрешает вывод. Для `LK_RECEIPT` статус должен быть `INTRODUCED`. Если дата
начала прослеживаемости наступила, `statusEx` должен отсутствовать либо быть
`CONNECT_TAP`/`WAIT_TRANSFER_TO_OWNER`; если не наступила — отсутствовать либо иметь
любое значение, кроме `MOVING_BY_UD`. WMS не угадывает начало прослеживаемости по
категории: если применимый режим для `pg` нельзя подтвердить, КИЗ fail-closed и в
документ не попадает. Официальное значение `statusEx="EMPTY"` означает «значение
отсутствует» и до обеих проверок нормализуется в отсутствие вместе с `null`/пустой
строкой; оно не становится отдельным статусом или причиной отказа.

Проверка доступности `DISTANCE` по официальному справочнику обязательна до проверки
МОД и сборки документа: причина недоступна для автозапчастей,
алкоголя, альтернативного табака, кабельной продукции, мясных изделий,
никотиносодержащей продукции, пива и слабоалкогольных напитков, пиротехники,
полимерных труб, полуфабрикатов и замороженных продуктов, радиоэлектронных систем
доставки никотина, сладостей, табака, титановой металлопродукции, товаров для дома и
интерьера и удобрений в потребительской упаковке. Null, неполный, чужой,
заблокированный, уже выведенный или противоречивый ответ превращает только этот КИЗ в
ошибку и не отправляется. Группа из ответа является `pg`; смешанные группы разделяются
на разные документы.

### BR4. ИНН, КПП и место деятельности

В inner `LK_RECEIPT.inn` используется ИНН seller `BillingProfile`, но только после
успешной auth тем же участником и совпадения с `ownerInn` всех КИЗ документа. Для auth
этот же ИНН всегда передаётся как `inn` в `/auth/simpleSignIn`: для физлица с МЧД он
обязателен, в остальных случаях официально допустим. Срок/отзыв МЧД отражается в
решении CRPT и `expireDate`, а не ручном browser-поле.

Для `DISTANCE` место осуществления деятельности обязательно только для следующих
официальных `pg`: `antiseptic`, `bicycle`, `perfumery`, `toys`, `conserve`, `petfood`,
`lp`, `milk`, `seafood`, `meat`, `shoes`, `radio`, `gadgets`, `vegetableoil`,
`softdrinks`, `construction`, `water`, `electronics`, `tires`. При этом `meat` не
поддерживает `DISTANCE` по BR3, поэтому документ с такой комбинацией не создаётся.
Отдельные правила матрицы для `alcohol`/`beer`, `otp`/`ncp`/`tobacco` и `vetpharma`
не расширяют доступность `DISTANCE` и не переносятся на другие группы.

Backend получает зарегистрированные МОД только через
`GET {v3}/mods/list?inns=<seller_inn>&productGroups=<pg>&limit=1000&page=<n>`; `page`
начинается с `0`, backend читает все страницы до `nextPage=false`. Ответ содержит строго
`result` — массив неудалённых записей с `inn`, `productGroups`, необязательными
`address`, `kpp`, `fiasId` и `isBlockedEgais`, — а также `total` и `nextPage`;
ключи `results` или `mods` для этого метода не принимаются. `isBlockedEgais`
относится только к ЕГАИС и не объявляется общим признаком активности.
Ручной ФИАС/КПП и категория WMS не подменяют ответ True API.

Если `pg` не входит в обязательную матрицу, `kpp` и `fias_id` в документе отсутствуют.
Если входит, backend оставляет из `result` только записи того же `seller_inn`, содержащие
точный `pg`, и требует ровно одну валидную внешнюю запись. Из неё без ручной подмены
берутся применимые идентификаторы: `kpp` — только для юридического лица в формате 9
цифр, `fias_id` — UUID; если присутствуют оба, True API ищет МОД по их сочетанию. Для
ИП `kpp` не отправляется. Ноль или несколько валидных записей — внешний data blocker:
блокируется только документ этой группы с фактической ошибкой, другие документы
операции продолжаются.

WMS не создаёт `WithdrawalModMapping`, привязку склада к МОД, экран настройки или выбор
МОД в withdrawal-модалке. `FbsSupply.warehouse_id` не участвует в поиске МОД. Первый
элемент результата не выбирается, а 0/multiple не разрешаются догадкой. **B1 закрыт
этим правилом**; технический вызов `/mods/list` и fail-closed builder приняты только
в local/emulator-границе, без live CRPT.

### BR5. Readiness браузера без чтения сертификатов

При открытии экрана загружается закреплённая локальная версия `cadesplugin_api.js`,
ожидается `window.cadesplugin`, создаётся `CAdESCOM.About`, читаются `PluginVersion` и
`CSPVersion`. На этом этапе `CAdESCOM.Store` и private key не открываются, PIN prompt не
вызывается. Отсутствие расширения/native host, timeout, отсутствие CSP (`0x80090019`) и
неподдержанная комбинация browser/OS/version показывают разные человеческие причины с
безопасным `getLastError`; автоматического цикла запросов разрешения/PIN нет.

Production работает только по HTTPS. Localhost, staging и production являются разными
origins и принимаются отдельно с учётом trusted-site и enterprise browser policy.

Источник browser runtime закрепляется в Git, а не загружается с CDN во время работы:
официальный архив `@crpt/cades-pluginer@0.0.1` по адресу
`https://registry.npmjs.org/@crpt/cades-pluginer/-/cades-pluginer-0.0.1.tgz`, SHA-256
`07520a6ae601ef509434ca4fa8188d6ddc0b2b8ee035e8bd3cc57623eef3ce5b`.
Из него воспроизводимо извлекается `cadesplugin_api.js` с SHA-256
`3f9e438a166336220eed1d292e8b5f07e9a7f617260e98b2aa695fe6a97d4bbb`; vendor-каталог
содержит сам файл, origin/version/hash и bundled license/notice. Метаданные архива
противоречат друг другу (`package.json` сообщает MIT, вложенный `LICENSE` содержит
Apache-2.0), поэтому до релиза нужен явный license-review; агент не делает юридический
вывод и не удаляет лицензионный файл.

Сборка обязана включить `/vendor/cryptopro/cadesplugin_api.js` теми же байтами. Smoke
проверяет HTTP 200, same-origin HTTPS в production, MIME `application/javascript` либо
`text/javascript`, отсутствие HTML fallback, совпадение hash и разрешение `script-src
'self'`. Событие `script.onload` само по себе не означает readiness: loader ждёт Promise
`window.cadesplugin` с bounded timeout и только затем вызывает `About`.

### BR6. Сертификаты только после действия пользователя

Только после клика «Вывести из оборота» браузер открывает
`CurrentUser/My/MaximumAllowed`, применяет `TIME_VALID`, оставляет сертификаты с
`HasPrivateKey=true` и показывает Subject, Issuer, срок и сокращённый thumbprint.
Полный thumbprint сохраняется только как идентификатор операции. Перед подписью
проверяются `IsValid` и `CPSigner.CheckCertificate=true`; guessed-фильтр по EKU/ИНН не
добавляется без требования True API.

PIN запрашивает только системный UI CSP/носителя. WMS не использует `CPSigner.KeyPin`,
не читает и не передаёт PIN, private key или container. Обычная USB-флешка не считается
криптоносителем. Отмена системного окна или неверный PIN не запускают автоповтор.
Browser `certificateBinding` не просит и не передаёт ручной признак/срок МЧД:
обязательность `inn` выполняет backend по BR7, а срок token берётся из ответа CRPT и
ограничивается сроком выбранного сертификата.

### BR7. Единая auth-сессия True API

Backend использует sandbox либо production v3 base URL из конфигурации:

1. `GET {v3}/auth/key` и сохраняет полученные `uuid` и `data` в операции;
2. браузер подписывает именно возвращённый challenge `data` присоединённой УКЭП через
   профиль официального `@crpt/cades-pluginer`: строка передаётся в
   `CadesSignedData.Content` без base64-decode, trim, CRLF-normalization и без явного
   `ContentEncoding`, то есть с default `CADESCOM_STRING_TO_UCS2LE`; затем вызывается
   `SignCades(CPSigner, CADESCOM_CADES_BES, false)`;
3. `POST {v3}/auth/simpleSignIn` получает `uuid`, base64 присоединённой подписи в
   `data`, `unitedToken=true` и проверенный seller `BillingProfile.inn`; `inn`
   валидируется как ровно 10 или 12 цифр. True API требует его физлицу с МЧД и допускает
   в остальных случаях, поэтому WMS отправляет уже известный ИНН всегда и не добавляет
   ручное поле/переключатель МЧД. `details` в MVP не отправляется, потому что это
   необязательные реквизиты аттестата соответствия объекта информатизации;
4. backend читает из успешного ответа `uuidToken` и `expireDate`, не отдаёт token
   JavaScript и хранит его зашифрованно с tenant/seller, thumbprint и сроком не более
   `expireDate` CRPT, 10 часов и окончания сертификата; срок/отзыв МЧД уже отражается
   в выдаче/`expireDate` CRPT и не вводится браузером вручную. `code`, `error_message` и
   `description` не превращаются в успешную сессию. Для передачи подписи удаляются
   только символы `CR`/`LF`; итог обязан быть корректным base64, остальные символы не
   исправляются молча.

Новый sign-in создаёт новую сессию и не продлевает старую. UUID без `/auth/key` и JWT
не являются допустимым боевым shortcut. Challenge, auth-подпись и document-подпись —
разные объекты; смена сертификата инвалидирует неподанные подписи и требует новой auth.
Доступ определяется зарегистрированными товарными группами и ролью участника; 403 для
роли без права create не обходится другим endpoint и сохраняется как auth/access error.

**B3 закрыт официальным Appendix 2 и кодом рекомендованного CRPT helper.** Текущий
`attached_auth_profile_unverified` и одноимённый runtime flag удаляются как устаревший
безусловный запрет; вместо них production create защищает отдельный
`WITHDRAWAL_PRODUCTION_SUBMIT_ENABLED=false` по умолчанию. Реализация auth обязана иметь
две независимые проверки: fake adapter фиксирует отсутствие `ContentEncoding`, exact
исходную строку, BES и `detached=false`; software-cert runner создаёт attached CMS
одноразовым сертификатом и независимо проверяет подпись/вложенный UCS-2LE content.
Это не заменяет физическую CryptoPro/ГОСТ-матрицу. Sandbox
`simpleSignIn` по B2 остаётся acceptance evidence, но не определяет уже опубликованный
профиль.

Истёкший UUID-token или 401 не должны оставлять существующую `submitted`/
`reconciling` operation в тупике. По тому же явному действию пользователя WMS повторно
открывает компактный выбор сертификата, получает новый challenge и auth-session для
той же tenant/seller operation. После неопределённого create новый token используется
**только** для v4 `doc/list`/`doc/{id}/info`: persisted payload/signature/intent не
перезаписываются и новый POST create запрещён. Поздняя отмена FBS-заказа не закрывает
read-only reconciliation уже отправленного документа; `eligible_rows` применяется при
создании/retry, но не как повторный запрет чтения persisted document. Изъятие токена,
отмена PIN или системного диалога оставляют operation в `reauth_required` без внешнего
действия. Явный retry после доказанного terminal failure остаётся новой attempt с новым
preflight и новыми bytes/signatures.

### BR8. Exact bytes и локальная подпись документов

После BR2–BR4 backend один раз сериализует inner `LK_RECEIPT` в UTF-8 bytes, сохраняет
immutable bytes и SHA-256 и отдаёт browser их base64 вместе с внутренним `document_id`.
Browser не парсит и не сериализует JSON. Для binary payload сначала устанавливается
`ContentEncoding=CADESCOM_BASE64_TO_BINARY`, затем `Content=payload_base64`; подпись
создаётся через CAdES/GOST по официальному примеру как отсоединённая УКЭП и возвращается
base64 без переносов строк. Тот же сертификат подписывает auth и все документы операции.

Backend принимает подпись только с тем же operation/document id, thumbprint и SHA-256,
и отправляет первоначальные bytes — повторная сериализация после подписи запрещена.
Каждый документ имеет отдельную подпись. Точные payload/signature могут храниться в
ограниченных данных операции, но не печатаются в application logs.

### BR9. Состав `LK_RECEIPT` для FBS

Inner JSON содержит `inn`, `action="DISTANCE"`, `action_date`, условные `fias_id`/`kpp`
по BR4 и `products` минимум с одним `{cis, product_cost}`. `action_date` — именно дата
вывода из оборота, а не дата передачи WB и не дата первичного документа. В MVP backend
ставит календарную дату формирования операции в `Europe/Moscow`; допустимое окно True
API — от текущей даты минус пять лет до текущей даты включительно, время допускается,
но игнорируется. `FbsSupply.delivered_at` остаётся основанием включения и периодом
реестра, но не копируется в `action_date`.

Для `DISTANCE` `document_type` и товарный `primary_document_type` необязательны. Если
любое из них заполнено, обязательны соответствующие номер и дата; допустимые типы —
`RECEIPT`, `SALES_RECEIPT`, `OTHER`, `CONSIGNMENT_NOTE`, `UTD`, а для `OTHER` обязательно
наименование. `document_date` — дата первичного документа в `dd.MM.yyyy` или
`yyyy-MM-dd`, не `action_date`; товарный `primary_document_date` имеет отдельный формат
и диапазон из официальной схемы. В MVP весь блок первичного документа не заполняется:
WB order ID не выдаётся за номер первичного документа.

Builder для MVP использует allowlist полей `inn`, `action`, `action_date`, условные
`kpp`/`fias_id` и `products[{cis, product_cost}]`. Условные либо запрещённые для
`DISTANCE` поля (`buyer_inn`, `withdrawal_type_other`, `state_contract_id`,
`partial_quantity` и другие поля иных причин) отклоняются до подписи, а не уходят как
`null` или пустая строка. `product_cost` обязателен для каждого товара при `DISTANCE`,
передаётся в копейках с НДС и в диапазоне `0..99999999999999999`.

Один документ содержит КИЗ одного seller и одного `pg`. Если группа поддерживает
`LK_RECEIPT`, но не `DISTANCE`, КИЗ получает ошибку до submit. Внутри группы документы
делятся так, чтобы каждый был не больше 30 MB и 30 000 КИЗ; пересечение КИЗ между
частями запрещено.

Outer create JSON содержит ровно `document_format="MANUAL"`,
`product_document=base64(exact inner bytes)`, `type="LK_RECEIPT"` и
`signature=base64(detached signature)`. Отправка выполняется через
`POST {v3}/lk/documents/create?pg=<group>`. HTTP 200/201 и полученный GIS document ID
означают только приём документа, не окончательный вывод КИЗ.

## Обязательные боевые требования: API, состояние и восстановление

### BR10. Same-origin WMS API и backend boundary

Browser обращается только к same-origin WMS API; CRPT CORS не предполагается, UUID-token
в page JavaScript не попадает. Минимальный контракт API:

- `GET /operations/marking-codes/self/withdrawals` — реестр BR1;
- `POST /operations/marking-codes/self/withdrawals/operations` — идемпотентно создать
  операцию по `row_ids` и `client_request_id`, повторно проверить локальные данные и
  вернуть operation id плюс auth challenge;
- `POST .../operations/{id}/auth-signature` — принять связанную присоединённую подпись,
  получить UUID-token, выполнить BR3–BR4 и вернуть immutable document blobs/hashes;
- `POST .../operations/{id}/reauth-challenge` и последующая auth-signature — возобновить
  auth существующей UNKNOWN/RECONCILING operation без повторного `eligible_rows`,
  пересборки документов или create; после reload этот путь возвращает `GET operation`;
- `POST .../operations/{id}/document-signatures` — принять весь набор связанных
  подписей и запустить submit;
- `GET .../operations/{id}` — перечитать/возобновить состояние после reload;
- `POST .../operations/{id}/retry` — разрешён только после доказанного terminal reject
  и повторной BR2–BR4; для UNKNOWN/RECONCILING возвращает существующую операцию.

Пути могут быть вынесены в отдельный router, но семантика `self`, tenant/seller scope,
operation id и разделение стадий не меняются без обновления постановки.

### BR11. Сохраняемая модель и внутренняя идемпотентность

До внешнего вызова сохраняются:

- операция: tenant, seller, user, `client_request_id`, selection hash, состояние,
  сертификат, auth-session, timestamps;
- элементы: marking/order/supply, CIS, дата передачи, price snapshot, pg, ownerInn,
  состояние и фактическая ошибка;
- документы: pg, exact payload, SHA-256, signature, attempt, request timestamps,
  GIS document id/status, errors и `next_poll_at`;
- auth-session: CRPT uuid, зашифрованный token, expiry, certificate/MChD metadata.

Уникальность `tenant+seller+client_request_id` возвращает ту же операцию. Database
constraint/locking не допускает два активных или успешных вывода одного marking code;
два пользователя, двойной клик и повтор HTTP не создают второй документ. Terminal
reject может породить новую attempt того же элемента только через BR10 retry; история
не перезаписывается.

Внутренние состояния операции: `created`, `auth_pending`, `documents_pending_signature`,
`submitting`, `submitted`, `reconciling`, `succeeded`, `partial_failed`, `failed`,
`cancelled`. Состояния не показываются отдельными чипами в штатном seller UI.

### BR12. Ограничение запросов и частичный submit

Общий для всех workers/replicas limiter не допускает больше 50 CRPT requests/s на
участника. Process-local sleep недостаточен. Документы отправляются и фиксируются
независимо: успех одного не откатывается из-за ошибки другого. После каждого create
GIS document ID сохраняется до следующего внешнего вызова.

HTTP 400/401/403/422 сохраняет точные provider `code`, `error_message`, `description`
и безопасный body без Authorization; `_auth_error` не имеет права заменить фактический
`error_message` одним локальным reason/status. 401 auth переводит в `reauth_required`,
а definite create reject — только затронутый документ в отказ. HTTP 429 obeys
`Retry-After`/bounded backoff через общий limiter без blind create; HTTP 5xx, сетевой
timeout или разрыв после начала POST не считаются отказом: результат становится
`reconciling` по BR14.

### BR13. Polling и окончательный результат

Для известного GIS document ID backend использует только v4
`GET {v4}/doc/{id}/info?pg=<pg>&body=true`. Старые v3 list/info и
`/doc/validator/status` для `LK_RECEIPT` запрещены. Polling выполняется сохраняемым
worker с backoff 2, 5, 10, 30, далее не чаще раза в 60 секунд и общим limiter BR12;
elapsed time сам по себе не превращает неизвестный исход в отказ.

`IN_PROGRESS` и `WAIT_FOR_CONTINUATION` продолжают polling. `CHECKED_OK` — единственный
обычный terminal success и только тогда связанные строки получают «Выведен».
`CHECKED_NOT_OK`, `PARSE_ERROR` и `PROCESSING_ERROR` — terminal failure с сохранением
`errors` и `commonErrors`. `UNDEFINED` и неожиданный статус не считаются успехом и
остаются на сверке/операторском контроле. Shipment-статус `ACCEPTED` не переносится на
`LK_RECEIPT` как доказательство вывода.

Если ошибка возвращена на документ, а не на отдельный CIS, одна и та же фактическая
причина помечается как «ошибка документа» у каждого входящего КИЗ; WMS не сочиняет
по-кодовую причину.

### BR14. Потеря create-ответа и безопасный retry

В create True API нет внешнего idempotency key, client request ID или уникального
клиентского номера. Поэтому после timeout/5xx/разрыва новый create автоматически не
выполняется.

Worker запрашивает v4 `GET {v4}/doc/list` с обязательным `pg` и узким окном
`dateFrom/dateTo`, `senderInn`, `documentType=LK_RECEIPT`, `documentFormat=MANUAL`, затем
читает кандидатов через `/doc/{id}/info?pg=<pg>&body=true`. Совпадение принимается только
по exact inner body либо одинаковым action/date/inn/MOD, множеству CIS и cost каждого
элемента. Один exact match присоединяется к операции и pollится; несколько совпадений
создают incident о возможном дубле. Отсутствие кандидата не доказывает отсутствие
документа: операция остаётся `reconciling` и повторяет только чтение.

`cises/info` может подтвердить текущий `RETIRED`, но не связывает его с нашим документом
и не закрывает audit. Новый submit после UNKNOWN разрешён только после внешнего
подтверждения CRPT/оператора, что документ не создан; публичный API не задаёт безопасный
автоматический timeout для такого решения.

После явного terminal failure новый retry повторно проверяет цену, `cises/info`, pg,
owner и MOD и формирует новые bytes/подпись. Повтор прежней подписи или payload после
изменения данных запрещён.

Reauth для уже отправленного/неопределённого документа не является retry: он не
переоценивает текущую eligibility заказа, не меняет items/documents и не создаёт POST.
Даже если заказ отменился после сохранённого submit intent, worker продолжает read-only
сверку persisted документа. Это исключение относится только к доказанно начатому
внешнему submit и не позволяет создать новый документ по отменённому заказу.

### BR15. UI результата и ошибки

По последнему решению владельца UI остаётся R1–R6/R9: один реестр и одно окно
сертификата. После «Подписать и отправить» все auth/document signatures выполняются
за одной командой без stepper. Системные CSP/PIN окна не копируются интерфейсом WMS.

После принятия подписей окно закрывается. Пока backend обрабатывает операцию, строка
остаётся с простым статусом «Не выведен»; повторное действие возвращает существующую
операцию и не создаёт дубль. При `CHECKED_OK` строка становится «Выведен» и допускается
краткий toast. Только при фактической local/CRPT terminal error показывается отдельная
простая модалка: конкретный КИЗ, WB order и дословная безопасная причина. `reconciling`,
pg, документ, auth, polling и служебные состояния в эту модалку не попадают.

Ошибки плагина, CSP, Store, сертификата, token/PIN и auth показываются до submit как
короткая причина и действие восстановления. Отмена пользователем не создаёт ошибку КИЗ.

### BR16. Tenant isolation, секреты и аудит

Каждый API/query/update включает tenant и seller из проверенной WMS-сессии; переданные
ID другого seller дают 404/403 без раскрытия. Сертификат одной операции нельзя применить
к другой seller/tenant. ИНН BillingProfile, ownerInn, auth participant и текущий seller
сверяются до документа.

Аудит хранит user, tenant/seller, operation/attempt, order/supply/marking ids, полный
thumbprint и безопасные сведения сертификата, payload SHA-256, CRPT document id/status,
timestamps, HTTP/status/error codes и связи per-KIZ. Exact payload/signature хранятся
как ограниченные данные операции и не пишутся в обычные логи. Authorization token,
challenge body, PIN, private key и container не логируются; token хранится только
зашифрованно и удаляется/инвалидируется по expiry.

### BR17. Контуры и release gate

Конфигурация выбирает связанные пары base URL:

- sandbox: `https://markirovka.sandbox.crptech.ru/api/v3/true-api` и `/api/v4/true-api`;
- production: `https://markirovka.crpt.ru/api/v3/true-api` и `/api/v4/true-api`.

Token, document IDs и операции разных контуров не смешиваются. Production WMS работает
по HTTPS. **BLOCKER B2:** до live-приёмки назначаются sandbox participant, УКЭП/МЧД,
зарегистрированные product groups и тестовые КИЗ/МОД; значения секретов в документ и
логи не попадают.

Поддерживаемый baseline, который нужно реально прогнать: Chrome/Chromium 104+,
Edge Chromium 104+ и Firefox на Windows 10/11 и macOS 11–14 x64/ARM64. Верхняя версия
браузера и macOS 15+ не обещаются без отдельного smoke. Найденные на одном Mac CSP
5.0.13003, CAdES 2.0.15003 и manifests доказывают только установленные артефакты, но не
enabled extension, native host, сертификат или подпись.

Выпуск требует: mock/unit, PostgreSQL concurrency, crypto integration runner с тестовым
сертификатом, ручную browser/OS/token матрицу, sandbox end-to-end и затем одну отдельно
разрешённую production-операцию с проверкой GIS document ID/status/body и повторным
открытием реестра. Commit/CI или локальный URL не заменяют ни один из этих слоёв.

Production submit в этой волне всегда выключен отдельным fail-closed runtime flag.
Release bundle обязан согласованно содержать миграции, API, Celery worker и beat,
общий Redis для limiter/очереди, vendor `cadesplugin_api.js` и одинаковые
`WITHDRAWAL_*`/base URL/encryption/runtime flags в API и worker. В локальном diff
compose/config/entrypoint и отдельные Railway worker/beat descriptors добавлены и
проверены статически и тестами конфигурации. Фактическая Railway topology, применённые
миграции, общий Redis и запущенные worker/beat не проверялись; один работающий
API-контейнер по-прежнему не считается готовой интеграцией.

### BR18. Возвраты и исторические данные

Отмены до передачи и возвраты не появляются в этом реестре. Физически полученный
возврат ранее выведенного товара требует отдельного обратного ввода в оборот и не
переводит строку WMS-517 обратно автоматически.

Исторические переданные строки без доказанной `delivered_at`, финальной RUB-цены,
однозначного owner/pg или обязательного MOD видны только как фактическая ошибка данных
и не отправляются. Исправление исходных данных и новый явный retry сохраняют историю;
подстановка фиктивного значения запрещена.

### BR19. Версионированный официальный режим прослеживаемости

Ручные owner-настройки `withdrawal_traceability_metadata_version` и
`withdrawal_traceability_modes` удаляются из бизнес-решения. Код содержит проверяемый
versioned snapshot официального раздела
[`Прослеживаемость товаров`](https://docs.crpt.ru/gismt/Прослеживаемость_товаров/): для
каждой поддержанной `pg` — точный mode и опубликованные источником даты, если они есть.
Дата фиксации snapshot не выдаётся за дату начала режима. Обновление — обычное code
change со ссылкой на версию официального источника, review и fixtures; runtime HTML
scraper, network refresh, отдельное persisted metadata-хранилище и ручной owner config
не нужны. Неизвестная группа fail-closed блокирует только её документ.

### BR20. Изолированная PostgreSQL-проверка

Конкурентные/trigger/migration проверки запускаются на изолированной одноразовой
PostgreSQL test DB, никогда на рабочей БД. Способ запуска не является продуктовым
контрактом: локально допустим отдельный Homebrew-кластер в temp-каталоге и на свободном
loopback port, в CI — обычный PostgreSQL service/container. Harness проверяет test-only
URL/имя БД, накатывает миграции с нуля и гарантированно очищает окружение. Проверяются
два пользователя/транзакции на один marking, uniqueness/claims, append-only observations,
rollback и соответствие свежей схемы миграциям.

### BR21. Официальная source-to-test матрица

До передачи в sandbox каждый внешний контракт покрывается одной трассируемой строкой
`официальный section/URL → HTTP request JSON/query → response JSON → используемые поля →
fixture/test`. Минимум:

| Контракт | Request/response, которые замораживает тест | Обязательное evidence |
|---|---|---|
| [Auth key / sign-in](https://docs.crpt.ru/gismt/True_API/#_единая_аутентификация) + [Appendix 2](https://docs.crpt.ru/gismt/True_API/#_приложение_2_примеры_аутентификации_по_укэп_подписание_документов) | `GET v3 /auth/key → {uuid,data}`; `POST v3 /auth/simpleSignIn {uuid,data,unitedToken:true,inn?} → {uuidToken,expireDate}` и error `code/error_message/description` | exact raw challenge, attached profile BR7, 10/12-digit MЧД INN, expiry/401 reauth |
| [`cises/info`](https://docs.crpt.ru/gismt/True_API/#_метод_получения_информации_о_кодах_маркировки) + [traceability](https://docs.crpt.ru/gismt/Прослеживаемость_товаров/) | POST JSON — массив 1–1000 provider CIS; ответ — массив строк с `cisInfo.{requestedCis,cis,productGroup,productGroupId,ownerInn,status,statusEx,gtin}` и `errorCode/errorMessage` | GS1 provider projection, `INTRODUCED`, `EMPTY` normalization, обе traceability branches, unknown fail-closed |
| [`mods/list`](https://docs.crpt.ru/gismt/True_API/#_метод_получения_списка_мод_по_участнику_оборота_товаров) | GET query `inns,productGroups,limit=1000,page=0`; response `{result:[{inn,productGroups,fiasId?,kpp?}],total,nextPage}` | все страницы, exact INN+pg, 0/1/>1, no `results`/`mods`, MOD только после DISTANCE |
| [Create/LK_RECEIPT](https://docs.crpt.ru/gismt/True_API/#_единый_метод_создания_документов) | `POST v3 /lk/documents/create?pg`; JSON `{document_format:"MANUAL",product_document:<base64>,type:"LK_RECEIPT",signature:<base64>}`; 200/201 response — GIS UUID scalar | immutable bytes/signature, 30 MB/30 000, no duplicate after uncertain |
| [v4 list](https://docs.crpt.ru/gismt/True_API/#_метод_получения_списка_загруженных_документов_в_гис_мт) / [info](https://docs.crpt.ru/gismt/True_API/#_метод_получения_содержимого_документа_по_идентификатору) | list query `pg,dateFrom,dateTo,senderInn,documentType=LK_RECEIPT,documentFormat=MANUAL,limit,order` → `{results,nextPage}`; info query `pg,body=true` → ровно один элемент массива с `number,productGroup:[pg],status,body,errors,commonErrors` | exact candidate comparison, terminal/pending/unknown, no v3/validator |
| Ошибки | 400/401/403/422/429/5xx, timeout, reload, partial documents | фактический provider message, shared limiter/backoff, 401 reauth, uncertain GET-only reconciliation, tenant isolation |

Матрица хранит официальную версию/дату и sample JSON без токенов/подписей. Fixture
parser tests запрещают permissive fallback на похожий ключ или поле. Для live evidence
записываются operation/document IDs и redacted timestamps, но не секреты.

### BR22. CI и граница релиза

До merge нужен PR, на котором один полный существующий CI проходит backend
ruff/mypy/full pytest и frontend TypeScript/build, а также действующие document/backlog
checks. В существующие suites добавляются предметные проверки vendor hash/artifact,
official fixtures, traceability snapshot, PostgreSQL BR20 и CryptoPro adapter; новых
network-доступов к документации или отдельной инфраструктурной системы CI не требуется.
Физический токен и sandbox B2 остаются acceptance evidence, а не зелёной unit-подменой.

Эта волна должна оставить код готовым к вводу реквизитов и сертификата без новой
разработки, но не выполняет production withdrawal. Sandbox включается только после B2;
production flag остаётся false до отдельного разрешения, повторной приёмки, review,
деплоя из проверенного SHA и разрешённого smoke.

## Матрица ошибок и восстановления

| Стадия | Факт | Состояние и восстановление |
|---|---|---|
| Plug-in/native host/CSP | Скрипт не загрузился, timeout, extension disabled/policy blocked, `0x80090019` | Внешний вызов не начинать; показать точную категорию и setup/help. Повтор только по действию пользователя. |
| Store/certificate | Нет time-valid cert, private key, chain/revocation invalid, token вынут | Не делать auth; сохранить выбранные КИЗ, после исправления заново открыть Store и проверить cert. |
| PIN/user cancel | CSP сообщил cancel/wrong/locked PIN | Не собирать PIN и не повторять prompt автоматически; операция остаётся до внешнего submit. |
| Local data | Нет delivered_at, RUB final price, owner/pg либо обязательного MOD | Не подписывать документ; показать конкретные КИЗ и фактическое отсутствующее поле. |
| Auth | 401/403, просрочен challenge/cert/MЧД либо сменился cert | Сохранить `code/error_message/description`; инвалидировать только auth token. Новый `/auth/key` по явному действию; для UNKNOWN/RECONCILING после reauth только read-only list/info, без create. |
| Create definite reject | HTTP 400/401/403/422 с ответом CRPT | Сохранить code/body, пометить документ failed; retry только после исправления и новых preflight/bytes/signature. |
| Create uncertain | 5xx, timeout или потеря ответа после начала POST | `reconciling`; только v4 list/info по BR14, без нового create. |
| Async reject | `CHECKED_NOT_OK`, `PARSE_ERROR`, `PROCESSING_ERROR` | Сохранить `errors/commonErrors`, строки получают «Ошибка»; успешные соседние документы не откатывать. |
| Async wait/unknown | `IN_PROGRESS`, `WAIT_FOR_CONTINUATION`, `UNDEFINED` либо новый статус | Продолжить polling/контроль; не ставить «Выведен» и не создавать новый документ. |
| Reload/concurrency | Reload, повтор запроса, двойной клик, второй пользователь | Возобновить одну server operation по idempotency/row lock; после expired token дать reauth существующей operation. Отмена заказа после submit не запрещает read-only reconciliation. |

## Решения боевой фазы

1. Browser только подписывает, а CRPT вызывает same-origin backend. Так UUID-token не
   попадает в page JS, CORS не становится скрытой зависимостью, а operation survives reload.
2. Backend создаёт и замораживает exact bytes; browser не строит JSON. Иначе подпись
   могла бы относиться не к тем байтам, которые ушли в `product_document`.
3. `product_cost` берётся из подтверждённой финальной RUB-суммы WB, а не из legacy
   `price`: это единственный источник, совпадающий с фактической оплатой со скидками.
4. `pg` и owner определяются повторным `cises/info`, а не категорией карточки WMS.
   Товарная группа влияет на юридический документ, но не добавляется в видимый реестр.
5. Технические состояния сохраняются на сервере, но в seller UI остаются только три
   статуса владельца. Повтор по строке возобновляет operation и не создаёт дубль.
6. После неопределённого create-ответа WMS выбирает сохранность перед скоростью: только
   сверка v4, потому что публичный create не имеет внешней идемпотентности.
7. МОД — только внешняя техническая проверка: для обязательной группы `/mods/list`
   должен вернуть ровно одну валидную запись того же ИНН и `pg`. Ноль или несколько
   записей блокируют только этот документ; WMS не создаёт настройку, mapping или новый
   UI и не связывает МОД со складом. Этим решением B1 закрыт на уровне постановки.
8. Auth challenge и document payload имеют разные browser-профили: raw JS string /
   default UCS-2LE / attached CAdES-BES для auth; base64 exact UTF-8 bytes /
   `BASE64_TO_BINARY` / detached CAdES-BES для документа. Смешивать их нельзя.
9. Прослеживаемость приходит из versioned официального snapshot, а не ручного owner
   config; неизвестная группа блокирует только свой документ, а не остальные группы.
10. Полный scanner GS1 остаётся в БД, а provider получает детерминированный AI01+AI21.
    Это сохраняет первичное доказательство и не отправляет криптохвост как `cis`.

## Реализационные волны

1. **Browser crypto + vendor asset — frontend ownership.** Закрепить BR5 artifact/license
   metadata, реализовать attached auth profile BR7 вместо unconditional throw, fake и
   independent software-cert verification. Detached document profile не менять.
2. **Provider identity + traceability — backend ownership.** Реализовать GS1→AI01+AI21
   provider projection, `EMPTY` normalization и versioned code snapshot BR19;
   удалить ручные traceability settings, сохранив per-pg fail-closed.
3. **Reauth/reconciliation — backend API/worker + frontend ownership по разным файлам.**
   Добавить reauth существующей operation, фактический provider `error_message`, GET-only
   recovery после uncertain submit и reload/PIN/token tests. `eligible_rows` не применять
   повторно к persisted submitted document.
4. **PostgreSQL/deploy — infrastructure ownership.** Выполнить BR20, прокинуть одинаковую
   конфигурацию в API/worker/beat, доказать Redis/shared limiter, migrations, vendor asset
   и Railway topology. Production-submit flag оставить false.
5. **Contract/CI evidence — test ownership.** Реализовать BR21 fixtures для каждого
   endpoint/error и предметные проверки BR22 в существующем CI; один полный PR CI
   обязателен после review/приёмки.
6. **Sandbox acceptance — после B2.** Ввести реквизиты без изменения кода, пройти
   auth→cises→mods→sign→create→v4 status, 401 reauth, uncertain reconcile и physical
   browser/token matrix. Реальные production withdrawal не выполнять.

Волны 1–5 реализованы в текущем локальном diff и приняты в доказанной ниже
code/emulator/OpenSSL/config/PostgreSQL-границе. Волна 6 ждёт B2 и физическую
КриптоПро/browser/token-матрицу; production release остаётся отдельной последующей
границей с явным разрешением.

## Проверки боевой реализации

Продуктовая приёмка 24.09.2026 охватывает текущий локальный diff после перекрёстного
review и повторной проверки замечаний: единый server-side состав строк, API и ledger,
emulator orchestration, document builder, внешнюю MOD-проверку, scheduler, browser auth
adapter/vendor asset, provider CIS/traceability, reauth и подключённый seller UI.
Reviewer подтвердил frontend Vitest `53 passed`, TypeScript, адресный ESLint, build и
`git diff --check`. Исполнитель трижды получил на SQLite `195 passed, 4 skipped`; четыре
skip — только PostgreSQL-сценарии. Отдельно исполнитель на одноразовом PostgreSQL 16
получил `5 passed` и полный Alembic head/downgrade/upgrade; reviewer этот PG-прогон
локально не воспроизвёл и видел те же четыре skip. Независимый auth-runner прошёл на
OpenSSL 3.6.3 с одноразовым RSA-сертификатом. Это локальные доказательства, не полный
GitHub CI и не доказательство КриптоПро/ГОСТ/USB-токена.

«Подтверждено в локальной code/emulator/browser/OpenSSL/PostgreSQL-границе» ниже не
означает production readiness: физический CryptoPro/browser/token/PIN, ГОСТ-провайдер,
sandbox/live True API, фактическая Railway topology/deploy, полный GitHub CI и
production отдельно не приняты.

| Проверка | Требования | Подготовка, действие и ожидаемый результат | Вердикт |
|---|---|---|---|
| BC1 | BR1, BR16, BR18 | Подготовить переданный WB FBS, неподтверждённый, отменённый, FBO/FBW, возврат и чужого seller. GET реестра возвращает только допустимый FBS текущего seller; подмена ID не раскрывает чужие данные. | Подтверждено в локальной internal/emulator/browser-границе: один `eligible_rows` SQL по `FbsOrderMarking → FbsOrder → FbsSupply.delivered_at` задаёт реестр; создание и retry операции повторно разрешают те же `row_ids` тем же predicate и отклоняют несовпадение состава. Tenant/seller, WB FBS, передача, отмена и возврат проверяются server-side; seller UI подключён к self API. Реальная PostgreSQL-конкуренция и live данные не приняты. |
| BC2 | BR2 | Импортировать WB rows с RUB `finalPrice`, только RUB `convertedFinalPrice`, нерублёвыми/пустыми и конфликтующими значениями. Проверить сохранённый immutable snapshot и точные копейки без fallback к `price`. | Подтверждено в локальной internal-границе: append-only observations сохраняют raw final/currency значения и ревизии, reload/backfill использует существующий импорт, RUB выбирается точно, legacy `price` не используется. Reviewer видел PG-only skip, а исполнитель отдельно предъявил PostgreSQL 16 evidence; live WB backfill не проверялся. |
| BC3 | BR3, BR4 | Эмулятор `cises/info`: разные pg, чужой ownerInn, null group, группа без `DISTANCE`, `meat`, статусы кроме `INTRODUCED` и обе официальные ветки `statusEx`, включая неизвестный режим прослеживаемости. В документы входят только однозначные допустимые коды; неизвестный pg/режим fail-closed, группы разделены, фактические причины сохранены. | Подтверждено в emulator-границе: orchestration повторно загружает выбранные строки, сверяет owner, однозначный pg, доступность `DISTANCE`, `INTRODUCED` и точные versioned правила `statusEx`; неизвестные данные fail-closed, `meat` отклоняется до MOD, причины сохраняются по item, документы разделяются по pg. Live `cises/info` не вызывался. |
| BC4 | BR4 | На emulator `/mods/list` проверить response key `result` (и отказ от `results`/`mods`), `page=0`, все страницы по `nextPage`, `total`, seller INN, точную матрицу после проверки доступности `DISTANCE`, группу без обязательного МОД и обязательные группы, включая `lp`; `meat` отклоняется до MOD. Для обязательной группы: ровно одна запись exact INN+pg передаёт её применимые `fiasId`/`kpp`; 0, 2+, другой ИНН или отсутствие pg дают per-document fail-closed. Первый результат не выбирается, warehouse/mapping/UI отсутствуют. | Подтверждено в emulator-границе: parser принимает только `result`, начинает с `page=0`, проверяет стабильные `total/nextPage`; MOD-фильтр применяется после доступности `DISTANCE`. Для обязательной группы используются поля единственного valid exact INN+pg MOD; 0/multiple дают per-document ошибку. В коде и маршрутах нет `WithdrawalModMapping`, warehouse→MOD, настройки или выбора; `FbsSupply.warehouse_id` в MOD resolution не участвует. Live `/mods/list` не вызывался. |
| BC5 | BR5, BR6 | Fake plug-in и ручной браузер: absent script, timeout, disabled extension/native host, CSP missing, Store denied, no cert, expired/no-key/bad chain, token removed, PIN cancel/wrong. Нет Store на readiness, PIN не попадает в WMS, восстановление требует явного действия. | Частично подтверждено в fake-adapter: readiness не открывает Store, certificate path проверяет время/private key/chain, CSP/PIN cancel не повторяется, PIN не читается. Реальный browser/plugin/token и ручная матрица не проверены. |
| BC6 | BR6, BR7 | На fake adapter передать challenge с Unicode и CRLF: `Content` получает исходную JS-строку без trim/decode, `ContentEncoding` не вызывается, `SignCades` получает BES и `detached=false`; из результата удаляются только CR/LF. Software-cert runner независимо проверяет attached CMS и вложенный UCS-2LE content. Сквозной test `certificateBinding → API → sign_in` берёт seller BillingProfile.inn без ручного `mchd_expires_at`, отправляет `unitedToken=true` и ограничивает token CRPT `expireDate`/cert expiry. | Подтверждено в local code/fake/OpenSSL-границе: adapter передаёт exact JS string без `ContentEncoding`, подписывает BES attached и независимо вызывает verify; backend всегда берёт проверенный seller INN, валидирует 10/12 цифр и учитывает `expireDate`/cert expiry. OpenSSL 3.6.3 runner независимо подтвердил attached CMS и exact 58-byte UCS-2LE без BOM, а изменение одного байта отвергнуто. Runner использует одноразовый RSA-сертификат: физическая КриптоПро, ГОСТ, USB-токен/PIN и sandbox auth этим не подтверждены. |
| BC7 | BR8, BR9 | Golden fixture exact UTF-8: server bytes→base64→CAdES detached→независимая проверка подписи; изменение одного byte ломает verify. Проверить setter order, hash, отсутствие reserialize и одну подпись на документ. | Частично подтверждено: fake CryptoPro проверяет `ContentEncoding` до неизменённого base64, detached CAdES-BES и отсутствие reserialize; backend отправляет исходные bytes и immutable hash. Реальная подпись и независимая криптографическая проверка test-cert runner не выполнены. |
| BC8 | BR9, BR12 | Проверить `action_date` как дату вывода в московском календаре, отдельно от `delivered_at` и `document_date`; отсутствие первичного документа в MVP; отклонение запрещённых/условных полей `DISTANCE`; обязательный `product_cost`. Создать несколько pg и объём у границ 30 000/30 MB; outer MANUAL/LK_RECEIPT точен, части не пересекаются, limiter не превышает 50 rps, успех одного документа не откатывается отказом другого. | Подтверждено в internal/emulator-границе: builder использует московскую дату операции, строгий MVP allowlist, обязательные `cis/product_cost`, exact immutable bytes, разделение по pg/MOD и лимиты 30 000/30 MB; guarded transport формирует MANUAL/LK_RECEIPT и использует shared 50 rps limiter, ledger сохраняет независимый результат документов. Реального submit не было из-за B2. |
| BC9 | BR10, BR11, BR20 | Двойной клик, повтор того же request, reload и два параллельных пользователя по одному КИЗ возвращают одну operation/attempt и не создают второй CRPT document. | Подтверждено в local API/emulator/DB-границе: request id, selection hash, active provider-CIS/marking claims, reload и tenant scope не создают вторую operation; intent сохраняется до POST, uncertain result не запускает второй create. Исполнитель отдельно получил `5 passed` на одноразовом PostgreSQL 16; reviewer PG-прогон не воспроизвёл и в SQLite-наборах видел четыре PG-only skip. Live CRPT concurrency не проверялась. |
| BC10 | BR12, BR13 | Эмулировать 400/401/403/422, 5xx и все async statuses. Только CHECKED_OK даёт «Выведен»; terminal errors сохраняют реальные errors/commonErrors, неожиданный статус не становится успехом. | Подтверждено в internal/emulator-границе: HTTP-классы и async statuses разобраны; только `CHECKED_OK` становится success, terminal provider errors проецируются в реальные `errors/commonErrors`, raw evidence остаётся append-only, неизвестный статус остаётся reconciling. Recovery task зарегистрирована в Celery beat. Live polling не выполнялся. |
| BC11 | BR14 | Оборвать create после внешнего принятия: list+info находят exact body и присоединяют ID без второго POST. Для no-match и multiple-match новый create не выполняется, состояние остаётся сверяемым/incident. | Подтверждено в internal/emulator-границе: recovery использует только v4 GET, сравнивает CIS/cost/action/date/INН/MOD, exact match присоединяет ID, no-match и duplicate остаются reconciling без POST; duplicate incident не исчезает от последующего чтения. Live потеря ответа не воспроизводилась. |
| BC12 | BR14, BR15 | После terminal reject исправить причину и явно retry: повторная цена/cises/MOD, новые bytes/signature/attempt; после UNKNOWN retry запрещён. UI остаётся одним окном и показывает только фактический terminal error. | Подтверждено в internal/emulator/browser-границе: terminal reject допускает явный retry с новой attempt, повторными price/cises/MOD, новыми immutable bytes и подписями; прежняя evidence сохраняется, UNKNOWN/RECONCILING retry запрещает. Browser slice показывает только плотный реестр, одну компактную сертификатную модалку и отдельную модалку фактической terminal ошибки. Live provider reject не проверялся. |
| BC13 | BR16 | Попытаться подмешать cert/signature/operation другого seller/tenant; проверить DB constraints, 403/404, audit и отсутствие token/PIN/private key/full payload в обычных логах. | Подтверждено в local API/DB-границе: self API даёт 403/404, composite FK и provider-CIS/marking claims защищают tenant/seller scope, evidence append-only, token/signature не возвращаются. PostgreSQL constraints/triggers вошли в отдельный локальный PG16-прогон `5 passed`; reviewer его независимо не воспроизвёл. Реальные deployed application logs, физический PIN/token и production audit не проверялись. |
| BC14 | BR17 | Пройти CSP/plugin/extension + software cert runner, затем Windows 10/11 и поддержанный macOS Intel/ARM × Chrome/Edge/Firefox, USB insert/remove, site trust/policy, несколько cert и tab reload. | Не проверено как CryptoPro integration: fake-adapter и независимый OpenSSL runner с одноразовым RSA-сертификатом подтверждают только программную CMS/UCS-2LE границу. Физический CSP/plugin/extension/token, ГОСТ и browser/OS matrix не выполнялись. |
| BC15 | BR17, BR22 | На назначенном sandbox participant выполнить auth→cises→sign→create→v4 terminal status, сверить GIS ID/body и реестр после reload. Убедиться, что production flag=false и production POST не выполняется. | Не проверено — BLOCKER B2; production withdrawal явно вне этой волны. |
| BC16 | BR7 | На emulator проверить точные auth payload/response: обязательные `uuid`/`data`, `unitedToken=true`, отсутствие `details` в MVP, seller BillingProfile.inn всегда и строго 10/12 цифр, чтение `uuidToken`/`expireDate`, fail-closed на `code`/`error_message`/`description`. | Подтверждено в local emulator/contract-границе: `sign_in` всегда получает seller BillingProfile INN без injected MЧД expiry, отправляет `uuid/data/unitedToken=true`, не добавляет `details`, читает token/expiry и fail-closed сохраняет фактический provider `error_message`/description. Attached browser профиль подтверждён только BC6 local evidence; sandbox ответ и live expiry не проверялись. |
| BC17 | BR3, BR9 | Табличными fixtures пройти все группы, для которых `DISTANCE` запрещён, и разрешённые группы; проверить, что builder допускает только MVP allowlist, не смешивает `action_date`, `delivered_at` и `document_date`, требует `product_cost` и отклоняет условные/запрещённые поля до подписи. | Подтверждено в internal/emulator-границе: business preflight fail-closed проверяет разрешённые/запрещённые группы до MOD, а builder принимает только MVP allowlist, не смешивает даты, требует `product_cost` и не добавляет условные/запрещённые поля. Реальный ЧЗ-схема/submit не проверялись. |
| BC18 | BR5 | Проверить pinned tarball/file hashes и bundled license metadata; production build отдаёт `/vendor/cryptopro/cadesplugin_api.js` как JS 200, не HTML fallback, loader ждёт `window.cadesplugin`, а не только `onload`. | Частично подтверждено в source/build-границе: vendor bytes, pinned hash, provenance и license metadata находятся в Git diff; loader-тест ждёт Promise `window.cadesplugin`, frontend build прошёл. Фактические HTTP 200/MIME/hash/CSP на production HTTPS origin не проверялись. |
| BC19 | BR3 | Fixture с реальным GS1 `01+GTIN+21+serial+GS+91+GS+92`: реестр и item сохраняют исходное `FbsOrderMarking.value`, а `cises/info`, builder и reconciliation получают одинаковый AI01+AI21 длиной 18–74; неоднозначный код не уходит провайдеру. | Подтверждено в local code/fixture/DB-границе: исходный scanner value сохраняется, provider projection детерминированно даёт AI01+AI21, а cises/builder/reconciliation используют один `provider_cis`; неоднозначные и недопустимые значения fail-closed. Миграция и уникальные active claims проверены локально; live CRPT не вызывался. |
| BC20 | BR3, BR19 | Official fixtures `INTRODUCED/statusEx=EMPTY`, null, CONNECT_TAP, WAIT_TRANSFER_TO_OWNER, MOVING_BY_UD и неизвестная pg. `EMPTY` равен отсутствию в обеих ветках; versioned code snapshot выбирает режим без owner config, неизвестная pg блокирует только свой документ. | Подтверждено в local code/fixture-границе: `EMPTY` нормализуется как отсутствие, versioned snapshot не использует ручной owner config и не придумывает даты, неизвестная pg fail-closed только для своего документа. Live `cises/info` и фактический внешний drift не проверялись. |
| BC21 | BR7, BR10, BR14 | Истечь token/вернуть 401 после uncertain submit, reload UI и reauth тем же явным действием. После reauth выполняются только v4 list/info; create count остаётся прежним, payload/signature/intent не меняются. Отмена заказа после submit не блокирует сверку; token removal/PIN cancel оставляют `reauth_required`. | Подтверждено в local code/emulator/frontend-границе: expired token/401 переводит operation в `reauth_required`; тот же пользователь и сертификат проходят новый auth, после чего выполняется только GET-reconciliation без нового create и без изменения persisted bytes/signature/intent. Поздняя отмена заказа не блокирует чтение; при `production-submit=false` reauth разрешён, но submit остаётся закрыт. Физический token/PIN и sandbox 401 не проверялись. |
| BC22 | BR12, BR21 | Fixtures 400/401/403/422/429/5xx проверяют точный request/response contract, фактический `error_message`, Retry-After/shared limiter, definite/uncertain границу, partial documents и tenant isolation. | Подтверждено в local fixture/emulator-границе: source-to-test manifest и тесты покрывают 400/401/403/422/429/5xx, фактический provider `error_message`, Retry-After, definite/uncertain границу, partial state и tenant isolation; blind create retry запрещён. Live provider responses не проверялись. |
| BC23 | BR20 | На одноразовой PostgreSQL test DB с миграциями с нуля пройти два конкурентных пользователя, claims/uniqueness, append-only triggers, rollback и свежую schema; тест отказывается от production-like URL. | Подтверждено отдельным локальным evidence исполнителя: на одноразовом PostgreSQL 16 получено `5 passed`, затем полный Alembic head/downgrade/upgrade. Reviewer не воспроизвёл этот PG-run и в своих SQLite-прогонах видел четыре PG-only skip. Это не удалённый CI и не production DB. |
| BC24 | BR17, BR22 | В production-like config API, worker и beat получают одинаковые WITHDRAWAL/base URL/encryption flags; Redis общий, recovery зарегистрирован, миграции применены, vendor asset присутствует, production-submit=false. Railway/compose topology имеет работающие worker/beat. | Частично подтверждено на уровне code/config: compose передаёт согласованные withdrawal settings, добавлены role-aware Railway entrypoint и worker/beat descriptors, shared Redis/migration-head/production-submit guards и vendor asset проверяются тестами. Фактические Railway services, общий Redis, применённые миграции, runtime flags и deploy не проверялись. |
| BC25 | BR21, BR22 | После реализации/review открыть PR и получить один полный существующий CI: backend, frontend, backlog/docs и новые предметные fixtures/PG проверки. | Не проверено как полный remote CI: существующий workflow дополнен PostgreSQL/Alembic и independent OpenSSL checks, а локально подтверждены frontend `53 passed`/TypeScript/ESLint/build/diff-check, три SQLite-прогона `195 passed, 4 skipped`, отдельные PG16 `5 passed` и Alembic up/down/up. PR и полный GitHub CI ещё не предъявлены. |

Все BC-проверки выполняются на emulator/sandbox; ошибки и timeout не воспроизводятся на
рабочих операциях селлеров. C1–C12 ниже подтверждают только принятый UX-прототип.

## Проверки для приёмки прототипа (историческая UX-приёмка)

| Проверка | Требования | Подготовка, действие и ожидаемый результат | Вердикт |
|---|---|---|---|
| C1 | R1, R2 | Открыть seller-портал и перейти «Честный знак → Вывод из оборота». Экран находится внутри раздела, имеет явную границу прототипа и сразу показывает плоский реестр без верхних status-карточек. | Принято: в браузере подтверждены раздел, вкладка, явная пометка прототипа и плоский реестр без верхних карточек статусов. |
| C2 | R2, R3, R9 | Подготовить напечатанный и внешний отсканированный КИЗ переданных FBS-заказов. Выбрать период и найти товар: обе строки видны без колонки источника, а таблица содержит только дату, заказ WB, отдельные артикул и наименование, КИЗ и простой статус. Проверить «Не выведен», «Выведен» без приписок и «Ошибка» с реальным текстом ЧЗ. | Принято в границе прототипа: таблица содержит только требуемые колонки, напечатанные и отсканированные коды выглядят одинаково, статусы ограничены тремя значениями, а подробность показана только у ошибки. |
| C3 | R4, R13 | В данных прототипа иметь переданный FBS, ещё не переданный FBS, отменённый до передачи FBS, FBO/FBW и возврат. В видимом реестре остаётся только КИЗ переданного FBS; остальные случаи вообще не показаны. | Исторически принято в границе демонстрационных данных: реестр и пользовательский путь содержат только уже переданные FBS-КИЗ. Текущая server-side выборка принята отдельно по BC1 в local/API-границе; live данные не проверялись. |
| C4 | R5 | На наборе больше 250 строк проверить поиск/товар, период и «Только не выведенные», страницы 50/100/250, sticky header и ручной выбор. «Выбрать всё» выбирает только доступные строки текущей видимой страницы после фильтров; кнопка недоступна при пустом выборе. | Принято: проверено на 305 найденных строках; доступны 50/100/250, sticky header остаётся читаемым, выбор ограничен текущей страницей, а смена страницы, фильтра и размера страницы очищает скрытый выбор и корректно сбрасывает/ограничивает номер страницы. |
| C5 | R6 | Выбрать несколько КИЗ и нажать «Вывести из оборота». Сразу открывается только компактное окно сертификата; нет stepper, summary, предупреждения, причины, периода, количества документов, списка КИЗ, групп, `LK_RECEIPT`, preflight или технических стадий. | Принято: после основного действия сразу открывается единственное компактное окно сертификата; перечисленных промежуточных блоков и технических стадий нет. |
| C6 | R7 | На демонстрационном сценарии и в коде проверить внутреннюю повторную проверку допустимости КИЗ. Она не создаёт отдельного шага или успешного уведомления; фактическая ошибка Честного знака передаётся в итоговую модалку по C8 без выдуманной причины. | Исторически принято в границе интерфейса прототипа: preflight не вынесен в UI, а заранее заданный сценарий ошибки передаёт конкретную причину в модалку C8. Текущая server-side повторная проверка принята отдельно по BC3 только на emulator, без live True API. |
| C7 | R8 | Открыть окно сертификата, отменить и повторить; затем выбрать сертификат и нажать «Подписать и отправить». В окне только выбор, «Отмена» и «Подписать и отправить»; challenge и подпись документа не показаны отдельными шагами, отмена не меняет реестр, ключ/PIN не отображаются как данные WMS. | Принято: отмена сохраняет выбор и позволяет открыть окно повторно; после выбора сертификата доступно одно действие отправки, отдельных шагов подписи нет, ключ и PIN не отображаются как данные WMS. Реальная КриптоПро-подпись не входит в прототип. |
| C8 | R9 | Отдельно пройти успешный и ошибочный ответы. При успехе окно закрывается, строки становятся «Выведен», допускается только краткий toast. Только при фактической ошибке Честного знака открывается простая модалка с конкретными КИЗ и возвращённой причиной по каждому; лишних технических статусов и окон нет. | Принято: успешный сценарий закрывает окно, переводит две строки в «Выведен» и показывает краткий toast; ошибочный сценарий показывает отдельную простую модалку с полным КИЗ, заказом WB и причиной «цена продажи не указана». |
| C9 | R10 | Осмотреть код и демонстрационный сценарий двойного клика/потери ответа: второй документ не создаётся, до сверки успех не фиксируется, слепого повтора нет. «Сверяем результат» и другие технические стадии в интерфейсе не показываются. | Исторически принято в границе прототипа: повторное нажатие блокируется на время имитации отправки, а lost-response и другие технические стадии намеренно не показаны. Текущая server-side идемпотентность и reconciliation приняты отдельно по BC9/BC11 на emulator; PostgreSQL и live потеря ответа не проверены. |
| C10 | R8, R11 | Осмотреть код и весь макет: отдельного seller-switch и audit UI нет; приватный ключ, контейнер и PIN не отображаются и не сохраняются. В документе остаётся backend-контракт будущего tenant-аудита, но прототип его не изображает выполненным. | Исторически принято для UX: seller-switch и audit UI отсутствуют, приватный ключ, контейнер и PIN не отображаются и не сохраняются макетом. Текущий backend-аудит принят отдельно по BC13 только в local/API-границе; application logs и PostgreSQL не проверены. |
| C11 | R1–R10 | На обычной и узкой ширине проверить плотные одноуровневые строки, длинные артикул/наименование/заказ/КИЗ, sticky header, пагинацию, единственное окно сертификата и отдельную простую модалку ошибки. Горизонтально прокручивается только таблица при необходимости; весь body, фильтры и основное действие не разъезжаются и не накладываются. | Принято: на обычной и узкой ширине строки остаются плоскими, фильтры и модалки помещаются, body не получает горизонтальную прокрутку; широкая таблица прокручивается внутри собственного контейнера, а её заголовок закреплён и имеет непрозрачный фон. |
| C12 | R12 | Осмотреть сетевые вызовы прототипа и сценарий с отсутствующей ценой. Внешнего вызова True API нет; неподтверждённая цена не выдумана, необходимость интеграционной проверки явно сохранена в контракте. | Принято: в коде и браузерных ресурсах нет вызова True API, цена не подставлена, а её отсутствие показано только как демонстрационная причина ошибки; обязательная интеграционная проверка сохранена в R12. |

## Сверка постановки

Исходный отчёт, период по передаче WB, товарный фильтр, номер FBS-заказа, чекбоксы,
массовый выбор, признак вывода, фильтр невыведенных и подписание сертификатом покрыты
R1–R9 и BR1/BR15. Последнее уточнение владельца о реестре только после «Отгрузить», удалении
источника/группы/служебных статусов, раздельных артикуле и наименовании, плоских строках
и больших объёмах покрыто R2–R5, R9, BR1 и C1–C4/C11. FBO/FBW, отмены и возвраты
исключены по R4/R13 и BR18. Немедленный выбор сертификата без предпросмотра и
технических шагов покрыт R6–R10, BR5–BR8/BR15 и C5–C9.

Боевой контракт не подменён макетом: финальная цена WB описана BR2, server-side
группа/владелец — BR3, МОД — BR4, CryptoPro и документные подписи — BR5–BR9, same-origin API и
хранение — BR10–BR11, create/limits/status — BR12–BR13, потеря ответа/retry — BR14,
tenant/audit — BR16, контуры/release — BR17, versioned code snapshot
прослеживаемости — BR19, PostgreSQL — BR20, источник→fixture — BR21, release/CI — BR22.
У всех BR есть BC-проверки.

B1 закрыт продуктовым решением: `/mods/list` используется только для технического
требования «ровно один внешний МОД exact seller INN+pg»; 0/multiple блокируют документ,
а WMS не хранит mapping и не добавляет выбор или настройку.
Неподтверждённые значения не превращены в требования: B2 требует назначенного sandbox
participant/cert/test data. B3 закрыт официальным Appendix 2 и рекомендованным CRPT
browser-helper: auth challenge подписывается как исходная JS-строка default UCS-2LE,
CAdES-BES, attached. Реализация и независимая проверка этого профиля — обязательная
BC6, а не оставленная продуктовая развилка.

## Заключение

**Текущий локальный WMS-517 slice принят в фактически доказанной
code/emulator/browser/OpenSSL/config/PostgreSQL-границе.** Код сохраняет единую цепочку
`eligible_rows → registry → row_ids → operation/document`; отдельной FBS
warehouse→MOD модели нет. Для обязательного МОД выполняется только свежая внешняя
проверка `/mods/list`: одна валидная запись даёт `fiasId`/`kpp`, 0 или несколько
блокируют документ своей группы. Business preflight, builder, operation ledger,
protected submit/reconciliation, reauth и recovery scheduler связаны.

Историческая браузерная UX-приёмка C1–C12 сохраняется. Финальный technical recheck не
нашёл новых блокирующих несоответствий. Reviewer подтвердил frontend Vitest
`53 passed`, TypeScript, адресный ESLint, build и `git diff --check`; исполнитель
трижды получил backend `195 passed, 4 skipped` на SQLite, где четыре skip относятся
только к PostgreSQL. Отдельный запуск исполнителя на одноразовом PostgreSQL 16 дал
`5 passed` и полный Alembic head/downgrade/upgrade; reviewer этот запуск независимо не
воспроизвёл. OpenSSL 3.6.3 runner подтвердил attached CMS/CAdES-BES и exact UCS-2LE
bytes с одноразовым RSA-сертификатом, но это не доказательство КриптоПро, ГОСТ или
физического USB-токена.

Attached auth adapter, закреплённый browser asset, versioned traceability snapshot,
`statusEx=EMPTY`, GS1→provider CIS, 401 reauth/reconciliation и local deploy-config
реализованы и приняты в указанной границе. Изменения WMS-517 не смешаны с WMS-514:
внешняя интеграция True API/Честного знака относится только к WMS-517.

**Продуктовая приёмка разрешает сохранить текущий diff отдельным commit, отправить
ветку и открыть PR, но не разрешает объявлять production readiness.** Сейчас результат
остаётся незакоммиченным локальным diff. Непроверенными остаются: физическая
КриптоПро/browser/token/PIN-матрица, поведение ГОСТ-провайдера, B2 sandbox/live True API,
фактическая Railway topology/deploy с API+worker+beat+Redis и миграциями, один полный
удалённый GitHub CI и любая production-операция. Production-submit остаётся выключенным;
секреты/сертификаты не использовались, sandbox/production вызовов и production withdrawal
не было.
