# WMS-442 — печать товара и ячейки из сортировки ТСД

## Исходный запрос и уточнения

Пункт 5 владельца, 13.09.2026, дословно:

> 5\. Нету возможности распечать шк ячейки или товара прям из сортировки - это наш отдельный трек с печатью - его надо дожать, там многое уже было сделано. Нужно придумать архитектуру как нам устаанвливать это приложение на ПК, либо что ещё лучше - вообще без установок, так чтобы система например в вебе хапросила рахращенеи и по нему сама сходила на пк и настроила всё как нужно. Если это необзходимо и неизбжено, можно вообще нейронку вызввать чтобы она сама там всё настроили. Но это коненчо маловероятно

Общие условия владельца: отдельная задача со своим БА, разработчиком, перекрёстным ревью и приёмкой; независимые задачи параллельно. Работа от origin/etalon в собственной ветке/worktree; WMS-433 и его сводная строка не изменяются. Эта задача продолжает WMS-402, а не объявляет её уже выполненной. Возможность нейросети — гипотеза владельца, обязательного вызова модели нет.

Уточнение ведущего 13.09: 18080/5199 заняты WMS-433, не запускать и не останавливать там сервисы. Production разрешён только для чтения обычных экранов. Код продукта БА не меняет. Ведущий запросил у владельца ОС/модель/подключение принтера; до ответа выполняется проверяемая платформенная основа, UI настройки платформы пока не реализуется. Это последовательность работы, а не снятие R9 или физической приёмки.

### Уточнение владельца о модели и границах изменений, 13.09.2026

После исчерпания лимита Opus владелец поручил Astra завершить работу:

> отдавй астре доделать
> жётско только скажи что дизайн код строго как делал опус и как есть в системе, ничего нового не добавлть и не меняиь
> помимо того что описано явно в поставноке

Сохраняются существующий дизайн, компоненты и подход к коду системы и уже сделанной работы Opus. Изменения допустимы только в явно описанном объёме этой постановки. Смена модели не отменяет отдельное ревью, чек-лист и приёмку аналитиком; функциональные требования не изменены.

## Существующий процесс и цель

Исходники основного checkout этой задачи проверены на e1bd91146c38069786f7d2d5c48ed616a0364807, мобильный Git на af9693126d19d16918abd5cc7b7fbe99dfebc142 (база APK11). Это доказательство исходников, не версии сервера или установленного устройства.

Работник сортирует принятую поставку на экране ТСД «Размещение»: выбирает товар/короб, сканирует ячейку, подтверждает размещение. В SortingScreen.kt печати нет. Ячейки уже загружаются с их barcode; товар и ячейка должны печататься по существующим данным. В вебе FfInboundRequestView.tsx уже содержит контекстную кнопку «Печать ШК ячейки» рядом с выбранной ячейкой, а FfWarehouseMapPage.tsx печатает её существующий штрихкод. Повторять эти экраны в новом разделе не нужно.

WMS-402 уже содержит backend/app/api/fbs_print_jobs.py, сервис fbs_print_job_service.py, BackgroundJob и FbsPrintAsset, одно задание с UUID, атомарное получение, проверку файла и квитанцию очереди ОС. POST /document привязан к FbsSupply: выдавать сортировку за FBS-поставку нельзя. tools/print-agent/wms_print_agent.py отправляет файл через lp (CUPS, очередь Linux/macOS), запускается на один проход вручную или планировщиком; Windows, постоянной привязки конкретной очереди и встроенного подключения ПК нет. Нынешний широкий доступ сотрудника не закрывает исходное требование WMS-402 об узком доступе агента только к печати.

Ветка codex/wms-print-20260911 прочитана через git show, HEAD 4f394d86a17d4031f0c1f4a8862da3f1308e6a9f; diff её print-agent/API/service с этой базой пустой. Патч docs/reviews/artifacts/wms402-mobile-20260911/tsd-network-print.patch сохранён отдельно, в APK11 не применён. Его README прямо отмечает неподтверждённую сборку и дефект: после потерянного ответа новое нажатие создаёт другой job_id. Такой патч нельзя перенести целиком без исправления. В APK11 FbsPrinting.kt использует Android PrintManager.

Цель: из документа сортировки напечатать нужную этикетку, продолжить сканирование, увидеть результат передачи. На ПК склада один раз подключают небольшую программу к уже установленному принтеру; дальнейшие задания она получает сама.

## Продуктовое решение и архитектура

Применены навыки wms-product-analyst и wms-product-decision из docs/reviews/2026-09-11-analyst-draft/skills в основном checkout. Прочитан плохой случай 1 библиотеки owner-cases.md: действие над строкой счёта потеряло контекст, когда его вынесли на общий экран. Здесь печать остаётся возле товара/ячейки текущего документа.

**Выбран путь:** ТСД → существующая очередь WMS → исходящее HTTPS-соединение программы на ПК → установленная очередь принтера ОС. Программа не открывает сетевой слушатель и не принимает произвольные команды. Развивается нынешний print-agent, а не второй движок очередей. Товарные и складские этикетки принимаются отдельным контекстом документа сортировки с проверкой его склада/селлера; существующий FBS API сохраняет договорённости и поведение.

**Подключение ПК:** распространяемый пакет программы с простым запуском настройки, выбором существующего принтера ОС и обязательным автоматическим запуском в фоне после подключения (уточнение 13.09 ниже заменяет прежнюю опциональность). Настройка WMS показывает соответствие склада и выбранной очереди, доступность программы и проверку подключения. Обычный сотрудник не собирает Python-зависимости и не копирует токен в переменную окружения. Авторизация использует существующую учётную запись и отдельный ограниченный контур печати; реальные секреты при разработке не выпускаются и не меняются. Установка драйвера при его отсутствии остаётся явным действием через ОС/официальный пакет изготовителя. ОС и принтер определяются при настройке, не угадываются.

Для первой поставки достаточно одного назначенного принтера склада, но его имя и назначение видимы на ТСД перед отправкой, а адресат фиксируется в задании. Минимальная постоянная привязка нужна: нынешний BackgroundJob хранит отдельную печать и не может быть реестром действующей настройки. Сначала проверить существующие настройки склада; новая таблица допустима только если там негде сохранить привязку и с объяснением в реализации. Не нужны журнал попыток, счётчики напечатанных товаров, новый складской статус или второй остаток. Смена назначения действует на новые задания; ожидающие и выданные задания не перенаправляются молча.

**Почему не обещана установка через одно разрешение браузера:** Window.print открывает диалог печати; WebUSB даёт доступ к выбранному совместимому USB-устройству и требует действия пользователя, затем код должен знать интерфейс/протокол. Native messaging требует уже установленного приложения и расширения. Из этих возможностей не следует право обычной страницы устанавливать драйверы, запускать произвольные команды ОС и обслуживать любой принтер при закрытом браузере. Поэтому универсальный браузерный способ здесь не выбран. ИИ не создаёт отсутствующих полномочий и в путь печати не входит.

Первичные источники проверены 13.09.2026:

- [MDN Window.print](https://developer.mozilla.org/en-US/docs/Web/API/Window/print): открытие диалога текущего документа.
- [Chrome WebUSB](https://developer.chrome.com/docs/capabilities/usb): выбор устройства, HTTPS, пользовательский жест, интерфейсы и протоколы.
- [Chrome Native messaging](https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging): связка расширения с отдельно установленным приложением ОС.

**Граница осмотра UI:** 13.09 в CUA открыт production /app/ff/locations; показана форма входа, авторизованного экрана нет. По роутингу эталон находится на /app/ff/warehouse-map. Затем открыт изолированный baseline http://127.0.0.1:5197/app/ff/warehouse-map (API18082, отдельная тестовая БД). Вход bootstrap-сотрудником admin@test.local отклонён валидатором email: reserved/special-use domain. Ведущий исправил только тестовый seed на admin@example.com; вход выполнен. Через браузер в новой тестовой БД создан склад «Тест печати WMS-442» (P442) и ячейка «А 1.1», отображаемый ШК LOC-9DF85B314B88. Осмотрены таблица и контекстная иконка принтера в строке ячейки; нажатие открывает компактный диалог «Печать ШК ячейки А 1.1», размер по умолчанию 58 × 40 мм, действия «Отмена»/«Печать». Нажата отмена, физическая печать не вызывалась. Визуально: белая таблица на светлом фоне, название и код в строке, действия справа, основной цвет фиолетовый. Это осмотр эталона, не приёмка нового ТСД-экрана. ТСД ещё не осмотрен; до его UI-реализации требуется осмотр доступного эмулятора. Production данные и авторизация не менялись.

### Уточнение БА: рабочее подключение ПК в вебе, 13.09.2026

Это конкретизация R9 перед завершением реализации, **не финальная приёмка**. Сохраняются исходные слова владельца и все последующие ограничения модели, дизайна и непрерывности выше/ниже. Прежняя фраза «UI настройки платформы пока не реализуется» относилась к неизвестным ОС, драйверу и устройству склада. Она не откладывает необходимый универсальный WMS-ввод кода подключения: уже подготовленному macOS-пакету нужен этот рабочий вход. Ни ОС, ни модель принтера для ввода кода, предпросмотра и назначения существующей очереди угадывать не требуется. Вопрос об оборудовании уже задан; повторно владельцу его не задаём.

**Основание по текущему коду.** В backend/PC-кандидате 3b594c75489449079c7603943f385cf89b05b7a2 есть получение назначения, предварительная проверка кода и подтверждение. README программы описывает действия администратора, но работающего веб-входа для них в предъявленном frontend нет. Это незакрытый программный участок R9. Мобильный checkpoint 217f313 и frontend checkpoint 9c50f7a сами по себе не доказывают сборку, браузерную или аппаратную приёмку. Перенос изменений WMS-441 в ветку означает наличие её кода; заключение БА WMS-441 этим не выдаётся.

**Проверка экрана.** В этой сессии CUA недоступен из-за заблокированного Mac. Через разрешённый отдельный Playwright/Chrome context без пользовательского профиля выполнен вход только в http://127.0.0.1:5197/app/ff/warehouse-map синтетической учётной записью. Наблюдаемое текущее состояние — «Карта склада», «Пока нет складов», кнопка «Создать склад»; форма входа уже поддерживает ФИО и прежний логин. База и runtime не изменялись. Полная панель склада и контекстная печать ячейки были осмотрены в этой же BA-сессии ранее (описание выше); текущий компонент WarehouseMapToolbar.tsx подтверждает переключатель склада, кнопку создания склада и существующие SecondaryAction/AppDialog. Поэтому место выбирается по осмотренному существующему контексту, а не как новая страница. Текущий пустой экран не выдаётся за проверку нового подключения.

**Место действия.** В разделе «Ячейки» (/app/ff/warehouse-map), в существующей панели рядом с переключателем выбранного склада, добавить одну компактную вторичную кнопку «Принтер». Она открывает один стандартный AppDialog «Принтер склада · <название>»; используется тема и компоненты уже существующей системы. Контекст — весь выбранный склад, поэтому кнопка не помещается в строку отдельной ячейки и не подменяет её действие «Печать ШК ячейки». Когда склада нет, сохраняется нынешнее пустое состояние: подключать принтер ещё не к чему. Новый пункт меню, отдельная страница, мастер с отдельными экранами и новая схема дизайна не нужны.

**Что показывает диалог.** Сначала перечитать GET /operations/print/warehouses/{warehouse_id}/destination. До ответа — загрузка; при ошибке — причина с «Повторить», а не ложное «Не подключён». Если destination=null, показать «Принтер не подключён». Если назначение есть, показать имя очереди, платформу и состояние **программы ПК**: online=true → «Программа на связи»; online=false при last_seen_at → «Нет связи с программой» и время последней связи; отсутствующее last_seen_at → «Связь ещё не подтверждена». Последний контакт и online не обозначают наличие бумаги, готовность принтера или выход этикетки. Открытие/кнопка «Обновить» перечитывают назначение; пока диалог открыт, допустим редкий повторный GET для актуализации состояния, без фонового опроса всего экрана и без новых серверных статусов.

**Первое подключение.** Администратор запускает распространяемый пакет на ПК, выбирает установленную очередь в программе и получает временный код. В диалоге WMS есть короткая инструкция «Введите код из программы WMS Print на компьютере с принтером», поле «Код подключения» и действие «Проверить». Название пакета/ссылка на существующий реально доступный macOS arm64-артефакт и краткая инструкция первого запуска должны быть доступны из этого же диалога, чтобы сценарий не требовал знания README. Ссылку проверяют фактическим скачиванием; путь исходников или несуществующий Windows-пакет не выдаются за скачивание. Если опубликованного артефакта ещё нет, конкретный пакет передаётся вместе с проверяемой сборкой и это остаётся явной границей публикации. Для рабочего подключения нельзя требовать широкий токен сотрудника или переменную окружения. Установка приложения, драйверов и подтверждения macOS остаются в программе/ОС; веб их не имитирует.

**Предпросмотр и назначение.** «Проверить» вызывает POST /operations/print/warehouses/{warehouse_id}/pair/preview с pairing_code; операция не назначает принтер. В том же диалоге показать выбранный склад, имя очереди и платформу из ответа. Если склад уже имеет другой принтер, рядом показать прежнюю очередь и текст «Новые задания будут направляться на <новая очередь>. Уже созданные задания сохранят прежний принтер». Только после успешного предпросмотра доступно явное действие «Подключить» (или «Заменить принтер» при другом назначении). Изменение кода или выбранного склада снимает прежний предпросмотр; устаревший ответ запроса другого склада не подменяет текущий диалог. Администратор подтверждает ровно показанные склад/код/очередь. Отмена не меняет назначение.

**Результат и повтор.** Подтверждение вызывает POST /operations/print/warehouses/{warehouse_id}/pair с тем же pairing_code. Затем GET destination должен показать действующее назначение: только его можно сообщить как текущее. После успешной проверки — «Принтер подключён», имя очереди и состояние программы; поле временного кода очищается. При потере ответа подтверждения диалог сохраняет намерение в текущей сессии, сначала читает destination и сверяет connection_id с предпросмотром. Совпадение подтверждает успех без нового подключения. Если назначение не изменилось — разрешён явный безопасный повтор того же кода в пределах его срока; при недоступном GET показать «Не удалось проверить подключение» и «Проверить состояние», не обещать успех. Если GET показывает более новое другое назначение, показать его как действующее, не возвращать прежнее автоматически. После обновления страницы назначение заново читается с сервера; код не пишется в URL, аналитику, журналы или долговременное хранилище браузера.

**Ошибки и права.** Неверный/истёкший код → «Код не найден или истёк. Получите новый код в программе на ПК»; уже использованный в другом подключении → понятная причина без раскрытия чужого склада; остальные ошибки → локальное сообщение в диалоге и возможность повторить/закрыть. Назначение и предпросмотр доступны администратору согласно существующему API. Работник с существующим правом приёмки может видеть назначение/состояние, но не форму кода и изменение привязки. Иные роли не получают новое право чтения через этот UI; 403 не выдаётся за отсутствие принтера. Ошибка, отсутствие связи или закрытие диалога не блокируют работу с ячейками и сортировку.

**Граница готовности.** По этому уточнению Terra high завершает только описанный web-вход и его связь с уже существующим backend/PC; Astra делает независимое ревью, исходный БА — последующую приёмку. C13 остаётся непроверенной до фактического оборудования. Windows, автоматическая установка драйверов, Gatekeeper на чистом профиле и выход бумаги не объявляются проверенными по успешному вводу кода. Наличие недоделанного web-входа нельзя списывать на отсутствие ответа об ОС.

## Требования

| ID | Условие → действие → видимый и сохранённый результат | Основание |
| --- | --- | --- |
| R1 | В документе сортировки работник выбирает товар и вызывает его печать непосредственно в контексте товара. На этикетке существующий товарный штрихкод, читаемая подпись товара/артикула; при различиях WB/Ozon человек видит, какой код печатает. Печать не подменяет товар другим SKU. | Пункт 5, данные маркетплейса по AGENTS.md. |
| R2 | В том же документе работник может выбрать ячейку из существующих ячеек склада и напечатать её существующий штрихкод с кодом/названием ячейки. Выбор доступен и когда этикетка отсутствует, поэтому обязательный предварительный скан этой же ячейки не требуется. | Пункт 5, решение БА для отсутствующей этикетки. |
| R3 | Для выбранной этикетки работник видит объект, назначенный принтер и число копий (по умолчанию 1), подтверждает отправку. Печать/отмена/ошибка не меняют размещение, принятые количества, остатки, завершение документа и не теряют текущую цель сканирования. Можно продолжить сортировку после возврата. | Пункт 5 и контекст действий, AGENTS.md. |
| R4 | Существующий код берётся из доступного пользователю товара/ячейки этого документа/склада. При отсутствии кода выводится конкретная причина без пустого листа или придуманного кода; остальные операции сортировки доступны. Этикетка использует действующий формат WMS, не обрезает код и читается сканером. | Повторное использование текущих данных, пункт 5. |
| R5 | Одно подтверждённое намерение печати получает один UUID и неизменяемый файл/параметры. ТСД сохраняет это до выяснения результата, включая уход с экрана и перезапуск приложения. Потеря ответа на создание восстанавливается чтением задания и повтором того же запроса; конкурентный повтор не создаёт второй файл в очереди. Изменённое намерение с тем же UUID не принимается. | Контракт WMS-402, защита повторов. |
| R6 | Сервер связывает задание со складом, документом, автором и выбранной очередью на основании проверенных объектов. Другой клиент/склад/неназначенный агент не получает файл или результат. Сортировка не требует FBS-поставки или права выполнять FBS для печати обычной ячейки. | Исходный узкий доступ WMS-402, границы клиентов AGENTS.md. |
| R7 | Программа на ПК атомарно получает предназначенное ей задание, проверяет размер/тип/контрольную сумму файла и передаёт его только выбранной очереди ОС. Два экземпляра программы не исполняют одно задание повторно. Никаких произвольных URL, путей или команд из задания. | WMS-402 и существующий агент. |
| R8 | ТСД показывает «Ожидает передачи», «Передано в очередь принтера» с подтверждённой квитанцией либо конкретный отказ до передачи. Если программа могла передать файл, но подтверждения нет, показывается «Результат печати неизвестен». Перезапуск/таймер не отправляет файл повторно в ОС. Повтор квитанции допустим; после проверки принтера работник может явно заказать ещё одну печать новым UUID с пониманием возможного дубликата. | Контракт неопределённого результата WMS-402. |
| R9 | ПК один раз подключают через распространяемый пакет программы: выбрать доступную очередь, связать со складом, проверить соединение. Программа сама включает автозапуск и начинает фоновую работу; Windows обязательна как основная платформа, macOS — дополнительная. WMS и ТСД показывают назначение; недоступный агент не превращает задание в успех. Настройка не требует копирования широкого токена сотрудника. Поддержанные ОС и зависимость от установленного драйвера явно названы; неподдержанная ОС не имитирует успех. | Пункт 5 + исходная настройка/узкий доступ WMS-402. |
| R9a | В «Ячейках» для выбранного склада пользователь открывает «Принтер», видит сохранённое назначение и связь программы. Администратор вводит код из программы ПК, сначала проверяет склад/очередь/платформу, затем явно подтверждает назначение; результат подтверждается повторным GET destination. Отмена/ошибка не меняют складские данные; потеря ответа восстанавливается сверкой connection_id без скрытой замены. Изменение кода/склада требует нового предпросмотра. | Конкретизация существующего R9 через осмотренный контекст склада, без зависимости от неизвестного оборудования. |
| R10 | Печать сортировки пользуется общим устойчивым механизмом, не удаляет существующую Android-печать FBS до подтверждённой работоспособности заменяющего пути. Регрессией проверяются готовые FBS-этикетки маркетплейса и листы товарного ШК/ЧЗ. Печать не меняет FBS-статусы/наклейку/расчёты. | Дожать прежний трек, граница APK11/WMS-402. |
| R11 | Кнопки и компактный результат находятся в документе возле соответствующего объекта, соответствуют теме ТСД и осмотренному эталону; длинный товар/ячейка и ошибка не перекрывают действия и сканирование. | Пункт 5, AGENTS.md, owner-cases. |

## Границы исполнения и зависимости

Backend/агент можно делать независимо от WMS-441. Общая точка — SortingScreen.kt/SortingViewModel.kt: UI-печать накладывается на итог WMS-441 и не исправляет параллельно его остаток/завершение. Координатор передаёт разработчику стабильные поля документа, товара и ячейки после WMS-441. Складское проведение и дата списка принадлежат WMS-441. Приёмка WMS-442 обязательно повторяет размещение после печати на совместном результате.

По последнему уточнению владельца разработчик — Terra high, независимое ревью и приёмка — Astra; историческое назначение Astra/Opus выше не отменяет это уточнение. Файлы требований исполнитель не меняет под реализацию. Ни весь старый print patch, ни чужие коммиты не переносятся без проверки конкретных изменений. Новые инструменты управления ключами не требуются.

Уточнение 13.09 установило основную ОС: Windows; macOS также используется. Модель, подключение и драйвер реального принтера ещё требуют фиксации при проверке. Реализовать только доступную разработчику macOS недостаточно. Обязательные Windows-выпуск, фоновая работа и проверка оборудования определены в R12–R19/C19–C30 ниже. Физический сценарий не заменяется моками.

## Проверки

Вердикты до предъявления реализации оставлены пустыми по навыку БА. При приёмке каждый заполняется наблюдением; «не проверено» не означает принято.

| Проверка | Требования | Подготовка → действие → ожидаемый результат → повторная проверка | Вердикт |
| --- | --- | --- | --- |
| C1 | R1,R3,R4,R11 | Открыть сортировку с обычным и длинным товаром WB/Ozon → выбрать товар, 1 и несколько копий → этикетки содержат именно выбранный существующий код/подпись, принтер и число видимы → вернуться, цель и количества неизменны. Проверить Android UI и декодировать сформированный код. | **Не проверено — блокирует приёмку.** Финальный APK показывает компактное действие «Товар», а API проверяет неизменяемый asset; выбора WB/Ozon, нескольких копий и декодирования сформированного кода в пользовательском проходе нет. |
| C2 | R2,R3,R4,R11 | В сортировке выбрать ячейку без скана, затем другую ячейку того же склада → печать → коды/подписи совпадают с API и эталоном ячеек → возврат сохраняет контекст, сканирование продолжается. | **Не проверено — блокирует приёмку.** Действие «Ячейка» видно на emulator-5580 и сервер проверяет склад/код, но печать двух выбранных ячеек без предварительного скана и возврат к сортировке не пройдены. |
| C3 | R4 | Товар без ШК, ячейка без ШК, длинная подпись → попытаться печатать → конкретная причина, задания/пустого листа нет → другой корректный объект печатается, сортировка не заблокирована. | **Не проверено — блокирует приёмку.** Отсутствующие коды отклоняются контрактным тестом без пустого задания; экранный отказ и продолжение с другим объектом не предъявлены. |
| C4 | R5 | Прервать ответ POST после commit → закрыть экран и перезапустить приложение → восстановить и повторить → один UUID/одно серверное задание, неизменяемый файл → конкурентный тот же запрос возвращает его же; другой payload с UUID получает конфликт. | **Не проверено — блокирует приёмку.** Durable UUID/replay покрыт клиентскими и серверными тестами, но настоящий обрыв ответа после commit с закрытием и перезапуском приложения не выполнен. |
| C5 | R5,R8 | Отказать API до сохранения → повторить тем же намерением → одно задание; затем успешное явное «ещё одна копия» → новый UUID. Не смешивать транспортный повтор с намеренной перепечаткой. | **Не проверено — блокирует приёмку.** Технически различены транспортный повтор и подтверждённая перепечатка новым UUID; полный пользовательский путь отказа до записи и следующей явной копии не сохранён. |
| C6 | R6,R7 | Два клиента, два склада и два назначения принтера → запросить/получить чужой job/content/result → отказ без чужого файла; свой обычный работник сортировки печатает без фиктивной FBS-поставки. | **Подтверждено в изолированном API.** Проверены два клиента/склада, запрет чужих job/content/result и узкий агентский доступ; обычная сортировочная печать не требует фиктивной FBS-поставки. |
| C7 | R7,R8 | Два агента одновременно получают одно задание → только один вызов очереди ОС. Подменить checksum/тип файла до передачи → явный отказ, вызова очереди нет → другое исправное задание исполняется. | **Подтверждено в локальном runtime/API.** Атомарный claim отдаёт job одному агенту, контроль размера/MIME/checksum блокирует подмену до вызова очереди, следующее исправное задание остаётся рабочим. |
| C8 | R8 | Очередь ОС приняла файл, ответ подтверждения WMS потерян → перезапустить агент/ТСД → повторной отправки в ОС нет, статус неизвестен или восстановлена та же квитанция. Противоречащее повторное подтверждение не меняет итог. | **Не проверено — блокирует приёмку.** Outbox не выполняет повторный spool после возможной передачи и финальный ТСД умеет проверить running/явно заказать копию; фактический цикл очередь ОС → потеря ack → restart не проходил. |
| C9 | R6,R9 | Из чистого пользовательского профиля поддерживаемой ОС запустить распространяемый пакет → выбрать установленную очередь, связать склад, включить автозапуск → после перезапуска получено тестовое задание. Изменить назначение → старое задание не переадресуется молча. Записать ОС/версию пакета/конкретную очередь. | **Не проверено полностью — блокирует приёмку.** 13.09 Astra скачал финальные Windows x64/macOS arm64 артефакты run34775859323 и сверил SHA256. Отсутствие Windows больше не является дефектом исходников. Чистая установка, настоящий автозапуск/очередь и смена назначения в полном пакетном проходе остаются непроверенными. |
| C10 | R9 | Выключить агент/оборвать HTTPS → создать задание с ТСД → оно ожидает, не «напечатано»; включить агент → только ожидающее задание забирается. Протокол подключения не требует входящего порта/копирования широкого токена. | **Не проверено — блокирует приёмку.** Polling/heartbeat и pending-job покрыты локальным контрактом, но выключение и возвращение реального агента через пакет с получением только ожидающего задания не выполнено. |
| C11 | R10 | Готовая FBS-этикетка и лист ШК+ЧЗ → существующая печать остаётся доступной; новый путь выдаёт тот же файл. Сверить FBS-состояния и расчёты до/после → побочных изменений нет. | **Подтверждено в локальном контракте.** Готовый FBS PNG и sorting PDF проходят через тот же агентский snapshot без изменения FBS-состояний; существующий Android PrintManager не удалён. |
| C12 | R3,R11 | Совместная сборка с WMS-441: в середине размещения печать/отмена/ошибка → следующий скан и последний скан → результат размещения и завершения совпадает с контрактом WMS-441. Просмотреть узкий экран и длинные данные. | **Не проверено — блокирует приёмку.** Финальный APK показывает печать в существующем документе и автозавершение WMS-441 отдельно доказано, но единый проход печать/отмена/ошибка → следующий и последний скан не выполнен. |
| C13 — физическая | R1,R2,R4,R7,R8,R9 | На фактическом ПК/принтере склада получить этикетки товара и ячейки из ТСД → бумага нужного размера, код полностью виден и сканируется тем же ТСД → выключить принтер/исчерпать бумагу и проверить честный статус, затем возобновить без скрытого дубля. Зафиксировать ОС, модель, подключение, код сборки. | **Не проверено — внешняя обязательная граница блокирует приёмку.** Фактический складской ПК, принтер, бумага, сканирование отпечатка тем же ТСД и восстановление после отсутствия бумаги/питания не предоставлены. |
| C14 | R9,R9a,R11 | На тестовом складе без назначения открыть «Ячейки → Принтер» → увидеть склад и отсутствие назначения; перейти по настоящей ссылке пакета/инструкции, получить синтетический код программы и нажать «Проверить» → видны именно очередь/платформа из preview, GET destination ещё пуст → «Подключить» и повторное открытие страницы показывают совпадающий connection_id/очередь. | **Публичный путь пакета подтверждён на 6c23c2bb; полный сценарий не проверен.** В Chrome без GitHub-входа нажаты четыре ссылки настоящего диалога; пакеты/инструкция/SHA256 скачаны. Отдельная анонимная загрузка всех assets HTTP200, хеши совпали. Полный проход программы ПК → pairing → reopen не завершён. Дельта workflow требует F9/F10 по [review](../reviews/wms442-astra-public-release-review.md). |
| C15 | R9a,R6 | Имеются два склада и прежний принтер; проверить новый код → видны старое/новое назначение и эффект только для новых заданий; изменить код или склад → прежний предпросмотр более не подтверждается; отменить → назначения неизменны. Подтвердить после нового preview → изменён только выбранный склад, старое задание хранит прежний connection_id. | **Не проверено полностью — блокирует приёмку.** В Chrome на текущем компоненте подтверждены preview без POST pair и исчезновение подтверждения при изменении кода. Проверка замены серверных назначений двух складов/старых jobs остаётся прежней отдельной API-проверкой, а единый финальный проход не завершён. |
| C16 | R9a | В тестовой среде потерять ответ pair после commit → UI читает destination и подтверждает совпадение без повторного назначения; при отказе до commit разрешён повтор того же кода. Если другой администратор уже назначил новую очередь, повтор старого подтверждения не возвращает её назад, UI показывает фактическую очередь GET. После обновления страницы виден серверный результат. | **Подтверждено поведение текущего web-компонента с синтетическим HTTP; полный сценарий не проверен.** В Chrome потеря ответа pair приводит к одному POST и GET destination: совпадающий connection_id подтверждается, более поздний connection_id показывается без второго POST и отката. Это проверка UI-реакции, не новое воспроизведение настоящего server commit/конкуренции. |
| C17 | R9a,R6 | Проверить неверный/истёкший/использованный код, 403 и сетевой отказ GET/preview/pair → конкретные сообщения без чужих данных и ложного «нет принтера»; повторить с корректным кодом/закрыть диалог → можно продолжить. Работник приёмки видит статус, но не подключает; администратор имеет форму. | **Адресный дефект C17/R9a исправлен и подтверждён в Chrome на ec07018f.** Initial GET failure → success очищает прежнюю ошибку; следующий failure обработан без pageerror и без старого online-статуса. Истёкший/использованный код, preview network failure, pair 403 и форма работника повторно проверены на настоящем компоненте с синтетическим print HTTP. Полный авторизованный проход склада/реальные серверные отказы ещё не завершены. См. [повтор приёмки](../reviews/wms442-astra-acceptance-20260913.md). |
| C18 | R9a,R11 | Для действующего назначения получить online=true, старый last_seen_at и null last_seen_at → три честных состояния программы, без обещания бумаги. Длинные склад/очередь, узкое окно → названия и действия не налезают, существующая строка ячейки и её кнопка печати сохранены. Временный код не сохраняется в URL/долговременном storage и не выводится в журналы. | **Частично подтверждено в Chrome; полный сценарий не проверен.** Текущий компонент показал online, старый last_seen_at и null last_seen_at. При 380×800 длинная очередь переносится, кнопки доступны, горизонтального переполнения нет; скриншот осмотрен. Синтетический код не попал в URL/localStorage, временный код не зарегистрирован в ошибках браузера. Эталонная строка ячейки и весь контроль журналов в полном авторизованном экране не проверены. |

## Сверка запроса с требованиями

«ШК ячейки» → R2/R4; «товара» → R1/R4; «прям из сортировки» → R3/R11/C12. «Отдельный трек, многое было сделано, дожать» → восстановленная WMS-402, R5–R10 и C11. «Устанавливать приложение на ПК» → выбранная архитектура и R9/C9. «Лучше без установок, разрешение в вебе, сама настроила» → проверены возможности браузера, дан конкретный минимальный выбор с автоматизацией в пределах запущенной программы; универсальный доступ к ОС не обещан. «Можно нейронку… маловероятно» → гипотеза рассмотрена и не превращена в платную обязательную зависимость.

Обратная сверка: R1–R4/R11 следуют из действия печати и правил контекста; R5–R8 — из уже принятого WMS-402 и его фактических незакрытых дефектов; R9 — из прямой просьбы подключения ПК; R10 — из продолжения существующей работы без регрессии. Новых складских ограничений или обязательных маркировочных операций не добавлено.

## Заключение

**Актуальное единое заключение Astra, 13.09.2026: READY / DEPLOYABLE TO STAGING.** C17 и Windows copies исправлены; F9/F10 закрыты в финальном workflow/publisher. Публичное скачивание подтверждено ранее. Физическая C25 и составные неполные UI-проверки остаются открытыми. Подтверждённых открытых кодовых P1/P2 нет. По последнему решению владельца неполное операторское/физическое покрытие отдельно от допуска на стенд. Полный общий вердикт: [единый финальный отчёт](../reviews/tsd-package-final-astra-20260913.md).

### Предыдущие оценки покрытия (история; не текущий допуск на staging)

**Актуальная приёмка Astra, 13.09.2026: WMS-442 не принята.** Windows-review PASS на e37d2192 сохраняется; найденный позже C17/R9a исправлен на ec07018f и адресно подтверждён в Chrome. В 6c23c2bb добавлен фактически работающий публичный путь Windows/macOS-пакетов: ссылки UI и анонимные downloads проверены, контрольные суммы совпали. Но [review дельты публикации](../reviews/wms442-astra-public-release-review.md) выявил F9/F10 — отключение прежнего branch CI и удаление прежних assets при повторном publish до безопасного завершения. Их необходимо вернуть исполнителю и адресно перепроверить.

Блокировка не сводится к оборудованию. Помимо физического C13/C25/C26 и нативных установочных/фоновых сценариев, остаются F9/F10 и незавершённые составные Android/web-проходы. Подтверждённые ранее C6/C7/C11 сохраняются с прежними границами. Исторические заключения об отсутствии Windows, ссылки или незакрытом C17 ниже относятся к прежним версиям; исходный запрос и R1–R19 не смягчены. Полная приёмка, deploy и производственная печать не заявляются.

## Уточнение владельца о моделях — 2026-09-13

По последнему уточнению владельца разработку выполняет Terra high. Бизнес-анализ, постановку требований, приёмку, независимое ревью и оркестрацию выполняет Astra. По возможности приёмку проводит исходный аналитик; при замене ему передаются исходный запрос, уточнения и доказательства. Это исправляет прежнее ошибочное распространение перехода на Terra на бизнес-анализ и приёмку и заменяет прежнее поручение Astra доделать реализацию.

Ограничение владельца сохраняется: дизайн и подход к коду строго как у Opus и в существующей системе; менять и добавлять только явно описанное в постановке. Сохранённые изменения и результаты проверок передавать следующему исполнителю, не повторять пройденные проверки без изменений или новых оснований. Порядок постановка → реализация → независимое ревью → приёмка сохраняется.


### Уточнение владельца о непрерывности работы

13.09.2026 владелец прямо указал: случайное обнаружение ключа само по себе не должно останавливать работу. Для этого пакета прежнее условие остановки по факту обнаружения заменено продолжением задачи. Значения не воспроизводятся в сообщениях и отчётах и не включаются в коммиты; это уточнение не поручает управление ключами.

### Заключение по уточнению web-подключения

Исходный БА конкретизировал R9/R9a и C14–C18: незавершённый ввод кода и назначение очереди надо реализовать сейчас в контексте выбранного склада. Это RELEASE постановки исполнителю, не принятие реализации. Полное заключение о приёмке WMS-442, WMS-441, устройстве и выпуске не выдавалось.

### Уточнение владельца: Windows и постоянная работа, 13.09.2026

Дословные реплики владельца сохранены отдельно от первоначального запроса:

> дблядь
> все компы на винде а не мак
> ну вернее и так и так бывает
> я не понялб ничё - программа эта должна работаь всешгда? или там каждый раз надо чё то настраивать?

> так а хули вы вставил то блдяьд если не готов ещё?

Ведущий передал обязательный контракт: одна настройка сервера, склада и системного принтера; далее постоянный фон и автоматический запуск после перезагрузки/входа. Повторное подключение требуется только при смене сервера, склада, принтера, переустановке или явном сбросе. Windows основная, macOS дополнительная. Факт включения реализации в staging-пакет не является приёмкой или доказательством готового установщика.

**Что проверил новый Astra-аналитик.** Прежняя BA-сессия заменена по поручению ведущего с передачей исходной постановки и уточнений. На исходном HEAD `f8f9ff3de97861922008f32f2794ae1cdbbbce30` README прямо исключает Windows, `build_package.py` допускает только `darwin`, CI собирает только macOS. `wms_print_runtime.py` использует CUPS и `fcntl`, не задаёт каталог Windows; настройка предлагает автозапуск с ответом «нет» по умолчанию и обещает его лишь на следующий вход. Это подтверждённые программные несоответствия, а не только отсутствие физического принтера. В этой сессии применены навыки аналитика и продуктового решения из основного checkout; учтены owner-cases (контекст действия) и failure-cases B02/B07/B09/B10 (неизвестный исход, продолжение работы и отсутствие скрытого повтора). Продуктовый UI не меняется, новый осмотр экрана не выдаётся за выполненный.

**Решение аналитика о выпуске.** Первый Windows-пакет — обычный установщик `.exe` для x64 с программой и всеми необходимыми библиотеками, включая чтение PDF/PNG; без Python, терминальных команд, отдельного PDF-приложения и WSL у сотрудника. Минимальная проверяемая матрица: Windows 11 x64 и Windows 10 22H2 x64, macOS arm64 с точной версией ОС в протоколе. Это заявка на проверки, а не обещание уже подтверждённой совместимости. ARM Windows, 32-битная Windows и Intel macOS не объявляются поддержанными по этой матрице без отдельной сборки и проверки; неизвестную архитектуру фактического Mac фиксируют до передачи пакета. Сохранить существующий macOS-пакет и исправить его фоновый запуск по тому же контракту. Linux в этот выпуск не добавляется.

Установка выполняется для текущего рабочего пользователя, например в `%LOCALAPPDATA%/Programs/WMS Print`; настройка и незавершённая квитанция отдельно в `%LOCALAPPDATA%/WMS Print`. Windows использует штатный механизм запуска при входе пользователя с перезапуском после аварии; macOS — существующий LaunchAgent. Конкретный системный механизм выбирает разработчик, он обязан выполнить C21/C22. Системная служба до входа пользователя не требуется: она усложняет доступ к принтеру, установленному в профиле. «Постоянно» означает, что ПК включён, не спит и рабочий пользователь вошёл; закрытый браузер, окно настройки и блокировка экрана не мешают. После сна или отсутствия сети связь восстанавливается сама. На выключенном ПК задания ждут на сервере. Пользователь не должен каждые сутки запускать настройку или оставлять окно терминала открытым.

Установщик регистрирует штатное удаление через список установленных приложений Windows. Повторный запуск установщика новой версии обновляет программу, сохраняя настройку и границу незавершённой передачи. Автоматическое скачивание/самообновление, новый экран управления, новая очередь или журнал не нужны. Используются существующие веб-диалог «Принтер», код подключения, `BackgroundJob` и один файл незавершённой квитанции. Права администратора ОС могут понадобиться для драйвера, но не для ежедневной работы; установщик не должен просить их без конкретной технической необходимости. Пакет публикуется по действующей ссылке загрузки с ОС, архитектурой, версией/commit SHA и контрольной суммой; CI-архив, который сотрудник не может получить, передачей программы не считается.

## Дополнительные требования Windows и фоновой работы

Эти требования дополняют R1–R11 и заменяют прежнее допущение об опциональном автозапуске и достаточности одной macOS.

| Требование | Проверяемое поведение | Основание |
| --- | --- | --- |
| R12 | Сотрудник получает фактический Windows x64 установщик из доступного места загрузки и устанавливает его на чистую ОС из матрицы без среды разработки. Пакет перечисляет реальные поддержанные ОС/архитектуры и содержит SHA исходников; отдельно предъявляется macOS-пакет. Непроверенная ОС не обозначается готовой. | Уточнение владельца о Windows/macOS и решение БА о минимальном выпуске. |
| R13 | При первом запуске на ПК сотрудник выбирает существующий системный принтер и адрес WMS; администратор WMS подтверждает склад существующим кодом. Сохраняются адрес, connection_id, узкий доступ, выбранная очередь и склад. После перезапуска и обновления это же подключение восстанавливается без нового кода. Перезапуск настройки показывает/использует существующее подключение, не создаёт новое сам. | Однократная настройка, R6/R9/R9a. |
| R14 | После успешного подключения фоновая программа запускается сразу и регистрируется на последующие входы автоматически. Закрытие окна настройки/браузера и блокировка экрана не останавливают её; при входе после перезагрузки запуск без действий сотрудника. После аварийного завершения процесс восстанавливается штатным механизмом без новой настройки и двойной передачи. Ошибка регистрации не выдаётся за успешную полную настройку. | Переданный ведущим контракт постоянной работы. |
| R15 | Windows получает список реальных очередей ОС и передаёт PDF/PNG в точно выбранную очередь с сохранением размера этикетки и числа копий. Результат опирается на подтверждённый приём очередью; запуск процесса или открытие PDF не считается квитанцией. Нет подмены выбранного принтера принтером по умолчанию. При исчезновении очереди — конкретный отказ, другие сортировочные действия доступны. | Печать этикетки R1–R4/R7/R8 и основная Windows-платформа. |
| R16 | После потери сети/перезагрузки/сна программа сама продолжает связь с сохранённым сервером и назначением. Ожидающие задания не теряются и не становятся «напечатано». После возможной передачи в ОС повторяется только известная квитанция, неизвестный исход не запускает новую печать. После такого задания следующие задания могут выполняться. | R5/R8 и обязательная постоянная работа; failure-cases B02/B07/B09/B10. |
| R17 | Конфигурация и незавершённое состояние сохраняются атомарно с доступом рабочего пользователя, без чтения обычным другим пользователем ОС; служебный доступ ОС допустим. Узкий доступ не хранится в аргументах автозапуска, логах или общедоступном каталоге. Работа с принтером использует права этого пользователя. Права настройки склада и печати в WMS остаются существующими. | R6/R9, исходный узкий агентский доступ и реальная установка Windows. |
| R18 | Установка новой версии поверх предыдущей останавливает старый экземпляр, сохраняет настройку/незавершённое состояние и запускает ровно один новый. При неудаче сохраняется возможность запустить прежнюю версию с прежним состоянием; автоматического повторного spool нет. Штатное удаление останавливает процесс и убирает исполняемые файлы/автозапуск; по умолчанию сохраняет локальную настройку и квитанцию для восстановления. Их удаление — отдельный явный сброс с предупреждением о незавершённой печати, не действие тихого обновления. | Однократная настройка и решение БА о минимальных установке/обновлении/удалении. |
| R19 | Обычные перезапуски, сетевые сбои и обновления не требуют повторного подключения. Явная смена сервера, склада или принтера выполняется существующим переподключением с подтверждением; старые задания сохраняют старый адресат. Неизменённые настройки соседнего склада/ПК остаются прежними. После удаления или выключения программы WMS честно показывает потерю связи, задания продолжают ждать. | Контракт ведущего, R6/R8/R9a. |

## Дополнительные проверки выпуска

Для каждого прогона сохранить SHA источников, хеш полученного установщика, версию/архитектуру ОС, тип пользователя, модель/драйвер/подключение принтера, номер задания, наблюдения очереди и бумаги. Использовать тестовый склад и синтетические данные. Значения доступа не включать в доказательства. Чистый ПК означает отсутствие Python, инструментов разработчика и ранее установленной программы WMS Print; драйвер принтера можно установить штатно как явную подготовку. Виртуальная Windows пригодна для проверки установки, но C25 требует физического принтера.

| Проверка | Требования | Подготовка → действие → ожидаемый результат → повторная проверка | Вердикт |
| --- | --- | --- | --- |
| C19 | R9,R12 | На чистых Windows 11 x64 и Windows 10 22H2 x64 открыть пользовательскую ссылку загрузки → сверить SHA256 и установить → пакет запускается без Python/WSL/PDF-приложения. Записать реальные запросы Windows о доверии/правах и инструкцию; защиту ОС не отключать. Установщик и запись удаления доступны из обычного профиля. | **Не проверено на чистых ОС — блокирует приёмку.** Windows x64 установщик создан и скачан из run34775859323; SHA256 совпал. CI выполнялся на windows-2022, а не на чистых Windows 10 22H2/11. Установка, системные запросы доверия и запись удаления пользователем не проверены. |
| C20 | R12,R13,R17 | На каждой ОС матрицы выбрать реальную очередь с пробелами/кириллицей, ввести тестовый сервер, подтвердить склад в существующем диалоге → повторно открыть программу и WMS → тот же connection_id/склад/очередь без второго кода. Другой обычный пользователь ОС не читает конфигурацию; ежедневный запуск не требует повышения прав. | **Не проверено — блокирует приёмку.** Обе финальные сборки существуют; полный проход реальной очереди/подключения/повторного открытия и проверка доступа вторым пользователем ОС не выполнены. |
| C21 | R14 | После первого подключения закрыть окно настройки и браузер, заблокировать экран → отправить с ТСД задание и проверить передачу → перезагрузить ПК и войти рабочим пользователем, не открывая WMS Print → ещё одно задание передано той же очереди. Повторить на Windows и macOS. | **Не проверено в настоящем пользовательском сеансе — блокирует приёмку.** По текущему коду autostart обязателен; register/start предусмотрены для Task Scheduler и LaunchAgent. Отсутствие Windows и опциональность старого macOS-кода устранены. Закрытие окна, блокировка и перезагрузка с настоящей печатью на обеих ОС не проверены. |
| C22 | R7,R14,R16 | В тестовой среде аварийно завершить фоновый процесс до получения задания → системный запуск восстанавливает один экземпляр без настройки → следующий job исполняется один раз. Одновременно запустить второй экземпляр: одной передаче соответствует одна квитанция. Отдельно воспроизвести отказ регистрации автозапуска: нет ложного подтверждения настройки. | **Не проверено полностью — блокирует приёмку.** Нативная Windows-блокировка и контролируемый запуск реализованы; CI и адресное техническое review PASS. Настоящий crash → system restart, двойной запуск установленного пакета и отказ регистрации ОС не предъявлены. |
| C23 | R8,R16,R19 | Отключить сеть/усыпить настроенный ПК, создать job на ТСД → ожидание, не успех → вернуть сеть/разбудить ПК → связь сама возвращается, один job у прежней очереди. Выключить ПК и затем включить/войти: тот же результат без повторной настройки. | **Не проверено — блокирует приёмку.** Требуется пакет на каждой заявленной ОС. |
| C24 | R5,R8,R16 | Тестовая очередь принимает файл; потерять ответ WMS и перезапустить программу → повторена только квитанция. Отдельно завершить процесс после возможного приёма ОС до сохранения квитанции → неизвестный итог без второго spool, следующий job обрабатывается. Сверить очередь, сервер и ТСД. | **Не проверено — блокирует приёмку.** Прежние локальные тесты не подтверждают реальную Windows-очередь и перезапуск пакета. |
| C25 | R1,R2,R4,R8,R12,R15 | На физическом Windows-ПК с принтером склада из сортировки ТСД напечатать товар и ячейку, 1 и 2 копии, PDF и применимый FBS PNG → измерить формат, пересчитать листы, считать коды тем же ТСД, сверить выбранную очередь/квитанцию. Смена принтера ОС по умолчанию не меняет назначенный. Отдельно предъявить бумагу с macOS, если заявляется её поддержка. | **Физический сценарий не проверен — блокирует приёмку.** На e37d2192 Astra технически закрыл остаток F5: подтверждённые драйвером Copies=1/DM_COPIES проверяются до CreateDC; отказ согласования не достигает StartDoc/успешной квитанции. Три целевых теста и десять независимых сценариев с подменёнными Windows API прошли, CI run34775859323 Windows/macOS — SUCCESS на точном SHA. Это не проверка бумаги: модель/реальный драйвер/подключение, число физических листов и считывание отпечатков не предъявлены. См. [финальное техническое review](../reviews/wms442-astra-windows-final-review.md). |
| C26 | R3,R8,R15,R16 | Выключить принтер/убрать бумагу после приёма ОС → WMS сообщает только передачу, не выход бумаги; восстановить принтер → очередь продолжает свой job без новой отправки агента. Затем удалить/переименовать назначенную очередь до job → конкретный отказ без выбора другого принтера, сортировка доступна. | **Не проверено — блокирует приёмку.** Нужны фактическая очередь и оборудование. |
| C27 | R13,R17,R18 | Установить предыдущую предъявленную версию, подключить склад, создать ожидающую квитанцию → установить новую версию → те же настройки/квитанция, один процесс, новая версия и прежний автозапуск. Вызвать отказ обновления до замены → прежняя версия запускается, состояние сохранено, повторного spool нет. | **Не проверено на установленном пакете — блокирует приёмку.** Windows NSIS содержит проверку остановки, сохранение previous и rollback при отказе; старое утверждение об отсутствии процедуры больше не действует. Реальный update с незавершённой квитанцией и вызванным отказом копирования/запуска не выполнен. |
| C28 | R18,R19 | Удалить программу штатно через Windows → процесс/файлы/автозапуск убраны, после нового входа процесс не появился; сервер показывает отсутствие связи без удаления jobs. Переустановить → сохранённая настройка восстанавливается. Отдельно выполнить явный сброс в тестовом профиле → повторное подключение ожидаемо, старые jobs не переназначены. | **Не проверено на установленном пакете — блокирует приёмку.** Windows-установщик и штатное удаление реализованы, исходный stop/reset дефект технически закрыт. Реальный uninstall → login → reinstall, сохранённое подключение и явный reset не пройдены. |
| C29 | R6,R13,R19 | Два тестовых склада и принтера → обновить/перезапустить один агент без изменения назначения → новые коды не нужны. Явно сменить принтер/склад/сервер через переподключение → preview и подтверждение относятся к выбранному объекту, старые jobs остаются адресованы прежнему connection_id, другой склад неизменён. | **Полный сценарий не проверен — блокирует приёмку.** На 35fe76fb независимое Astra-review технически закрыло остаток F6: до подтверждённого pairing старое подключение сохраняется при отмене/ошибке; start вызывается после освобождения блокировки, успешный pairing сохраняет новое подключение. Подтверждено пятью целевыми тестами и девятью независимыми локальными сценариями с настоящим временным файлом/POSIX lock и подменёнными HTTP/планировщиком. Windows/macOS CI run34775465101 — SUCCESS на точном SHA. Реальный пакетный проход с двумя складами, очередями и внешними назначениями не выполнен. F6 не менялся и не перепроверялся при закрытии F5 на e37d2192; технический review теперь PASS, полный C29 остаётся открытым. См. [узкое review](../reviews/wms442-astra-windows-final-review.md). |
| C30 | R9,R12,R14,R18 | Из пользовательского места загрузки получить финальные Windows и macOS артефакты, проверить их SHA/архитектуру и воспроизводимый source_commit → пройти C19–C29 применимо к каждой ОС; macOS — закрытие окна, вход, обновление, удаление штатной инструкцией. При отсутствии хотя бы Windows-артефакта, автозапуска или физического C25 выпуск не отмечать готовым. | **Скачивание текущего public release подтверждено; выпуск целиком не принят.** Windows/macOS из wms-print-v2026.09.13.1 скачаны по ссылкам UI и анонимно; отдельные/общие SHA256 совпали. macOS build.json=6c23c2bb, arm64. Windows payload отдельно не извлечён. Чистая установка/фон/обновление/удаление и физический C25 не пройдены; workflow требует исправления F9/F10, см. [review](../reviews/wms442-astra-public-release-review.md). |

### Сверка дополнения и передача разработчику

Windows/macOS → R9/R12/C19/C30; «каждый раз настраивать?» → R13/R14/R16/R18/R19/C20–C24/C27–C29; печать на ПК → R15/C25/C26. Права и отсутствие дубля происходят из R5–R8, не из новых складских ограничений. Установщик, обновление и удаление — явно обозначенные решения БА для одной установки с сохранением результата. Автообновление, системная служба до входа, новые экраны и новый серверный механизм не добавлены.

Terra high получает этот документ и исходные правила проекта: реализовать Windows-адаптер/пакет, переносимые хранение и блокировку, обязательный фон/автозапуск на Windows и macOS, установку/обновление/удаление и точную инструкцию; сохранить текущий веб-дизайн и серверную очередь. Исполнитель не меняет требования под доступное оборудование. Отдельный commit/push с SHA и фактические артефакты передать Astra на независимое ревью, затем вернуть этому аналитику на приёмку. Чужую работу, WMS-433/«Чат» и соседние задачи не менять.

### Заключение по Windows и постоянной работе

**WMS-442 не принята; есть подтверждённые несоответствия реализации.** Windows отсутствует, имеющийся macOS автозапуск опционален, постоянная работа сразу после настройки не обеспечена предъявленным контрактом. Прежний вывод «дефектов нет, Terra handoff не требуется» отменён этим уточнением. Передача постановки разработчику разрешена; выпуск программы и приёмка не объявляются. Помимо исправления кода остаются все незакрытые C1–C18 и новые C19–C30. Протокол установки и физическая печать нужны на фактически заявленных платформах; прохождение синтетической очереди не заменяет бумагу.

### Последующее техническое заключение Astra, 13.09.2026

На `e37d21925a4fc614a4843ecf3c95daee0b98e1af` независимое [Windows-review](../reviews/wms442-astra-windows-final-review.md) получило **технический PASS**: последний остаток F5 закрыт согласованием DEVMODE с драйвером и проверкой Copies=1/DM_COPIES до создания контекста печати. Прежнее закрытие F6 сохраняется; повторного прогона F6 не было. CI run34775859323 завершён SUCCESS для Windows и macOS на этом SHA. Формулировки выше об отсутствии Windows относятся к исходной реализации до последующих исправлений; фактические установки, оборудование и прежние незакрытые пользовательские проверки из них автоматически не становятся принятыми.

Это заключение технического ревьюера, не новая приёмка аналитиком. **WMS-442 в целом не принята; физическая C25 и остальные незавершённые обязательные проверки остаются открытыми.** Результат передаётся аналитику с указанными доказательствами и границами; выпуск и деплой не объявлены.

### Передача после приёмочного дефекта C17, 13.09.2026

Ведущий поручил зафиксировать текущий результат и вернуть C17 тому же Terra. Следующая приёмка продолжится после его исправления и отдельного адресного review; нельзя объявлять её завершённой этим документом. Требования не изменялись. Актуальные программные/внешние границы и минимальный аппаратный сценарий находятся в приёмочном отчёте.

### Адресная переприёмка C17, 13.09.2026

На ec07018f19418094077ffc423830608f3fb241da Astra повторил исходный Chrome-сценарий: прежняя ошибка очищается после успешного GET; повторный GET failure обрабатывается, pageErrors=[], старое назначение/online-утверждение убрано. C17-дефект закрыт в проверенном компонентном объёме. Предыдущее заключение о возврате этого дефекта историческое; общая WMS-442 ещё не принята из-за C14/C30, незавершённых полных пользовательских и внешних сценариев. Продолжение приёмки не объявляется оконченным этим checkpoint.

### Review и доступная приёмка публичных пакетов, 13.09.2026

На 6c23c2bb публичная часть C14/C30 подтверждена: четыре UI-ссылки и шесть анонимных downloads работают, хеши совпали. Старое утверждение об отсутствии ссылки относится к прежнему коду. [Astra-review](../reviews/wms442-astra-public-release-review.md) требует F9/F10: восстановить прежний branch-push CI и безопасный повтор publish без удаления существующих assets до успешного результата. WMS-442 в целом не принята; исходные R1–R19 не менялись. Следующий шаг — возврат этих двух технических дефектов новой Terra, затем адресная перепроверка и продолжение доступных пользовательских сценариев.

## Чек-лист единого финального допуска, 13.09.2026

| Проверка | Вердикт |
|---|---|
| FINAL-REVIEW — код, границы и ранее исправленные дефекты | READY. Подтверждённых открытых P1/P2 нет; [единый отчёт](../reviews/tsd-package-final-astra-20260913.md). |
| FINAL-ACCEPTANCE — фактически подтверждённое покрытие | C17 и Windows copies исправлены; F9/F10 закрыты в финальном workflow/publisher. Публичное скачивание подтверждено ранее. Физическая C25 и составные неполные UI-проверки остаются открытыми. |
| FINAL-STAGING — допуск к выкладке | DEPLOYABLE TO STAGING. Выкладка и фактически обслуживаемая версия этим документом не подтверждены. |
