# WMS-476 — Автоматически распознавать товар при загрузке PDF с КИЗ

**Актуальный контракт от 23.09.2026. Макет утверждён владельцем; этап B разрешён.**
Канонический образец: `prototypes/wms476-auto-import` в коммите
`2b0380f991098e05b1ad942ac01538b3c49a54ad`, дерево каталога
`3ecf72943227e1d2d9b72addf488f1fad443a9fb`. Локальный просмотр:
`http://127.0.0.1:5476/`. Именно сохранённый Git-образец, а не изменяемый процесс
на порту, определяет утверждённый интерфейс и переходы. Раздел 8 ниже фиксирует
все поздние уточнения владельца и имеет приоритет над историческими формулировками
22.09.2026. Дополнительные изменения интерфейса, поведения и соседних частей
системы сверх этого контракта строго запрещены.

Задача появилась 19.09.2026; постановка аналитика обновлена по уточнениям владельца
22.09.2026. Рабочая копия:
`/Users/deniscivkunov/Projects/WMS/.worktrees/wms476-auto-import`, ветка
`codex/wms476-auto-import` от `origin/etalon`, исходный HEAD на момент постановки —
`74909f97cf5dea7d8a822a7eeea2d25fc6bbd526`.

Запись задачи: [канонический бэклог, WMS-476](../KANONICHESKIY_BACKLOG.md#wms-476).
В `origin/etalon` на момент начала этой работы запись отсутствовала, но задача не
является новой: существующая запись WMS-476 найдена в основном checkout на ветке
`fix/wms392-kiz-scan-unlock` и восстановлена оттуда в актуальный канонический
бэклог. Новая постановка ниже уточняет прежнюю, а не создаёт вторую задачу.

## 1. Исходный запрос и уточнения

### Предыстория 19.09.2026

Первоначальная формулировка владельца в Claude, дословно:

> провертяь ЧЗ по артикулу сразу при загрузке — выдавать ошибку если киз привязан не к тому чз

Уточнение владельца вечером того же дня, дословно:

> задачу вносил про то чтобы автоматически распознавать чз по артикулам? при загрузке автоматом — если есть то раскидываем сами если нет то нет

В сохранённой записи также зафиксирован наблюдавшийся 19.09 пример AVpack: GTIN из
КИЗ не совпадал с сохранённым WB-штрихкодом товара, зато на PDF-этикетке был
напечатан артикул, совпадавший с `sku_code` каталога. Это объясняет, почему задача
с самого начала требовала читать артикул с этикетки, а не полагаться только на
GTIN. Прежние аналитические предположения о том, должен ли ненайденный код остаться
в непривязанном пуле, не переносятся в требования: владелец 22.09 явно заменил их
сценарием частичного успеха и отдельного PDF с неподгруженными этикетками.

### Полное уточнение 22.09.2026

Владелец 22.09.2026 в Codex, дословно (слова-паразиты и разговорная форма сохранены):

> Заведи фичу в бэклог и сразу начни её делать. Точнее, она уже есть в бэклоге, найди её. Короче, тема какая. Когда мы загружаем кизы, должна быть возможность просто загрузить их без товара. И тогда будет, ну, то есть не выбирая галочками товары. Тогда система скажет, что кизы будут распознаны, ну, типа автоматически. Вот, соответственно, в чём прикол? Нам нужно попробовать искать артикул и размер, сопоставление. Если он есть на самом кизе, как-то его считывать и, соответственно, сверять уже, сверять его с теми товарами, которые в базе есть, в каталоге. Соответственно, если такой товар есть, то мы, ну, как бы туда его подгружаем. Если товар не найден, то говорим, что, ну, типа вот такие-то кизы там не распознались, типа того. Происходить это всё должно в той же самой модалке, в которую мы сейчас это всё подгружаем, да. Но просто если делаем это в автоматическом режиме, тогда она должна поменяться. То есть там будут как бы показаны артикулы, которые мы нашли: артикул, размер, да, там, шК, и типа сколько кизов мы к ним подгрузили, если они подгрузились. И внизу дальше будет также типа раздел Без артикула или Не найдено, типа Не подгружено, да, условно. Вот. И напротив Не подгружено будет кнопка типа этот... Сейчас. Да, короче, будет кнопка просто скачать этот PDF чисто с неподгруженными кизами, чтобы их потом можно было уже там точно к какому-то товару загрузить. Вот. Соответственно, если что-то не подгружено этим, не стопорим всю загрузку, загружаем отдельно. Но вот, напиши, как понял задачу.

Уточнение владельца 22.09.2026, дословно:

> Вот смотри ещё раз. Не рядом он появляется, а просто если я не выбираю никакие товары, точнее, если я не выбираю никакие товары, у меня горит кнопка внизу распознать автоматически. Если я какие-то товары выбираю, то она гаснет, да, и уже, ну, наш стандартный типа выбор товаров появляется. Какая-то такая тема.

Критичное уточнение владельца, переданное ведущим агентом во время постановки:

> Продуктовый фронт, API и данные пока вообще не менять. Первым артефактом должен быть отдельный полностью кликабельный React-макет, не подключённый к приложению. После него остановиться и дождаться утверждения владельца.

## 2. Существующий процесс и цель изменения

### 2.1. Наблюдаемое сейчас

Текущий экран открыт 22.09.2026 в авторизованном браузере на
`/app/ff/honest-sign`. Кнопка «Загрузить КМ» открывает модалку «Загрузить КМ» поверх
раздела «Честный знак». На пустом состоянии в ней есть одна зона выбора CSV, TXT или
PDF, «Отмена» и неактивная кнопка «Загрузить». Реальные файлы и данные при этом
визуальном осмотре не загружались и не изменялись.

После выбора файлов `MarkingImportDialog.tsx` сам вызывает
`POST /operations/marking-codes/import/preview`. Сервер извлекает из PDF каждый
DataMatrix, сохраняет его полный КИЗ и отдельный PDF-фрагмент этикетки, берёт GTIN
из КИЗ и группирует результат по GTIN. Для CSV сервер уже умеет читать столбцы
`Артикул`/`sku` и `Штрихкод`/`gtin`; из PDF он сейчас возвращает пустой `sku` и не
извлекает размер или артикул из видимого текста этикетки.

Для каждой GTIN-группы текущая модалка показывает название пула, поиск по каталогу
и таблицу товаров с галочками. Таблица использует каталог выбранного селлера и
показывает только товары с действующим признаком «Нужен Честный знак при упаковке».
Кнопка «Загрузить» отправляет выбранные `product_ids` в существующий импорт. Сейчас
она допускает загрузку и без выбранного товара: тогда КИЗ попадают в пул без
привязки, а на основном экране такой пул предлагается привязать позднее.

В серверном сервисе уже существует неиспользуемая функция
`_resolve_product_for_row`: она ищет товар выбранного селлера сначала по точному
`sku_code`, затем по `wb_barcode`, включая соответствие 13- и 14-значного вариантов
GTIN. Текущие preview/import эту функцию не вызывают. Это техническая заготовка,
но не готовое выполнение запроса: PDF не даёт ей артикул/размер, результат не
разделён на найденные и ненайденные этикетки, PDF ненайденных не формируется.

### 2.2. Цель

Оператор остаётся в существующей модалке загрузки КИЗ. Если он не выбрал ни одного
товара, нижнее основное действие предлагает автоматическое распознавание. Система
читает доступные на PDF-этикетке идентификаторы, сопоставляет каждую этикетку с
каталогом выбранного селлера и загружает все однозначно найденные КИЗ к найденным
товарам. Ненайденные этикетки не отменяют успешную часть: оператор видит их
отдельным итогом и скачивает отдельный PDF только с ними для последующей ручной
загрузки.

Если оператор поставил хотя бы одну существующую галочку товара, автоматический
режим гаснет и остаётся нынешний ручной сценарий загрузки. Снятие всех галочек
сразу возвращает автоматическое действие.

## 3. Этапы и обязательный продуктовый gate

### Этап A — только кликабельный макет

Первый разрешённый на 22.09.2026 артефакт реализации — отдельный
полностью кликабельный React-макет на синтетических данных. Он показывает все
состояния из требований ниже, но не подключается к маршрутам приложения, не
вызывает настоящий API и ничего не читает и не пишет в рабочую базу.

Остановка до утверждения выполнена. После исправлений владелец 23.09.2026
явно подтвердил: «да, макет супер», потребовал закрепить его как строго
канонический и поручил передать задачу изолированному Sol high в разработку.

### Этап B — продуктовая реализация

Продуктовая реализация разрешена владельцем 23.09.2026 только в пределах
утверждённого макета и настоящих требований. Разработчик — изолированный
`gpt-5.6-sol`, reasoning effort `high`, без наследования истории переписки.
Рабочий каталог: `/Users/deniscivkunov/Projects/WMS/.worktrees/wms476-implementation-sol-high`;
ветка: `codex/wms476-implementation-sol-high`. Явное указание модели владельцем
действует и для необходимых изменений фронта. Ведущий контролирует соответствие,
возвращает дефекты исполнителю и организует проверку реализации. Merge и деплой
этим поручением не разрешены. Продуктовая приёмка пока не выполнена.

## 4. Требования

| ID | Условие, действие и наблюдаемый результат | Основание |
|---|---|---|
| R1 | В существующей модалке «Загрузить КМ» после разбора файлов и при **нуле выбранных товаров во всех группах** нижнее основное действие называется «Распознать автоматически» и доступно, если файл успешно разобран. Отдельный переключатель, вторая соседняя кнопка автоматического режима или новый экран не появляются. | Уточнение владельца: «не рядом», «если я не выбираю никакие товары… горит кнопка внизу распознать автоматически»; правило сохранять контекст действия. |
| R2 | Как только оператор выбирает хотя бы один товар любой существующей галочкой, «Распознать автоматически» гаснет, а нижнее действие работает как нынешняя ручная кнопка «Загрузить» с выбранными товарами. Автоматическое и ручное сопоставление не смешиваются в одном нажатии. Если оператор снимает все галочки, снова доступно «Распознать автоматически», а ручное действие гаснет. | Дословное уточнение владельца; решение аналитика о взаимно исключающих нижних действиях без нового режима. |
| R3 | Нажатие «Распознать автоматически» запускает автоматический разбор именно загруженных этикеток. Для каждой PDF-этикетки система использует GTIN/ШК из DataMatrix и пробует извлечь из видимого текста артикул и размер, когда эти сведения на этикетке есть. Отсутствующее на этикетке поле не выдумывается. | Исходный запрос: «искать артикул и размер», «если он есть на самом кизе, как-то его считывать»; сохранность исходных данных. |
| R4 | Сопоставление выполняется только с тем seller-scoped каталогом, который уже обслуживает ручной выбор в этой модалке. Допустимый товар принадлежит текущим tenant и seller и имеет действующий признак `requires_honest_sign = true`, как в ручном picker; автоматический импорт не включает этот признак сам и не рассматривает товар с `false`. Этикетка считается найденной, только если доступная комбинация GTIN/ШК, артикула и размера однозначно указывает на один допустимый товар. При отсутствии совпадения, противоречии полей или нескольких кандидатах система не выбирает товар произвольно, а относит этикетку к «Не подгружено». Товары другого селлера или tenant не рассматриваются и не раскрываются. | Исходный запрос про сверку с каталогом и «если такой товар есть»; действующие границы селлера/tenant; фактическая граница текущего ручного picker; решение аналитика против случайной неверной привязки. |
| R5 | Все однозначно найденные КИЗ загружаются к соответствующим товарам, даже если часть других этикеток не распознана. Нераспознанная этикетка не создаёт КИЗ без товара и не попадает в пул без привязки в рамках автоматического действия. Один неуспех сопоставления не откатывает успешные группы. | Исходный запрос: «если что-то не подгружено… не стопорим всю загрузку, загружаем отдельно» и PDF для последующей ручной загрузки. |
| R6 | После автоматического действия эта же модалка остаётся открытой и переходит в итоговое состояние. Успешные результаты сгруппированы по найденному товару; для каждой строки видны «Артикул», «Размер», «ШК» и фактическое число КИЗ, загруженных к этому товару. Не показывается обещанное количество вместо подтверждённого результата импорта. | Исходный запрос про ту же модалку и набор полей: артикул, размер, ШК, сколько КИЗ подгрузили «если они подгрузились». |
| R7 | Ниже успешных строк находится отдельный блок «Не подгружено» с количеством ненайденных этикеток. При количестве больше нуля рядом доступно действие «Скачать PDF»: оно отдаёт один PDF только с исходными этикетками этой группы, без успешно загруженных КИЗ, в исходном порядке. PDF сохраняет сами исходные этикетки/КИЗ, а не генерирует заменяющие коды. Если ненайденных нет, блок показывает ноль и не предлагает пустой файл. | Исходный запрос про нижний раздел и отдельный PDF «чисто с неподгруженными кизами» для ручной загрузки; правило сохранять исходные данные. |
| R8 | Скачанный PDF можно затем выбрать в той же существующей модалке и пройти обычный ручной сценарий: выбрать нужный товар галочкой и нажать стандартное действие загрузки. Автоматическая попытка не ставит на эти КИЗ новый пользовательский запрет и не делает их непригодными для ручного продолжения. | Исходный запрос: «чтобы их потом можно было уже там точно к какому-то товару загрузить»; правило не добавлять непрошенные блокировки. |
| R9 | Существующий ручной сценарий не меняет свой смысл: выбор товара включает стандартную загрузку, сохраняются текущие поиск, галочки и действующие правила импорта; отображение названий и групп — строго по финальному каноническому макету. Автоматический режим не появляется отдельным действием на основном экране и не меняет соседние пулы, печать, FBS или приёмку. | Уточнение владельца про «наш стандартный выбор товаров»; граница исходного запроса. |
| R10 | Дубликаты и невалидные КИЗ не выдаются за «товар не найден»: оператор получает существующую либо столь же конкретную причину пропуска. Повтор доставки одного автоматического запроса или два одновременных вызова не создают второй экземпляр того же КИЗ, повторную привязку или расход; потеря ответа после сохранения выясняется по уже сохранённому результату, а не запускает слепую повторную загрузку. После ошибки оператор может продолжить с тем же файлом либо выбрать другой. | Действующие правила импорта и проекта о повторах, конкуренции, восстановлении и понятных ошибках; библиотека B02/B03/B05/B09. |
| R11 | В итоговом состоянии при техническом сбое явно различимы: подтверждённо загруженные КИЗ, ненайденные товары и КИЗ с технически неизвестным результатом. Неизвестный результат не включается ни в подтверждённое число загрузки, ни в скачиваемый PDF как гарантированно «не подгруженный», пока система не выяснила состояние. | Правило проекта не повторять внешнее/записывающее действие при неизвестном результате; исходный запрос требует фактическое количество «если подгрузились». |
| R12 | Макет этапа A кликабельно демонстрирует как минимум: пустое состояние; разобранный PDF без выбранных товаров и активное «Распознать автоматически»; выбор одной/нескольких галочек и переход к ручному «Загрузить»; снятие всех галочек и возврат автоматического действия; процесс распознавания; частичный успех с успешными строками и «Не подгружено»; полный успех; техническую ошибку с возможностью продолжить. Действие «Скачать PDF» в макете выполняет настоящее браузерное скачивание валидного синтетического PDF с тестовыми неподгруженными этикетками; файл открывается как PDF и не требует API или рабочих данных. | Критичное уточнение владельца о полностью кликабельном отдельном React-макете до продуктового кода; исходный сценарий включает скачивание PDF; хороший случай библиотеки про макет, который можно попробовать. |
| R13 | Макет этапа A визуально повторяет текущую модалку и существующую тему WMS: действие остаётся внизу модалки, результаты читаемы при длинном артикуле, размере и ШК, кнопки не накладываются. Макет не добавляет новые окна, чипы, подсказки или сущности сверх перечисленных состояний. | AGENTS.md о сохранении дизайна, длинных значениях и контексте действия; визуальный осмотр текущей модалки 22.09.2026. |

## 5. Продуктовые решения аналитика

1. **Не вводить переключатель режима.** Режим определяется единственным уже
   наблюдаемым фактом — выбраны ли товары. Это ровно повторяет уточнение владельца
   и не создаёт новую настройку, которую оператор может забыть переключить.
2. **Внизу один актуальный основной путь.** При нуле галочек активен текст
   «Распознать автоматически»; при любой галочке автоматический путь гаснет и
   актуальным становится нынешнее ручное «Загрузить». Снятие всех галочек
   обратимо возвращает автоматический путь. Точная анимация или оттенок не является
   отдельным бизнес-правилом и должна быть показана в макете.
3. **Не угадывать при неоднозначности.** Неверная привязка КИЗ к похожему товару
   хуже дополнительного ручного шага. Достоверные поля не должны указывать на
   разных кандидатов; несколько кандидатов идут в «Не подгружено». Различие GTIN
   ЧЗ и WB-штрихкода само по себе не является конфликтом: см. R24 и C25.
4. **Автоматический итог остаётся в текущей модалке.** Иначе оператор не увидит
   требуемое разделение успешных и ненайденных КИЗ и потеряет контекст исходного
   файла. Ручной сценарий остаётся текущим, включая его обычное завершение.
5. **Ненайденные КИЗ не сохраняются как безымянный запас.** В автоматическом
   сценарии их рабочий носитель — скачиваемый PDF для последующей осознанной ручной
   привязки. Это выполняет прямой запрос и не дублирует их одновременно в пуле без
   товара и в файле.
6. **Сначала макет, затем утверждение.** Условие выполнено 23.09.2026;
   реализация допускается только по закреплённому каноническому образцу.

## 6. Проверки для приёмки

### 6.1. Gate макета — выполняется первым

| Проверка | Требования | Подготовка и действие | Ожидаемый результат | Вердикт |
|---|---|---|---|---|
| C1 | R12, R13 | Открыть отдельный React-макет напрямую, не через маршрут WMS; пройти пустое состояние и выбрать синтетический PDF. | Макет полностью кликабелен, визуально узнаваем как текущая модалка, не вызывает рабочий API и не меняет данные. | **Подтверждено для этапа A.** В браузере пройдено пустое окно → выбор тестового файла → preview с двумя GTIN-группами, названием пула, поиском и галочками. По исходникам `prototypes/wms476-auto-import` отсутствуют `fetch`, XHR, WebSocket, внешние URL и local/session storage; данные синтетические. |
| C2 | R1, R2, R12 | На разобранном файле не выбирать товары → выбрать одну галочку → вторую → снять одну → снять последнюю. | При нуле галочек доступно только актуальное автоматическое действие; при одной или нескольких галочках оно гаснет и доступен стандартный ручной путь; снятие последней возвращает автоматический путь без перезагрузки. | **Подтверждено для этапа A кликами.** При нуле галочек показано «Распознать автоматически»; выбор товара в группе заменяет действие на «Загрузить»; после снятия последней галочки автоматическое действие возвращается. Отдельного переключателя или одновременных основных действий нет. |
| C3 | R6, R7, R12, R13 | В макете запустить частичный успех: несколько найденных товаров, длинный артикул, разные размеры/ШК и несколько ненайденных этикеток; нажать «Скачать PDF», дождаться настоящего браузерного скачивания и открыть полученный файл локальным PDF-парсером или просмотрщиком. | В той же модалке читаемы строки «Артикул / Размер / ШК / Загружено КИЗ», ниже отдельное «Не подгружено: N» и одна кнопка PDF; успешные и ненайденные количества не смешаны. Скачан непустой валидный PDF с синтетическими неподгруженными этикетками, без обращения к API и рабочим данным. | **Подтверждено для этапа A.** Частичный сценарий пройден кликами. После исправления заголовка браузер отдельно скачал `WMS-476-nepodgruzhennye-kizy (2).pdf`: `pdfinfo` — PDF 1.4, 4 страницы A4, 19 540 байт, файл не зашифрован и не содержит JavaScript. Первая post-fix страница отрендерена и визуально просмотрена: двухстрочный заголовок `NEPODGRUZHENNAYA` / `ETIKETKA` имеет достаточный правый отступ; DataMatrix-подобная тестовая графика, GTIN, артикул, размер, номер листа, КИЗ и предупреждение «не сканировать» читаемы. Успешные и ненайденные количества в модалке показаны отдельно; PDF создан на синтетических данных без обращения к API. |
| C4 | R6, R7, R12 | Переключить макет на полный успех, затем на техническую ошибку и восстановление. | При полном успехе нет кнопки пустого PDF; ошибка не закрывает модалку, не рисует ложный успех и позволяет продолжить. | **Подтверждено для этапа A кликами.** Пройдены частичный и полный успех, ручной сценарий, техническая ошибка и «Повторить»: повтор после ошибки приводит к частичному результату. Полный успех показывает «Не подгружено — 0 КИЗ» без кнопки PDF. Ложный успех при ошибке не показан. |
| C5 | R12, R13 | Проверить макет на ширине 1024 и 1440 px, с длинными значениями и несколькими строками; сравнить с текущей модалкой «Загрузить КМ». | Кнопки и колонки не накладываются, нижнее действие остаётся в контексте модалки, лишнего нового окна нет. | **Подтверждено для этапа A независимым ревьюером.** На viewport 1024×768 и 1440×900 проверены overlay, длинные строки, колонки, прокрутка и footer: после исправлений они не перекрываются и остаются читаемыми. Первичное Astra high review нашло четыре P2/P3; все исправлены, повторное review дало PASS, последний P3 PDF-заголовка закрыт отдельно. |
| C6 | R12 | До предъявления макета сравнить diff с исходным `origin/etalon`. | Изменён только самостоятельный код/ассеты макета и его изолированный способ запуска; продуктовые frontend-компоненты, маршруты, API-клиенты, backend, миграции и данные не изменены. | **Подтверждено для этапа A.** Diff ограничен `docs/KANONICHESKIY_BACKLOG.md`, `docs/requirements/WMS-476.md` и отдельным `prototypes/wms476-auto-import`; `frontend/` и `backend/` не изменены. Текущий `npm run build` макета прошёл (`tsc -b`, Vite, 35 модулей), `git diff --check` чистый. |
| C7 | R12, R14 | Предъявить владельцу ссылку/способ запуска макета и дождаться его реакции после кликов. | Есть явное утверждение владельца и закреплена утверждённая версия. | **Подтверждено 23.09.2026 владельцем:** «да, макет супер» и поручение закрепить его каноничность и передать в разработку. Утверждён SHA `2b0380f991098e05b1ad942ac01538b3c49a54ad`. |

### 6.2. Продуктовая приёмка — только после утверждения макета и реализации этапа B

С 23.09.2026 этап B разрешён; актуальный вердикт всех C8–C17 —
**не проверено: ожидает реализации и продуктовой приёмки**. Наблюдения о том,
что ещё не реализовано, относятся к моменту передачи в разработку.
Поздние требования R14–R25 и проверки C18–C26 дополняют и уточняют эту таблицу.

| Проверка | Требования | Подготовка и действие | Ожидаемый результат | Вердикт |
|---|---|---|---|---|
| C8 | R1, R2, R9 | В настоящей модалке загрузить тестовый PDF, пройти последовательность ноль → одна → несколько → ноль выбранных товаров, затем выполнить ручную загрузку отдельного файла. | Нижнее действие переключается по R1–R2; ручная загрузка работает как до задачи, соседние действия раздела не изменены. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Настоящая продуктовая модалка не изменялась. |
| C9 | R3, R4 | Подготовить для одного селлера этикетки: GTIN+артикул+размер совпадают; GTIN совпадает и текстовых полей нет; артикул/размер противоречат GTIN; два кандидата; товар другого селлера/tenant. Выполнить авто-распознавание. | Только однозначные допустимые совпадения привязаны к товарам выбранного селлера; отсутствующие поля не выдуманы, противоречие/неоднозначность/чужие товары находятся в «Не подгружено» без раскрытия чужих данных. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Сопоставление с реальным каталогом не реализовывалось. |
| C10 | R5, R6 | В одном PDF смешать этикетки для двух найденных товаров и ненайденные; выполнить автоматическое действие и проверить БД/API/экран. | Обе успешные группы сохранены и показаны с фактическими числами, ненайденные не создали коды/пулы без товара, модалка осталась открыта. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** API, БД и продуктовый экран не изменялись. |
| C11 | R7, R8 | Скачать PDF частичного результата; сравнить его страницы/этикетки с исходным PDF и снова загрузить этот файл вручную к выбранному товару. | В PDF только ненайденные исходные этикетки в исходном порядке; успешно загруженных нет; ручная загрузка этого PDF доступна и сохраняет КИЗ к выбранному товару. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Реальный PDF и последующая продуктовая загрузка не проверялись. |
| C12 | R7 | Выполнить полный автоматический успех без ненайденных этикеток. | Блок показывает «Не подгружено: 0» без активной загрузки пустого PDF; все строки отражают подтверждённый импорт. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Реальный импорт не реализовывался. |
| C13 | R10 | В одном файле дать повторяющийся КИЗ, уже загруженный КИЗ и невалидную этикетку; после ошибки выбрать другой валидный файл. | Причины не смешаны с «товар не найден», дубликат не создаётся, после ошибки модалка продолжает работу с другим файлом. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Продуктовая обработка ошибок и дубликатов не изменялась. |
| C14 | R10, R11 | На тестовой среде оборвать ответ до сохранения и отдельно после сохранения; повторить тот же запрос и дважды запустить его одновременно. | До сохранения можно безопасно повторить; после сохранения система находит фактический результат; каждый КИЗ сохранён не более одного раза, неизвестное не названо успехом или гарантированным «не подгружено». | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Серверное сохранение, повтор и конкуренция не реализовывались. |
| C15 | R4, R10 | Повторить C9–C14 для второго селлера в том же tenant и пользователя без права на выбранного селлера. | Каталоги и результаты не смешиваются; чужой доступ закрыт понятной ошибкой, данные первого селлера не изменены и не раскрыты. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Реальные права и границы селлеров не затрагивались. |
| C16 | R5, R9 | Сравнить до/после существующие пулы без привязки, печать ЧЗ, FBS и приёмку; автоматический импорт выполнить только на тестовых данных. | Изменены только найденные КИЗ и их требуемая привязка; соседние процессы и прежние пулы не получают нового поведения или блокировки. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Продуктовые процессы не изменялись. |
| C17 | R6, R7, R13 | В настоящем браузере проверить частичный и полный результат на 1024/1440 px, длинные артикулы/размеры/ШК и большой реальный набор этикеток. | Строки и количества читаемы, модалка прокручивается, нижние действия доступны и не накладываются; PDF скачивается один раз на клик. | **Не проверено — этап B разрешён, ожидает реализации и приёмки.** Настоящий продуктовый экран и большой реальный набор не проверялись. |

## 7. Сверка постановки

Исходный запрос покрыт: загрузка без выбора товара и автоматическое распознавание —
R1–R5; чтение артикула/размера и сверка с каталогом — R3–R4; успешные строки с
артикулом, размером, ШК и количеством — R6; отдельное «Не подгружено» и PDF —
R7–R8; частичный успех без остановки — R5–R7. Уточнение о взаимном исключении
автоматического и ручного действия покрыто R1–R2 и C2/C8. Требование о первой
отдельной кликабельной версии без изменения продукта покрыто разделом 3, R12–R13
и C1–C7.

В обратную сторону: R9 сохраняет только действующий соседний процесс; R10–R11
добавляют обязательные технические защиты проекта, но не новый пользовательский
запрет; R12–R13 фиксируют прямой gate владельца. Новые статусы, экраны, режимы,
таблицы или ограничения площадки не введены.

## 8. Полный контракт после уточнений и утверждения владельца 23.09.2026

### 8.1. Дополнительные исходные слова владельца

До продуктовой реализации владелец потребовал отдельный работающий макет:

> Строго-настрого ничего не меняй больше, ничего не ломай, не трогай фронт, блять, нихуя не делай, только точечно то, что я сказал. И ещё предварительно сначала сделай мне кликабельный макет на React, прям реальный, чтобы я мог в него потыкать, полностью, с переходами, со всеми делами, чисто собери, чтобы я его посмотрел, утвердил.

Владелец отверг непонятное разделение выбора на GTIN-блоки:

> Я что-то не понял, что у тебя за... почему там два-то раздела? Я вообще, ну, не одупляю. Что такое GTIN 4-3-2-1 и дальше GTIN 1-2-3-4? Это что за блоки? Они за что отвечают?

Затем отдельно потребовал сохранить верхнюю разбивку уже загруженных КИЗ:

> Сука, придурок, зачем ты теперь убрал та верхнюю область, блять, где была разбивка нахуй по уже после привязки по товарам, блять? Сколько, каким товарам, что подгружено, блять? У тебя же до этого нормально было.

Относительно придуманной шапки SellerFocus и любых посторонних изменений:

> это чё за хуйня
> она есть у нас сейчас на стейдже или проде? если нет то убирай нахуй её блядь
> сука строго ничего не трогай по отнршению к тому что есть сейчас кроме этих правок

> откуда вообще селлер фокус блядь? это вооще что ?

> даю право чтобы дизайн делал астра
> только строго в том же стиле, ниего не добавляя от себя

Последнее уточнение ручного распределения, дословно:

> Да, теперь уже лучше, только давай сделаем следующее. Там, где блок не подгружено, там, где коды маркировки, нужно добавить ещё столбец с размером и артикулом. То есть если мы их распознали на честном знаке, если они там есть, то мы их выводим туда. И ещё напротив каждого кнопка... Не, не так. Можно выбрать чекбоксы, мети коды, либо все, либо по одному, и будет кнопка добавить к товару. И там уже будет стандартное наше добавление раскрываться, кроме тех, которые вообще не распознаны, то есть GTIN там повреждён или что-то ещё такое. Вот. Соответственно, если мы их добавляем, да, то они из этого поля, где не загружено, это количество уходит и остаются только актуальные. Кнопка скачать PDF тоже уже, соответственно, скачает только актуальные, которые реально мы руками не распределили.

Утверждение и поручение на реализацию, дословно:

> да, макет супер
> теперь фиксируй ровно все мои требования к тому как должно работать из этого чата
> плюс фиксируй что данный макет строго канонический, и кроме него ничего менять и добавлять строго нельяза в системе
> как будет готово - отдавай изолированной сол хай в разработку, а ты мониторишь

### 8.2. Обязательные уточнения к R1–R13

| ID | Условие, действие и наблюдаемый результат | Основание |
|---|---|---|
| R14 | Утверждённый React-макет в SHA `2b0380f991098e05b1ad942ac01538b3c49a54ad` — строго канонический образец изменений модалки. Его разметку, состав блоков, стиль, подписи и переходы нельзя самовольно улучшать, упрощать, заменять или дополнять. Сам каталог прототипа при реализации неизменен. | Прямое утверждение и запрет владельца 23.09.2026. |
| R15 | В системе меняется только необходимая для этого сценария модалка загрузки и обслуживающая её логика импорта/сопоставления/остаточного PDF. Окружающий экран, навигация, название продукта, тема, соседние процессы и данные не получают дополнительных изменений. Демо-панель, синтетические товары и тестовая оболочка прототипа не являются новыми элементами системы и не переносятся в продукт. SellerFocus и иная придуманная шапка запрещены. | Последовательные требования «строго ничего не трогай… кроме этих правок» и каноничность макета. |
| R16 | До запуска импорта используется единый стандартный поиск и выбор товаров, как в финальном макете. Не возвращать два отдельных пользовательских блока GTIN и отдельные поиски/списки для них. Внутренняя техническая группировка не должна становиться дополнительным экранным блоком. R1–R2 относятся к общему выбранному набору: ноль товаров — единственное основное действие «Распознать автоматически»; любой выбор — стандартная ручная загрузка; снятие всех — возврат авто. | Возражение владельца против GTIN-блоков, исправленный и утверждённый макет. Уточняет прежние слова «во всех группах» в R1–R2. |
| R17 | После автоматического распознавания верхняя таблица уже загруженных КИЗ обязательно остаётся в той же модалке: товар/артикул, размер, штрихкод и подтверждённое количество по товару. После последующего ручного распределения она отражает суммарный результат авто и ручной привязки. Подпись не называет вручную добавленные коды автоматически распознанными. | Требование вернуть верхнюю разбивку и утверждённый финальный макет. |
| R18 | Нижняя таблица «Не подгружено» показывает для каждой оставшейся этикетки код маркировки, распознанный артикул, распознанный размер и конкретную причину. Артикул и размер берутся с этикетки, когда действительно распознаны; отсутствующее поле отображается как `—`, а не подставляется из догадки или похожего товара. | Последнее уточнение владельца и R3. |
| R19 | В «Не подгружено» есть чекбокс строки и общий чекбокс. Можно выбрать один код, несколько или все доступные. Общий выбор включает только коды, которые технически распознаны и допускают ручную загрузку; снятие выбора обратимо. Вместо отдельных кнопок у каждой строки используется общая кнопка «Добавить к товару», неактивная при пустом выборе. | Владелец отменил идею кнопки напротив каждого кода и явно выбрал чекбоксы. |
| R20 | Нечитаемый/технически повреждённый код, в том числе повреждённый GTIN, остаётся в остатке с причиной и недоступным чекбоксом; массовый выбор его пропускает. Отсутствие товара в каталоге, отсутствие размера на этикетке или неоднозначность совпадения сами по себе не запрещают ручное назначение технически валидного КИЗ. | Прямое исключение владельца «кроме тех, которые вообще не распознаны». Новых пользовательских запретов не вводить. |
| R21 | «Добавить к товару» раскрывает стандартный выбор товара внутри этой же модалки, как в каноническом макете. Выбранная пачка КИЗ назначается одному выбранному товару. Доступны существующий поиск, выбор и возврат; «Назад» сохраняет выбор КИЗ и не меняет данные. Нельзя добавлять новый способ/экран выбора товара сверх макета. | Последнее уточнение про стандартное добавление и утверждённые переходы макета. |
| R22 | Только после подтверждённого сохранения ручной привязки действительно добавленные коды удаляются из «Не подгружено», количество остатка уменьшается на их число, а верхний результат обновляется у существующего товара либо получает строку выбранного нового товара. Неподтверждённые/неуспешные коды не исчезают. При следующих назначениях работают только оставшиеся строки; уже назначенные не загружаются повторно. | «из этого поля… количество уходит и остаются только актуальные»; требование фактических количеств и действующая защита от дублей. |
| R23 | Каждое скачивание PDF использует актуальный остаток на момент действия, исключая уже автоматически или вручную распределённые этикетки. Содержит исходные оставшиеся этикетки, включая повреждённые, в исходном порядке; не заменяет реальные КИЗ синтетическими. После очередного ручного распределения повторное скачивание уменьшается соответственно. При нулевом остатке нет действия скачивания пустого PDF. | Прямое последнее уточнение владельца; R7 уточнён с первоначального результата на текущий остаток. |
| R24 | При смешанном файле система загружает однозначно найденные товары, не останавливая всё из-за ненайденных/повреждённых этикеток. Важны артикул и размер на этикетке, а не только совпадение GTIN с WB-штрихкодом. Само различие GTIN ЧЗ и marketplace-штрихкода не считается доказанным конфликтом товара и не запрещает однозначное сопоставление по артикулу/размеру. Реально противоречивое или неоднозначное соответствие не угадывается. Существующие границы селлера и доступа сохраняются. | Исходный запрос, известный пример AVpack в разделе 1 и запрет добавлять ограничения. Уточняет чрезмерно широкое «все поля должны быть согласованы» в решении 3: речь о смысле идентификаторов, а не равенстве разных систем кодирования. |
| R25 | Продуктовая реализация выполняется отдельным Sol high по этому документу и зафиксированному макету. Требования нельзя менять под получившийся код. Разрешены только технически необходимые изменения для R1–R26 и проверки; никаких дополнительных функций, рефакторинга соседних процессов или новых пользовательских сущностей. Ведущий отслеживает прогресс и отклонения. Коммиты с WMS-476 сохраняются в отдельной ветке; merge/deploy требуют отдельного поручения. | Прямое последнее поручение владельца и контракт сохранности проекта. |
| R26 | Legacy-ручка `POST /operations/marking-codes/import` не имеет права неявно создавать непривязанные КИЗ. До любых постоянных записей каждый фактически разобранный валидный GTIN файла должен разрешаться в переданную спецификацию с непустым `product_ids`, прошедшим действующие проверки tenant/seller. Если хотя бы для одного GTIN назначение отсутствует или пусто, весь запрос атомарно завершается `422`: не создаются import batch, сохранённый исходный PDF, пул, связь пула с товаром, КИЗ или событие, в том числе для остальных GTIN того же запроса. Сервер не подменяет такой legacy-вызов скрытым автоимпортом; полностью назначенный ручной импорт продолжает действовать как раньше. Канонический UI и его переходы не меняются. | Подтверждённый production-инцидент AVpack 23.09.2026: старый browser bundle отправил legacy-import с пустыми `product_ids`, а backend молча создал 9 непривязанных пулов и 108 кодов. Техническая защита от повторения инцидента старым клиентом. |

Технические защиты R10–R11 сохраняют данные и позволяют выяснить результат
после ошибки. Они не разрешают добавлять интерфейсные состояния, окна, ограничения
или обязательные действия, которых нет в утверждённом сценарии. При обнаружении
существенного противоречия исполнитель передаёт его ведущему, не меняя контракт.
Существующие форматы ручного импорта сохраняются; новый сценарий не является
поручением расширять перечень форматов или переписывать весь импорт.

### 8.3. Дополнительные проверки продуктовой приёмки

| Проверка | Требования | Подготовка и действие | Ожидаемый результат | Вердикт |
|---|---|---|---|---|
| C18 | R14–R16, R25 | Сравнить Git-дерево прототипа с закреплённым SHA, изменения продукта с базой, открытый экран и все переходы с утверждённым образцом. | Прототип неизменен; нет новых GTIN-блоков, придуманного брендинга, демо-панели или соседних изменений. Единый выбор и взаимное переключение действий повторяют макет. | Не проверено — ожидает продуктовой реализации. |
| C19 | R17–R18 | Загрузить смешанный PDF; проверить верхний результат и остаток с полными, частичными и отсутствующими метаданными. | Успешная разбивка сохранена; в остатке видны фактически прочитанные код, артикул, размер и причина; отсутствующие поля — `—`. | Не проверено — ожидает продуктовой реализации. |
| C20 | R19–R20 | Подготовить три технически валидных ненайденных/неоднозначных КИЗ и один повреждённый; выбрать один, несколько, все; снять выбор. | Выбираются только три допустимых; повреждённый недоступен; кнопка активна только при непустом выборе. | Не проверено — ожидает продуктовой реализации. |
| C21 | R21 | Выбрать пачку, открыть «Добавить к товару», найти товар, вернуться, открыть снова и подтвердить один товар. | Стандартный выбор внутри той же модалки; возврат сохраняет пачку и ничего не записывает; подтверждение использует выбранный товар. | Не проверено — ожидает продуктовой реализации. |
| C22 | R17, R22 | Назначить три оставшихся КИЗ товару с 128 уже загруженными; затем отдельно назначить оставшиеся допустимые другому товару. | Первая строка 128 → 131; остаток 4 → 1 в соответствующем наборе; новые назначения используют только актуальный остаток. Для нового товара появляется его строка, без потери остальных. Сверить API/БД и повторное открытие результата/товара. | Не проверено — ожидает продуктовой реализации. |
| C23 | R7, R23 | Скачать PDF до ручной привязки и после каждого назначения; сравнить исходные этикетки и порядок. | В каждом PDF только текущий остаток; автоматически и вручную загруженных нет. Повреждённая этикетка остаётся; при нуле пустой PDF не предлагается. | Не проверено — ожидает продуктовой реализации. |
| C24 | R10–R11, R22 | На тестовой среде вызвать ошибку ручной привязки до записи, потерю ответа после записи и повтор/два одновременных запроса. | Успешное сохранение выясняется без дубля; неподтверждённые строки не исчезают и не увеличивают успешный счётчик. Можно продолжить стандартным сценарием. | Не проверено — ожидает продуктовой реализации. |
| C25 | R3–R5, R24 | Этикетка имеет артикул/размер одного товара, а GTIN ЧЗ отличается от его WB-штрихкода; отдельно дать реально противоречивые поля, два кандидата и чужого селлера. | Первое само по себе не мешает однозначному распознаванию по артикулу/размеру; реальные неоднозначности попадают в остаток, чужие товары не раскрываются. Успешные этикетки загружаются независимо. | Не проверено — ожидает продуктовой реализации. |
| C26 | R9, R14–R16, R25 | Пройти обычную ручную загрузку, существующие допустимые форматы, соседние действия и длинные значения на 1024/1440 px; проверить целевые технические тесты и сборку. | Ручной смысл и соседние процессы сохранены; изменения только согласованные, интерфейс соответствует макету, действия доступны без перекрытий. | Не проверено — ожидает продуктовой реализации. |
| C27 | R26 | На тестовой БД отдельно отправить legacy `/import`: все `product_ids` пусты; файл содержит два GTIN, у одного назначение пусто; валидный GTIN отсутствует среди спецификаций. Перед и после каждого запроса сравнить import batch, сохранённые исходные PDF, пулы, связи пулов, КИЗ и события. Затем выполнить полностью покрытый ручной импорт. | Каждый неполный запрос получает `422` и оставляет нулевое изменение во всех перечисленных сущностях; частично допустимый GTIN также не сохраняется. Полностью назначенный ручной импорт по-прежнему создаёт только ожидаемые привязанные КИЗ. | **PASS.** На продуктовом SHA `122d704f` подтверждены `422 manual_import_product_required` для запросов с пустыми назначениями и смешанного файла без полного покрытия, отсутствие постоянных записей и сохранение прежнего полностью назначенного ручного сценария. Дополнительный test-only SHA `3952f7b4` точным тестом подтвердил смешанный случай с непустой и явно пустой спецификациями, а также неизменность before/after ID snapshots всех перечисленных сущностей на заранее заполненном seller. Тест прошёл (`1 passed`), `ruff` целевого тестового файла и `git diff --check` прошли; reviewer follow-up — PASS. |
| C28 | R4, R5, R24 | Подготовить одинаково подходящие артикул/размер у товара текущего селлера с `requires_honest_sign = true`, товара текущего селлера с `false` и товара другого селлера с `true`; отдельно оставить только `false`. Запустить auto-import, проверить БД и флаги. Затем явно включить признак у целевого товара и повторить файл с новым `request_id`; отдельно повторить случай GTIN ЧЗ ≠ WB barcode при однозначных артикуле/размере. | Рассматривается только допустимый товар текущего селлера с `true`; `false` и чужой товар не раскрываются и не получают КИЗ. При варианте только `false` строка остаётся в «Не подгружено», пул/КИЗ без товара не создаётся и флаг сам не меняется. После явного включения признака новый запрос сопоставляет товар; различие двух видов штрихкода само по себе не мешает совпадению по артикулу/размеру. | **Не проверено.** SHA `122d704f` не меняет auto matcher и не добавляет полный сценарий `true`/`false`/чужой seller с повтором после явного включения признака; принятой backend-защитой R26 эта проверка не закрывается. |
| C29 | R1–R2, R16, R26 | В целевом frontend-тесте и реальном браузере посмотреть Network: разобранный файл и ноль выбранных товаров; затем один выбранный товар. Дополнительно сымитировать старый browser bundle прямым legacy-запросом без назначения к обновлённому backend. | При нуле выбранных отправляется только `/import/auto`; при выборе — только `/import`, и каждый фактический GTIN имеет непустое назначение. Старый legacy-запрос получает `422` без записей. Разметка, подписи и переходы канонического UI не меняются. Проверка production отдельно подтверждает фактически загруженный frontend/backend, а не только локальный source. | **Не проверено полностью.** Backend-часть подтверждена тестами C27: старый legacy-запрос получает `422` до записей. Frontend в diff не менялся, но целевой frontend-тест, реальный browser Network и фактически выложенная frontend/backend-версия в этой приёмке не проверялись. |

### 8.4. Инцидентное уточнение 24.09.2026

Владелец подтвердил, что постоянная причина автоматического нераспознавания должна
быть исправлена. По фактам production-инцидента AVpack от 23.09 браузер отправил
старый `POST /operations/marking-codes/import` с пустыми `product_ids` вместо
`/import/auto`; действующий legacy backend разрешил это и создал 9 непривязанных
пулов и 108 КИЗ. Независимо от этого все 9 целевых товаров имели
`requires_honest_sign = false`, поэтому актуальный auto matcher не мог считать их
допустимыми товарами.

Данные уже восстановлены отдельно: 108 КИЗ привязаны к нужным товарам, а признак
включён у 9 товаров. Это доказательство инцидента и выполненного data repair, но
не замена постоянной защиты R26 и не поручение повторять repair в коде.

Проверка актуального `origin/etalon` показала: текущий frontend уже отправляет
ноль выбранных товаров в `/import/auto`, а ручной `/import` вызывает только при
непустом выборе. При этом backend legacy-import всё ещё допускает пустые назначения,
поэтому старый или закэшированный клиент способен повторить потерю связи. Фильтр
`requires_honest_sign = true` одинаков для auto matcher и текущего ручного picker;
он остаётся правильной границей допустимых товаров по R4, а не удаляется как
исправление. Никакого нового UI, режима или изменения канонического макета это
уточнение не разрешает.

### 8.5. Сверка с полным запросом

Авто без товара и замена нижнего действия покрыты R1–R2/R16; чтение с этикетки
и каталог — R3–R4/R24; частичный успех — R5; верхняя разбивка — R6/R17;
нижний список и метаданные — R7/R18; ручное продолжение скачанного PDF — R8;
сохранение старого ручного сценария и окружающей системы — R9/R15;
чекбоксы и запрет только для повреждённых — R19–R20; стандартное добавление — R21;
пересчёт остатка и успешной таблицы — R22; актуальный PDF — R23;
канонический образец без новых элементов — R14–R16; Sol high и контроль — R25;
атомарный запрет legacy-import без назначения — R26.
R10–R11 — технические защиты существующих данных, не дополнительные желания
владельца. R12–R13 — выполненный предварительный этап макета. Ни одна ранняя
версия макета или старое ожидание утверждения не отменяет финальный контракт.

## Заключение

**Актуально на 24.09.2026:** владелец утвердил макет на SHA
`2b0380f991098e05b1ad942ac01538b3c49a54ad` и разрешил реализацию по полному
контракту R1–R26. C7 закрыта явным утверждением. Инцидент AVpack добавил только
серверную fail-closed защиту R26 и проверки C27–C29; канонический UI неизменен.
Документ передаётся изолированному
Sol high в ветку `codex/wms476-implementation-sol-high`; канонический прототип
заморожен. Продуктовая реализация, её независимая проверка и приёмка ещё не
завершены; все продуктовые C8–C29 требуют фактической проверки. Деплой не выполнен.

### Приёмка incident guard на SHA `122d704fef9347aebe8ec8a5e5d353c1afc7070b`

Backend-часть R26 реализована без изменения канонического UI и auto matcher.
Сервис разбирает файл и проверяет покрытие каждого фактического валидного GTIN
непустым назначением до создания `MarkingCodeImport`, сохранения исходного PDF,
пула, связи, КИЗ или события. Неполный legacy-вызов получает отдельную ошибку
`manual_import_product_required`, которую API возвращает как `422`; скрытого
перехода в auto-import нет. Полностью назначенный ручной импорт продолжает
существующий путь.

Предъявленные проверки неизменённого SHA: 17 целевых backend-тестов прошли,
`ruff` изменённых файлов и `git diff --check` прошли. Независимый Astra-review —
PASS без замечаний. Повторный запуск тех же тестов, сборки и review аналитик не
выполнял.

**Итог этой узкой приёмки: backend guard R26 и проверка C27 приняты полностью.**
Test-only SHA `3952f7b4` закрыл оба прежних пробела: смешанный сценарий с непустой
и явно пустой спецификациями и before/after ID snapshots на заранее заполненном
seller; reviewer follow-up завершён PASS. Полная продуктовая приёмка WMS-476
остаётся незавершённой: C28 и браузерная/deploy-проверка C29 относятся к финальной
интеграционной проверке релиза и пока не могут быть записаны как PASS.

### Историческое техническое заключение этапа A от 22.09.2026

Этап A технически принят по C1–C6: отдельный React-макет собирается, основные
состояния и взаимное исключение ручного/автоматического действия пройдены кликами,
оба viewport проверены, независимое Astra high review после исправлений завершено
PASS, а скачанный синтетический файл является валидным четырёхстраничным PDF.
После последней косметической правки браузер отдельно скачал post-fix PDF:
`WMS-476-nepodgruzhennye-kizy (2).pdf`. `pdfinfo` подтвердил PDF 1.4, четыре
страницы A4, 19 540 байт, отсутствие шифрования и JavaScript. Первая страница
отрендерена и визуально просмотрена: двухстрочный заголовок имеет достаточный
правый отступ, тестовая графика и все подписи читаемы.

Необоснованных пользовательских блокировок в макете не обнаружено: выбор товара
сразу переводит к ручной загрузке, снятие последней галочки возвращает
авто-распознавание, а после технической ошибки доступны повтор и выбор другого
файла. Остановка между этапами A и B — не новое продуктовое ограничение, а прямой
gate владельца.

На момент этого исторического заключения C7 ожидала владельца, поэтому этап B
был запрещён. Этот запрет снят явным утверждением 23.09.2026, зафиксированным
выше; он не является актуальным статусом задачи. C8–C29 остаются непроверенными
до продуктовой реализации и её приёмки.

## 9. Точечное уточнение сопоставления `One Size` от 24.09.2026

| ID | Условие, действие и наблюдаемый результат | Основание |
|---|---|---|
| R26 | При автоматической загрузке PDF размер этикетки `One Size` совместим с размером товара WB `0`, если совпал тот же артикул в каталоге выбранного селлера. Это единственное дополнительное соответствие размеров: пустой размер не считается `One Size` или `0`, а остальные размеры по-прежнему должны совпадать точно. Границы клиента/селлера, неоднозначность, артикул и проверка GTIN сохраняются. | Прямое уточнение владельца 24.09.2026. |

| Проверка | Требования | Подготовка и действие | Ожидаемый результат | Вердикт |
|---|---|---|---|---|
| C27 | R24, R26 | В одном адресном backend-сценарии создать у одного селлера товары общего артикула с размерами WB `0` и `M`, затем загрузить PDF этого артикула с размерами `One Size`, `L` и без размера; отдельно загрузить `One Size` без артикула, но с GTIN товара `0`. | `One Size` с тем же артикулом однозначно загружается в товар `0`; `L` не сопоставляется с `0`; пустой размер не выбирает один из двух товаров и остаётся неоднозначным; один GTIN без совпавшего артикула не включает исключение `One Size` → `0`. | **Подтверждено локально.** End-to-end тест `test_auto_match_treats_one_size_label_as_wb_size_zero_only` прошёл; рядом повторно прошёл существующий сценарий совместного сопоставления артикула и GTIN. Прод и физический PDF пользователя этим не подтверждены. |

### Заключение по точечному уточнению

R26 реализовано минимальным изменением чтения явно подписанного размера и сравнения
размеров в существующем автоматическом сопоставлении. Точечные положительный и
отрицательные сценарии C27 прошли. Изменение ещё не опубликовано и не развёрнуто;
его production-статус должен подтверждаться отдельно по фактически обслуживаемому SHA.
