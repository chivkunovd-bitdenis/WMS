# WMS-445 — FBS на ТСД: заказы, поставка и завершение работы

## Исходный запрос и уточнения

Пункт 9 владельца, 13.09.2026, дословно:

> ФБС - ты так и не исправил. Я давал конкретные требования как мы создаём отгрзуку, как раблотаем с заказами и всё такое. Щас там ебаная каша просто - зачем то и отгрухзки и заказы и нет возможности их прверсти нормално

Общие указания того же запроса: агрегировать связанные замечания в самостоятельные задачи; каждая проходит отдельные постановку БА, реализацию, перекрёстное ревью и приёмку; независимые работы идут параллельно. База — собственный worktree от `origin/etalon`; WMS-433, её ветка, worktree и запись в бэклоге не затрагиваются. Общий stash запрещён.

Эта задача продолжает мобильные WMS-401/363; не считает их исторические статусы сегодняшним состоянием. Пункт 8 о всех складских действиях в расчётах передан отдельной задаче; здесь проверяется отсутствие второго списания и повторной операции при FBS-передаче. Настройка сетевой печати относится к отдельному треку печати; существующая печать FBS должна сохраниться.

### Уточнение владельца о модели и границах изменений, 13.09.2026

После исчерпания лимита Opus владелец поручил Astra завершить работу:

> отдавй астре доделать
> жётско только скажи что дизайн код строго как делал опус и как есть в системе, ничего нового не добавлть и не меняиь
> помимо того что описано явно в поставноке

Сохраняются существующий дизайн, компоненты и подход к коду системы и уже сделанной работы Opus. Изменения допустимы только в явно описанном объёме этой постановки. Смена модели не отменяет отдельное ревью, чек-лист и приёмку аналитиком; функциональные требования не изменены.

### Уточнение о старых заказах в ТСД, 13.09.2026

Ведущий передал новый факт владельца: в FBS на ТСД у клиента/сотрудника «Виталик Хорс» отображаются непонятные старые неактуальные заказы. Это уточнение входит в WMS-445. Кавычками выделено только переданное имя; дословная полная реплика владельца в этой сессии аналитика не предоставлена, поэтому пересказ не выдаётся за цитату.

Уточнение сужает историческое «все заказы» до всех заказов текущей складской работы. Возраст заказа сам по себе не доказывает неактуальность. Не поручены очистка истории, удаление заказов, новые статусы, фильтры, вкладки или изменение дизайна.

### Уточнение по production: ТСД и сервер расходятся в начале работы, 14.09.2026

Ведущий передал результат read-only проверки: оператор ТСД считает поставку взятой/собранной, а веб показывает «Начать работу» и 0/1. Это пересказ сообщения и наблюдений ведущего, а не самостоятельно восстановленная дословная реплика владельца. Поставка Ozon `0d365875-a885-4c88-b442-376f8d3e65c7`, отправление `0153104185-0005-1`. Сохранённые факты: supply.status=draft, packaging_task_id=NULL; order.status=in_supply, pick_status=pending, pack_status=pending, sticker_status=not_requested; позиция qty=1, picked=0. Короб №1 с переданным префиксом ID `67a50115...` имеет status=open, строк fbs_packing_box_items нет. Наличие пустого короба не подтверждает подбор/упаковку. Эти production-объекты не изменять ради воспроизведения.

Поручение: сервер — источник истины; ТСД не показывает успешное рабочее состояние без сохранённого начала на сервере. При первом входе/начале любой операции поставка атомарно становится assembling и получает существующее задание упаковки; после действия приложение перечитывает серверное состояние, потерю ответа восстанавливает без дубля. Сохраняется прямое ограничение владельца на существующий дизайн Opus/системы: никаких новых экранов и оформления.

## Источники и восстановленный процесс

Актуальные [правила проекта](../../AGENTS.md), включая отсутствие обязательного завершения подбора/упаковки для открытия рабочих вкладок, выше старых документов.

1. [WMS-401](../KANONICHESKIY_BACKLOG.md#wms-401), раздел «Прямое поручение владельца»: отдельный FBS в настоящем приложении, первоначально **все заказы**, фото, ШК, поступление/срок; галочки → добавить в поставку. Подбор сканом — старейший ещё не подобранный подходящий заказ; источник ячейка/короб/палета/грузоместо сохраняется; зелёная строка и прогресс после возвращения. Упаковка — только признак, без складских изменений и блокировок. Крупная печать заказа, количество ШК/ЧЗ, QR в той же ленте, общая печать; QR заказа → ЧЗ через общий сервер. Короба: открыть, сканировать, закрыть, изменить количество, распечатать QR. Последующее прямое «ну так надо доделать» распространяет путь на финальную передачу через существующий сервер.
2. [WMS-363](../KANONICHESKIY_BACKLOG.md#wms-363) задаёт использование существующего Ozon-процесса и позиций отправления. [WMS-339](../KANONICHESKIY_BACKLOG.md#wms-339) фиксирует жалобу на навигацию по сущностям базы вместо работы сотрудника. Предложения старого HTML-макета не выдаются за согласованную замену всего приложения: текущая задача ограничена FBS.
3. [Финальный веб-handoff 06.08](../../tasks/fbs-operator-flow/FINAL_FBS_UI_HANDOFF_2026-08-06_RU.md) подтверждает контекст заказа/поставки, совместимость выбранных заказов, сохранение состояния и повторов, различие WB ПВЗ/склад. Его старые ограничения готовности и инструкции о моделях не переопределяют актуальные AGENTS и мобильную WMS-401. [Ранний correction-handoff](../../tasks/fbs-operator-flow/CURRENT_FBS_UI_CORRECTION_HANDOFF_2026-08-06_RU.md) — история отвергнутого разнесения упаковки и печати, а не разрешение вернуть прежние шесть обязательных этапов.
4. [APK11 recovery](../reviews/wms401-login-recovery-candidate-20260910.md), [Ozon implementation evidence](../reviews/wms363-astra-20260910.md): отделяют уже сделанное от оставшегося. История доказательств не заменяет приёмку новой сборки.
5. Применены актуальные навыки постановки/приёмки и продуктового решения из основного checkout `docs/reviews/2026-09-11-analyst-draft/skills/`, библиотека owner-cases (действие в контексте документа, срок Ozon не запрещает работу), failure-cases B01–B04, B06–B09 (повторы, внешняя неопределённость, переключение документа после ошибки).

Оператор ФФ работает с импортированными заказами выбранной площадки. Выбранные заказы формируют **существующую поставку FBS сервера**, либо попадают в уже существующую совместимую. Из её контекста доступны подбор, упаковка с маркировкой/печатью, короба и явная передача площадке. Это один документ в вебе и ТСД. Обычные отгрузки из другого раздела приложения не являются вторым способом провести тот же FBS-заказ.

### Проверенная исходная реализация

Мобильная база: `af9693126d19d16918abd5cc7b7fbe99dfebc142` (APK11), отдельный Git в `mobile`. Проверены FbsScreens.kt, FbsViewModel.kt, FbsModels.kt, FbsApi.kt, Navigation.kt, HomeScreen.kt. PROGRESS и материалы подписи не открывались согласно ограничению координатора.

Уже есть переключатель WB/Ozon, список всех заказов **перед** поставками, разбор дат с часовыми поясами, старейший подходящий заказ, признаки подбора, многопозиционный Ozon, общие API подбора/упаковки/ЧЗ/коробов/печати/передачи. Это сохраняем, а не реализуем заново. Упаковка при необходимости создаёт существующее задание через start-work; неверно утверждать, что этот вызов отсутствует.

Статически подтверждённые пробелы:

- `OrderCard` не выводит имя селлера, маршрут сдачи и обычный статус заказа; `FbsNamedRef` теряет идентификатор. Поэтому оператор не различает совместимость похожих заказов и состояние неподобранных строк. API worklist уже возвращает seller, склады, delivery_route, can_pvz и selection_blockers.
- `chooseSupply` показывает все активные поставки выбранной площадки, только имя, без проверки соответствия селлеру/складу/маршруту выбранных заказов. Веб уже выбирает совместимые поставки и объясняет пустой результат; сервер отклоняет несовместимость. Наличие серверного отказа не делает выбор понятным.
- Диалог создания показывает один и тот же выбор WB «Склад / СЦ» и «ПВЗ» для Ozon, хотя worklist возвращает маршрут Ozon из метода доставки. Само наличие этого выбора ещё не доказательство серверного сбоя создания.
- При нажатии «Создать»/выборе поставки диалог закрывается до ответа. Ошибка показывается над общим списком без состава и маршрута неудачной попытки. Повтор хранится лишь в текущей ViewModel; передача уже сохраняет запрос до сети. Создание после потери ответа/выхода требует отдельной проверки, нельзя объявлять дубль без воспроизведения.
- Открытие существующей поставки доступно через строку заказа либо длинный хвост «Поставки в работе». На карточке поставки не показаны площадка, селлер, маршрут и состав, только имя и счётчики; оператору приходится открывать операцию, чтобы увидеть заказы.
- Для Ozon кнопки упаковки всех позиций остаются одинаково доступными, пока весь заказ не packed; контракт экрана не показывает остаток упаковки каждой позиции. Это риск повторной упаковки уже завершённой позиции; необходимость исправления подтверждается сценарием C8.

13.09.2026 аналитик открыл в браузере изолированный `http://localhost:5197/app/ff/warehouse-map` и FBS `/app/ff/fbs`. В «Ячейках» визуально проверены светлая тема, читаемая иерархия, действия внутри рабочего блока и таблица. Веб FBS начинает работу с заказов и имеет группы статусов и фильтры. Данных FBS пока нет: этот проход не доказывает создание и проведение документа. Затем на выделенном emulator-5580 аналитик открыл настоящий APK11: Home → FBS, переключил WB → Ozon и вернулся назад. Оба экрана показывают «Все заказы» и «Заказов пока нет», переключатель площадки работает, навигация возвращает Home. Снимки фактически просмотрены; данные/складские операции не изменялись. Наполнение изолированной базы ещё требуется; статические пробелы выше не названы воспроизведёнными сбоями устройства.

## Требования

| ID | Условие → действие → наблюдаемый результат | Основание |
|---|---|---|
| R1 | Оператор открывает FBS → видит список заказов текущей складской работы выбранной WB/Ozon по R11, старейшие первыми, фото/товар/ШК, площадку, селлера, поступление, срок и понятное текущее состояние; просроченный и близкий срок выделены, но сам срок не запрещает работу. Ozon показывает все позиции и их количества. | Прямая WMS-401, текущий запрос, AGENTS. |
| R2 | Оператор отмечает новые заказы → видит состав выбранного набора и формирует новую поставку либо добавляет в существующую совместимую. Селлер, площадка, склад и маршрут видны до сохранения. Несовместимость объяснена применительно к выбранным заказам по существующему серверному контракту, без новых локальных запретов. | WMS-401, сохранённый веб-процесс. |
| R3 | При создании WB оператор использует существующий маршрут ПВЗ или склад/СЦ. В Ozon видит действительный маршрут/метод доставки заказов; WB-термины не предлагают ему ложный выбор. Сохранённый документ открывается сразу и при повторном входе совпадает с веб-документом по ID, составу и площадке. | WMS-363, AGENTS о принадлежности площадке; решение D2. |
| R4 | Оператор возвращается к начатой работе → на первом FBS-экране есть прямой вход в поставки в работе без прокрутки всех заказов. Открытая поставка показывает имя, площадку, селлера, маршрут, состав и сохранённый прогресс. Подбор, упаковка/этикетки, короба и передача доступны в контексте этого документа. Возврат сохраняет выбранную площадку. | Жалоба на смешанную навигацию, WMS-401, решение D1. |
| R5 | Оператор открывает любое действие существующей редактируемой поставки, в том числе assembling с нулевым подбором/упаковкой → приложение открывает выбранный экран; не перенаправляет к предыдущему этапу и не добавляет обязательных локальных чеклистов. Передача использует действующий серверный процесс, технические отказы показываются по фактической причине. | Прямой актуальный AGENTS и WMS-401. |
| R6 | Скан источника и товара при подборе → сервер подбирает единицу для старейшего подходящего незавершённого заказа, для Ozon именно соответствующую позицию. Подобранная строка выделена; остаток задания и источник понятны. Новый физический скан второй единицы отличается от повторной доставки того же запроса. После возвращения сохранённый результат прежний. | Прямой WMS-401; существующая складская механика. |
| R7 | В упаковке оператор печатает один заказ или весь выбранный набор → доступны прежние количества товарных ШК/ЧЗ и QR/этикетка площадки; отображаемая площадка соответствует данным. QR заказа → ЧЗ сохраняет код общим API и показывает фактический результат площадки. После ошибки можно явно выйти из выбора заказа и продолжить с другим; чужие сохранённые коды не теряются. | Прямой WMS-401; B06/B07. |
| R8 | Отметка упаковки WB или конкретной позиции/единицы Ozon → сохраняется признак и точный прогресс существующего задания, без складского списания; уже завершённая позиция не выглядит следующей неупакованной единицей. Короба используют реальные order_ids для WB и позиции с количеством для Ozon; повтор/возврат сохраняет состав, печать получает соответствующий asset площадки. | WMS-401/363; сохранение количественной правды. |
| R9 | Оператор явно передаёт поставку → приложение вызывает общий процесс соответствующей площадки, показывает только подтверждённый сервером результат и доступные документы/этикетки передачи; после возвращения тот же документ имеет тот же статус в вебе и ТСД. Потерянный ответ сначала разрешается чтением/повтором той же операции, без второй поставки, передачи, списания или начисления. | Прямое продолжение WMS-401, текущий запрос, AGENTS. |
| R10 | Любое изменяемое действие получает отказ до выполнения или теряет ответ после выполнения → видны понятная причина и конкретный объект; выбор и введённые данные сохраняются для продолжения. Повтор одной попытки использует прежнюю идентичность, при возвращении приложение выясняет сохранённый результат. Можно выйти и открыть другую поставку без переноса сканов/ошибок. | WMS-401, AGENTS, B01/B02/B03/B07/B09. |
| R11 | Оператор открывает/обновляет FBS, загружает следующую страницу или возвращается после работы → получает только заказы текущей складской работы: `new` с `supplier_status` пустым/`new`, `in_supply`, `assembling`, `packed`. `in_delivery`, `sorted`, `done`, `cancelled`, `defect`, `external_processing` не попадают в начальную рабочую ленту. Возраст, истёкший срок, отсутствие подбора/упаковки, сопоставления товара или остатка сами по себе не исключают рабочий заказ. Список старейших, оба маркетплейса и существующий вход «В работе» сохраняются. История и данные заказов не удаляются. | Новое уточнение о старых неактуальных заказах; R1/R5, AGENTS; решение D4 ниже. |
| R12 | Для клиента/сотрудника «Виталик Хорс» и любого другого входа рабочие заказы/поставки относятся к авторизованному tenant (организации клиента) и действующей области селлера. Имя сотрудника не подменяет идентификаторы организации/селлера и не ограничивает список только созданными им заказами. При смене учётного контекста, обновлении и продолжении пагинации данные предыдущего контекста не примешиваются. У WB сохраняется существующее условие обслуживаемого активного склада (`served=true`); настройка публикации остатков не становится новым условием видимости. | Уточнение с конкретным клиентом; действующие границы доступа AGENTS и API. |

## Решения аналитика и вопросы

**D1.** Сохраняем первую точку входа «Заказы» и добавляем рядом компактный доступ «В работе» к уже существующим поставкам FBS. Не дублируем поставки вторым длинным блоком под всеми заказами. После выбора/создания открывается один контекст поставки с составом и действиями. Это устраняет смешение очереди и документов без новой сущности и без общего перепроектирования главного экрана. Обычный раздел отгрузок остаётся самостоятельным процессом; его не удаляют ради FBS.

**D2.** Селлер/маршрут/статус — короткие текстовые строки в текущей карточке и шапке документа, по существующей теме Compose; не переносим вебовую широкую таблицу на узкий ТСД. Данные берём из общего ответа API. Для Ozon показываем известный метод доставки вместо выбора маршрута WB. Сервер определяет допустимую совместимость; отсутствие имени не подменяем другой площадкой или выдуманным маршрутом.

**D3.** Уже рабочие подбор, печать, ЧЗ, упаковку и доставку не переписываем. Исполнитель проходит обязательные проверки ниже и исправляет подтверждённые дефекты; наличие API-метода/юнит-теста не закрывает операторский путь. Сетевой агент печати не добавляется в WMS-445 и не может удалить имеющуюся Android-печать.

Неизвестного намерения владельца, требующего нового вопроса, сейчас нет: желаемый путь восстановлен из прямой WMS-401. Остаётся техническая подготовка синтетических заказов и отдельного эмулятора площадок для полного прохода. Никаких настоящих заказов или внешних действий ради приёмки.

## Проверки

Среда: изолированные API18082/UI5197, собственная новая БД; Android API24+ с APK предъявленного SHA. Нужны два селлера, WB ПВЗ и склад/СЦ, Ozon с двумя позициями и количеством минимум 2, новые/начатые/переданные заказы, одинаковый товар в старом и новом заказе, два складских источника с остатком, отдельный эмулятор WB/Ozon. Стандартный исторический fixture363 содержит лишь начатые поставки без свободного остатка и не закрывает эту подготовку. Ошибки воспроизводятся только здесь. Физический принтер/ТСД и live-площадки обозначаются отдельно и не подменяются эмулятором.

| Проверка | Требования | Подготовка → действие → ожидаемое наблюдение и перечитывание | Вердикт |
|---|---|---|---|
| C1 Список и навигация | R1,R4 | Несколько страниц заказов со смешанными датами/часовыми поясами, сроками до/после 6 ч, длинными товарами → открыть WB, перейти Ozon и «В работе», открыть и вернуть поставку → список по реальному времени, читаемая принадлежность/срок/статус; начатая поставка доступна без прокрутки всех заказов, площадка сохранена. Сравнить узкий экран с текущей темой. | **Не проверено — блокирует приёмку.** Финальный APK открывает WB/Ozon и показывает новый orders-first контекст, но несколько страниц, порядок разных часовых поясов/сроков, длинные данные и сохранение площадки не пройдены. |
| C2 WB создание | R2,R3,R4 | Совместимые новые WB-заказы одного селлера, отдельно ПВЗ и склад/СЦ → отметить, создать, открыть состав → одна поставка нужного маршрута и те же заказы в вебе после обновления. | **Не проверено — блокирует приёмку.** Один WB заказ создан и проведён на emulator-5580; обязательны обе ветви ПВЗ и склад/СЦ и web-reread тех же заказов. |
| C3 Ozon создание | R1,R2,R3 | Новые многопозиционные Ozon-заказы с известным методом доставки и большим ID склада → создать поставку → виден реальный метод, нет ложного WB-выбора; состав/количества/идентификатор совпадают в вебе и ТСД после возврата. | **Не проверено — блокирует приёмку.** Ozon 2+1 и маршрут показаны на устройстве, поставка создана, но полный повторный web/ТСД контроль идентификатора и состава на финальном пакете не сохранён. |
| C4 Добавление | R2,R4 | Две активные поставки разных селлеров/складов и новые заказы → «В поставку» → доступны совместимые, либо понятный пустой результат; добавление отражается в том же документе после перечитывания. Несовместимый серверный отказ не теряет выбор. | **Не проверено — блокирует приёмку.** Совместимость и add-recovery покрыты ViewModel/API-тестами; фактический выбор двух поставок, пустой результат/отказ и reread UI не выполнены. |
| C5 Повтор создания | R2,R10 | Новый набор → двойное нажатие и два одновременных запроса одного намерения; отдельно потеря ответа после серверного успеха → повтор/выход/возврат → единственная локальная и внешняя поставка, исходные заказы связаны один раз, следующий рабочий шаг доступен. | **Не проверено — обязательная интеграционная граница блокирует приёмку.** Нативный обрыв create/add после настоящего commit и восстановление после выхода не выполнены; JVM-тест не заменяет этот путь. |
| C6 Свободная навигация | R4,R5 | WB/Ozon assembling, picked=0/packed=0 → открыть упаковку, короба, подбор, вернуться → именно выбранные экраны без принудительного шага и нового запрета. Просроченный Ozon также доступен. | **Не проверено — блокирует приёмку.** Свободные вкладки сохранены кодом, но WB и Ozon assembling с picked/packed=0 и просроченный Ozon не пройдены по всем экранам итогового APK. |
| C7 Подбор | R6,R10 | Два заказа одного товара с разными датами, два источника; Ozon со второй позицией quantity2 → источник, товар, второй физический скан, повтор HTTP первого → старейший подходящий заказ/позиция, точные количества без второго применения повтора. Перезапустить экран и сравнить складской результат с общим веб-путём. | **Не проверено — блокирует приёмку.** Серверный тест подтверждает старейший заказ и exact retry, а один WB-подбор выполнен на устройстве; полный WB/Ozon сценарий двух источников, второй позиции и restart не пройден. |
| C8 Упаковка | R5,R8 | Ozon две позиции 2+1 и WB один товар → отметить единицы первой позиции до полного количества, затем другую → экран различает остаток каждой; повтор одного запроса не меняет склад и не добавляет лишнюю единицу. Перечитать задание в вебе. | **Не проверено — блокирует приёмку.** На emulator-5580 Ozon позиции2+1 упакованы как2+1 и reread сохранил прогресс; обязательный повтор запроса и web-перечитывание задания не сохранены. |
| C9 ЧЗ и выход после ошибки | R7,R10 | Два заказа с пригодными тестовыми маркировками → QR A, ошибочный код/API500, сброс выбора, QR B, верный код → выбран B, ошибка A не мешает, ранее сохранённые коды прежние. Потерянный ответ commit выясняется без второй привязки; отображается фактический статус эмулятора. | **Не проверено — блокирует приёмку.** Полный ЧЗ-сценарий A→ошибка/500→сброс→B и потеря ответа после commit не выполнялся. |
| C10 Короба | R8,R10 | WB несколько одинаковых товаров и Ozon позиции 2+1 → новый короб, сканы, изменение состава, второй короб, возврат → точные order/position ID и количества; одна физическая единица не учитывается дважды; распределение количества одной Ozon-позиции между коробами проверяется по существующему контракту площадки, суммарно не более доступного количества. Сам номер позиции не является запретом второго короба. Повтор после потери ответа не переносит следующую единицу. | **Не проверено — блокирует приёмку.** Короба WB/Ozon созданы в отдельных примерах, но split двух коробов, изменение состава и потеря ответа без переноса следующей единицы не пройдены. |
| C11 Печать | R7,R8 | WB и многопозиционный Ozon → один заказ, все QR, всё с разными числами ШК/ЧЗ, этикетка короба → Android-превью содержит верные объекты/количество страниц/площадку; ошибка asset не печатает неполный набор как успешный. Работающий способ печати APK11 сохранён. | **Не проверено — блокирует приёмку.** Существующий Android PrintManager сохранён, но полный набор WB/Ozon preview, QR/ШК/ЧЗ с разными количествами и отказ asset не проверен. |
| C12 Передача WB | R5,R9 | Собственная WB-поставка каждого маршрута → передать через ТСД, обновить/переоткрыть в вебе и ТСД → общий подтверждённый статус, нужный QR/документ доступен; один физический расход/начисление. Неподтверждённый ответ не рисует успех. | **Не проверено — блокирует приёмку.** Одна WB поставка дошла до in_delivery с одним расходом и начислением; оба маршрута WB и сценарий неизвестного ответа на итоговом ТСД не выполнены. |
| C13 Передача Ozon | R3,R9 | Собственная Ozon-поставка → сборка/этикетка по общему серверу, передача; эмулятор create→approve→formed, допустимые null → тот же внешний результат признан без ложного успеха на неизвестном статусе, данные перечитаны в обоих клиентах. | **Не проверено — блокирует приёмку.** Ozon local API-smoke подтвердил create→approve→formed, расход3 и один ledger-набор, а Android-поставка дошла до in_delivery; единый полный UI→web reread сценарий на финальной интеграции не сохранён. |
| C14 Потеря ответа передачи | R9,R10 | Передача подтверждена эмулятором, ответ оборван → закрыть/открыть приложение, проверить результат → та же операция, без второй внешней передачи/списания/начисления; сбой последующего QR повторяет получение QR, не передачу. | **Не проверено — блокирует приёмку.** Emulator API восстановил потерянный approve и повтор deliver без второго расхода; нативный restart/reconcile и отдельный сбой QR не выполнены. |
| C15 Контекст отказа | R2,R10 | Создание/добавление получает 500 до сохранения → форма/набор/маршрут остаются, понятен объект ошибки; восстановить API и продолжить → один результат. При выходе к другой поставке прежние сканы/сообщения не применяются к ней. | **Не проверено — блокирует приёмку.** Отказ до создания, сохранение конкретного набора/маршрута, продолжение и изоляция другой поставки не пройдены в пользовательском UI. |

## Сверка перед передачей разработчику

Жалоба «как создаём отгрузку» покрыта R2/R3/R9; «как работаем с заказами» — R1/R4/R6/R7/R8; «каша… отгрузки и заказы» — D1/R4; «нет возможности провести» — полный путь C2–C15, а не одна кнопка. Предыдущая сортировка, фото/срок, источник и старейший заказ из WMS-401 сохранены R1/R6. Упаковка как признак, прежняя печать/ЧЗ/короба сохранены R5/R7/R8. Ozon не сведён к WB — R1/R3/R8/R9. Каждое R имеет источник и проверку; новые сущности, обязательные этапы, offline и глобальный редизайн не добавлены.

## Заключение

**Актуальное заключение, 14.09.2026: mobile review PASS; полная приёмка серверного начала ещё не закрыта.** Изменённый мобильный код cef944b1 проверен независимо, свежие FbsViewModelTest 60/60 PASS. Подтверждены автоматическое начало draft из рабочего контекста, восстановление потерянного ответа чтением и сохранение assembling без повторного start. Подтверждённых дефектов изменённого кода нет. Реальные API/БД/web, атомарность/конкуренция start и физический APK по C22–C28 остаются непроверенными; новый production-инцидент ещё не объявлен закрытым. Точные границы ниже.

**Историческое заключение 13.09.2026 (не текущий вердикт по новому дефекту): READY / DEPLOYABLE TO STAGING.** C16–C20: независимые HTTP/DB/ViewModel проверки подтверждены, F1–F3 закрыты; native current queue итогового прохода не завершён. C21 «Виталик Хорс» остаётся отдельным реальным read-only пунктом и synthetic данными не закрывается. Подтверждённых открытых кодовых P1/P2 нет. По последнему решению владельца неполное операторское/физическое покрытие отдельно от допуска на стенд. Полный общий вердикт: [единый финальный отчёт](../reviews/tsd-package-final-astra-20260913.md).

### Предыдущие оценки покрытия (история; не текущий допуск на staging)

**WMS-445 не принята.** Сохранены реальные локальные положительные примеры WB и Ozon: device-документы дошли до `in_delivery`, Ozon API-emulator дал один create/approve/formed, расход3 и один набор начислений, а технический Astra-review закрыл FBS identity recovery. Однако ни одна составная проверка C1–C15 не имеет полного набора требуемых шагов на финальной интеграции. Подтверждённого дефекта после Astra closure нет; Terra handoff не требуется.

Приёмку блокируют нативная потеря ответа create/add после настоящего commit, оба маршрута WB, полный подбор Ozon, ЧЗ и восстановление после ошибки, раскладка по нескольким коробам, полный печатный набор и единое web/ТСД reread передачи. Реальные WB/Ozon, физическая передача, production, публикация APK и deploy не проверялись. Приёмку провёл новый Sol-аналитик как явная замена прежних BA-сессий.

## Уточнение владельца о моделях — 2026-09-13

По последнему уточнению владельца разработку выполняет Terra high. Бизнес-анализ, постановку требований, приёмку, независимое ревью и оркестрацию выполняет Astra. По возможности приёмку проводит исходный аналитик; при замене ему передаются исходный запрос, уточнения и доказательства. Это исправляет прежнее ошибочное распространение перехода на Terra на бизнес-анализ и приёмку и заменяет прежнее поручение Astra доделать реализацию.

Ограничение владельца сохраняется: дизайн и подход к коду строго как у Opus и в существующей системе; менять и добавлять только явно описанное в постановке. Сохранённые изменения и результаты проверок передавать следующему исполнителю, не повторять пройденные проверки без изменений или новых оснований. Порядок постановка → реализация → независимое ревью → приёмка сохраняется.


### Уточнение владельца о непрерывности работы

13.09.2026 владелец прямо указал: случайное обнаружение ключа само по себе не должно останавливать работу. Для этого пакета прежнее условие остановки по факту обнаружения заменено продолжением задачи. Значения не воспроизводятся в сообщениях и отчётах и не включаются в коммиты; это уточнение не поручает управление ключами.

## Ограниченный аудит объёма и передача на приёмку — 13.09.2026

Этот ограниченный Astra-аудит сверил исходные слова и сохранённые проверки, не проводил новую приёмку и не менял продуктовый код. Исторические 88 строк относятся к пяти задачам WMS-441–445, включая 18 прежних проверок WMS-442; в четырёх задачах этого аудита 70 строк (13/19/23/15). Добавленные позже проверки Windows не входят в число88 и здесь не меняются. Непроверенное не переведено в «подтверждено».

Среда, доступная для подготовки: локальный API18082 сейчас ответил `health=ok`; web5197, WB19092 и Ozon19093 принимают TCP-соединение. Это доступность процессов, не подтверждение их SHA или поведения. Тестовая БД `wms_tsd_20260913` и `emulator-5580` с интегрированным APK описаны в [совместном контуре](../reviews/tsd-local-integration-20260913.md) и [mobile checkpoint](../reviews/artifacts/tsd-package-20260913/mobile-integration.md). Android в этом аудите не управлялся, установленная версия не перечитывалась. Эти fixture-данные находятся локально; на удалённом staging их наличие не доказано. Перед приёмкой зафиксировать фактические API/web/APK SHA и тестовый контекст, затем перенести/воссоздать только синтетические данные штатным seed. Старые завершённые документы не возвращать в работу ради повтора.

Состав R1–R10/C1–C15 следует из прямых требований WMS-401, на которые сам владелец сослался в новом запросе: оба WB-маршрута, многопозиционный Ozon, ЧЗ, печать, короба и передача не добавлены этой приёмкой. Тестовые сбои/повторы обязательны для единственной передачи и расхода. Единственная найденная лишняя формулировка C10 — абсолютный запрет «одна позиция не находится в двух коробах»: он шире защиты одной физической единицы и R8 с количеством позиции. Заменён контролем точных количеств и реального контракта площадки, без придуманного ограничения по одному ID позиции. Это уточнение не разрешает обход обязательного правила площадки и не объявляет split уже рабочим.

**Минимальный план закрытия на staging.** Три связанные поставки: WB ПВЗ, WB склад/СЦ и Ozon с позициями2+1. WB-набор содержит два заказа одного SKU разного возраста и два источника; создать одну поставку, второй заказ добавить существующим действием. До подбора открыть все вкладки, затем подобрать/упаковать/собрать два короба, проверить Android-превью и передать. Один create/add с обрывом после commit и отдельный отказ до commit закрывают C5/C15, одна передача с потерей ответа/restart и отказом получения QR — C14. Во втором WB-маршруте достаточно собственного полноценного документа с перечитыванием результата. На Ozon проверить каждую единицу2+1, маршрут, состав коробов и create→approve→formed с web/ТСД перечитыванием. ЧЗ A→ошибка→выход→B и сохранение старого результата C9 проходят на подходящих тестовых заказах; эту часть нельзя заменить отсутствующей реализацией заглушки. Список дат/длинных названий и сохранение площадки снять во время тех же переходов. Ledger/авторов и денежный отчёт дочитывает444/443 по этим же ID; повторно передавать поставки для другой задачи не нужно.

**Готовые заготовки и конкретный пробел.** `backend/tools/wms445_seed.py`, `wms445_local_http.py`, `wms445_ozon_emulator.py`, `wms445_smoke.py`; [manifest](../reviews/artifacts/wms445-backend-20260913/manifest.json) и [wire summary](../reviews/artifacts/wms445-backend-20260913/ozon-wire-summary.json) сохраняют подготовку18 заказов и подтверждённый Ozon результат. Shared ID двух ранее переданных поставок записаны в совместном контуре: они пригодны для перечитывания, не повторного начального цикла. Handoff явно сообщает, что Ozon fake не реализует ЧЗ/exemplar и multi-box split, неизвестный маршрут даёт501; этот пробел среды надо закрыть адресно до C9/C10, а не выдавать501 за ошибку продукта или обращаться к рабочим заказам. Физическая печать/установщик442 и реальные WB/Ozon не являются условием C1–C15: по D3 здесь сохраняется Android-печать и используются разрешённые эмуляторы. **Задача не принята.**

## Дополнение аналитика: актуальность рабочей ленты — 13.09.2026

### Проверенные факты и предел доказательства

Ветка `codex/tsd-package-20260913`, исходный снимок перед этим дополнением `d9a85c1d`. В `FbsViewModel.loadOrders` Android вызывает `orders(marketplace=..., group=null, sort=oldest)`. `FbsApi.orders` обращается к `GET /operations/fbs-orders/worklist`. В `fbs_worklist_service._fetch_orders_page` отсутствие `status_group` означает отсутствие ограничения по статусам. Поэтому серверная выдача допускает завершённые, отменённые и переданные заказы, а старейшая сортировка выносит их наверх. Отдельный запрос поставок уже использует `status_group=active` с `draft/assembling/packed`.

Существующая группа заказов `active` не решает задачу: не включает `new` и включает `external_processing`. Для WB этот статус означает, что заказ ушёл в обработку кабинета вне нового складского заказа. Для Ozon в него отображается в том числе `awaiting_deliver`, если отсутствует сохранённый локальный этап складской работы. Синхронизация Ozon сохраняет локальные `in_supply/assembling/packed`: нельзя повторно классифицировать их по сырому статусу площадки и потерять собственную незавершённую поставку. Источники: `backend/app/services/wb_marketplace_orders_service.py`, `ozon_fbs_sync_service.py`, `fbs_worklist_service.py`, `fbs_supply_service.py`.

Группа API `new` дополнительно отсеивает WB по сроку; поэтому механическое объединение `new+active` также не выполняет R11. R1 и актуальные правила проекта не разрешают вводить запрет по возрасту/сроку. Этот анализ не подтверждает обязательный запрет WB по дедлайну; старый комментарий в коде не является доказательством такого обязательства. Изменение рабочего набора ТСД не должно переписывать состав существующих веб-групп или менять бизнес-статус заказа.

13.09.2026 в браузере открыт локальный FBS `http://localhost:5197/app/ff/fbs`: видны существующие «Новые», «В работе», «В доставке», «Просрочены», «Завершённые», «Отменённые». В текущем авторизованном контексте «Анна Тестовая WMS» лента пустая; это не контекст «Виталик Хорс». Визуально просмотрены FBS и эталон «Ячейки»; новый дизайн для этого дополнения не требуется. Android в этой аналитической сессии не переключался, чтобы не прервать общую приёмку; его вывод проверен по коду, не выдан за новый проход устройства.

Read-only запросы к локальной БД `wms_tsd_20260913` по `users.full_name`, `sellers.name`, `tenants.name` с подстроками «Витал»/«Хорс» дали 0 совпадений. В доступных сохранённых XML экранов `.local-run` это имя также не найдено. База содержит 18 тестовых FBS-заказов: WB 9 `new` и 3 `in_delivery`, Ozon 5 `new` и 1 `in_delivery`; завершённых/отменённых в этом наборе нет. Это подтверждает доступность локальной подготовки, но не идентифицирует клиента или его проблемные заказы. Production tenant, роли и ID заказов не установлены; реальную причину именно у «Виталик Хорс» ещё необходимо подтвердить по C21.

### Решение D4 и точная граница исправления

Начальная лента ТСД предназначена для текущей работы склада: новые заказы и заказы незавершённых складских поставок. Сохраняем имеющиеся статусы и навигацию; R11 задаёт набор, R12 — принадлежность. Не показываем в этой ленте доставку, завершение, отмену/брак и внешнюю обработку, где оператору не предлагается заново собирать заказ. Существующий контекст активной поставки остаётся доступным через «В работе»; скрытие её отдельного нерабочего заказа из начальной ленты не удаляет состав документа и не запрещает восстановить начатую операцию.

Набор должен оставаться полным при постраничной загрузке: страница старых нерабочих строк не может изображать конец списка, пока дальше есть рабочие заказы. Исполнитель выбирает минимальный механизм запроса/фильтрации без нового пользовательского режима, новой таблицы или статуса. Изменять веб по этому уточнению не поручено. Подпись «Все заказы» означает все заказы рабочего набора и не требует нового оформления.

При неактуальном локальном статусе исправление должно опираться на существующую синхронизацию и сохранённый результат площадки. Запрещено массово выставлять `done/cancelled`, прятать заказы по произвольной дате или менять реальные заказы ради проверки. Расхождение с площадкой воспроизводится на синтетических данных через существующий процесс обновления статусов. Контекст «Виталик Хорс» проверяется по настоящим идентификаторам учётной записи, tenant и seller; совпадения имени недостаточно. Для сотрудника склада без ограничения по селлеру допустимы все селлеры своего tenant. Если конкретный заказ окажется в действительно рабочем статусе и владелец считает его неактуальным по иному бизнес-признаку, сначала зафиксировать этот факт, не придумывать новый признак.

### Дополнительные проверки к R11–R12

| Проверка | Требования | Подготовка → действие → ожидаемое наблюдение и перечитывание | Вердикт |
|---|---|---|---|
| C16 Статусы рабочего набора | R1,R11 | В своей тестовой организации создать WB/Ozon по каждому из `new/in_supply/assembling/packed/in_delivery/sorted/done/cancelled/defect/external_processing`, у нового supplier пустой/new; отдельно новый с supplier уже не new. Открыть ТСД, сменить WB/Ozon, обновить → первые четыре рабочих состояния присутствуют, остальные отсутствуют, новый уже во внешней обработке не предлагается заново. Сверить ID с API/БД, затем существующими веб-группами: история осталась, статусы и количества не изменены чтением. | Частично подтверждено новой Astra-сессией приёмки: полный набор статусов WB/Ozon, supplier NULL/new/confirm, история и неизменность статусов/связей прошли отдельным HTTP/SQLite сценарием. Нативное открытие/смена площадки/обновление ещё не выполнены; C16 не закрыта. См. [приёмку](../reviews/wms445-astra-current-work-review-20260913.md). |
| C17 Возраст и срок не подменяют состояние | R1,R5,R11 | Два рабочих заказа одного товара разного возраста, включая старый и просроченный Ozon; аналогичный старый рабочий WB, свежий done/cancelled, активная поставка без подбора. Открыть/обновить ТСД → рабочие видны старейшими первыми, свежие нерабочие отсутствуют, собственная активная поставка открывается. Сверить отсутствие произвольной даты отсечки и потери старого рабочего WB из-за механического вызова группы new. | Частично подтверждено: старые и просроченные рабочие WB/Ozon без сопоставления/резерва возвращаются старейшими первыми; возраст не фильтруется. Нативное открытие активной поставки без подбора и проверка оформления ещё не выполнены; C17 не закрыта. |
| C18 Пагинация и повторный вход | R1,R11 | Подготовить больше одной страницы нерабочей истории перед несколькими рабочими заказами. Открыть ТСД, загрузить продолжение до конца, выйти/вернуться и сменить площадку → все рабочие ID доступны один раз, нет ложного пустого/конечного списка; история не появляется на следующих страницах. Длинная старая дата не ломает существующее оформление. | Частично подтверждено: для каждой площадки 105 более старых done перед 5 рабочими; все рабочие ID получены ровно один раз на 3 страницах по 2, повторные запросы совпали. Переключение площадки проходит Android unit-test. Нативный возврат/пагинация/длинная дата ещё не проверены; C18 не закрыта. |
| C19 Смена статуса и восстановление | R9,R10,R11 | Синтетический рабочий заказ довести существующим процессом до передачи/отмены; отдельно доставить в имитаторе WB sold/cancel и Ozon delivered/cancelled/awaiting_deliver, обновить существующей синхронизацией. Обновить/переоткрыть ТСД → сохранённые нерабочие состояния исчезли из начальной ленты, история/документ доступны существующим путём. Собственная Ozon-поставка с локальным этапом не теряется при обычном опросе. Потеря ответа передачи восстанавливается прежним процессом, а скрытие строки не становится повторной передачей. | Частично подтверждено: существующие процедуры применения статусов WB sold/cancel и Ozon delivered/cancelled/awaiting_deliver + HTTP-перечитывание исключают нерабочие строки, собственные Ozon-этапы и active-поставка сохранены. Полный опрос имитатора, передача и восстановление через ТСД ещё не выполнены; C19 не закрыта. |
| C20 Организация, селлер и обслуживаемый склад | R12 | Два tenant, по два селлера; сотрудник своего tenant без seller scope и отдельный вход с seller scope. Войти по очереди, прочитать ленту и «В работе», загрузить продолжение/переоткрыть; проверить также явный чужой seller ID штатным запросом → видны только разрешённые организация и селлеры, чужие ID не раскрывают данные, старый контекст не примешивается. Для WB сравнить активную обслуживаемую привязку с served=false/inactive; непубликуемый, но обслуживаемый рабочий заказ остаётся по действующему контракту. Никаких фильтров по автору заказа. | F1–F3 закрыты повторным ревью Astra на backend a599499a/mobile f3c7f23a: scoped/unscoped fulfillment_staff и явный seller ID проверены HTTP, смена scope и отказы — независимыми Android-тестами; tenant/served/inactive/выключенная публикация проверены изолированным сценарием. Нативная смена входа/переоткрытие ещё не выполнены; C20 не закрыта. См. [повторное ревью и приёмку](../reviews/wms445-astra-current-work-review-20260913.md). |
| C21 Реальный контекст «Виталик Хорс» | R11,R12 | Read-only установить актуальные endpoint/APK, user ID/роль, tenant и область селлера фактического входа; открыть FBS на той же площадке и зафиксировать ID нескольких названных старых строк. Сверить существующую карточку/API с локальным/внешним статусом и датой последней доступной синхронизации без изменения заказа. После исправления и публикации согласованной версии перечитать этот же контекст: нерабочие строки исключены, старые рабочие сохранены, чужих tenant нет. Не создавать и не проводить реальные заказы ради проверки. | Не проверено — обязательная проверка реального инцидента; локальный тестовый контекст не содержит названного клиента. |

### Сверка уточнения и заключение

Жалоба на старую неактуальную ленту покрыта R11/C16–C19; названный клиент и границы доступа — R12/C20–C21. Отказ от произвольного срока следует из R1 и правил проекта; отсутствие новой навигации/дизайна — из прямого ограничения владельца. Существующие R2–R10 и C1–C15 не ослаблены и не объявлены пройденными.

Дополнение подготовил Astra-аналитик как новая сессия для уточнения о старых заказах; это не продолжение прежней Sol-приёмки и не самостоятельная приёмка реализации. Документ передаётся Terra high. Продуктовый код, данные и APK этой сессией не изменены. **WMS-445 не принята:** к прежним незакрытым сценариям добавлены C16–C21, а реальный tenant «Виталик Хорс» ещё не установлен.

## Чек-лист единого финального допуска, 13.09.2026

| Проверка | Вердикт |
|---|---|
| FINAL-REVIEW — код, границы и ранее исправленные дефекты | READY. Подтверждённых открытых P1/P2 нет; [единый отчёт](../reviews/tsd-package-final-astra-20260913.md). |
| FINAL-ACCEPTANCE — фактически подтверждённое покрытие | C16–C20: независимые HTTP/DB/ViewModel проверки подтверждены, F1–F3 закрыты; native current queue итогового прохода не завершён. C21 «Виталик Хорс» остаётся отдельным реальным read-only пунктом и synthetic данными не закрывается. |
| FINAL-STAGING — допуск к выкладке | DEPLOYABLE TO STAGING. Выкладка и фактически обслуживаемая версия этим документом не подтверждены. |

## Фактическое native продолжение приёмки, 13.09.2026

**N445-WB/Ozon PASS в ограниченном объёме:** на APK12 обе реальные native очереди прочитаны со скроллом; new/in_supply/assembling/packed старше60дней и просроченные видимы, остальные fixture статусы исключены. Это не повтор всех native scope/error сценариев C20. C21 остаётся невыполненным. [Действия, точные ID и evidence](../reviews/artifacts/tsd-native-444-445-20260913/README.md). Новое code-review не проводилось; общий допуск на staging сохраняется.

| Проверка | Вердикт |
|---|---|
| NATIVE-CONTINUATION | **N445-WB/Ozon PASS в ограниченном объёме:** на APK12 обе реальные native очереди прочитаны со скроллом; new/in_supply/assembling/packed старше60дней и просроченные видимы, остальные fixture статусы исключены. Это не повтор всех native scope/error сценариев C20. C21 остаётся невыполненным. |

## Дополнение: начало работы и серверное состояние ТСД — 14.09.2026

### Факты, решение и границы

База постановки main `86835459dd9e8394f0ef6700317e42b3e7fb215e`. Аналитик прочитал актуальные AGENTS.md, навыки аналитика и сохранённое продуктовое решение, owner-cases и failure-cases. На этой базе лично проверен существующий POST /fbs/supplies/{supply_id}/start-work: общий сервис вызывает create_packaging_task_for_supply, переводит draft в assembling и in_supply-заказы в assembling; маршрут фиксирует транзакцию перед успешным ответом. Наличие этого кода не доказывает его вызов из каждого экрана ТСД или устойчивость к конкурентным повторам.

По техническому обследованию ведущего новый ТСД открывает «Подбор»/«Упаковка»/«Короба» из draft, а startWork вызывается только в packOrder. В этом новом worktree каталога mobile нет: аналитик не выдаёт переданный мобильный диагноз за самостоятельный просмотр актуального APK/source. Исполнитель должен закрепить точный mobile SHA и проверить все точки входа. Сам физический экран в данном проходе аналитиком не осматривался; сведения о нём переданы ведущим. Дизайн заново не выбирается.

**D5.** Использовать существующий общий серверный start-work и существующий workspace как источник состояния. При входе в рабочий контекст draft-поставки автоматически обеспечить сохранённое начало; тот же путь действует при прямом входе в любую операцию. Не добавлять отдельный экран, обязательную кнопку-подтверждение или локальный «начатый» статус. Чтение общего списка само по себе не начинает все поставки. Начало одной поставки не выбирает за оператора этап и не делает подбор обязательным перед упаковкой/коробами. У завершённой/отменённой поставки это правило не возвращает статус в assembling.

Данные создания задания и начала работы должны сохраняться вместе; завершённое начало не откатывается из-за отдельной последующей ошибки подбора/печати. В этом случае показывается фактическое assembling и фактический незавершённый результат конкретного действия. Ошибка или отсутствие ответа не разрешают ни рисовать успех, ни без выяснения результата повторять внешнее/складское действие.

### Дополнительные требования

| ID | Условие → действие → наблюдаемый результат | Основание |
|---|---|---|
| R13 | Оператор впервые входит в рабочий контекст существующей draft-поставки либо напрямую открывает «Подбор», «Упаковка» или «Короба» → существующий серверный процесс атомарно сохраняет supply.status=assembling и связь с одним заданием упаковки; соответствующие in_supply-заказы переходят по тому же общему контракту. ТСД допускает изменяемое действие и показывает начатую работу только после подтверждения серверного результата. При отказе начала остаётся фактическое прежнее состояние и понятная ошибка в текущем экране, без локального успеха | Новое поручение; R3–R5, B09 |
| R14 | После начала и каждого складского действия, при возврате/повторном открытии → ТСД перечитывает состояние того же серверного документа и показывает сохранённые статус, task ID, количества подбора/упаковки и состав коробов. Веб после обновления совпадает по этим значениям. При сбое перечитывания нельзя показать неподтверждённые новые количества или успех; существующие данные/контекст сохраняются для восстановления | Сервер — источник истины; R4,R6,R8,R10 |
| R15 | Ответ начала/действия потерян после commit либо одна попытка доставлена повторно/одновременно → прежде дальнейшего действия выяснить состояние той же поставки; повтор использовать как продолжение той же попытки. Не возникает второго задания, короба, подбора, упаковки, списания, начисления или внешней операции для одной попытки. Новый намеренный скан следующей единицы остаётся новой операцией | Прямое поручение без дубля; R10; AGENTS, B01/B02/B09 |
| R16 | Оператор начинает работу или создаёт пустой короб → показывается только этот сохранённый результат. Само начало/пустой короб не увеличивают picked/packed, не добавляют товар в короб и не показывают поставку собранной. Создание короба не обходит R13; после реального подбора/упаковки/раскладки показываются точные позиции и количества | Переданный production-факт qty1/picked0, пустой короб; R6/R8 |
| R17 | Повторный вход в уже assembling-поставку, WB вместо Ozon, другой документ/tenant/seller → использовать существующее задание и полномочия именно выбранной поставки, не терять её прогресс и не примешивать состояние предыдущей. Существующий дизайн, экраны и свобода выбора этапа сохраняются; готовность подбора не становится новым условием открытия упаковки/коробов. Завершённые/отменённые документы не запускаются заново | Прямое ограничение дизайна; R5/R12 и существующий жизненный цикл |

### Дополнительные проверки

Все изменения и сбои ниже воспроизводятся на собственных синтетических поставках; production-поставка используется только как read-only исходное evidence.

| Проверка | Требования | Подготовка → действие → ожидаемое наблюдение и перечитывание | Вердикт |
|---|---|---|---|
| C22 Ozon: каждый первый вход | R13,R14,R16 | Подготовить три Ozon draft с qty1, picked/packed0, без task. На итоговом APK по очереди первым открыть «Подбор», «Упаковка», «Короба» соответствующих документов; ни один путь не зависит от предварительного packOrder. До изменяемого действия сервер сохраняет assembling+один task, прогресс 0/1. Для одного документа выполнить реальные подбор/упаковку/раскладку; после каждого действия и повторного открытия сверить точные ID/qty через API/БД и существующий веб | Частично подтверждено: свежий unit-test проверил серверный draft→assembling/task для Ozon и WB, общий FbsOperationScreen обслуживает pick/pack/boxes; сохранённые нативные действия и API/БД/web не проверены |
| C23 Пустой короб и старый черновик | R13,R14,R16 | Воссоздать draft без task, qty1/picked0 и пустой open-короб №1 как в инциденте. Открыть «Короба», затем создать второй намеренно новый пустой короб → начало сохранено один раз; оба пустых короба не увеличивают подбор/упаковку/состав. Обновить ТСД и веб: нет ложного «собрано», совпадают нулевые количества и фактическое начало | Не проверено на устройстве/API/БД; по diff нет локального увеличения picked/packed или добавления box items от начала работы |
| C24 Отказ до сохранения | R13,R14,R17 | До commit start-work вернуть 500/оборвать связь. ТСД не показывает начало/сборку и не выполняет зависимый скан/создание короба; в БД нет частично созданного задания или assembling без связи. Контекст/ввод остаются, можно выйти к другой поставке. Восстановить сеть, повторить → одно начало и прежний выбранный этап, без принудительного подбора | Частично подтверждено: unit unconfirmed draft start оставил workspace=null и ошибку, scan/createBoxes не вызывались. Реальный rollback/атомарность БД и смена документа при отказе не проверены |
| C25 Потеря ответа начала | R13–R15 | Сервер сохраняет start-work, ответ не доставить; закрыть/открыть приложение и тот же документ. Приложение выясняет сохранённое assembling/task и продолжает выбранную операцию. Сверить единственность task/связей и отсутствие повторных складских/внешних последствий после повтора и веб-перечитывания | Подтверждено на уровне ViewModel: потерянный ответ start перечитан как тот же assembling/task, start вызван один раз, workspace дважды. Перезапуск APK и единственность фактов в БД не проверены |
| C26 Потеря ответа действия | R14–R16 | На начатой Ozon-поставке по отдельным попыткам оборвать ответ после сохранённых подбора единицы и создания короба; отдельно вернуть ошибку последующего workspace-read после успешного ответа упаковки. Не рисуется неподтверждённый новый результат. Восстановить чтение и продолжить ту же попытку → точные picked/packed/box items, без дублей; новый осознанный скан следующей единицы работает отдельно | Не проверено в данном ограниченном review: новые тесты относятся к start, не ко всем потерям ответа подбора/короба/упаковки. Прежние пути не выданы за новое native/API доказательство |
| C27 Повтор и конкуренция | R13,R15,R17 | Два оператора одновременно начинают одну draft-поставку; повторить start и затем открыть уже assembling с частичным прогрессом. Один task/связь, прогресс сохранён, нет сброса и лишнего начисления/списания. Повторить вход с недопустимым чужим tenant/seller ID штатным API → существующий отказ, чужое состояние не показано и не изменено | Частично подтверждено: unit resume assembling сохранил task/workspace и выбранный короб, start не вызывался. Два оператора и изоляция на настоящем backend этим прогоном не проверены |
| C28 WB и текущий экран | R13,R14,R17 | Собственный WB draft и уже assembling с нулевым/частичным подбором → первым открыть упаковку/короба, затем подбор; тот же общий start, один task, без нового порядка этапов. Перейти к другому документу и обратно, обновить веб/ТСД: статусы/прогресс совпадают. Сравнить текущие экраны на обычных/длинных названиях: сохранены дизайн Opus, расположение и доступность действий, новых экранов/элементов нет. Проверить, что чтение списка и просмотр завершённого документа не стартуют их заново | Частично подтверждено: WB draft и assembling resume прошли unit-test; все три mode используют общий экран и сохранённые элементы. Тексты/экраны не добавлены, только существующие действия ожидают workspace/busy. Полный native/web и визуальный проход не выполнен |

### Сверка и передача разработчику

«Сервер — источник истины» раскрыто R13/R14; «при первом входе/любой операции» — R13/C22; «атомарно assembling и task» — R13/C24/C27; «после каждого действия» — R14/C22/C26; «потеря ответа/без дубля» — R15/C25–C27; пустой короб из инцидента — R16/C23; WB и неизменный дизайн — R17/C28. Старые R1–R12/C1–C21 и их доказанные результаты не переписаны под этот дефект. Проверки внешней печати/передачи и остальные задачи заново не расширяются.

Постановка для Terra high готова: связать все рабочие точки входа ТСД с существующим серверным началом и перечитыванием; проверить атомарность и повторы общего backend-контракта, при необходимости исправить подтверждённый общий дефект. Не создавать новые сущности/дизайн, не чинить реальные документы напрямую. Передать точные main/mobile SHA и evidence APK→API/БД→web для одной и той же синтетической поставки. После реализации требуется перекрёстное ревью и возврат этому аналитику на C22–C28. До этого новый дефект WMS-445 остаётся открытым.

## Независимое mobile review и аналитическое заключение — 14.09.2026

Astra проверил точный diff mobile `3206669f269dcce8d97773e1bb44606f40b0b934..cef944b1b73f383c2ce71ad125853a5fe4d4d261` против постановки main `277eb66a31ea11674619477dd08e9796e028a9b0`. Актуальные AGENTS/owner/failure библиотеки прочитаны в этой сессии постановки; повторное широкое ревью старого пакета не проводилось. Изменены только FbsViewModel, FbsScreens и целевые тесты.

**Вердикт изменённого кода: PASS.** Перед публикацией workspace draft проходит существующий startWork; ответ должен содержать тот же supply ID, assembling и непустой task. При исключении/неподтверждённом ответе перечитывается та же поставка; продолжение допускается только по подтверждённому серверному состоянию. Отмена coroutine не поглощается. Вход в другую поставку очищает прежнее состояние, результат refresh публикуется только для открытого supply ID. Уже assembling проходит обычное чтение без start; прежний выбранный короб сохраняется. Все три операции используют общий экран; до загрузки workspace действия disabled, скан/createBox при null не отправляют запросы. Никаких локальных picked/packed/task/status от самого старта не создаётся. Новых экранов и элементов нет; используются прежние панели busy/error.

Техническая деталь без изменения продуктового вердикта: параметр startWork в refresh не используется как условие — начало draft выполняется при любом refresh, включая первый вход в карточку поставки. Это соответствует D5/R13 о начале при входе в рабочий контекст, а не только после packOrder. Чтение общего списка этот путь не вызывает. Не-draft не переводится локально обратно в assembling. Серверные поздние/неполные состояния и повторы операций вне нового start требуют оставшегося API/БД-прохода, а не предположения по mock.

Свежая проверка: `ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew :app:testDebugUnitTest --rerun --tests ru.wms.tsd.features.fbs.FbsViewModelTest --console=plain` — BUILD SUCCESSFUL, одна test task исполнена заново. XML FbsViewModelTest: tests=60, failures=0, errors=0, skipped=0, timestamp=2026-09-14T14:08:29. Прогон исполнителя не использован вместо независимого запуска. Новые тесты покрывают обе площадки, успешный draft start, потерю ответа с reread, неподтверждённый start без скана/короба и assembling resume без start. Последовательность API/БД/web на установленном APK ими не заменена.

Аналитическая приёмка подтверждает указанную unit/static часть R13–R17; C22–C28 остаются с конкретными границами выше. Код можно передавать на изолированную native/API/БД/web приёмку. APK в этом review не устанавливался, не публиковался и физически не принимался. Production-инцидент нельзя объявлять устранённым до этой проверки и фактического выпуска.
