# WMS-437 — Введение общего процесса разработки

## Исходный запрос и уточнения

Последнее поручение владельца, дословно:

> так вводи
> и в клод мд надо в чтоности тоже самое джоюавить
> одинаково долджна работать разработка в независимости

Предыдущий согласованный контекст (краткое изложение, не дословная цитата):
аналитик сохраняет требования и сценарии, отдельный разработчик реализует,
другая модель проводит ревью, тот же аналитик принимает результат. Ревьюеру
передаются все правила и обе библиотеки примеров. Фронт реализует Opus;
Opus проверяет Astra, Codex проверяет Opus. Подтверждённые ошибки исправляются
без ограничения двумя кругами ревью. CI проверяет заполненность документа,
без идентификаторов сессий, подписей и привязки заключения к SHA.
Старые документы MVP, упаковки и UI исключены по требованию владельца.

## Существующее состояние и цель

При постановке проверены AGENTS.md, CLAUDE.md и запись WMS-437 в подготовленной
ветке. AGENTS.md описывает новый порядок, но прямо говорит, что проверка документа
в CI ещё не подключена. CLAUDE.md всё ещё содержит «Конвейер агентов снят».
Цель — включить единый порядок в основном локальном checkout WMS для следующих
задач, независимо от входа через Codex или Claude, и подключить простой контроль
заполненности документов. Эта задача не изменяет складские экраны и не включает
деплой продукта или обещание проверенного ночного автономного выпуска.

## Требования

- **R1.** При начале следующей задачи в основном локальном checkout агент получает
  новый порядок из AGENTS.md либо CLAUDE.md; оба файла содержат одинаковый текст.
  Старое указание об отмене конвейера отсутствует. Основание: последнее поручение.
- **R2.** Ведущий явно вызывает отдельных аналитика, разработчика и независимого
  ревьюера; после реализации и ревью возвращает результат в сессию аналитика.
  Правила назначения моделей, передачи примеров и повторного исправления дефектов
  сохраняются. Основание: предыдущие согласованные уточнения.
- **R3.** Все рабочие ссылки из инструкций и скиллов разрешаются в существующие
  файлы; устаревшие MVP_DECISIONS_RU.md, PACKAGING_RU.md и UI_DESIGN_SYSTEM_RU.md
  не используются как действующие источники. Основание: поручения удалить их.
- **R4.** CI для каждой новой затронутой задачи проверяет наличие документа,
  минимум одной проверки, непустой вердикт у каждой проверки и непустое итоговое
  заключение. Отсутствие документа, проверки, любого вердикта или заключения
  даёт понятную ошибку с указанием задачи/поля. Основание: согласованный простой CI.
- **R5.** Любой непустой вердикт, включая «нарушено», допустим для проверки
  заполненности. Непустое заключение также не ограничено словом «принято».
  CI не проверяет сессии, подписи, SHA или соответствие R/C по специальной схеме.
  Исправление нарушений остаётся обязанностью участников процесса. Основание:
  прямой отказ владельца от сложного технического допуска.
- **R6.** Включение проверки не требует задним числом документов для исторических
  задач до коммита введения проверяющего скрипта. Новые изменения после этой
  границы проверяются, даже если номер задачи уже встречался раньше. Основание:
  решение аналитика о вводе процесса для новых работ без блокирования истории.
- **R7.** Результат включён в основной локальный checkout и сохранён в Git;
  существующие незакоммиченные изменения пользователя сохранены без правок.
  Статусы локальной проверки, публикации и удалённого CI сообщаются отдельно.
  Основание: поручение ввести процесс и правила сохранности проекта.

## Решения аналитика

Для одного поведения Claude/Codex используется полный одинаковый текст двух
входных файлов и простая проверка их равенства. Документ задачи расположен
в docs/requirements/WMS-NNN.md; таблица содержит колонки «Проверка» и «Вердикт»
(другие колонки допустимы), итог — раздел «Заключение». Это формат ввода для
простого проверяющего скрипта, без отдельного реестра, подписей и состояния.
Вопросов о бизнес-намерении не осталось. Продуктовые экраны не изменяются,
визуальные и складские сценарии к этой задаче не применимы.

## Проверки

| Проверка | Требования | Вердикт | Наблюдаемый результат |
| --- | --- | --- | --- |
| C1. Прочитать AGENTS.md и CLAUDE.md из основного checkout; сравнить побайтово. Ожидается новый одинаковый порядок без старого запрета конвейера. | R1, R7 | Подтверждено | Прочитаны правила основного checkout на 33cc9cf2972ec8dfa70c7457dabba87b14e17529; побайтовое сравнение AGENTS.md и CLAUDE.md успешно, старого запрета нет. |
| C2. Прочитать порядок вызовов и скиллы; проверить отдельного разработчика, независимые модели ревью, обе библиотеки целиком и возврат в ту же сессию аналитика. Сверить с фактическими вызовами этой задачи, не объявляя её проверкой складского UI. | R2 | Подтверждено | Прочитаны правила и три скилла. Отдельный разработчик ci_document_check подтверждён списком агентов; независимое Opus-ревью приложено в rollout-opus-review.md. Аналитик постановки возобновлён в этой же сессии. Порядок передачи обеих библиотек, назначения моделей и повторного исправления сохранён. |
| C3. Проверить относительные ссылки инструкций и скиллов; каждый рабочий файл существует. Проверить удаление трёх устаревших документов и отсутствие их обязательного чтения. | R3 | Подтверждено | В основном checkout проверены 22 относительные ссылки из AGENTS.md, CLAUDE.md и трёх SKILL.md: все разрешаются. Три удалённых документа отсутствуют; действующие правила не требуют их чтения. |
| C4. На временном тестовом репозитории запустить проверку с документом, проверкой, непустым вердиктом и заключением. Ожидается успех. Повторить с вердиктом «нарушено» и отрицательным заключением: проверка заполненности также успешна. | R4, R5 | Подтверждено | CLI в отдельном временном Git завершился кодом 0 с заполненными проверками; «Нарушено: две записи» и «Нужно исправить повторное сохранение» также дали код 0. Тест test_filled_failed_verdict_is_valid прошёл. |
| C5. По отдельности удалить документ, все проверки, один из нескольких вердиктов и заключение; затем оставить эти поля пустыми. Каждый дефект вызывает ошибку о соответствующем отсутствии; после заполнения повтор проходит. | R4 | Подтверждено | CLI дал код 1 отдельно для отсутствующего/пустого документа, отсутствующих проверок, пустого вердикта, отсутствующей ячейки, отсутствующего/пустого заключения. После восстановления — код 0. Реальный незаполненный WMS-437 дал девять ошибок вердиктов и одну заключения. |
| C6. Проверить документ с произвольным непустым текстом вердикта без ключа сессии, подписи и SHA. Ожидается успех. Прочитать скрипт и workflow: скрытых требований такого рода нет. | R5 | Подтверждено | CLI принял произвольные тексты «Результат зафиксирован словами» и «Ответ пока требует решения» без служебных идентификаторов. Исходник проверяет наличие/заполненность и равенство инструкций, workflow не добавляет сессий, подписей или сопоставления заключения с SHA. |
| C7. В тестовом Git создать историческую задачу без документа до введения скрипта. Сравнение через эту границу не требует её документ задним числом. Новый коммит задачи после границы, включая старый номер, проверяется. | R6 | Подтверждено | Повторно запущены Git-тесты: исторические коммиты пропущены, введение скрипта проверяется, старый номер в новом коммите требует документ, база после введения охватывает новые коммиты. Все прошли. |
| C8. Проверить вызов скрипта в workflow и защиту равенства инструкций. В тесте различающиеся AGENTS/CLAUDE обнаруживаются; одинаковые проходят. | R1, R4 | Подтверждено | Workflow вызывает скрипт в задании backlog с fetch-depth: 0. Прогон 17 тестов в основном checkout успешен, включая несовпадающие/одинаковые инструкции. Удалённый запуск CI на момент приёмки ещё не предъявлен; проверены подключение и локальное выполнение. |
| C9. Сверить заранее сохранённое состояние чужих изменений и итог в основном checkout; изменения пользователя побайтово сохранены. Проверить отдельный коммит процесса, существование файлов в основном checkout и точный отчёт о локальной/удалённой проверке без заявления о деплое. | R7 | Подтверждено | Основной checkout содержит коммит 33cc9cf2972ec8dfa70c7457dabba87b14e17529. SHA256 бинарного diff трёх чужих файлов совпал с исходным: 9b6ad285642626b2f8657023ec8dfc4ca210f577d4e2ab500e7ef9c7500b0802. Существующий незарегистрированный каталог сверки бэклога остался. Основная локальная ветка сохранена; публикация, удалённый CI и деплой не объявляются выполненными. |

## Сверка с запросом

«Вводи» покрыто R4–R7; одинаковая разработка через CLAUDE.md и AGENTS.md — R1–R3.
Распределение ролей и простой CI сохранены из предыдущих уточнений. Формат таблицы,
равенство файлов и граница применения явно обозначены как решения реализации,
а не приписаны словам владельца. Новых продуктовых ограничений не добавлено.

## Заключение

Принято в пределах введения процесса в основном локальном checkout WMS.
Аналитик постановки проверил C1–C9 в той же сессии; обязательные сценарии
подтверждены, несоответствий требованиям R1–R7 не обнаружено.

Проверены реальные файлы на коммите 33cc9cf2972ec8dfa70c7457dabba87b14e17529,
17 автоматических тестов и дополнительные вызовы CLI на ошибочных/восстановленных
документах. Подготовленный скрипт отклонял пустую приёмку WMS-437; после заполнения
документа выполнен повторный локальный запуск.

Границы результата: правила введены в канонической локальной копии, а не во всех
исторических worktree и не в удалённой ветке по умолчанию. На момент заключения
публикация ветки и удалённый прогон CI ещё не завершены. Это не проверка складского
пользовательского пути, не деплой и не доказательство автономного ночного выпуска.
