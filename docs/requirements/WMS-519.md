# WMS-519 — Сохранять геометрию Data Matrix при печати и перепечати ЧЗ

Задача зарегистрирована 24.09.2026 по дефекту печати ЧЗ на production. Постановка
подготовлена в ветке `codex/wms519-kiz-label-aspect-ratio` от `origin/etalon`
`f9ac3bc97b9c7c620f30a6992391153ad57a7618`.

Запись задачи: [канонический бэклог, WMS-519](../KANONICHESKIY_BACKLOG.md#wms-519).

## 1. Исходный запрос владельца

Переданная ведущим формулировка владельца:

> При перепечати ЧЗ из каталога, раздела «Честный знак», Chrome preview показывает
> сжатую по высоте/растянутую по ширине этикетку; на screenshot Data Matrix
> прямоугольный, а должен сохранять квадратные модули и нормально помещаться на
> физической этикетке. Нужно срочно исправить на production и убедиться, что во
> всех остальных местах перепечати ЧЗ этикетки не искажаются.

Уточнённая граница поручения:

> Исправить геометрию/масштаб печатной этикетки и унифицировать существующие
> reprint-входы через корректный общий путь, без изменения данных КИЗ,
> allocation/pool state, пользовательских процессов, экранов и бизнес-логики.

Уточнение по результату production-проверки после первой выкладки:

> Свежий screenshot Chrome preview с Xprinter после deploy всё ещё показывает
> сильно растянутый по горизонтали Data Matrix. Сохранённый
> `label_artifact_pdf` / native PDF при перепечати обошёл исправленный
> `bwip-js` fallback renderer. Каждая точка перепечати ЧЗ независимо от источника
> этикетки должна выдавать квадратные модули; другой UI и бизнес-поведение менять
> нельзя.

## 2. Исходное evidence и подтверждённый симптом

Скриншот владельца от 24.09.2026 сохранён в исходной переписке. Полученный файл
имел размер 1334×764 px и SHA-256
`e5b156ff383e08976712d96aa5395c9fe9243bd6c9e228fe7a6aef52fc3f425c`.
На нём видны Chrome print preview, одна страница, выбранный принтер
`Xprinter XP-370B` и ЧЗ-этикетка с явно неравномерно растянутым Data Matrix:
горизонтальный шаг модулей больше вертикального. Это screenshot evidence дефекта,
но не доказательство его причины и не физическая проверка напечатанной наклейки.

После выкладки получен второй screenshot
`/Users/deniscivkunov/Downloads/2026-09-24 13.02.59.jpg`, 1273×745 px, SHA-256
`54c1cdaa8d38c6755166614139cdc101bf896c0253d24bb77dfec3d44e25532e`.
На нём в интерактивном Chrome print preview выбран `Xprinter XP-370B`, показана
одна страница, а Data Matrix по-прежнему сильно растянут по горизонтали. Это
прямое production evidence того, что инцидент не закрыт: уже до отправки на
физическую печать фактический reprint output нарушает R1/R3/R6/R9.

В текущем коде подтверждены следующие факты:

- штатный базовый размер термоэтикетки — 58×40 мм; также уже поддерживаются
  60×80, 60×40 и 70×120 мм, а в конструкторе ЧЗ есть существующая ориентация;
- HTML-печать задаёт выбранный размер через `@page` и строит ЧЗ в
  `frontend/src/utils/printMarkingCodeLabel.ts`;
- Data Matrix генерируется `bwip-js` с общим `scale: 4` и отдельным
  `height: 12`, после чего растровое изображение помещается в квадратный CSS-блок;
  тесты проверяют наличие квадратного внешнего блока, но не квадратность самих
  модулей растра. Сочетание является подтверждённой зоной риска и должно быть
  проверено исправлением, однако окончательная причина устанавливается тестом
  фактического raster output, а не предположением по одной опции;
- `printMarkingCodeLabels` → `printMarkingCodeTape` →
  `buildMarkingTapeSections` → `buildMarkingTapeDocument` →
  `printHtmlInIframe` — общий browser-print путь для точного отсканированного КИЗ;
- если у кода сохранён `label_artifact_pdf`, чистая ЧЗ-лента может печататься
  нативным PDF и обходить генерацию Data Matrix через исправленный `bwip-js`
  fallback renderer. Равномерное вписывание страницы не исправляет символ,
  который уже искажён внутри сохранённого PDF. Именно этот путь теперь
  подтверждён как пропущенный первой реализацией. В смешанной ленте PDF
  превращается в PNG и помещается через `object-fit: contain`; фактический output
  этого пути также должен проверяться, а не считаться исправленным по CSS;
- локальный print-agent в текущем `origin/etalon` обслуживает другие готовые
  FBS/sorting assets. Отдельного agent endpoint для КИЗ-перепечати не найдено;
  текущая перепечать ЧЗ вызывает browser `window.print()`;
- в проверенном кандидате WMS-514 `8069a12515d6512551c1c123d0f5b8581684fb57`
  мгновенная печать/перепечать после скана также вызывает тот же
  `printMarkingCodeLabels`. Поэтому исправленный общий renderer обязан покрыть и
  этот путь при совместном включении задач; новый второй шаблон для WMS-514 не нужен.

## 3. Точки печати и перепечати, которые входят в регрессионный охват

| Контекст | Действие | Фактический путь на исследованной базе |
|---|---|---|
| FF → «Честный знак» → «Перепечатать ЧЗ» | скан с немедленным запуском печати; значок у одной строки; «Печать всё» | `HonestSignScreen` → `KizReprintDialog` → `printMarkingCodeLabels` |
| FF → возвратная приёмка ЛЗ/WB/Ozon → «Перепечатать ЧЗ» | те же три действия общей модалки | `FfInboundRequestView` → `KizReprintDialog` → `printMarkingCodeLabels` |
| FF → «Честный знак», строка товара | штатная печать; если коды уже печатались — существующая повторная печать выбранных кодов | `HonestSignScreen` → `MarkingPrintDialog` → общий tape renderer или сохранённый `label_artifact_pdf` / native PDF; второй путь воспроизвёл дефект после deploy |
| FF → задание «Упаковка», меню строки → «Повтор» | повторная печать выбранных ранее выданных кодов | `FfPackagingPage` → `MarkingPrintDialog` → общий tape renderer или сохранённый `label_artifact_pdf` / native PDF |
| FBS → «Упаковка и маркировка» | круговая стрелка у точного operator КИЗ; меню «Перепечатать» заказа | `FfFbsSupplyWorkspace` → `MarkingPrintDialog` → FBS tape → общий tape renderer |
| FBS WMS-514 при совместном выпуске | мгновенная печать ЧЗ и точная перепечать ЧЗ после скана | `FfFbsSupplyWorkspace` → `printMarkingCodeLabels` → общий browser renderer |

Экран «Осталось промаркировать», обычная выдача ЧЗ из пула, первичная печать из
каталога/упаковки и смешанная лента ЧЗ+ШК/QR не являются новыми reprint-входами,
но используют тот же renderer и входят в защиту от регрессии. Устаревший
`MarkingProductCodesDialog` помечен в коде как не монтируемый в UI и не считается
отдельным пользовательским входом.

## 4. Продуктовое решение

Результат исправляется для всех существующих печатных контуров, а не только для
одного renderer. Для сгенерированного по полному КИЗ Data Matrix растр или вектор
обязан иметь одинаковый масштаб по X и Y. Для сохранённого `label_artifact_pdf`,
нативного PDF и PNG fallback проверяется фактический символ на итоговой странице:
равномерное вписывание уже искажённого артефакта само по себе не выполняет
требование. Контракт не предписывает способ исправления источника; он требует
квадратные модули на выходе каждого существующего reprint-входа.

Существующий набор размеров, ориентация, кнопки, модалки, порядок действий,
автоматический запуск `window.print()` и выбор системного принтера не меняются.
Новый preview, настройка размера, подтверждение, состояние КИЗ или серверная
сущность для этого дефекта не нужны. Если WMS-514 включается в тот же релиз,
мгновенный путь наследует исправленный renderer и не вводит собственную разметку.

## 5. Требования

| ID | Условие, действие и наблюдаемый результат | Основание |
|---|---|---|
| R1 | На итоговой печатной странице Data Matrix из полного КИЗ имеет квадратные модули независимо от источника: новая генерация, сохранённый `label_artifact_pdf`, native PDF или PNG fallback. Запрещено растягивать или сжимать символ по одной оси; квадратный внешний `img`, контейнер или равномерное вписывание всей страницы не считаются выполнением требования, если сам символ уже искажён. | Прямой дефект на исходном и post-deploy screenshot владельца. |
| R2 | Data Matrix сохраняет неповреждённую светлую quiet zone (свободное поле вокруг символа), не обрезается контейнером/страницей и остаётся считываемым штатным сканером после физической печати. | Прямое требование о читаемости и безопасная геометрия 2D-кода. |
| R3 | ЧЗ-этикетка равномерно вписывается в уже выбранные размер и ориентацию. Основной production-сценарий Xprinter XP-370B на 58×40 мм помещает Data Matrix и существующий информационный блок без обрезки; существующие 60×80, 60×40 и 70×120 мм не получают неравномерного scaling. Новый размер или дизайн не вводится. | Screenshot, текущий принтер и существующий источник размеров. |
| R4 | На один КИЗ создаётся ровно одна печатная страница выбранного размера: нет обрезанной второй страницы, пустого дополнительного листа, переноса части полного кода или служебного блока за пределы этикетки. Лента из N этикеток содержит ровно N страниц в заданном порядке. | Требование нормального размещения на физической этикетке. |
| R5 | Исправленный результат применяется ко всем фактическим reprint-входам из раздела 3 и ко всем их веткам источника: скан/строка/«Печать всё» общей модалки, повтор из каталога и упаковки с `label_artifact_pdf` и без него, оба FBS-входа и мгновенная WMS-514-перепечать при совместном выпуске. Отдельный экранный шаблон не создаётся. | Требование проверить все места и подтверждённый пропуск native PDF ветки. |
| R6 | Для сгенерированного ЧЗ, сохранённого `label_artifact_pdf` / native PDF и PNG fallback смешанной ленты итоговый Data Matrix сохраняет квадратные модули и целиком равномерно вписывается в страницу. Проверяется фактический output каждого источника; сохранение aspect ratio файла недостаточно, если символ внутри файла уже искажён. Способ получения корректного output этим контрактом не предписывается. | Реально существующие источники этикетки и post-deploy воспроизведение дефекта на native PDF ветке. |
| R7 | В печать передаётся тот же полный канонический КИЗ, включая допустимые GS-разделители и криптохвост. Исправление не нормализует, не сокращает, не заменяет и не повторно выделяет КИЗ; текст под символом не является источником данных для Data Matrix. | Жёсткая граница владельца и сохранность маркировки. |
| R8 | Для длинного полного КИЗ renderer выбирает допустимый Data Matrix и размер модулей так, чтобы символ целиком помещался с quiet zone и сканировался. Нельзя добиться размещения неравномерным scaling, обрезкой или удалением части КИЗ. | Явное требование контроля длинного полного КИЗ. |
| R9 | Browser print preview на каждом reprint-пути и для каждого источника этикетки отображает ту же геометрию, которая передаётся в `window.print()`: квадратные модули, правильная страница, отсутствие обрезки и второго листа. Системный preview/диалог не заменяется новым окном WMS и не получает новых операторских шагов. | Исходный и post-deploy дефекты наблюдаются в Chrome preview; фронт менять сверх печатной геометрии нельзя. |
| R10 | Если мгновенный/тихий путь WMS-514 включён, он использует тот же исправленный документ и запускает печать сразу по действующему контракту, без WMS preview или нового подтверждения. Если в конкретной сборке КИЗ передаётся локальному агенту, агент получает aspect-safe готовый artifact с тем же размером страницы; отдельного локального шаблона не создаётся. На исследованной базе отдельного KIZ local-agent path нет. | Совместимость с уже подготовленным мгновенным скан-процессом без расширения WMS-519. |
| R11 | Печать и перепечать не меняют allocation/pool state, привязку КИЗ к заказу/товару, историю, статус упаковки, остатки, резервы и marketplace metadata иначе, чем они менялись до WMS-519. Ошибка renderer/печати не создаёт новый КИЗ и не переключает бизнес-ветку. | Жёсткая граница владельца: только геометрия и общий печатный путь. |
| R12 | Экраны, тексты, кнопки, модалки, шрифты, таблицы, порядок действий и доступность существующих операций остаются прежними. Допустимы только внутренние изменения renderer/style печатного документа и узкое подключение существующих входов к общему корректному пути. | Прямой запрет менять пользовательские процессы и дизайн. |
| R13 | Обычная первичная печать ЧЗ, приёмка возвратов, FBS, ручная упаковка, «Осталось промаркировать», печать ШК/QR и существующие manual reprint-действия продолжают работать в прежнем порядке. Исправление Data Matrix не меняет геометрию товарного ШК или QR заказа. | Обязательная регрессия соседних процессов. |

## 6. Проверки для приёмки

| Проверка | Требования | Подготовка и действие | Ожидаемый результат | Вердикт |
|---|---|---|---|---|
| C1 | R1, R2, R8 | На синтетическом полном GS1 КИЗ с GS-разделителем и длинным криптохвостом получить фактический raster/vector Data Matrix из ветки новой генерации production renderer. Измерить повторяющийся шаг модулей по X и Y и светлое поле вокруг символа; декодировать получившийся символ. | Шаг по X и Y одинаков в пределах одного пикселя/округления renderer; ни один край символа и quiet zone не обрезаны; decoder возвращает исходные байты полного КИЗ. | **Подтверждено программно только для ветки новой генерации.** Независимый before/after raster для короткого и длинного КИЗ воспроизвёл старые модули 8×6 и 8×4 px; исправленный `bwip-js` renderer дал 8×8 px и непрозрачное белое поле 16 px, то есть два модуля, со всех сторон. ZXing декодировал оба исправленных растра байт-в-байт, включая два ASCII GS длинного КИЗ. Проверка не покрывает сохранённый `label_artifact_pdf` и не является приёмкой всех reprint-путей. |
| C2 | R3, R4, R9 | Для 58×40 мм в штатной ориентации открыть headless Chrome preview документа, собранного веткой новой генерации без сохранённого PDF-артефакта. Сравнить число страниц, геометрию символа и размещение блока. | Одна этикетка — одна страница 58×40; модули квадратные, Data Matrix и текст внутри страницы, пустого/обрезанного второго листа нет. | **Подтверждено программно только для сгенерированного документа в headless Chrome.** Фактические `toCanvas` и печатный документ для короткого/длинного КИЗ дали квадратную матрицу; визуально проверенная 58×40 страница содержит матрицу и информационный блок без crop. Два КИЗ дали ровно две PDF-страницы. Post-deploy screenshot фактической native PDF ветки нарушил C10; результат C2 нельзя переносить на неё. |
| C3 | R3, R4, R6 | Для 58×40, 60×80, 60×40, 70×120 и существующих ориентаций проверить фактический output каждого источника: новая генерация, реальный сохранённый `label_artifact_pdf` / native PDF и mixed-tape PNG fallback. Для PDF использовать артефакт из реальной записи, а не синтетическую квадратную страницу без внутреннего искажения. | В каждом итоговом output модули Data Matrix квадратные, символ и страница не обрезаны; для N этикеток ровно N страниц. | **Нарушено.** Headless Chrome подтвердил четыре размера × две ориентации только на подготовленном программном контуре, а тесты native PDF проверяли равномерное вписывание страницы, не геометрию модулей внутри реального `label_artifact_pdf`. Post-deploy Chrome preview реальной перепечати показал сильно растянутый Data Matrix: сохранённый native PDF обошёл исправленный fallback renderer. PNG fallback на реальном сохранённом артефакте также должен быть воспроизведён заново. |
| C4 | R5, R7, R11 | В `/app/ff/honest-sign` открыть «Перепечатать ЧЗ»: последовательно проверить автопечать после скана, значок одной сохранённой строки и «Печать всё». Сравнить переданные полные КИЗ и состояние pool/history до и после. | Все три действия используют общий исправленный renderer; печатаются точные исходные КИЗ, геометрия одинакова, пул/привязки/история меняются только по прежнему контракту WMS-489. | **Подтверждено на программном контуре.** Source/call-path тест доказал два вызова `printMarkingCodeLabels` внутри общей модалки и её монтаж в `HonestSignScreen`; отдельного `bwip-js` там нет. Полный `cis` передаётся renderer без новой нормализации. Product diff не содержит API/DB или pool/history-кода. Реальный операторский browser-проход не выполнялся. |
| C5 | R5, R11, R13 | В возвратной приёмке отдельно пройти ЛЗ, WB и Ozon: скан-автопечать, одиночную и общую повторную печать. | Геометрия совпадает с C4; приёмка, строки, сканы и бизнес-состояния не изменены WMS-519. | **Подтверждено на программном контуре.** `FfInboundRequestView` монтирует ту же `KizReprintDialog`, поэтому все три действия используют исправленный общий renderer. Код приёмки, marketplace-развилки и состояние не менялись. Реальные документы ЛЗ/WB/Ozon в браузере не проходились. |
| C6 | R5, R6, R11, R13 | На реальных строках маркируемого товара раздела «Честный знак» и обычного задания «Упаковка» выполнить повтор ранее напечатанных кодов: отдельно для записи с сохранённым `label_artifact_pdf` и для записи без него. Зафиксировать выбранную ветку источника и фактический Chrome preview. | Оба входа и обе ветки источника дают Data Matrix с квадратными модулями; повтор не выделяет новый КИЗ и не меняет прежние состояния. | **Нарушено.** В production-перепечати из каталога запись с `label_artifact_pdf` выбрала native PDF путь и показала в Chrome preview сильно растянутый Data Matrix. Предыдущий call-path тест подтвердил только маршрутизацию и равномерное вписывание файла, но не геометрию символа внутри фактического артефакта. Состояния/выделение КИЗ этим evidence не проверялись и не заявляются нарушенными. |
| C7 | R5, R7, R11, R13 | В FBS WB и Ozon проверить круговую стрелку у точного operator КИЗ и меню «Перепечатать» заказа; отдельно проверить обычную печать и смешанную ленту ЧЗ+ШК/QR. | Перепечатывается выбранный точный КИЗ с правильной геометрией; обычная печать, товарный ШК, QR, привязка и упаковка работают как раньше. | **Подтверждено на программном контуре.** FBS остаётся на существующем `MarkingPrintDialog` и общем tape renderer; QR, товарный ШК, FBS API и привязки отсутствуют в product diff. Source invariants и 48 адресных frontend-тестов прошли. Реальные WB/Ozon-заказы и бумажная печать не проверялись. |
| C8 | R5, R9, R10 | На сборке, содержащей WMS-514, пройти шесть конфигураций галок, в которых фактически запускаются печать или перепечать ЧЗ; для reprint проверить скан без и после QR заказа. | Мгновенный вывод использует тот же aspect-safe документ и прежнюю WMS-514-логику; WMS-519 не добавляет окно/подтверждение и не меняет mutex/QR-поведение. | **Частично подтверждено, совместная сборка не проверена.** Публичная сигнатура `printMarkingCodeLabels` сохранена, а ранее проверенный WMS-514 вызывает именно её; отдельного шаблона нет. Frozen hotfix `bd641fdb` не содержит WMS-514, поэтому шесть конфигураций галок, scan без/после QR и мгновенный вывод должны быть повторены на объединённом release candidate. Это не блокирует отдельный WMS-519 hotfix, но блокирует утверждение совместимости объединённого выпуска. |
| C9 | R1–R4, R6, R8 | На физическом Xprinter XP-370B с фактически используемой этикеткой 58×40 для веток с `label_artifact_pdf` и без него распечатать короткий и длинный полный КИЗ без ручной коррекции; отсканировать каждый штатным складским сканером несколько раз с обычного рабочего расстояния. | Для обоих источников этикетка целиком на одном носителе, модули визуально квадратные, quiet zone сохранена, оба КИЗ стабильно считываются и байт-в-байт совпадают с исходными. | **Нарушено.** Post-deploy интерактивный Chrome preview с выбранным Xprinter XP-370B на фактической native PDF ветке уже до печати показывает сильно растянутые по горизонтали модули, поэтому ожидаемый физический output не допускается к PASS. Печать на бумаге и считывание складским сканером не выполнены; после исправления C9 нужно повторить полностью для обоих источников. |
| C10 | R1, R3, R6, R9 | Для каждой ветки источника (`label_artifact_pdf` / native PDF, новая генерация и mixed-tape PNG fallback) открыть фактический интерактивный Chrome preview с выбранным Xprinter, сверить размер бумаги, масштаб/ориентацию и геометрию модулей; сохранить screenshot. | В каждом preview модули квадратные, страница не обрезана и исходный дефект не воспроизводится; драйвер не добавляет неравномерный масштаб. | **Нарушено.** На post-deploy screenshot 1273×745 px с выбранным `Xprinter XP-370B` Data Matrix сохранённого native PDF по-прежнему сильно растянут по горизонтали. Исправленный headless `bwip-js` fallback не покрывает фактический путь перепечати. Paper preset/100% scaling отдельно не зафиксированы, но наблюдаемого нарушения геометрии уже достаточно для FAIL; после исправления проверка повторяется по всем трём веткам. |
| C11 | R11, R12, R13 | Сравнить UI screenshots и API/DB effects до/после; выполнить адресные frontend-тесты, TypeScript, production build и тесты PDF fitting. | В UI нет новых или изменённых элементов; product diff ограничен общим renderer/style и подключением; данные и соседние процессы не изменены. | **Подтверждено программно и после deploy по составу версии.** Product diff `2d2c73dd..bd641fdb` ограничен общим renderer, его типами и тестами; экранов/API/DB нет. Аналитик повторно получил 48/48 frontend-тестов, 7/7 PDF-fitting тестов, чистые `tsc --noEmit`, production build и `git diff --check`. После выкладки readback production-кода подтвердил `scale: 4`, `padding: 4`, белый фон и отсутствие отдельного `height`; единственное визуальное изменение находится внутри печатной ЧЗ-этикетки и соответствует задаче. Реальный операторский UI-проход после deploy не выполнялся и не подменяет C9/C10. |
| C12 | R1–R13 | Независимое ревью сопоставляет итоговый diff со всеми входами и ветками источника из раздела 3, включая фактический сохранённый `label_artifact_pdf`, и проверяет отсутствие обхода исправленной геометрии. | Все активные входы и источники дают aspect-safe output; нет непроверенной ветки, сохраняющей искажённый символ; UI и бизнес-логика не расширены. | **Нарушено по полноте охвата.** Astra high дала PASS на frozen SHA `bd641fdb`, но post-deploy evidence выявил пропущенную ветку: native PDF reprint обходит исправленный `bwip-js` fallback. Ревью и программные тесты не воспроизвели реальный сохранённый артефакт и потому не доказали R5/R6 для всех входов. После исправления требуется новое независимое ревью фактических веток; прежний PASS остаётся историческим результатом для проверенного diff, но не является приёмкой WMS-519. |

## 7. Граница программной и физической приёмки

Кодовые тесты могут доказать одинаковый коэффициент масштаба, сохранность байтов
КИЗ, количество страниц и размер `@page` только для реально поданного им
источника. Синтетическая генерация не доказывает ветку сохранённого
`label_artifact_pdf`; равномерное вписывание PDF-страницы не доказывает
квадратность модулей внутри неё. Поэтому программный набор обязан воспроизводить
фактический persisted artifact и каждую ветку выбора источника.

Кодовые тесты также не доказывают настройки драйвера, фактический размер рулона,
качество термоголовки и считывание бумажной наклейки. C10 проверяется реальным
интерактивным preview. C9 окончательно закрывается только физической печатью и
сканированием на рабочем Xprinter XP-370B, но может получить FAIL раньше: если
preview уже содержит прямоугольные модули, ожидаемый физический output заведомо
не соответствует требованию и печать не нужна для фиксации нарушения.

Если production-рабочее место использует не 58×40 мм, перед физической приёмкой
фиксируется фактический выбранный штатный размер из уже поддерживаемого списка.
Это не требует нового продуктового решения: алгоритм обязан сохранять пропорции
на каждом существующем размере.

## 8. Сверка постановки

Исходный и post-deploy прямоугольный Data Matrix покрыты R1–R4/R6/R9 и
C1–C3/C6/C9/C10. Раздел «Честный знак», все найденные reprint-входы и отдельные
ветки с сохранённым `label_artifact_pdf` и без него покрыты R5 и C4–C8. Quiet zone и реальная
считываемость покрыты R2, R8 и C1/C9. Длинный полный КИЗ и сохранность его байтов
покрыты R7–R8. Chrome preview, отсутствие обрезки и второй страницы покрыты
R4, R9 и C2/C3/C10. Мгновенный WMS-514-путь и возможный local-agent artifact
покрыты R10/C8 без добавления нового механизма. Запрет менять pool/allocation,
процессы и UI покрыт R11–R13 и C4–C12.

В постановку не добавлены новый размер бумаги, новый экран, настройка, модалка,
подтверждение или бизнес-ограничение. Единственное продуктовое изменение —
корректная геометрия существующей печатной этикетки на каждом текущем пути.

## Заключение

### Предварительная программная проверка 24.09.2026 — исторический результат

Frozen product SHA `bd641fdb9f48f01abebde60f17cfd7b281cd15fb` устраняет
доказанную программную причину дефекта: отдельный `height: 12` масштабировал
Data Matrix только по Y. Новый общий renderer применяет один `scale: 4`, добавляет
симметричную quiet zone в два модуля и не меняет полный КИЗ. Before/after raster,
ZXing с ASCII GS, реальный headless Chrome, размеры страниц, общий маршрут вызовов,
48 повторно пройденных frontend-тестов, 7 PDF-fitting тестов, TypeScript и
production build подтвердили исправление ветки новой генерации. Astra high дала
PASS без findings на этом же SHA, но не воспроизвела реальный сохранённый
`label_artifact_pdf`.

Этот предварительный PASS **отменён как итоговая приёмка WMS-519** последующим
production evidence. Он остаётся доказательством только того, что исправленный
`bwip-js` fallback выдаёт квадратные модули; он не доказывает все reprint-входы.

### Выкладка production 24.09.2026

WMS-519 выложена из изолированной ветки `codex/wms519-prod-hotfix`: точный SHA
production checkout и его upstream после выкладки —
`270215a5c065e4f45293efba45b0a018311840ad`. Этот checkout содержит программно
принятый product commit `bd641fdb9f48f01abebde60f17cfd7b281cd15fb` и отдельную
документальную приёмку, не изменяющую продуктовый код.

Интеграционный PR #243 влит в `etalon` коммитом
`8249746928b87f7ba138a7e631d017c990fe1730`; CI run `35971240661` успешно прошёл
проверки backlog, frontend-build и backend. Deploy guard подтвердил целевой
`270215a5` относительно `origin/etalon` `82497469`. `prod-update.sh` завершился с
кодом 0: private backup проверен, миграции выполнены, сервисы пересозданы и
запущены. После выкладки `api`, `worker`, `beat` и `web` работают, `db` и `redis`
healthy; корневой URL, seller URL и API health вернули HTTP 200, health body —
`ok`. Readback renderer на production подтвердил `scale: 4`, `padding: 4`, белый
фон и отсутствие отдельного `height`.

**Выкладка и доступность сервисов: подтверждены.** Эти факты доказывают только,
что SHA был выложен и сервисы после обновления доступны. Они не доказывают
выполнение продуктового требования и не являются закрытием инцидента.

### Повторная production-приёмка 24.09.2026 — FAIL

Screenshot `2026-09-24 13.02.59.jpg` с фактического рабочего пути после deploy
показывает в Chrome preview выбранный Xprinter XP-370B и по-прежнему сильно
растянутый по горизонтали Data Matrix. Подтверждена причина неполного охвата:
перепечать использовала сохранённый `label_artifact_pdf` / native PDF и обошла
исправленный `bwip-js` fallback renderer. Равномерное вписывание PDF сохранило
пропорции страницы, но не исправило уже искажённые модули внутри артефакта.

**Итоговая продуктовая и production-приёмка WMS-519: НАРУШЕНА.** C3, C6, C9,
C10 и C12 имеют FAIL. Инцидент не закрыт, хотя deploy технически успешен. C9
также требует после исправления физической печати короткого и длинного КИЗ из
обеих веток и считывания штатным сканером; эта часть ещё не выполнялась.

Для повторной приёмки нужно предъявить исправленную реализацию без изменения UI
и бизнес-логики, затем воспроизвести фактический `label_artifact_pdf`, native PDF,
новую генерацию и mixed-tape PNG fallback в интерактивном Chrome preview и
завершить C9 на бумаге. Контракт фиксирует требуемый результат, но не предписывает
технический способ исправления.

C8 остаётся отдельным integration gate для будущего объединённого кандидата с
WMS-514: шесть конфигураций галок и scan без/после QR на совместной сборке не
запускались. Текущий FAIL WMS-519 должен быть устранён до утверждения
aspect-safe результата в любом объединённом кандидате.
