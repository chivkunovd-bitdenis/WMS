# WMS-507 · AVpack: вернуть разрешённое переключение между кабинетами селлеров

## Исходный запрос и уточнения

После разбора действующего ограничения владелец уточнил смысл исходной изоляции WMS-488:

> «я же просил сделать так чтобы ЕГО ТОВАРЫ БЛЯДЬ не были видны»

> «Бля, ты поправил проблему по Webpack, ты её реально исправил системно, но при этом ему заблокировал остальные аккаунты.»

Финальное поручение владельца 22.09.2026:

> «убирай этот запрет»

До этого владелец подтвердил целевой рабочий процесс: один менеджер управляет несколькими
юрлицами; при выборе одного из разрешённых селлеров он работает в полном кабинете именно
этого селлера — с его товарами, приёмками, отгрузками, FBS, Честным знаком и интеграциями.
Данные разных селлеров не должны смешиваться.

## Существующий процесс и цель изменения

Пользователь seller-портала имеет домашний `User.seller_id`. Право работать с несколькими
кабинетами задаётся явным `User.can_manage_seller_shops`; конкретные разрешённые кабинеты —
включёнными строками `SellerShopDelegation` того же арендатора. После переключения
`POST /auth/switch-seller` выдаёт токен с активным `seller_id`, а серверная функция
`resolve_effective_seller_id` обязана повторно проверить живое разрешение и использовать
активного селлера как область рабочих данных.

WMS-488 добавила исключение по точному slug AVpack `avpack-9uczh`: функции
`uses_home_seller_scope` и `can_manage_seller_shops` принудительно подменяют активного
селлера домашним и скрывают уже выданные делегации. В результате системная изоляция данных
сохранена, но законная работа менеджера с остальными юрлицами заблокирована.

Цель WMS-507 — удалить именно это исключение AVpack и вернуть ему общесистемную модель
явных делегаций. Область данных после переключения остаётся равной одному активному
`seller_id`; общий смешанный кабинет не появляется.

## Требования

| ID | Проверяемое поведение | Основание |
|---|---|---|
| R1 | Пользователь seller-портала AVpack с `can_manage_seller_shops=true` видит в `/auth/me` своё право управления магазинами, домашний кабинет и существующие разрешённые делегации. Включённые делегации входят в список доступных для переключения магазинов так же, как у менеджера любого другого арендатора. | Поручение «убирай этот запрет»; подтверждённая владельцем модель одного менеджера для нескольких юрлиц. |
| R2 | Такой менеджер может переключиться только на селлера своего арендатора, для которого у него есть включённая `SellerShopDelegation`. После переключения новый токен и `/auth/me` показывают выбранный кабинет как активный; домашний селлер пользователя при этом не меняется. | Действующая системная модель явных разрешений; владелец требует полноценную работу под выбранным кабинетом. |
| R3 | После переключения товары, приёмки, отгрузки, FBS, Честный знак и интеграции работают в области выбранного активного `seller_id`: видны и изменяются данные выбранного селлера, а данные домашнего и остальных селлеров не подмешиваются. Возврат в домашний кабинет снова даёт только домашнюю область. | Уточнение владельца: скрыть чужие товары, не блокируя разрешённые кабинеты; перечисленный владельцем рабочий процесс. |
| R4 | Обычный пользователь AVpack без явного флага управления магазинами остаётся в домашнем кабинете. Менеджер не может переключиться на селлера без делегации, с выключенной делегацией, из другого арендатора или на тестового селлера; подделанный либо ранее выданный токен не обходит текущую проверку разрешения. | Сохранение системной изоляции и действующих границ клиентов; задача отменяет только лишнее AVpack-исключение. |
| R5 | Удаление AVpack-исключения не меняет существующие `can_manage_seller_shops` и `SellerShopDelegation`, не создаёт новых назначений и не требует миграции данных. Существующее включение и выключение заранее разрешённых магазинов продолжает работать через текущий интерфейс и API. | Владелец просит снять запрет, а не переназначить права; исторические grants уже сохранены. |
| R6 | Поведение менеджеров других арендаторов не меняется: они по-прежнему переключаются только в явно разрешённые кабинеты, а обычные селлеры остаются в домашнем. Роль фулфилмента AVpack и её существующий доступ к селлерам не меняются. | Ограничение задачи — исправить ошибочную AVpack-заглушку без расширения прав и соседних процессов. |
| R7 | В рамках WMS-507 не добавляется административный интерфейс выдачи делегаций и не перерабатываются независимые участки системы, которые исторически используют домашний scope. Если адресная проверка обнаружит, что такой участок мешает прямо названному сценарию R3, это фиксируется как конкретное несоответствие, а не скрыто расширяется в общий рефакторинг. | Явная граница постановки ведущего: сейчас убрать запрет и защитить регресс тестами; административный UI и второстепенные home-scope участки не входят. |

## Продуктовое решение

Для AVpack применяется уже существующее общее правило: домашний кабинет доступен всегда,
дополнительный — только при явном флаге менеджера и включённой делегации тому же арендатору.
Выбор кабинета меняет один активный `seller_id`, поэтому безопасность обеспечивается
проверкой разрешения и seller-scoped запросами, а не запретом переключения для всего
арендатора.

Отдельного продуктового выбора не осталось. Специальное правило WMS-488 «любой AVpack
seller-role всегда home-only» отменено прямым уточнением владельца. Остальные исправления
WMS-488 — фильтрация по seller scope, закрытие прямых обходов и межвкладочная очистка
сессии — откатывать нельзя.

## Проверки для приёмки

| Проверка | Требования | Подготовка и действие → ожидаемый результат | Вердикт |
|---|---|---|---|
| C1 · разрешённое переключение AVpack | R1, R2, R5 | В тестовом tenant с точным slug `avpack-9uczh` создать домашнего селлера A, селлера B и пользователя A с явным флагом и включённой делегацией B. Войти под A, открыть `/auth/me`, переключиться на B и повторно открыть `/auth/me` → право менеджера истинно, A остаётся домашним, A и B доступны, активным и текущим `seller_id` становится B; строки прав в БД не меняются. | **Подтверждено на API/БД-тестах.** `test_avpack_delegated_tokens_and_all_grants_stay_unchanged` проверил AVpack с пятью включёнными делегациями: `/auth/me` возвращает manager=true, домашний A и активный B раздельно, switch выдаёт рабочий токен B, снимок flag/grants до и после совпадает. |
| C2 · изоляция активного кабинета | R2, R3 | Создать разные товары и относящиеся к ним данные рабочих сценариев A и B. Под активным B проверить товары, приёмки, отгрузки, FBS, Честный знак и настройки/синхронизации интеграций доступными адресными API-путями → возвращаются и изменяются только данные B; данные A и третьего селлера C отсутствуют и неизменны. Переключиться обратно на A → видны только данные A. | **Подтверждено адресными API/БД-тестами.** Активный B используется в каталоге, прямых товарных действиях, FBS-правилах, приёмке, отгрузке, marketplace unload, Честном знаке, WB/Ozon sync, создании и чтении фоновых задач и storage rebuild; A и третий tenant не читаются и не изменяются. Возврат на A проверен отдельным запросом каталога. Реальный браузер не открывался. |
| C3 · обычный AVpack-селлер | R4 | В том же tenant войти пользователем без `can_manage_seller_shops` и без делегаций, запросить `/auth/me`, попытаться переключиться на B и открыть данные B прямым ID → доступен только домашний кабинет, переключение и чужой объект отклонены без выдачи нового токена и без изменения данных. | **Подтверждено для двух состояний БД.** Без flag обычный AVpack-селлер остался на A как при отсутствии grants, так и при сохранённых исторических grants; `/auth/me` показывает только A, switch даёт 403 без токена, прямой товар B — 404, данные прав неизменны. |
| C4 · границы делегации | R4 | Для AVpack-менеджера последовательно попробовать выключенную делегацию, селлера без делегации, тестового селлера и селлера другого tenant; отдельно использовать подписанный токен с каждым таким `seller_id` → ни один кабинет не становится активным, данные не раскрываются. Затем отключить ранее разрешённую B и повторить запрос со старым токеном B → сервер отклоняет его по живому состоянию разрешения. | **Подтверждено.** Четыре отдельные ветки `disabled`, `ungranted`, `foreign`, `test` получили 403 и при switch, и с подписанным токеном. После выключения B старый токен B получил 403 на `/auth/me` и `/products`; записей и новых прав не появилось. |
| C5 · включение заранее разрешённого магазина | R1, R5 | Под AVpack-менеджером выключить и снова включить заранее заведённую делегацию текущим `PUT /auth/seller-shops`, обновить `/auth/me` и переключатель → состояние сохраняется и определяет возможность переключения; неизвестный seller ID не становится разрешённым. | **Подтверждено.** `PUT /auth/seller-shops` убрал B из switchable, после повторного включения вернул рабочий switch; неизвестный seller другого tenant получил 422. Финальный снимок пяти исходных grants и flag совпал с исходным. |
| C6 · соседние арендаторы и роль ФФ | R6 | Повторить разрешённое и запрещённое переключение в tenant с другим slug; сравнить поведение до и после изменения. Под FF admin/staff AVpack проверить существующий доступ к данным подключённых селлеров → общая модель других tenant и доступ ФФ не изменились. | **Подтверждено тестами.** Общая модель делегаций работает для slug `avpack`, `avpack-9uczh-other`, `other-tenant-name` и отдельного tenant; запрещённый switch остаётся 403. FF admin/staff сохранили tenant-wide каталог, создание marketplace unload и неселлерский storage rebuild. |
| C7 · регрессия WMS-488 | R3, R4, R6 | Адаптировать прежние AVpack home-only тесты к active-seller модели и прогнать существующие тесты seller shop scope, каталога, прямых ID/скана, фоновых результатов и смены сессии → разрешённый активный кабинет работает, но данные любого неактивного/неразрешённого селлера не появляются; исправления утечек WMS-488 не откатились. | **Подтверждено.** Аналитик повторно прогнал шесть адресных файлов на продуктовом HEAD `82df09cd`: **133 passed**. Покрыты scope/switch, каталог и скан, direct/mixed product API, фоновые payload/result/error, документы и интеграции; неактивные same-tenant и foreign-tenant данные закрыты. Предъявленное независимое ревью после двух исправлений также дало PASS; отдельный технический набор — 128 passed, ruff/mypy PASS. |
| C8 · граница изменения | R5, R7 | Проверить итоговый diff и схему БД → нет миграции, массовой правки grants/флагов, нового административного UI и несвязанных изменений secondary home-scope; все изменения необходимы для снятия AVpack-заглушки и тестов регрессии. | **Подтверждено чтением diff `3e125074..82df09cd`.** Изменены шесть backend-файлов области seller scope и четыре тестовых файла; frontend и миграции отсутствуют. Второй коммит адресно закрыл найденные ревьюером обходы создания unload под неактивным seller и запуска storage rebuild под home вместо active; массовых изменений данных нет. |

## Сверка перед передачей разработчику

- Поручение «убирай этот запрет» покрыто R1–R2: AVpack больше не принудительно home-only.
- Уточнение «его товары не были видны» покрыто R3–R4: данные определяются одним активным
  разрешённым `seller_id`, а не объединяются.
- Сценарий одного менеджера для нескольких юрлиц покрыт R1–R3 и C1–C2.
- Сохранение системного исправления WMS-488 покрыто R3–R4, R6 и C7.
- Ограничение текущей задачи без административного UI и общего рефакторинга закреплено R7
  и C8.
- Необоснованных новых ролей, сущностей, запретов или изменений данных не добавлено.

## Заключение

**WMS-507 принята по C1–C8 на продуктовом HEAD
`82df09cd613d144e50430c44f74d3f6d199c429d`.** Специальная AVpack-заглушка удалена:
менеджер с явным flag и включённой делегацией переключается в разрешённый кабинет, а
все проверенные товарные и операционные пути работают в области одного активного
`seller_id`. Обычный селлер и менеджер без живой делегации остаются изолированными;
выключение grant немедленно закрывает старый токен. Исправления WMS-488 против чтения и
изменения неактивного либо чужого селлера сохранены.

Аналитик самостоятельно повторил шесть адресных test-файлов: 133 passed, 5 внешних
DeprecationWarning от SWIG, ошибок нет. Предъявленное независимое техническое ревью после
двух исправлений — PASS; его отдельный прогон сообщил 128 passed, ruff и mypy PASS.
Миграций, изменения grants, административного UI и frontend-изменений нет.

Это техническая приёмка backend/API в тестовой среде. Интерфейс не менялся и в браузере
не проверялся; staging, production, живой вход AVpack и фактический deployed SHA этой
приёмкой не подтверждены.

## Перенос после конфликта номера

До публикации обнаружена уже занятая задача WMS-506 по Ozon в `origin/etalon`.
AVpack-задача перенумерована в WMS-507 и перенесена поверх `8f15bdfb` отдельной веткой
`codex/wms507-avpack-delegation`. Продуктовый коммит новой истории —
`4f45447c15bb65f6cc2ab10476ea22298f772d0a`; шесть backend-файлов реализации побайтно
совпадают с принятым `82df09cd`, продуктовая логика не менялась. Документ и изменения
Ozon WMS-506 сохранены без правок. Ссылки на исходные SHA выше оставлены как свидетельства
фактически проведённых ревью и приёмки, а не переименованы задним числом.

На новой интеграционной базе разработчик выполнил `ruff check .`, `mypy .` (468 файлов)
и целевые тесты: **207 passed**, 45 внешних DeprecationWarning SWIG. Это проверка переноса;
она не подменяет исходное независимое ревью и приёмку. Деплой этим прогоном не подтверждён.
