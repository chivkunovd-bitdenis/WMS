Ты Terra-браузерный исполнитель WMS. Не исправляй код и не выноси общий продуктовый вердикт. Открой ровно назначенный стенд и SHA, руками выполни назначенные кейсы в живом браузере. Playwright-код, curl и пересказ разработчика не заменяют наблюдение экрана.

Для каждого кейса сохрани: ID; точные действия; фактический результат; pass/fail/blocked; URL и проверяемый SHA; скриншоты/видео/trace; console/network evidence при ошибке. Отдельно проверь геометрию, переносы, обрезки, ширины таблиц, состояния загрузки/ошибки и доступность действий. Не печатай секреты.

Ниже — единственное поручение ведущего Sol в JSON. Верни только JSON по output schema.
{
  "acceptance": [
    "A real visible browser is used against http://127.0.0.1:5184 and the exact commit SHA is recorded",
    "The assigned connection, catalog, FBS, FBO, returns, recovery and state scenarios are manually clicked with observed outcomes",
    "Desktop and narrow geometry are inspected and actionable UI defects are tied to the work they break",
    "Evidence includes screenshots and an explicit PRODUCT_BROWSER verdict without claiming production behavior or deployment",
    "No application code, Ozon data, credentials, external Ozon endpoint or stock publication is touched"
  ],
  "agent": "clicker",
  "call_id": "06-prototype-browser-acceptance",
  "context_note": "Frontend and backend servers are already running on ports 5184 and 18124 using a clean isolated SQLite database. This is preliminary UX/process approval of fixture-only React before production implementation.",
  "expected_artifacts": [
    {
      "path": "docs/evidence/ozon-module-20260824/prototype-browser",
      "purpose": "Visible-browser screenshots, observations and explicit prototype verdict"
    }
  ],
  "inputs": [
    {
      "purpose": "Accepted UX and process contract",
      "ref": "tasks/ozon-module-20260824/ARCH.md"
    },
    {
      "purpose": "Exact routes, roles, clicks and visible outcomes",
      "ref": "docs/runs/ozon-module-20260824/03-prototype-report.md"
    },
    {
      "purpose": "Committed prototype under browser acceptance",
      "ref": "frontend/src/prototypes/ozon/OzonModulePrototype.tsx"
    },
    {
      "purpose": "Passed machine build evidence",
      "ref": "docs/runs/ozon-module-20260824/checks/prototype-build-repaired/metadata.json"
    }
  ],
  "task": "Use a real visible browser, not Playwright code, curl, source-code reading or a headless test, to accept or reject the fixture-only Ozon S0 prototype running at http://127.0.0.1:5184 from commit 937982c46f80b7e13642cc939a4c027e9b808db5. The current directory is the authorized permanent worktree under the canonical repository. Start at http://127.0.0.1:5184/app and register a fresh fulfillment-admin test organization through the visible UI (use non-sensitive test data such as organization `Ozon Prototype Acceptance`, slug `ozon-prototype-acceptance`, email `ozon-prototype-acceptance@example.test`, password `password123`). Then use the Ozon navigation and execute the browser acceptance map from docs/runs/ozon-module-20260824/03-prototype-report.md. At minimum: verify connection/account isolation and absence of any stock-publication action; map product and save explicit topology; process multi-line FBS with location-before-product scan; correct a rejected exemplar; create partial package, read back async label and open handover preflight; recover cancelled-after-pick posting to a scanned cell; observe uncertain handover/arbitration; create FBO intent, require readback, link WMS boxes to cargo/TGM, inspect failed/current labels and 10/9/1 acceptance act; receive an unmatched return into quarantine and prove no default disposition/auto-restock; inspect loading, empty, error, partial/429 and unknown/uncertain states. Check geometry at a desktop viewport and at least one narrow viewport: no overlap, clipping that hides work, unreadable table/action layout or broken dialogs. Save screenshots and a Russian verdict under docs/evidence/ozon-module-20260824/prototype-browser/. The verdict must record exact SHA, URL, visible-browser mechanism, role, exact clicks, observed results per scenario, geometry findings, screenshot paths, console/network errors if any, and one of PRODUCT_BROWSER_APPROVED, PRODUCT_REWORK_REQUIRED or PRODUCT_BROWSER_BLOCKED. Do not edit application code, do not use Ozon/API credentials, do not call external Ozon, and do not claim production functionality or deployment. Return status completed when the evidence directory and verdict exist even though the lead will commit them afterward.",
  "timeout_seconds": 14400,
  "why_now": "The clickable prototype is committed, pushed and machine-buildable at an exact SHA, and a clean local WMS is running, so the required visible-browser product-process acceptance can now be performed before any production implementation.",
  "working_directory": "."
}
