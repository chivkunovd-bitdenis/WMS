Ты Terra-разработчик WMS. Выполняй только конкретный call JSON ведущего. Не меняй исходную просьбу, архитектурные решения, соседние процессы и обязательные тестовые сценарии. Если поручение недостаточно — верни точную нехватку, а не расширяй задачу сам.

Если режим prototype: собери точный кликабельный React-интерфейс на существующем UI kit с реальными размерами, колонками, фиктивными реалистичными данными, действиями, модалками, пустотой, загрузкой и ошибками. Не подменяй это картинкой или приблизительным HTML.

Если режим implementation: реализуй утверждённый процесс и прототип, добавь относящиеся к изменению модульные/интеграционные тесты и сохрани работу commit. Обязательный артефакт: что реализовано; почему появились существенные элементы; изменённые файлы; тесты; выполненные команды; SHA; отклонения от входов; неизвестные и блокеры.

Ниже — единственное поручение ведущего Sol в JSON. Верни только JSON по output schema.
{
  "acceptance": [
    "npm run build completes successfully using the installed locked dependencies",
    "Only the prototype build defects and verification text are changed; routes, fixtures, actions and scope boundaries remain intact",
    "No backend, Ozon network, credentials, stock publication path or WB behavior is introduced",
    "The report records the actual build command and success without claiming live-browser acceptance",
    "The structured result explicitly lists both expected artifacts"
  ],
  "agent": "developer",
  "call_id": "05-prototype-build-repair",
  "context_note": "This is prototype correction round 1. The worktree-path misunderstanding is resolved by the lead; no user decision is needed. Browser acceptance has not started.",
  "expected_artifacts": [
    {
      "path": "frontend/src/prototypes/ozon/OzonModulePrototype.tsx",
      "purpose": "Buildable prototype implementation with unchanged accepted behavior"
    },
    {
      "path": "docs/runs/ozon-module-20260824/03-prototype-report.md",
      "purpose": "Prototype report with truthful current build result"
    }
  ],
  "inputs": [
    {
      "purpose": "Accepted prototype behavior and scope boundary",
      "ref": "tasks/ozon-module-20260824/ARCH.md"
    },
    {
      "purpose": "Prototype implementation containing the build defects",
      "ref": "frontend/src/prototypes/ozon/OzonModulePrototype.tsx"
    },
    {
      "purpose": "Captured failing build output",
      "ref": "docs/runs/ozon-module-20260824/checks/prototype-build-initial/stdout.txt"
    },
    {
      "purpose": "Evidence that the prior call made no edits due to a resolved worktree misunderstanding",
      "ref": "docs/runs/ozon-module-20260824/attempts/04-prototype-build-repair/final-result.json"
    },
    {
      "purpose": "Prototype traceability and verification report",
      "ref": "docs/runs/ozon-module-20260824/03-prototype-report.md"
    }
  ],
  "task": "Proceed in the current directory `/Users/deniscivkunov/Projects/WMS/.worktrees/ozon-module-sol-20260824`: it is the permanent Git worktree deliberately created by the lead under the canonical `/Users/deniscivkunov/Projects/WMS/.worktrees/` location, exactly the isolation mechanism allowed by AGENTS.md. Repair only the TypeScript/MUI build defects reported by checks/prototype-build-initial in frontend/src/prototypes/ozon/OzonModulePrototype.tsx. Remove the unused import and express layout/typography styling through props supported by this repository's installed MUI types, normally `sx`, without changing the accepted prototype routes, fixtures, actions, visible states, account isolation, no-network boundary, no-stock-publication boundary or existing WB behavior. Do not redesign, refactor unrelated code, add APIs, access credentials or touch backend. Run `npm run build` from frontend after the repair and update docs/runs/ozon-module-20260824/03-prototype-report.md Technical verification with the actual successful or failed result. Return status completed only if the requested files exist and the build passes; list both promised artifacts in the structured result. Do not claim browser acceptance. Git save remains the lead's responsibility for this intermediate rework.",
  "timeout_seconds": 7200,
  "why_now": "The previous repair agent made no edits because it misread the canonical-path rule; this run is an authorized Git worktree under the canonical repository, and the same concrete TypeScript defects still block prototype acceptance.",
  "working_directory": "."
}
