# Wave 2 acceptance: Приёмка на FF - список

Дата: 2026-08-15 16:42 MSK

Рабочая копия: `/Users/deniscivkunov/Projects/WMS/.worktrees/wave2-reception-list-20260815`

Ветка: `iteration/wms-wave2-reception-list-20260815`

ORDER 033: принят как текущий приказ на acceptance artifact по экрану. Отдельный файл с точным текстом `ORDER 033` в `docs/` не найден через `rg`; проверка выполнена по тексту задачи пользователя, backlog REC-01, REC-10..REC-14 и правилу 6а из `docs/WMS_GATE.md`.

Модель/режим: пользователь запросил `gpt-5.5 high`; текущий runtime не дает мне надежного способа подтвердить фактический model id, поэтому фиксирую ограничение честно.

## Экран

Приёмка на FF - список.

Задачи экрана: REC-01, REC-10, REC-11, REC-12, REC-13, REC-14.

## Стадия

Worker acceptance artifact after development. UI и бизнес-код в этом artifact-step не менялись.

## Статус

Artifact оформлен по уже реализованному и запушенному экрану. Worker-проверка кода, targeted tests и внешний видимый Chrome/CDP-прогон зафиксированы. Интеграция в `etalon` не выполнялась этим агентом и остается отдельной стадией.

## Commit

Проверенный UI/code commit: `abfa83647415e80af3c004a1e28374de66798a3c`.

Коммиты реализации в ветке:

- `a2a04d8` - основной checkpoint экрана REC-01, REC-10..REC-14.
- `2ab9a27` - backend typing follow-up.
- `abfa836` - REC-10 подпись коробов в колонке состава и e2e expectation.

## Тесты реальные цифры

- Backend `ruff check .`: `All checks passed!`
- Backend `mypy .`: `Success: no issues found in 258 source files`
- Backend targeted pytest:
  - `tests/test_inbound_intake.py::test_ff_created_inbound_summary_keeps_seller_and_product_terms`: `1 passed in 7.47s`
  - `tests/test_staff_reception_inbound_draft.py`: `2 passed in 42.22s`
  - `tests/test_inbound_intake_api_be03.py`: `10 passed in 54.70s`
- Frontend `npm run build`: passed; Vite transformed `11998 modules`; only existing chunk-size warning.
- Playwright targeted regression, not acceptance:
  - `tests-e2e/ff-reception-sorting.spec.ts`: `3 passed (1.2m)`
  - `tests-e2e/seller-inbound-operation-type.spec.ts`: `1 passed (37.4s)`

Примечание: один параллельный pytest-прогон `tests/test_inbound_intake_api_be03.py` дал sqlite setup error `unable to open database file` при отдельной базе. Тот же набор был перезапущен сериализованно на коротком sqlite path и прошел `10 passed`; это зафиксировано как инфраструктурный sqlite-сбой, а не product/code finding.

## Браузер

Внешний видимый браузер ОС: Google Chrome, открытый как обычное видимое окно. Управление выполнялось через Chrome DevTools Protocol на `127.0.0.1:9222`; это не Playwright, не headless и не скриншотная подмена.

Честная фиксация: внешний Chrome/CDP-прогон был выполнен и зафиксирован на UI/code commit `abfa83647415e80af3c004a1e28374de66798a3c` до создания этого markdown-артефакта. После него UI/бизнес-код не менялся; текущий шаг добавляет только acceptance report.

Локальный стенд для прохода:

- Backend: `127.0.0.1:18000`, FastAPI, `WMS_AUTO_CREATE_SCHEMA=1`, sqlite `/tmp/wms-wave2-live-reception.sqlite`.
- Frontend: `127.0.0.1:5174`, Vite dev server, `VITE_API_PROXY=http://127.0.0.1:18000`.
- Live user: `live-rec-1786798357186@example.com`.
- Live rows:
  - red mismatch row: `1813b21a-53cf-4391-ab89-f2a347886bc2`
  - green matched row: `dc9fb092-2151-4596-898e-d225ef6448e1`
  - fresh submitted row: `5f126e05-fde4-458c-8259-0784bf06d289`

Что проверено в видимом Chrome:

- меню слева показывает `Приёмка на FF`;
- заголовок экрана показывает `Приёмка на FF`;
- счетчик показывает `Новые приёмки: 1`;
- таблица показывает порядок колонок `Селлер -> Номер документа -> Дата -> Состав -> Статус`;
- строка fresh показывает `Передано на склад`, статус `submitted`, discrepancy `none`;
- строка red показывает статус `sorting`, discrepancy `mismatch`;
- строка green показывает статус `sorting`, discrepancy `matched`;
- поиск по `LIVE-RED-1786798357186` оставил только red row;
- сортировка по дате первым поставила green row с датой `18.08.2026`;
- сортировка по номеру визуально сработала, список остался интерактивным;
- фильтр селлера `Live Green Seller 1786798357186` оставил только green row;
- фильтр статуса `Передано на склад` оставил только fresh row;
- кнопки `Создать приёмку` и `Создать возврат` видны отдельно;
- по кнопке `Создать возврат` открылся dialog с выбором `Селлер` и кнопкой `Создать`.

## Находки

Стоп: 0.

Тормоз: 0.

Хвост: 0.

## Раунд

Раунд: 0.

## Блокеры

Для worker-ветки: нет.

Для общего завершения проекта: ветка еще не интегрирована в `etalon`; это не делалось по текущему заданию.

## 6а mapping

Правило 6а: каждый видимый элемент должен иметь task ID; если ID нет, элемента быть не должно.

| Видимый элемент | Task ID | Решение |
|---|---:|---|
| Пункт меню `Приёмка на FF` | REC-01 | Оставить: требование переименования раздела. |
| Заголовок `Приёмка на FF` | REC-01 | Оставить: требование переименования экрана. |
| Описание списка документов для складской приёмки | REC-01 / REC-10 | Оставить: уточняет назначение диспетчерского списка без новой операции. |
| Счетчик `Новые приёмки: N` | REC-10 | Оставить: прямо требуется счетчик новых приёмок. |
| Кнопка `Создать приёмку` | REC-12 | Оставить: отдельная кнопка FF-создания приёмки. |
| Кнопка `Создать возврат` | REC-12 | Оставить: отдельная кнопка FF-создания возврата. |
| Dialog создания с заголовком выбранной операции | REC-12 | Оставить: FF выбирает селлера перед созданием документа. |
| Select `Селлер` в dialog создания | REC-12 | Оставить: обязательный выбор селлера перед созданием FF-документа. |
| Dialog-кнопка `Создать` | REC-12 | Оставить: завершает создание выбранной отдельной операции. |
| Dialog-кнопка `Отмена` | REC-12 | Оставить: стандартный выход без создания из обязательного dialog выбора селлера. |
| Фильтр списка `Селлер` | REC-10 | Оставить: прямо требуется фильтр селлера. |
| Фильтр списка `Статус` | REC-10 | Оставить: прямо требуется фильтр статуса. |
| Поле `Поиск` | REC-10 | Оставить: поиск по номеру, товару и селлеру. |
| Колонка `Селлер` | REC-10 | Оставить: первая колонка по требованию. |
| Колонка `Номер документа` | REC-10 | Оставить: вторая колонка, сортировка кликом по номеру. |
| Колонка `Дата` | REC-10 | Оставить: третья колонка, сортировка кликом по дате. |
| Колонка `Состав` | REC-10 | Оставить: компактно объединяет короба/грузоместа и единицы. |
| Текст состава вида `0 из 1 коробов — 7 ед.` | REC-10 | Оставить: показывает объем и единицы без лишних колонок. |
| Колонка `Статус` | REC-10 / REC-14 | Оставить: человеческий статус и статусная окраска строки. |
| Chip с человеческим статусом | REC-10 / REC-14 | Оставить: статусная метка внутри строки, но цвет строки не ограничен chip. |
| Окраска строки по статусу | REC-14 | Оставить: строка/заметная часть строки окрашена по статусу. |
| Красная строка при расхождении | REC-13 | Оставить: цветовая индикация расхождения, без отдельной колонки/chip. |
| Зеленая строка при совпадении | REC-13 | Оставить: цветовая индикация совпадения, без отдельной колонки/chip. |
| Приоритет красного расхождения над статусным цветом | REC-13 / REC-14 | Оставить: конфликт цветов решается в пользу расхождения. |
| Loading indicator при обновлении | REC-11 | Оставить: список явно показывает обновление при входе/refresh. |
| Error alert загрузки | REC-11 | Оставить: понятное состояние ошибки загрузки. |
| Кнопка `Повторить` в ошибке | REC-11 | Оставить: retry после ошибки загрузки. |
| Empty state `Нет приёмок в очереди` | REC-11 | Оставить: явное состояние пустого списка после загрузки. |

6а-элементы, которых не добавляли:

- отдельная колонка или chip `Расхождение` не добавлены, потому что REC-13 требует цвет, а не дополнительную колонку;
- toggle типа операции внутри формы создания не добавлен, потому что REC-12 требует две отдельные кнопки;
- настраиваемые колонки, counters вне `Новые приёмки`, технические статусы и лишние chips не добавлены, потому что для них нет task ID в REC-01, REC-10..REC-14.
