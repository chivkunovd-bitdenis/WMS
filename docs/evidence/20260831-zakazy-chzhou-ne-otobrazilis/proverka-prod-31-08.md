# Проверка: «почему заказы ИП Чжоу не отобразились у ФФ» — 31.08.2026, ~07:10 UTC

Среда: **прод** `194.87.96.144:8088`, checkout `/opt/wms` = `94fcaebca7938c5248829ffeaab9943facce9f49`.
Арендатор «Империя ФФ» `7b98a8aa-…`, селлер «ИП Чжоу» `bf8eea6b-…`, склад WMS «основной» `2ed64bca-…`.

## 1. Заказы в системе есть

```
status              | count | max_created
--------------------+-------+---------------------
new                 |   213 | 2026-08-31 06:41:39
assembling          |    45 | 2026-08-31 04:26:39
sorted              |  1253 | …
cancelled           |  1687 | …
```

Прогон боевого кода экрана (`fetch_worklist_page`, status_group=new, limit=500):

```
vsego strok na vkladke Novye: 491
{'Loviana': 219, 'ИП Чжоу': 213, 'ООО Фэшн': 38, 'ИП Фадин': 15, 'ИП Бугаев А.': 5, 'ИП Горячева': 1}
Chzhou: 213   blockers>0: 0   ostatok>0: 0
```

Первый заказ со скриншота WB — `5614615783`, «Кроссовки легкие школьные на липучке»,
арт. `2090-2D`, размер 34 — найден: `id=31534ba7-b8e5-4711-b68c-c7be4de91df2`,
`wb_warehouse 2068977`, `inventory.available_unpacked = 0`, `locations: []`.

Распределение по складам WB: `2068977` (ФФ Империя Львов_Кавказский б.) — 199 новых
+ 35 в сборке; `972894` (Фулфилмент ПакДрайв) — 13 новых; `2041537` (ВельветБокс_Подольск)
— 1 новый + 1 в сборке; ещё 8 в сборке на других складах ВельветБокса.

## 2. Собирать нечего

115 артикулов в 213 новых заказах:

```
polozh | nol | otricat | net stroki ostatka | samyy minus
     0 |   1 |      62 |                 52 |          -7
```

Весь остаток Чжоу в WMS — 488 позиций / 1501 шт — целиком лежит в `__SORTING__`,
и это **другие** артикулы. Отрицательных позиций 144 на −277 шт.

## 3. Поставка со второго скриншота

`FBS 27.08.2026` = `WB-GI-270174126`, `id=bd7d3d6f-…`, статус `assembling`,
создана 27.08 09:53, последнее касание заказов 27.08 18:31.
Все 35 заказов: `pick_status=picked` (проставлен автоматом при создании, 09:53:10),
`pack_status=pending`, `sticker_applied_at=NULL`.
Грузомест (`fbs_trbxes`) — 0. Задание на упаковку `27db856b-…` в статусе **draft** с 27.08.

## 4. Остатки на WB публикуем не мы

14 привязок складов Чжоу: у всех `stock_sync_enabled = false`.
В `fbs_stock_sync_items` по этому селлеру — **0 записей** за всё время.
`POST /api/v3/stocks/2068977` по 100 chrtId из новых заказов → у всех `amount = 0`.

## 5. Автоопрос работает

Цикл 31.08 07:10:05 — `sellers=17 upserted=524 errors=10`. Чжоу без ошибок.
Ошибки 401 (мёртвые токены) у: ИП Нестерюк, ИП Комарова (`d18e384f`, ИП Львова В.В.),
**ИП Комарова `4e67e471` — в арендаторе «Империя ФФ»**, Беляев В.П., ИП Акишен В.П.,
ИП Герус Д.В., ИП Аннабабаев, ИП Чулков; без токена — Виталик, ИП Шерин.

## Чего НЕ делал

Портал ФФ на проде глазами в браузере не открывал — рабочего аккаунта прода у меня нет.
Проверка сделана запросами к боевой БД и прогоном кода экрана внутри `wms_prod-api-1`.

---

# Догон: что сломала выкатка (проверено 31.08 ~10:30 MSK)

## Прод катали четыре раза за сутки

```
7eb91d93  30.08 23:29 UTC (02:29 MSK 31.08)  ← пересобран web (фронт)
94fcaebc  31.08 07:06 UTC
32a0ecac  31.08 07:11 UTC
a3fed0b6  31.08 07:21 UTC                    ← пересобран только api/celery
```

Сейчас фронт собран из `7eb91d93`, бэк — из `a3fed0b6`.

## Колонку склада убрал коммит `2c64cd55` (31.08 01:42 MSK)

`git diff a5a28b13..7eb91d93 -- frontend/src/screens/v2/FfFbsOrdersScreen.tsx`

Было:
```tsx
<TableCell sx={{ minWidth: 180 }}>Склад селлера / WB</TableCell>
...
{order.wb_warehouse.name || `WB ${order.wb_warehouse.id}`}
WMS: {order.wms_warehouse.name}
```
Стало:
```tsx
<TableCell sx={{ minWidth: 125 }}>Маршрут сдачи</TableCell>
...
<Chip label={order.can_pvz ? 'ПВЗ' : 'Склад / СЦ'} />
```

Заголовок `Склад селлера / WB` удалён в обеих ветках таблицы (вкладка «Новые» и остальные).
Блок фильтра `fbs-worklist-warehouse` ночью **не менялся**.

## Фильтр не сломан, но подписи — номера

`warehouseOptionLabel` = `sellerWarehouseNames[id] || option.name || …`.
`option.name` бэк отдаёт как `WB <id>` (в `tenant_wb_mp_warehouses` складов селлера нет —
там кэш `GET /api/v1/warehouses`, то есть приёмки WB). `sellerWarehouseNames` フронт
подтягивает через `GET /api/v3/warehouses` **только когда выбран конкретный селлер**.
Итог: при «все селлеры» в фильтре номера, при выбранном селлере — нормальные имена.
Логика эта с 12.08 (`2f526743`), сегодня не трогали.

## Привязки складов целы

По всей боевой базе нет ни одной привязки без склада WMS, с `served=false`
или `is_active=false`. У восьми селлеров «Империя ФФ» — 45 привязок, все на «основной».
Заказов без склада WMS в «Империя ФФ» — ноль (33 таких есть у «ПакХаб»).

## Утренняя выкатка запретила подбор без остатка

`32a0ecac fix(fbs): enforce stock at picking` — `FbsPickingError("insufficient_unpacked")`
при нехватке физического остатка. По Чжоу положительного остатка нет ни по одному из
115 артикулов → собрать эти заказы теперь нельзя. До этого подбор был фантомным:
на поставке от 27.08 `picked_at` у всех 35 заказов = `09:53:10`, секунда создания поставки.

## Вкладка «В работе»

`list_supply_worklist(status_group="active")` на проде отдаёт 21 поставку, среди них
`FBS 27.08.2026` / `WB-GI-270174126` / `assembling` / 35 заказов — та самая со скриншота.

---

# Чистка чужих складов на проде, 31.08 ~10:40–11:00 MSK

## Шаг 1. Сняли обслуживание с чужих складов

`update fbs_warehouse_bindings set served=false` — 12 привязок:
Чжоу ×10 (ВельветБокс 6, КрафтМ 3, ПакДрайв 1), Loviana «Helpberries», Фэшн «Фулфилмент 2035877».
Признак «чужой» — ноль поставок, созданных из WMS (`fbs_supplies.source='wms'`) за всё время.
После этого автоопрос: было 524 заказа за цикл, стало ~503.

## Шаг 2. Резервная копия

Таблицы-снимки `zz_bak20260831_*` в самой базе + дамп
`/root/backups/chuzhie-zakazy-20260831.sql` (2,6 МБ) и копия у меня локально.
Содержимое: orders 4277, supplies 18, picks 1, reservations 6, print_assets 13,
reversal 1, boxes 1, trbxes 1; order_products / markings / box_items / prod_picks — 0.

## Шаг 3. Удаление

```
delete from fbs_orders   … → DELETE 4277
delete from fbs_supplies … → DELETE 18
```
Начислений за упаковку (`fbs_packaging_fulfillments`) и списаний пула на этих заказах
не было ни одного — биллинг не задет. Смешанных поставок нет: во всех 18 состав был
чужим на 100 %. У одного заказа (`5532381407`, КрафтМ 2) был подбор с движением остатка
от 19.08 — движение в журнале осталось, баланс не изменился.

## Проверка после

Чжоу — только `2068977` (199 открытых) и `2069067`; Фэшн — только «Империя Львов»;
Loviana — только `2046998`/`2088990`. Вкладка «Новые» — 426 строк вместо 491,
«В работе» — 14 поставок вместо 21.

## Осталось нерешённым (жду слова владельца)

| селлер | склады | заказов | наших поставок |
|---|---|---|---|
| ИП Фадин | 8 «городских» (Мытищи, Новосибирск, Н.Новгород, Екат, СПб, Краснодар, Казань, Воронеж) | 4382 | 0 |
| ИП Бугаев А. | 3 × «ФФ Царицыно …» | 113 | 0 |
| ИП Горячева | 2 × «ФФ Царицыно …» | 70 | 0 |
| ИП Комарова | 2020862 | 17 | 0 |

У Фадина наши поставки были только на четырёх складах семейства «Лыткарино».
