# Разбор замечаний независимого ревью WMS-501

Read-only reviewer вызван через Claude CLI alias `opus`, effort `max`. Runtime сообщил `claude-opus-4-7` и вспомогательный `claude-haiku-4-5-20251001`; исходный ответ сохранён без переписывания в independent-review.md. Это дополнительная проверка **отчёта**, не перекрёстное ревью реализации: продукт не менялся. Она не выдаётся за ревью модели Opus 5 Extra, указанной в правилах для реализации Astra.

| Замечание | Решение |
|---|---|
| Z1: роль стартового preload | Принято. Сводка уточняет FF administrator/сотрудника с доступом к warehouse catalog; seller-портал не приписан этому условию. |
| Z2: AVpack-only | Scope уточнён в ISO-01 и сводке: дело именно в GET фонового задания и известном UUID внутри того же tenant. Нельзя переносить это на все поверхности AVpack либо считать остальные tenants полностью открытыми. |
| Z3: рекомендация scan вне страницы | Принято как уточнение будущей постановки; рекомендации не выданы за реализованный или полностью спроектированный fix. |
| Z4: reset_rate_limit_state якобы обходит route limiter | **Фактическая интерпретация reviewer отклонена.** Сброс test state возвращает пустое окно; не подменяет check_login_rate_limit, не меняет max_attempts=5 и не выключает dependency/route. Пять реальных HTTP login возвращают 401; шестая попытка не используется. Старый service-only heartbeat действительно обходил route по определению; это отдельный опыт. В BE-01 разделены эти два опыта и явно добавлен reset. Источники: scan_api_probe.py, backend/app/api/auth.py:268–291, backend/app/services/login_rate_limit.py:54–103. Отдельный SQL/ASGI скрипт проверен исходным аналитиком. |
| Z5: одинаковый P1 для preload и потери данных | Принято. Полные каталоги BE-02/FE-01/сводка получают P2; устранение нарушения доступа и потери обновлений раньше. Результат объёма/тайминга не изменён. |
| Z6: блокировка только одного API процесса | Принято. В сводку перенесены предел «тот же процесс» и неизвестное число production процессов. |
| Z7: идентичность попытки | Принято как уточнение рекомендации: row lock не позволяет отличить повтор доставки от новой штуки, поэтому нужна сохранённая идентичность физического действия; сначала проверить повторное использование имеющегося draft mutation_id. Не предписаны новая таблица/журнал/счётчик. |
| Z8: browser race != серверный replay | Принято. FE-05 и S4 явно разделены; первый не объявлен воспроизведённым двойным списанием. |
| Z9: mobile/R4 | Принято. В сводке явно написано, что R4 закрыт только по web, мобильная часть остаётся существенным пробелом; итоговая приёмка не означает полный охват mobile. |
| Z10: production сверка связей | Сохранена граница: production не проверялся, текущие очищающие test fixtures категорически не подходят для рабочей БД. Дополнительный запрет требовать новое разрешение для любой read-only проверки **не вводится**: это не требование исходного пользователя/AGENTS для обычного чтения. Нужны идентифицированное окружение, подходящий read-only доступ и отдельный безопасный сценарий проверки; кабинеты секретов остаются запрещены. |

Повторное ревью того же продуктового кода не запускалось: изменений продукта нет, найденных технических дефектов в новом продукте эта задача не исправляла. Изменения формулировок переданы исходному аналитику на окончательную приёмку.
