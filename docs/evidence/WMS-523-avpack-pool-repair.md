# WMS-523: срочное восстановление пулов AVpack, 24.09.2026

Операция прямо разрешена владельцем: создать пулы для всех уже загруженных
AVpack-кодов без пулов. Продуктовый код production не менялся (`2f7c710d`).

Скрипт: `scripts/repair_wms523_avpack_pools.py`, dry-run по умолчанию.
Проверенный tenant: `d6e1ad21-8afa-4acf-8d0b-907b9f2adcfe`, имя `AVpack`.

До записи прочитаны все строки этого tenant с `pool_id IS NULL`: 408 кодов,
все `source=pool`, `status=available`, с существующими товаром и import batch.
Проверено совпадение tenant/seller у кода, товара и документа загрузки.

После dry-run выполнен один `--apply --expected-count 408 --expected-groups 27`:
созданы 27 пулов и 27 связей с 22 товарами. Группировка — seller, исходная
загрузка, товар и GTIN. Сохранена исходная дата загрузки. У 408 кодов изменён
только `pool_id`: контрольные суммы всех остальных полей, включая статус,
сам код, PDF и привязки к заказам/упаковке, совпали до и после изменения.
Все записи выполнены одной транзакцией; для каждой группы записан
`document_event` с меткой WMS-523, перечнем code IDs и прежним `pool_id=null`.

Независимое чтение после commit подтвердило: кодов AVpack без пула — **0**.
Проверены обе реальные функции подсчёта: каталог (batch) и упаковка (single;
эта же выборка используется для печати):

| Товар | До | После: каталог | После: упаковка |
|---|---:|---:|---:|
| PAL_BROWN_AV | 3 | 58 | 58 |
| LEG_BL_AV/XS | 4 | 64 | 64 |
| LEG_BL_AV/L | 18 | 68 | 68 |

Саму печать в production не запускали, коды не резервировали и не списывали.
Открытые страницы надо обновить: отдельный снимок задания упаковки в браузере
не перечитывается обычным фоновым обновлением workspace.

Восстановление текущих данных завершено. Оно не заменяет постоянное исправление
новых auto/assign-загрузок WMS-523: до его выкладки новая загрузка старым кодом
ещё может создавать записи без пула. Повторный запуск repair без новой проверки
области и явного решения запрещён; скрипт по умолчанию только читает данные.

## Постоянное исправление опубликовано

После успешного CI `36017596543` PR #250 объединён. Выкатка
`36019384348` завершилась успешно 24.09.2026 в 15:21 UTC. Production SHA —
`37b738bfc70b863bdd89f745e84812c2e5f08879`. Хеш файла service в работающем API
совпал с Git: `b41172665114c01a000f53ebf85f6d0ea38a7e6a0917af39563044dc45a2fd0d`.
Все шесть сервисов работают, публичные `/`, `/seller/`, `/api/health` — HTTP 200.
Независимое post-deploy чтение снова подтвердило 0 кодов AVpack без пула и
counts 58/64/68 одновременно для каталога и упаковки. Повторный repair не нужен
и не выполнялся. Новых боевых импортов/печатей для проверки не делали.
