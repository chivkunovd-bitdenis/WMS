# S-17 — локальный вердикт

Маршрут: `/app/ff/reception`.

- `Создать приёмку` — `UNCHANGED`: создаёт внутренний документ приёмки, внешнего provider-вызова нет.
- `Создать возврат` — `UNCHANGED` для текущей приёмки: создаёт тот же внутренний документ возврата,
  не вызывает marketplace API.
- Поиск, фильтры селлера/статуса, сортировка и открытие строки — `UNCHANGED`: это локальная очередь.
- Ozon return list/giveout — `BLOCKED`: подтверждённого правила физической приёмки и возврата в
  sellable stock нет; отдельного Ozon-действия на этом экране не существует, поэтому UI не добавлялся.
- WB-приёмка и существующий процесс возврата не менялись.

Доказательство поведения: `uv run pytest -q tests/test_inbound_intake.py
tests/test_inbound_intake_api_be03.py tests/test_inbound_intake_service_be01.py` — 36 passed.
Живое визуальное доказательство не получено: Browser plugin не содержит обязательный клиент
управления браузером (см. `../S-12/BROWSER_BLOCKED.md`).
