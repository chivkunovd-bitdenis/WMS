# Проверка: «у Виталика Хорса (фулик) отображаются остатки ФФ Адам» — 31.08.2026, ~11:30 MSK

Среда: **прод** `194.87.96.144:8088`, `/opt/wms` = `a3fed0b623b9764dcc6adedfcb3907a1448877f0`.
Проверка сделана запросами к боевой БД (`wms_prod-db-1`) и прогоном боевого кода экранов
внутри `wms_prod-api-1`. **Портал в браузере под учёткой Виталика не открывал — пароля нет.**

## Кто есть кто

| | арендатор | id | админ |
|---|---|---|---|
| «фулик Виталика Хорса» | **AVpack** | `d6e1ad21-8afa-4acf-8d0b-907b9f2adcfe` | `vitalik-hors@mail.ru` |
| «ФФ Адам» | **FFAdam** | `561e0810-b95e-4b1c-a478-ad6f24d99523` | `ffadam@mail.ru` |

## 1. Данных ФФ Адам внутри AVpack нет

Сквозные проверки целостности по всей базе — расхождений ноль:

```
balance vs product     | 0
balance vs location    | 0
product vs seller      | 0
location vs warehouse  | 0
```

Пересечения между двумя арендаторами:

```
nmID:      AVpack 170 | FFAdam 259 | общих 0
ШК:        общих 0
названия:  общих 0
склады WB: общих 0
```

Остаток AVpack — 18 строк / 981 шт, все по своим селлерам:
Аннабабаев, Герус, Горячкина, Чулков, Шерин. Остаток FFAdam — 32 SKU / 1966 шт.

Поиск строк `адам` / `adam` по **всем 329 текстовым колонкам** боевой базы даёт
только `tenants.name`, `tenants.slug`, `users.email` и `operation_facts.actor_name_snapshot`
— и всё это внутри самого FFAdam. То есть слово «Адам» физически неоткуда взяться
на экране AVpack.

## 2. Прогон боевого кода экранов с арендатором AVpack

```
[1] inventory-balances/summary : 18 строк, чужих товаров 0
    селлеры: Аннабабаев, Герус, Горячкина, Чулков, Шерин
[2] reports/overview           : отработал, ключи стандартные
[3] catalog list_products      : 184 товара, чужих 0
```

Эндпоинты остатков (`/operations/inventory-balances/summary`, отчёты,
`/operations/storage/statements`) фильтруются по `user.tenant_id` — проверено по коду.
Кэширования `/api/*` в Caddy нет.

## 3. Что действительно нашлось у Виталика — чужой фулфилмент, но не Адам

Живой запрос `GET /api/v3/warehouses` токеном селлера:

```
AVpack | ИП Горячкина Т.И. | 1852149=DBS костя; 890727=Новая тура;
                             2107811=ФФ Царицыно; 2033956=Мой склад Москва (Белая дача);
                             2123478=ФФ Царицыно (Краснодар)
```

Обе привязки AVpack ведут на склады **«ФФ Царицыно»** — это склады другого
фулфилмента. С них пришло 70 заказов: `2107811` — 47 sorted + 14 done +
4 external_processing, `2123478` — 4 sorted + 1 new.

## 4. Один кабинет WB заведён у двух ФФ сразу

```
wb_warehouse_id | арендаторы                   | селлеры
2107811         | AVpack + Империя ФФ          | ИП Горячкина Т.И. | ИП Горячева
2123478         | AVpack + Империя ФФ          | ИП Горячкина Т.И. | ИП Горячева
1887957         | Империя ФФ + ИП Львова В.В.  | ИП Комарова
2020862         | Империя ФФ + ИП Львова В.В.  | ИП Комарова
1155120         | ИП Львова В.В. + ИП Тестовый | ИП Тест | Denmarcs
```

Пересечение nmID между арендаторами:

```
AVpack/Горячкина      ↔ Империя ФФ/Горячева : 100
ИП Львова/ИП Тест     ↔ ИП Тестовый/Denmarcs: 100
Империя ФФ/Комарова   ↔ ИП Львова/Комарова  : 33
AVpack/Горячкина      ↔ ИП Тестовый/Виталик : 30
```

**FFAdam не участвует ни в одной паре.**

## Вывод

Отображения данных ФФ Адам в портале AVpack на проде я не нашёл: ни в данных,
ни в выдаче боевого кода экранов. Зато у Виталика в FBS висят склады чужого
фулфилмента «ФФ Царицыно», а тот же кабинет WB заведён параллельно у «Империи ФФ».

## Чего НЕ делал

Не открывал портал ФФ на проде в браузере под `vitalik-hors@mail.ru` — рабочей
учётки прода у меня нет. Ничего в базе не менял, только читал.
