# Browser evidence · release gate repair

Проверено 27.08.2026 в реальной видимой вкладке встроенного браузера на локальном WMS
`http://127.0.0.1:5190` с API на порту `18101` и отдельной временной SQLite-базой.

## Действия

1. Вход администратором фулфилмента.
2. Переход в «FBS» и визуальная проверка экранов «Заказы» и «Остатки WB», вкладок,
   фильтров, таблиц и пустых состояний.
3. Переход в «Упаковка» и визуальная проверка экрана заданий и пустого состояния.
4. Переход в очередь маркировки и проверка пустого состояния «Все требуемые КМ напечатаны».

## Результат

FF-shell, FBS и упаковка открываются без ошибки; seller-портал не подменяет FF-маршруты.
Ошибок консоли, наложений и горизонтального переполнения нет. Production UI-файлы в этой
ветке не менялись.

Вердикт: `PRODUCT_BROWSER_APPROVED`.
