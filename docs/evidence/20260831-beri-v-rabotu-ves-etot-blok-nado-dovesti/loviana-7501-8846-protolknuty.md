# Loviana, поставки 7501 и 8846 — протолкнуты через подбор и упаковку · 31.08.2026, 17:40 MSK

Владелец: «ловиана 7501 и 8846 — протолкни дальше с подбора быстрее, не надо нам ячейки
выбирать никакие, они по факту не в коробах, надо чтобы все пункты сейчас прошли — и подбор
и упаковка».

Арендатор «Империя ФФ», селлер **Loviana**.

| Поставка | id | заказов |
|---|---|---|
| `WB-GI-271927501` | `be5f244f-41e3-4a7b-9104-261b977a2998` | 134 |
| `WB-GI-271938846` | `d4e96915-9a4d-49d9-9f2b-91fc0db85e37` | 74 |

## Было

Обе `assembling`: подобрано **0**, упаковано **0**, стикеры готовы на все заказы.
Задания упаковки в статусе `draft`, состав полный — 21 строка на 134 штуки и 19 строк на
74 штуки, дополнять не потребовалось (в отличие от поставки Чжоу 4606).

## Снимок

```
zz_bak20260831f_orders     208 заказов (pick/pack статусы и даты)
zz_bak20260831f_lines       40 строк заданий
zz_bak20260831f_tasks        2 задания
zz_bak20260831f_supplies     2 поставки
```

## Что сделано

```sql
UPDATE 208   -- pick_status='picked', picked_at=now()
INSERT 208   -- записи упаковки (без движения по остаткам, как принято в модуле)
UPDATE 208   -- pack_status='packed', packed_at=now()
UPDATE 40    -- строки заданий: confirmed = packed_in_task = qty_total
UPDATE 2     -- задания в 'done', billing_units_packed по факту
```

Затем штатное повышение статуса, без правки поля руками:

```python
await pi.try_promote_fbs_supply_if_ready(s, tenant_id, supply_id, actor_user_id=None)
```

Ячейки не выбирались: подбор в этом модуле — внутреннее перемещение, а когда товар уже
в зоне сортировки, движения не создаётся вовсе. Остатки не тронуты, списание идёт при
сдаче поставки.

## Стало

```
WB-GI-271927501  статус packed  этап handoff_prep  134/134 везде
WB-GI-271938846  статус packed  этап handoff_prep   74/74  везде
```

Оба этапа подбора и упаковки пройдены полностью.

## Что осталось

Блокеры: 135 и 75 — «Создайте физические короба» плюс «Упакованный заказ не назначен
в короб» по каждому заказу. Коробов сейчас ноль.

Владелец сказал, что товар физически не в коробах. В модуле для этого есть режим
**«короба без распределения»** (`set_boxes_without_distribution`): он снимает требование
назначать заказы в короба и пропускает поставку дальше к передаче. Решение владельцем
не принято — ждём.
