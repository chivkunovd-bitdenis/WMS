# Браузерная проверка стенда 29.08.2026

Проверял руками в браузере на `https://web-production-9e7c1.up.railway.app`,
учётка `claude-check@example.com`, роль администратора ФФ, окно 1440×900.
Стенд стоял на коммите `f6ed6f46`.

Сверялся с требованиями владельца из `tasks/_zhurnal/TREBOVANIYA-CHATA-28-29-08.md`.

---

## Сходится с требованиями

**Меню.** «Ячейки» и «Инвентаризация» на местах.

**Карта склада заменила раздел «Ячейки».** Склады вкладками сверху, зона
«Без ячеек» со 132 штуками, внутри видна тара — короб `КР-000001` и грузоместо
`ГМ-000001`. Ячейки с содержимым, сканер сверху, кнопка «Создать ячейку».

**Пересчёт больше не падает.** Ошибки «объект уже переместили или удалили», на
которую жаловался владелец, нет. Окно открывается с составом, сканером и полем
комментария под пересорт.

**Инвентаризация проходит насквозь.** Ввёл факт 20 при 22 числящихся — строка
покраснела, показала «Недостача −2». Нажал «Провести» → «Проведено движений: 1»,
остаток товара стал 20, итог по зоне 132 → 130. Документ реально делает проводку.

**Раскладка внутри документа сортировки.** Встроена, старого блока под ней нет.

**Пропорции макета.** Замерил: таблица 1132 px, колонка «Ячейки склада» 320 px
при окне 1440. Ровно то, что выверялось.

**Снятие с ячейки и вложение в тару.** В окне «Куда положить» есть пункт
«Россыпь — вынуть наружу» и палеты `П-001001…004`.

**Каталог.** Чекбоксы у строк, колонка «В Wildberries» со значением и
«не передаётся». По отметке строки появляется кнопка «Задать остаток · N» над
таблицей справа. Зелёных графиков в строках нет.

**Окно остатка FBS.** Ползунок «Доля свободного остатка», галка «Одинаково по
всем складам», отдельные ползунки на каждый склад с сопоставлением склада WB,
подпись «N шт уедет в Wildberries прямо сейчас и будет пересчитываться само».

---

## Дефект 1. Раскладка тянет весь склад, а не документ

**Где:** документ сортировки → «Раскладка по ячейкам».

**Что видно.** Открыл приёмку `№000034`. В шапке документа: «Принято: 110 из 120».
Раскладка под ней пишет: «130 штук осталось поставить из **1 475 принятых** — это
2 объектов и 3 позиций россыпью». В таблице лежит «Корсет для осанки», который
минутой раньше пересчитывался на карте склада и к этой приёмке отношения не имеет.

**Требование владельца (дословно, 29.08):** «Нам приезжают все короба, все
грузоместа и палеты, вся тара по документу, который мы выбрали. То есть если там
задавали короба, значит они приезжают в сортировку».

**Причина.** Ручка `GET /warehouses/{id}/sorting-objects` работает по складу и про
документ ничего не знает.

**Важность:** высокая. Оператор видит чужую тару и может разложить не то.

---

## Дефект 2. Каталог уезжает вправо

**Где:** раздел «Каталог», вкладка «Товары», окно 1440.

**Что видно.** Замер: ширина таблицы 1488 px при видимой области 1130 px —
**уезжает на 358 px**. За правым краем остаются колонки «ТЗ», «ЧЗ», «Резервы» и
действия строки.

**Требование владельца (дословно, 29.08):** «Каталог. Все поехало нахрен. У тебя
кнопки все уехали».

**Важность:** высокая, правка фронта.

---

## Замечание. Кнопка сверх макета

Рядом с «Задать остаток · N» стоит вторая кнопка «Весь остаток на FBS». На
согласованном макете `/products.html` её нет. Владелец 29.08 требовал: «Надо как
на макете строго именно кнопка». Нужно решение: оставить или убрать.

---

## Не проверено

Подбор в отгрузке, подбор ФБС, сканер в подборе, наполнение грузоместа товарами,
а также снятие с ячейки и вложение короба в палету **в действии** — до них не
дошёл.

Отдельно: в середине проверки в панели браузера перестали проходить клики (не
работал даже пункт меню). Я ошибочно принял это за поломку кнопки «Положить» в
раскладке; контрольный клик показал, что дело в панели, а не в приложении.
После пересоздания вкладки всё восстановилось. **Вывод о неработающей кнопке
снят как недоказанный** — её ещё предстоит проверить.
