# Аварийный фикс S-03 · 20.08.2026

## Причина

Вкладка «Новые» запрашивала и одновременно отрисовывала 500 заказов. На рабочем
ноутбуке массовый выбор приводил к тяжёлой повторной отрисовке всей MUI-таблицы.

## Ограниченная правка

- Только для `status_group=new` клиентский лимит уменьшен с 500 до 100.
- Поставки и остальные вкладки сохраняют прежний лимит 500.
- Верхняя галка отдельно загружает все страницы по текущим фильтрам и выбирает
  весь доступный объём, не добавляя скрытые заказы в таблицу.
- Создание новой поставки принимает весь выбранный объём. Сервер сам делит
  обращения к WB на пачки по 100 заказов.

## Проверки до деплоя

- `npm run build` — успешно.
- Целевой Playwright: при 610 заказах таблица содержит только 100 строк, верхняя
  галка забирает две cursor-страницы API и показывает `Выбрано заказов: 610` —
  1 passed.
- Полный `ff-fbs-orders.spec.ts` — 7 passed.
- `git diff --check` — успешно.

## Прод

- Первый код hotfix: `cca8d1743845223f47baba86e9453f2c2e73f7df`.
- Массовый выбор всех отфильтрованных заказов: `db52e9c26ead3f2b65d03bdc09d5c84ced15f5b0`.
- Ветка опубликована: `origin/codex/hotfix-fbs-100-orders-20260820`.
- `/opt/wms` fast-forward выполнен на `db52e9c26ead3f2b65d03bdc09d5c84ced15f5b0`.
- Docker image `wms_prod-web:latest` и запущенный `wms_prod-web-1`:
  `sha256:9630a79f32f954cb9d9d7ca4eacf8bbeea03b945a1475fed44c88c54175f50be`.
- OCI revision label образа: `db52e9c26ead3f2b65d03bdc09d5c84ced15f5b0`.
- Локальная и production SHA-256 для `/srv/index.html` совпала:
  `3641331f36fb6cc6fd129a270061eb341b5b6d977c8656a224b76adfa457786c`.
- Локальная и production SHA-256 для `/srv/assets/ff-B5zV35YM.js` совпала:
  `0b09cf06bcb96c33e84471da09f32611b2fb4f8114625b80901c21cf5d45bd81`.
- `GET /api/health` — `{"status":"ok"}`.
- `GET /app/ff/fbs` — HTTP 200.
- Живой браузер после cache-bust загрузил `/assets/ff-B5zV35YM.js` и показал
  штатную форму входа WMS. Авторизованный операторский сценарий не выполнялся:
  в диагностическом браузере нет production-сессии.

Штатная серверная TypeScript-сборка была остановлена до переключения контейнера:
на VPS с 3.8 GiB RAM компилятор ушёл в длительное ожидание диска. Старый web всё
это время оставался доступен. Production image собран из локального `dist`, для
которого до загрузки прошли `npm run build` и Playwright; точные контрольные суммы
выше подтверждают, что запущен именно этот проверенный артефакт.
