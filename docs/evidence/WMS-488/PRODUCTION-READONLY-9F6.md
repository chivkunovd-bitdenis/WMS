# WMS-488 · обезличенный результат production READ ONLY для `9f6f1521`

21.09.2026 ведущий выполнил опубликованный `production_readonly_audit.py` в работающем API-контейнере. Источник агрегата — локальный вывод `/tmp/wms488-prod-readonly-audit-9f6.jsonl` (81 JSONL-строка); сырой вывод с перечнем псевдонимов аккаунтов в Git не переносится. Инструмент вызывает только GET через ASGI transport с существующими пользователями, подставленными как текущий пользователь; SQL-транзакции `READ ONLY` завершаются rollback. Это проверка серверных обработчиков и production-данных, **не вход по настоящему JWT** и не браузерный сеанс.

| Область | Результат |
|---|---|
| Каталог и связанные чтения | 50/50 записей аудита `pass`, 356/356 проверенных GET `pass`; 8 аккаунтов AVpack (7 seller-role, включая один с флагом менеджера, и один FF admin), остальные — контроль других арендаторов. Во всех проверенных ответах `foreign_seller_count=0` и `foreign_tenant_count=0`. Проверены списки товаров/WB, FF-каталог и страница, поиск SKU, scan, инвентарный отчёт и доступные вспомогательные маршруты; доступ определяется ролью. |
| Фоновые задачи по ID | 16/16 покрытых сценариев `pass`, 40/40 GET `pass`: семь seller-role читают доступные домашние задачи (14 ответов 200) и не читают проверенные чужие (21 ответ 404); FF admin читает доступные задачи обоих селлеров (5 ответов 200). Один из семи селлерских аккаунтов имеет флаг менеджера; его домашний и чужой сценарии прошли. |
| Недостающие данные production | 14 сценариев `not-covered`: для семи seller-role в существующих данных нет подходящих `tenant-wide` и `ff-only` фоновых задач. Отсутствие записи не засчитывается как отказ в доступе. |
| Ошибки проверки | `fail=0`, `error=0`. Сводная строка самого инструмента — `not-covered` из-за 14 отсутствующих классов, а не `pass` всей C9. |

После выкладки ведущий сверил состав контейнеров с production checkout `9f6f15211b55ea9914c9e12778ba0b6cb3e69b09`: API, worker и beat — по 798/798 файлов каждый без отличий. Веб-образ, запущенный `2026-09-21 02:21:14 UTC`, после локальной сборки из того же checkout на Node 20.20.2 с `npm ci` совпал по 180/180 файлам frontend/dist, без пропущенных, лишних и отличающихся файлов. Применена миграция `20260921_0489`; `/health` ответил `ok`, селлерский маршрут — HTTP 200. Эти сведения о версии и здоровье переданы ведущим после собственной сверки; сам READ ONLY аудит не вычисляет SHA веб-образа.

Граница C9: реальные логин/JWT, браузерное переключение вкладок на production, внешние WB/Ozon, физическая печать, мутации и отсутствующие два класса задач здесь не проверялись. Изолированные тесты этих классов описаны в [требованиях](../../requirements/WMS-488.md), но не заменяют production-наблюдение. Аудит не менял карточки, остатки, права или задания.
