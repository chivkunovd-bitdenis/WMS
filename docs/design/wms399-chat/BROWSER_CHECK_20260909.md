# WMS-399 — проверка координатора в Chrome, 09.09.2026

Проверялся только локальный синтетический прототип http://127.0.0.1:5199.
Изолированный headless Google Chrome, взаимодействие через видимые
роли/названия кнопок браузерным инструментом; постоянные Playwright-тесты
в проект не добавлялись. CUA оказался недоступен из-за блокировки Mac;
координатор основного выпуска явно указал рабочий отдельный Chrome.
Ни авторизация, ни реальные API склада/маркетплейсов не использовались.
React правки во всех проходах выполнял исключительно Claude Opus
claude-opus-4-7, effort max, resume8f165d2e-0399-4c15-a9be-cb182241cde4.

## Проверенные действия

- Default инбокс и Приёмка IN-2926 открылись; созданное текстовое сообщение
  появилось в ленте и превью канала. Ответ отправлен в существующую ветку.
- Из seed-фотографии открыт и закрыт lightbox.
- Из карточки открыт предпросмотр документа с таблицей и таймлайном.
  Через кнопку документа в редакторе открыт picker; поиск F-880, выбор,
  затем отправка карточки вывели её в ленту.
- Участники: оператор видит три имени и запрещённое приглашение; админ
  получает кнопку. Её нажатие показывает явную демо-заглушку, не создаёт
  настоящего участника. Это проверенное ограничение макета.
- Поиск «недовоз» дал seed-сообщение; клик привёл к
  #/c/conv_in2926?msg=msg_in2926_photo с видимым сообщением.
- Уведомления открылись; «Отметить всё прочитанным» обнулило badge
  и отключило кнопку. Переключатель «Только на упоминания» и выбор
  ежедневного digest меняют отображённое выбранное состояние.
- Role→seller скрывает внутренний складской канал, внутреннюю заметку
  в основной ленте и внутренний ответ в ветке. Текст этих сообщений
  отсутствовал в DOM роли селлера.
- Mobile: инбокс→канал по строке, кнопка возврата присутствует.
- Offline: отправка дала «Не отправлено. Отправим при появлении сети»;
  после выключения демо-offline нажатие «Повторить» убрало ошибку.
- Вложение: локальный buffer27bytes synthetic-wms399.txt через file chooser.
  При демо upload-fail показаны ошибка вложения и ошибка сообщения.
  После выключения failure «Повторить» показал прогресс0%, затем файл
  без ошибки. Никаких пользовательских файлов не передавалось.
- Outdated: карточки и контекст показали предупреждения об изменении.
  Denied403: вместо ленты «Нет доступа к каналу».
- Архив: Fashion Home показал «Только чтение», редактора нет.
  Невозможный поисковый запрос в инбоксе показал «Здесь ничего нет».

## Проверенные исправления Opus

В исходном проходе счётчик включал скрытый ответ; после исправления
на свежем seed роль селлера показывает2 вместо3. До правки body имел
1606px при desktop viewport1000 и1658px при mobile viewport844; после
правки height равен viewport1000/844. На mobile390x844 кнопка отправки
имеет x234,y782.5,w131,h36.5, полностью внутри окна.
Снимки до: desktop-20260909.png, mobile-conversation-20260909.png.
Снимок после: mobile-fixed-20260909.png.

В проверенных путях browser pageerror=[]; это не утверждение о покрытии
всех возможных действий. Typecheck/build PASS после данного прохода.

## Ещё проверяется

Авторский проход по COORDINATOR_COMPLETENESS_20260909.md продолжается:
даты поиска, reachable deleted/empty/loading, локальная демонстрация push,
отказ от remote fonts. Дополнительно выявлен необработанный badge
уведомлений: raw4 вместо visible3 для селлера; передан тому же автору.
Прототип пока не объявляется полностью принятым. Реальные push-разрешения
браузера не менялись, настоящие уведомления/письма не отправлялись.

## Сверка дополнительных состояний

Typecheck/build PASS. В Chrome пустой IN-2932 показал «Пока пусто»,
после отправки первого сообщения заглушка исчезла, сообщение появилось.
Демо-loading скрыл строки/ленту; deleted переключатель дал в preview
«Документ удалён. Ссылка недействительна». Поиск «недовоз» + дата с09.09
дал пустой результат; диапазон08.09–08.09 вернул seed-сообщение.
Локальный push denied показал предупреждение, granted убрал его.
Список внешних request URL в этой свежей странице пуст, pageerror=[].
Осталась последняя ролевая правка badge/read-all уведомлений у автора.
