# WMS-399 — статус дизайна

**09.09.2026:** Клод Опус завершил авторскую сессию дизайна. Прототип
`prototypes/wms399-chat/` содержит все запрошенные экраны, требования и
хендофф написаны. Реального браузерного прохода автор не выполнял —
следующий шаг за Codex: собрать (`vite build`), пройти `tsc -b --noEmit`
и проверить в браузере, вернув Клоду Опусу конкретные замечания без
переверстки.

Сессия дизайна `8f165d2e-0399-4c15-a9be-cb182241cde4`, модель
`claude-opus-4-7`. Первый заход не завершился без итога; второй заход
упёрся в лимит пятичасового окна (`out of extra usage`) и был досрочно
остановлен. Третий заход 09.09.2026 после явного возобновления координатора
довёл прототип до полного состава и выполнил авторские поправки совместимости с
установленными версиями зависимостей (MUI 9, TypeScript 6).

## Что теперь есть

- `docs/design/wms399-chat/REQUIREMENTS_RU.md` — продуктовые требования
  (интент, роли, IA, сценарии, состояния, вложения, уведомления, поиск,
  контракт интеграции, открытые вопросы). Формулировки скорректированы:
  примеры проблем описаны как проектные сценарии, а не подтверждённые
  инциденты; роли/каналы/статусы — проектные view-модели с явной
  оговоркой, что бэкенд опирается на существующие права ФФ и селлера.
- `docs/design/wms399-chat/DESIGN_HANDOFF_RU.md` — техническое описание:
  запуск, роутинг, токены, матрица достижимых состояний, контракты
  действий и данных, инварианты видимости, известные ограничения
  прототипа, что Codex не должен переделывать без владельца.
- `prototypes/wms399-chat/` — runnable Vite + React 19 + MUI 9 + TS 6
  прототип, зависимости берутся из симлинка `node_modules` основного
  фронта. Работает hash-роутинг, тема повторяет базу WMS.

## Экраны и состояния

Все обязательные состояния из требований достижимы через UI и/или
демо-меню (иконка молотка в топ-баре): инбокс, канал (лента, разделители,
system-события, внутренние заметки), композер (вложения с прогрессом,
неудачей и повтором, ограничения, offline), треды, реакции, followup,
пикер документа, карточка документа и её устаревшее/удалённое состояние,
предпросмотр документа в drawer, лайтбокс, ящик участников с правилом
приглашений, кросс-канальный поиск, центр уведомлений, настройки
уведомлений (push permission, digest, per-conversation mute), мобильная
и десктопная ширина.

## Ограничения, о которых нужно помнить

Полный список — в `DESIGN_HANDOFF_RU.md` §8. Коротко: нет реалтайма,
виртуализации ленты, серверной пагинации, реальных push и email,
антивируса вложений, аудита правок, персистентности драфтов между
перезагрузками. Автор дизайна не проверял макет вручную в браузере —
это следующая задача Codex.

## Что следующим шагом делает Codex

1. Собрать прототип (`node_modules/.bin/vite build` из
   `prototypes/wms399-chat/`) и пройти `tsc -b --noEmit`.
2. Открыть `http://127.0.0.1:5199/` и пройти по демо-меню все
   переключатели (роль, ширина, оффлайн, upload fail, permission
   denied, doc outdated). Убедиться, что переходы по маршрутам, отправка
   сообщений, треды, вложения, пикер документа, предпросмотр документа,
   лайтбокс, поиск, уведомления и настройки работают.
3. Вернуть Opus замечания по дизайну и по компиляции (если они есть).
   Codex не редактирует React-исходники прототипа сам; любые правки —
   включая технические — вносит Opus в той же сессии по возвращённой
   диагностике.
4. Обновить эту сводку, канонический бэклог WMS-399 и подтверждённый
   commit SHA.

## Проверка координатора после авторского прохода

По первому проходу координатор зафиксировал `npm run build` — PASS,
`npm run typecheck` — FAIL: неиспользуемый `Stack` в `Composer` и
отсутствующее свойство `notificationPrefs` в `NotificationsScreen`
(точная диагностика — в `TYPECHECK_20260909.txt`, обратная связь —
в `COORDINATOR_FEEDBACK_20260909.md`). Opus точечно устранил обе ошибки
в той же сессии: убрал неиспользуемый импорт `Stack` из `Composer.tsx`
и переключил `NotificationsScreen` на реальный источник настроек
`ui.notificationPrefs` (как это уже делает `PreferencesScreen`).

Автор дизайна повторные `vite build`/`tsc -b --noEmit`/браузер не
запускал сам; их прогоняет Codex и возвращает Opus диагностику.
React-исходники прототипа Codex не редактирует.

## Браузерная обратная связь (следующий заход, 09.09.2026)

Координатор открыл Chrome на `127.0.0.1:5199`, компилятор чист, runtime
`pageerror=[]` на проверенных путях. Обратная связь — в
`COORDINATOR_BROWSER_FEEDBACK_20260909.md`, скриншоты
`desktop-20260909.png` и `mobile-conversation-20260909.png`. Opus в той
же сессии устранил три отмеченные проблемы, сохранив композицию:

1. **Счётчик обсуждения включал скрытые ответы.** Селлер видел
   `Обсуждение · 3` при 2 видимых для его роли. Теперь `threadReplyCount`
   в `MessageBubble` считает только те ответы ветки, которые проходят
   через существующий селектор `visibleMessagesForActor(branch, currentActor)`
   — без новых счётчиков и без хранимых полей. Заодно устранены две
   родственные микро-протечки видимости: `ConversationRow` теперь берёт
   время последнего сообщения и признак «внутр.» из `lastVisible`
   (а не из глобального `last`), и `Composer` не рендерит превью для
   `replyToId`, если сообщение-цитата — internal, а читает селлер.
2. **Мобильный composer.** В нижней строке добавлен `flexWrap: 'wrap'`
   и `rowGap`; чип `Селлеру виден` завёрнут в `Box flexShrink: 0`, а
   кнопка `Отправить` получила `ml: 'auto'` и `flexShrink: 0`. На узком
   экране кнопка либо остаётся справа, либо аккуратно переносится на
   вторую строку с прижимом к правому краю — тесноты и обрезки нет.
   Цвета, иконки и логика видимости сохранены.
3. **Высота чата.** Корневой контейнер `App` теперь `height: '100vh'`
   и `overflow: 'hidden'` (вместо `minHeight`), у промежуточного flex-Row
   добавлены `minHeight: 0` / `minWidth: 0`, у боковой панели канала —
   `minHeight: 0` на обёртке и на внутреннем `Box flex: 1 overflow: 'auto'`.
   Скроллится только лента сообщений и панель контекста/ветки; шапка
   приложения, шапка канала и composer остаются видимы. Мобильная ветка
   `route.screen === 'conversation'` теперь тоже обёрнута во flex-Box с
   `minHeight: 0 / minWidth: 0`, чтобы наследовать ту же логику.

Повторную браузерную проверку и прогон компиляции запускает Codex.
Пока он не подтвердил результат — «дизайн готов» не заявляется.

## Координатор: повторная техническая и браузерная проверка

Typecheck и Vite build после поправок Opus — PASS. Пройден ряд сценариев
в отдельном headless Chrome. Найдены дефекты счётчика скрытых ответов и
размеров редактора/прокрутки; точные шаги в COORDINATOR_BROWSER_FEEDBACK_20260909.md.
Автору возвращены замечания, полной приёмки дизайна пока нет.

Координатор повторил проверку после этого прохода: typecheck/build PASS;
Chrome1440x1000 body1000px, Chrome390x844 body844px; кнопка отправки
целиком внутри viewport (x234,y782.5,w131,h36.5). После role→seller
seed-ветка показывает2 доступных ответа вместо3. Runtime pageerror=[] в
повторённых путях. Исходный brief ещё сверяется по COORDINATOR_COMPLETENESS_20260909.md.
