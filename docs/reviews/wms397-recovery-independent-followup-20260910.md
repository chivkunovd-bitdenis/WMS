**Срез `08a62fc9..655b566d5b314e1d4bee52f2f84e27e6a3336fa2` пока не принимаю: два конкретных P2.** Первый ломает действия с FBS-заказом, открытым из чата. Второй позволяет отправить восстановленное вложение, физический файл которого уже удалён после неуспешного завершения транзакции. P1 в проверенной дельте не обнаружил.

Полный отчёт и интеграционный патч прочитаны. Выводы ниже основаны на исходниках указанного SHA.

1. **P2 — общий чекбокс точного FBS-заказа выбирает UUID, но не сам заказ для последующих действий.**

   Новая ветка `load()` очищает `selectedCache`, затем записывает полученный заказ только в `orders`. Обычная ветка загрузки заполняет оба набора. Между тем общий чекбокс меняет только множество выбранных UUID, а `selectedOrders` вычисляется исключительно через `selectedCache`. Причина находится в [FfFbsOrdersScreen.tsx:628](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:628); потребители — [строка 895](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:895) и [строка 1027](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1027).

   Воспроизведение: открыть из чата доступный новый заказ без блокирующих ошибок, дождаться загрузки и отметить общий чекбокс таблицы. Получается `selected.size = 1`, но `selectedOrders.length = 0`: «Показать выбранные» пусто, «Сформировать поставку» и «Добавить в существующую поставку» отключены условием на [строке 1831](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1831). Индивидуальный чекбокс строки этот случай маскирует, поскольку отдельно заполняет кэш.

   **Самостоятельная проверка:** исполнил извлечённые из этого SHA функции загрузки и общего выбора с подставленным ответом API, полностью в памяти. Результат: один выбранный UUID, размер кэша **0**, разрешённых выбранных заказов **0**. Это проверка функций, не браузерный тест.

   **Исправление:** заполнять и актуализировать кэш полученной строкой также в ветке точного заказа. Это исправление требуется и в координаторском интеграционном патче — там повторяется та же ветка.

2. **P2 — после успешного удаления объекта и неуспешного commit черновик можно отправить с отсутствующим файлом.**

   `discard_draft_attachment()` выполняет SQL delete/flush и физическое удаление, после чего маршрут отдельно вызывает commit. Физическое удаление уже необратимо при откате SQL: [chat_service.py:669](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/services/chat_service.py:669), [chat_routes.py:702](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/api/chat_routes.py:702).

   Воспроизведение: удалить собственный draft; после успешного удаления объекта получить ошибку commit с откатом транзакции; перечитать черновики и прикрепить сохранившийся ID к сообщению. Список восстановления проверяет только SQL, а `_attach_uploads_to_message()` проверяет владельца, чат, `message_id` и размер, **но не наличие содержимого** — [chat_service.py:484](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/services/chat_service.py:484). Сообщение принимает вложение, скачивание которого затем возвращает `content_not_found`.

   **Самостоятельная проверка:** исполнил исходные функции удаления и присоединения с сессией и хранилищем, подставленными в памяти, и ошибкой commit. Получено: SQL-черновик присутствует, объект отсутствует; последующее присоединение устанавливает `message_id` при по-прежнему отсутствующем объекте. Реальную БД и файловое хранилище не использовал.

   Отчёт автора честно называет разрыв SQL/storage, но возможность затем успешно отправить битое вложение остаётся дефектом восстановления. **Минимальное требование к исправлению:** такой ID не должен приниматься как исправное вложение; нужен понятный отказ с возможностью повторить удаление либо загрузить файл заново. Новая таблица или журнал для этого замечания не требуются.

В остальных проверенных границах новых блокеров не нашёл. Список и удаление drafts проверяют текущий доступ к чату, tenant, uploader и conversation; администратор не получает права удалить чужой draft. Отправленный файл защищён проверкой `message_id` после блокировки строки с `populate_existing`. При обычном успешном завершении транзакций отправка и удаление согласованы через эту блокировку; собственный PostgreSQL-прогон я не выполнял.

Лимиты **25 МиБ на файл, 100 МиБ и 10 файлов** проверяются сервером. Бюджет незавершённых загрузок вычисляется по существующим строкам во всех чатах пользователя и сериализуется блокировкой пользователя. HTML/SVG не получают режим встроенного изображения; сохранены проверка MIME, защитные заголовки и скачивание нерастровых файлов как `application/octet-stream`.

Точный FBS endpoint выбирает заказ по **UUID + tenant + seller**, проверяет права и использует существующую проекцию рабочей таблицы. Нового списания или навигационного запрета эта дельта не добавляет. Интеграционный патч прошёл мой **`git apply --check` без применения** в coordinator; обработчики `open_inbound`, `supply_id` и `onDirtyChange` там существуют и должны сохраняться.

Границы приёмки остаются явными:

- У seller существующие приёмка и MP-диалог используются повторно. Для FBS-заказа, FBS-поставки и обычной отгрузки сохраняется проекция только для чтения: соответствующих рабочих seller-маршрутов в проверенном `SellerApp` нет. Изобретать страницы для закрытия этого разрыва не требуется.
- Черновики из чата с отозванным доступом продолжают занимать общий бюджет, но недоступны для восстановления и удаления. Это сохранённое ограничение, не закрытое данной дельтой.
- Клики после интеграции, Back/Forward/Reload, удалённое object storage и staging/production мной не проверены. Чужие тестовые и UI-результаты не считаю своими.

Выполнил две изолированные проверки функций, `git diff --check` и проверку применимости патча. Файлы, БД и чужие worktree не изменял; полные наборы тестов не запускал.

Автоматическая проверка доступа отклонила открытие локального браузера `127.0.0.1:5397`, поскольку доступ к адресу не разрешён. Отказ не обходил; собственной проверки интерфейса в этом review нет.