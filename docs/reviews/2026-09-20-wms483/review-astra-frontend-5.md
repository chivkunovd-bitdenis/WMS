# WMS-483 · перекрёстное ревью фронта, проход 5

**Вердикт: PASS для коммита `dd2c5cdcf54d3b4090cfda2889f2ae94987a5c71`.** Reviewer Astra high прочитал diff и проверил обработку состояния, а также локально исполнил чистые функции. Новых конкретных дефектов в проверенном изменении нет.

HTTP 500 при загрузке `/warehouse-bindings` теперь показывает ошибку загрузки, а не пустой справочник. Неразрешимый числовой ключ также вызывает ошибку. Правила преобразуются до обновления состояния экрана, поэтому неверно пустой редактор и дублирующий ключ не возникают. При повторном отказе загрузки может оставаться прежнее, уже нормализованное состояние; некорректное новое правило в редактор не поступает.

Проверены: пустой справочник и неизвестный номер вызывают исключение; неактивный WB №123 вместе с активным Ozon №123 и WB №777 даёт раздельные ключи; активная привязка с `served=false` участвует в определении площадки; при совпадении активных номеров очистка WB не удаляет лимит Ozon. Контракт одиночного и группового PUT этим коммитом не менялся и сохраняет предыдущий положительный вывод.

Пределы ревью: проверка diff, кода состояния и функций в памяти. Полная сборка, браузер с принудительным HTTP 500, API, production и живой WB в этом проходе не проверялись. Это техническое ревью, отдельное от аналитической приёмки.
