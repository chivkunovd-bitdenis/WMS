# WMS-483 · повторное перекрёстное backend-ревью, круг 4

**Вердикт: PASS** для `03870a7378b304ce72776b26dfa87050e5047dbc` относительно `dd2c5cdc`. Независимый reviewer Claude Opus 5 проверил точный diff, тест и вызовы; новых конкретных дефектов не нашёл. Файлы не менял.

При переходе `0 → пусто → 0` прежняя запись синхронизации `confirmed/0` переводится в `pending` только для новой явной установки WB-лимита 0 при включённой публикации. Это снимает прежнее десятиминутное подавление и открывает событийную отправку сразу после сохранения. Сохранение неизменённого явного нуля не сбрасывает таймер. Адресный тест подтверждает два PUT за две минуты при самовольной прибавке WB, отсутствие лишнего PUT при повторном сохранении, отсутствие повтора через пять минут и повтор через десять.

Изменение выполняется в той же транзакции и по паре «привязка + товар». Привязки и товары заранее выбираются в рамках селлера и арендатора; для Ozon новый переход не применяется. Последнее наблюдавшееся количество сохраняется. Если событийная публикация не состоится, `pending` остаётся для ближайшей общей сверки.

Границы ревью: тест прочитан, но reviewer не запускал ruff/mypy/pytest из своей среды; запуск разработчика — отдельное свидетельство. Браузер, стенд и живой WB reviewer не проверял. Приёмка C1–C11 остаётся отдельным заключением аналитика.
