# WMS-483 · перекрёстное ревью фронта, проход 4

**Вердикт: FAIL.** Проверен коммит `6c421a751dfdcafe089cd6266a53ec4783c96efa`. Прежний дефект с неактивным WB 123 и активным Ozon 123 устранён; активная коллизия номеров и привязка с `served=false` также корректно разрешаются. Reviewer Astra high подтвердил это исполнением чистых функций в памяти. UI и API в этом ревью не проверялись.

Остался дефект при отказе обязательного GET `/operations/fbs-sellers/{id}/warehouse-bindings`: `FfProductsFbsPage.tsx` заменяет HTTP 500 на пустой массив, а `ruleKeysForScreen` подавляет ошибку определения площадки и возвращает числовой ключ `{"777":0}`. Строка `wb:777` показывает пустое поле. Новый ввод 0 создаёт дубль `"wb:777"`, сохранение отклоняется; очистка удаляет лишь новый ключ и оставляет скрытый старый ноль. Это нарушает R7.

Нужно показывать существующую ошибку загрузки и не передавать ненормализованное правило в редактор. Замечание возвращено тому же исполнителю Opus для адресного исправления и теста.
