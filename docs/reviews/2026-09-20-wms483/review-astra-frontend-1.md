# WMS-483 · первое перекрёстное ревью интерфейса

Ревьюер: отдельный агент gpt-6-astra, high. Проверен коммит `1ac056373fc6ba858bcb2941fbd092714105e767` после полного чтения AGENTS.md, owner-cases.md, failure-cases.md и требований WMS-483. Ревью на чтение, без изменений файлов и обращения к живому WB.

**Вердикт: FAIL для R7 во всех входах в существующее окно.** Сам коммит корректно различает пустое поле и явный ноль в `FbsStockDialog` и сохраняет ключи в основном каталоге, но второй доступный вход `/ff/fbs-stock` не доставляет это правило в API.

1. **P1 · одиночное сохранение.** `frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:192–197` отправляет `publish`, `same_everywhere`, `percent`, `by_warehouse` без `units_mode` и `units_by_warehouse`. После ввода `0 шт.` серверная схема подставляет долевой режим и пустую карту, так что намерение оператора не сохраняется. Ожидание R7/C7 — явный ключ со значением 0 и поштучный режим.
2. **P2 · групповое сохранение.** Там же, строка 210, отправляется `{ product_ids, ...body }`, тогда как API `backend/app/api/products.py:435` требует `{ product_ids, rule: body }` и запрещает лишние поля. Выбор нескольких товаров завершится ошибкой валидации. Требование R7 должно работать и для этого входа в окно.

Проверенный основной путь: API `units_remaining_by_warehouse` → `toRule` → `qualifyWarehouseRuleValues` → черновик окна → PUT `units_by_warehouse` сохраняет отсутствие ключа отдельно от значения 0. Очистка `NumberInput` даёт `null`; новый helper удаляет выбранный ключ, оставляя остальные WB/Ozon. Изолированный browser preview `localhost:15183/products.html` показал читаемые пустые поля и подсказку без наложений. Живой API, production и WB ревьюер не вызывал; тесты/сборку повторно не запускал. Это техническое ревью, не продуктовая приёмка.
