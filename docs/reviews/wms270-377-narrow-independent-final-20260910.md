**ACCEPT в пределах узкого кодового ревью** `96a8dc4c9601e131bdd6f58cd35b2515db8d126e` относительно deployed `eaa6ae9aecee875c50b31afa6b2cc224bb57d043`. Новых подтверждённых P1/P2 не обнаружил.

Прочитал отчёт узкого выпуска и фиксированный Git-diff. Ранее принятые AUTH, proxy и deploy/network-защиты сохранены. Изменений frontend, миграций, stock и chat в этом выпуске нет.

Проверил следующие связи:

- Production-скрипт подключает `docker-compose.prod.yml` и присутствующий в коммите `docker-compose.wms-host-8088.yml`. Overlay задаёт привязку web к `172.18.0.1:8088` и доверенную для API сеть `172.21.0.0/16`; Caddy передаёт вычисленный client IP согласно ранее принятой конфигурации доверенных прокси.
- Успешный вход удаляет только собственную попытку. Моя отдельная проверка кода ограничителя подтвердила сохранение четырёх других попыток того же IP.
- Повторный deploy после остановки сервисов сохраняет принятую семантику `f0bd66a2`: остановленные контейнеры проверяются по сохранённому NetworkID и настроенному NetworkMode, работающие — также по активному адресу. Проверка выполняется до сборки и остановки; запуска старого API ради прохождения проверки нет. Backup и проверка архива предшествуют migration/start.

Самостоятельно выполнил только чистые проверки фиксированного кода и `bash -n`; набор тестов координатора не повторял. Сохранённый JSON подтверждает **ранее записанную координатором** проверку топологии, а не мою новую проверку сервера.

Граница отката: схема кандидата совпадает с deployed, поэтому этот delta сам по себе не требует downgrade. При откате приложения нужно сохранить принятые ограничения портов и proxy-конфигурацию: полный возврат старых deployment-файлов возвращает и прежние настройки доступа.

До закрытия WMS377 остаются проверка точного установленного SHA после CI, фактического подключения overlay и топологии, прохождения реального client IP через оба прокси с отклонением поддельных заголовков, внешней недоступности закрываемых портов и результата backup/start/HTTP-проверок. **Live deploy и браузер этим ACCEPT не приняты.** WMS111/112 остаются STOP; WMS418 не затрагивал.