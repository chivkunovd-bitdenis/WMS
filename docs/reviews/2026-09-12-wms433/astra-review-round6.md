# Ревью Astra · WMS-433 круг 6 · 13.09.2026

Проверены исправления `ae8c47f5` и `002b344c`. Итоговый HEAD — **`0abce1b7f5a703acc9b1adc84d7443ecec45f7d4`**, ветка `feat/wms433-assistant`. Последний коммит появился во время ревью и меняет только требования R17/C13; проверенный код остался прежним.

Файлы не изменял. Внешних обращений и реального `claude -p` не было. Фронтовые последовательности проверены на извлечённом коде компонента с подменой React-состояния, запросов и таймеров — **без визуальной проверки браузером**.

## 1. Статус №35–36

| № | Статус | Результат |
|---|---|---|
| **35. Одиночный код и код в списках** | **Исправлен для обязательного корпуса** | Все пять входов круга 5 — `import os`, `print("hello")`, `answer = 42`, список импортов и предложение с импортом/вызовом — блокируются обеими `sanitize_answer`. `os.getcwd()` тоже блокируется. Остаточный вариант того же класса указан ниже без блокировки. |
| **36. Продолжения запросов после размонтирования** | **Исправлен** | Во всех трёх последовательностях круга 5 после размонтирования — **ноль обновлений состояния и ноль новых GET**. Успех POST не восстанавливает локальные идентификаторы; ошибка POST не запускает перечитывание; завершение отменённого GET после закрытия → открытия → размонтирования не запускает GET №2. |

## 2. Оценка компромисса по коротким словам

**Компромисс приемлем для технического фильтра R17 с учётом R12.** Совпадения короткого слова с именем в коде недостаточно, чтобы запрещать рабочий ответ. Обе копии пропускают `Login на портале селлера`, `Просмотр Health системы`, `Статус: users не найден` и осознанно разрешённое `Таблица users хранит пользователей.`

При этом `Таблица assistant_messages; функция submit_executor_result` и `Класс autherror описывает исключение.` блокируются. Списки исполнителя и бэкенда совпадают: **3398 идентификаторов**; исполнитель проверен со списком, полученным через `_load_internal_identifiers(Path("backend/app"))`.

Критерий «≤6 символов» не доказывает, что слово обычное, но как ограниченное исключение вместе с положительным корпусом он достаточен для этого круга. Дополнительного ужесточения не предлагаю. Пропуск фразы про `users` означает допустимость для фильтра; качество реального пользовательского объяснения остаётся предметом проверки инструкции модели.

## 3. Новые подтверждённые дефекты

**Новых блокирующих дефектов не обнаружено.** В выполненных проверках раскрытие секретов или чужих данных не подтверждено. Найденный дополнительный пропуск относится к прежнему классу фильтра.

## 4. Замечания без блокировки

**Класс фильтра: `print(42)` проходит обе копии без замены.**

Воспроизведение: передать строку `print(42)` в серверную `sanitize_answer` и в исполнительскую `sanitize_answer` с реальными идентификаторами. Обе возвращают **`print(42)`**. Одиночный вызов без кавычек и точки не получает сильного признака кода, а одной строки без отступа недостаточно для оставшейся проверки.

Из пяти собственных негативных входов остальные четыре блокируются:

- `from math import sqrt`
- `> - print('ok')`
- `1) retries = 3`
- `Вызов os.listdir() показывает каталог.`

Уточнение к описанию №36: `requestReload` запрашивается также после ошибки POST для восстановления ленты. Тики опроса его больше не устанавливают. Это поведение прошло проверку отката отправки.

## 5. Что проверено и совпало

**Фильтр:** 34 уникальных положительных примера из сохранённых тестовых корпусов проходят обе копии; 31 отрицательный пример текущего обязательного корпуса блокируется. Дополнительно прошли все заданные рабочие фразы с итогом, скидкой, email, Login, Health, users и пять собственных примеров с PDF, CSV/Excel, SKU/OK, status/FBS и Login внутри инструкции. Ложных отказов на проверенном наборе нет.

**Регрессии части 1:** №18, №21, №28, №29, №30 прошли по тестам. Восстанавливается опубликованный номер, выполняется уборка после ошибки подготовки, подставные карточки не попадают чужому тенанту, незавершённые вопросы исключают регистрацию через `duplicate_of`, вид карточки определяется правильно.

**Регрессии фронта:** №22, №25, №31, №32, №34 прошли. Старый GET сохраняет новое сообщение; медленный GET применяет ответ без лишнего запроса; свежая выборка содержит ровно последние 500 строк без старого A. После отказа POST текст восстанавливается, перечитывание возвращает 500 строк, повтор использует прежний идентификатор отправки.

| Проверка | Результат |
|---|---|
| Ruff без файлового кэша | Прошла |
| Mypy с кэшем `/dev/null` | Прошла, 449 файлов |
| Штатный `pytest -n auto` | Остановился до тестов: запрещены временные файлы |
| Те же три файла, SQLite в памяти, один процесс, без файлового захвата и кэша | **50 прошли** |
| Unittest исполнителя | **115 прошли**, 17 остановились на временных каталогах |
| Повтор доступных файловых сценариев с хранением в памяти | **Ещё 14 прошли**; три теста настоящих временных Git-репозиториев не выполнены |
| `npx --no-install tsc --noEmit -p tsconfig.app.json` | Прошла |

**Целостность:** в `675e69c7..HEAD` **26 коммитов**, все с префиксом `WMS-433:`. Появившееся во время ревью изменение требований сохранено коммитом `0abce1b7`. На итоговой проверке **рабочее дерево чистое, незакоммиченный diff пуст**.

## 6. Вердикт

**Часть 1 — принять.** №35 закрыт по обязательному корпусу; `print(42)` остаётся неблокирующим замечанием класса фильтра.

**Часть 2 — принять.** №36 исправлен, указанные регрессии не вернулись.

Вердикт относится к локальному ревью и выполненным проверкам. Визуальная приёмка, живые провокации модели и проверка выложенной версии не выполнялись.