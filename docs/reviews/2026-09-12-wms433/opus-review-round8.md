Проверки завершены, worktree чист (ни одного изменённого файла, HEAD dba08e9b), стенд не затронут. Санити-чек: все утверждения ниже — из воспроизведений кодом и прогонов в этой сессии (160 unittest исполнителя OK, 51 pytest passed, ruff и mypy чистые).

# Ревью Opus · WMS-433 круг 8 · 13.09.2026

Проверен коммит `dba08e9b` (worktree `.worktrees/wms433-assistant`, ветка `feat/wms433-assistant`). Файлы не менял, стенд 127.0.0.1:18080 не трогал, `claude -p` не вызывал; воспроизведения — настоящие функции с подменой сети/git/CLI/часов.

| № | Статус | Чем подтверждено |
|---|---|---|
| 37 | **исправлен** | Реальная `redact_unsafe_fragments` со списком из `backend/app` (3398 имён): код с отступом → `Вот пример:\n\n[фрагмент скрыт]`; `SELECT\n id,\n email\nFROM users` → `[фрагмент скрыт]` целиком; PEM (BEGIN+тело+END) → один плейсхолдер, и внутри текста тоже; `Ошибка (/srv/private/thing.conf) — повторить.` → строка убрана; при пустом списке `/srv/assistant_messages.py/assistant_messages` → убрано; `После Update повторите вход.` — не тронуто. Контрольная проверка: `Запрос: SELECT id\nFROM users` эвристика оставляет, `contains_unsafe_content` ловит → в карточку идёт `answer_text` (проверено сквозь `process_one`). |
| 38 | **исправлен** | `process_one` со сводкой `` `post_supply()` `` и с ограждённым блоком: карточка получает `answer_text`, ни `[скрыто]`, ни заглушки. |
| 39 | **исправлен** | `+7 (999) 123-45-67`, `8 (999) 123-45-67`, `+79991234567`, `8-999-123-45-67` → `[телефон скрыт]`; `1120 px: +120+170` и `+120 и +170, всего 1410` — не тронуты. Полный путь `format_backlog_card → list_chat_backlog_candidates`: почта и телефон скрыты, «+120+170» остался. |
| 40 | **исправлен** | Бэк (SQLite, свой тест): создано 40 мин назад → `executor_attempts=0`; захват → 1; повторный захват до таймаута — не выдаётся, счётчик не растёт; после протухания захвата → 2, → 3; `session.refresh` после UPDATE, значение отдаётся в `/executor/next` (тест API). Исполнитель: возраст 40 мин + attempts=1 или 2 + сбой модели → `AssistantAgentError`, карточки и ответа нет; attempts=3 → запасной ответ + карточка «баг» с «за 3 попыток захвата». Поле отсутствует (старый сервер) → 0, без отказа. `alembic heads` — единственный head `20260913_2200`, down_revision `20260912_0305` верный, ревизует её только этот файл. |
| 41 | **исправлен** | (а) На этом репозитории (822 ссылки): новая `find_next_wms_number` — 5 git-вызовов, **0.90 с**; старая (из 5b9fe59b) — 1915 вызовов, **88.7 с**; результат одинаковый (447). (б) Подмена часов: сценарий Astra (119+119+599) → отправка на 837 с; git 500+500 → модели уходит 140 с; 500+500+модель 100 с при карточке → «бюджет исчерпан до push», отправки нет; 600+540 → выход до модели; 1100 на checkout кода → выход до checkout бэклога. (в) `EXECUTOR_CLAIM_TIMEOUT=20 мин`, формула в коде 960 с < 1140 с дедлайна исполнителя < 1200 с. |
| 42 | **исправлен (код), теста нет** | Изолированный temp: живой pid + возраст 30 мин и 3 ч — оставлен; мёртвый pid + 5 с — удалён; без файла/битый JSON/без pid + 30 мин — удалён; без файла 30 с — оставлен; pid 1 (PermissionError) — оставлен. Реальный `_clone_with_real_origin` на этом репо пишет `owner.json` с pid процесса. **Юнит-теста на живой/мёртвый pid в коммите нет** — три теста `CleanupOrphanedCheckoutsTest` проверяют только возраст. |

`--once`: пустая очередь → 0 и «очередь пуста»; `AssistantAgentError` и `KeyError` → код 1, «очередь пуста» не печатается, причина в stderr; `line_buffering=True` на обоих потоках (проверено в реальном подпроцессе).

Регрессии: C16 (`test_result_is_idempotent_and_conflicts_on_different_content`, `test_repeated_result_submission_is_idempotent_not_duplicated`), №18/21/28/29/30 — в прогоне, зелёные. `unittest discover` — 160 OK; `pytest -n auto` три файла — 51 passed; `ruff check .` и `mypy .` (450 файлов) — чисто.

## Новые подтверждённые дефекты
нет.

## Замечания без блокировки
1. **Миграция 20260913_2200 не выполняется на SQLite**: `op.alter_column(..., server_default=None)` рендерится в `ALTER TABLE … ALTER COLUMN … DROP DEFAULT` → `OperationalError`, колонка добавлена, версия не проставлена (воспроизведено stamp'ом на 20260912_0305). Не блокер: alembic в проекте — для Postgres (там синтаксис верный), вся цепочка на SQLite и так падает на 0005, тот же приём в 20260910_0100; локальный стенд создаёт схему через `WMS_AUTO_CREATE_SCHEMA=1`/`create_all`. Но `create_all` не добавляет колонку в уже существующую `dev.db` — аналитику на повторной приёмке нужно убедиться, что на стенде колонка есть, иначе все запросы помощника упадут.
2. №42: при переиспользовании pid ОС сирота становится «вечно живым» и не убирается никогда; `started_at` записывается, но не используется. Плюс отсутствие теста (см. таблицу).
3. №41: остаток бюджета передаётся таймаутом только модели; git-шаги сохраняют фиксированные таймауты и лишь проверяются «≥120 с до старта». Участок после последней проверки (`create_backlog_card` + отправка) по таймаутам может занять до ~390 с при покрытии 180 с — в модели часов отправка на 1227 с > 1200 с. Требует зависания push и локальных git-команд у их потолков; дубль ответа гасит C16.
4. №40: время до честного запасного ответа выросло с 3×12 до 3×20 минут (захват держится 20 мин даже после отказа модели за 10 мин) — по требованиям допустимо (ответ конечен), но пользователь ждёт до часа.
5. №41(а): старый обход `docs/requirements` был рекурсивным (`ls-tree -r`), новый дампит только верхний уровень дерева; сегодня во всех 13 уникальных деревьях подкаталогов нет, результат идентичен — расхождение латентное.
6. №39: голые 10–15-значные числа прячутся как телефон («Заказ 12345678901», «ШК 4601234567890», «Артикул 2024-09-12-0001»); 11+ прятал и старый шаблон, 10-значные — новое. Ухудшает суть кандидата, данных не раскрывает.
7. №38: `_has_meaningful_content` требует ≥20 символов — короткая честная сводка («Ошибка прав.») подменяется `answer_text`.
8. Вне коммита, пре-существующее в `sanitize_answer` (обе копии): `Ошибка (/srv/private/thing.conf)`, `~/Projects/WMS/backend/dev.db`, `docs/requirements/WMS-433.md`, `config/settings.yaml` доходят до пользователя (путь в скобках/с тильдой/относительный без .py). Карточный путь первые два ловит. По 4.3 — замечания в бэклог, не блокер этого круга.

**Вердикт: принять.** Все шесть дефектов №37–42 воспроизведены входами ревьюера и закрыты; новых подтверждённых дефектов, нарушающих требования или раскрывающих секреты/чужие данные, нет. Замечания 1–2 (колонка на стенде для повторной приёмки, тест на pid) рекомендую закрыть попутно.

result: WMS-433 круг 8 (Opus): коммит dba08e9b — дефекты №37–42 подтверждённо исправлены, тесты/ruff/mypy зелёные, новых блокирующих дефектов нет, вердикт «принять» с 8 замечаниями без блокировки.
