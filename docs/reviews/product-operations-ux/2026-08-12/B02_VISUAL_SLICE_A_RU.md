# B02 Visual Slice A — документы empty, создание товара, populated-каталог и ТЗ упаковки

## Граница

Лично открыты через `view_image` только `b02-001`–`b02-011`. Таблица ниже описывает исключительно то, что видно в соответствующем PNG, с учётом отдельно переданных runtime-фактов для действия до/после. Кадры `012+` в этом микросрезе не оценивались. Общий gate B02 не выносится.

`b02-008` исключён как доказательство 1920×1080: несмотря на имя файла, измеренный CSS viewport оставался `1280×720`, `DPR=2`, `scrollWidth=2041`. Он не закрывает 1920 gate.

| PNG | Что визуально доказано | Layout / usability verdict | Process verdict | Статус | Риск для пользователя с низкой цифровой грамотностью |
|---|---|---|---|---|---|
| `b02-001-seller-documents-empty-1280x720.png` | Seller `/documents`: заголовок, три CTA, фильтры и честный empty-state «Пока нет документов» полностью видны на 1280×720. В шапке видны email и сырой role-code `fulfillment_seller`. | Основные действия помещаются и визуально различимы; роль остаётся технической и не показывает бренд/селлера человеческим языком. | Empty state достижим, но не доказывает populated документы или row action. | `PASS_WITH_FRICTION` | Может не понять, чей кабинет открыт; кнопка акта выглядит рабочей, хотя её реальный outcome этим кадром не доказан. |
| `b02-002-admin-product-dialog-empty-1280x720.png` | Admin dialog «Создать товар»: seller, название и SKU помечены `*`; видны optional vendor, size, barcode, ТЗ, dimensions, ЧЗ, cancel/create. | Поля собраны в одну форму; подписи dimensions обрезаны многоточием (`нео...`), а различие SKU/«Артикул продавца» не объяснено. | Role boundary видим: товар заводится в FF/admin UI с привязкой к seller, не в seller portal. Сам кадр не доказывает required-field error. | `PASS_WITH_FRICTION` | Может перепутать два артикула и не понять единицы/обязательность обрезанных размеров. |
| `b02-003-admin-product-a-filled-1280x720.png` | Synthetic seller выбран; заполнены name, unique SKU, seller article, size M и synthetic barcode; Create доступен. | Заполненная форма читаема, но bottom-right close к краю, есть внутренний scroll; поле ТЗ пусто без подсказки о последствиях для ФФ. | Доказана готовность ручного no-WB ввода до submit; факт создания подтверждается следующим кадром, не этим. | `PASS_WITH_FRICTION` | Без пояснения может решить, что все optional поля можно безопасно пропустить, хотя упаковке/печати могут понадобиться ШК и ТЗ. |
| `b02-004-admin-product-a-readback-1280x720.png` | После создания в каталоге видна seller-bound строка, «Заполнено» и ссылка «ТЗ», все остатки 0; runtime передан как double-click create → один товар. | На 1280 таблица обрезана слева/справа: название/SKU и часть колонок не попали в кадр, явной локальной горизонтальной подсказки нет. | Один product A создан и прочитан обратно; double-click не породил дубль. Передача admin→seller должна подтверждаться seller-кадром `007`. | `PASS_WITH_FRICTION` | Может не увидеть, какой именно SKU создан, и принять нулевые складские показатели за ошибку создания. |
| `b02-005-admin-product-duplicate-sku-error-1280x720.png` | Диалог остаётся открыт, введённые значения сохранены; сверху ясная ошибка «Такой артикул (SKU) уже есть». | Ошибка заметная, конкретная и не стирает форму; retry возможен. | Duplicate SKU корректно отклонён без второй записи. Separate duplicate barcode outcome этим кадром не доказан. | `PASS` | Низкий риск: ясно, что изменить. Полезно дополнительно подсветить конкретное поле SKU. |
| `b02-006-admin-product-b-readback-1280x720.png` | В admin-каталоге после второго создания видна seller-bound строка B, «Заполнено», «ТЗ», все остатки 0. | Таблица снова горизонтально смещена/обрезана; в строке видна только конечная часть имени «товар Б», идентификация затруднена. | Product B создан и прочитан обратно; вместе с `004` обеспечивает два synthetic товара для seller scope, но полный seller scope доказывает `007`. | `PASS_WITH_FRICTION` | Может открыть/напечатать не тот товар из-за скрытых ключевых колонок. |
| `b02-007-seller-products-populated-1280x720.png` | Seller видит ровно два synthetic товара A/B с SKU, size, barcode, vendor article, name и нулевыми первыми stock-колонками; WB nm отсутствует; API sync CTA и FBS block видимы. | Горизонтальный overflow обрезает правые stock/action columns; explanatory sentence также обрезана. Опасные внешние/bulk CTA визуально сильнее локального ТЗ. | B01 populated-products fixture gap закрыт: seller получил оба seller-scoped товара. Нулевой stock физически ожидаем до приёмки. Других tenant rows в кадре нет, но абсолютную изоляцию одним кадром не доказать. | `FAIL_UX` | Может не найти ТЗ/остаток «К отгрузке», случайно выбрать API sync/FBS вместо подготовки товара или решить, что товар неполный. |
| `b02-008-seller-products-populated-1920x1080.png` | Видна только узкая верхняя полоса страницы; контент/таблица отсутствуют. Runtime: фактически `innerWidth=1280`, `innerHeight=720`, `DPR=2`, `scrollWidth=2041`. | Кадр malformed/mislabeled; никакой layout-проверки 1920×1080 не даёт. | 1920 viewport gate не выполнен; нельзя переносить verdict из имени файла. | `NOT_RUN` | Если принять имя за доказательство, аудит ложно пропустит скрытые действия на широком экране. |
| `b02-009-seller-packaging-task-open-1280x720.png` | Dialog «ТЗ на упаковку» показывает конкретные SKU/name, marking checkbox, понятную инструкцию для ФФ и CTA Cancel/Print/Save. Переданный runtime: перед этим изменение отменили, старый текст сохранился. | Диалог компактный, читаемый, все действия видны; «Печать» рядом с «Сохранить» без объяснения, печатается текущий draft или сохранённая версия. | Cancel не изменил сохранённое ТЗ; физическая инструкция для следующей роли ФФ представлена человеческим текстом. | `PASS_WITH_FRICTION` | Может распечатать несохранённый/старый вариант или не понять, обязательно ли печатать и прикладывать его к коробу. |
| `b02-010-seller-packaging-task-saved-1280x720.png` | Dialog закрыт; в обеих строках статус «Заполнено» и action «Редактировать». Переданный runtime: save выполнен, но новое содержимое на этом кадре не видно. | Горизонтально смещённая таблица скрывает идентификаторы строк слева; right action виден, связать его с конкретным SKU трудно. | Видимый post-save outcome — возврат в каталог; durable content должен подтверждаться `011`. Отдельная защита double-click одним PNG не доказана. | `PASS_WITH_FRICTION` | Может отредактировать не тот SKU, поскольку на текущем scroll position видны ТЗ/actions, но не видны имена товаров. |
| `b02-011-seller-packaging-task-reload-readback-1280x720.png` | После reload/reopen у product A видны сохранённый новый текст с суффиксом B02 и включённый checkbox ЧЗ. | Текст и marking state читаются, product identity видна в заголовке; CTA доступны. | Save durable: инструкция и checkbox пережили reload. ФФ получит persisted packing requirement; фактическая печать/доставка инструкции не доказаны. | `PASS` | Низкий риск для read-back; остаётся неопределённость, что именно селлер должен распечатать/передать ФФ физически. |

## Итог микросреза

- Лично просмотрено PNG: **11 из 11** (`001–011`).
- `PASS`: **2** (`005`, `011`).
- `PASS_WITH_FRICTION`: **7** (`001`–`004`, `006`, `009`, `010`).
- `FAIL_UX`: **1** (`007`).
- `NOT_RUN`: **1** (`008`, invalid 1920 evidence).
- `FAIL_PROCESS`, `BLOCKED_FIXTURE`: **0** в этом микросрезе.

Это counts по PNG микросреза, а не по 63 execution-checks и не gate всего B02.
