# B03. Sanitized state log физической приёмки

Лог фиксирует только идентификаторы synthetic fixture и наблюдаемое состояние staging. Секретов, cookies, токенов и credential payload здесь нет. Browser network не использовался как замена визуальной проверке.

## Среда

- Staging: `https://web-production-9e7c1.up.railway.app/`.
- Проверка выполнена настоящим in-app Browser.
- Основной CSS viewport: `1280×720`, `devicePixelRatio=1`.
- Дополнительный доказанный CSS viewport: `1920×1080`, `devicePixelRatio=1`.
- Роль: fulfillment admin в уже существовавшей безопасной staging-сессии.

## Exact synthetic inbound

- Request ID: `41823675-2b08-4714-97b6-8782486c4dda`.
- Человеческий номер: `000007`.
- Seller: `B01 UX Seller 960724`.
- Плановая дата: `2026-08-12`.
- План коробов: `2`.
- SKU A: `B02-UX-35204480-A`, ШК `299835204480`, план `3`.
- SKU B: `B02-UX-35204480-B`, ШК `299735204480`, план `2`.

Совпадение доказано не порядком строки, а seller/date/line count в очереди и human number, двумя SKU, двумя barcode, планом `3/2` и boxes=`2` в detail.

## Контрольные переходы

1. Исходно: `submitted`, факт A=`0`, B=`0`.
2. После первого валидного скана A: статус `receiving`, A=`1`, B=`0`; reload сохранил A=`1`.
3. После повторных сканов: A=`3`, B=`0`; reload сохранил факт.
4. Ручное `2.9` для B сохранилось как `2` без ошибки; reload подтвердил `2`.
5. Пустое значение B было молча проигнорировано: B осталось `2`; reload подтвердил `2`.
6. Ручное A=`5` сохранилось как overage и пережило reload; затем факт восстановлен до A=`3`, B=`2`.
7. Создан ровно один тестовый короб. В коробе A=`1` при loose fact A=`3` дал общий факт A=`4`; добавление B=`1.9` сохранило B=`1` в коробе и общий B=`3`. Reload сохранил композицию. Короб очищен и удалён; reload подтвердил отсутствие коробов.
8. Underage B=`1` открыл confirmation. Cancel оставил документ в `receiving`; reload не провёл stock transition. B восстановлен до `2`.
9. При плане `2` короба, факте A=`3`/B=`2` и нуле фактических коробов завершение прошло без предупреждения о коробах. Double-click дал один переход в `sorting`.
10. После завершения документ исчез из Reception, появился в Sorting с остатком `5` и строками A=`3`, B=`2`.
11. FF stock read-back: A — total=`3`, unpacked=`3`, packed=`0`, sorting=`3`, cells=`0`, available=`0`; B — `2/2/0/2/0/0`.
12. Один клик `Редактировать` без confirmation вернул документ в `receiving` и временно обнулил оба stock read-back. Документ затем повторно завершён, чтобы не разрушить B04 fixture.

## Финальное состояние для B04

- Документ `000007` отсутствует в Reception и присутствует в Sorting.
- Accepted A=`3`, B=`2`; remaining sorting qty=`5`.
- Фактических synthetic коробов нет; тестовый короб удалён.
- Stock A/B снова находится только в `sorting`, available=`0`.
- Distribution/putaway не выполнялись.

