# B02 Visual Slice B — seller inbound draft: товары, количества, короба, reload и удаление

## Граница

Лично открыты через `view_image` только `b02-012`–`b02-027`. Один PNG не доказывает невидимый network/state outcome, поэтому таблица соединяет видимое только с явно переданными runtime-фактами. Layout crop оценивается отдельно от продуктового процесса. `b02-015` и `b02-016` — преждевременные transitional captures и исключены как `NOT_RUN / INVALID_EVIDENCE`, а не записаны как app failures. Кадры `028+` и полный B02 не оценивались.

| PNG | Что визуально доказано | Layout / usability verdict | Process verdict | Статус | Риск для пользователя с низкой цифровой грамотностью |
|---|---|---|---|---|---|
| `b02-012-seller-inbound-new-empty-1280x720.png` | `/seller/inbound/new`: автоматически появился «Черновик» с сегодняшней датой, планом 1 короб, пустой таблицей, CTA add/save и disabled submit. | Верхняя форма помещается, но колонки «Наименование»/«Кол-во» визуально налезают; screen title говорит «Новая», не показывает номер/ID и не предупреждает, что документ уже создан. | Сам вход на `/new` уже мутирует данные: draft создан до осознанного save. Нет явного cancel/delete draft. | `FAIL_PROCESS` | Пользователь может просто посмотреть форму/уйти и не понимать, что оставил реальный документ. |
| `b02-013-seller-inbound-product-picker-1280x720.png` | Picker показывает ровно два seller-scoped synthetic товара с SKU, ШК, size, vendor article, name и полями количества; Cancel/Add видны. | Диалог читаем, search/category и обе строки помещаются; обязательность и допустимый диапазон количества не обозначены. | Populated picker и role scope доказаны. Seller имеет достаточно идентификаторов, чтобы выбрать физический товар, если знает SKU/ШК. | `PASS_WITH_FRICTION` | Может оставить количество пустым/нулевым и ожидать пояснения, которого форма заранее не даёт. |
| `b02-014-seller-inbound-picker-invalid-qty-1280x720.png` | После попытки с `0/-2` picker закрыт, lines пусты и alert отсутствует; runtime: оба значения handler молча пропустил. | Экран выглядит так же, как до попытки; отсутствует feedback, какое значение было неверным и что не добавилось. | Невалидные quantities не создали lines, но процесс провалился молча вместо validation/recovery. | `FAIL_UX` | Решит, что кнопка не сработала, повторит действие или уйдёт с пустым документом. |
| `b02-015-seller-inbound-two-products-1280x720.png` | Transitional picker показывает 3/2, но Add и Cancel temporarily disabled. | Кадр снят до settled outcome; disabled state без spinner мог бы выглядеть зависанием, но длительность/результат кадр не доказывает. | Не использовать как доказательство добавления или app failure; authoritative next steps — `017` и `022`. | `NOT_RUN` / `INVALID_EVIDENCE` | Риск по этому кадру не adjudicated: transient frame не позволяет отличить нормальный busy от зависания. |
| `b02-016-seller-inbound-hidden-invalid-lines-save-1280x720.png` | Transitional frame снова показывает пустую таблицу и boxes 2; Save в focus. | Кадр преждевременный и не фиксирует settled state; нельзя выводить, что строки потеряны/скрыты. | Исключён как invalid evidence, не app defect. | `NOT_RUN` / `INVALID_EVIDENCE` | Нельзя приписывать пользователю риск по неустоявшемуся кадру. |
| `b02-017-seller-inbound-picker-valid-quantities-1280x720.png` | Picker settled: A=3, B=2, Add active; identifiers полностью видимы. Runtime: эти quantities затем добавлены. | Хорошая читаемость и явная основная кнопка; нет итогового total units/selected positions. | Корректные quantities можно задать и применить; физический план 5 единиц сформирован. | `PASS_WITH_FRICTION` | Может ошибиться в сумме перед коробами: система не показывает «2 позиции / 5 единиц». |
| `b02-018-seller-inbound-invalid-box-count-1280x720.png` | Boxes field содержит `0`, visible alert отсутствует; строки присутствуют, но таблица на 1280 сильно сломана — название идёт вертикально по буквам. | `FAIL_UX`: товарная строка почти нечитаема; operator не может быстро сопоставить name/qty. | Кадр не доказывает сохранение 0, но показывает отсутствие немедленной field error. Intended validation не объяснена. | `FAIL_UX` | В спешке не сверит товар и может считать 0 короб допустимым значением. |
| `b02-019-inbound-line-zero-after-blur-1280x720.png` | Quantity A вручную установлено `0`; после blur alert отсутствует. Qty B=2 видна ниже; таблица по-прежнему деформирована. | Field accepts visibly invalid zero без inline error; layout делает name вертикальным и связь name→qty слабой. | Нулевое expected quantity остаётся в форме после blur. Дальнейшая durable mutation этим кадром не доказана, потому что следующий `/new` создаёт другой draft. | `FAIL_UX` | Может передать/сохранить физически бессмысленную строку 0 или не понять, применилось ли значение. |
| `b02-020-inbound-line-zero-reload-readback-1280x720.png` | После reload literal `/seller/inbound/new` открыт новый пустой draft: boxes снова 1, lines нет, status «Черновик». | UI нигде не сигнализирует «создан новый черновик» и не предлагает восстановить предыдущий. | Это не read-back старого zero: `/new` создал новый документ и потерял рабочий контекст. Старый filled draft не удалён, но исчез из текущей работы. | `FAIL_PROCESS` | Уверен, что потерял работу; начнёт заново и размножит документы. |
| `b02-021-documents-after-new-route-reload-1280x720.png` | Documents route открыт, но capture oversized/cropped: таблица rows ниже не видна, CTA/filters обрезаны справа. Runtime: список уже содержал накопленные drafts. | Как layout evidence кадр слабый из-за неверной геометрии; row-level список этим PNG не доказан. | Переданный runtime подтверждает накопление drafts, но точный visible rows outcome нужно брать из authoritative later frame, не из этого crop. | `PASS_WITH_FRICTION` | Даже если draft есть ниже, пользователь без подсказки должен искать его среди документов; кадр не показывает признаки различения. |
| `b02-022-recovered-two-line-draft-1280x720.png` | После ручного выбора строки открылся старый draft с A=3/B=2 и enabled submit; boxes сейчас 1. | Таблица на full-page 1280×1062 физически нечитаема: names вертикально, header overlaps, приходится прокручивать. | Старый filled draft durable и recoverable, но recovery только через ручной выбор в списке, не через reload `/new`. | `PASS_WITH_FRICTION` | Может не найти нужный draft и не отличить его от пустых, особенно без номера/ID в заголовке. |
| `b02-023-inbound-boxes-zero-validation-1280x720.png` | На recovered two-line draft boxes=0; visible error отсутствует; submit disabled. | Нет inline helper/error, объясняющего, почему submit недоступен; таблица сломана по ширине. | Zero блокирует дальнейшую передачу, но validation молчалива; пользователь должен догадаться изменить число. | `FAIL_UX` | Будет нажимать disabled submit/save и не поймёт, что ошибка именно в коробах. |
| `b02-024-inbound-boxes-two-reload-readback-1280x720.png` | После установки boxes=2 и reload значение 2 сохранилось, submit enabled; строка A qty3 видна. | Durable field read-back понятен, но товарная таблица остаётся нечитаемой на 1280. | Положительный box count сохраняется. Цифровой план теперь отражает два физических короба, но экран не объясняет маркировку/идентификацию коробов. | `PASS_WITH_FRICTION` | Поймёт количество коробов, но не что распечатать/наклеить на каждый короб перед передачей ФФ. |
| `b02-025-inbound-line-delete-no-confirm-1280x720.png` | После click delete вторая строка уже исчезла; confirmation/undo/success message нет. A=3 остаётся, boxes=2. | Destructive action расположен как текстовая ссылка без защитного dialog; outcome visually immediate. | Удаление line выполняется сразу. Для draft reversible только через повторное добавление товара, но это не предложено как undo. | `FAIL_UX` | Случайный click удалит позицию; пользователь может передать неполный физический состав коробов. |
| `b02-026-inbound-line-delete-reload-readback-1280x720.png` | После reload остаётся только A=3; deleted B не восстановился. | Persistent outcome виден, но нет audit/notice, какая позиция удалена; layout всё ещё затрудняет сверку. | Delete durable. Система не предупреждает о потере позиции и не даёт одношаговый rollback. | `PASS_WITH_FRICTION` | Может не заметить отсутствие B до физической приёмки и создать расхождение на ФФ. |
| `b02-027-inbound-line-readded-before-submit-1280x720.png` | B повторно добавлен; two-line draft снова A=3/B=2, boxes=2, submit enabled. | Recovery возможен, но второй row partly below fold, names вертикальны; быстрая финальная сверка перед submit ненадёжна. | Ручной recovery после delete работает: исходный состав восстановлен. | `PASS_WITH_FRICTION` | Перед submit легко не заметить неверную/пропущенную вторую строку; нет компактного итога «2 позиции, 5 шт., 2 короба». |

## Итог микросреза

- Лично просмотрено PNG: **16 из 16** (`012–027`).
- `PASS`: **0**.
- `PASS_WITH_FRICTION`: **7** (`013`, `017`, `021`, `022`, `024`, `026`, `027`).
- `FAIL_PROCESS`: **2** (`012`, `020`).
- `FAIL_UX`: **5** (`014`, `018`, `019`, `023`, `025`).
- `NOT_RUN / INVALID_EVIDENCE`: **2** (`015`, `016`).
- `BLOCKED_FIXTURE`: **0**.

Это counts по PNG микросреза, не по 63 execution checks и не gate всего B02.
