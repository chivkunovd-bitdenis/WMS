# B02 Visual Slice C — submit, stock semantics, discrepancy, MP draft, settings и финальный список

## Граница

Лично открыты через `view_image` только `b02-028`–`b02-041`. Таблица опирается на видимое и на явно переданные runtime-факты. `031`, `034`, `038` — transitional evidence с ограниченным применением; `040` — malformed geometry и не используется как layout proof. Authoritative финальный список — `041` (`innerWidth=1280`, `innerHeight=720`, `DPR=2`, `scrollWidth=1280`). Полный B02 и gate в этом микросрезе не синтезируются.

| PNG | Что визуально доказано | Layout / usability verdict | Process verdict | Статус | Риск для пользователя с низкой цифровой грамотностью |
|---|---|---|---|---|---|
| `b02-028-inbound-submit-doubleclick-result-1280x720.png` | Documents list показывает одну строку «Поставка / Передано на склад / 2 строки» среди drafts. Runtime: double-click submit создал ровно один submitted inbound. | Capture имеет неверную oversized/тёмную геометрию, поэтому layout не оценивается; содержимое строки читаемо. | Double-click защищён: одна заявка перешла в submitted. Но строки одного дня без номера не позволяют уверенно связать результат с конкретными 3/2 и двумя коробами. | `PASS_WITH_FRICTION` | Видит успешный статус, но может открыть не тот документ среди одинаковых поставок. |
| `b02-029-inbound-submit-reload-readback-1280x720.png` | После reload Documents route сохранился, но capture cropped до header/CTA/filters; строки вне видимой области. Runtime подтверждает durable submitted state. | Непригоден для row/layout read-back из-за crop. | Durable submit подтверждён runtime, но PNG сам row не показывает; использовать вместе с `028/041`, не автономно. | `PASS_WITH_FRICTION` | Нет видимого success confirmation в этом кадре; оператор будет прокручивать/искать документ. |
| `b02-030-inbound-submitted-detail-locked-1280x720.png` | Detail показывает status «Передано на склад», date/boxes/add/qty/delete/submit disabled; `Сохранить` остаётся enabled. A=3/B=2 и boxes=2 видны, но names вертикальны. | `FAIL_UX`: таблица физически нечитаема на 1280; enabled Save среди locked controls выглядит как активное изменение, хотя бизнес-полей для save нет. | Submitted документ защищён от правки; handoff ФФ стабилен. «Сохранить» создаёт ложную affordance и не объясняет следующий физический шаг: собрать/маркировать/привезти два короба. | `FAIL_UX` | Может нажимать «Сохранить», ждать изменения или решить, что ещё обязан что-то подтвердить перед доставкой. |
| `b02-031-products-zero-stock-after-inbound-submit-1280x720.png` | Сразу после перехода Products показывает spinner, `0 из 0` и «Пока нет товаров». Runtime: через 1,1 с это был transient false-empty, не durable catalog truth. | Нельзя использовать как settled catalog; зато видно, что loading одновременно рендерит достоверно звучащий empty state. | Submit не доказал удаление товаров. Это loading-state UX defect: false-empty до завершения загрузки. | `PASS_WITH_FRICTION` / `INVALID_AS_DURABLE_TRUTH` | Решит, что товары исчезли после передачи заявки, и начнёт аварийное восстановление/повторное создание. |
| `b02-032-products-after-submit-second-readback-1280x720.png` | После settled reload видны два товара; все stock columns (`На ФФ`, sorting, cells, reserved, остаток, к отгрузке) равны 0. | На этом широком capture таблица читаема целиком, но это не валидированный 1920 viewport gate; external API/FBS controls заметнее локальной подготовки. | Submit только передал план: stock не проведён, что физически корректно до приёмки ФФ. Оба товара сохраняются. | `PASS` | Важно пояснить в UI: «Передано на склад» не означает «товар на ФФ»; иначе нули будут восприняты как ошибка. |
| `b02-033-discrepancy-cta-placeholder-1280x720.png` | После click CTA виден error alert: «Акт расхождений: будет реализован отдель…»; кнопка остаётся основной рабочей CTA. | Сообщение обрезано справа из-за malformed geometry; action визуально рекламируется как готовый. | Реального акта/документа/recovery нет — только обещание будущей реализации. Seller не может формально передать расхождение ФФ. | `FAIL_PROCESS` | Потратит время на неработающую функцию и останется без понятного канала фиксации недостачи/лишнего товара. |
| `b02-034-mp-shipment-create-open-1280x720.png` | В момент create все три CTA disabled; dialog ещё не виден. Runtime: in-flight transitional capture. | Нет spinner/текста «Создаём…»; disabled CTA выглядят как зависание, но длительность кадр не доказывает. | Не использовать как settled create outcome. Mutation подтверждается `035/041`. | `NOT_RUN` / `INVALID_EVIDENCE` | По одному кадру невозможно отличить нормальный busy от зависания; пользователь склонен повторно нажать. |
| `b02-035-mp-shipment-create-result-1280x720.png` | Settled Documents list уже содержит «Отгрузка на МП / Черновик / 0 строк» до осмысленного заполнения. Runtime: MP create сразу создал empty draft. | Геометрия malformed, но строка/статус/0 видимы; номера документа нет. | Click CTA мутирует данные до product/date/warehouse choice и создаёт abandoned-draft risk, аналогично inbound `/new`. | `FAIL_PROCESS` | Может закрыть форму и не понимать, что оставил реальный пустой MP-документ; позже не отличит его от другого. |
| `b02-036-mp-shipment-product-picker-zero-available-1280x720.png` | Из-за malformed capture видна лишь тонкая полоса, сам picker/message неразличим. Runtime: picker корректно сообщил отсутствие available in-cell stock. | PNG непригоден для visual layout/text adjudication. | По runtime бизнес-guard корректен: submitted inbound не превращён в stock, поэтому MP planning block при zero in-cell stock ожидаем. Exact wording/low-literacy recovery не доказаны визуально. | `PASS_WITH_FRICTION` | Может не понимать зависимость: сначала ФФ должен принять и разложить товар в ячейки; нужен явный next-step hint. |
| `b02-037-mp-shipment-close-leaves-draft-1280x720.png` | После Close Documents list временно показывает empty MP draft первой строкой, затем inbound rows. | Malformed geometry; row content различим, layout нет. | Close не отменяет созданный пустой draft и не предлагает delete/discard. Abandoned MP document durable (окончательно `041`). | `FAIL_PROCESS` | Ожидает, что «Закрыть» отменило создание, но мусорный документ остаётся. |
| `b02-038-mp-shipment-close-draft-reload-1280x720.png` | Early reload frame показывает только inbound rows, MP временно отсутствует. Runtime: снято через 1,1 с, позже MP появился. | Transitional false-list; не использовать как durable truth. Как loading UX показывает неполный список без loader/settling marker. | MP draft не исчез — это преждевременная выдача partial state. Authoritative persistence — `041`. | `PASS_WITH_FRICTION` / `INVALID_AS_DURABLE_TRUTH` | Решит, что Close/reload удалил MP draft, хотя тот появится позже; повторит create и создаст дубли. |
| `b02-039-seller-settings-overview-1280x720.png` | Settings overview: WB key «не добавлен», WB products `—`, Add key active, sync disabled; ЧЗ integration card описывает tokens/signing/MЧД/ЭДО. Credential dialogs не открывались. | Capture malformed/узкий, settings cards сжаты в одну колонку и нижняя часть ЧЗ ниже fold; overview wording читаемо. | Credentialless state coherent: внешняя синхронизация недоступна без ключа. Secret/non-secret save, validation, cancel/reload не проверялись по safety rule. | `PASS_WITH_FRICTION` | «Добавить ключ» — самая заметная внешняя CTA; без роли/предупреждения пользователь может пытаться добыть/ввести секрет вместо продолжения локальной работы. |
| `b02-040-documents-settled-final-readback-1280x720.png` | Текст/runtime list показывал MP draft + inbound rows, но screenshot остаётся в malformed dark geometry. | Полностью исключён как layout proof; authoritative geometry/list — `041`. | Содержательно согласуется с `041`, но не добавляет независимого процессного доказательства. | `NOT_RUN` / `INVALID_EVIDENCE` | Если принять за layout truth, можно ошибочно считать нормальную 1280 страницу узкой мобильной колонкой. |
| `b02-041-documents-normalized-viewport-final-1280x720.png` | Authoritative settled list: 1 empty MP draft + 5 inbound документов — один submitted 2-line, четыре drafts (0/0/2/0 lines). Все одной даты; только type/date/status/line count, без номера/seller-facing ID/box count. CTA/filters/table полностью помещаются, `scrollWidth=1280`. | На нормальном 1280 layout список readable и без horizontal overflow. Однако rows визуально не показывают click affordance и не различимы между собой beyond status/line count. | Auto-create/reload/close накопили 5 abandoned/working inbound drafts и 1 empty MP draft. Submitted handoff существует, но seller не может уверенно выбрать нужный двухстрочный draft или отличить три пустых. | `FAIL_PROCESS` | Высокий риск открыть/доработать/передать не тот документ; мусорные drafts скрывают единственный рабочий результат. |

## Итог микросреза

- Лично просмотрено PNG: **14 из 14** (`028–041`).
- `PASS`: **1** (`032`).
- `PASS_WITH_FRICTION`: **6** (`028`, `029`, `031`, `036`, `038`, `039`; `031/038` недопустимы как durable truth).
- `FAIL_PROCESS`: **4** (`033`, `035`, `037`, `041`).
- `FAIL_UX`: **1** (`030`).
- `NOT_RUN / INVALID_EVIDENCE`: **2** (`034`, `040`).
- `BLOCKED_FIXTURE`: **0**.

Проверка суммы: `1 + 6 + 4 + 1 + 2 = 14`.

Это counts по PNG микросреза, не по 63 execution checks и не gate всего B02.
