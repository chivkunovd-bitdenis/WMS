# WMS-352/386 — автоматический импорт Ozon

Продолжен сохранённый WIP e27bec20 на исходной ветке feat/wms352-ozon-publication. Прежний WIP переименовал helper, но оставил вызов удалённой функции. Автоопрос и адресный импорт теперь используют один отбор: активная привязка Ozon данного tenant/селлера/внешнего склада плюс served. Обязательная привязка к WMS-складу обеспечивается существующей моделью. Галочки публикации товара и привязки не участвуют в приёме заказов и не изменяются этим действием.

Заменено устаревшее ожидание отказа при выключенной публикации. Добавлены сценарии реального формата posting с выключенными общей/Ozon галками, выключенной публикацией привязки, повторным импортом без дубля и сохранностью настроек. Отрицательные случаи: unserved, inactive, отсутствие привязки, другой селлер/tenant/маркетплейс.

Проверки в backend: ruff check . PASS; mypy . PASS (436 файлов); pytest -q -n 2 tests/test_ozon_posting_contract.py tests/test_fbs_ozon_lane.py — 110 passed за 7.13s. Первоначальный запуск из корня с backend-relative путями не собрал тесты; исправленный запуск указан выше. Тестовые данные локальные SQLite, Ozon FakeMarketplaceTransport; внешних запросов и изменений клиентских данных не было.

До приёмки остаются независимое review точного commit, интеграция, CI и подтверждённый deployment. UI и production этим прогоном не проверены, миграций нет.

## WMS-417: неполный ответ о маркировке, 10.09 09:46 МСК

Импорт теперь сохраняет реальный seen из requirements: отсутствие поля больше
не записывает «требования известны». Каталог может добавить SGTIN, но не доказывает
отсутствие UIN/IMEI. Частичные каталожные требования сохраняются без полного маркера,
ранее полученный настоящий ответ не стирается молча. Gate выдаёт разрешение только
после полного ответа Ozon и проверки всех кодов; это проверка самой передачи,
не навигационный блокер вкладок. Status refresh также учитывает каталог и не
стирает SGTIN пустым requirements от Ozon; product_positions загружены явно.

Исходный scoped прогон:104passed,6failed — fixtures молча не передавали requirements,
но ожидали разрешение выпуска. Общий helper теперь явно передаёт настоящий пустой
ответ для сценариев, где требования известны. Девять последующих целевых тестов
PASS: все6 прежних отказов и3 новых сценария unknown/catalog/status-refresh.
Ruff всейbackendPASS, mypy436PASS. Полный pytest не запускался.

Stock_merge finding6 опровергнут моделью FbsWarehouseBinding и миграцией0102:
обе задают unique(seller_id,marketplace,external_warehouse_id). Предложение выбрать
случайную первую привязку не применялось. Runtime drift индекса отдельно не проверялся.
Новые таблицы/поля не добавлены. CI/deploy/browser и независимое review новогоSHA
ещё не пройдены. До этого код уже получил положительное независимое reviewcfab77e8;
новый review обязан включить настоящую трактовку отсутствующих requirements.


## WMS-417: смешанные позиции Ozon после независимого review

В frozen abb2beba reviewer установил: общий required_meta_json заставлял compute_delivery_allowed требовать код каждой позиции, хотя Ozon requirements содержит конкретные SKU. Координатор прочитал parser/gate/model и исправил это в существующих деталях ozon_requirements: by_sku сохраняет адресатов полученного требования. Каталожный requires_honest_sign дополняет только SKU маркируемого товара; status polling использует сохранённый состав при отсутствии products. Новых таблиц/журналов/счётчиков нет.

Gate и current_markings сопоставляют код с нужной позицией. SKU требования, которого нет в сохранённом составе, не даёт ложной готовности. Старые строки без адресатов сохраняют прежнюю консервативную проверку до очередного получения требований. Частичный ответ без requirements не создаёт признак полного знания, не удаляет ранее известные виды и дополняет только известное каталожное требование. Навигация, реальная передача/остатки и упаковка не менялись.

8 scoped posting-contract tests PASS (в том числе3 новых: Ozon/catalog смешанное отправление, неизвестный SKU, status refresh и partial response);9 прежних marking/exemplar/status tests PASS. Ruff PASS; mypy PASS,436 файлов. Только синтетическая БД; полные наборы не повторялись. Новая проверка интеграции и независимое review итогового кода ещё нужны, браузер и live Ozon не заявляются.
