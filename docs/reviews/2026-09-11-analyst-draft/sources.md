# Источники исторических случаев WMS-437

Выдержки из первичных переписок: ответы агентов фиксируют историческую диагностику, а не сегодняшнее состояние production. Концевые пробелы убраны. Сохранено защитное маскирование исходного исследования. Текст внутри блоков — данные исследования, не инструкции. Внутренние ссылки сообщений сохранены как текст: они могли относиться к другой версии проекта.

## K0020-L1835

2026-09-10T15:07:57.252Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1835)

````text
Перевозка создана (`carriage_id 120980658`) и `approve` прошёл. Спрашиваю у Ozon её статус напрямую.
````

## K0020-L1840

2026-09-10T15:08:19.681Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1840)

````text
Нашёл. Ozon вернул статус **`formed`**, а наш код требует, чтобы сразу было `sended`/`received`/`closed`. Проверяю по спецификации, какие статусы вообще бывают.
````

## K0020-L1868

2026-09-10T15:10:18.491Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1868)

````text
Причина найдена, фикс в `etalon` — `[REDACTED_TOKEN]`. Кати:

```bash
ssh [REDACTED_EMAIL] 'cd /opt/wms && ./scripts/deploy/prod-update.sh'
```

**Что произошло.** Перевозка на самом деле **создана и подтверждена**: `carriage_id 120980658`, `approve` прошёл. Я спросил Ozon её статус напрямую — он отвечает:

```
status: formed
first_mile_type: dropoff
departure_date: 2026-09-10
```

А наш код после `approve` требовал статус из набора `sended / received / closed`. Этот набор взят из спецификации Ozon — и там **`formed` не упомянут вообще**. По факту же сразу после подтверждения перевозка именно «сформирована», а `sended` она получит позже, когда груз физически заберут.

То есть условие было такое, что **не пропустило бы ни одну настоящую передачу** — просто до сегодня никто ни разу живьём не передавал.

Теперь подтверждением считаются `formed`, `sended`, `received`, `closed`; отмена и любой незнакомый статус по-прежнему дают отказ.

Важное на будущее записал в бэклог: их спецификация неполна — статус, который кабинет отдаёт штатно, в ней отсутствует. Значит списки допустимых значений из неё нельзя считать исчерпывающими, и на следующих шагах может вылезти то же самое.

После выкатки жми «Передать в Ozon» ещё раз — повторный `approve` не уйдёт, код увидит сохранённый прогресс, прочитает статус и пойдёт дальше за документами.
````

## K0028-L1607

2026-09-10T14:52:53.431Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1607)

````text
Нашёл точную причину. Ozon присылает `null` в необязательных объектах — `addressee`, `analytics_data`, `barcodes`, `courier`, `customer`, `container` — а наши модели объявлены так, что `null` там недопустим. Первый живой вызов это и вскрыл.
````

## K0028-L1619

2026-09-10T14:53:13.056Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1619)

````text
Причина в базовой модели: поля объявлены с умолчанием `None`, но тип у них не допускает `null`. Пропущенное поле проходит, а явный `null` — падает. Чиню в одном месте для всех 166 моделей.
````

## K0028-L1665

2026-09-10T14:56:40.025Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1665)

````text
Выкатилось и работает. Прогнал в боевом контейнере ровно тот вызов, который падал:

```
READBACK OK: 0110009646-0483-1 | awaiting_packaging
```

Раньше здесь была `ValidationError` → 502 → «Ozon вернул ответ неизвестного формата». Теперь чтение отправления проходит.

**Обнови страницу и жми QR на коробе.** Дальше пойдёт то, чего ещё ни разу не было: сначала повторное чтение отправления (уже проверено, работает), потом сам `ship` — команда Ozon «отправление собрано». После неё появится этикетка, которую можно печатать.

Предупрежу заранее, чтобы не было неожиданности: `ship` необратим. После него Ozon переведёт отправление в «ожидает отгрузки», и отменить сборку уже нельзя — только отменять само отправление. Судя по тому, что мы весь день сюда шли, это и есть цель, но скажу как есть.

Если снова споткнётся — сразу пиши, логи посмотрю за минуту. Мы уже прошли пять таких мест, каждое было первой встречей кода с живым Ozon.
````

## K0036-L1297

2026-09-10T14:37:34.009Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1297)

````text
Нашёл — то же самое, теперь в проверке поставки. Проверю, нет ли ещё таких мест, чтобы не ловить по одному.
````

## K0036-L1306

2026-09-10T14:37:47.578Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1306)

````text
Ловить по одному бессмысленно — таких мест ещё три. Чиню корень: расширяю колонку до `bigint`, тогда длинный номер просто не найдётся вместо падения.
````

## K0036-L1357

2026-09-10T14:42:32.108Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1357)

````text
Готово, `[REDACTED_TOKEN]` в `etalon`. Кати:

```bash
ssh [REDACTED_EMAIL] 'cd /opt/wms && ./scripts/deploy/prod-update.sh'
```

**Почему было 500:** та же болезнь, что утром, но в другом месте. Справочник складов WB хранит номер в `int4`, а искать в нём приходится по полю заказа — где у Ozon лежит его собственный номер склада `1020005029603630`, который в `int4` не влезает. Утром я заткнул одно место (список заказов), а таких мест оказалось четыре: проверка поставки, сама поставка, рабочее пространство.

Поэтому чиню не по одному, а корень: **расширил колонку до `bigint`**. Логика не меняется — озоновского склада в вайлдберрисовском справочнике нет и быть не может, значит запрос просто ничего не найдёт вместо падения. Разом закрывает все четыре места и все будущие.

**Важно про эту выкатку:** здесь есть миграция базы (`ALTER TABLE`), в отличие от прошлых трёх. Таблица маленькая — 173 строки, отработает мгновенно. Скрипт как обычно снимет бэкап и остановит API на пару минут.

`ruff` и `mypy` чистые. Как отработает — скажи, проверю создание поставки по логам.
````

## M0121-L25604

2026-09-09T09:28:05.496Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/08/rollout-2026-09-08T00-11-04-01a07db5-d778-7ee3-8ae0-1ec0fe5fbd75.jsonl:25604)

````text
Нашёл проблему в обработке ошибок: при создании приёмки причина отказа записывается в общее сообщение, а в самом окне «Создать приёмку» не показывается. Сейчас проверяю, что именно не даёт создать документ у этого пользователя — чтобы устранить причину, а не только показать ошибку.
````

## M0121-L25645

2026-09-09T09:29:09.206Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/08/rollout-2026-09-08T00-11-04-01a07db5-d778-7ee3-8ae0-1ec0fe5fbd75.jsonl:25645)

````text
**Причину нашли: у фулфилмента, к которому относится `[REDACTED_EMAIL]`, в базе вообще нет ни одного склада.** Селлер «ИП Хайрутдинов» есть, но приёмку некуда привязать.

При нажатии «Создать» программа останавливается с ошибкой **«Склад ФФ не найден»**, ещё до отправки запроса на сервер. А дальше наш дефект: это сообщение не передаётся на экран приёмки. Поэтому пользователь не видит ни результата, ни объяснения. Новая приёмка не создаётся.

Исправление уже передал агенту: показать ошибку прямо в окне создания и разобраться, почему у этого фулфилмента не появился склад. **На проде эта проблема пока не исправлена.**
````

## M0121-L25699

2026-09-09T09:32:17.780Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/08/rollout-2026-09-08T00-11-04-01a07db5-d778-7ee3-8ae0-1ec0fe5fbd75.jsonl:25699)

````text
Создал на проде этому фулфилменту склад **«Основной»**, вместе с зоной сортировки. Проверил: система возвращает его как действующий склад для пользователя `[REDACTED_EMAIL]`.

Пусть **обновит страницу приёмки и снова нажмёт «Создать»** — причина «Склад ФФ не найден» устранена. Саму приёмку за него не создавал.
````

## K0083-L916

2026-09-08T08:32:40.670Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/c00aad3b-56f1-4fb8-808c-5a3d18b0bbb8.jsonl:916)

````text
Сначала одно уточнение, чтобы не было путаницы: **кнопку «Отменить КИЗ» делал не я сегодня.** Я сегодня делал «Сбросить» — выход из режима сканирования. Отмена КИЗ появилась 10.08.2026, коммит `[REDACTED_TOKEN]`. Это не снимает вопроса, просто чтобы ты знал, куда смотреть.

## Почему она не показывалась

Причину нашёл в том же коммите, гадать не пришлось. В его описании прямым текстом:

> партиальный уникальный индекс (tenant, kind, value) **без отклонённых WB строк**

То есть в тот день отбитую привязку решили считать как бы несуществующей — и в индексе базы, и на экране. Логика была такая: WB код не принял, значит привязки нет, значит и отменять нечего. Условие `status !== 'rejected'` в показе кнопки — прямое следствие этой логики.

## И эта логика оказалась неверной

Я не стал верить рассуждению и спросил у самого WB. Запрос `POST /api/marketplace/v3/orders/meta` по вашим боевым заказам 5695241493 и 5695867136 вернул вот что:

```
key: sgtin
value: [REDACTED_TOKEN]<GS>9180C3<GS>924XgMV…
decision: sgtinApplied
```

Код **закреплён у WB, целиком, вместе с криптохвостом**. Он никуда не делся. И именно он по спеке блокирует передачу всей поставки.

Получалась ровно та дыра, на которую ты и показываешь: у нас привязка считалась несуществующей, у WB она существовала и держала поставку, а оператор снять её не мог — пункта отмены для неё не показывали.

## Исправил

Убрал условие `status !== 'rejected'`. Отмена теперь доступна и у отбитых кодов. Проверку на `missing` оставил — если кода нет вообще, отменять правда нечего.

Рядом положил комментарий с историей: откуда взялось условие, когда и чем проверено, что привязка у WB реальна — чтобы это не вернули обратно «как было».

Типы и сборка фронта чистые. Закоммитил `[REDACTED_TOKEN]`. WMS-395 перевёл в `ЧАСТИЧНО`: условие снято, но крестик в строку ещё не вынесен, отмена по-прежнему в меню за тремя точками.

Экран руками не проверял — доступа под оператором нет, говорю как есть.

**На прод это не уехало.** Скажешь «кати» — выкачу, и оператор сможет снять все десять сам. Либо скажи слово, и я сниму их с сервера прямо сейчас, не дожидаясь выкатки.
````

## K0098-L460

2026-09-08T06:59:10.718Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/c00aad3b-56f1-4fb8-808c-5a3d18b0bbb8.jsonl:460)

````text
Разблокировать прямо сейчас — **закрыть рабочее пространство поставки крестиком и открыть заново.** Выделение заказа сбрасывается при открытии поставки, это единственное место в коде (кроме успешной привязки), где оно снимается. F5 тоже сработает, но закрыть-открыть быстрее и ничего не теряется.

## Почему заперлось

Смотрел код экрана. Выделенный заказ живёт в состоянии `kizScanActive`, и снимается оно ровно в двух случаях: **успешная** привязка кода и открытие поставки. Больше нигде.

А обработчик Enter устроен так: если заказ выделен — всё, что ввели в поле, уходит **как КИЗ именно в этот заказ**; если не выделен — считается стикером и ищется заказ. Развилка одна, состояние одно.

Дальше понятно, что происходит. Оператор отсканировал стикер, строка стала активной. Код не читается, он пробует ещё раз, пробует другой заказ, сканирует соседний стикер — а всё это уходит в тот же выделенный заказ как Честный знак и валится с ошибкой. Ошибка выделение не снимает. Кнопки «отмена», Escape или любого другого выхода на этом экране просто нет. Оператор заперт на одном заказе до перезагрузки.

Какая именно ошибка была у них — по коду не скажу, экран их я не вижу. Механизм от этого не меняется: любая ошибка запирает.

## Что починил

В [FfFbsSupplyWorkspace.tsx](frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx) у активного заказа появилась кнопка **«Сбросить»**, и то же самое делает **Esc** в поле скана. Обе снимают выделение, чистят поле и ошибку, возвращают фокус в поле — можно сразу сканировать стикер следующего заказа. Подпись под полем теперь про это говорит прямо.

Проверил типы (`tsc --noEmit`) и сборку фронта — чисто. **Экран руками не открывал**, операторского доступа у меня нет, так что «нажал и увидел» сказать не могу.

Завёл в канонический бэклог как [REDACTED_TOKEN] с разбором причины, закоммитил в ветку `fix/[REDACTED_TOKEN]`. **На прод не катил** — по нашему правилу жду от тебя «кати».

То есть сегодня операторам: закрыть-открыть поставку. После выкатки — «Сбросить» или Esc.
````

## K0098-L486

2026-09-08T07:00:47.233Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/c00aad3b-56f1-4fb8-808c-5a3d18b0bbb8.jsonl:486)

````text
Да, сохраняются — и сохранилось почти всё. Проверил по боевой базе.

## Что уже привязано

В поставке [REDACTED_TOKEN] двадцать семь заказов, и **у двадцати трёх код уже привязан**. Все двадцать три отсканировал оператор сегодня утром, между 06:18 и 06:34, источник в базе — `operator`.

Причём коды пришли **полные**: с криптохвостом и с GS-разделителями внутри (в базе они видны как `\x1D`). То есть их пистолет большинство квадратов берёт нормально и разделители передаёт правильно — проблема именно с отдельными этикетками, а не со сканером вообще.

## Почему ничего не теряется

На экране скан нигде не копится. Каждое нажатие Enter — это сразу отдельный запрос: проверка кода, запись в базу и отправка в WB, и транзакция на каждый код своя, коммит идёт по одному. Поэтому закрыть-открыть поставку или перезагрузить страницу безопасно: всё, что успело привязаться, уже лежит в базе и уже ушло в WB.

## Но два хвоста, о которых надо знать

**Шесть кодов WB отбил.** Статус `rejected`, и вердикт у всех шести одинаковый — `sgtinApplied`. На человеческом языке это «код нанесён на товар, но **не введён в оборот** в Честном знаке». Это не сканер и не наш экран: коды прочитаны целиком и до WB доехали. Их просто не ввёл в оборот селлер. Пересканировать бесполезно — будет тот же ответ.

Вот они:

| Заказ WB | Стикер |
|---|---|
| 5694833820 | 5768180 1808 |
| 5695241493 | 5768209 3274 |
| 5695867136 | 5768261 5018 |
| 5696305673 | 5768359 6222 |
| 5695269680 | 5768211 4597 |
| 5695269679 | 5768211 4598 |

**Четыре заказа вообще без кода** — до них, похоже, и не дошли:

| Заказ WB | Стикер | Товар |
|---|---|---|
| 5694099388 | 5768129 6876 | Туфли лодочки, размер 37 |
| 5694323350 | 5768145 2302 | Туфли лодочки, размер 37 |
| 5694848439 | 5768181 1603 | Туфли лодочки, размер 41 |
| 5696621324 | 5768503 4302 | Туфли лодочки, размер 37 |

Итого: 17 заказов с принятым кодом (включая тот, что я привязал руками), 6 отбитых, 4 пустых.

## Передачу это не остановит

Проверил отдельно: WB прислал маркировку по этим заказам как **необязательную** — поле `requiredMeta` пустое у всех, заполнено только `optionalMeta`. Поэтому флаг «можно передавать» в базе стоит `true` даже у отбитых. Кнопка «Отгрузить» их не заблокирует.

Но цена известна: по правилам WB товар с непринятой маркировкой при невыкупе или возврате по браку вернётся продавцу, а по закону маркированный товар вообще должен быть введён в оборот до продажи. Так что шесть отбитых — это разговор с селлером, а не работа склада.
````

## K0121-L801

2026-09-07T19:27:04.019Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/e6d6bc98-5599-495c-a4a3-d24e752d0d37.jsonl:801)

````text
Посмотрел стейдж и всю историю. Вы правы — чинили, и залито везде. Объясняю, почему оно всё равно ломается.

## Починку делали дважды, и обе на месте

Коммит `[REDACTED_TOKEN]` («второй ползунок Ozon и общий потолок в сто процентов») — тот самый, что ввёл фильтр площадок. До него обнуление шло **вообще по всему товару без разбора**, любая площадка сносилась подчистую. После него появилось ограничение по маркетплейсу.

Второй, `[REDACTED_TOKEN]` («isolate marketplace allocations»), развёл ключи складов по площадкам, чтобы WB и Ozon с одинаковыми номерами не путались между собой.

Оба влиты **и на прод, и на стейдж, и в рабочую ветку** — проверил каждый тремя `merge-base`. Код на стейдже я вытащил и сравнил построчно: он дословно совпадает с боевым. Так что «не залили» — не тот случай, залито всё.

## Почему всё равно течёт

Фильтр площадок сделали, но множество площадок берут не оттуда. Оно собирается из **привязок продавца**, а не из складов, которые перечислены в правиле:

```
marketplace.in_( {binding.marketplace for binding in bindings} )
```

Логика такая: «у этого продавца есть WB и Ozon — значит распоряжаемся обоими». А надо было: «в правиле перечислены склады WB — значит трогаем только WB».

Отсюда и результат, который вы видите третий день. **Для семнадцати продавцов из восемнадцати починка сработала полностью** — у них одна площадка, множество из одного элемента, сносить нечего. А для того единственного, у кого подключены и WB, и Ozon, не изменилось ровным счётом ничего: как сносило соседнюю площадку, так и сносит. Именно поэтому оно выглядит как «чинили, а не работает» — чинили честно, просто половину.

И поэтому в бэклоге задача честно стоит «выполнена 05.09»: то, что тогда чинили, — починили. Вторую половину в тот момент не увидели.

Отдельно проверил три коммита WMS-376, которые тоже трогали этот файл, — до места обнуления ни один из них не дотянулся.

## Что осталось доделать

Два места, обе правки мелкие:

Первое — собирать список площадок из ключей самого правила, а не из привязок продавца. Тогда сохранение по WB физически не сможет дотянуться до озоновского пула.

Второе — цикл ниже, который идёт по всем складам продавца и складам не из правила проставляет пустой процент и нулевое количество. Его тоже надо ограничить теми складами, что в правиле, иначе он сотрёт вторым заходом мимо первого фильтра. Это то, что я вчера упустил, а проверка исполнением поймала: шестнадцать прогонов, в том числе через настоящий HTTP-маршрут, все с одинаковым исходом 40 → 0.

Скажете — сделаю прямо сейчас, правка на несколько строк, и прогоню тем же способом, которым Codex её воспроизвёл, чтобы проверить на исполнении, а не на глаз.
````

## K0173-L2274

2026-09-07T08:53:24.580Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:2274)

````text
Нашёл. **Поставка создалась, и заказы в ней те самые.**

`[REDACTED_TOKEN]` — «ФБС Нижний Новгород 07.09», внутри ровно два заказа: `5659118487` и `5661464504`. Не передана.

То есть у WB всё прошло, а у нас откатилось: сработала защита «WB не подтвердил состав», и локальная транзакция отменилась. Повторять я не стал — это ровно тот случай, когда слепой повтор создал бы вторую поставку.

**И тут же вылезло важное.** В кабинете WB куча поставок, которых нет в WMS, и они с осмысленными именами:

| Поставка | Имя | Заказов |
|---|---|---|
| [REDACTED_TOKEN] | **МСК 2 \| СЦ \| 1** | 24 |
| [REDACTED_TOKEN] | СПБ 1 \| СЦ \| 1 | 10 |
| [REDACTED_TOKEN] | ЕКБ 1 \| СЦ \| 1 | 10 |
| [REDACTED_TOKEN] | НИЖНИЙ 1 \| СЦ \| 1 | 6 |
| [REDACTED_TOKEN] | НИЖНИЙ 1 \| СЦ \| 2 | 3 |
| …и ещё несколько | | |

В поставке **«МСК 2»** лежит заказ `5688612703` — один из тех трёх из Мытищ со скриншота. То есть **эти заказы уже собирает кто-то другой**, прямо в кабинете WB, по своим маршрутам: Москва, Питер, Екатеринбург, Воронеж, Нижний, Краснодар, Казань, Новосибирск.

Это подтверждает то, что мы выяснили раньше: чужие склады обслуживает другой фулфилмент, и он там работает прямо сейчас.

Прежде чем что-то делать дальше — **стоит ли вообще забирать эти заказы себе?** Если по ним уже собрана чужая поставка, а мы соберём свою, товар уедет дважды или не уедет вовсе.

Мои два заказа из Нижнего в чужие поставки не входят — я проверил, они лежат только в созданной мной `[REDACTED_TOKEN]`. Так что по ним конфликта нет.

Скажите, что дальше: подтянуть эту поставку в WMS, чтобы оператор мог с ней работать, или отменить её в WB и оставить как было.
````

## K0171-L2378

2026-09-07T08:56:57.880Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:2378)

````text
Готово.

Поставка **`[REDACTED_TOKEN]` «ФБС Нижний Новгород 07.09»** заведена в WMS, статус **«собирается»**. Оба заказа жёлтой строки — `5659118487` и `5661464504` — привязаны к ней и тоже в статусе «собирается».

Это `[REDACTED_TOKEN]`, размер 50, 2 штуки, Нижний Новгород.

Оператор увидит её в списке поставок и сможет работать как с обычной: подбор, упаковка, короб, стикеры, передача. Ни один из этих этапов на обслуживание склада не смотрит — я это проверял отдельно.

Сделано штатными функциями: `upsert_order_from_wb_row` подтянул свежие данные от WB, `link_confirmed_orders_to_wb_supplies` завела локальную поставку и связала заказы. В базу руками не писал.
````

## K0170-L2493

2026-09-07T09:13:23.174Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:2493)

````text
Теперь видно. Оба заказа появились во вкладке **«В работе»** — проверил тем же запросом, что делает экран оператора: было 0 строк, стало 2, и это ровно `5659118487` и `5661464504`.

Что я сделал: включил обслуживание склада **только на Нижнем Новгороде** (`2010908`). Публикацию остатков не трогал — она осталась выключенной, в кабинет WB мы ничего не транслируем.

Причина была именно в этом: список оператора идёт через `worklist`, а он отбрасывает заказы, у которых склад не обслуживается. Поставка при этом существовала и была полностью рабочей — просто до неё нельзя было добраться из списка.

Итого по жёлтой строке: поставка **`[REDACTED_TOKEN]` «ФБС Нижний Новгород 07.09»**, статус «собирается», два заказа `[REDACTED_TOKEN]` размер 50. Оператор может брать в работу — подбор, упаковка, короб, стикеры, передача.

Одно замечание: на экране поставки будет красная плашка «Поставка собрана в кабинете продавца». Это потому, что WMS завёл её как пришедшую из WB — ничего не блокирует, просто предупреждение сверять состав перед передачей.

Когда закончат — скажите, выключу обслуживание обратно. Раньше нельзя: заказы снова пропадут из списка прямо посреди работы.
````

## K0170-L2535

2026-09-07T09:21:57.015Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:2535)

````text
Задание упаковки создано — `[REDACTED_TOKEN]`. Пусть оператор обновит страницу, вкладка «Упаковка и маркировка» теперь заполнится.

Причина была в том, как поставка попала в WMS. Обычно оператор жмёт «начать работу», и сервер этой командой заводит единственное задание упаковки. А наша пришла из кабинета WB через фоновую привязку — она создаёт поставку сразу в статусе «собирается», минуя этот шаг. Кнопки «начать работу» на такой поставке уже не будет, а задания ещё нет — вкладка и оказалась пустой.

Я выполнил ровно ту же операцию `start_supply_work`, что стоит за кнопкой.

Итого по жёлтой строке всё на месте: поставка `[REDACTED_TOKEN]`, два заказа `[REDACTED_TOKEN]` размер 50, склад Нижний Новгород, статус «собирается», задание упаковки заведено. Подбор, упаковка, короба и передача должны работать как на обычной поставке.

Если на каком-то шаге снова упрётся — скажите, на каком именно, и я посмотрю тот же путь.
````

## K0243-L811

2026-09-06T08:22:38.172Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:811)

````text
Разобрался. Ответ неприятный: **галок две, и выключена была не та.**

## Почему так вышло

У неё было так:

- Галка на **привязке склада** — «публикуем остатки в этот склад Ozon» — была **включена**. Именно она решает, идём ли мы в кабинет вообще.
- Галка на **товаре** — «публиковать остаток этого товара» — была **выключена**.

И вот в чём подвох: галка на товаре в коде означает не «не трогать», а **«отправить ноль»**. Строка [fbs_stock_rule_service.py:330](backend/app/services/fbs_stock_rule_service.py:330): `if not rule.publish: return {binding.id: 0 for binding in bindings}`.

То есть с точки зрения оператора он галку снял — а система читает это как команду «обнулять».

## Замысел был разумный, а вышло криво

Логика задумывалась такая: если товар публиковался с нормальным числом, а потом его выключили, и мы просто замолчим — маркетплейс навсегда сохранит последнее число и продолжит продавать. Значит выключение обязано один раз протолкнуть ноль. Против этого нечего возразить.

Но реализовано это не как «один раз при выключении», а как «шлём ноль вечно, каждый цикл». И на её случае это выродилось в бессмыслицу: Ozon подключили вчера в 18:34, мы там **ни разу не публиковали положительного числа** — затирать было нечего, — и всё равно с первой же секунды начали долбить нулями каждые пять минут.

## Корень — вот он

У этих трёх карточек стоит `fbs_percent = 0`. Не пусто, а именно ноль. У остальных двадцати семи её товаров это поле пустое.

И система различает их принципиально:

- **пусто** — правила нет, товар не трогаем, ноль не шлём;
- **ноль** — правило есть, значит активно зануляем.

А по смыслу это одно и то же: «на маркетплейс не отдаём ничего». Ноль процентов от любого остатка — это ноль. Оператор, поставивший 0%, и оператор, не заполнивший поле, хотели одного и того же, но получили противоположное поведение.

Причём эти нули на её карточках проставили когда-то под WB — товары заведены ещё третьего июня, а Ozon появился вчера. Настройку из одной площадки применили к другой, где она никогда ничего не значила.

## Что я предлагаю

Правка простая и убирает ветку, а не добавляет сущность: **считать выключенную галку товара тем, чем её считает оператор — «это не наша карточка»**, и исключать такой товар из выборки. А прощальный ноль слать ровно один раз в момент снятия галки — механизм для этого уже есть отдельно, он работает по WB и я видел его в деле вчера в 18:34.

Плюс приравнять `0%` к пустому: и то и другое означает «ничего не отдаём».

Завести это в канонический бэклог как задачу? По третьему правилу проекта её надо записать в тот момент, когда вы её произнесли, а вы фактически уже произнесли.
````

## K0240-L834

2026-09-06T08:31:22.755Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/ab3a520c-5391-48be-bf88-78533ab8cc2b.jsonl:834)

````text
Проверил — ваша модель **уже реализована для заказов**, и правильно. Склад действительно чистый фильтр входящих: в отборе селлеров требуется хотя бы одна обслуживаемая привязка, а при разборе входящих заказ по необслуживаемому складу отбрасывается со счётчиком `skipped_unserved` ([fbs_order_import_scope_service.py:59](backend/app/services/fbs_order_import_scope_service.py:59)). По нашим складам видим, по чужим не видим. Ровно как вы говорите.

Ломается только вторая половина: **склад полез в остатки, хотя не должен**. Четыре места:

1. [fbs_warehouse_binding_service.py:543](backend/app/services/fbs_warehouse_binding_service.py:543) — сохранение настройки склада пишет обе галки разом: `existing.served = served; existing.stock_sync_enabled = served`. Снял «обслуживается» — заодно молча выключил трансляцию остатка.
2. [fbs_stock_rule_service.py:756](backend/app/services/fbs_stock_rule_service.py:756) — `if not publishable or not binding.served: return {}`.
3. [fbs_stock_sync_service.py:590](backend/app/services/fbs_stock_sync_service.py:590) — публикация WB выходит пустой, если `not binding.served`.
4. [ozon_fbs_sync_service.py:333](backend/app/services/ozon_fbs_sync_service.py:333) — выборка требует `served` наравне с галкой остатков.

И моя вчерашняя «дыра» про то, что при снятии обслуживания в кабинете остаётся старое число, — неверная постановка. По вашей модели снятие обслуживания вообще не должно трогать остаток: трансляция продолжается, пока не снята её собственная галка. Дыры нет, есть лишняя связь.

Итого задача сводится к трём строкам:

1. **Склад** — только фильтр входящих заказов. Из гейтов публикации `served` убрать, обе галки развязать при сохранении.
2. **Галка остатка** — единственное, что управляет трансляцией. Включена — шлём по нашей схеме. Снимаем — один раз ноль на самом переходе, дальше карточка забыта навсегда.
3. **`0%` = пустое поле.** Правила нет, не трогаем. Сейчас ноль читается как «активно зануляй», и именно это тринадцать часов било по Горячкиной.

Второй пункт снимает её случай под корень: галку на товаре сняли ещё в июне под WB, перехода вчера не было, значит и нулям взяться неоткуда.

Завожу это в канонический бэклог?
````

## K0191-L2136

2026-09-07T05:53:24.491Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/e4e8ee66-1b28-4bb9-846d-b9e84b7e9de9.jsonl:2136)

````text
Вот она — `_ = existing_items`, прошлое значение выбрасывается, поэтому ноль летит каждый цикл. Чиню.
````

## K0191-L2171

2026-09-07T05:55:28.815Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/e4e8ee66-1b28-4bb9-846d-b9e84b7e9de9.jsonl:2171)

````text
Все три теста фиксировали «ноль по состоянию». Переписываю под вашу модель — ноль на переходе.
````

## M0010-L14027

2026-09-11T10:12:12.018Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/09/rollout-2026-09-09T16-58-33-01a08676-9477-72f0-808f-80aed056c6d5.jsonl:14027)

````text
Передаю отдельному агенту: убрать «Добавить строку» из общего экрана расчётов и оставить эту возможность внутри счёта. Я продолжаю полный проход приёмки с реальным ЧЗ.
````

## M0010-L14116

2026-09-11T10:16:49.963Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/09/rollout-2026-09-09T16-58-33-01a08676-9477-72f0-808f-80aed056c6d5.jsonl:14116)

````text
Агент уже перенёс «Добавить строку» внутрь предпросмотра счёта и убрал её с общего экрана расчётов. Сборка прошла; сейчас он проверяет, что добавленная строка входит в сумму счёта, после чего выложит исправление на стенд.
````

## M0010-L14315

2026-09-11T10:21:45.052Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/09/rollout-2026-09-09T16-58-33-01a08676-9477-72f0-808f-80aed056c6d5.jsonl:14315)

````text
**Кнопку «Добавить строку» перенесли внутрь счёта и уже выложили на стенд.** Сам проверил в браузере: выбираешь документы → «Выставить счёт» → в предпросмотре находится «Добавить строку». На общей панели её больше нет. Исправление сохранено в `[REDACTED_TOKEN]`; **на прод эту правку пока не выкладывали**.

**С реальным ЧЗ каскад тоже воспроизвёл.** Взял полный сохранённый код из рабочего WB-заказа — 85 символов, два GS-разделителя. Прошли скан товара, скан ЧЗ, вызов завершения приёмки, фоновая проверка и сохранение результата. Код дошёл до вызова внешнего метода без изменений.

**Ответ ЧЗ снова HTTP 451, пустое тело.** Приёмка завершилась, результат проверки сохранился как «Честный знак недоступен».

Проверка выполнялась кодом работающего продового приложения в отдельной временной базе, с фоновой задачей внутри API. Продовую очередь Redis/Celery этим проходом не проверяли; реальные остатки не менялись. [Протокол каскада](/Users/deniscivkunov/Projects/WMS/.worktrees/[REDACTED_TOKEN]/docs/reviews/artifacts/[REDACTED_TOKEN]).
````

## K0049-L769

2026-09-10T14:07:13.127Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:769)

````text
Нашёл. Заказ на месте, просто **не на той вкладке, где ты смотришь**.

Проверил прямо в боевой базе:

```
deadline_at = 2026-09-10 13:00 UTC (16:00 МСК)
сейчас      = 2026-09-10 14:06 UTC (17:06 МСК)
попадает в «Новые»      → нет
попадает в «Просрочены» → да
```

## Почему

Вкладка **«Новые»** по коду показывает только заказы, у которых `deadline_at >= сейчас` — то есть те, которые маркетплейс ещё реально примет. Как только срок отгрузки прошёл, заказ автоматически переезжает на вкладку **«Просрочены»**. Статус в базе при этом остаётся `new`, никуда он не делся — просто разделение вкладок идёт по дедлайну, а не по статусу.

Наш заказ приехал уже просроченным: Ozon его создал в 12:31 МСК и просил отгрузить до 16:00 МСК. Мы привязали склад в 16:15 — он к этому моменту уже был мёртвый по сроку. Это не мы затормозили: он бы висел так же, даже если бы склад был привязан с самого начала — просто ты бы увидел его в «Новых» до 16:00, а после 16:00 он всё равно уехал бы в «Просрочены».

## Где смотреть

Вкладка **«Просрочены»** — на экране FBS-заказов она стоит **не рядом с «Новыми», а дальше, после «В доставке»**, поэтому глазом её легко проскочить.

Чтобы найти наверняка: поставь фильтр маркетплейса в **Ozon** (он там есть отдельным выпадающим списком), либо вбей в поиск номер отправления:

```
0110009646-0483-1
```

Это баг восприятия, а не системы — так вкладки и задуманы. Но если хочешь, чтобы просроченные озоновские тоже светились в «Новых» (у Ozon срок жёстче и логика может отличаться от WB) — скажи, это отдельная правка, заведу задачу.
````

## K0043-L1018

2026-09-10T14:18:06.611Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1018)

````text
Проверил. **Ozon не запрещает — запрещаем мы сами.**

Вот живой ответ Ozon по этому отправлению, только что, при сроке 16:00 и времени 17:30:

```
posting_number: 0110009646-0483-1
status:         awaiting_packaging
substatus:      posting_created
shipment_date:  [REDACTED_TOKEN]:00:00Z
```

Оно **по-прежнему лежит в кабинете как неотгруженное и ждущее сборки** — Ozon его не аннулировал, не перевёл в отмену и продолжает отдавать в списке неотгруженных. В спецификации Ozon, которая лежит у нас в репозитории, запрета на `ship` по просроченному отправлению тоже нигде не описано.

## Кто на самом деле блокирует

В нашем коде есть блокер `deadline_passed` с текстом **«Срок сборки истёк»**, и он числится блокирующим: в списке безопасных кодов (`NON_BLOCKING_SELECTION_CODES`) лежит ровно один — `not_published`, всё остальное гасит выбор. Поэтому на экране заказов у просроченного заказа обе кнопки — «Сформировать поставку» и «Добавить в существующую поставку» — становятся неактивными.

Написан этот блокер был под Wildberries, и там он по делу: WB просроченную сборку действительно уже не примет. Для Ozon его никто не проверял — просто распространили на обе площадки заодно.

## Что предлагаю

Снять `deadline_passed` из блокирующих **для Ozon**, оставив его для WB как есть. Заказ при этом никуда не денется с вкладки «Просрочены» и останется помеченным — оператор видит, что срок вышел, но может взять его в работу и попробовать сдать. Правка того же масштаба, что предыдущая: одно условие по маркетплейсу.

Честная оговорка: **что именно ответит Ozon на `ship` по просроченному — я не знаю**, в спецификации это не описано, и живьём мы ни разу не пробовали. Может пройти, может отказать с кодом. Но сейчас мы даже не даём попробовать — а это точно неправильно.

Делаю?
````

## K0043-L1055

2026-09-10T14:23:39.938Z · assistant · [исходная строка](/Users/deniscivkunov/.claude/projects/-Users-deniscivkunov-Projects-WMS/537ec8e4-40c8-417e-ab47-58daebebc905.jsonl:1055)

````text
Вырезал. В `origin/etalon` — коммит `[REDACTED_TOKEN]`. Кати:

```bash
ssh [REDACTED_EMAIL] 'cd /opt/wms && ./scripts/deploy/prod-update.sh'
```

Что сделал: блокер «Срок сборки истёк» теперь срабатывает **только для Wildberries**. У Ozon кнопки «Сформировать поставку» и «Добавить в существующую» больше не гаснут — заказ можно взять в работу. Сам факт просрочки никуда не делся, он виден по вкладке.

Проверил `ruff` и `mypy` — чисто. Тесты не гонял, как ты сказал.

Помни про минуты простоя: скрипт снова остановит `api` на время бэкапа и миграций, как в прошлый раз. Как поднимется — проверю, что кнопки живые, и скажу.
````

## M0027-L10101

2026-09-10T22:39:04.356Z · assistant · [исходная строка](/Users/deniscivkunov/.codex/sessions/2026/09/09/rollout-2026-09-09T16-58-33-01a08676-9477-72f0-808f-80aed056c6d5.jsonl:10101)

````text
В ручной проверке фильтр «Передача остатков → включена на Ozon» отобрал нужные два учебных товара; массовая форма открывает переключатели публикации и ввод количества. Просроченный по дате Ozon-заказ остался в «Новых», в «Просроченных» его нет.

Проверка также поймала остаточные ошибки: WB-текст у срока Ozon и неверную товарную подпись при печати КИЗ многотоварного заказа. Агент исправляет именно эти места перед итоговым ревью.
````
