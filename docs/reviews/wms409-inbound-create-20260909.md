# WMS-409 — visible inbound creation errors

The reported production user belonged to tenant `5baaf211-7cb7-495d-9c2b-b42121606bf9`, with seller «ИП Хайрутдинов» `a403d86b-86f0-4fc4-b61f-6aedcca56498`. A read-only query initially returned zero warehouses for this tenant and no inbound documents in the preceding two hours. Port 8088 belongs to wms_prod-web-1. Available current API logs had no matching create POST or traceback; history before the latest container restart was not established.

The frontend warehouse loader resets selectedWarehouseId on an empty list. The creation handler then returned before POST, storing «Склад ФФ не найден.» in opsError, while the queue received inboundListError. Thus the failure was hidden. The handler now rejects with the precise error; the real queue dialog catches it and remains open. A null result also leaves the dialog open. Success returns the created ID and keeps the existing document-opening path. A failure of the subsequent list GET cannot turn an already successful POST into a failed creation; the list loader retains its separate error.

Only App.tsx and FfInboundQueuePage.tsx changed. No backend, schema, warehouse, FBS or design changes. Existing MUI Alert is used inside the existing dialog.

The canonical backlog already has WMS-062, «Один склад по умолчанию при регистрации», marked not implemented. The current register_fulfillment creates tenant, user and disabled tariff matrix, but no warehouse. This confirms that registration does not enforce the existing default-warehouse requirement; it does not prove the historical creation/deletion history of this specific tenant. That broader task is not silently implemented in WMS-409.

The owner explicitly requested a production warehouse «Основной». The root agent alone performed catalog_service.create_warehouse(session, tenant_id, name='Основной', code='main'), whose existing behavior also creates the sorting location. This agent independently reread production with SET TRANSACTION READ ONLY: warehouse `050fb5f6-dacf-4742-96ec-e9128a73cc81`, code main, operational true; location `5ab8f9de-db22-46b4-950f-71b647bdcbd4`, code __SORTING__. No real inbound document was created. After this recovery the existing warehouse GET supplies a valid warehouse on browser reload; creation by the real user remains unverified.

Validation: TypeScript noEmit and production build passed. A one-off local actual Chrome run exercised the real queue component with explicit synthetic callback outcomes: missing warehouse, API error, null result all retain the dialog and seller; success closes it; reopening clears the prior error. No unhandled page errors and no external/API writes. Chrome and its own Vite process closed, temporary harness removed. The screenshot was visually inspected. This is component browser evidence, not a claim of production user acceptance.

Evidence: [browser results](artifacts/wms409-inbound-create-20260909/result.json), [screenshot](artifacts/wms409-inbound-create-20260909/missing-warehouse.png), [production read-only result](artifacts/wms409-inbound-create-20260909/production-readonly.json), [one-off script](scripts/wms409-inbound-create-browser.cjs). No session values or authorization headers are included.
