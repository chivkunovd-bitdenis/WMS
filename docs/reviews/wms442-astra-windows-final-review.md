# WMS-442 — независимое Astra review Windows и постоянной работы

Дата: 13.09.2026. **Актуальный вердикт: CHANGES REQUIRED на `35fe76fb5b7345f7221d51416672b7437949b126`: F6 закрыт в проверенных сценариях; остаток F5 требует согласования DEVMODE с драйвером. Подробности — в конце документа.** Ниже сохранено исходное review. **Исходный вердикт: CHANGES REQUIRED.** Проверен implementation commit `e76c5c4b67c768039decfbb419fc32027c36f335` относительно BA `fbb343a85486c92f9910432464a90440e8ca1537`. На исходном SHA Windows-программа не была готова: имелись подтверждённые технические дефекты, Windows CI завершился ошибкой до сборки установщика. Это независимое техническое ревью, не физическая или продуктовая приёмка.

До проверки повторно прочитаны основной `AGENTS.md`, полный `docs/requirements/WMS-442.md`, `owner-cases.md`, `failure-cases.md`. Проверена дельта целиком; изменения требований441/443/444/445 принадлежат отдельному BA-аудиту и не являются реализацией442. Применимые критерии: один внешний результат без скрытого повтора (B02/B09/B10), возможность продолжить после сбоя (B07), сохранение контекста и отсутствие придуманных ограничений. Продуктовый код, БД, принтеры, системные задачи и общие процессы review не меняет. Параллельные незакоммиченные исправления Terra не входят в этот вердикт.

## Подтверждённые дефекты для того же Terra high

### F1 · P1 · XML автозапуска содержит недопустимый RestartOnFailure Count

`tools/print-agent/wms_print_runtime.py:520` задаёт `<Count>999</Count>`. В опубликованной Microsoft XML-схеме Count имеет тип unsignedByte с минимумом1, то есть1–255. Такой XML не соответствует контракту регистрации; после сопряжения `_register_windows_task` не может считаться рабочим. Текущий unit test подменяет весь `schtasks` ответом0 и не валидирует XML.

[Microsoft Count/restartType](https://learn.microsoft.com/en-us/windows/win32/taskschd/taskschedulerschema-count-restarttype-element) и адресная проба подтверждают значение999 за пределами схемы. Реальная регистрация именно этого XML на Windows в данном review не запускалась — это подтверждение по коду и официальному контракту, не выдуманный Windows traceback.

Исправить допустимое значение и проверить настоящий register → query → run → аварийное завершение → restart в пользовательском профиле Windows. R14, C21/C22.

### F2 · P1 · Unicode-настройка и русская ошибка не сохраняются на обычной Windows

`tools/print-agent/wms_print_runtime.py:100–101` открывает JSON без encoding и пишет `ensure_ascii=False`; чтение на115 также использует неявную кодировку. На Windows с ANSI cp1252 русское имя очереди/сообщение нельзя записать. Это уже **реальная ошибка Windows CI**: `test_destination_and_size_mismatch_are_rejected_before_native_call` падает в `write_private → json.dump → cp1252` с `UnicodeEncodeError`. Следствие — не только невозможность сохранить «Принтер склада»: отрицательная квитанция не доходит до сервера, хотя отказ был до передачи.

Адресная проба повторяет запись кириллической очереди в cp1252 без изменения продукта. Использовать явное согласованное UTF-8 чтение/запись локального состояния; если нужен переход со старых файлов, определить совместимость отдельно. Проверить настоящие Unicode queue/config/error round trips на Windows с обычной ANSI-локалью, не скрывать дефект одной переменной UTF-8 в CI. R13/R16/R17, C20/C24.

### F3 · P1 · Установщик заменяет/удаляет файлы до подтверждённой остановки, rollback отсутствует

`tools/print-agent/windows-installer.nsi:15–19` вызывает `--stop`, ждёт фиксированные1500мс и перезаписывает рабочий каталог; на33–39 удаление тоже не ждёт worker. В runtime625–639/782–788 `--stop` только сигнализирует событие и сразу выходит0. Worker читает событие лишь после HTTP/печати, на857; штатный сетевой вызов или драйвер легко работает дольше1,5с. `ExecWait` ждёт процесс команды `--stop`, а не процесс печати.

В результате обновление попадает в заблокированные DLL/exe или частично заменяет bundle, удаление может оставить работающий процесс/файлы после удаления записи программы. Установщик не проверяет результат stop, не сохраняет предыдущий целый bundle и не восстанавливает его при отказе в середине копирования. R18 требует возможность вернуться к прежней версии с сохранённым состоянием; фраза README о сбое только «до замены файлов» не закрывает это.

Нужны подтверждённая остановка/освобождение блокировки, проверяемая замена целой версии с восстановлением при неудаче и честный результат удаления. Проверить обновление/удаление при задержанном HTTP и после начала spool, плюс отказ копирования в середине. R18, C27/C28.

### F4 · P1 · Обновление и переустановка запускают worker в обход автозапуска

`tools/print-agent/windows-installer.nsi:28–29` при наличии сохранённого `connection.json` выполняет только `wms-print.exe --run`. В runtime824–829 эта ветка **не регистрирует** задачу и не запускает её через Планировщик. После удаления задача уже стёрта `disable_autostart`; переустановка сохраняет настройку, запускает печать сейчас, но следующий вход ничего не запустит. После обновления самостоятельный процесс также не управляется `RestartOnFailure`; аварийный выход оставит печать выключенной до будущего входа/ручного запуска.

После установки/обновления/восстановления использовать общий путь регистрации и запуска ровно одного worker через штатный механизм, сохраняя connection_id. Проверить remove → reinstall → reboot/login без повторного сопряжения и аварийный restart после update. R13/R14/R18, C21/C22/C27/C28.

### F5 · P1 · Размер bitmap не задаёт размер бумаги и параметры драйвера

`tools/print-agent/wms_print_runtime.py:328–355` создаёт `CreatePrinterDC(queue)` с текущими параметрами драйвера, читает только DPI88/90 и рисует прямоугольник от0,0. Размеры58×40 влияют лишь на bitmap; настройки бумаги, ориентация, printable area/physical offsets не задаются и не проверяются. При очереди с иной формой/непечатаемыми полями файл может обрезаться или уйти на другой формат, а код всё равно вернёт `windows-N`. Число копий дополнительно умножается циклом, но собственное число копий драйвера не нормализовано.

Адресная GDI-проба фиксирует только CreatePrinterDC, DPI, draw464×320, две страницы и квитанцию: вызова конфигурации/проверки формы нет. Это проверка границы вызовов, **не имитация отпечатанного листа**. [Microsoft DEVMODE](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/ns-wingdi-devmodea) описывает отдельные параметры бумаги и копий; [создание printer DC](https://learn.microsoft.com/en-us/windows/win32/printdocs/retrieving-a-printer-device-context) допускает передачу этих параметров.

Согласовать с драйвером размер/ориентацию/копии и проверить итоговую геометрию, либо до spool честно сообщить неподдерживаемый формат без обрезанной «успешной» этикетки. Проверить PNG и настоящий PDF, разные размеры, ненулевые поля, один/несколько экземпляров, затем физический C25. R4/R15, C25/C26.

### F6 · P2 · Переподключение и повторный запуск настройки заблокированы собственным фоном; macOS update не перезапускает версию

`tools/print-agent/wms_print_runtime.py:817–823` захватывает тот же exclusive lock до обработки `--reconnect`. После введённого обязательного фона он постоянно занят worker: указанная в README команда смены очереди теперь получает общий отказ вместо настройки. Проба с реально удерживаемой временной POSIX-блокировкой возвращает1 и не доходит до переподключения. На Windows логика владения та же.

На macOS `build_package.py:81–86` копирует bundle поверх работающего и запускает настройку; остановки/перезапуска LaunchAgent нет. Даже после освобождения lock повторный `launchctl bootstrap` на уже загруженный label (runtime590–600) не является идемпотентным запуском. Настройка не должна объявлять уже подключённую программу неподключённой только из-за собственного фонового экземпляра.

Дать рабочий путь повторного открытия/смены назначения с координированной остановкой и возобновлением, а macOS-update — остановку, сохранение состояния и запуск новой версии. Сохранять старое назначение jobs; не удалять ключи сервера и не вводить новый экран. R13/R18/R19, C20/C27/C29/C30.

### F7 · P2 · Windows worker собирается консольным приложением

`tools/print-agent/build_package.py:39–72` собирает один обычный console executable; задача с `InteractiveToken` (runtime514/522) запускает его напрямую. Нет windowless worker, скрывающего launcher или управляемого отделения консоли. Поэтому требование фона реализовано новым постоянным консольным окном пользователя; его закрытие завершает worker, после чего Планировщик может открыть его снова.

Это следует из вызова сборки и [документированного default PyInstaller console](https://pyinstaller.org/en/stable/usage.html); адресная проба фиксирует отсутствие windowed/hide режима. Окно на чистой Windows я не наблюдал. Сохранить простую настройку, но обеспечить действительно фоновый worker без постоянного терминала и проверить его stdout/error поведение. Не превращать это замечание в новый продуктовый UI. R14, C21/C22.

### F8 · P1 · Сброс удаляет незавершённое состояние, пока worker ещё может печатать

`tools/print-agent/wms_print_runtime.py:795–799` после одного сигнала stop сразу удаляет connection/inflight, не дождавшись worker и не взяв его lock. Сам worker уже держит config в памяти и может находиться внутри submit/ack; затем он способен заново записать outbox поверх объявленного сброса или продолжить печать старого соединения. Удаление задачи само по себе не подтверждает завершение запущенного процесса.

Адресная проба держит lock от имени worker, вызывает именно CLI reset с подставленной доставкой stop и получает0 при реально удалённых connection/inflight. Это воспроизведение отсутствующего барьера, без настоящего сервера/принтера. Явное согласие на сброс разрешает удалить состояние, но не устраняет гонку с прежним исполнителем. После подтверждённой остановки сброс должен владеть состоянием эксклюзивно; предупреждение о незавершённости должно предшествовать удалению. R17/R18/R19, C24/C28.

## Проверки и ограничения

[GitHub run34773072757](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34773072757) проверен через `gh run view` на точном SHA. Windows job103766105214 завершён `failure`:18tests,4failures/2errors; build/self-test/upload пропущены. Среди причин — реальная Unicode-ошибка F2, POSIX `0600` проверки на Windows и запуск shell-fixture с WinError193. Исправить платформенные тесты, сохранив проверки прав/повторов на настоящих Windows API; просто пропустить эти проверки нельзя. macOS job103766105302 прошёл tests/build/self-test/upload. Повторный полный локальный запуск тех же18tests не выполнялся без основания.

[Адресные пробы](artifacts/wms442-astra-windows-review/probe.py) загружают продуктовые модули через `git show e76c5c4b:...`, поэтому не затрагивают и не случайно принимают параллельные исправления Terra. [Результат](artifacts/wms442-astra-windows-review/probe-results.json) содержит проверки F1/F2/F5/F6/F7/F8. Пробы используют временный синтетический профиль и mock границы ОС; реальные задачи, драйверы, ключи, печать и WMS не вызываются.

Положительное по коду: серверная дельта ограничена `win32` и Unicode-именем очереди; tenant/warehouse/connection/claim проверки не ослаблены. Сохраняются проверка MIME/размера/checksum, явная очередь, durable `submitting`, повтор только квитанции и отсутствие скрытого повторного spool при неопределённом исходе. NSIS использует user/HKCU/LocalAppData; в аргументах autostart нет доступа агента. Windows ACL применяет текущий SID и SYSTEM, но проверка фактического доступа вторым обычным Windows-пользователем остаётся C20; POSIX mode-биты её не заменяют. Новый web/Android UI, новая серверная очередь и складские ограничения в implementation diff не добавлены. Сохранение прежнего CUPS пути подтверждено кодом и macOS CI; это не снимает F6 и аппаратную границу.

Сам текущий `--self-test` на Windows использует synthetic command spooler и **не создаёт WindowsAdapter**, не доказывает доступность pywin32/PIL/PyMuPDF в установленном bundle и не проверяет NSIS install/update/uninstall или настоящий Task Scheduler. Поэтому даже исправленный зелёный build job будет только промежуточным доказательством. Нужен artifact, запуск установленного пакета на Windows с перечислением реальных очередей/декодированием PDF/PNG, C19–C30 применимо к каждой заявленной ОС и физическая бумага C13/C25. Ссылка на исходники/CI без доступного сотруднику установщика не закрывает R12. Конкретный Windows artifact этого SHA отсутствует из-за пропущенного build/upload; готовность матрицы Windows10/11 не заявляется.

## Заключение

**Вернуть F1–F8 тому же Terra high, затем предметно перепроверить исправленные переходы и Windows CI.** Приёмку R12–R19 и выпуск не разрешать сейчас. Отдельно остаются чистая установка/вход/перезагрузка/права/обновление/удаление, реальные PDF/PNG queue receipts и физическая этикетка; исходные незавершённые C1–C18 этим review не приняты и не повторены. Решение не сводится к ожиданию GitHub: кроме упавшего Windows job есть перечисленные дефекты кода.


## Предметная перепроверка F1–F8: de8deca9 → e85fd924

Проверены только исправления исходных замечаний: `d146917177b44b28ca64d873d21205ba226bfdce`, `de8deca9fffbfbff7ae48c71410beb1680cc284e`, затем узкая дельта `e85fd924cd737e843fdce9e8f21ff00215406447`. Источники адресных проб читаются через `git show` соответствующего SHA; параллельный незакоммиченный продуктовый diff не принят и не изменён. Прежние серверные проверки и аппаратная приёмка повторно не запускались.

| Замечание | Вердикт по коду после e85fd924 | Основание и граница |
|---|---|---|
| F1: Task XML | Исправлено | Count=255; register теперь дополнен query. Это закрывает недопустимое значение, но настоящий Task Scheduler register/run/crash/restart не исполнялся в review. |
| F2: Unicode | Исправлено | Явный UTF-8 при записи и чтении; адресный round trip «Принтер склада» проходит. Windows CI тоже проходит прежнюю ошибочную ветку. |
| F3: stop/update/uninstall/rollback | Исправлен найденный Windows control flow; C27/C28 ещё не приняты | Вместо Sleep используется ожидание mutex до30с. Update проверяет stop и сохраняет previous bundle, копирование/ошибка запуска имеют ветку восстановления. В de8 uninstall игнорировал exit code, но e85 проверяет его до удаления. Реальные заблокированные файлы, отказ установки и восстановление на Windows ещё не проверены. |
| F4: supervision/reinstall | Исправлено по коду | Сохранённая connection вызывает --ensure-autostart: register/query и запуск через планировщик; raw --run удалён. В de8 свежая установка попадала в сравнение неинициализированного $0; e85 разделяет have_connection/no_connection и устраняет этот путь. |
| F5: geometry/copies | Частично исправлено; остаётся P1 | e85 проверяет physical и printable размеры, получает offsets, отказывает до spool при недостаточной области/отсутствующих размерах. Число копий драйвера всё ещё не проверяется и не нормализуется; подробнее ниже. |
| F6: повторная настройка/reconnect | Частично исправлено; остаётся P2 | --reconnect сначала ждёт остановки. Но обычное открытие setup остаётся внутри lock worker; при pending receipt reconnect останавливает worker, отказывает и не возобновляет старое подключение. Ни de8, ни e85 не меняют эти ветки. macOS launcher теперь вызывает stop перед копированием; нативное update/restart доказательство отсутствует. |
| F7: консоль worker | Исправлено по коду | Windows собирает отдельный --windowed worker и console setup; scheduler получает worker. Консоль только для первичной настройки соответствует простой существующей настройке. Новая GUI-форма не требуется и не добавляется. Реальное отсутствие окна и stderr/stdout поведение bundle относятся к C19/C21. |
| F8: reset до остановки | Исправлена исходная гонка с активным worker по коду | Runtime требует успешный wait прежде disable/unlink; на stop timeout адресная проба подтверждает сохранение connection/inflight и отсутствие disable. Нативная Windows блокировка/одновременный старт остаются проверкой C22/C24/C28. |

### Остаток F5 · P1 · Windows driver copies всё ещё умножает страницы

На frozen e85 `tools/print-agent/wms_print_runtime.py:379` остаётся `CreatePrinterDC(queue)` без per-job DEVMODE; на389 программа сама повторяет страницы `copies` раз. В модуле нет чтения или установки `dmCopies/DM_COPIES`. При очереди, чьи штатные настройки драйвера содержат две копии, запрос двух копий создаёт две страницы, каждую из которых драйвер может повторить дважды. WMS получает успешную квитанцию, хотя вышло четыре экземпляра. Это продолжение исходного F5 и прямого R15, а не новое требование.

[Microsoft DEVMODE](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/ns-wingdi-devmodea) определяет отдельный параметр копий драйвера; [создание printer DC](https://learn.microsoft.com/en-us/windows/win32/printdocs/retrieving-a-printer-device-context) позволяет передать настройки конкретного задания. Вывод основан на неизменённой границе вызовов и документированном контракте драйвера; четыре физических листа в review не печатались. Строка README о том, что физическая приёмка «исключает умножение», не исправляет код.

**Handoff Terra:** получить и валидировать per-job DEVMODE выбранной очереди, нормализовать driver copies до1 при существующем повторе страниц (либо отказать до StartDoc, если драйвер не позволяет гарантировать количество). Не менять глобальные настройки принтера. Проверка должна начинаться с default copies=2 и доказать, что WMS copies=1/2 не превращается в2/4. Сохранить private driver data и Unicode queue. Аппаратный C25 остаётся отдельно.

### Остаток F6 · P2 · Отказ переподключения оставляет прежнюю печать остановленной

На frozen e85 `tools/print-agent/wms_print_runtime.py:959–966` сначала останавливает background, затем обнаруживает `inflight.json` и возвращает ошибку. Ветка не вызывает `start_registered_background` для старого сохранённого подключения. Адресная проба de8 (эти строки в e85 логически не менялись) получает exit1, stop_called=true, restart_called=false, config сохранён. Квитанция, которую программа просит восстановить, больше не обрабатывается автоматически её worker. При обычном открытии ярлыка настройки без --reconnect с занятым собственным lock также получается exit1 до setup.

**Handoff Terra:** проверить препятствие до остановки либо гарантированно возобновить прежний background при отказе/отмене reconnect; повторное открытие setup для существующего подключения не должно ошибочно сообщать отказ из-за собственного рабочего процесса. Сохранить прежний connection и запрет повторного spool; отдельный экран не нужен. Адресная проверка: pending receipt → reconnect → понятный отказ и работа старого worker/доставка старой квитанции без новой печати; обычное открытие ярлыка при уже работающей программе сохраняет подключение и фон.

### Проверки дельты и честная граница выпуска

[Run34773452733](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34773452733) завершён SUCCESS на точном de8deca9; Windows и macOS jobs зелёные, опубликованы неистёкшие artifacts WMS-Print-Windows-x64 (42 780 142 bytes) и WMS-Print-macOS (15 172 786 bytes). [Run34773660458](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34773660458) также завершён SUCCESS на точном e85fd924: обе платформы прошли tests/build/self-test/upload. Это впервые подтверждает существование собранного Windows артефакта, но не корректную установку или физическую печать.

Просмотрены новые/изменённые проверки в наборе21tests. Они закрывают UTF-8, диапазон Count, synthetic Windows cmd receipt и mock geometry. Installer test проверяет наличие строк, а не исполняет NSIS install/uninstall; именно поэтому пропустил две ошибки ветвления de8. Проверка настоящего process-exit после spool на Windows пропущена и заменена process_once mocks; эту границу нельзя выдавать за нативное подтверждение recovery. Self-test по-прежнему пользуется synthetic command, а не WindowsAdapter/драйвером. Повторно гонять весь прежний набор локально без новых оснований не потребовалось.

[Delta-проба de8](artifacts/wms442-astra-windows-review/delta-probe.py) и [результат](artifacts/wms442-astra-windows-review/delta-probe-results.json) подтверждают Count255, UTF-8, удержание состояния на stop timeout, остаток F6 и прежний пропуск printable area. [Узкая проба e85](artifacts/wms442-astra-windows-review/latest-probe.py) и [результат](artifacts/wms442-astra-windows-review/latest-probe-results.json) подтверждают отказ до spool для области440×300 при физическом формате464×320, отказ для отсутствующего размера, пропуск правильного58×40 и новые NSIS source guards. Это воспроизводимые проверки кода и mock API, не запуск Windows/NSIS/принтера.

**Актуальное заключение:** CHANGES REQUIRED: вернуть тому же Terra только остатки F5/P1 и F6/P2. Закрытые замечания повторно не исследовать без нового дефекта. После исправлений возможен технический PASS; C19–C30 остаются отдельными проверками установленного пакета и складского процесса, C25 требует реальной бумаги и считывания кода. Ни зелёные jobs, ни этот review не закрывают чистую Windows установку, вход/перезагрузку, права, update/uninstall и аппаратную приёмку. Статус «Windows готова» сейчас недопустим.

## Узкое продолжение: 97311c53 → 35fe76fb

Проверен кандидат `35fe76fb5b7345f7221d51416672b7437949b126` относительно `97311c53d3470bad117ae145fc2f39f50758f2bb`. Прочитаны только новая продуктовая дельта setup/reconfigure и её тесты; прежние закрытые замечания не проверялись заново. AGENTS.md, требования WMS-442 и библиотеки owner-cases/failure-cases прочитаны в этой же сессии; документ требований и правила в данной дельте не менялись. Соседние документы WMS-441/WMS-443 не входят в вердикт. Продуктовый код, основной checkout, системные задачи и оборудование ревью не изменяет.

### F6 — PASS по исправленному сохранению подключения и порядку stop/lock/resume

На 97311c53 независимо воспроизводилось удаление прежнего connection.json при отмене первого ввода или неверном URL: обработчик запускал фон после unlock, но без прежнего файла. На 35fe76fb force_new_connection обходит чтение старой настройки, сохраняя сам файл; новая конфигурация записывается через существующий атомарный write_private только после ответа paired=true и получения warehouse_id.

Пять целевых regression tests из test_wms_print_runtime.RuntimeTest прошли локально: отмена reconnect, неверный сервер, inflight rejection, ошибка обычного setup, stop перед lock при обычном setup. Полный прежний набор локально не повторялся.

Дополнительная независимая проба загрузила runtime через git show точного 35fe76fb и выполнила настоящий setup/reconfigure с временным синтетическим файлом и настоящей POSIX-блокировкой. HTTP, выбор очереди, stop/register/start подменены; настоящие WMS, планировщик и принтер не вызывались. При подставленном start повторно захватывалась настоящая блокировка, что проверяет её освобождение до запуска.

| Сценарий | Наблюдение | Вердикт |
| --- | --- | --- |
| Отмена первого ввода; неверный URL; неверный номер очереди | Прежний файл побитово сохранён; stop → lock → unlock → start с прежним подключением | PASS в локальной пробе |
| Ошибка /pairing; ошибка /agent/heartbeat во время ожидания; отмена во время ожидания | До подтверждённого pairing старый файл не заменён; после ошибки блокировка освобождена и вызван start с прежним подключением | PASS в локальной пробе |
| Успешное подтверждение pairing | Старый файл сохраняется до paired=true; затем записано новое подключение со складом, register → unlock → start с новым подключением | PASS в локальной пробе |
| Reconnect при inflight; ошибка обычного setup | Прежний connection.json сохранён; start после unlock; для inflight новая настройка не начинается | PASS в локальной пробе |

Это закрывает предъявленный остаток F6 о потере прежнего подключения до завершения смены. Проба не доказывает реальную остановку/запуск Windows worker, доставку квитанции настоящему WMS или поведение при потере ответа уже подтверждённого внешнего назначения. Полный C29 не принят.

### F5 — неизменённый остаток после 97311c53, P1

На 97311c53 per-job DEVMODE получает Copies=1, Collate=0 и соответствующие Fields; глобальный SetPrinter не вызывается. Независимые подмены API подтвердили 1/2 программные страницы при запросах 1/2 и сохранение defaults copies=2; отсутствие DEVMODE, невозможность присваивания/сохранения Copies и исключение CreateDC останавливают submit до StartDoc. Это положительная часть исправления.

Однако runtime:379–386 проверяет только присваивание в объект, затем передаёт его непосредственно в CreateDC. Согласования изменённых общих и приватных параметров с драйвером нет. Microsoft документирует, что при неприемлемом DEVMODE CreateDC может использовать параметры драйвера по умолчанию; для локального изменения параметров описан DocumentProperties с DM_IN_BUFFER | DM_OUT_BUFFER перед CreateDC. Источники: [CreateDCW](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/nf-wingdi-createdcw), [DocumentProperties](https://learn.microsoft.com/en-us/windows/win32/printdocs/documentproperties).

Подмена CreateDC, моделирующая этот документированный fallback к defaults copies=2, пропускает выполнение до StartDoc и windows-442 для запросов 1/2. Это доказательство отсутствующего отказа на границе API, а не воспроизведение физических 2/4 листов. В 35fe76fb WindowsAdapter не изменён; F5 не перепроверялся повторно и не закрыт.

**Handoff тому же Terra:** согласовать изменённый DEVMODE с драйвером, сохранив приватные данные; проверить успешный результат и возвращённые Copies=1/DM_COPIES. Отказ или неподдерживаемое число копий должны завершать операцию до StartDoc. Глобальные параметры очереди не менять. Проверить отказ согласования и возврат другого числа копий, затем штатные WMS copies=1/2. Физическая проверка количества листов остаётся C25.

### CI и итог

[Run34775465101](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34775465101) проверен через gh run view: SUCCESS на точном `35fe76fb5b7345f7221d51416672b7437949b126`; windows-package и macos-package завершены success. Ранее [Run34773987533](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34773987533) также проверен SUCCESS на точном 97311c53.

**Итог: F6 — технический PASS в указанном объёме; общий review — CHANGES REQUIRED из-за остатка F5.** Физический Windows-принтер, C25, чистая установка и полный C19–C30 не приняты. Новая продуктовая приёмка и деплой не заявляются.
