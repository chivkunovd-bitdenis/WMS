# Вердикт: PASS

**F5 закрыта в обоих вариантах. F2–F4 не регрессировали; новых дефектов в проверенном diff не обнаружено.** F1 WMS-455 остаётся закрытой по границе изменений, без повторной приёмки этой задачи.

Astra, пятый круг независимого перекрёстного ревью WMS-437, 17.09.2026. Проверен worktree `wms453-455-review2`, HEAD `a0b0168116063f8a9c8f433fcd2a7cf5bcdd5d1e`. Адресное сравнение с `4f15f1fe63220656a5c3858904eeff39291a5d06` подтвердило три изменённых файла фронта: `FfFbsSupplyWorkspace.tsx`, `fbsUx.ts`, `fbsUx.test.ts`; их рабочее содержимое совпадает с объектами HEAD. База задачи `origin/etalon 388f389b` — из поручения. До проверки прочитаны основной `AGENTS.md`, обе библиотеки `owner-cases.md` / `failure-cases.md` целиком, требования WMS-453 §§3–5 и отчёты front-1..4. Изменения документов приёмки не ревьюировались.

## F5 — закрыта

В `fbsUx.ts:153–162` ветка незавершённой отправки теперь всегда заканчивается после одного повтора старого тела и ключа. Окончательный отказ возвращается обработчику, автоматической отправки нового тела нет. Успех закрывает окно только при полном совпадении формы со старым телом; иначе возвращается `resolved`. В `FfFbsSupplyWorkspace.tsx:1641–1646` пустая отмеченная строка учитывается при вычислении полноты, а на строке 1043 эта полнота передаётся helper.

Независимое воспроизведение выполнено на настоящей `sendFbsBoxShipment` и извлечённых через TypeScript AST (синтаксическое дерево) обработчиках `assignBoxOrders`, `run`, `load`, галки, поля, открытия/закрытия окна и выражениях выбора/доступности кнопки. Подставлены транспорт, состояние и функции обновления состояния React; ошибки создавались настоящим `FbsApiError`. P и Q — две позиции одного заказа по 100 штук. Проверены тела, ключи, количество вызовов, итоговые количества, выбор, поля, ошибка, состояние окна и согласованность ref/state незавершённой отправки.

| Сценарий | Полученный результат |
|---|---|
| **F5/А:** P=5/K1 применились, ответ потерян; Q отмечена, поле Q очищено; повтор | Ровно два запроса P=5/K1. Итог P=5, Q=0. Окно открыто, выбраны P и Q, поля `{P:"5", Q:""}` сохранены, свежий остаток P=95, ошибка и pending сняты. «Добавить» заперта; дополнительный вызов обработчика ничего не отправляет. |
| **Продолжение А:** заполнить Q=1 | Один новый запрос `[P=5,Q=1]` с K2, итог P=10/Q=1, окно закрыто. Это отдельное добавление после подтверждения старого. |
| **F5/Б:** P=70/K1 не дошли; другой оператор добавил P=40; ввод P=60, Q отмечена с пустым полем; повтор | Уходит только P=70/K1, получен 409. На этом нажатии второго POST нет. Итог P=40/Q=0; окно открыто, ошибка установлена, P=60 и пустая отмеченная Q сохранены, pending снят, кнопка заперта. |
| **Продолжение Б:** заполнить Q=1 | Один новый запрос `[P=60,Q=1]` с K2. Итог P=100/Q=1, окно закрыто. |

Новые vitest покрывают оба варианта на helper. Проверка извлечённых обработчиков дополнительно подтвердила вычисление полноты из реальных галок/полей и блокировку кнопки после снятия pending — это не подстановка готового `formComplete=false` вместо проверки экрана.

## F1–F4 и остальные границы

- **F1:** файлы WMS-455 не менялись относительно четвёртого круга; оснований пересматривать закрытие в этом diff нет.
- **F2/A и C:** после применившейся либо недоставленной первой отправки повтор идёт как 5/K1; итог 5, окно закрыто. **F2/B:** изменение 5 → 10 сначала подтверждает 5/K1, сохраняет открытое окно и ввод 10 со свежим остатком 95; следующее нажатие отправляет 10/K2, итог 15. Проверено при обоих исходах первой отправки.
- **F3/M1 и M2:** другой оператор добавил 5 между первой попыткой и повтором. Независимо от того, дошла первая попытка или нет, два запроса проверяемого оператора содержат 5/K1; после повтора итог 10, окно закрыто. Подтверждение по одному чужому приросту не возвращено.
- **F4:** 100/K1 применились, ответ потерян, настоящий `load(true)` обновил остаток до нуля. Текущее новое тело пусто, сохранённый ввод содержит 100; повтор доступен, уходит 100/K1, итог остаётся 100, окно закрывается. Проверено также с поиском без совпадений. Таймер заменён непосредственным вызовом того же `load(true)`.
- **Полная неизменная форма и чужой остаток:** 70/K1 применились с потерей ответа, другой оператор добавил 30, выполнен `load(true)`. Повтор 70/K1 успешно закрывает окно, итог 100. В прочитанном серверном сервисе узнавание применённого ключа стоит до проверки остатка, поэтому такой ответ соответствует контракту.
- **Сняты все галки:** старое тело/K1 повторяется. После успеха окно остаётся открытым, pending снят, пустой выбор запирает кнопку; нового POST нет. При 409 показывается отказ с теми же ограничениями отправки.
- **Отказы:** 409/422 без `retryable` снимают pending; 500/503 и retryable 409 сохраняют старое тело/K1. Повторный сетевой обрыв сохраняет и старую отправку, и изменённый ввод. При полном изменённом вводе 60 или 80 старый 409 также не запускает новый POST автоматически: только следующее нажатие создаёт K2; 60 проходит, 80 получает собственный 409 без очистки формы.
- **Двойное нажатие:** при удержанном ответе второй прямой вызов `assignBoxOrders` не отправляет запрос. Проверено и для новой отправки, и для повтора; `busy` запирает кнопку, ref-флаг снимается после завершения и отказов. Реальное планирование DOM-событий без браузера не проверялось.
- **Закрытие:** отказ от закрытия сохраняет pending; подтверждённое закрытие очищает его. Открытие другого короба отправляет новый ввод туда с K2, без переноса старого тела/K1.
- **R12:** в diff нет изменений JSX-разметки, темы, WB-ветки и соседних вкладок. Изменены сравнение формы, результат повтора, комментарии и целевые тесты. Новых элементов интерфейса нет; визуальная приёмка не выполнялась.

## Команды и результаты

Из `frontend` данного worktree:

```sh
npx tsc --noEmit -p tsconfig.app.json
npx tsc -p tsconfig.app.json --tsBuildInfoFile /private/tmp/wms453-review5-app.tsbuildinfo
npx tsc -p tsconfig.node.json --tsBuildInfoFile /private/tmp/wms453-review5-node.tsbuildinfo
npx vite build --configLoader runner
npx vitest run --configLoader native src/screens/v2
```

Первые четыре выполнены последовательно через `&&`: **код 0**, типы app/node и сборка прошли. Vite 8.0.8: 12195 модулей, 1.47 s; предупреждение о chunks больше 500 kB. `npm run build` отдельно не запускался: использован разрешённый способ с временными tsbuildinfo и runner. Vitest — **код 0, 9 файлов / 92 теста**, 74.33 s; `fbsUx` — 36, `fbsApi` — 14.

Два завершённых `node <<'JS' … JS` с `typescript.transpileModule`, AST и `vm` — **код 0**, assertions всех описанных сценариев прошли. Пробники запускались через stdin, без файлов тестов. Два предварительных запуска основного пробника остановились до сценариев из-за его настройки `exports` и заглушки `useMemo`; исправлены только эти настройки пробника. Реальных HTTP-запросов, БД и внешнего Ozon в нём не было.

`pwd`, адресные `cat/sed/rg/nl`, Python для чтения объектов и построения diff — выполнены. Git-команды не запускались, Git не изменялся; код, тесты и конфиги не редактировались, коммитов нет. Единственный документ результата — этот отчёт; сохранение в Git остаётся ведущему по поручению.

Браузер, скрипт `f5_check.py` и снимки `14-f5-*` в этом круге не открывались. Сообщённые ведущим прогоны А/Б и F4/M1/M2/A/B/C на стенде учтены как переданные свидетельства, не как мои клики. **PASS относится к пятому техническому ревью фронта; продуктовая приёмка и деплой этим отчётом не подтверждаются.**
