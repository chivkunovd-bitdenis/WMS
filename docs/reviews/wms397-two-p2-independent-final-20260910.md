**ACCEPTED в границах независимого code review: оба моих P2 закрыты в `6438dfbceb869a9ba7920ed2f9928c2f22e238fb`.** Для ранее проверенного chat-среза с этими исправлениями и обновлённым интеграционным патчем к coordinator `10aeccb3c3d11654f3d75340e9fb8265b266838c` оставшихся P1/P2 не обнаружил. Это принятие кода для интеграции, не подтверждение браузерной приёмки или выпуска.

Полный `wms397-recovery-two-p2-20260910.md` прочитан. Проверены фактическая дельта, новые тесты и совокупный интеграционный патч.

**Выбор точного FBS-заказа исправлен с учётом координаторского эффекта очистки.** Каждый актуальный ответ теперь записывает серверную строку одновременно в `orders` и `selectedCache`; общий чекбокс получает полноценный `selectedOrders`. Повторное чтение обновляет кэш, устаревшие ответы игнорируются, актуальный отказ очищает строки и выбор. Основание — [FfFbsOrdersScreen.tsx:636](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:636).

В интеграционном патче также исправлен эффект смены контекста: при `linkedOrderId` он сбрасывает выбор, сохраняя только что загруженную строку в кэше; для обычного списка прежняя очистка остаётся. **Эта часть патча необходима при интеграции.** Одной строки из worker-файла недостаточно.

Самостоятельно исполнил четыре теста исходных функций:

- **4/4 прошли** на неизменяемом worker source `6438dfbc`.
- **4/4 прошли** на результате применения патча к source `10aeccb3` полностью в памяти.

Проверки включают общий выбор и доступность действий после эффекта контекста, обновление строки, смену статуса и устаревшие/актуальные ответы. Это исполнение исходных функций с подставленным API, не браузерный тест.

**Отправка восстановленного draft без файла теперь отклоняется.** Проверка наличия объекта выполняется после ACL и лимита размера, под существующей блокировкой строки вложения с `populate_existing`, до установки любого `message_id`. Поэтому отсутствующий файл даёт `attachment_content_missing` → **422**, а не ложный **201**; ошибка второго объекта не оставляет первый частично присоединённым. См. [chat_service.py:484](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/services/chat_service.py:484).

Проверка действительно не загружает содержимое: локальный адаптер использует `stat`, S3 — `HEAD` с существующими bucket/prefix. `403`, `SlowDown`, `500`, I/O и ошибка создания адаптера не превращаются в «файл отсутствует»: они дают **503**, сохраняя точный запрос для retry. Повтор уже сохранённого сообщения остаётся идемпотентным и не требует новой проверки объекта. См. [object_storage_service.py:46](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/services/object_storage_service.py:46) и [строку 117](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp/backend/app/services/object_storage_service.py:117).

Самостоятельно проверил извлечённые методы с подставленными зависимостями в памяти: семь S3-сценариев HEAD/delete, три локальных stat-сценария и четыре исхода присоединения — missing, transient, ошибка инициализации, успех. Все прошли; чтение байтов было запрещено проверкой. Собственный PostgreSQL-прогон не выполнял: **34 PG и последующие scoped PASS остаются результатами автора**, их тестовый код прочитан.

Commit удаления теперь входит в обработку ошибки с rollback/503. Повторное удаление отсутствующего объекта допускается, реальные ошибки доступа не подавляются. Composer различает исправляемый missing и временную недоступность, сохраняя текст и корректную семантику retry. ACL, бюджеты, MIME и порядок блокировок upload/send/delete этой дельтой не ослаблены.

Обновлённый патч прошёл мой **`git apply --check` без применения** в текущем coordinator; `git diff --check` также прошёл. Патч сохраняет координаторские `open_inbound`, `supply_id` и `onDirtyChange`. Новых страниц, складских движений или навигационных запретов нет.

Сохраняются ранее обозначенные границы: отсутствующие рабочие seller-маршруты, drafts из чатов с отозванным доступом, объекты без SQL-записи после аварийного upload и внешняя потеря файлов уже отправленных сообщений. Данный fix устраняет именно возможность новой отправки известного отсутствующего файла.

Браузерный запрет для `5397` соблюдён, обходов не было. Кликов после интеграции и staging/production-проверки здесь нет. Код, БД, файлы и чужие worktree не изменял.