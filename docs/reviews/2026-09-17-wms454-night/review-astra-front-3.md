# Вердикт: PASS

Независимое перекрёстное ревью Astra по WMS-437, третий круг фронта, 17.09.2026. **Оставшийся F2 второго круга закрыт:** отклонение `fetch`, обрыв тела успешного ответа и HTTP-ошибка с конвертом обрабатываются для WB и Ozon. При успешно полученных правилах и привязках загрузчик возвращает данные окна с причиной и сохранёнными строками. Отказ обязательного запроса правил по-прежнему прерывает открытие. Новых блокирующих дефектов в исправлении `2b892fb1` не обнаружено.

PASS относится только к этому исправлению. Это не повторная приёмка всего пакета, не браузерная проверка и не разрешение считать общий набор тестов зелёным: три падения общего запуска разобраны в F4.

## Область и источники

Проверены `pwd`, `git branch --show-current`, `git rev-parse HEAD`, `git status --short --untracked-files=all`: каталог `/Users/deniscivkunov/Projects/WMS/.worktrees/wms454-night`, ветка `fix/wms454-456-457-stock-dialog`, HEAD **`2b892fb1047b5cd797499fb3475d606af0c83406`**, заданная база `4474ac96`. Состояние production не проверялось; база используется согласно поручению.

До ревью полностью прочитаны основной `/Users/deniscivkunov/Projects/WMS/AGENTS.md`, обе библиотеки `owner-cases.md` и `failure-cases.md` в `docs/reviews/2026-09-11-analyst-draft/`, все три текущих документа требований WMS-454/456/457 и оба предыдущих отчёта `review-astra-front-1.md`, `review-astra-front-2.md`.

Полный diff от базы получен командой `git diff 4474ac96..HEAD -- backend frontend`; состав пакета сверён через `--stat`. Предмет подробного разбора — `git show 2b892fb1` и текущие четыре изменённых файла: загрузчик, сборщик строк/текстов ошибок и их тесты. Команда `git diff --name-only 0115f82d..HEAD` показала только эти четыре файла и бэклог (отдельный коммит `280047d6`). Проверка `git diff --exit-code 0115f82d..HEAD -- backend frontend/src/screens/ff/products-fbs/stub.ts frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx frontend/src/screens/v2/FfProductsCatalogScreen.tsx` вернула **0, без вывода**. Неизменённая реализация и бэкенд повторно не разбирались.

На входе уже были изменены три документа требований и существовали три неотслеживаемых файла предыдущих ревью. Они не изменялись ревьюером. Браузер, PostgreSQL, production и реальные кабинеты не использовались по границам поручения. Везде ниже «данные окна возвращены» означает результат исполнения кода, а не визуально открытое окно.

В ссылках ниже короткие имена `fbsStockDialogLoader*`, `fbsSellerWarehouseRows*`, `stub.ts`, `FbsStockDialog*`, `FfProductsFbsPage.tsx` относятся к `frontend/src/screens/ff/products-fbs/`; `FfProductsCatalogScreen.tsx` — к `frontend/src/screens/v2/`.

## F1 — замечание без блокировки: исправление WB-доли из предыдущего круга не затронуто

**Доказательство:** `stub.ts:253–269`, `FfProductsFbsPage.tsx:113–135`; отсутствие изменений этих файлов подтверждено указанным выше `git diff --exit-code`. Все 14 тестов `FbsStockDialog.ozon.test.tsx` прошли в текущем запуске.

В дополнительном исполнении настоящих `loadFbsStockDialog → dialogShowsOzon → visibleWarehouses → initialDraft → fbsRuleBody` старое раздельное правило с общим `percent=0`, WB-долей 60 и Ozon-долей 40 проверено при каждом из трёх отказов каждого справочника. Для WB-товара во всех шести случаях единственная видимая строка — WB, в JSON остаётся `percent=60`, `publish_ozon=false`, поле `publish` отсутствует. Для объединённого товара во всех шести случаях сохраняются раздельные 60/40 и отсутствуют оба нетронутых флага публикации. Это проверка совместимости нового загрузчика с прежним исправлением F1, по WMS-454 R1/R2/R3/R7. Фактическая отправка остатка на площадку не выполнялась.

## F2 — замечание без блокировки: подтверждённый ранее дефект обрыва тела закрыт

**Код исправления:** `fbsStockDialogLoader.ts:49–59,92–122,128–150,152–196`. **Штатный вызов:** `FfProductsCatalogScreen.tsx:632–648`. **Требования:** WMS-457 R3/R4 (`docs/requirements/WMS-457.md:114–136`), сценарий C12 этого документа.

`loadDirectory` теперь ожидает и `fetch`, и `response.json()` внутри одного `try/catch`. При любой из этих сетевых ошибок возвращается `failure`; список остаётся `received:false`. Это один общий обработчик для двух площадок, без дублирования механизма. При HTTP-ошибке сохраняется сам `Response`: WB читает код конверта через `readFbsErrorEnvelope`, Ozon — текст сервера через `readApiErrorMessage`. Отказ не превращается в успешно полученный пустой справочник.

Активные привязки собираются штатным `buildSellerWarehouseRows` (`fbsSellerWarehouseRows.ts:62–98`): номер, `boundTo`, `fbsEnabled` и площадка сохраняются, для неполученного списка ставится `list_unavailable`. Неактивные привязки вне списка по-прежнему пропускаются. Правила проходят отдельно: `fetch` правил остаётся обычным участником `Promise.all`, HTTP-статус проверяется с `throw`, JSON читается без подавления исключения (`fbsStockDialogLoader.ts:92–108`). В каталоге `setFbsDialog` вызывается только после успешного `await` (`FfProductsCatalogScreen.tsx:635–648`).

### Исполненная матрица отказов

Тестовый транспорт возвращал успешные правила и активные WB/Ozon-привязки на WMS-склад `w`; сбой вводился в один справочник. Отдельно проверены WB-товар и объединённый товар. В каждом успешном результате проверены причина нужной площадки, номер и `list_unavailable` отказавшего справочника, неизменённое имя здорового справочника, сопоставление, обслуживание, обе доли правила и ровно четыре запроса. Имена здорового справочника содержали пробелы по краям; пробелы сохранились.

| Сценарий | WB | Ozon | Доказательство в тестах ветки |
|---|---|---|---|
| `fetch` отклонён с `TypeError('Failed to fetch')` | Данные возвращены, WB-причина и привязка сохранены | Данные возвращены, Ozon-причина и привязка сохранены | `fbsStockDialogLoader.test.ts:90–115` |
| HTTP 200, поток тела оборван | Данные возвращены, причина содержит `terminated while reading response body` | Данные возвращены с той же конкретной причиной в поле Ozon | `fbsStockDialogLoader.test.ts:118–143` |
| HTTP-ошибка с конвертом | Код `wb_upstream_error_401` даёт текст об отклонённом ключе | Сохраняется текст конверта Ozon | `fbsStockDialogLoader.test.ts:150–161`; Ozon HTTP 503 вместе с отказом WB — `:90–102`; отдельно Ozon HTTP 502 проверен дополнительной матрицей |

В дополнительной матрице все **12** комбинаций «2 вида товара × 2 площадки × 3 отказа» завершились `resolved:true`, `wbPercent:60`, `requests:4`. Три контрольных отказа правил завершились строго ошибкой:

```text
HEAD rules/fetch: resolved=false, error="Failed to fetch"
HEAD rules/body:  resolved=false, error="terminated while reading response body"
HEAD rules/http:  resolved=false, error="rules refused"
```

Тесты ветки также проверяют все три отказа правил (`fbsStockDialogLoader.test.ts:145–147,164–171`). Ни один из них не возвращает данные для установки окна.

### Качество проверки и воспроизводимость

Новые тесты импортируют **настоящий** `loadFbsStockDialog` (`fbsStockDialogLoader.test.ts:1–2`), подменяют только `fetch` (`:50–67`). Обрыв создан настоящими `Response` и `ReadableStream`, а не заранее заданным `received:false` и не подменой результата сборщика (`:41–48`): поток получает начало JSON `[{"name":`, затем `controller.error(new TypeError('terminated while reading response body'))`. Два теста проверяют именно возврат правильных строк/причин после этого сбоя, третий — отказ обязательного запроса. Все **9** тестов загрузчика прошли.

Дополнительная проверка выполнена командой `node /private/tmp/wms454-front3-check.cjs` — **exit 0**. Она компилирует прочитанные TypeScript-исходники через `typescript.transpileModule`; `toProduct`, `toRule`, `fbsRuleBody` и `TOTAL_KEY` извлекает из штатного файла через синтаксическое дерево. Подменены транспорт и базовый URL; бизнес-формулы не переписаны. Для отрицательного контроля только исходник загрузчика взят из `git show 2b892fb1^:frontend/src/screens/ff/products-fbs/fbsStockDialogLoader.ts`; остальные модули и входные данные те же:

```text
parent-loader wb/body:   resolved=false, error="terminated while reading response body"
parent-loader ozon/body: resolved=false, error="terminated while reading response body"
HEAD wb/body:           resolved=true, wbPercent=60, requests=4
HEAD ozon/body:         resolved=true, wbPercent=60, requests=4
```

Таким образом контрольное воспроизведение различает старый и исправленный загрузчик. Это отдельное исполнение исходников, не запуск Vitest на изменённом рабочем дереве. Временный проверочный скрипт не является частью поставки; вход, штатный путь и результаты зафиксированы здесь. В первой попытке скрипт остановился из-за неверного имени вспомогательной функции в самом проверочном коде; после сверки сигнатур `stub.ts:221–269` исправлен только скрипт в `/private/tmp`, и вся матрица выполнена заново успешно.

Небольшая сопутствующая правка убирает двойную точку из общих сообщений WB/Ozon (`fbsSellerWarehouseRows.ts:129–136,175–177`). Названия складов через эту функцию не проходят (`:68–80`). Проверки точного текста WB скорректированы только в пунктуации (`fbsSellerWarehouseRows.test.tsx:160–166`); различение отсутствующего ключа, 401, 403 и прочих ошибок сохранено, все 14 тестов файла прошли.

## F3 — риск: ранее переданный риск запоздалой записи привязки этим исправлением не закрывается

**Источник:** `docs/reviews/2026-09-17-wms454-night/review-astra-front-2.md`, раздел F3; указанный там участок `FfProductsCatalogScreen.tsx:655–708,727–735`. Проверка отсутствия изменений каталога между `0115f82d` и HEAD дала exit 0.

Второй круг отдельно оставил риск применения запоздалого ответа PUT к текущему окну без проверки принадлежности ответа конкретному открытию/продавцу. В третьем круге этот путь не изменён и, по прямому ограничению области, заново не воспроизводился. Поэтому риск не объявляется устранённым и не превращается в новый блокер исправления F2.

Связь и проверка остаются прежними: WMS-454 R5 и AGENTS.md §3; задержать запись привязки из окна A, открыть B с тем же ключом строки и завершить старый ответ, отдельно поменять порядок двух ответов одной строки. Это сценарий для отдельной работы с записью, а не требование добавить новые окна или ограничения в загрузчик.

## F4 — замечание без блокировки данного исправления: общий набор фронта не зелёный

`npm run test:unit -- --configLoader runner` завершился **exit 1: 443 passed, 3 failed; 61 файл прошёл, 2 упали; 100.94 s**. Весь набор нельзя выдавать за успешный.

Два падения — `frontend/src/screens/ff/FfInboundRequestView.test.ts:145,170`: набор `data-testid` не совпадает с ожидаемым, а `FfInboundRequestView.tsx` содержит 4128 строк при пределе 600. Тест читает исходники по маскам в `:13–28`; `git diff --exit-code 4474ac96..HEAD -- 'frontend/src/screens/ff/FfInboundRequest*' 'frontend/src/screens/ff/useFfInboundRequest*'` вернул **0, без вывода**. Эти два падения не вызваны изменениями рассматриваемой ветки. Воспроизведение — общий запуск выше или этот тест отдельно.

Третье падение — `frontend/src/screens/ff/inventory/InventoryScan.test.ts:130–141`: 1000 сканирований заняли **1153.946792 ms** при пороге `<1000 ms`; проверка итогового количества перед ней прошла. Тест импортирует `InventoryScan` и `InventoryRows` (`:1–4`). Diff каталога `frontend/src/screens/ff/inventory` от базы пуст. Отдельный повтор `npm run test:unit -- --configLoader runner src/screens/ff/inventory/InventoryScan.test.ts` прошёл: **19 passed, exit 0, 418 ms на запуск**. Эти 418 ms — длительность всего отдельного запуска, не измерение цикла сканирований. Причина колебания времени не устанавливалась; единичный отдельный успех не стирает падение общего запуска. Оснований связывать его с обработкой справочника в `2b892fb1` проверка не дала.

## Что проверено и в порядке по требованиям

Таблица ограничена изменением третьего круга. Успех прежних требований не выдаётся за повторную браузерную/серверную приёмку. Неизменность перечисленных файлов подтверждена Git-командой из раздела «Область и источники»; текущие результаты тестов приведены ниже.

| Требование | Результат в пределах третьего круга |
|---|---|
| WMS-454 R1 | При отказах справочников штатная цепочка до черновика оставляет WB-товару только WB-строку; `marketplaces` переносится загрузчиком (`fbsStockDialogLoader.ts:174–187`), фильтрация — `stub.ts:221–229`. 6 комбинаций WB-товара прошли. Компонент не изменён, все 14 тестов `FbsStockDialog.ozon` прошли. |
| WMS-454 R2 | В 6 комбинациях WB-товара сборщик возвращает `publish_ozon=false`, нетронутый `publish` отсутствует, WB-доля остаётся 60 (`stub.ts:253–269`, `FfProductsFbsPage.tsx:113–135`). Настоящий PUT и публикация не запускались. |
| WMS-454 R3 | Для объединённого товара при всех 6 отказах остаются обе привязки, доли 60/40, раздельный режим, оба нетронутых флага отсутствуют в JSON. Смешанная пачка и расчёт общего потолка в этом коммите не менялись; заново не принимались. |
| WMS-454 R4 | Подписи галок, чипов и их обработчики не менялись: `FbsStockDialog.tsx` отсутствует в diff третьего круга. Тесты компонента прошли. Исправление меняет только текст сетевой причины и его пунктуацию, не смысл галки заказов. |
| WMS-454 R5 | Дополнительная матрица сохраняет `boundTo` и `fbsEnabled` обеих привязок; запросы загрузчика — три GET и POST чтения правил (`fbsStockDialogLoader.ts:92–104`). Запись галки/правила не добавлена и не изменена. Конкурентная граница записи — ранее переданный F3, без нового вердикта по ней. |
| WMS-454 R6 | При каждом отказе в WB-черновик попадает только WB-строка (`stub.ts:226–229`); Ozon-данные загрузчика не становятся видимой Ozon-галкой. Сохранность реальной БД не проверялась. |
| WMS-454 R7 | Прежний F1 не вернулся в проверенных сценариях; все 48 тестов `products-fbs` прошли. Второй маршрут и сам диалог не менялись. `rg` по вызовам нового загрузчика и изменённых функций ошибок показал единственного продуктового потребителя — каталог (`FfProductsCatalogScreen.tsx:636`); второй маршрут не подключён к ним. |
| WMS-457 R1 | Здоровый справочник преобразуется без изменения имени (`fbsStockDialogLoader.ts:112–115,130–140`; `fbsSellerWarehouseRows.ts:68–80`). Это проверено именами с пробелами и прежними тестами WB/Ozon. |
| WMS-457 R2 | Сборка активной привязки вне полученного списка не менялась (`fbsSellerWarehouseRows.ts:83–96`), 14 тестов сборщика/рендера прошли. Новый загрузчик не меняет успешный список на неполученный без отказа. |
| WMS-457 R3 | Закрыт F2 для обеих площадок: все три отказа дают неполученный список, сохранённую активную привязку и причину соответствующей площадки. Доказательство — матрица F2 и `fbsStockDialogLoader.test.ts:90–161`. |
| WMS-457 R4 | Конверты WB всё ещё различаются по коду (`fbsSellerWarehouseRows.ts:144–180`); обрыв тела теперь попадает в сетевую причину. 401 не назван ошибкой прав, общий текст не содержит двойной точки. Проверки `fbsSellerWarehouseRows.test.tsx:152–195` прошли. |
| WMS-457 R5 | Фильтр неактивных привязок не изменён (`fbsSellerWarehouseRows.ts:83–88`); тесты этого файла прошли. |
| WMS-457 R6 | Четыре запроса сохранены (`fbsStockDialogLoader.ts:92–104`, тест `:74–87`), порядок «полученные кабинеты → дополнительные активные привязки» сохранён и проверяется при отказе каждой площадки (`fbsStockDialogLoader.test.ts:125–141,157–160`). Новых окон, кнопок, PUT или блокировок коммит не добавляет; геометрия интерфейса здесь не проверялась. |
| WMS-456 R1–R6 | Прочитаны для границы задачи, но целиком вне этого круга. Бэкенд не изменён после второго ревью; новых серверных проверок и нового PASS WMS-456 этот отчёт не содержит. |

## Соседние процессы и устройство исправления

Риски B08/B10, независимость галок WMS-351/376, одноразовый ноль и общий потолок WMS-350 учтены через проверку границы изменения. В `2b892fb1` нет изменений серверной публикации, сохранения правила, изменения обслуживания и расчётных функций; нет новых запросов записи. Дополнительная матрица проверяет сохранность обеих долей и нетронутых флагов после отказа справочника. Следовательно, в этом круге не появился новый путь изменения соседней площадки через тело PUT; действительные внешние нули и фоновые циклы не исполнялись и их повторная проверка не заявляется.

Новая форма `Directory<Row>` описывает только временный результат чтения, заменяя прежний `Settled`; это не сущность БД, счётчик или сохраняемый статус (`fbsStockDialogLoader.ts:49–59`). Тип результата разделяет строки, HTTP-ответ с ошибкой и исключение; чтение веток использует проверку наличия соответствующего поля (`:112,121,130,143`). Обработчик чтения общий для двух площадок. Новая конкурентная запись отсутствует; ранее переданный риск существующего обработчика записи не маскируется успешным чтением.

## Выполненные проверки и итоговый артефакт

1. `npx tsc --noEmit -p tsconfig.app.json` — **exit 0**.
2. `npm run test:unit -- --configLoader runner` — **443 passed, 3 failed**, разбор F4. Все шесть файлов `products-fbs` прошли: `FbsStockDialog.ozon` — 14, `fbsSellerWarehouseRows` — 14, `fbsStockDialogLoader` — 9, `FbsStockDialog.preview` — 4, `FbsStockDialog` — 5, `fbsWarehouseRuleKeys` — 2; всего **48**. Использован существующий `vitest.config.ts` с `environment:'node'`; `runner` позволяет загружать конфигурацию без сборки её временного файла в общий `node_modules`. Исходники и конфигурация не менялись.
3. Отдельный повтор `InventoryScan.test.ts` — **19 passed**, exit 0; подробности F4.
4. `npx tsc --noEmit -p tsconfig.node.json --tsBuildInfoFile /private/tmp/wms454-front3-node.tsbuildinfo` и затем `npx vite build --configLoader runner --outDir /private/tmp/wms454-front3-dist` — **exit 0**, Vite `built in 1.74s`. Предупреждения: выходной каталог вне корня проекта и фрагмент сборки больше 500 kB. Типизация приложения, типизация конфигурации и сборка выполнены раздельно с разрешёнными путями вывода; успешный неизменённый `npm run build` не заявляется.
5. Дополнительное исполнение штатного загрузчика и преобразований, 12 комбинаций справочников, 3 отказа правил и 2 отрицательных контроля старого загрузчика — **exit 0**, результаты F2. Первый неуспешный запуск самого проверочного скрипта раскрыт там же.
6. Бэкенд-тесты, Ruff и mypy не запускались: по прямому поручению бэкенд вне этого круга, его diff после второго ревью пуст. Браузерная приёмка C12 остаётся аналитику; от ревьюера она не требовалась.
7. `git diff --check` — **exit 0** до записи отчёта. После записи проверены сохранность HEAD, полного отслеживаемого diff и SHA-256 всех шести ранее существовавших документов (три требования и три ревью); изменений нет. Единственный добавленный ревьюером файл в checkout — этот отчёт.

По прямому поручению пользователя код, тесты, требования и бэклог не правились; коммитов и push нет. Результат этого круга — локальный файл ревью. Для исправления F2 дополнительная правка кода по результатам этого ревью не требуется.
