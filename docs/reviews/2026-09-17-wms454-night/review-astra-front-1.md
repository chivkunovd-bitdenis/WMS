# Вердикт: FAIL

Независимое перекрёстное ревью Astra по WMS-437, 17.09.2026. Блокирующие находки: F1 и F2. F1 — регресс сохранения WB-доли; F2 — невыполненная ветка требования о недоступном справочнике. F3 — отдельно отмеченный существующий риск конкурентной работы интерфейса, не новый регресс этих коммитов.

Проверен checkout `/Users/deniscivkunov/Projects/WMS/.worktrees/wms454-night`, ветка `fix/wms454-456-457-stock-dialog`, HEAD `e6f14c8aa01d591f80f4db75d400ba8b3e4e76a3`. Область: `git diff 4474ac96..HEAD -- backend frontend`, включая реализацию `27166518` и `e6f14c8a`. Diff по backend пуст. WMS-456 прочитан для контекста, его реализация этим заключением не принимается.

До проверки реализации полностью прочитаны `/Users/deniscivkunov/Projects/WMS/AGENTS.md`, обе библиотеки `owner-cases.md` и `failure-cases.md` из `docs/reviews/2026-09-11-analyst-draft/`, а также все три документа требований WMS-454/456/457. Браузер и PostgreSQL не использовались по условиям поручения. Ни внешний кабинет, ни production не опрашивались. Ниже подтверждение кодом и локальными исполнениями отделено от визуальной и серверной приёмки.

## F1 — дефект (подтверждён кодом/тестом), высокий приоритет: скрытие Ozon меняет сохранённую долю WB без действия оператора

**Места:** `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:140–163`; `frontend/src/screens/ff/products-fbs/stub.ts:246–252`; `frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:124–134`; `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:2119–2129`. Серверный потребитель: `backend/app/services/fbs_stock_rule_service.py:410–419,847–850`.

После фильтрации Ozon количество обслуживаемых складов становится равным одному. `initialDraft` принудительно ставит `sameEverywhere=true`, но оставляет `percent` от старого общего ползунка. Если сохранённое правило задавало доли отдельно, действующая WB-доля лежит в `byWarehouse`, а не в `percent`. Таким образом окно меняет источник числа; сохранение без редактирования записывает другой действующий процент WB. Отсутствие поля `publish` в PUT не защищает: меняются `same_everywhere` и `percent`.

**Воспроизведение:** товар с `marketplaces=['wb']`; у продавца по одному активному обслуживаемому WB- и Ozon-складу. Правило: `publish=true`, `publish_ozon=true`, `same_everywhere=false`, `percent=0`, `by_warehouse={'wb:501001':60,'ozon:1020005029603630':40}`, `units_mode=false`. Открыть окно и сохранить без изменения ползунков. WB должен сохранить заданные 60%; скрытие Ozon и выключение его флага не являются командой обнулить WB.

При ревью выполнены штатные `visibleWarehouses`, `servedWarehouses`, `initialDraft`, `fbsRuleBody` и `splitAmounts`. Для сравнения выполнен прежний инициализатор `useState` из `git show 4474ac96:frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx`. TypeScript компилировался из прочитанного исходника через `typescript.transpileModule`, формула черновика заново не переписывалась. Получен вывод:

```text
F1 {"oldSingle":false,"newSingle":true,"beforeWb":60,"afterWb":0,
"put":{"publish_ozon":false,"same_everywhere":true,"percent":0,
"by_warehouse":{"wb:501001":60,"ozon:1020005029603630":40},
"units_mode":false,"units_by_warehouse":{}}}
```

Дополнительно вызвана настоящая серверная функция `split_amounts` с `FbsRule` до/после и двумя `FbsWarehouseBinding`, без БД и внешнего транспорта:

```text
production split_amounts: {'before_WB': 60, 'after_WB': 0}
```

Это подтверждает изменение рассчитываемого количества при 100 свободных единицах; реальная отправка нуля в WB в ревью не выполнялась. В базовой версии при этих же двух складах `single=false`, поэтому отдельная WB-доля сохранялась. В новой версии общий ползунок также показывает 0 (`FbsStockDialog.tsx:341–353`), но пользователь не выбирал переход с сохранённых 60% на 0%.

**Нарушение:** WMS-454 R7 (`docs/requirements/WMS-454.md:193–198`) и правило AGENTS.md §2 о побочных изменениях соседних процессов; задача R1/R2 скрывает Ozon и выключает его флаг, не задаёт новый WB-процент. Особенно важно проверить старые правила, созданные до исправления: наличие в них Ozon-доли у WB-товара и есть исходная проблема задачи.

**Что проверить после исправления:** сохранять действующую долю единственного видимого WB-склада при переходе от раздельного правила к отображению одного ползунка. Проверить старые `percent=0` и `percent=50` при WB-доле 60, плюс прежний `same_everywhere=true` и режим штук. Тест должен получить черновик штатным путём и проверить тело PUT и эффективную WB-долю. Новый тест `FbsStockDialog.ozon.test.tsx:33–36,105–120` использует только `same_everywhere=true`; этот регресс он не ловит.

## F2 — дефект (подтверждён кодом/тестом), средний приоритет: сетевой отказ одного справочника отменяет открытие всего окна

**Места:** `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:658–674,688–700,779–781`; тесты `frontend/src/screens/ff/products-fbs/fbsSellerWarehouseRows.test.tsx:51–59,175–183`.

Все четыре запроса открытия объединены в `Promise.all` без отдельной обработки отклонённого `fetch` справочника. Если запрос `/operations/fbs-sellers/{seller}/warehouses` или `/ozon-warehouses` отклоняется с сетевой ошибкой, выполнение сразу попадает в общий `catch`. Ветки `received:false`, сборка сохранённых привязок и `setFbsDialog` не выполняются, даже если правила и привязки уже успешно загружены. Вместо окна со строками «№ …» остаётся ошибка открытия.

**Граница:** HTTP 502 с `wb_transport_error` уже обрабатывается правильно. Здесь речь именно об отсутствии HTTP-ответа между фронтом и API, а не об ошибке WB, которую API успел вернуть конвертом. Общий `Promise.all` существовал и в базе; это непокрытая новым требованием ветка отказа, не приписываемый коммиту новый сетевой механизм.

**Воспроизведение/локальное подтверждение:** из исходника через TypeScript AST извлечён и выполнен настоящий callback `openFbsStockDialog`. Выбран один товар; для запроса WB-складов `fetch` возвращает rejected Promise с `TypeError('Failed to fetch')`, остальные запросы возвращают успешные заглушки. Записаны вызовы `setFbsDialogError` и `setFbsDialog`. Результат:

```text
F2 [["error",null],["error","Failed to fetch"]]
```

Вызов установки окна отсутствует: до чтения успешных ответов код не дошёл. Это исполнение callback с подменным транспортом, не браузерный тест.

**Нарушение:** WMS-457 R3 (`docs/requirements/WMS-457.md:114–120`) прямо включает ошибку сети и требует показать сохранённые активные привязки с причиной отсутствия названия; R4 включает обрыв/тайм-аут (`:122–136`). Требуемый путь восстановления здесь отсутствует.

**Что проверить после исправления:** при отказе каждого из двух необязательных справочников отдельно и успешных правилах/привязках окно открывается, сохраняет строки и показывает причину нужной площадки. Ошибку обязательного запроса правил не превращать в успешное открытие с выдуманными данными. Нужен тест самого загрузчика: имеющийся тест сборщика получает готовый `received:false`, а тест чтения конверта получает объект `Response`, поэтому оба обходят точку отказа `Promise.all`.

## F3 — риск: запоздалый ответ записи привязки применяется к другому открытому окну

**Места:** `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:794–803,819–835,860–866,2109–2117`. В `git diff 4474ac96..HEAD` обработчик `saveFbsWarehouse` не изменён; это существующий риск, отдельно от двух блокирующих находок.

Запрос адресуется продавцу из замыкания `fbsDialog`, но при ответе обновляется текущее окно. Защита проверяет только `if (!current)`, затем ищет строку по ключу склада. Идентичность продавца/открытия и порядок ответов не проверяются. Старый ответ может заменить состояние более нового окна с таким же ключом строки. Запрет на новые пользовательские блокировки этому не мешает: проверка принадлежности ответа не требует окна подтверждения.

**Локальная проверка:** штатный callback `saveFbsWarehouse` извлечён из исходника через TypeScript AST. PUT от seller-A оставлен незавершённым; текущее состояние заменено окном seller-B с тем же ключом `wb:501001`, но складом `WMS-B`. Затем отдан ответ первого PUT `{served:false,wms_warehouse_id:'WMS-A'}`. Получено:

```text
F3 late response applied: {"seller":{"id":"seller-B","warehouses":[
{"id":"wb:501001","marketplace":"wb","boundTo":"WMS-A","fbsEnabled":false}]}}
```

Подтверждено применение ответа не к своему состоянию интерфейса. Запись неправильной привязки в БД этим тестом не заявляется; риск дальнейшей записи связан с тем, что следующий `setFbsWarehouseServed` берёт `current.boundTo` из уже изменённого состояния (`:862–865`). Сценарий с одинаковым ключом задан явно как тестовое условие, не как обнаруженные боевые данные. Сам документ WMS-457 допускает существование ошибочных чужих привязок (`docs/requirements/WMS-457.md:69–77`).

**Связь с правилами и проверка:** AGENTS.md §3 о границах клиентов и конкурентных изменениях; WMS-454 R5 о сохранении именно выбранной привязки. Для отдельного исправления проверить задержанный ответ после закрытия/повторного открытия, смены продавца и два ответа одной строки в обратном порядке. Проверять принадлежность и актуальность ответа; новые пользовательские подтверждения для этого не нужны.

## Что проверено и в порядке по требованиям

Указанные ниже положительные результаты относятся к коду и выполненным тестам. Они не отменяют F1/F2 и не являются визуальной приёмкой.

| Требование | Проверенный результат и доказательство |
|---|---|
| WMS-454 R1 | Признак Ozon берётся из `marketplaces` выбранных товаров; пустой список даёт WB. Фильтруются строки, число складов, сумма процентов и штук, группы и общие подписи. `stub.ts:221–229`; `FbsStockDialog.tsx:140–150,172–222,295–310`. Рендер WB-товара у продавцов с/без Ozon совпадает: `FbsStockDialog.ozon.test.tsx:58–85`, тесты прошли. Сохранность старой WB-доли не прошла — F1. |
| WMS-454 R2 | Черновик WB-товара получает `publishOzon=false`, `changedPublication=['ozon']`, поэтому `publish_ozon:false` присутствует в JSON, а нетронутый WB-флаг отсутствует. `stub.ts:246–252`; `FfProductsFbsPage.tsx:122–134`; тест `FbsStockDialog.ozon.test.tsx:105–120` прошёл. Каталог использует этот сборщик в `FfProductsCatalogScreen.tsx:2129`, второй маршрут — `FfProductsFbsPage.tsx:245–258`. Старое раздельное правило небезопасно — F1. Реальный PUT/GET с локальной БД не выполнялся. |
| WMS-454 R3 | Для объединённого товара и смешанной пачки `some(...includes('ozon'))` оставляет Ozon видимым; нетронутые флаги не отправляются. Расчёт общего потолка по включённым направлениям сохранён в `FbsStockDialog.tsx:184–204`. Тесты `FbsStockDialog.ozon.test.tsx:73–80,113–128` прошли. Серверные тесты общего потолка входят в успешный `test_fbs_stock_rule_service.py`. |
| WMS-454 R4 | Подпись называет приём заказов продавца, чип говорит только о заказах, сохранены причины недоступности галки и прежний чип сопоставления. `FbsStockDialog.tsx:441–464`; тест `FbsStockDialog.ozon.test.tsx:88–103` прошёл. |
| WMS-454 R5 | Штатный обработчик отправляет PUT привязки с `served`, текущим `wms_warehouse_id` и площадкой строки, не вызывает PUT правила. Состояние меняется после успешного ответа; при отказе остаётся прежним, текст попадает в `saveError`. `FfProductsCatalogScreen.tsx:788–868`; `FbsStockDialog.tsx:259,444,488,493,521`. Это чтение пути кода; HTTP 500, повторное открытие и сохранность введённого через интерактивный компонент в данном ревью не прогонялись. Конкурентный риск — F3. |
| WMS-454 R6 | В WB-окне Ozon-строки не рендерятся, следовательно их обработчик галки через это окно недоступен. `stub.ts:226–229`; `FbsStockDialog.tsx:214,432–444`; успешный тест отсутствия `fbs-stock-served-ozon:*` в `FbsStockDialog.ozon.test.tsx:58–66`. Проверка сохранённых `served` через GET БД не выполнялась. |
| WMS-454 R7 | Режим штук и обе карты значений сохраняются общим сборщиком (`FfProductsFbsPage.tsx:124–134`). Вторая страница сохраняет одиночный/массовый маршруты (`:243–260`) и передачу `marketplaces` (`:66–79,191–193`); данные складов там остаются WB (`:201–225`). Изменений таблицы каталога, фильтра площадки и значков в diff нет. По R7 имеется регресс F1; визуальная геометрия не проверялась. |
| WMS-457 R1 | Имена строк полученного кабинета не обрезаются и не переименовываются (`fbsSellerWarehouseRows.ts:68–80`), у них нет нового `nameIssue`. Проверки WB/Ozon прошли (`fbsSellerWarehouseRows.test.tsx:38–48,73–91`). Сервер WB сохраняет непустое имя, `served` учитывает `is_active` (`backend/app/services/fbs_seller_warehouse_service.py:168–176`). |
| WMS-457 R2 | Активная привязка вне полученного списка получает номер и `not_in_cabinet`; обработчики общие с обычной строкой. `fbsSellerWarehouseRows.ts:83–96,106–113`; `FbsStockDialog.tsx:441–489`. Тесты сборки и рендера прошли (`fbsSellerWarehouseRows.test.tsx:38–48,94–123`). |
| WMS-457 R3 | При полученном HTTP-ответе с ошибкой активные привязки показываются с `list_unavailable`; WB-причина хранится отдельно от ошибки записи и остаётся в состоянии при успешном изменении строки (`FfProductsCatalogScreen.tsx:698–700,779,819–835`; `FbsStockDialog.tsx:256–259,423–430`). Тесты `fbsSellerWarehouseRows.test.tsx:51–59,125–146` прошли. Сетевое отклонение самого `fetch` не покрыто — F2. |
| WMS-457 R4 | Код читается из `detail.code`; отсутствующий ключ, 401, 403 и прочие `wb_*` различаются (`fbsSellerWarehouseRows.ts:124–159`). Все пять конкретных конвертов и чтение Response проходят тесты (`fbsSellerWarehouseRows.test.tsx:150–183`). Полная ветка обрыва запроса — F2. |
| WMS-457 R5 | Неактивные привязки вне списка кабинета пропускаются (`fbsSellerWarehouseRows.ts:88`), кабинетная строка остаётся. Проверки `fbsSellerWarehouseRows.test.tsx:38–70` прошли. Снятое состояние кабинетной WB-строки обеспечивает серверное `binding.is_active and binding.served` (`fbs_seller_warehouse_service.py:173`). |
| WMS-457 R6 | Сохраняется порядок кабинетов и дополнительных привязок; новые чипы используют существующий `StatusChip`, старый обработчик PUT, дополнительных запросов/окон/кнопок в diff нет. `fbsSellerWarehouseRows.ts:68–97`; `FbsStockDialog.tsx:454–489`; `FfProductsCatalogScreen.tsx:659–674`. Второй маршрут не подключён к новому сборщику названий (`FfProductsFbsPage.tsx:201–225`). Переносы и отсутствие наложений в браузере не проверялись. |

Пути без полного префикса в таблице относятся к `frontend/src/screens/ff/products-fbs/`; `FfProductsCatalogScreen.tsx` — к `frontend/src/screens/v2/`.

## Соседние процессы, архитектура и качество тестов

Новая сборка JSON централизована в `fbsRuleBody`: прежняя одинаковая логика двух маршрутов заменена вызовом одной функции, поля штук не потеряны (`FfProductsFbsPage.tsx:113–135,245`; `FfProductsCatalogScreen.tsx:2129`). `nameIssue` — производный признак строки для требуемого чипа; он не сохраняется в БД и не вводит самостоятельного жизненного цикла (`fbsSellerWarehouseRows.ts:62–98`). Ни новых серверных сущностей, ни изменений упаковки, отгрузки или навигации WB FBS в проверенном diff нет (`git diff --stat 4474ac96..HEAD`; diff backend пуст).

Для B08 и B10 проверен неизменённый серверный путь: старый и новый флаги сравниваются по площадке; ноль вызывается только при переходе из включённого состояния в выключенное (`fbs_stock_rule_service.py:713–730,811–819`), повторное сохранение выключенного состояния повторного нуля здесь не вызывает. `_clear_previous_ozon_stock` отбирает активные связи Ozon и возвращается до получения транспорта, если отправлять нечего (`fbs_warehouse_binding_service.py:320–348`). Значит для WB-товара без связки принудительное `publish_ozon=false` само по себе не отправляет запрос Ozon по этому пути. Это подтверждение конкретной ветки кода, не прогон фоновых циклов внешнего кабинета.

`test_wms351_marketplace_publication_switches.py:42–81` действительно вызывает штатные `get_rule_view`, `set_rule_for_products`, `publish_amounts_for_binding`, а подменяет внешнее обнуление/планирование. Он проверяет независимость флагов, неизменность `served`, отсутствие повторного нуля при повторном сохранении и планирование только WB при обратном включении. Набор прошёл. Серверные блокировки продавца и `SELECT FOR UPDATE` остаются в `fbs_stock_rule_service.py:668–684`; полноценный параллельный тест PostgreSQL пропущен и успехом не считается.

Новые фронтовые тесты вызывают штатный `FbsStockDialog` через `renderToStaticMarkup`, подменяя только оболочку `AppDialog` для обхода портала (`FbsStockDialog.ozon.test.tsx:9–14,48–54`; `fbsSellerWarehouseRows.test.tsx:16–21,94–110`). Тест тела PUT использует настоящий `initialDraft` и `fbsRuleBody`; тесты строк и конвертов — настоящие экспортированные функции. Это полезные проверки, а не независимая копия реализации. Однако они не нажимают кнопки, не выполняют загрузчик/сетевые обработчики и не проверяют порядок ответов: именно за этой границей остались F1/F2/F3. Тест длинного имени (`FbsStockDialog.ozon.test.tsx:131–138`) проверяет наличие текста в HTML, но не переносы и наложения. Визуальный вывод по нему делать нельзя; это часть приёмки аналитика, а не требование повторить её ревьюеру.

## Выполненные команды и результаты

1. `pwd`, `git status --short`, `git branch --show-current`, `git rev-parse HEAD`: указанные выше каталог/ветка/SHA; до ревью рабочее дерево чистое. `git diff 4474ac96..HEAD -- backend frontend` прочитан вместе с файлами; backend не изменён.
2. В `frontend`: `npx tsc --noEmit -p tsconfig.app.json` — exit 0. Следовавший за ним штатный `npm run test:unit` не запустился из-за `EPERM` при записи конфигурации в общий `node_modules/.vite-temp` за пределами разрешённого каталога.
3. `npm run test:unit -- --configLoader runner` — тесты выполнены с тем же `vitest.config.ts`, без временной сборки конфигурации в общем `node_modules`. Итог: **61 файл прошёл, 1 упал; 428 тестов прошли, 2 упали**. Все пять файлов `products-fbs` прошли: `FbsStockDialog.ozon.test.tsx` — 8, `fbsSellerWarehouseRows.test.tsx` — 13, `FbsStockDialog.preview.test.tsx` — 4, `FbsStockDialog.test.ts` — 5, `fbsWarehouseRuleKeys.test.ts` — 2.
4. Оба падения — `frontend/src/screens/ff/FfInboundRequestView.test.ts:145,170`: несовпадение набора `data-testid` и 4128 строк при лимите 600. `git diff --exit-code 4474ac96..HEAD -- 'frontend/src/screens/ff/FfInboundRequest*' 'frontend/src/screens/ff/useFfInboundRequest*'` вернул 0 без вывода; тест и весь набор исходников, который эти две проверки читают (`FfInboundRequestView.test.ts:13–28`), в рассматриваемой ветке не менялись. Это не регресс WMS-454/457; весь набор при этом нельзя назвать зелёным.
5. В `backend`: `PYTHONDONTWRITEBYTECODE=1 /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_fbs_stock_rule_service.py tests/test_wms351_marketplace_publication_switches.py tests/test_wms351_publication_concurrency.py` — **70 passed, 2 skipped, 5 warnings in 9.08s**, exit 0. Оба пропуска — параметры `manual_sync`/`stale_legacy` теста конкурентности, которому нужен PostgreSQL (`test_wms351_publication_concurrency.py:25–33`). Ruff/mypy бэка не запускались: серверного diff в этом круге нет.
6. `npm run build -- --configLoader runner` остановился на записи двух `.tsbuildinfo` в общий `node_modules/.tmp` (`TS5033/EPERM`). После этого отдельно выполнены `npx tsc --noEmit -p tsconfig.node.json --tsBuildInfoFile /private/tmp/wms454-astra-node.tsbuildinfo` и `npx vite build --configLoader runner --outDir /private/tmp/wms454-astra-dist`: оба успешны, `built in 1.60s`; Vite сообщил предупреждение о фрагментах больше 500 kB. Конфигурация проекта не менялась. Это успешные отдельные проверки типов и сборки с разрешёнными путями вывода, а не успешный неизменённый `npm run build`.
7. Дополнительные локальные исполнения исходников для F1/F2/F3 и серверного `split_amounts` завершились с exit 0; наблюдаемые результаты приведены непосредственно в находках. Это синтетические воспроизведения, без локального PostgreSQL, настоящего браузера и внешних записей.

Для устранения FAIL нужны исправление сохранности WB-доли F1 и обработка отказа справочника F2 с тестами штатных путей. F3 передан отдельно как существующий риск. Требования, код продукта, тестовые файлы и статусы задач ревьюер не менял. По прямому поручению пользователя создан только этот файл ревью; коммитов не сделано.
