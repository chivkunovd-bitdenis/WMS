# WMS-277/394/395/396/401/403/404: независимая Astra xhigh

09.09.2026, агент release_review_verdict. Read-only review a738d82c (277),
34d07299 (UI), 6d7912ff/664bfdd7 (401), e57401e0 (396). Тесты самостоятельно
не повторял, реальные внешние операции не запускал.

Новых P1 не найдено. Два P2 в UI: Escape с фокусом на кнопке минует
input-handler и закрывает внешний Dialog; красный текст на error.light
плохо читается (просмотрен PNG). Минимальные поправки: внешний
escapeKeyDown сбрасывает активный скан и не закрывает диалог в запросе;
фон только ошибочной строки alpha(error.main,0.08). Root внёс эти правки,
tsc прошёл; Tab/фокус кнопки→Escape проверен root в настоящем Chrome.

По277 дополнительных замечаний в принятом periodic-only объёме нет:
HTTP вне DB-сессий, повторная проверка token/seller, ошибки по продавцам
изолированы, используются прежние сохранения, нет Ozon/stockpublication.
Строгая дедупликация simultaneous synchronous manual fetch и атомарная
отмена многошагового сохранения при late-disconnect не обеспечены; это
указанные ограничения, а не завершённые гарантии.

По401 обхода через order_id не найдено: выбор только среди подходящих
заказов/позиций поставки/товара; tenant/seller/source проверки сохранены.
Повтор key не создаёт новый подбор. Root проверил интегрированный файл
test_wms397_pick_scan_order.py: 15passed на отдельном файловом SQLite
за11.23s. Первый in-memory прогон дал14pass/1failOzon; отдельный Ozoncase
и полный целевой файл на файловомSQLite прошли. Причина различия окружений
отдельно не доказана; она не скрывается и не является основанием менять
рабочую логику. Временная тестовая БД не production/staging.

По396 повторно подтверждён request-lock→freshjob→events, который
закрывает stale-worker race. Process-local pacing остаётся открытым
ограничением; положительная live-приёмка не проверена. Отдельный Opus
review сохранён в wms396-true-api-opus-20260909.md.
