# WMS-352 / WMS-386 — отдельный серверный выпуск Ozon

PR [214](https://github.com/chivkunovd-bitdenis/WMS/pull/214) слит в etalon. Проверенный head: `f4ff4219f36d0c1315512c7b07c1d013660e05b3`; merge и фактический production SHA: **`eaa6ae9aecee875c50b31afa6b2cc224bb57d043`**.

Выпущены только ozon_fbs_sync_service.py и ozon_fbs_marking_gate_service.py. Обслуживаемый склад принимает заказы независимо от публикации остатков; требования маркировки сопоставляются с нужным SKU, неизвестный ответ не превращается в разрешение. Исполняемые файлы совпадают с независимо принятым срезом 1bd677f3. Исправления после первого CI меняли только тесты: явную загрузку состава и старое ожидание зависимости импорта от публикации.

CI [34458094624](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34458094624) прошёл полностью: backend 10m7s, frontend 1m2s, backlog 10s. Первый CI с двумя отказами сохранён в истории и не называется успешным. Координатор выполнил 33 первоначальных целевых проверки и пять проверок после исправления конкретных отказов; полный pytest локально не повторялся.

Перед выкаткой скрипт проверил чистый tracked checkout сервера, прежний SHA0771833b, принадлежность целевого SHA origin/etalon и точный перечень двух исполняемых файлов. Проверена неизменность frontend, миграций и deploy-конфигурации. Затем последовательно собраны и перезапущены только API, celery_worker и celery_beat. Скрипт завершился с кодом0. Миграции, web, сеть и старый seller-listener в этом выпуске не менялись.

После запуска root заново прочитал Git HEAD сервера и хеши обоих файлов внутри всех трёх контейнеров. Каждый совпал с Git eaa6ae9a. Время запуска контейнеров — 10.09.2026 09:11:20 UTC. Через публичный HTTPS со штатной проверкой сертификата получены200 для `/`, `/seller/`, `/api/health` и `/api/openapi.json`.

[Точные хеши, контейнеры и HTTP](artifacts/wms415-astra-takeover-20260910/resumed/ozon-backend-release-eaa6ae9a-verification.json). [Сохранённая команда выпуска](artifacts/wms415-astra-takeover-20260910/resumed/ozon-backend-release-eaa6ae9a.sh).

Это доказательство серверного выпуска, а не ручной приёмки реального Ozon-отправления. Клиентские складские операции и принудительный импорт ради теста не запускались. Общий кандидат415/416, чат, stock338 и сетевые изменения377 этим выпуском не применялись. Замороженные111/112 не входят в выпущенную дельту. APKv10 и его фактическое обновление через приложение приняты отдельно в опубликованном протоколе363/412.
