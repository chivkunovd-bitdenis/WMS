**P2 на frozen `10aeccb3c3d11654f3d75340e9fb8265b266838c`: новая проверка блокирует повторный deploy после ошибки backup. До исправления — не ACCEPT. P1 не обнаружил.**

В [verify-wms-host-network.py:65](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/verify-wms-host-network.py:65) API ищется через `docker ps -q`, который возвращает только работающие контейнеры. [Документация Docker](https://docs.docker.com/reference/cli/docker/container/ls/#show-both-running-and-stopped-containers--a---all).

Точный сценарий:

1. Первый deploy проходит предварительную проверку и останавливает API на [prod-update.sh:85](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/prod-update.sh:85).
2. Выгрузка или проверка backup завершается ошибкой. Скрипт намеренно оставляет API остановленным.
3. Причину ошибки устраняют и повторяют deploy.
4. Новая проверка на строке 69 получает пустой результат поиска API и завершает выполнение с `Expected one existing WMS api container`, хотя сеть не менялась. До восстановления сервисов выполнение не доходит.

**Самостоятельно воспроизвёл отказ**, исполнив frozen-verifier в памяти с подставными ответами Docker: корректные подсети, шлюзы и работающий web, но остановленный API → указанная ошибка.

Нужное исправление: учитывать существующий остановленный API и проверять его настроенное подключение к подтверждённой сети; отсутствие назначенного IP у остановленного контейнера должно обрабатываться отдельно. Простого добавления `--all` недостаточно, если последующие проверки требуют активного IP. Старый API ради прохождения проверки запускать не следует. Нужен целевой сценарий **«backup failed → API остался остановлен → повторный deploy при неизменной сети»**. В прочитанных тестах повторного запуска после ошибки нет.

Остальная часть исправления принимается в проверенном объёме:

- WMS-сеть выбирается по проекту контейнера БД и метке `default`; API/web — по тому же проекту и точной метке сервиса. Проверяются ID сети, шлюз, префикс и принадлежность IP подсети.
- Edge выбирается как единственный работающий контейнер с опубликованным `443`; неоднозначность отклоняется. Проверяются его подключение и параметры соответствующей сети.
- Значения доверенных сетей в frozen overlay/Caddy согласованы с проверяемой топологией. Замена IP контейнера внутри прежней подсети проходит; изменение подсети, отсутствие сети, отключённый API и ошибка Docker отклоняются.
- Вызов verifier стоит до сборки, остановки сервисов, backup и миграций. Ошибки не маскируются.

Самостоятельно исполнил девять сценариев verifier с подставными метаданными и `bash -n` frozen deploy-скрипта. Авторские **15 PASS не выдаю за собственный прогон**. Сохранённый candidate-preflight JSON прочитан: он содержит `verified: true`, WMS `172.21.0.0/16`, edge `172.18.0.0/16` и режим проверки с кандидатной конфигурацией. Это результат координатора; собственного повторного обращения к хосту не было.

Файлы и runtime не изменял. Принятие AUTH сохраняется; проверка полного HTTPS-пути и закрытия портов остаётся отдельной.