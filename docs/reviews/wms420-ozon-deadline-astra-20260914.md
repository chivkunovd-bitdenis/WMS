# WMS-420/WMS-422 — перекрёстное ревью и границы приёмки

14.09.2026. Reviewer: Astra, BA исходной постановки. Ветка codex/wms420-ozon-deadline-chip; реализация 66bc78f53bcbdb1df11e512b3361bc0c811767b4, постановка 67694a0bfae9a4a10f7d9b486eb78d42fa4a9526. Прочитаны актуальные AGENTS.md, owner-cases.md и failure-cases.md целиком, навыки аналитика/разработчика и требования WMS-420.

Вердикт кода: **PASS**. Подтверждённых дефектов P1/P2 нет. Diff продукта — одна строка в FbsChips.tsx: marketplace ozon и msLeft<=0 возвращает null. Возврат расположен после hooks и прежней ветки cancelled/null; подписка часов сохраняется. WB и будущий Ozon проходят прежний код. Новый UI, текст, запись данных, сеть и изменение прав отсутствуют. Применимые риски библиотеки: общий компонент проверен во всех местах (B04), соседняя площадка сохранена (B08), срок не стал запретом (owner-cases, плохой случай 2).

Три места вызова прочитаны в точном кандидате: FfFbsOrdersScreen строки 420 и 1736 передают order.marketplace, FfFbsSupplyWorkspace строка 1828 передаёт workspace.supply.marketplace. Табличные ячейки не удалены. Нейтральный текст даты workspace на строках 1825–1826 остался неизменным. Источником площадки служит объект, а не глобальный фильтр.

Свежая команда `npm run test:unit -- src/components/fbs/FbsChips.test.ts`: 4/4 PASS, старт 16:22:00, длительность 1.06s. Тесты рендера доказали отсутствие любой разметки для Ozon past/equal и сохранение future Ozon/past WB; два теста server time сохранились. SSR-тест не выдаётся за браузерный тик или полный экран.

Doc validator из основного checkout завершился FAIL: AGENTS.md/CLAUDE.md не совпадают в исходной ветке; их diff относительно ef0915f2 пуст. Сам скрипт отсутствует в ветке. Прямая функция document_errors на требованиях WMS-420 вернула []: заполненность документа PASS. Правила проекта не менялись ради обхода проверки.

BA: статическая/автоматическая часть подтверждена; полный C5 и операторские части C1–C3 не выполнены. Production при подготовке показал форму входа; просмотр живого заказа Виталика не заявлен. До production остаются выпускной CI и адресная проверка списка/workspace на кандидате, включая повторное открытие. Код разрешён к передаче на стенд; production в этом review не изменялся. Условия требований не смягчались.

Дополнительные свежие проверки кандидата: `npx tsc --noEmit -p tsconfig.app.json` и `npm run build` — PASS. Vite сообщил только предупреждение о размере части сборки; ошибок компиляции нет.
