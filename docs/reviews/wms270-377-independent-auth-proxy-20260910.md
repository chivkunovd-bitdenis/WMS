**На frozen `891281e77bfe7d9e5623931fccb3f9e97fff9833`: AUTH и ограничитель принимаю в проверенном объёме кода. P1 не обнаружил. Остаётся один P2 в защите от изменения сетевой конфигурации; WMS-377 по этой проверке закрывать нельзя.**

Оба указанных отчёта прочитаны. Проверены изменения `f2c75652` и `76c20cec` и конечные файлы через Git, без обращения к меняющемуся рабочему дереву.

**P2 — доверенные подсети зафиксированы, но перед применением overlay не проверяются.**

В [docker-compose.wms-host-8088.yml:8](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/docker-compose.wms-host-8088.yml:8) API доверяет `172.21.0.0/16`, а в [deploy/Caddyfile.http:4](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/deploy/Caddyfile.http:4) Caddy доверяет `172.18.0.0/16` и шлюзу `172.21.0.1`. При этом production Compose не закрепляет подсеть, а в проверенных deploy/workflow-файлах нет проверки соответствия фактической сети этим значениям.

Точный триггер: сеть WMS пересоздана с другой подсетью, например `172.22/16`, а overlay остался прежним. API перестаёт принимать переданный Caddy клиентский IP и ограничивает всех пользователей по адресу внутреннего web. **Собственное исполнение ASGI подтвердило:** при peer `172.22.0.6` шесть запросов с шестью разными клиентскими IP дали пять `401`, затем `429`; единственным ключом ограничителя стал `172.22.0.6`.

Это **не дефект обычного пересоздания контейнера внутри сохранённой сети**: новый адрес web в пределах `172.21/16` остаётся доверенным. Docker отдельно описывает смену адреса контейнера при сохранении сети и имени сервиса. [Документация Docker](https://docs.docker.com/compose/how-tos/networking/#updating-containers-on-the-network).

Исправление: обязательная проверка фактических subnet/gateway и принадлежности прокси доверенным сетям перед применением host overlay, с остановкой при несовпадении. Автоматически пересоздавать production-сеть для этого не требуется. Ранее проверенная координатором топология подтверждает исходные значения, но не заменяет такую проверку при последующих выкатках.

В остальном принимаю следующее:

- **Ответы и установка пароля.** [auth.py:258](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/api/auth.py:258) возвращает одинаковые `401 / invalid_credentials` для отказов login. В сервисе отсутствие пользователя и незаданный пароль проходят проверку фиктивного хеша. `/set-password` требует токен; [auth_service.py:369](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/auth_service.py:369) проверяет ссылку, блокирует строку пользователя и повторно сверяет отпечаток актуального пароля перед изменением. Это принятие кода, без собственного измерения времени ответов или запуска PostgreSQL-теста конкуренции.
- **Ограничитель.** [login_rate_limit.py:54](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/login_rate_limit.py:54) атомарно занимает попытку до сервиса login/reset. Число клиентов и записей ограничено, истёкшие записи освобождаются; успешный вход удаляет свою попытку, сохраняя остальные. Приложение использует только `request.client`, самостоятельно XFF не разбирает.
- **Передача IP при указанной топологии.** Внутренний Caddy разбирает доверенную цепочку справа налево и перезаписывает XFF вычисленным клиентским адресом. Это соответствует [семантике Caddy](https://caddyserver.com/docs/caddyfile/options#trusted_proxies_strict). Защита действует для внешних поддельных заголовков при корректной настройке внешнего Caddy. Узлы внутри явно доверенных подсетей сами входят в границу доверия.

Самостоятельно, **без БД, сети и файловых изменений**, исполнил frozen-ограничитель и извлечённые login/reset-обработчики с подставными сервисами. Проверены атомарный предел, освобождение собственной попытки, предел памяти и очистка; HTTP-пакет из 20 запросов допустил пять обращений к сервису и отклонил 15; reset допустил пять обращений и затем вернул `429`. На локальном Uvicorn `0.52.1` проверены разные клиентские квоты, поддельный префикс XFF и недоверенный непосредственный отправитель. Это не выполнение двух Caddy и не production-проверка.

**До закрытия WMS-377 остаются:**

1. Сверить фактические сети, адреса непосредственных прокси и активные настройки обоих Caddy/Uvicorn. Внешний Caddy в этом проходе самостоятельно не инспектировал.
2. После разрешённого deploy проверить весь HTTPS-путь двумя контролируемыми клиентами: отдельные квоты, сохранение настоящего IP и отсутствие обхода через одиночные, цепочные и повторяющиеся XFF-заголовки.
3. Проверить публичные `/`, `/seller/`, `/api/health`, недоступность `8088` и `15174` извне и фактическую остановку старого seller-listener.
4. Подтвердить SHA запущенной версии.

SSH, браузер, production-запросы и авторские тестовые наборы в этом проходе не запускались. Прежнее принятие исправлений deploy-скрипта сохраняется.