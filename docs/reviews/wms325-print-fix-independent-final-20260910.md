**SAFE — ACCEPT для ограниченного среза `a40c124d + cbe4cdf19d18a339028ac309a4f58dca52aad63d`.** Прежний P2 с порядком `PrintAsset → Order` закрыт. В исправлении новых P1/P2 не обнаружил. Код прочитан из фиксированного коммита, отчёт и свидетельства — из `b0700fcc4aedff1bdc66f62d6cab0e478251f06c`.

Подтверждены следующие условия:

- **Порядок блокировок совпадает с Ozon: `Order → PrintAsset`.** Загрузчик сначала блокирует заказ, затем актив; оба запроса ограничены организацией. Это устраняет прежний цикл с восстановлением Ozon, которое уже держит заказ перед обновлением этикетки. См. [загрузчик, строки 395–415](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/fbs_print_asset_service.py:395) и [обновление этикетки Ozon, строка 300](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/ozon_box_assembly_service.py:300).
- **Предварительная привязка к заказу перепроверяется под блокировкой.** Запрос актива повторно проверяет `asset_id`, `tenant_id` и `fbs_order_id`. При несовпадении выполняется откат точки сохранения перед новой попыткой: блокировки неудачной попытки не переносятся на следующий заказ. Для актива без заказа применяется условие `IS NULL`; чужой заказ не разрешает выдачу содержимого. См. [строки 379–421](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/fbs_print_asset_service.py:379).
- **Устаревшие объекты в памяти обновляются.** `populate_existing=True` установлен для заказа и актива. Проверки отмены и готовности, а также изменение `sticker_status` используют перечитанные значения. После инвалидирования этикетки Ozon старый готовый файл не выдаётся. См. [проверки перед чтением файла, строка 1078](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/fbs_print_asset_service.py:1078).
- **Параллельные первые открытия сериализованы, откат не оставляет событие.** При сохранении первого открытия второе видит установленный `print_opened_at` и не создаёт повторную запись. При откате первого событие создаёт второе открытие со своим автором. Прочитаны все четыре варианта теста: с заказом/без заказа × commit/rollback, включая заранее загруженные устаревшие объекты и проверку автора. См. [тест, строки 177–227](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/tests/test_wms325_print_lock_order.py:177).

PG-свидетельства прочитаны: [исходная функция — `40P01`](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/docs/reviews/artifacts/wms325-print-lock-order-20260910/baseline-deadlock.txt:7), [исправленный набор — 14 passed](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/docs/reviews/artifacts/wms325-print-lock-order-20260910/fixed-tests.txt:15). Сам тест действительно использует два разных соединения PostgreSQL, синхронизацию через `pg_blocking_pids` и настоящий путь восстановления Ozon. **Это сохранённые результаты исполнителя; я проверил код теста и вывод, но самостоятельно тесты не запускал.**

Границы приёмки: закрыт конкретный конфликт блокировок в ранее рассмотренном срезе аудита. Обычное ожидание занятой строки остаётся; отсутствие любых задержек или взаимных блокировок во всём WMS этим не доказано. Однократность события относится к успешной записи аудита: сохранённый режим fail-soft допускает отсутствие события при ошибке аудита, чтобы не отменять рабочую операцию. Новых сущностей, складских движений, начислений и навигационных условий исправление не добавляет.

Это **не закрытие всей WMS-325 и не разрешение на выпуск**. Правок, дочерних агентов, браузерных проверок и действий с окружением не выполнял.