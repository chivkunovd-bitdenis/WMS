# WMS-444: synthetic final-package WIP evidence

**Status:** paused before Android-device acceptance. This is not an acceptance of
WMS-444 and does not claim an installable, signed APK.

## Isolation

All observations below used a disposable local PostgreSQL database and a
synthetic tenant. The WMS API ran on `127.0.0.1:18084`; the local web client
ran on `127.0.0.1:5198`; local WB and Ozon emulators used loopback-only ports.
No WMS-433 process, production tenant, marketplace cabinet, or real order was
read or changed.

## Completed evidence

* The synthetic Ozon FBS smoke flow completed supply
  `0564e7fb-2022-488d-97e9-2dfef0ca3732` to `in_delivery`.
* Its only billable server facts were a single `fbs_order` ledger entry for
  3 items at 40.00 RUB (120.00 RUB) and one packing ledger entry for 3 items
  at 30.00 RUB (90.00 RUB). No duplicate billable fact was found.
* Browser reread at the isolated web URL showed the same summary: FBS 3 items
  and 120.00 RUB, packing 3 items and 90.00 RUB, total 210.00 RUB.
* Commit `bcc9663ae5d9a8a17fe852c5483600c267626105` corrects the discovered
  detail-only grouping defect: nonbillable `fbs_pick` remains visible under
  `Прочие услуги` but no longer increases the FBS section's document or item
  totals. The browser reread after that commit showed FBS as 1 document,
  3 items, 40.00 RUB, 120.00 RUB; the three picks were separate and disabled
  for invoicing.
* Passing checks completed before this pause:
  `npm run test:unit -- --run src/screens/ff/FfBillingSellerDetails.test.ts`,
  `npx tsc --noEmit -p tsconfig.app.json`, `npm run build`, targeted WMS-444
  pytest groups, and repository `ruff` plus `mypy`.
* The Android release APK was built successfully but is deliberately unsigned;
  it has not been installed or accepted on a device.

## Resume point

Wait for the shared emulator to be explicitly released. Then use the isolated
synthetic server only to install and exercise the APK flows, reread the web
state and ledger, append concrete Android evidence here, and make a separate
non-WIP acceptance commit only if every required scenario is actually proved.
