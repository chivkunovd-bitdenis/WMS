# WMS-375 — фактическая проверка исправлений

06.09.2026. Рабочая ветка `feat/ozon-merge-staging-20260905`.
Это протокол проверки, не отдельный бэклог и не заявление о готовности production.

## Первый объединённый прогон

15 файлов backend: Ozon transport/lane/delivery/sources/cleanup, FBS stock rules,
FBO availability, warehouse bindings, event publication, reports, storage,
WB delivery/gates/autopoll/stock sync. `pytest -n 4`: 263 passed, 9 failed,
24.39 секунды. Все девять падений разобраны:

- 3 сценария FBO: новый тест не передал обязательный `is_fbs=False`;
  после исправления 3 passed, 37 deselected.
- Ozon delivery/sources: неверное имя поля движения `delta` вместо
  `quantity_delta`; fixture нехватки оставлял quantity_packed=1 при quantity=0.
  Исходные данные исправлены, ожидаемый минус сохранён. Два файла: 10 passed.
- 2 сценария связей отчёта: запрос movements не задавал обязательную группу;
  добавлено `operation=FBS`, API не ослаблен. Весь файл reports_movements: 9 passed.

`ruff check .` прошёл; `mypy .`: 421 source files, без ошибок.
Frontend: FfReportsPage.test.tsx — 3 passed; `npm run build` прошёл.
Предупреждения сборки о размере chunks и тестовые Swig deprecations сохранены
как ограничения инструментов, не выданы за функциональные дефекты.

## PostgreSQL

Отдельная локальная БД `wms375_lock_20260906`, PostgreSQL 17.10, драйвер psycopg.
Первый запуск с asyncpg не стартовал: драйвер отсутствует, использован драйвер
из pyproject. Новый тест реального пути событийной публикации прошёл:
после двух внутренних commit конкурент не получает тот же seller/Ozon lock;
`ozon-delivery` доступен; после завершения stock lock снова доступен.

Две существующие проверки конкурентной сборки WB первоначально не смогли
создать схему: в ORM Product отсутствовал уникальный ключ (tenant_id,id),
который уже создан миграцией 0111 и используется FK billing_ledger_lines.
Наличие этого ключа на production подтверждено чтением pg_indexes.
Метаданные приведены в соответствие существующей миграции, новой миграции нет.
После исправления: 2 passed, 12 deselected — параллельное создание поставки
из одного заказа и конкурентное добавление заказа в поставку.

## Текущие границы

Сервер проверен чтением: `/opt/wms` HEAD
`ed72c8888a6e383f5101e0c1bd96d3793810e4fc`; api, worker, beat, web работают.
Исправления на сервер пока не выкладывались. Новые browser/printer/внешние
подтверждения публикации не получены. A07 редактор коллизий и WMS-369/371
дорабатываются отдельно; указанный выше build не проверяет их будущий diff.
A08 остаётся исследованием снимка, количества в production не менялись.


## Второй пакет и браузерная проверка 06.09

Первый пакет: `3470b969818d54fd7516cac7c12174af0c01554c`, PR #188.
CI run 33993898195 завершён успешно: backlog, backend, frontend-build.
Последующие проверки ниже относятся к ещё не выложенному второму пакету.

Локальный браузер: порт 15175, API 18080, отдельная PostgreSQL
`wms375_browser_20260906`, только синтетические данные, WB API — локальный
эмулятор 18099. Это не подтверждение боевого кабинета.

- WB: два заказа 500001/500002 в поставке
  `6e886ffb-17de-43b3-b21c-1f515c0e12b5` переданы кнопкой после предупреждений,
  без начала упаковки. Поставка/заказы in_delivery; два движения по -1,
  остаток товара 50 → 48. Вкладка «Короба» не возвращает на подбор.
- Найденная браузером проблема печати после такой передачи исправлена:
  панель и кнопки видны без packaging task. Открыты общая и одиночная формы.
  Поле количества можно очистить и ввести 3; preview общей формы — два QR
  и шесть ШК, одиночной — три ШК. Физическая кнопка печати не нажималась.
- Печать только ШК не выпускает ЧЗ. Повторная печать существующего ЧЗ после
  WB передачи без строки упаковки переиспользует код заказа; operator-КИЗ
  защищён, новый код автоматически не выделяется. Целевой файл: 6 passed.
- A07: сохранено через каталог WB=5 и Ozon=7 с одинаковым ID 501001;
  повторно открыто окно, SQL подтвердил две разные pool-строки. Резервный
  список показывает served=true и при stock_sync_enabled=false.
  Проверки совместимости: 7 passed, 39 deselected; до них основной адресный
  прогон правил/API — 46 passed. Неактивный режим не блокирует сохранение.
- Отчёты: загружено три селлера, CSV активен и нажат; детализация normal
  показывает приход 50, расход 2, остаток 48. API-журнал подтвердил CSV200 именно после клика; отдельное чтение того же
  экспорта вернуло 14 товарных строк, у normal приход50/расход2/остаток48.
  Локальный скачанный файл браузер отдельно не предоставил.
- A08: снимок до миграции восстановлен в отдельную локальную базу,
  аудит READ ONLY: все 206 результатов baseline-публикации совпали с 0252,
  общий итог 2423. Автокомпенсация не нужна по этому снимку; production-данные
  не исправлялись. Метод/агрегаты сохранены отдельно.
- WMS-371: два цифровых PDF по три непустые страницы; физический пропуск
  не установлен, задача открыта до проверки реального принтера.

После заморозки второго application diff: ruff check . прошёл, mypy . —
421 файлов без ошибок; tsc --noEmit и npm run build завершились с кодом 0.
Полный pytest локально повторно не запускался; после commit/push его выполнит CI.


## Обычная передача Ozon и повтор

Отдельный процесс API18081/Vite15176 и SQLite, синтетический seller и существующий
FakeMarketplaceTransport. Исходящий HTTP из этого процесса запрещён bootstrap;
application файлы не подменены на диске. Воспроизведение — local_ozon_browser.py,
полный журнал и данные — OZON_BROWSER_RESULT.json.

Начальное открытие «Подбор» выявило неполный fixture: wb_supply_id=None не проходит
существующий FbsSupplyOut. Настоящий create_supply_from_orders сохраняет PENDING-…
для Ozon; именно такой placeholder добавлен в синтетические данные. После этого
API чтения поставки200, код приложения не ослаблялся.

Root нажал QR первого короба: transport получил один ship с двумя упаковками
количеством2/1, вернул два posting, затем две этикетки. В окне печати «Готово1».
Открытие PDF кнопкой «Печать» отклонено browser security policy; обходов не было.
Просмотр самого PDF и физическая печать не подтверждены.

Root нажал «Передать в Ozon», увидел предупреждение «Не хватает1шт.; после
подтверждения остаток будет списан в минус» и подтвердил. Поставка in_delivery,
в UI доступен QR поставки. Журнал: один ship, один carriage/create, один
carriage/approve; set-postings отсутствует. Остатки2/0 →0/-1, два движения -2/-1.
Повтор API с исходным idempotency_key и подтверждённой версией вернул200;
внешних вызовов, движений и изменений остатков не добавил. Новый ключ после
законченной передачи отвергнут409; повторного списания нет.

## CI второго пакета

cf42d6d51deba75100fcad3e79d4396fe2fc9e69, run33995059889: фронт/backlog успешны;
backend1900passed,13skipped,1xfailed,1failed. Единственное падение — старый
неперсистентный fixture test_fbs_stock_models не задавал served, из-за чего
новое обязательное boolean-поле получалоNone. В модели/БД served NOT NULL,
сериализатор не ослаблен. Fixture теперь явно задаёт served/sync и проверяет
независимость этих двух полей. Приложение по сравнению с cf42d6d5 не изменено.
