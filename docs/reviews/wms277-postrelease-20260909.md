# WMS-277 — завершённый штатный проход после выпуска, 09.09.2026

На production после выпуска `743a794bf0ee9e20cccf7f87c05138b035604ec9`
штатное часовое задание завершилось: **23 селлера успешно, 5 отказов WB401,
0 пропусков**. Оба селлера, у которых прежний проход падал на уникальности SKU,
на этот раз имеют успешное завершение. Все пять оставшихся отказов относятся
к тем же селлерам и коду `wb_upstream_error_401`, что в предыдущем финальном
отчёте; ошибок импорта другого типа в итогах этого прохода нет.

Деплой подтверждён координатором в 04:10:24 UTC: API/worker/beat — совпадение
428 Python-файлов, точный web image, миграция 0257, restarts=0, health=200.
Это источник привязки к SHA; наблюдатель сам не повторял проверку всех файлов.
Read-only наблюдение начато после этого разрешения на `sellerfocus.pro`,
в существующем `/opt/wms`, контейнер `wms_prod-celery_worker-1`.

Задание `faf2efe5-ca95-478d-abc2-55c9d0de8afc` получено worker в
**04:17:00.004 UTC / 07:17:00 МСК**. Итог `all sellers done` записан в
04:27:27.786 UTC, а `succeeded` для того же ID — в 04:27:27.792 UTC.
Длительность из журнала — **627.7813 секунды, 10 минут 27.8 секунды**.
Наблюдение завершено после обеих финальных записей, а не после первых
успешных селлеров. Запускался штатный beat; ручного импорта не было.

## Два прежних конфликта SKU

| Селлер | Журнал текущего прохода | Ограниченная проверка SQL |
| --- | --- | --- |
| `bf8eea6b-eaa6-47ea-8dfc-289142372dab` | `ok` в 04:26:02.602 UTC; cards=1657, created=245, updated=9791, legacy_old=0 | Product 9372 → 9617; после 04:17 создано 245; обновлены все 1657 сохранённых карточек. |
| `06b2e991-0533-4adf-83f3-d6cf21251433` | `ok` в 04:27:24.589 UTC; cards=68, created=0, updated=497, legacy_old=0 | Product 279 → 279; обновлены все 68 карточек; отпечаток Product ID/SKU полностью совпал. |

Счётчик `updated` взят буквально из результата импортёра: это обработки
вариантов карточек, а не число уникальных строк Product. Поэтому 497 обновлений
не означают 497 разных товаров при 279 Product.

Исходная SQL-выборка выполнена в **04:12:35.897 UTC**, конечная — в
**04:28:39.134 UTC**. Обе с `transaction_read_only=on`; в обеих ноль
блокируемых чужих сессий и ноль чужих транзакций старше 30 секунд. Это два
снимка, не непрерывный мониторинг блокировок. Количества по времени сами по
себе не дают исключительную атрибуцию записи конкретному заданию.

**Граница сохранности:** у большого каталога `bf8…` изменился не только общий
отпечаток с учётом новых 245 Product, но и отпечаток набора Product ID/SKU,
созданного до 04:17. Предварительная выборка сохранила агрегатный отпечаток,
а не исходную строку каждого товара. Поэтому она не устанавливает, какие
конкретные прежние SKU изменились, и не доказывает сохранность каждого из двух
исторически конфликтных вариантов. Неизменность всех старых SKU этого селлера
не заявляется. Подтверждённый здесь результат исправления — прежнее падение
целого импорта не повторилось, проход этого селлера завершился, новые карточки
и продукты сохранены. Прямых корректировок идентичности не выполнялось.

Пять WB401: `d18e384f-6faa-4ff3-961f-9d3a5f308f17`,
`2eadaf08-c9b7-48f8-b8e7-efe633483cc1`,
`4e67e471-5cc4-4732-bc0b-f8f1143b5271`,
`0746134b-f252-4543-91b9-fd872d008322`,
`0d9c9932-c32e-47f9-af25-8719746e2e07`.
Их авторизация этим исправлением не восстановлена; ключи не читались,
не выпускались и не менялись.

## Соседние фоновые операции FBS

В том же временном интервале журнал содержит успешные завершения:

- `wms.fbs_orders_autopoll`, ID `6a8889d0-4d6a-462a-8c06-5d12c0443db2`,
  04:18:31.272 UTC, **6.4997 с**;
- `wms.fbs_stock_reconcile`, ID `ef4e13b5-142b-4528-8682-eec915cc8907`,
  04:19:29.954 UTC, **5.1748 с**.

Это доказывает, что указанные FBS-задачи завершались во время импорта каталога,
а не полную браузерную приёмку всех FBS-процессов. Дополнительные завершения
сохранены в безопасном JSON без payload.

Источники: [фильтрованные события и обе SQL-выборки](wms277-postrelease-proof-20260909.json),
[точный SELECT](wms277-postrelease-20260909.sql),
[предыдущий финальный проход](wms277-production-final-log-proof.json).
Журналы фильтровались на сервере до передачи: сохранены только тип события,
ID задания/селлера, времена, числовые итоги и код ошибки. Traceback, SQL-параметры,
содержимое карточек и credentials в доказательства не включены. Для PostgreSQL
использовано окружение уже работающего DB-контейнера без вывода его значений,
`default_transaction_read_only=on` и `statement_timeout=8000`.

Приложение, runtime, расписание, учётные данные и складские документы наблюдатель
не менял. Работающий импорт не останавливался. Дополнительных ручных вызовов WB,
тестовых импортов, браузера, тестов и установки зависимостей в этом наблюдении нет.
