# WMS-111/112 — сохранение работы и исключение из выпуска

Владелец оспорил происхождение этих задач. Первичный источник разрешения не подтверждён; WMS231 относится к другой существующей процедуре и не заменяет его. Новых изменений функциональности111/112 не выполнялось.

Независимый read-only reviewer исследовал abb2beba и подтвердил отсутствие новых зависимостей Ozon/stock/merge/chat от спорного среза. Полный анализ: wms415-astra-independent-integrated-20260910.md. После чтения diff координатор применил обратные патчи только backend/frontend коммитов64d312e7,b26d92f0,55d0c7aa,8157be7e. Коммит330e0c68 содержит отчёт, его текст сохранён с явным историческим статусом. Общие файлы не возвращались целиком к старым версиям; патчи применились без конфликтов. Моделей/миграций срез не добавлял.

Исключены из исполняемого кандидата helper автоматического возврата, его вызовы и retry sweep, новые поля классификации/времени/ссылки возврата и соответствующий UI. Удалены из кандидата только относящиеся к исключённому helper новые тесты. Прежний реестр, обычная отмена, _lock_order, _release_reservation, reversal billing/shipment сохранены. Shipment/tracking вызывают согласованную старую сигнатуру. Поиск новых символов в backend/app и двух frontend файлах не дал совпадений. Исправления merge NOWAIT, stock cleanup, Ozon unknown requirements и чат не затронуты.

Сохранность: remote archive/wms415-disputed-candidate-20260910 фактически указывает40a3216b226772f8b9362c94f65ee20ab05a635a. Исходный wms111-wb-cancel-return с46fb5c25 и dirty отчётом не менялся. Ни reset, ни переписывания истории, ни миграции/изменения клиентских данных не было. История кандидата также сохраняет исходные коммиты и новый обратный diff.

Целевые проверки после исключения: test_fbs_cancellations, test_fbs_cancelled_after_pack, test_fbs_cancelled_operations, test_wb_marketplace_orders_service, test_ozon_cancel_posting, test_product_merge_service_wms349 —36passed,3PGskip (59.81s). Ruff изменённых backend-файлов PASS, frontend tsc PASS. Предыдущие PG merge replay сохраняются, заново в этом прогоне не выполнялись. Это не подтверждает реальный WB/Ozon вызов, браузер или выпуск. Новый кандидат по-прежнему проходит исправление остальных findings и независимую проверку итогового SHA до deploy.
