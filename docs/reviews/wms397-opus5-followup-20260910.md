# WMS-397 / WMS-399 — продолжение после Opus5, 10.09.2026

Это task-specific отчёт координатору WMS-415/WMS-417, не новый бэклог и не приёмка.
Канон и coordinator handoff не изменены. Независимое ревью и проверка интеграционного
SHA обязательны; merge/deploy не выполнялись. WMS-111/112 остаются STOP по прямому
решению владельца и handoff-коммитам abb2beba/40a3216b. WMS-418 не затрагивается.

Рабочее дерево: `/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-chat-mvp`.
Ветка `feat/wms397-chat-mvp`, проверенный сохранённый старт
`1ce34a9b8ef851d8b1f57f5c4b853f50d6fb86bb`. Работа продолжена поверх этого HEAD,
без сброса/чужого checkout. Новых агентов не запускалось.

Прочитаны AGENTS, текущий coordinator takeover целиком, исходный полный Claude
handoff в сохранённом thread, точные карточки WMS-397/399, полный Opus5
`artifacts/wms417-opus5-six-20260910/chat-review.md` и текущая coordinator triage.
Последнее распределение владения имеет приоритет: App.tsx, FfSuppliesShipmentsPage,
FfInboundRequestView принадлежат coordinator177/179 и не менялись. Его
`06b471c74a132fef6229dd2d1228289b90de4a83` не перезаписывался и не переносился в
эту ветку. Поэтому локальные клики не доказывают интеграцию новых общих URL.

## Точная сверка замечаний Opus5

| № | Статус этого среза | Основание и граница |
| --- | --- | --- |
| 1 | OBSOLETE | В сохранённом HEAD обе сортировки main/extra уже kind.desc(). Повторная реализация не делалась. |
| 2 | OBSOLETE | В сохранённом ChatAttachmentView обычные файлы ждут клика, картинки загружаются отдельно. HTML/SVG blob остаётся application/octet-stream. Этот компонент не менялся. |
| 3 | RESOLVED локально | На poll один пакет текущих карточек по видам документов, один запрос авторов, один запрос селлеров; состав документов не загружается для карточек. Для смешанных документов принадлежность проверяется отдельным пакетным запросом по существующим строкам. ACL перечитывается на каждый запрос; исчезнувшая/запрещённая карточка скрывается, возврата к сохранённому снимку нет. Тест сравнивает 1 и 200 разных заказов/авторов: рост не более одного SELECT, всего меньше 15. Удаление одного fixture-документа скрывает ровно его карточку при следующем чтении. |
| 4 | RESOLVED лимит, PENDING восстановление/разбор остатков | До записи файла вычисляется SUM(size_bytes)/COUNT существующих message_id IS NULL вложений пользователя во всех чатах tenant: до 100 MiB и 10 файлов. PostgreSQL сериализует загрузки блокировкой существующей строки User; новых counters/tables нет. Отправка освобождает бюджет. Сохранённые черновые файлы автоматически не удаляются. Закрытая вкладка может оставить файлы, занимающие бюджет, без UI восстановления; очистка/восстановление требуют определённого владельцем контракта. |
| 5 | RESOLVED локально | Вместо ensure-main на каждого селлера один запрос отсутствующих main и INSERT ON CONFLICT DO NOTHING пакетами по 100. Тест на 150 селлерах: меньше 15 SQL, повторный GET без INSERT с теми же UUID. GET по-прежнему лениво создаёт отсутствующий основной чат — это сохраняет текущий продуктовый контракт. |
| 6 | RESOLVED локально | Raw Content-Type >128, управляющие/не-ASCII символы отклоняются 422 до хранилища. Тест 300 символов проверяет, что put_bytes не вызывался. ORM flush перенесён перед put_bytes, чтобы нарушение SQL-ограничений не записывало объект. SQL commit и object storage не образуют общую транзакцию; аварийные остатки после storage/commit отдельно не убираются. |
| 7 | RESOLVED локально | Проверка seller-ограничения в main использует роль. Тест назначает FF-администратору home seller и открывает другого продавца своего tenant без ложного 403. Межселлерный доступ seller-аккаунтов остаётся запрещён. |
| 8 | OBSOLETE | Сохранённый сервис уже задаёт datetime.now(UTC) при создании сообщения. Сценарий порядка/пагинации проходит на PostgreSQL и SQLite. |
| 9 | RESOLVED по коду, ручной forbidden PENDING | Исчезновение при любой ошибке уже заменено retry в сохранённом HEAD. Теперь именно 403 показывает disabled «Написать сообщение» с подсказкой о недостающем праве; остальные ошибки сохраняют retry. Права не расширены. Физическое наведение на forbidden-tooltip под ограниченным аккаунтом не проверено. |
| 10 | PARTIAL, не закрывать целиком | Клик продавцом всех пяти карточек открывает точный UUID и текущий состав с серверными правами. Добавлен минимальный переход seller-приёмки в существующий /seller/inbound/:requestId. Для FF сохранены существующие рабочие переходы четырёх видов. Но seller FBS/order/outbound/MP и отдельный FF FBS-order всё ещё открывают chat-проекцию без отдельного рабочего маршрута; настоящий рабочий переход всех пяти видов не доказан и не объявляется завершённым. Отдельных страниц или редизайна не добавлено. |
| 11 | RESOLVED ACL, PENDING текст модели | Администратор вне extra-чата, как и staff, не читает/не добавляет себя/не получает вложения. После явного включения читает; после отзыва теряет доступ. Оба варианта проверены тестом. Неявное право администратора не добавлено. Добавление участника в main отклоняется 422 main_participants_implicit. Service docstring уже соответствовал этому в сохранённом HEAD. Неверный комментарий ChatParticipant про main в models/chat.py оставлен координатору: модели вне текущего владения. Автоматический захват осиротевшей группы не реализован. |
| 12 | RESOLVED исполняемый мусор, PENDING schema | Удалены неиспользуемые create_attachment, _known_chat_kinds, delete_object, ChatStorageUnavailable, искусственное подавление unused imports и игнорируемый multipart is_image/client argument. OpenAPI тест подтверждает отсутствие поля. ExtraChatCreateDialog comment уже исправлен в стартовом HEAD. Неиспользуемая deleted_at в модели/миграции не удалялась: schema вне текущего владения, миграционный контракт не расширялся. |

Подпись автора пока использует существующий email аккаунта; это текущее поведение,
а не новая политика раскрытия. Отдельный профиль/реестр имён не создавался.
Область модели/политики отображаемого имени остаётся координатору.

Отсутствовавшие в review packet migration/router действительно есть в Git ветки:
`backend/alembic/versions/20260910_0300_chat_messenger.py`, include_router(chat_router)
в `backend/app/main.py`, импорты моделей, маршруты App/SellerApp. Их отсутствие в
пакете ревью не равно отсутствию в реализации. Полную миграционную цепочку на стенде
в этом продолжении не выполняли; прежняя изолированная проверка описана в
`wms397-astra-20260910.md` и не выдается за новый live-прогон.

## Проверки текущего diff

- PostgreSQL, отдельная `wms397_chat_astra_test_20260910`: `pytest -q --tb=short tests/test_chat_api.py` — 21 passed, 68.96 s. Включены конкурентные main/message/attachment/member, revoked participant, tenant/seller/document ACL, смешанные документы, HTML/SVG/download headers, PNG, пагинация и новые бюджет/SQL-count проверки. Оба активных формата остаются обычными файлами даже с переданным старым is_image=true; проверены attachment, nosniff, sandbox, no-store и неизменные байты.
- SQLite, изолированная test fixture: тот же файл — 20 passed, 1 skipped, 60.10 s. Пропуск только проверки PostgreSQL row-lock на одновременные загрузки.
- Последние полные прогоны только этого scoped файла выполнены после всех backend/test-изменений, включая flush перед storage. В обоих прогонах 5 предупреждений существующего PyMuPDF/SWIG о __module__; ошибок нет.
- Ruff пяти затронутых backend-файлов — PASS; mypy четырёх API/service-файлов — PASS.
- `npx eslint src/components/chat src/screens/chat` — PASS; `npx tsc --noEmit -p tsconfig.app.json` — PASS.
- Последний `RAYON_NUM_THREADS=2 npm run build` после seller-inbound перехода — PASS, Vite 3.75 s. Существующее предупреждение chunk >500 kB остаётся.
- `git diff --check` — PASS. Full pytest/xdist не запускались; одновременно не более двух однопроцессных целевых pytest. Мобильные журналы/секреты/клиентские складские и финансовые данные не использовались.

## Ручной локальный браузер

Chrome browser extension CUA в этом продолжении доступен. Старый Mac-lock из
предыдущего отчёта сейчас не является блокером. Native AX сначала показал только
заголовок Chrome Web Store, затем использованы существующие локальные WMS-вкладки
через браузерное расширение; кабинеты секретов и посторонние страницы не открывались.

API 127.0.0.1:8397 и Vite 127.0.0.1:5397 используют только существующую
`wms397_chat_astra_ui_20260910`. API перезапущен с итоговым backend; fixture сохранена.
Рабочие операции документов не выполнялись. Единственная новая пользовательская
запись в UI — учебное сообщение склада с FBS-поставкой, видимое учебному продавцу.

Наблюдалось непосредственно кликами/AX и снимком страницы:

- warehouse-chat397@example.com и seller-chat397@example.com читают сохранённые main, текст после правки, картинку и файловую кнопку после перезапуска API.
- Продавец открыл «Учебная группа WMS397» и прочитал сохранённое сообщение «Повтор после сбоя WMS397 — один экземпляр». Этим закрыт прошлый конкретный пробел чтения группы вторым аккаунтом.
- Склад раскрыл компактную форму на учебной FBS-поставке, отправил текст. UI показал «Сообщение отправлено», карточка появилась у продавца.
- Продавец нажал карточки fbs_order, fbs_supply, inbound_intake, marketplace_unload, outbound_shipment в ленте. Все открыли точные fixture UUID и свои данные. FBS-order — WB3970001 / CHAT397-TSHIRT / 1; supply — заказ WB3970001 / 1; приёмка и обе отгрузки — учебная футболка / 2.

- Новый «Открыть рабочий документ» у seller-приёмки открыл `/seller/inbound/f62c7525-a5eb-414e-a64e-a75a3089a436`, заявку №000397 и существующую строку «Учебная футболка» / 2. Сохранение/передача не нажимались.
- Из seller-списка документов открыт MP-CHAT397; кнопка чата в его настоящем модальном документе раскрыла компактную форму. Переход «Перейти в полный чат» вернул в ленту без оставшегося модального документа. Это закрывает прошлый пробел по наличию и открытию seller MP-точки входа.
- Read-only SQL той же fixture подтвердил ровно по одной карточке каждого из пяти видов и count=1 нового текста FBS-поставки.

Идентификаторы совпадают с предыдущим `wms397-astra-20260910.md`; новых клиентских
сущностей не создавалось. Физический Ctrl+V Windows не проверялся; прежний Cmd+V
на Mac и retry описаны предыдущим отчётом, повторная реализация не делалась.

## Передача координатору и ограничения выпуска

Не закрывать WMS-397/399 как принятые. Требуются независимое review итогового commit,
проверка интеграции с coordinator 06b471c7/последующим HEAD, рабочие document routes
по пункту 10, ручной restricted-account forbidden/ACL проход, серверная миграция и
сохранность attachment storage после рестарта разрешённого стенда. Удалённый S3,
CI, staging/production и deploy в этом продолжении не проверялись.

При восстановлении/удалении брошенных drafts использовать отдельное явное решение
владельца. Не добавлять уборку автоматически и не считать текущий бюджет полным
решением жизненного цикла. Он ограничивает сохранённые черновые вложения, но не
объекты, оставшиеся при аварии между файловой записью и SQL commit.

Точные изменённые файлы этого продолжения (все остальные сохраняются):

- backend/app/api/chat_routes.py
- backend/app/services/chat_attachment_storage.py
- backend/app/services/chat_document_service.py
- backend/app/services/chat_service.py
- backend/tests/test_chat_api.py
- frontend/src/components/chat/ChatComposer.tsx
- frontend/src/components/chat/ChatDocumentAction.tsx
- frontend/src/components/chat/chatApi.ts
- frontend/src/screens/chat/ChatDocumentScreen.tsx
- docs/reviews/wms397-opus5-followup-20260910.md

Commit с этим отчётом является передаваемым срезом; проверенный полный SHA и
remote SHA передаются в финальном сообщении после push именно этой ветки.
