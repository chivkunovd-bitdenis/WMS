**ACCEPT — frozen `f0bd66a27a89bd432185ba2e4c752d25c353eba4`. Предыдущий P2 закрыт в проверенном объёме кода; новых P1/P2 в этом delta не обнаружил.**

В [verify-wms-host-network.py:65](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/verify-wms-host-network.py:65) поиск теперь включает остановленные API/web. Для них **сначала обязательно проверяется сохранённый `NetworkID`**, затем — соответствие настроенного `NetworkMode` имени или ID подтверждённой сети. Отсутствие активного IP допускается только в ветке остановленного контейнера. Отсоединённый контейнер, чужой ID сети, неправильный режим сети и неопределённое состояние не проходят.

Для работающих сервисов сохранены проверки шлюза, префикса и принадлежности IP подсети. В [prod-update.sh:68](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/prod-update.sh:68) поиск БД также включает остановленный контейнер. Проверка топологии остаётся перед сборкой; запуска старого API ради её прохождения нет. Ошибка backup по-прежнему завершает deploy с остановленными обработчиками, а при повторном запуске API стартует только после успешных backup, проверки архива и миграций.

Полностью прочитал исправление, оба целевых тестовых файла и отчёт. Новый сценарий в [test_prod_deploy_backup.py:111](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/tests/test_prod_deploy_backup.py:111) действительно выполняет два запуска скрипта, сохраняя между ними состояние подставного Docker: первый backup падает, второй запуск видит остановленный API с пустым IP.

**Самостоятельно исполнил десять сценариев frozen-verifier в памяти.** Корректные работающие и остановленные сервисы приняты; неправильные `NetworkMode`/`NetworkID`, отсутствие подключения, неопределённое состояние и недопустимый IP работающего API отклонены. Дополнительно проверил порядок команд в frozen-скрипте и выполнил `bash -n`.

Полный двухпроходный shell-тест и авторские **18 PASS/Ruff** самостоятельно не запускал. Реальный Docker, остановку/восстановление production и HTTP-путь не проверял; файловых и runtime-изменений не было. Прежнее принятие AUTH сохраняется.