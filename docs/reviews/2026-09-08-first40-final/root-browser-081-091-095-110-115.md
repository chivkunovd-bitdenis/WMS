# Ручная проверка root 08.09.2026

Проверка выполнена в in-app browser на изолированных локальных PostgreSQL стендах. Backend/frontend взяты из общей ветки: production-код `0a672b40`, полный проверенный CI SHA `5c371f39ccd836be96f85d4ffb68b8e52d5864cf` (последующие изменения до него — документы и тесты). Внешние соединения стендов запрещены, вместо WB использован синтетический ответчик. Это не браузерная приёмка Railway и не проверка production.

## WMS-115 / WMS-110

UI `15615`, API `18615`, база `wms115110_browser_20260908`. Root открыл «Отменённые» → «К вскрытию · Wildberries»: две строки 115001/115002 с товаром, SKU 6020-4R/31, коробом №7 и WMS115-BOX-7. Поиск 115001 оставил одну строку; кнопка номера поставки открыла её рабочее окно.

В упаковке показан только активный заказ 115003. Нажаты «Всё упаковано» и печать всей активной части; предпросмотр содержал один заказ с QR и ШК. Отменённые в эту печать не вошли. Смешанная пачка со сменой статуса непосредственно во время печати этим браузерным сценарием не проверена.

После перехода на «Короба» нажатие передачи показало оба отменённых заказа с артикулом и коробом и выбор «Вернуться и поправить» / «Передать без этих заказов». Возврат действительно закрыл диалог; внешний счётчик deliver остался 0, движений склада 0. Повторное подтверждение передачи без отменённых завершилось появлением QR поставки. Отменённые исключены из коробных связей, осталась одна активная; счётчик deliver ровно 1, PUT маркировки 0.

SQL: товар отменённых остался 3/3/0 (всего/неупаковано/упаковано). Активный товар после упаковки и печати оставался 2/2/0, после передачи стал 1/1/0. Создано ровно одно движение `fbs_shipment` на -1 только активного товара. Резерв активного удалён при передаче. Два резерва отменённых были освобождены подготовительным preflight до упаковки; его отдельный baseline сохранён, этот эффект не приписывается упаковке.

Снимки: `.worktrees/backlog40-review-ship-tests/tmp/qa115110/sql-after-preflight.json`, `sql-after-pack.json`, `sql-after-return.json`, `sql-after-deliver.json`. На экране до передачи верхний счётчик коробов показывал «3 из 1», внутри короба только активную штуку; после передачи стало «1 из 1». Это наблюдение не скрыто и не объявлено исправленным.

## WMS-091

UI `15191`, API `18091`, база `wms091095_browser_20260908`. Фильтр «Печать» показал одну строку: Defect Product, SKU-DEF-19272d06, место «Упаковка/FBS-печать», документ УПАК-26-09-08-1, сотрудник, дата/время и полный код. Фильтр «Расхождение с WB» показал отдельную внешнюю запись с теми же измерениями, без выдуманного пула.

Кнопка «Экспорт» нажата; инструмент не получил событие download за 10 секунд, ошибки на экране и в console не появились. Сохранение файла на компьютер этим кликом не подтверждено. Предыдущие API/CSV проверки не заменяют эту границу.

В задании 99108f2c-1dca-43a9-bf9e-616d7325795a root прошёл «Дополнительные действия» → «Повтор» → «Перепечатать». Выбран прежний код …BBBBBBBB0001. SQL после клика: `inventory_balances`, `inventory_movements`, все четыре таблицы резервов и `marking_codes` полностью совпадают с baseline. Добавлено только событие reprinted прежнего code_id 308f99cd-eaf3-4b39-a9e9-2833874a3547 с сотрудником и номером документа. Снимки и readonly-проверка: `.worktrees/backlog40-review-ship-tests/tmp/qa091095/after_browser.py`.

## WMS-095

В «Честный знак» → «Загрузить КМ» root выбрал синтетический `wms095-text-only.pdf`, не содержащий читаемого DataMatrix. После разбора экран показал: «В файле не удалось распознать коды маркировки. Загрузите PDF с читаемыми Data Matrix или CSV/TXT с полными кодами». Кнопка загрузки осталась недоступна. HTTP-код или английская внутренняя ошибка вместо объяснения не показаны.

## WMS-081

UI `15681`, API `18681`, база `wms081_defect_browser_20260908`. Root открыл задание ed0c3ed8-a7ba-4e01-a457-594b55b9d5ea → «Дополнительные действия» → «Брак». В списке был ранее напечатанный и уже нанесённый код a0e56653-df14-4752-a309-7dc83e04ad38. Введена причина «Порвана этикетка QA081», нажат «Подтвердить брак», диалог закрылся.

SQL: этот код стал defective; создана одна pending-заявка на повторную печать с причиной и сотрудником. Изменились только marking_codes, marking_code_events, marking_reprint_requests. Обе строки остатков и три существовавших движения приёмки/перемещения, все резервы и задание упаковки остались прежними. Физический брак товара этим сценарием не выполнялся. Снимки: `.worktrees/backlog40-review-ship-tests/tmp/qa081defect/sql-before.json` и `sql-after-defect.json`.
