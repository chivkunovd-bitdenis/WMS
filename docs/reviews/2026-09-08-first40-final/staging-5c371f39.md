# Выкладка первых 40 на staging — 08.09.2026

Выпущен код `5c371f39ccd836be96f85d4ffb68b8e52d5864cf`. Полный CI GitHub run34215423427 успешен: 2076 passed,36 skipped,1 xfailed; frontend-build, Ruff, mypy и проверка канонического номера успешны. Ветка origin/staging обновлена обычным fast-forward после окончания CI. Production, main и etalon этой пачкой не менялись.

Railway backend deployment ebfa35d1-e2c4-41bb-bd57-93ac8ed37f5a и web52af7300-4807-441b-905e-138929d97862: SUCCESS/RUNNING, branch staging, точный SHA5c371f39. Старых активных deployment в этих сервисах не осталось. Публичные `/`, `/api/health` дали200, SPA shell присутствует. Это доказывает выпуск, но само по себе не доказывает все операторские сценарии.

Root перезагрузил реальный staging в in-app browser, вошёл в уже существующую сессию сотрудника denis.tsd.staging@example.com, открыл FBS → «В работе» → тестовую поставку продавца «Эмулятор WB» WB-GI-245619454EE7. В списке появился новый поиск по поставке, рабочее окно загрузилось. FBS API фактически направлен на wb-emulator.railway.internal:8000; адрес подтверждён чтением единственного разрешённого настройки URL через SSH в работающем API, без вывода ключей и environment.

На staging выполнено «Всё упаковано» для двух синтетических заказов500007/500038. Экран показал «Упаковка завершена», упаковано2из2. Root переключился на «Короба»: вкладка открылась, возможность передачи доступна при нулевом количестве коробов и ненапечатанных стикерах. Саму передачу этой исторической тестовой поставки root не выполнял.

Независимый SELECT-снимок до/после операции: задача77ae4141-6aab-4abd-86d9-8b7d5b190328 сталаdone, две строки получили по1 упакованной штуке, оба заказа pack_status=packed. При этом JSON остатков, резервов и всех14 существующих движений полностью совпал. Два факта упаковки имеют inventory_movement_id=NULL; pick_status осталсяpending. **Упаковка на фактически развёрнутом staging не изменила склад/резервы, не создала движения и не заблокировала переход к коробам.**

Ограничения не скрыты: на подборе сотрудника виден старый403 linked-wb-catalog (сам подбор загружается); запрос и admin-only guard одинаковы с base6afa, права не повышались. В одной исходной складской строке уже было-2 до упаковки, историческая коррекция не выполнялась. Полный цикл ЧЗ на staging требует проверки возможностей фактически развёрнутого эмулятора; изученный код имеет неполные batchmeta/DELETE. Локальные синтетические проверки отмен, ЧЗ, перепечатки и единичной передачи сохранены отдельно и не выдаются за сценарием реального WB.

27 реализаций теперь имеют статус «НА СТЕНДЕ · ПРИЁМКА». Шесть снятых/объединённых задач и семь частичных пунктов сохраняют свои границы. Заявления «все40 полностью работают» или «33 полностью приняты» нет. Heartbeat20мин остаётся включённым для оставшейся приёмки и подтверждённых исправлений.
