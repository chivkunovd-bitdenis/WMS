**SAFE — bounded ACCEPT для `d3103aa8 + 89a86643fb9e6c2dbcdc236c4642c0d9596e2273`. Прежний P2 двойной отмены закрыт; новых P1/P2 в исправлении не обнаружил.** Код прочитан из фиксированного SHA, отчёт и свидетельства — из `171aa1d4bd5cdf0da22adb130bc4131ce29a4300`.

Подтверждены условия закрытия:

- **Обе отмены сериализуются до снятия before-снимка.** Самостоятельно проверил компиляцию извлечённых из SHA выражений: `.with_for_update(key_share=True)` даёт PostgreSQL **`FOR NO KEY UPDATE`**, а `populate_existing=True` сохранён. Проверка выполнена без подключения к БД. См. [legacy:676](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/billing_invoice_service.py:676), [V2:758](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/billing_invoice_v2_service.py:758).
- **Устаревший объект перечитывается после ожидания.** После commit первой отмены второй запрос получает `cancelled`, поэтому равные before/after не создают событие. После rollback первой отмены второй получает `issued` и записывает переход под своим автором. Загрузка `lines_v2` остаётся после получения актуальной строки; дополнительная блокировка включена только для отмены. См. [V2:761–784](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/billing_invoice_v2_service.py:761).
- **В проверенных путях create/cancel встречного порядка блокировок не добавлено.** Отмена блокирует строку счёта и не запрашивает блокировку Seller. Загрузка строк V2 не добавляет блокировок дочерних строк. Выбранный `NO KEY UPDATE` совместим с `KEY SHARE`, необходимым для проверки внешних ключей на счёт; ключевые поля счёта при отмене не меняются.
- **Прежняя семантика сохранена.** Tenant-фильтры и ошибки отсутствующего счёта остались. Выпуск, повторный запрос выпуска, суммы, автор выпуска V2 и существующие commit/rollback не переписаны. Общий audit-helper, снимки автора и fail-soft не изменены.

Полностью прочитан [тест двух соединений](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/tests/test_wms325_invoice_cancel_concurrency.py:55). Он использует разные PostgreSQL PID, два аутентифицированных HTTP-запроса и фактическое ожидание через `pg_blocking_pids`. Проверены четыре сочетания legacy/V2 × commit/rollback первой транзакции, заранее загруженный объект `issued`, одно сохранившееся событие и автор победившей транзакции.

Сохранённые свидетельства автора согласуются с кодом: baseline-проверка проходит, **подтверждая два события на старой функции**; исправленный целевой набор — **8 passed**. Это результаты исполнителя, не мои PG-запуски. Моя выполненная проверка — компиляция SQL; повторный pytest не запускал.

Приёмка относится к прежнему блокеру и завершает независимое ревью ранее рассмотренного billing/settings-среза с этим исправлением. Она не означает закрытия всей WMS-325, проверки интеграционного SHA координатора или разрешения на выпуск. Файлов не менял, дочерних агентов и внешних действий не выполнял.

Координатор после cherry-pick d310/14f/89a/171 в3d160fca/46321ce4/c7082fb8/e9e62951 прочитал app-delta и выполнил оба новых тестовых файла:23 PostgreSQL tests PASS за30.33s в собственной учебнойwms415_review325_20260910. Scoped Ruff PASS. Это интеграционная проверка, не production.
