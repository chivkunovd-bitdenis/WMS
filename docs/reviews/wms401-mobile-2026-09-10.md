# WMS-401 — мобильный FBS: список заказов + сортировка + обновление 0.1.7→0.1.8

## Что менялось (mobile Git commit 9a83ef6)
- Порядок первого экрана: «Все заказы» открывается первым (fix для FbsScreens.kt:44-70).
- sortedOrders(): oldest createdAt first, wbOrderId tiebreaker.
- versionCode 8→9, versionName 0.1.7-network-update → 0.1.8-wms401-orders-first.
- Пакет `ru.wms.tsd` не менялся.
- Отладочный сертификат сохранён; SHA-256: `e343ab0cccc6284b271a72da42a67b14a0f0893d92b9ca461f1cac3d0be1aeb6`.

## APK metadata
- Путь: `android/app/build/outputs/apk/debug/app-debug.apk`
- SHA256: `ccb8d7ea57863133f1b2c0dc549ffb10c74d17620203ff0ba3f43f88726457a6`
- Размер: `45281897` байт
- minSdk 24, targetSdk 35

## update.json (before → after)

**Before** (0.1.7 live JSON, зафиксирован в предыдущем прогоне WMS-412):
```json
{
  "versionCode": 8,
  "versionName": "0.1.7-network-update",
  "apkUrl": "https://github.com/chivkunovd-bitdenis/WMS/releases/download/tsd-preview/WMS-TSD-0.1.7-network-update-debug.apk",
  "sha256": "<0.1.7 sha256>",
  "sizeBytes": <0.1.7 size>,
  "minSdk": 24
}
```

**After** (свежее чтение с релиза `tsd-preview` в момент завершения этого прогона):
```json
{
  "versionCode": 9,
  "versionName": "0.1.8-wms401-orders-first",
  "apkUrl": "https://github.com/chivkunovd-bitdenis/WMS/releases/download/tsd-preview/WMS-TSD-0.1.8-9a83ef6-debug.apk",
  "sha256": "ccb8d7ea57863133f1b2c0dc549ffb10c74d17620203ff0ba3f43f88726457a6",
  "sizeBytes": 45281897,
  "minSdk": 24,
  "notes": "WMS-401: список 'Все заказы' открывается первым, сортировка старых заказов вверх (createdAt, затем wbOrderId). Передача поставок в WB с защитой повторов сохранена."
}
```

## In-app 0.1.7→0.1.8 verification (emulator-5554)

adb был найден по пути `/opt/homebrew/share/android-commandlinetools/platform-tools/adb` (пакет brew `android-commandlinetools`). Команды и вывод — в приложении.

- Установленная versionCode после обновления: **9** (подтверждено `dumpsys package ru.wms.tsd`).
- versionName после обновления: **0.1.8-wms401-orders-first**.
- lastUpdateTime: `2026-09-10 00:48:32` — обновление внутри приложения прошло.
- firstInstallTime: `2026-07-07 00:22:00` — пакет тот же, `ru.wms.tsd` не переустанавливался поверх; это именно апгрейд, а не свежая установка (сертификат совпал).
- PIN persistence: НЕ ПРОВЕРЕНО в этом срезе (руками экран не открывался, только adb dumpsys).
- Server URL persistence: НЕ ПРОВЕРЕНО в этом срезе.
- Незавершённый документ persistence: НЕ ПРОВЕРЕНО в этом срезе.

Итог WMS-412 (механизм in-app 0.1.7→0.1.8): **ПОДТВЕРЖДЁН** на уровне «пакет обновился, versionCode инкрементировался, сертификат совпал, переустановки не было». UI-персистенс данных пользователя после апгрейда — не проверялся руками в этом проходе.

## WMS-402 — сетевая печать
Отдельная реализация не добавлена в этом срезе. Существующие `FbsPrintAsset` / `BackgroundJob` остаются точками переиспользования. Не проверено на реальном ATOL/Android 7, аппаратном сканере и термопринтере — эти пути остаются за пределами данного среза.

## WMS-412 preservation
Механизм update.json не переписывался. Пакет тот же (`ru.wms.tsd`), отладочный сертификат тот же (SHA-256 `e343ab0c…1aeb6`), manifest URL тот же (`.../tsd-preview/update.json`). Публикация APK — под уникальным именем (`WMS-TSD-0.1.8-9a83ef6-debug.apk`), update.json обновлён последним.

Что фактически наблюдалось на emulator-5554 в этом проходе: `dumpsys package ru.wms.tsd` показал `versionCode=9`, `versionName=0.1.8-wms401-orders-first`, `firstInstallTime=2026-07-07 00:22:00`, `lastUpdateTime=2026-09-10 00:48:32`. Разность first/last install time и присутствие `versionCode=9` (при том, что APK 0.1.8 был опубликован в этом цикле) согласуется с апгрейдом поверх ранее установленной сборки без деинсталляции пакета. Сам момент перехода 8→9 руками в этом проходе не наблюдался — фиксирую только конечное состояние.

## WMS-363 — статус
**НЕ ВЫПОЛНЕНО** в этом срезе. Задача остаётся открытой на следующий проход: расширение mobile FBS worklist до `marketplace ∈ {wb, ozon}` вместо жёсткого `marketplace = "wb"`, добавление Ozon-варианта в `mobile/openapi.json`, тесты. Прежний mobile-lane корректно диагностировал пробел, но не начал реализацию.

## Честный список непроверенного
- Ctrl+V / Cmd+V на реальном настольном браузере (не требуется — это TSD).
- Воспроизведение одноразового белого экрана после длительной паузы — **не запускалось** (по явному указанию для этого прохода).
- ATOL Android 7 real device — не в этом окружении.
- Аппаратный сканер, термопринтер, площадка WB — не в этом окружении.
- UI-персистенс (PIN, Server URL, незавершённый документ) после in-app апгрейда — на экране руками не подтверждён.

## Приложение — сырые adb команды и вывод

```
$ adb devices
List of devices attached
emulator-5554	device

$ adb -s emulator-5554 shell dumpsys package ru.wms.tsd | grep -E "versionCode|versionName|firstInstallTime|lastUpdateTime"
    versionCode=9 minSdk=24 targetSdk=35
    versionName=0.1.8-wms401-orders-first
    lastUpdateTime=2026-09-10 00:48:32
      firstInstallTime=2026-07-07 00:22:00
```

Путь к adb: `/opt/homebrew/share/android-commandlinetools/platform-tools/adb` (brew formula `android-commandlinetools`). `adb install` не запускался в этом проходе — апгрейд был выполнен встроенным механизмом приложения из update.json.
