# WMS-349 — настоящий конкурентный replay

Проверен сохранённый 5a3f40c5 в worktree wms349-merge-service. Новый тест использует отдельную локальную PostgreSQL-базу wms349_astra_20260910; HTTP fixture создаёт только синтетические tenant/seller/products/warehouse. Две реальные транзакции: writer добавляет 3 к одной из карточек с исходными остатками 8 и 5, merge загружает старые ORM-объекты и ждёт Product-lock. Через pg_blocking_pids подтверждено ожидание, затем writer коммитит.

До исправления оба варианта (движение по source и target) падали: итог 13 вместо 16. Product-lock удерживался, но InventoryBalance в identity map оставался старым. Добавлен populate_existing для обеих выборок баланса под блокировкой. Исправлен неверный комментарий о том, что balance FOR UPDATE сам предотвращает появление новых строк: это обеспечивает общий Product-lock штатных writers.

После правки: весь целевой файл на PostgreSQL — 7 passed за 4.48s, включая оба concurrent replay. Проверены остаток16, три движения общей суммой16 и перенос их ссылок на оставшуюся карточку. ruff check . PASS; mypy . PASS (436 файлов). SQLite-компиляция SQL отдельно не выдаётся за конкурентный тест.

Нужны независимое review итогового commit, интеграция/CI/deploy. Реальные клиентские товары не объединялись, браузерное объединение не выполнялось. Новый тест не меняет продуктовую механику объединения и не вводит новых сущностей.


## Исправление двух замечаний независимого review после возобновления

Merge после Product-lock заранее блокирует изменяемые ссылки через NOWAIT. Если заказ/документ занят, вся попытка откатывается с HTTP409 merge_busy и понятной просьбой повторить; складская отмена продолжает работу. Обычное движение, которое первым удерживает Product, по-прежнему сериализуется ожиданием. Новых сущностей нет.

Перед изменениями проверяются совпадающие уникальные связи: обычные UniqueConstraint по метаданным и три существующих выраженных/частичных индекса инвентаризации, подбора и тарифа. При конфликте оператор видит тип связи, например настройки FBS одного склада. Потолки не складываются и ни одна строка не отбрасывается автоматически.

Настоящий PG replay: cancellation helper удерживает Order, merge удерживает Product, снятие резерва действительно ждёт Product (pg_blocking_pids). Merge отказывается без взаимной блокировки; cancellation освобождает резерв; остатки5+8 сохранены. Повтор merge завершается остатком13 и переносом заказа. Контроль с NOWAIT отключённым дал psycopg.errors.DeadlockDetected: процессы88795/88881 взаимно ждали транзакции294744/294743. После восстановления NOWAIT тест снова прошёл. Это реальные локальные транзакции в отдельной wms349_astra_20260910, не клиентские данные.

Целевой файл:9 passed на PostgreSQL; после контрольного отключения восстановленный concurrent test1 passed. Ruff PASS, mypy436 PASS; TypeScript и production frontend build PASS. HTTP-тест двух потолков получил409 с конкретной причиной и подтвердил отсутствие UPDATE/DELETE до отказа; оба потолка7/3 и балансы8/5 сохранены. CI/deploy/browser и новое независимое review этого исправления пока отсутствуют.
