# WMS-058 / WMS-396: текущая ручная приёмка staging

09.09.2026, начало около 00:20 МСК. Это результаты выполненных действий,
а не перечень обещаний. Проверка продолжается; production ещё не обновлялся.
Приложение staging — кандидат `ad116f0d`; CI 34258212809 перечитан:
completed/success, headSha точно совпадает. После кандидата в интеграционной
ветке менялись только AGENTS и документы.

## Доступ и контур

Root открыл настоящий сайт `https://web-production-9e7c1.up.railway.app`
в обычном Chrome. Уже существовавшая сессия автоматически открыла
`staging-admin@example.com`, администратор «WMS Staging». Пароли не запрашивались,
новые токены не выпускались, сохранённый токен в браузер не вставлялся.
В IAB была другая тестовая организация без заказов; это не потеря данных.
Целевой tenant, существующая fixture и доступность WB-эмулятора отдельно
проверены SQL/HTTP в `wms-stage-qa-fixtures-20260909.md`.

Root нажал «В работе», увидел существующие поставки и открыл
`4e38cabf-b129-4a28-85b0-3dd3631da6a4`, «FBS 04.09.2026»,
WB `WB-GI-245619454EE7`, селлер «Эмулятор WB», склад WMS «Тестовый».
Проверки проведены только на этой тестовой поставке. Документы других
селлеров и production не изменялись.

## Подбор и упаковка: выполненные действия

Исходное состояние: 2 заказа, подбор 0/2, упаковка 2/2. Root открыл
«Короба» при незавершённом подборе: вкладка открылась, видны создание
коробов и передача. Передача не нажималась. Затем вернулся на «Подбор».

Прямой скан `2000000000015` без выбранного источника вернул видимую ошибку
«Недостаточно товара в выбранном месте подбора». Раскрытая строка показывала
FBS-VIDEO-01 → палета П-001012 → грузоместо FBSVID-CARGO-01 (20 шт.)
и короб FBSVID-BOX-02 (40 шт.). Это воспроизведённый отказ, не вывод из теста.
Агенту передан минимальный разбор WMS-058; до исправления и повторной проверки
прямой скан с несколькими контейнерами не принят.

Скан источника `FBSVID-BOX-02`, затем `2000000000015` прошёл. UI показал
снято 1 шт. из этого короба, общий остаток подбора 1/2. Отдельный SQL-снимок
21:22:39 UTC подтвердил pick `c4816f43…` для WB 500038, тот же короб,
`movement_id=NULL`. По этому товару физический остаток 60, число движений 6,
резерв FBS 9 остались прежними. Это показатели товара во всей тестовой
организации, а не только двух заказов поставки.

Root снова открыл упаковку и нажал «Всё упаковано». Появилось «Упаковка
завершена», счётчик остался 2/2. SQL 21:26:11 UTC: WB 500038 picked/packed/
reserved, один активный pick с прежним источником; WB 500007 pending/packed/
reserved. У первого товара 60/6/9, у второго 58/8/15 (физическое количество /
число движений / резерв) не изменились. Это проверка повторного подтверждения
уже установленного признака, не перехода из неупакованного состояния.

После возврата на подбор root раскрыл товар и изменил «Снять» в строке
FBSVID-BOX-02 с 1 на 0. UI вернулся к оставшимся 2/2. SQL 21:27:37 UTC:
WB 500038 pending/packed/reserved; у того же pick появился `undone_at`,
источник сохранился, `movement_id=NULL`; физическое количество 60, число
движений 6 и резерв 9 не изменились. Упаковка отмену подбора не заблокировала.
Повторный GET pick-options подтвердил picked 0 и прежнюю доступность источников.

## Стикер и вход в сканирование ЧЗ

Root нажал QR заказа 500038. Открылась «Проверка перед печатью», загрузился
один настоящий макет WB с QR и номером 57115752 / 8424; изображение просмотрено
на скриншоте. Печать на устройство и «Подтвердить нанесение» не нажимались.

Сохранённый `sticker_barcode` прочитан из БД: `*DU7aq2hE`. Его ввод в штатный
сканер упаковки действительно активировал WB 500038: показан нужный товар,
поле сменилось на «Сканируйте Честный знак». На двух строках нельзя доказать
прокрутку длинного списка; такая проверка здесь не заявляется.

## Приёмка 000046: реальный staging, 21:30–21:40 UTC

После блокировки Mac root продолжил интерактивную проверку отдельным headless
Chrome через имеющийся bundled Playwright: реальные страницы, нажатия,
клавиатурный скан и скриншоты, без подмены сетевых ответов. Использован уже
предоставленный staging-token только в памяти браузера; значение не выводилось.
Это отдельный сеанс от описанного выше обычного Chrome.

Создана только синтетическая приёмка `05cd913b-7f50-4f64-9c45-d1b124737166`,
видимый номер 000046, селлер «Эмулятор WB», план по 2 штуки EMU-KIZ-REQUIRED
и EMU-NORMAL-B2C. После «Начать приёмку» скан первого товара дал количество 1,
следующий полный КИЗ добавил дочернюю строку и оставил количество 1. Повтор
товара дал 2. Дополнительный тестовый код удалён кнопкой строки: HTTP 204,
количество осталось 2. То же чередование второго товара и КИЗ завершилось
общими 4/4, по два экземпляра каждого товара. Раскрытие/скрытие проверены
ожиданием видимости; после закрытия панели и повторного открытия данные сохранились.

Два оставшихся синтетических кода имеют серийные части QA39609090001 и
QA39609090002, хвост QA396-TEST-NOT-FOR-SALE. Реальные GS-разделители вводились
клавиатурой Control+]. SQL после проведения подтвердил точное совпадение обоих
64-символьных значений и GS в позициях 31/38 (от нуля), без обрезания хвоста.

«Завершить приёмку» вернул HTTP 200, экран перешёл в «В сортировке» и сообщил
о принятии товара. Последующий повтор того же complete через API вернул 409
`not_verifying`. Независимый SQL-срез 21:40:04 UTC подтвердил ровно два движения
inbound_intake по +2: KIZ-товар 59→61, NORMAL 58→60. Резервы 10 и 15 не изменились.
У удалённого кода и его события строк нет. Сохранившиеся коды не связаны с FBS,
пулом или упаковкой; их привязка сама складских движений не добавила.
После проведения кнопок удаления КИЗ в открытой панели нет.

Оба кода получили «Не удалось проверить». Родительские и дочерние строки
красные; фактическая подсказка при наведении на !: «Честный знак недоступен.
Повторите проверку позже.» Кнопка «Проблемные коды ЧЗ в Excel» скачала настоящий
xlsx. Файл открыт openpyxl: лист «Проблемные КИЗ», заголовки Артикул / Код
Честного знака / Причина, ровно две строки с правильными SKU, полными кодами
(GS представлен буквальным `\u001d`) и причиной недоступности, формул нет.
Удалённого кода в выгрузке нет. Сам файл оставлен в tmp/qa396 этого worktree;
результаты проверки сохранены этим Git-отчётом.

Положительная проверка ЧЗ **не подтверждена**. Дополнительный единственный
запрос синтетического кода из каждого runtime дал HTTP 451 без тела: staging
21:42:25 UTC и production 21:43:54 UTC. Причина отказа провайдера не установлена.
У точного тестового селлера сохранённого CZ-токена нет (проверен только EXISTS).
Официальный интеграционный путь и ограничения описаны в
`wms396-live-check-readiness.md`. Ошибка честно показана, но это не доказывает
работоспособность проверки оборота на разрешённом официальном API.

## WB КИЗ и повтор создания поставки

На упаковке прежней поставки после QR `*DU7aq2hE` root отсканировал существующий
синтетический пуловой КИЗ `010200000000001521EMU020014STAGE01`. UI сохранил хвост
4STAGE01 и показал «WB ещё не подтвердил ЧЗ». GET эмулятора подтвердил хранение
точного кода и legacy checkStatus=ok. WMS требует metaDetails, который в
прочитанной реализации эмулятора пуст: в WMS unknown/error. Подтверждение WB
не заявляется; это не очередь ожидания отправки. Доказательства и границы
перехвата HTTP приведены в `wms-stage-qa-fixtures-20260909.md`.

Для WMS-272 созданы два новых тестовых заказа WB 500041/500042. Root выбрал
только эти две строки в «Новые», нажал «Сформировать поставку», оставил маршрут
«Склад или сортировочный центр» и нажал «Создать поставку». HTTP 201 открыл
поставку `2617f1d5-ee49-4dfd-b108-3e1f6a8711ed`, WB-GI-38C5DB4FA3CD, с ровно
этими двумя заказами. Повтор идентичного запроса с тем же idempotency_key
573efa59-55f4-4142-be67-491a11890953 через API вернул 201 и те же оба ID.
Проверка параллельной гонки/частичного отказа этим последовательным повтором
не подменяется; соответствующие технические проверки описаны отдельно.

## Что этим ещё не подтверждено

Исправление прямого выбора источника 2e908faf ещё не развернуто и не повторено
на staging. Полный CI этого SHA упал на единственном тесте даты подписки
(11 вместо 12 после московской полуночи); backend идентичен ранее зелёному ad116f0d,
фронтовые проверки прошли. Исправляется временная предпосылка самого теста.
Многопозиционный Ozon, передача/повтор передачи, отрицательные ответы эмулятора,
публикация обеих площадок и оставшиеся сценарии общей матрицы требуют проверки.
Итоговое ревью всего выпуска Opus CLI max и production-деплой ещё не выполнены.
