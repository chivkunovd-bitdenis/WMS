**SAFE — ограниченный ACCEPT для обоих коммитов вместе: `e36b8c157081c170809862fb5ed0e1a0fa4a5297` → `019fb8bc22e83c27c545373d981e89bce9fc9779`. Подтверждённых P1/P2 в итоговом срезе не обнаружил.** Прочитал полный отчёт из `343a7b24`, совокупный production-diff, новый файл тестов и связанные существующие пути. Первый коммит отдельно этим вердиктом не принимается.

Что подтвердилось по итоговому коду:

- **Связи Pool/Product:** замена сериализуется отдельной транзакционной блокировкой конкретного tenant/pool. Строка Pool при этом не блокируется, существующие связи читаются с блокировкой в порядке ID. Вставка использует `ON CONFLICT DO NOTHING … RETURNING`, поэтому событие включает только собственные изменения и не приписывает оператору связь, которую раньше вставил параллельный запрос. Явных блокировок строк Product этот путь не добавляет. См. [marking_code_service.py:875](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/marking_code_service.py:875).
- **Пороги Pool:** `FOR NO KEY UPDATE` и `populate_existing=True` обеспечивают актуальный before после ожидания. Изменение порогов не захватывает связи. Повторные равные значения, включая очистку в `null`, обрабатывает существующий no-op helper. См. [marking_code_service.py:2465](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/marking_code_service.py:2465).
- **Удаление привязки к приёмке:** событие записывается после прежних запретительных проверок, перед удалением привязки и общим commit. Сохраняются идентификаторы кода, строки, исходного события и первоначального автора привязки; текущий автор удаления берётся отдельно из контекста. Содержимое кода не копируется. Ветка печатного пула сохраняет сам код, ветка ошибочного внешнего ввода отражает его удаление через `code_exists`. См. [inbound_marking_service.py:395](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/inbound_marking_service.py:395).
- **Шаблоны и default:** блокировка области tenant/owner/product-or-seller берётся до блокировки целевого шаблона. Повторное чтение проверяет область и обновляет загруженный объект; прежние default-шаблоны читаются в порядке ID и получают отдельные события только при изменении флага. Снимок содержит разрешённые поля макета, а некорректный старый layout не препятствует удалению. См. [print_template_service.py:241](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/print_template_service.py:241).

Общий helper авторства, снимки имени и точки отката не изменены. Новые события остаются в транзакции бизнес-операции; отдельного commit аудита нет. History API сохраняет ограничение для администратора и фильтрацию tenant. Новых складских движений, финансовых фактов или навигационных условий совокупный diff не добавляет.

Прочитанные тесты действительно проверяют разные PostgreSQL PID, фактическое ожидание блокировки, commit/rollback первого запроса, устаревшие объекты, встречные переключения default и создание default в пустой области. Отдельный тест вызывает настоящий `add_pool_products`, пока замена приостановлена перед вставкой, и проверяет отсутствие ложного события замены. В сохранённом выводе автора — **46 passed, 5 warnings**. **Это результаты автора: самостоятельно pytest и PostgreSQL я не запускал.**

Граница ACCEPT — перечисленные операции и проверенные сочетания конкурирующих запросов. Это не доказательство отсутствия взаимных блокировок со всеми сторонними writers и не полный аудит WMS-325. Основные изменения last-layout/seller-label-options, создание пула и аудит additive/remove/import-путей остаются вне этого среза. При отказе истории сохраняется fail-soft: операция может завершиться без события.

Файлов и Git-состояния не менял, дочерних агентов и production-действий не выполнял.