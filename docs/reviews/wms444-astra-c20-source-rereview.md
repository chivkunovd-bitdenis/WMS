# WMS-444 — повторное независимое ревью покрытия источников C20

13.09.2026. Проверяемый backend SHA **`9e3929f0ea43feb33b9322f4ef4283e303f80c09`**. Ревьюер — та же независимая сессия Astra high. **PASS: последний P1 из `wms444-astra-c20-review.md` закрыт; новых технических замечаний в изменённых ветках не найдено.** Можно передать предъявленный backend исходному БА по общей цепочке; этот PASS не заменяет приёмку всей WMS-444.

## Что проверено в изменениях

Прочитаны новый diff и `wms444-terra-c20-source-coverage.md`; текущие R13/C20 сохраняют согласованное ранее правило. Покрытие теперь вычисляется отдельно для packed, known-unpacked и unknown. Каждое снятие — неотрицательная разница между соответствующей коробочной частью и оставшимся подбором; суммарное снятие больше не ограничивается общим boxed−picked. Другой класс, ещё не разложенный в короб, не оставляет в нём уже возвращённую часть.

Ветка baseline сначала восстанавливает сохранённый исторический результат, затем общая синхронизация добавляет только оставшиеся known единицы. R13 изменяет baseline только на фактически снятый unknown. Полностью снятый состав остаётся0/0. Новых источников оплаты, таблиц, миграций, действий или ограничений нет. Код продукта и требования ревьюером не изменены.

## Собственные доказательства

Пробы выполнены в новой отдельной PostgreSQL17.10 БД `wms444_astra_c20_final_20260913` с явным DATABASE_URL. Начальные данные — собственные ORM-fixture, дальнейшие действия — настоящие сервисы `set_pick_allocation`, подбора/раскладки и complete. Сохранённые скрипты содержат assertions ожидаемого результата. Это сервисная/DB проверка, не подмена живой web/Android приёмки.

| Проверка | Подтверждённый результат | Вердикт |
| --- | --- | --- |
| Прежний контрпример A: boxed packed2, unboxed unpacked2; убрать packed allocation | Box0/task0, остаётся unpacked allocation2; на склад вернулись packed2 ровно одним движением | PASS |
| Прежний контрпример B: boxed packed2+unknown1, unboxed unpacked1; убрать packed allocation | Успех без insufficient_picked. Box unknown1, ready1/work0; остаются unknown1+unboxed unpacked1. Возврат packed2 один раз | PASS |
| Все6 упорядоченных сочетаний разных классов: удалить boxed class, оставить unboxed другой class | Box0, чужой unboxed allocation2 остаётся; правильный split возврата, одно новое движение. Repeat set0 ничего не меняет | PASS |
| Неполная раскладка, отдельно для каждого из3 классов: picked2/boxed1 → set1 | Box1 сохраняет исходный класс и ready/work; возвращается только1. Покрытый короб не обрезается | PASS |
| После удаления known-unpacked2 остаётся только historical work1 | Box unknown1, ready0/work1, complete units1/700коп. Удалённая новая работа не удерживается в факте | PASS |
| Исходный C20: historical2 ready1/work1 + fresh unpacked1; set3→2→0→repeat0 | Box3→2→0→0, stock0→1→3→3. Ready/work после2 =1/1, после0 =0/0. Повтор не создаёт нового движения | PASS |
| Новый unpacked1 после полного нуля | Ready0/work1; complete700коп, исторический ready не возвращается | PASS |

В проверенных сценариях ledger селлерских начислений оставался0: изменение подбора/коробов не создало платы до отгрузки. Единичность возврата проверена не только остатком, но и числом InventoryMovement. Для обеих прежних ошибок и межклассовой матрицы каждый настоящий set добавил ровно одно движение, repeat0 — ни одного.

Доказательства: [исходный C20 и оба прежних контрпримера](artifacts/wms444-astra-c20-source-rereview/wms444_c20_review.py), [матрица классов и historical-work baseline](artifacts/wms444-astra-c20-source-rereview/source_matrix.py). Запуск из корня checkout с root `backend/.venv/bin/python`, `PYTHONPATH=backend` и явным DATABASE_URL указанной собственной БД. Основной скрипт проверяет current_database перед созданием своей схемы; новые документы получают случайные ID. После добавления явных assert для A/B основной скрипт отдельно завершился exit0. Синтаксис обоих сохранённых файлов проверен AST, diff проверен на пробелы.

## Границы PASS и передача

Текущие изменения ограничены одной сервисной областью. Прежняя матрица18, D1, Android, полная suite и неизменённые миграции повторно не запускались; их ранее зафиксированные результаты не переобозначались как новые прогоны. Авторские13 pytest/ruff/mypy не выдаются за собственные проверки. Общая БД/runtime, Gradle, устройства, production и внешние маркетплейсы не использовались. Настроенных внешних аккаунтов в собственных fixture нет; локальная публикация использовала явно заданную отдельную БД, ошибки роли не наблюдались.

Коробочный R13 PASS и предыдущие закрытые исправления сохраняются; последний остаточный C20 source-coverage P1 закрыт фактическими проверками. **RELEASE** ревьюерского слота для интеграции ведущим и приёмки тем же исходным БА. Отдельно предстоят предусмотренные требованиями API/web/Android, отчёт и общий интегрированный сценарий; ревьюер не объявляет их пройденными, не выполняет merge или деплой.
