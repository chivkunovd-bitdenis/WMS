# WMS-418: срочный отдельный выпуск

Прямое поручение владельца — немедленно выпустить фильтр товаров с включённой публикацией остатков WB/Ozon. Production и origin/etalon перед созданием отдельной ветки оба проверены: eaa6ae9aecee875c50b31afa6b2cc224bb57d043. Worktree wms418-release-20260910 создан внутри .worktrees от этого точного SHA. Исходная ветка95ab5fc4 целиком не сливается.

Перенос содержит ровно3 app-файла: products API, seller_wb_catalog_service, существующий FfProductsCatalogScreen; один файл с2 HTTP-тестами и узкую карточку418/доказательства. Auth/network/deploy config/новые миграции/111112/широкий415 не включены. Coord перенос уже сохранён923904b5; root worktree418 не изменён.

Root вручную прошёл полный исходный каталог, протокол приложен. После минимального переноса проверяются целевые HTTP, Ruff/mypy, tsc/build и независимый Astra review. Затем один штатный CI в PR, точный merge/deploy после повторной проверки server HEAD. Production flags ради теста не менять. На момент этой записи CI/deploy ещё не выполнены.

Проверка адаптации координатором:2 HTTP-теста PASS на отдельной PostgreSQL wms418_release_20260910 за27.07s, scoped Ruff PASS, mypy2 изменённых файлов PASS, frontend tsc/build PASS. Первый setup переиспользованной учебной БД397 отказал из-за зависимых таблиц чата, отсутствующих в production-моделях; приложение не меняли ради обхода, создана отдельная пустая учебная БД418 и целевой запуск прошёл. Локального полного pytest не было. Новых code-изменений после проверок нет.
