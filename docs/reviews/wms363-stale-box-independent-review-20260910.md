**В `c9d27e16e992a52ae80b9f7127aad16967f1915d` остаётся один P2, препятствующий preview publication: восстановление после скана короба, отсутствовавшего в локальном списке.** Исходный сценарий с уже известным коробом B исправлен.

Полный follow-up прочитан. Двухфайловая дельта **побайтно совпала** с diff неизменяемых Android-коммитов `30bab83d → c9d27e16`.

Для известного B цепочка теперь корректна: `scan()` синхронно выбирает B до постановки в очередь, поэтому следующий товар сохраняет B при исходном выборе A или `null`. Завершение refresh не перезаписывает последующий ручной выбор A. После потерянного ответа повтор использует сохранённые короб и состав назначения; уже выполненное назначение определяется чтением без второго запроса записи. Это проверено по коду, включая новые четыре регрессионных теста; заявленные **74 passed — результат автора**, я тесты не запускал.

**Оставшийся P2 — после сообщения «Отсканируйте короб ещё раз» ожидающий товар сохраняет старый короб A.** Конкретная последовательность:

1. Локальный workspace содержит A, выбран A; B существует на сервере, но ещё отсутствует локально.
2. Сканируется `BOX-B`; его refresh задержан. Следом сканируется свободный товар — оба элемента очереди сохраняют A.
3. Refresh возвращает B. Проверка `B != scannedBoxId(A)` показывает требование повторного сканирования.
4. Повторное сканирование B теперь синхронно выбирает B, но **не исправляет и не отменяет уже ожидающий товар с A**.
5. Пока ошибка открыта, очередь стоит. `dismissError()` снимает ожидание, после чего ранее поставленный товар проходит в A. Если вместо закрытия нажать retry, исходное действие снова сравнивает B с захваченным A и повторяет ошибку.

Основание в `FbsViewModel.kt` указанного SHA: [ожидание очереди, строка 103](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-mobile/android/app/src/main/java/ru/wms/tsd/features/fbs/FbsViewModel.kt:103), [снятие ошибки, строка 140](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-mobile/android/app/src/main/java/ru/wms/tsd/features/fbs/FbsViewModel.kt:140), [фиксация адресата, строка 270](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-mobile/android/app/src/main/java/ru/wms/tsd/features/fbs/FbsViewModel.kt:270), [проверка после refresh, строка 399](/Users/deniscivkunov/Projects/WMS/.worktrees/wms397-mobile/android/app/src/main/java/ru/wms/tsd/features/fbs/FbsViewModel.kt:399). При исходном `null` ошибочного назначения в A нет, но ожидающий товар остаётся без адресата.

**Обязательное исправление:** восстановление после обнаружения неизвестного короба должно исключать выполнение зависимых ожидающих товаров со старым адресатом. Одного сообщения о повторном сканировании недостаточно. Нужна узкая регрессия «устаревший список → BOX-B → товар → ошибка → повторный скан/закрытие ошибки», проверяющая отсутствие назначения в A. Это продолжение прежнего замечания о последовательности сканов, а не новый полный аудит.

APK самостоятельно проверен:

- SHA-256: `e2685e7772fcdcee00a871002b083043d90e2e7490b6f435bf291940c023c98b`.
- Размер: **45 624 726 байт**.
- `ru.wms.tsd`, versionCode **10**, `0.1.9-ozon-tsd`, minSdk **24**, targetSdk **35**, debug.
- `apksigner`: подпись v2 действительна, один подписант; сертификат `e343ab0cccc6284b271a72da42a67b14a0f0893d92b9ca461f1cac3d0be1aeb6`. Метаданные совпали с candidate JSON и ожидаемым сертификатом.

UI и обновление кнопкой **9→10** я не проверял. Физический ATOL не выставляю предварительным условием разрешённой preview-публикации; препятствие здесь — описанный P2 в обработке очереди. Ничего не изменял.