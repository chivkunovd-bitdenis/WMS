# WMS-351: независимая публикация WB и Ozon, выпуск 08.09.2026

На production `sellerfocus.pro:8088` выпущен коммит
`0773d71f9b8e8871607aa0ae6b4e9054dbdb28dc` из ветки
`codex/hotfix-wms351-stock-toggles`. Выкладка завершена 08.09.2026 около
12:40 UTC. В main/etalon эта ветка не сливалась. PR:
https://github.com/chivkunovd-bitdenis/WMS/pull/195.

Существующая галка управляет WB, одна добавленная галка — Ozon. Изменение
одной площадки не переключает другую. При выключении выбранная площадка
получает подтверждаемый финальный ноль, после чего дальнейшая публикация
прекращается. Массовое сохранение сохраняет нетронутую галку отдельно у
каждого товара. Обслуживание складов не переключается; упаковка, списание
товара и сканирование ЧЗ в этот выпуск не входят.

## Проверка кода и сборки

- Полный CI для выпущенного SHA:
  https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34226619679 — success.
  Pytest: 1917 passed, 15 skipped, 1 xfailed за 375.79 s. Ruff, mypy,
  typecheck, frontend build и проверка канонического бэклога прошли.
- Первый прогон обнаружил три устаревших ожидания/фикстуры Ozon. Исправлены
  только тестовые данные и ожидание отсутствия повторной отправки нуля при
  выключенной публикации; весь файл прошёл: 62 tests.
- Код приложения, миграции и frontend между реализацией `7f682bb6` и
  тестовым уточнением `0773d71f` идентичны (проверен пустой git diff).
- Frontend собран из `7f682bb6`, совпадающего по frontend-дереву с выпуском.
  Проверены все 177 файлов. SHA256 манифеста сборки:
  `a9442151af9087d2c56ddcc10cc4efb031886a0de83318ed3e164f8a228a26cb`.

## Проверка браузером

На отдельном локальном стенде с подменёнными WB/Ozon проверены обе комбинации
галок, повторное сохранение и включение одной площадки. Выполнены настоящие
нажатия в Safari и Codex Browser. Для production-сборки отдельно открыт
предпросмотр на порту 15552: обе галки включены → выключен только WB →
сохранение → повторное открытие. WB выключен, Ozon включён; доли 50/50
сохранены, оба склада обслуживаются. В журнале транспорта появились только
WB PUT с amount=0 и WB POST для подтверждения; вызовов Ozon не прибавилось.
Физический остаток тестового товара остался 10.

На самом production после выкладки в браузере открыт URL каталога и получен
рабочий экран входа. Авторизованная сессия production в этом браузере
отсутствует. Переключение боевых настроек и публикация реальных остатков в
рамках проверки не выполнялись. Это ограничение не подменяется результатом
локального браузера или зелёным CI.

## Проверка работающего production

- В API, worker и beat проверены хеши всех 260 Python-файлов: отличий от
  выпущенного Git SHA нет. Все 177 файлов frontend в `/srv` совпали со сборкой.
- Все четыре контейнера работают и имеют OCI revision выпущенного SHA.
- `GET /api/health` с сервера и с внешнего адреса вернул `{"status":"ok"}`.
- Alembic revision: `20260908_0256`. Новый Ozon-флаг NULL у 12873 товаров,
  явных значений — 0: миграция сохранила прежнее поведение существующих товаров.
- В API, worker и beat проверены только несекретные параметры:
  `ozon_live_api_enabled=true`, hostname `api-seller.ozon.ru`.
- Сохранён существующий том `wms_prod_wms_appdata`; DB, Redis и отдельный
  seller web не пересоздавались. Старые хешированные web-ресурсы сохранены
  в образе для уже открытых вкладок.

Образы выпуска:

| Сервис | Image SHA256 |
|---|---|
| API | `5cc343a39bd9ae595ac054dace9ccb7bebc4ac03eecc291b52cc76c4e659e5f2` |
| Worker | `0b9cd4c633e2a3f309633b10452fc286aa43657fff790b4294fa732d56de1c44` |
| Beat | `abdd2c44d03ea24933ab941dadb42f1a4cfa1675c881e7746b900e8e3a06c8a9` |
| Web | `bffa255648afedc248db209b3341234eed891b92ac8b6b14b2af95f8bb16255a` |

До выкладки сверены исходный production SHA `6b1bd3d8` и образы всех четырёх
работающих контейнеров. Каждый новый серверный образ построен поверх своего
прежнего образа с заменой только Git-файлов app/alembic; зависимости не
менялись. Старые образы сохранены под тегами
`wms_prod-<service>:wms351-base-6b1`. Артефакты и список исходных образов:
`/opt/wms/.deploy-backups/wms351-7f682bb6/`.

Проверка не является подтверждением всего живого процесса Ozon. Исправление
сканирования и проверки ЧЗ на упаковке ведётся отдельно по WMS-357.
