**Ограниченный ACCEPT для WMS-418: `05ddd738f794d3bbcc74623aac02f531a747639a` относительно `eaa6ae9aecee875c50b31afa6b2cc224bb57d043`. Подтверждённых P1/P2 в этом delta не нашёл.** Препятствий со стороны данного code review для отдельного выпуска фильтра нет.

Проверены ровно три изменённых app-файла, оба HTTP-теста и документы переноса.

- **Семантика соответствует поручению владельца.** WB проверяется по `fbs_stock_sync_enabled`; Ozon — по `coalesce(fbs_ozon_stock_sync_enabled, fbs_stock_sync_enabled)`. Поэтому старый Ozon `NULL` наследует WB, а явный `False` остаётся выключенным даже при включённом WB. Варианты WB/Ozon/обе/хотя бы одна/ни одна составлены корректно. Остаток, наличие складской привязки и успешность последней публикации в условие не входят. См. [seller_wb_catalog_service.py:554](/Users/deniscivkunov/Projects/WMS/.worktrees/wms418-release-20260910/backend/app/services/seller_wb_catalog_service.py:554).
- **Область доступа, комбинации и пагинация сохранены.** Tenant берётся из текущего пользователя; seller и marketplace остаются дополнительными ограничениями. Новый фильтр объединяется с поиском и категорией перед подсчётом `total` и выборкой страницы. Сортировка остаётся устойчивой: `sku_code, id`. `scope_total` сохраняет прежний смысл общего количества внутри tenant/seller/marketplace до дополнительных фильтров. Неизвестное значение фильтра отклоняется API через `Literal`. См. [products.py:787](/Users/deniscivkunov/Projects/WMS/.worktrees/wms418-release-20260910/backend/app/api/products.py:787) и сервис, строки **600–617**.
- **Новый фильтр подключён к существующему управлению состоянием экрана.** Его смена сбрасывает страницу, выделение и ошибку прежнего выделения; значение включено в параметры запроса и зависимости `load`. Предыдущий запрос отменяется, а после чтения каталога и остатков проверяется `signal.aborted`, поэтому отменённый ответ не устанавливает старые строки. «Выбрать всё» работает по текущим строкам страницы. После сохранения существующих флагов вызывается повторная загрузка с выбранным фильтром. См. [FfProductsCatalogScreen.tsx:321](/Users/deniscivkunov/Projects/WMS/.worktrees/wms418-release-20260910/frontend/src/screens/v2/FfProductsCatalogScreen.tsx:321), строки **335–401, 568–584, 2168–2170**.

**Сверка с `95ab5fc4`:** app-сервис фильтра и файл двух тестов совпадают полностью. API и экран отличаются только сохранением production-участков, которые не относятся к WMS-418: прежней сериализации `StockDirection.is_fbs` и прежней обработки ошибок merge, без нового `merge_busy`. Это корректный узкий перенос; функциональные добавления фильтра сохранены.

Относительно указанной production-базы **нет изменений auth, сетевой/deploy-конфигурации, миграций, исполняемого WMS-111/112 или широкого кандидата WMS-415**. Новых страниц нет. Переключение фильтра выполняет существующие GET-запросы; складские количества, настройки публикации и маркетплейсы оно не изменяет.

Полный [отчёт ручной проверки root](/Users/deniscivkunov/Projects/WMS/.worktrees/wms418-release-20260910/docs/reviews/wms418-manual-catalog-20260910.md) прочитан. Это его проверка исходного `95ab5fc4` на изолированной установке; за собственные клики или проверку deployed `05ddd738` её не выдаю. Тесты не повторял. App/test `diff --check` чистый; полный diff отмечает только лишнюю пустую строку в конце канона — не P1/P2.

Файлы, Git, БД и runtime не менял. **Вердикт относится к коду конкретного отдельного среза; текущие scoped tests, CI, deploy и проверка развёрнутого SHA остаются отдельными этапами root.**