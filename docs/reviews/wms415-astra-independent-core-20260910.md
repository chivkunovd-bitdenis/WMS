**Ревью завершено. Общий выпуск четырёх срезов без замечаний не принимаю: в merge остаются два P2, включая встречный порядок блокировок с отменой заказа.** Исправление арифметики stock, отбор Ozon и исправления восьми прежних замечаний к backend возвратов принимаю на уровне независимого чтения кода. Полная WMS-112 остаётся незавершённой.

Проверены именно указанные SHA относительно `bc7f760c11b032fbb94376f042bb6d0f2dd7c13b`:

| Срез | Проверенный SHA | Вердикт |
|---|---|---|
| Stock | `331a0339cfe585d9265550eca46cd27518026e3d` | Прежние блокеры исправлены; полная уборка WMS-338 и приёмка UI не закрыты |
| Ozon352/386 | `cfab77e8d67aab1a978e637e09bfc32d8b93abb4` | Изменение отбора принимаю |
| Merge349 | `9ce9a9952af3e43f68ef3fb7f9cd5204904bcc19` | Потеря движения исправлена; два P2 остаются |
| Cancel111/112 | `1644091b749ec354e0961872faafa3981c7166ac` | Восемь прежних backend-замечаний исправлены; полная WMS-112 не принята |

**1. [P2, merge349 — исправить перед совместным выпуском] Product → Order в merge против Order → Product в отмене.**

В [product_merge_service.py:242](/Users/deniscivkunov/Projects/WMS/.worktrees/wms349-merge-service/backend/app/services/product_merge_service.py:242) merge блокирует обе строки `Product` через `FOR UPDATE`. Затем на строках **283–285** переносит ссылки универсальными `UPDATE`. Этот обход включает `FbsOrder.product_id`: соответствующий внешний ключ находится в `backend/app/models/fbs_order.py:187–190`.

Обратная последовательность есть в отмене:

- [fbs_cancellation_service.py:309](/Users/deniscivkunov/Projects/WMS/.worktrees/wms111-wb-cancel-return/backend/app/services/fbs_cancellation_service.py:309): сначала блокируются родительские строки и сам заказ, затем заказ перечитывается через `FOR UPDATE`.
- Завершение отмены вызывает снятие резерва на **строке 214**.
- [inventory_service.py:155](/Users/deniscivkunov/Projects/WMS/.worktrees/wms338-stock-min-formula/backend/app/services/inventory_service.py:155): общая операция резерва блокирует `Product`, в том числе при `reserve=False`.

**Точная последовательность воспроизведения на двух транзакциях:** взять заказ, связанный с исчезающей карточкой `source`. Транзакция отмены блокирует заказ и останавливается перед снятием резерва. Транзакция merge блокирует обе карточки и доходит до переноса `FbsOrder.product_id`, где ждёт заказ. Продолжить отмену: она запрашивает блокировку `source Product`, которую держит merge. Получается замкнутое ожидание: **merge ждёт заказ; отмена ждёт товар**.

Это конкретный конфликт порядка блокировок, установленный чтением двух путей. **Этот сценарий я не запускал на PostgreSQL**, поскольку ревью запрещает записи в тестовую БД. PostgreSQL при таком цикле прерывает одного участника; это не доказательство потери остатка. Механизм подтверждается [документацией PostgreSQL о взаимных блокировках](https://www.postgresql.org/docs/17/explicit-locking.html#LOCKING-DEADLOCKS). В merge на строках **295–297** обрабатывается только `IntegrityError`; отдельного повтора всей операции после deadlock здесь нет.

Добавленный PostgreSQL-тест проверяет другую конкуренцию — merge против приходного движения. Он этого случая не закрывает. Требуется согласовать порядок блокировок с путями заказов либо обеспечить корректное восстановление операции; новый журнал для этого не нужен.

**2. [P2, merge349 — незакрытое требование канона] Совпадающие уникальные связи дают только общий `merge_conflict`.**

Конкретный вход: у обеих объединяемых карточек есть настройки FBS для одной привязки склада. Ограничение [fbs_binding_stock_pool.py:30](/Users/deniscivkunov/Projects/WMS/.worktrees/wms349-merge-service/backend/app/models/fbs_binding_stock_pool.py:30) требует уникальности пары `(binding_id, product_id)`.

Эта таблица не входит в исключения универсального переноса. Поэтому `UPDATE … product_id=target.id` на `product_merge_service.py:283–285` сталкивается с уже существующей строкой target. Вся операция откатывается на **295–297**, а API возвращает лишь `409 merge_conflict` — `backend/app/api/products.py:915–916`.

Остаток при этом защищён откатом, но оператор не получает причины отказа. Это **унаследованный незакрытый пункт**, а не регресс `populate_existing`: [канон координатора:7576](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/docs/KANONICHESKIY_BACKLOG.md:7576) прямо требует до изменений обработать дубли уникальных связей либо вернуть точный конфликт. Автоматически складывать операторские потолки как физический товар нельзя.

**По stock: исправления `ab0bd727` устраняют причины прежнего отказа.**

Полный исходный stock-review и новый отчёт прочитаны. Между `ab0bd7275aa12f93f0ff42f98b5ca0acb48cc32a` и итоговым `331a0339…` изменены только два документа.

В [fbs_stock_rule_service.py:741](/Users/deniscivkunov/Projects/WMS/.worktrees/wms338-stock-min-formula/backend/app/services/fbs_stock_rule_service.py:741) неизменённый или уменьшенный потолок освобождён от повторной проверки против упавшего свободного остатка. Новое увеличение проверяется по каждому товару и физическому складу на **743–767**. Проверка выполняется после блокировки Product; старые строки потолков перечитываются с `populate_existing=True` на **230–233**. Выключение публикации не позволяет обойти ограничение нового ввода.

Второй блокер также исправлен: [fbs_stock_availability_service.py:73](/Users/deniscivkunov/Projects/WMS/.worktrees/wms338-stock-min-formula/backend/app/services/fbs_stock_availability_service.py:73) больше не возвращает потолки как физически выделенный товар. FBO-доступность на `marketplace_unload_service.py:466–488` вычитает действующие резервы и направления, но не потолок. Список доступных товаров и распределение каталога используют согласованный расчёт.

Прослеженный жизненный цикл сохраняет смысл одного операторского числа:

- Резерв проверяет свободный остаток и создаёт существующие строки резерва; потолок не уменьшает — `inventory_service.py:200–249`.
- Отмена удаляет резерв без прибавки к потолку — **252–265**.
- Недостача проводится физическим движением; отдельной «FBS-недостачи» из потолка больше нет — **268–283**.
- Передача списывает физическое количество — **1668–1703**. Повторное списание ограничено существующим подтверждением движения в WB/Ozon-сервисах.
- Публикация берёт свободное после реальных резервов и последовательно распределяет его между направлениями через `min(share, remaining)` — `fbs_stock_rule_service.py:385–422,963–981`.

Тест недостачи теперь действительно затрагивает прежнюю ошибку: `on_hand=5`, `cap=5`, недостача `3`; проверяются физический остаток и публикация `2`, потолок `5`. Тест полного цикла WB/Ozon проверяет остаток, освобождение резерва, публикацию и повтор передачи. Ozon-проверка нехватки второй позиции теперь проверяет отсутствие **обоих** резервов. Тест конкуренции за последнюю штуку содержит настоящее ожидание PostgreSQL-блокировки.

**Нового арифметического P1/P2 в этом diff не нашёл.** Но полную WMS-338 закрывать нельзя: регистрация `FbsStockPoolDebit` остаётся, как и вызов `consume_fbs_pool` вне units-mode на `inventory_service.py:1679–1691`. WMS-384 объединяет два прежних SQL-предиката, а не все определения наличия правила.

Исправление отправки `units_mode/units_by_warehouse` и снятие ошибочного ограничения в модалке прочитаны. Браузерная приёмка WMS-060 этим ревью не выполнена. Исторически уменьшенные потолки новый код автоматически не восстанавливает.

**По Ozon352/386: требуемая независимость входящих заказов реализована.**

В [ozon_fbs_sync_service.py:503](/Users/deniscivkunov/Projects/WMS/.worktrees/wms352-ozon-publication/backend/app/services/ozon_fbs_sync_service.py:503) поиск привязки сохраняет ограничения tenant, seller, marketplace=`ozon`, внешний склад и `is_active`. Helper на **513–519** проверяет `served`; вызов на **804–808** больше не проверяет публикацию товара. Связь с физическим WMS-складом обязательна в модели.

Отбор одинаков для автоматического и адресного импорта. Настройки публикации этим изменением не включаются. Вызов существующего резервирования сохранён на **928**, а снятие резервов при терминальных статусах — на **703–719**. Тесты охватывают выключенные товарные и складские галки публикации, повтор без дубля и отрицательные случаи чужого tenant/seller/marketplace, неактивного, необслуживаемого и отсутствующего склада.

**P1/P2 в изменении отбора не нашёл.** Резервирование по-прежнему зависит от сопоставленного товара и существующих правил доступности; импорт не обещает успешный резерв при отсутствии товара или остатка.

**По cancel111/112: восемь прежних backend-блокеров исправлены в проверенном SHA.**

Проверка по каждому замечанию:

| Прежнее замечание | Что прочитано в исправлении |
|---|---|
| Передача теряется после отвязки | Helper вызывается до detach: `wb_marketplace_orders_service.py:782–800`, `fbs_cancellation_service.py:197–214`; исходный supply ID сохраняется |
| Разные границы времени | Создание и реестр используют общую проверку `transfer_at <= cancelled_at` |
| HTTP отбрасывает четыре поля | Поля добавлены в `fbs_orders.py:134–137`; есть тест реального HTTP-ответа |
| Дубли при устаревших объектах | Metadata перечитывается под блокировкой заказа до autoflush |
| Ошибка документа ломает отмену | Документ и его ссылка создаются внутри savepoint — точки локального отката; после ошибки заказ перечитывается |
| Недостоверный fallback времени | Первое наблюдение сохраняется как `observed_at`, включая отмены до передачи; batch-время фиксируется до дальнейшей обработки |
| Tracking/shipment теряют payload | Оба передают исходную строку WB и время наблюдения |
| Фильтры используют updated_at | Период, сортировка и total используют сохранённое время отмены; для старых строк остаётся явно обозначенный fallback |

Ключевой участок — [fbs_cancel_return_document_service.py:186](/Users/deniscivkunov/Projects/WMS/.worktrees/wms111-wb-cancel-return/backend/app/services/fbs_cancel_return_document_service.py:186): блокировка и перечитывание маркера, сохранение наблюдения, затем изолированное создание документа на **230–238**. Повтор отсутствующего документа подключён к локальной обработке терминальных отмен.

Прочитанный PostgreSQL-сценарий использует две заранее загрузившие заказ сессии, проверяет реальное ожидание блокировки, одинаковый ID документа, повтор в первой транзакции и неизменность существующих остатков/движений. Тесты ошибок выполняют настоящий ошибочный SQL внутри savepoint, а не только выбрасывают Python-исключение.

Создаётся существующий `InboundIntakeRequest`: `return`, `wildberries`, `draft`, `actual_qty=None`, `posted_qty=0`. Физическая приёмка не вызывается. **Новых самостоятельных P1/P2 сверх описанного взаимодействия с merge не нашёл.**

Полная WMS-112 не принята: в frozen SHA `FbsCancelledAfterPackDialog.tsx:107–111` ещё показывает текущую передачу поставки и подпись «Обновлено»; новые поля не представлены пользователю. Появившиеся во время ревью dirty-изменения `FbsCancelledAfterPackDialog.tsx` и `fbsApi.ts` принадлежат координатору и **исключены из проверки**; frozen UI повторно прочитан через `git show`.

Совместимость stock и отмены по арифметике соблюдена: создание возврата не приходует товар, снятие резерва не меняет потолок. Самостоятельный выпуск cancel-ветки поверх исходного inventory-кода оставит прежнюю мутацию потолка, поэтому этот вывод относится к совместной интеграции со stock-срезом. Новых таблиц, счётчиков, складских эффектов упаковки или навигационных блокеров рассматриваемые изменения не вводят.

Файлы и БД не менял, тесты не перезапускал. `git diff --check` всех четырёх срезов прошёл. Авторские результаты PostgreSQL-прогонов сопоставлены с кодом сценариев; за собственный повторный прогон, CI, браузерную или production-приёмку их не выдаю.