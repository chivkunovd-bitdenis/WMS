# WMS: основной выпуск на production, 09.09.2026

Задачи: WMS-000 (сверка канонического списка), WMS-058, WMS-351, WMS-378,
WMS-379, WMS-381, WMS-382, WMS-383, WMS-392, WMS-396, WMS-400.

Основной выпуск развёрнут около 02:16 МСК из **054897e270533d3e0fc65b651d420b3af8332fce**
(`etalon`, PR201). Проверка сервера сохранена в
[машинном подтверждении](wms-production-20260909-proof.json), время 02:17:27 МСК.
Рабочий адрес — https://wms.sellerfocus.pro; `app.sellerfocus.pro` сейчас показывает
другой продукт, поэтому не использовался для проверки WMS.

## Что выпущено и как проверено

Код приложения равен кандидату `ee12f2fa0afd226708ac38b3505b6ec431c2e8a7`.
Следующие коммиты до merge добавляли отчёты, не изменяя приложение. PR200
объединён в staging как `873f54c4244d746628ca6e12e03a23d777337133`, затем PR201 — в etalon.

Полный CI `34287539602` на ee12 и `34288502184` на e24f1c42 завершился успешно:
каждый дал **2183 passed, 47 skipped, 1 xfailed**, Ruff, mypy, frontend и backlog
зелёные. Полную suite локально повторно не запускали.

Окончательный Opus CLI (`claude-opus-4-7`, effort max, session
`a1007a28-72dd-4d9b-a0ed-1b22d2be7b6d`) завершил 37 ходов за 506 секунд.
Пять дефектов первоначальной независимой Astra исправлены; окончательный
Opus и последующая независимая Astra не нашли нового подтверждённого блокера
в проверенном diff. Ссылки: [Opus](wms-release-opus-final-20260909.md),
[независимая сверка](wms-release-review-adjudication-20260909.md).

На Railway staging backend `bec448d9-616e-42e9-aa89-0bb5e86f3079` и web
`54dc0dd8-099e-44c3-9c47-a4d70020c264` завершились SUCCESS на ee12.
Реальные ручные проверки Chrome и SQL описаны в
[браузерном отчёте](wms-release-browser-20260909.md). Root проверил приёмку
КИЗ с сохранением GS, количества и Excel, единичное проведение, WB создание
поставки и повтор, подбор из конкретной тары, упаковку без изменения остатков,
передачу с ровно двумя списаниями и повтор без новых списаний, свободный доступ
к вкладкам при 0/2 подобранных и 2/2 упакованных. Проверки переключателей,
поиска и выборочной инвентаризации выполнял Huygens; их evidence сохранён
в отчётах, это не личные клики root.

## Production после выкладки

Использован штатный `scripts/deploy/prod-update.sh` в `/opt/wms` на
`root@sellerfocus.pro`, с явным `COMPOSE_PROJECT_NAME=wms_prod`, сначала guard.
Деплой завершился кодом 0. Существующие DB/Redis и их тома сохранены.

У api, celery_worker и celery_beat сверены пути и хеши **427 Python-файлов**
приложения и миграций с Git: отсутствующих, лишних и отличающихся нет.
Все три службы running, restart count 0. Образ работающего web совпал с
собранным образом; его SHA и SHA серверных образов записаны в JSON рядом.
Миграции дошли до единственной головы `20260908_0257`.

HTTPS health вернул `{"status":"ok"}`. Главная FF и seller-страница вернули
200 примерно за 0.386 и 0.379 секунды соответственно. В просмотренном
интервале после 23:16:25 UTC в логах api/worker/beat traceback, error и HTTP5xx
не найдены. Это ограниченная проверка после старта, не обещание отсутствия
ошибок при любой дальнейшей нагрузке.

В production Chrome открыты публичные формы FF и seller; вручную нажато
«Забыли пароль» и показана форма восстановления. Письмо не отправлялось,
пароль не менялся. Прежние найденные сессии для защищённого WMS вернули 401;
защищённый production-FBS в этом проходе **не проверен**. Полный рабочий
сценарий выше выполнен на staging. Копии локальных браузерных данных,
созданные исключительно для этой проверки, закрыты и удалены.

После миграции срок подписки не настроен у существующих организаций:
configured=0, expired=0. Новая проверка подписки их не заблокировала.
SMTP и ЮKassa настройки присутствуют, значения не выводились; реальное
письмо и денежный платёж в этом выпуске не выполнялись. Живые операции
Ozon на клиентском аккаунте и положительная криптопроверка реального ЧЗ
этим отчётом не подтверждаются.

## Восстановление и оставшаяся работа

Перед обновлением сохранены четыре старых образа с тегами
`before-release-ee12f2fa-20260909`. Каталог на сервере:
`/opt/wms/.deploy-backups/before-release-ee12f2fa-20260909/`.
В нём `images.txt`, `previous-git-sha.txt`, `deploy.log`, `verification.json`
и дамп БД `wms.dump` (385 MiB, `pg_restore --list` успешен).
SHA256 дампа: `6af332a852f7a073a7df69aad38a53db1d022c173ac585b35456147482f50098`.
Восстановление дампа не выполнялось; откат данных требует учёта новых
операций склада, поэтому не равен слепому восстановлению старой БД.

Из фиксированных первых 40 код 28 пунктов входит в выпуск, 6 сняты или
объединены, 6 незавершены: WMS-047, WMS-048, WMS-013, WMS-084, WMS-277,
WMS-275. Детальные статусы остаются только в каноническом бэклоге.
Исторические складские/денежные коррекции не выполнялись.

WMS-396 частична: дерево КИЗ, привязка, количество и Excel опубликованы;
True API адаптер `18a0bd2ab1702e7dcdd6bedc09147bccb6ad2e76` сохранён и отправлен
в отдельной ветке `codex/wms396-true-api`, но ещё не включён в этот production.
Нужны его review/CI/staging и отдельное подтверждение реальной интеграции.
Мобильный FBS, локальная сетевая печать и макеты чата в этот выпуск не входят.
Автоматизация продолжения каждые 20 минут остаётся активной.
