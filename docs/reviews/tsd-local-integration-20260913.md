# Локальный совместный контур ТСД, 13 сентября 2026

Это сведения о тестовом контуре, не о внешнем деплое и не о приёмке задач.

API `127.0.0.1:18082` перезапущен из coordinator checkout на `3f1764c3`; он включает сервер WMS-444, независимо проверенный на `9e3929f0`, и ранее проверенные исправления WMS-440. Локальная PostgreSQL `wms_tsd_20260913` переведена с `20260913_0305` на единственную вершину `20260913_0306`. Миграция добавляет шесть nullable-полей, не удаляет строки и не переписывает их значения.

Обновление выполнено в согласованном окне между сценариями Terra440. Первая попытка предварительного резервного копирования не удалась: `pg_dump16` несовместим с сервером PostgreSQL17. Миграция после этого уже была выполнена. Полноценная копия создана **после миграции** совместимым `pg_dump17`: `.local-run/after-wms444-20260913-1136.dump`, 568246 байт. Файл остаётся локальным и не публикуется; не считать пустой файл неудачной первой попытки резервной копией.

После запуска `/health` вернул200. Read-only чтение показало13 приёмок и сохранённые состояния `in_delivery` двух тестовых FBS-поставок `7354349e-645a-40ea-9b7c-45ff8676e790` и `4095c41a-8333-41f6-8279-bbc5034b826b`. Исполнитель получил сигнал продолжать нативную проверку WMS-440. Общий прогон приёмки WMS-444 ещё не выполнен.

Исходящие HTTP-вызовы тестового API ограничены localhost и портами18082/19092/19093. WB и Ozon проверяются локальными имитаторами. Порты и документы WMS-433 не используются.
