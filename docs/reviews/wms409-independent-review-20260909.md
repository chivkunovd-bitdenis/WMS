# WMS-409: независимые Astra и Opus Max review

Проверен frozen commit `3de8963e1852269f773e498a5569e0141e5d4c41`.
Вердикт обеих проверок: **PASS, обязательных исправлений не обнаружено**.
Это разрешённый узкий hotfix видимости ошибок создания приёмки/возврата;
ревью не является подтверждением его deployment или production-приёмки.

Независимо прочитаны diff `App.tsx` и `FfInboundQueuePage.tsx`, все их
относящиеся к созданию callers, загрузчики списка/деталей, обработчик ошибок
API, backend POST и `create_request`/`_request_out`, авторский отчёт и
сохранённый скрипт компонентной проверки с результатом. Код не менялся,
новые тесты и внешние операции этим ревью не выполнялись.

В production caller ошибка бросается до POST при отсутствии склада, а
исключения POST и null ловятся в диалоге. Диалог и выбранные поля остаются;
`opsBusy` после запроса сбрасывается в finally. Успешный POST возвращает
созданный ID даже при ошибке последующего GET списка, который сохраняет
собственную ошибку в `inboundListError`. Открывается созданный документ;
автоматического повторного POST не добавлено. Параметры обычной приёмки и
возврата не менялись. Два void-callback в preview/knowledge по-прежнему
закрывают демонстрационный диалог; production caller возвращает ID либо null.

Авторские `tsc`/build PASS и browser result прочитаны как предоставленное
доказательство. Его browser script проверяет настоящий компонент с
искусственными callback-ответами и без API-записей. Это не полный путь
App → backend и не проверка реального клиента на production.

## Вызов Opus

Использована существующая CLI-конфигурация без чтения секретных settings:
`claude -p --model opus --effort max --output-format json`.
В CLI переданы точный frozen diff и 66282 символа исходников, зависимостей и
авторских проверок. Tools/MCP, Chrome и slash commands отключены; session
persistence выключен. Новые действия в репозитории модель не выполняла.

Фактический reviewer — `claude-opus-4-7`, effort запрошен `max`,
session `a51db797-7345-41b2-ab3a-bec3be64f608`; `subtype=success`,
`is_error=false`, 1 turn, 184094 мс, exit code 0. CLI metadata также содержит
вспомогательное использование Haiku; оно не заменяет результат Opus.
[Исходный JSON результата](wms409-opus-max-20260909.json) сохранён без изменений.
Его SHA-256: `562fd7821211994800244f7dd0da282a6e291c38379b3219fd1972025d9e369d`.
SHA-256 отправленного prompt:
`bd6fbfcf633d0c46d12207f81a2fc2bfdcb9cb8362482a547ce155a43de88c1a`.

Фраза Opus «повторного POST уже не сделает» требует узкого прочтения:
доказан путь successful POST → failed list GET → открытие полученного ID.
Общей серверной защиты от повторного создания патч не вводит; потеря ответа
после серверного commit остаётся прежней границей API. Она не объявляется
новой регрессией этого diff. Гарантий, которых нет в коде, это ревью не даёт.
