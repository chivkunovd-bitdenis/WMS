# WMS-396: действующий доступ и исходное состояние staging для QA

Проверка 09.09.2026, около 00:17–00:20 МСК (08.09, 21:17–21:20 UTC).
Только чтение SQL в транзакциях `SET TRANSACTION READ ONLY`, обычные HTTP GET
и чтение проектных файлов. Аудитор не пользовался браузером root, не менял
прикладные данные, права, пароли или настройки, не выпускал новые токены.

## Почему текущий пользователь видел пустой FBS

`wms-review-1786528687943@example.com` — администратор организации
`WMS System Review 687943`, tenant
`222e0907-4710-4885-adb2-7495931aca62`. Прямой подсчёт в текущей staging-базе
показал **0 FBS-заказов и 0 FBS-поставок**; подписка NULL. Это отдельная
организация, не потеря тестовых документов после выпуска.

Нужная организация **WMS Staging**:
`9c31f3f4-ce62-4c1f-891a-295b278f1e69`. Её существующий администратор
`staging-admin@example.com` имеет действующую сохранённую сессию в локальном
проектном файле `/Users/deniscivkunov/Projects/WMS/.secrets/staging-token.txt`.
Формат — одна строка Bearer/JWT. Содержимое файла не включено в отчёт и не
выводилось при проверке. Существующим значением выполнен
`GET https://web-production-9e7c1.up.railway.app/api/auth/me`: **200**,
роль `fulfillment_admin`, user `00980c4b-c5ae-49c5-8c25-0771436b952f`,
tenant совпал с WMS Staging. POST login и генерация JWT не использовались.

В `frontend/src/api.ts` подтверждён существующий ключ FF-сессии
`localStorage['wms_token_ff']`; `setStoredToken` удаляет старый `wms_token`.
Путь к уже сохранённой сессии и этот контракт переданы root для продолжения
его браузерной приёмки. Аудитор сессию браузера самостоятельно не менял.

В нужной организации также существует `denis.tsd.staging@example.com` с
правами приёмки, упаковки, отгрузки на МП, ячеек и инвентаризации. Новый
пользователь или сброс пароля для продолжения текущей QA не нужны.

## Текущий маршрут действительно ведёт в эмулятор

Через уже доступный Railway SSH использован project
`c28e681d-4535-4c96-ac97-c7b600a7f8e4`, environment
`58a08b66-1290-45a2-8737-e3d7408389e5`, service `WMS`. Это staging-контур,
несмотря на историческое имя environment `production`.

Из работающего процесса прочитано только поле адреса FBS, выведены разобранные
scheme/hostname/port: **http / wb-emulator.railway.internal / 8000**.
`GET /health` из WMS-контейнера вернул **200**. Проверка не опирается на имя
селлера или сам факт наличия Railway-сервиса.

У существующего селлера **Эмулятор WB**
`50110328-fa03-4604-b2e4-8ca27fc8bb41` найден сохранённый WB credential.
Штатный сервис прочитал его для двух GET к уже проверенному emulator-host;
значение runtime credential не выводилось. Ответы:

- `GET /api/v3/supplies/WB-GI-245619454EE7`: **200**, `done=false`,
  `orders=[500007,500038]`, `trbxIds=[]`.
- `GET /api/marketplace/v3/supplies/WB-GI-245619454EE7/order-ids`: **200**,
  тот же состав `[500007,500038]`.

Текущий OpenAPI эмулятора возвращает 200, содержит bulk metadata, но не
регистрирует DELETE `/api/v3/orders/{order_id}/meta` и GET коллекции
`/api/v3/supplies`. Эти ограничения не надо выдавать за регресс WMS или за
успешную приёмку отвязки маркировки. Изоляция Content/FBO API и Ozon в этом
срезе не подтверждалась.

## Конкретная живая тестовая поставка

WMS supply **`4e38cabf-b129-4a28-85b0-3dd3631da6a4`**, имя `FBS 04.09.2026`,
WB **`WB-GI-245619454EE7`**, статус `assembling`, склад
`307c0ccd-9a6b-41df-9180-f8ed68022237`. Это тот же существующий документ,
который root проверял ранее; новый seed не выполнялся.

| Заказ WB | WMS order ID | product ID | Состояние |
| --- | --- | --- | --- |
| 500007 | `7fd74d95-ba1e-4a4b-b714-40990e259d56` | `95aebe2c-5824-4ee7-aa61-8e5e0d609b91` | assembling, pick pending, pack packed, reserved, sticker ready |
| 500038 | `84049d60-2c7d-4352-a520-24c24e38c27e` | `3d6050c5-0d29-4612-9e30-e90ecbf9408a` | assembling, pick pending, pack packed, reserved, sticker ready |

Сохранённой сессией администратора выполнены GET workspace и pick-options,
оба **200**. Workspace: `stage=picking`, `blockers=[]`, прогресс
`picked=0, packed=2, metadata_ready=0, stickers_ready=2, total=2`.

| SKU | Место из pick-options | Доступно для подбора | Физический остаток товара | Строк движений | Резерв FBS | Активные WB-подборы |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `EMU-CAN-PVZ-TRUE` | FBS-VIDEO-01, `43a2a23a-0653-49de-82f5-b60f2ba48530` | 59 | 60 | 6 | 9 | 2 |
| `EMU-NORMAL-B2C` | FBS-VIDEO-02, `37d69433-b9e3-4315-b1e1-0c83c4307721` | 57 | 58 | 8 | 15 | 9 |

Физический остаток, движения, резервы и активные подборы в последних четырёх
колонках — прямые агрегаты **по товару**, включая другие существующие заказы;
это не показатели только данной поставки. Они переданы root как before-снимок.
Сам аудитор не проверял результат будущего клика упаковки или подбора.

У этого synthetic-селлера всего **55 заказов и 17 поставок**, включая **20 новых
заказов**. Для следующих сценариев не нужно засевать другого пустого tenant.
Существующие неначатые поставки также есть, например
`9c532b61-fc72-4c8f-a53f-8fe24cb844e6` / `WB-GI-6D0C042DEBBE`
с двумя заказами pending/pending. Её состояние на стороне эмулятора отдельным
GET здесь не проверялось; для первого сценария уже сверена поставка 4e38.

GET списка приёмок администратора вернул **43 документа**. Старые активные
приёмки других селлеров не выбраны как разрешённые объекты для проведения:
для WMS-396 основной задаче следует использовать согласованный тестовый
документ. Ни один из найденных документов аудитом не изменён.

Практический путь продолжения: root восстанавливает найденную существующую
FF-сессию на staging, открывает FBS селлера «Эмулятор WB», начинает с уже
сверенной 4e38 и сопоставляет результат ручных действий с этим снимком.
Готовность маркировки и передачи не следует из доступности workspace:
`metadata_ready=0` зафиксировано явно. UI и итог операции принимает основная
задача, этот отчёт подтверждает доступ, данные и маршрут к эмулятору.
