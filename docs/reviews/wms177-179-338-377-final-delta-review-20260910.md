**На frozen `e3aac1d3860cf9abd09469409c9a7bdd545fceac` оставшихся actionable-замечаний в указанном срезе не нашёл. Исправления принимаю на уровне проверенного кода.**

Три прежних замечания к деплою закрыты:

- В [deploy.yml:40](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/.github/workflows/deploy.yml:40) ошибка `git show` теперь завершает отдельное присваивание ошибкой под `set -e`. Следующий `bash -c` получает текст скрипта аргументом и пустой stdin.
- В [prod-update.sh:88](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/prod-update.sh:88) stdin `pg_dump` отдельно перенаправлен из `/dev/null`; чтение архива для `pg_restore` сохранено. Порядок остановки обработчиков, backup, проверки архива и миграций не изменён.
- В [prod-update.sh:120](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/scripts/deploy/prod-update.sh:120) ошибка `docker ps` передаётся основному скрипту через присваивание и больше не маскируется. Успешный пустой список безопасно пропускается; ошибка `docker stop` также завершает скрипт. Фильтры проекта и сервиса `web_seller` сохранены.

Исправление исходного замечания о запоздалом `load` также принимаю в ограниченном объёме. В [FfFbsSupplyWorkspace.tsx:408](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:408) поколение открытия меняется при смене `open`/UUID и очистке эффекта. Уже начатый запрос сохраняет своё поколение; устаревший ответ не меняет `workspace`, этап, ошибку и `busy` — проверки стоят на строках 422, 433 и 435. Сброс `busy` в эффекте открытия на строке 443 закрывает зависание индикатора при повторном открытии с начальными данными, когда новая загрузка не запускается. В этом diff нет изменений складской механики упаковки или блокировки навигации по прогрессу.

Самостоятельно выполнил **два сценария на точных строках bootstrap из frozen workflow**: ошибка подставного `git show` возвращает `17`; чтение stdin внутри полученного скрипта не поглощает его команды. Оба прошли, как и `bash -n` замороженного deploy-скрипта. Код семи целевых deploy-тестов и трёх FBS-регрессий прочитал, но эти наборы сам не запускал. FBS-тесты исполняют извлечённый callback и вручную меняют поколение; **жизненный цикл React и браузер этой проверкой не подтверждены**.