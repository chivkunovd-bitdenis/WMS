# WMS-277 / WMS-396 / WMS-394 / WMS-395 / WMS-403 / WMS-404 / WMS-401: второй выпуск 09.09.2026

Production обновлён в 04:04 МСК из **40b722a4d2d0ff1a6a5899cdb2713a98d1265bc5**
(PR204 → etalon). Это следующий выпуск после 054897e2, а не утверждение о
завершении всех задач. Прикладной код backend/app, frontend/src и миграции
сравнены Git diff с 7259d630: отличий нет.

В выпуск входят True API для проверки ЧЗ на приёмке, часовой WB-каталог,
необязательный выбор конкретного заказа для мобильного подбора и точечные
исправления сканера: повторный QR, сброс/Escape с возвратом фокуса, доступ
к отмене отвергнутого операторского КИЗ и видимость отказа WB.

## Проверки до выпуска

CI34296798983 на d0a23577333999251a3e6d7b42301e225ac49dd5: 2226 passed,
48 skipped, 1 xfailed; frontend, Ruff, mypy и канонический бэклог успешны.
Единственная поздняя правка — детерминированный тест публикации: фоновая
задача уже подтверждает ноль, повторная синхронизация обоснованно ничего
не публикует. Приложение ради этого теста не менялось.

Завершены Opus CLI max, независимая Astra и отдельная итоговая сверка:
[wms-followup-adjudication-20260909.md](wms-followup-adjudication-20260909.md).
Реальный staging-браузер и пределы проверки:
[wms-followup-stage-20260909.md](wms-followup-stage-20260909.md).

## Проверка после выпуска

В 04:12 МСК сверены все 428 Python-файлов api, worker и beat с Git:
нет отличий, пропусков и лишних файлов. Все четыре обновлённых сервиса
running, перезапусков 0. Образ запущенного web совпал с собранным образом.
Миграция осталась единственной головой 20260908_0257. DB и Redis не пересоздавались.
[Машинное подтверждение](wms-followup-production-20260909-proof.json).

Настоящий API health **/api/health** вернул 200 и {"status":"ok"} за 0.033с.
FF и seller SPA вернули 200. Путь /health без /api возвращает HTML приложения,
поэтому не используется как доказательство исправности API.

После запуска до примерно04:14МСК в api и beat ошибок не найдено. Worker
работает, но WB вернул 401 для двух селлеров в трёх циклах опроса (12 строк
ошибок, без traceback). Причина и наличие этих ошибок до обновления ещё
проверяются; нельзя утверждать, что весь журнал чист или что это вызвано релизом.

В настоящем Chrome открыты FF и seller, нажато «Забыли пароль», форма
восстановления отрисовалась без pageerror. Письмо не отправлялось. Защищённый
production-FBS в этом проходе не открыт: доступна форма входа. Рабочие сценарии
выполнены на staging, это отдельно от production-приёмки.
[Браузерное подтверждение](wms-followup-production-browser-20260909.json).

Положительный ответ настоящего ЧЗ не принят: у учебного селлера отсутствует
существующий CZ-токен, проверка честно показывает причину и экспортирует
проблемные коды. Первый фактический часовой WB Content запуск ещё требуется
подтвердить. Приложение ТСД и локальная сетевая печать этим выпуском не приняты.

## Восстановление и следующий срез

Backup /opt/wms/.deploy-backups/before-release-7259d630-20260909/: 385MiB дамп,
pg_restore --list успешен, SHA256
e3f2606a8e801fe4163043f3c4fda84e65cc2c465a9bd965b166ee39218835ae,
четыре тега прежних образов, предыдущий SHA054897e2. Слепое восстановление
старой БД после новых операций склада не является допустимым обычным откатом.

Следующий PR205 содержит WMS-084 перепривязку физического КИЗ и WMS-058 точный
источник MP-короба. Четыре падения CI исправлены test-only (503a80f7,7b997689),
все четыре целевых сценария прошли, отдельная Astra сверила их с реальным
контрактом64de7f82 и не нашла ослабления проверок. Приложение не меняли ради
старых ожиданий тестов. После повторного CI нужен новый staging и браузер.
Финальный Opus этого следующего среза ещё не завершён: CLI usage до06:10МСК.
Он не включён в текущий production40b722a4.
