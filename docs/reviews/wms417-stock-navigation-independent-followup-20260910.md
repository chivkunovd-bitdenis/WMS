**По frozen `719ed85089cbca02a8e35e34af5e04b421a3f001`: stock-исправления `705d2af6` принимаю в проверенном объёме кода. В навигации остаётся один P2 — запоздалая загрузка может подменить открытую поставку. P1 в этом узком срезе не установил.**

**P2 — ответ загрузки предыдущего UUID перезаписывает текущую поставку.**

В [FfFbsSupplyWorkspace.tsx:414](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:414) после `await fetchFbsWorkspace(...)` безусловно выполняется `setWorkspace(next)`. Проверки актуальности UUID или конкретного открытия окна нет. Эффект открытия на строках 433–469 сбрасывает состояние, но не отменяет и не исключает применение предыдущего ответа. Очистка таймера на строке 484 останавливает последующие опросы, уже начатый запрос продолжает выполняться.

Точный сценарий:

1. В поставке A на вкладке коробов начинается фоновая загрузка.
2. Оператор через Back возвращается к списку и открывает B.
3. Ответ B приходит первым — окно показывает B.
4. Поздний ответ A вызывает `setWorkspace(A)`: URL и `supplyId` остаются B, содержимое становится A.

Я воспроизвёл этот порядок **в памяти на callback, извлечённом из frozen-кода**, с отложенными синтетическими ответами: конечный `currentSupplyId=B`, `workspace.supply.id=A`. Это не браузерное воспроизведение.

Проверка начальных данных в [FfFbsOrdersScreen.tsx:1150](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1150) правильно исключает чужой `workspaceSeed`, но не защищает от этого позднего ответа. Последствие существенно: например, сохранение даты на строке 595 использует уже подменённый `workspace.supply.id`.

**Нужное исправление:** привязать результат загрузки к актуальному UUID и конкретному открытию окна; после смены или закрытия окна игнорировать прежний результат, включая его изменения `workspace`, этапа, ошибки и состояния загрузки. Проверить порядок ответов **B → A**. Запирать навигацию на время запроса для этого не требуется.

Остальные проверенные исправления принимаю на уровне кода:

- **Коллизия WB123/Ozon123 устранена.** [FfProductsFbsPage.tsx:83](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:83) приводит ключи правила к формату с площадкой, а строки складов используют тот же формат. Собственное исполнение frozen-функций с подменённым `fetch` подтвердило: изменение WB5 → 4 формирует и в single, и в bulk ровно `{"wb:123":4,"ozon:123":3}`. Неоднозначный старый ключ `"123"` явно отклоняется.
- **Ложный прогноз публикации убран.** В [FbsStockDialog.tsx:157](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:157) используется серверное `savedPublishedNow`. Подпись на строках 531–535 прямо отделяет сохранённый расчёт при последней загрузке от текущего ввода. Перенос серверного `2` и сохранение отсутствующего значения как `undefined` проверены собственным исполнением функций. Обещания прежних `3 + 3 = 6` из общей суммы в диалоге удалены.
- **Прежний обход подтверждения при Back/Forward закрыт кодом блокировки перехода.** [App.tsx:354](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/App.tsx:354) проверяет актуальные признаки несохранённого ввода до изменения маршрута; отказ вызывает `reset`, согласие — `proceed`. Callback передачи dirty-состояния стабилен.
- **Закрытие вложенных окон FBS защищено.** [FfFbsSupplyWorkspace.tsx:390](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:390) сначала спрашивает подтверждение и только после согласия очищает распределение или выбранные заказы. Эти обработчики подключены к закрытию диалогов и кнопке отмены добавления заказов.

В [main.tsx:12](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/main.tsx:12) сохранены существующие `App`, тема WMS и оболочка; замена роутера обеспечивает блокировку переходов. Новых продуктовых страниц или переработки оформления в проверенных production-изменениях нет. Новые проверки навигации опираются на несохранённый ввод, а не на прогресс упаковки.

**Граница результата:** это read-only проверка frozen-кода и два собственных сценария исполнения функций в памяти. Браузер, реальные HTTP-запросы, авторские тестовые наборы и deploy я не выполнял. Для принятия навигационного исправления нужен указанный сценарий позднего ответа, затем проверка Back/Forward и вложенных диалогов в существующей оболочке. Правок, Git-изменений, установки зависимостей и запуска дочерних агентов не было.