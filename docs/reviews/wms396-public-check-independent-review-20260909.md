# WMS-396 — independent read-only implementation review

**PASS: no blocking defect found** in source `4f124cfa646431aa6ae4a1d949bcfdb8155eab1b`. The source checkout and remote branch `codex/wms396-public-check` were verified at that exact SHA. The implementation diff against parent `1ff40469` contains only `inbound_marking_service.py`, the new `public_marking_check.py` helper and their two targeted test files. Compared with production base `c3aa4dfaccbc99c6d58cf9e5a864bc248cc8ae3f`, the additional file is the preceding authorized canonical-backlog update. No frontend, schema migration or stock code changed.

The public transport sends the stored full CIS as the single JSON `code` field, retaining GS separators and the cryptographic suffix. The receiving service no longer imports or calls the seller-token lookup and does not project the code to a short identifier. It uses a default-verifying httpx client with a ten-second timeout and the fixed mobile-check URL; there is no endpoint fallback or HTTP retry. HTTP failures, including 451, do not create a positive code verdict. Raw response and numerical HTTP status are retained when a response exists; network exceptions produce unavailable status.

The response interpreter requires literal boolean `checkResult` and `codeResolveData.verified`. A positive verdict additionally requires `outerStatus=INTRODUCED` and no conflicting explicit status/warning. Present optional `codeFounded` and `isBlocked` fields must also be literal booleans. Missing required structure, nonboolean truthy strings/numbers, unknown statuses and malformed successful bodies do not turn green. Explicit unverified, blocked or not-found responses and known nonintroduced lifecycle statuses receive problem descriptions. Current HTTP 451 observations remain an external availability problem; the implementation is not represented as a successful live code verification.

The background job is claimed under a row lock only from pending state. Each external request starts after its database session has closed. Before writing its result, the worker reacquires the receiving-request lock and checks that its job is still running, then locks the event and checks its tenant and pending status. An expired worker therefore cannot overwrite the replacement job's pending snapshot. Deleted events are skipped; newly scanned pending events are drained by the existing outer loop. The exception cleanup also checks for a newer active job before changing pending snapshots. Existing receiving capacity, deletion, lifecycle and inventory operations are outside the implementation diff.

Request pacing uses the existing process-local MarketplaceBackoff with a two-second interval. In one asynchronous event loop, there is no await between observing an expired interval and reserving the next interval. It is deliberately not a shared rate limit across multiple processes; this review does not claim a global provider quota. No additional persistent queue or counter was introduced.

The reviewer read the changed tests for full GS/no-auth requests, malformed and negative results, HTTP errors, no open database session across HTTP, idempotent re-execution, unchanged inventory-movement count, deletion/rescan draining and expired-job replacement. Tests were not rerun, as requested. The author's reported result is 61 passed with four PostgreSQL-only cases skipped; those skipped row-lock cases are not represented as runtime concurrency proof. The author also reported Ruff on four changed files and Mypy on the two services passing.

This review made no application, database, deployment, browser or credential changes and launched no Opus run. It is a code-review verdict, not staging or production acceptance. Detailed record: [review JSON](wms396-public-check-independent-review-20260909.json).

## Subsequent PostgreSQL-only test evidence

The four previously skipped row-lock cases now have a separate saved passing run for the same source `4f124cfa646431aa6ae4a1d949bcfdb8155eab1b`. The reviewer opened the runner's existing pytest output, JUnit XML and run metadata; no tests were repeated. The XML records four tests, zero failures, zero errors and zero skips, with 4.954 seconds total test time. The cases cover concurrent WB/Ozon external-code binding and receiving capacity/duplicate serialization.

The runner used an isolated localhost PostgreSQL database (PostgreSQL 17.10 as reported by mobile_resume). The saved run metadata records `pytest_exit=0`, `drop_exit=0` and `owned_database_remaining_count=0`; it also pins the source SHA. Thus the original four skips are now covered by actual local PostgreSQL results. The checked CI workflow does not start a PostgreSQL service, so this evidence remains separate from the ordinary CI run.

The three artifacts were inspected before copying: they contain test names, outcomes, local paths and the disposable local database identifier, with no credentials, real CIS or customer payload. Preserved evidence: [pytest output](artifacts/wms396-pg-only-20260909/pytest.txt), [JUnit XML](artifacts/wms396-pg-only-20260909/junit.xml), [run and cleanup metadata](artifacts/wms396-pg-only-20260909/run.json).
