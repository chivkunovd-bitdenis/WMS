# WMS-443 C5 — адресная приёмка исправления журнала, 13–14.09.2026

**PASS.** Проверено только исправление7435c2f53a6a340ec7b28b322e5d88b1b7139cf6 с адресным тестомe39673159b80822223cfb2812d41d3fafbbed4b1; общего review не повторяли. API18084 перезапущен после функциональногоfix; web5204 использует его, APK12/source117de6d7. Production этим прогоном не проверяется.

Новый synthetic документ7445ef2a-77fe-4206-9367-be562f0b0c36 (№000004), один товар3шт. На native Final Astra5da311 разместил1 в FINAL-JOURNAL2-A-5da311. Затем штатный парольный вход по ФИО Final Boris Split5da311, PIN только своего профиля, **Home B проверен до действия**; B разместил оставшиеся2 в FINAL-JOURNAL2-B-5da311. Native очередь пуста; web карточка того же документа показывает «Оприходовано», остаток0 и ячейки1/2.

В существующем web «Ячейки» журнал показывает A1 (13.09 23:59) и B2 (14.09 00:01), правильные ФИО, источник «Сортировка» и соответствующие ячейки. Первая строка не стала строкой B. Изображение web-history.png просмотрено; web ответ map содержит те же строки.

Дополнительно выполнен ровно один HTTP replay operation_idA с тем же product/location/quantity под сессией B. Ответ200/done. Полный журнал before/after совпал, ID/автор/количество A не изменены, второе событие не создано. После replay в БД ровно один inbound ledger этого документа, quantity3, amount3000коп. Полный ответ/журнал находятся в replay-readback.json, итоговые ledger/events — final-db.json. Никаких повторных физических размещений завершённого документа не выполняли.

| Проверка | Вердикт |
|---|---|
| WMS-443 R5/R8/C5: native A/B → существующий web журнал | PASS. Два фактических действия, две строки с соответствующими ФИО; новое B не меняет A. |
| Replay и начисление | PASS. Старое намерение A под B возвращает прежний результат, журнал неизменен, ledger один3×10=30руб. |
| Новые экраны/таблицы/поля document_id | Не добавлялись; используются существующие события и журнал. |
| Остальные составные проверки/физический принтер/реальный Виталик | В этом проходе не выполнялись, прежние границы сохранены. |

Первый ускоренный повтор на документе7fa09644-a979-4fbd-a782-08087596d3c1 ошибочно остался под A из-за выбора профиля автоматизацией. Он корректно создал две строки A и **не засчитан** за C5. Его fixture и журнал сохранены как first-attempt-same-actor.json / first-attempt-journal.json. После проверки Home выполнен отдельный корректный новый A/B пример выше. Старые завершённые документы не исправлялись и не backfill-ились.

Артефакты: fixture.json и seed_split.py — подготовка нового документа/ячеек с существующими собственными A/B; a-after.png, b-home.png, b-before.png, b-after.png — native этапы; web-document.png/.txt, web-history.png/.txt и web-responses.json — фактическое web чтение; journal-after-a.json — строка A до B; replay-readback.json/final-db.json — точное неизменное состояние после B и replay. Пароли/токены в артефакты не включены, скрипт web получает пароль через переменную окружения.

Адресная оценка отдельного CI-fix7300ec54: `_map_order` использует `ctx.get('packed_positions', {}).get(position.id, 0)`; штатная сборка контекста по-прежнему наполняет packed_positions реальным количеством. Поэтому обычный результат не меняется, lightweight контекст без ключа не падает с KeyError; фильтры/tenant/seller/pagination не изменены. PASS чтением дельты, без нового общего review. Разделение seller plan/fact подтверждено чтением test_seller_plan_is_not_ff_fact: created_by_seller_id задан, expected10 не превращается в actual; до верификации complete отвергается. Тест не удалён и не ослаблен заменой устаревшего FF-only ожидания.

Приёмка этого пробела завершена. Новых сценариев после фиксации не запускать; дальнейшие CI/деплой ведёт основной агент.
