# WMS-440 — PUBLIC11 → APK13, 14.09.2026

Astra выполнила один адресный native проход на отдельном emulator-5582 / `wms440_upgrade13`, Android35. Реальное устройство владельца и existing5580 не изменялись; образ не скачивался. Первый запуск software GPU был медленным/показал System UI ANR; один перезапуск с GPU host сохранил userdata и установленный11. Это ограничение тестовой среды, а не отказ APK-подписи.

## Исходный и конечный артефакты

- PUBLIC11: `WMS-TSD-0.1.10-8435c3abcd24-debug.apk`, SHA256 `8435c3abcd240afbaadb95e157512d52a56c105fd73440e6991933741237a81f`, ru.wms.tsd/code11/name0.1.10-mobile-recovery/minSdk24. SHA исходного файла проверен Astra; первоначальная установка на чистый AVD через adb Success.
- Candidate13 / публичный asset: [WMS-TSD-0.1.12-android7-update-f81d8be26298-debug.apk](https://github.com/chivkunovd-bitdenis/WMS/releases/download/tsd-preview/WMS-TSD-0.1.12-android7-update-f81d8be26298-debug.apk), SHA256 `f81d8be26298839720becb08341ee8152dd5754c3ccfec6e3af01c857a8164f0`, 45,576,192 байта, ru.wms.tsd/code13/name0.1.12-android7-update/minSdk24. Mobile source локально сохранён Terra в `9202edd513efed59f35f5e025d9c0a545f8c3ee8`; mobile history не публиковалась.
- По адресному artifact proof Terra: прежний certificate SHA256 `e343ab0cccc6284b271a72da42a67b14a0f0893d92b9ca461f1cac3d0be1aeb6`; apksigner API18–23 проверяет v1=true, API24–35 проверяет v2=true; JAR подпись присутствует и проверяется. Общий headline apksigner выбирает сильнейшую подходящую схему и сам по себе не означает отсутствия v1.
- После разрешённого переключения preview manifest Terra и координатор анонимно подтвердили code13/url/hash/size/minSdk; manifest SHA256 `b271a9749a3466362f6af6562711707ba7cb8d91aeeba77cc15f4cfa304e60b8`. Эти внешние сверки выполнены ими; Astra подтверждает фактическую загрузку и проверку старым приложением через публичный адрес.

## Фактический UI путь

1. В PUBLIC11 через штатный вход создан собственный synthetic профиль `a@b.co` на локальном HTTP fixture `http://10.0.2.2:18086/`; тестовый PIN установлен и повторно подтверждён. Это изолированный ответный сервер, не реальная учётная запись. Home показал профиль: `home11.xml/png`. Незавершённая складская очередь не создавалась.
2. Home → «Обновление приложения» → «Проверить обновление» показало13 (`offer13.xml`); нажата «Скачать обновление». После одной загрузки исходное приложение11 показало «Обновление скачано и проверено» (`downloaded.xml`). Candidate не устанавливался через adb.
3. «Установить обновление» → стандартное Android-разрешение установки из этого приложения → повторное нажатие штатной кнопки → системное «Do you want to update this app?» (`android-confirm.xml/png`) → Update. Защита подписи ОС не отключалась, uninstall/clear-data не выполнялись.
4. PackageManager подтвердил versionCode13/name0.1.12-android7-update (`installed13.txt`). SHA256 зашифрованного `shared_prefs/auth_store.xml` до и сразу после установки одинаковый (`prefs-before.sha256`, `prefs-after.sha256`); сам файл с содержимым не публикуется.
5. В13 адрес сервера остался `http://10.0.2.2:18086/` (`server13.xml/png`). Вместо выбора старого email-профиля показана новая форма ФИО/код организации/«Вход по прежнему логину» (`after13.xml`). Старый PIN-вход не доступен на показанном экране; повторный вход не выполнялся. Это соответствует ранее обнаруженной фильтрации legacy-профилей до новой идентификации, но не является доказательством сохранения прежнего PIN-доступа.

## Вердикт и границы

**PUBLIC11 → штатная загрузка/проверка → системный installer →13 на API35: PASS. Сохранность файла авторизации и server URL: PASS. Прежний PIN-доступ: NOT PASS в проверенном переходе; полное обещание C20 не закрыто.** Потери файла данных не обнаружено. Старые legacy-профили и доступ к ним после повторной идентификации требуют отдельного решения координатора; текущим тестом это не подменяется.

Реальный Android7/OEM не проверен; гипотеза archive parser физического отказа не считается воспроизведённой. C23 остаётся владельцу. Очередь, дубли операций/начислений и физическая печать в этом delivery проходе не проверялись. Складские проверки пакета не повторялись.
