**Вердикт: изменения в целом направлены по контракту владельца, но безусловно разрешить выпуск этой ветки не могу.** Удаление расходования потолка выполнено; остаются несовместимые с постоянным потолком проверки сохранения и расчёты доступности для FBO, а один новый регрессионный тест не проверяет заявленную ошибку.

Проверил семь коммитов между `bc7f760c11b032fbb94376f042bb6d0f2dd7c13b` и `453bb87d9331209908235ab14bcfe9a10b903372`. Все номера строк ниже относятся к **проверяемому HEAD**, прочитанному через Git; ссылки на неизменённые функции приведены как контекст вызывающего кода. Файлы и данные не менял.

1. **До выпуска: сохранение неизменённого потолка отклоняется после штатного уменьшения свободного остатка.**
   Изменённый участок `backend/app/services/inventory_service.py:205–226` сохраняет потолок и создаёт резерв по физической доступности. Поэтому нормальное состояние теперь — потолок 5, свободно 4.
   Но `backend/app/services/fbs_stock_rule_service.py:729–780` повторно проверяет всё правило при каждом сохранении; `_validate_units()` на строках `169–172` возвращает `units_sum_exceeded`.
   Я исполнил функции непосредственно из HEAD в памяти: `split_amounts(cap=5, free=4)` возвращает 4, а проверка того же правила с потолком 5 отказывает.
   Это блокирует повторное сохранение и сопутствующие изменения при сохранении прежнего потолка. Нужно отличать ввод нового/увеличенного числа от сохранения уже установленного; ограничение нового ввода реальным остатком сохранить.
   В коде диалога имеется аналогичная проверка `unitsSum > base`: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:170–172,219–224`. **В браузере её не проверял.**

2. **До выпуска: оставшиеся потребители трактуют постоянный потолок как занятый товар.**
   Изменённый тест `backend/tests/test_fbs_stock_rule_service.py:756–768` закрепляет доступность `250 − 1 − 300 = −51` и объясняет её недостачей.
   Причина прочитана в коде: `backend/app/services/fbs_stock_availability_service.py:85–98` суммирует исходные потолки; `backend/app/services/marketplace_unload_service.py:487–495` вычитает эту сумму вместе с действующими резервами.
   Это число используется для отказа в операции на `marketplace_unload_service.py:519–540`; оно не является исключительно пояснением на экране.
   Постоянный потолок может законно превышать текущий свободный остаток — именно для этого существует `min()`. Само превышение не доказывает физическую недостачу.
   До выпуска нужно согласовать этот расчёт с контрактом потолка публикации. Закрепление отрицательного результата новым ожиданием теста не устраняет несовместимость. Реальные резервы заказов при этом должны остаться.

3. **`ea472236`: удаления корректны, но их четыре, а не пять.**
   Резерв: `backend/app/services/inventory_service.py:205–210` — `if pool is None: ... NOT_PUBLISHED`; строки `217–226` — проверка `available < quantity`. Прежнего `pool.quantity -= required[pid]` нет.
   Отмена: `inventory_service.py:259–260` — `for reservation in reservations: await session.delete(reservation)`. Прежнего прибавления `reservation.quantity` к потолку нет.
   Недостача: `inventory_service.py:305–311` — `take = min(remaining, pool.quantity)`, добавление атрибуции и `remaining -= take`. Присваивание `pool.quantity -= take` удалено.
   Передача без резерва: `inventory_service.py:1700–1707` — блокировка товара и проверка принадлежности заказа; прежний `pool.quantity = max(0, pool.quantity - unreserved)` удалён.
   Физическое списание сохранено: `inventory_service.py:1724–1735`, вызов движения с `quantity_delta=-quantity`.
   Пятый пункт — **изменение условия резервирования**. При существующем пуле заказ теперь может резервироваться и при нулевом потолке, если физически свободного товара достаточно. Для потолка публикации это семантически последовательно.
   Проверка принадлежности заказа также расширена: теперь действует при любом режиме товара, если передан `fbs_order_id`, а не только в режиме штук.

4. **Оставшиеся записи `quantity` существуют, но в найденных путях настройки пользователем.**
   `backend/app/services/fbs_stock_rule_service.py:882,896,904,907` — сохранение правила, создание строки и переключение режима.
   `backend/app/services/fbs_warehouse_binding_service.py:769,776` — ручная установка через API `backend/app/api/fbs_sellers.py:609–636`.
   SQL-обнуления остаются в `backend/app/services/catalog_service.py:1335,1439` и `fbs_stock_rule_service.py:955`: очистка старого лимита, команда «из остатка» и явный сброс старых ограничений.
   Их вызовы проверены: `backend/app/api/products.py:1494,1780`, `backend/app/api/fbs_stock_rule_reset.py:75`. Это отдельные пользовательские команды, поэтому утверждение «пишет только одна функция сохранения» неверно.
   Других фоновых присваиваний этому полю в просмотренном `backend/app` не обнаружил; в `inventory_service.py` их больше нет.

5. **Публикация `min(cap, free_stock)` сохранена; сама формула находится в сервисе правил.**
   `backend/app/services/fbs_stock_availability_service.py:239–252` вычисляет свободное как физический остаток минус существующие резервы, без вычитания потолка.
   `backend/app/services/fbs_stock_rule_service.py:996–1009` передаёт это свободное в `split_amounts`; строки `407–422` читают потолок и выполняют `amount = min(share, remaining)`.
   Для нескольких направлений действует последовательное распределение общего свободного остатка: последующие направления получают оставшееся. Это прежнее поведение, не новая независимая публикация полного `min(cap, free)` каждому складу.
   WB получает рассчитанные количества через `backend/app/services/fbs_stock_sync_service.py:220,276–294,699–708`. Положительное значение не отбрасывается только потому, что совпало с предыдущим.
   Дополнительного занижения публикации из-за прекращения расходования потолка в этой цепочке не нашёл. Исключение — уже уменьшенные исторические значения, описанные ниже.

6. **`8f748ccb`, WMS-384: строго эквивалентная замена в обоих изменённых местах.**
   `backend/app/services/fbs_stock_rule_service.py:42` возвращает ровно прежнее `or_(Product.fbs_percent.is_not(None), Product.fbs_units_mode.is_(True))`.
   Вызовы: `backend/app/services/fbs_stock_sync_service.py:190–193` и `backend/app/services/fbs_warehouse_binding_service.py:327–333`; соседние ограничения запросов сохранены.
   `_has_rule()` остаётся отдельной, более точной проверкой. Новый помощник ей не эквивалентен и в данном diff её не заменяет.

7. **`ac79e809`, WMS-060: состав запроса исправлен для обоих вариантов сохранения.**
   `frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:207–208` безусловно добавляет `units_mode: rule.unitsMode` и `units_by_warehouse: rule.unitsByWarehouse`.
   Одиночный PUT использует это тело на строке `215`, массовый — `{ product_ids: productIds, ...body }` на строке `222`; обработчик экрана подключён на строке `265`.
   Поэтому прежнее отсутствие полей исправлено. Полный пользовательский сценарий ещё нельзя считать подтверждённым: остаётся описанный отказ сохранения, браузерную проверку я не выполнял.

8. **`e3260f78`: тесты вызывают операции, но покрытие слабее заявления автора.**
   В `backend/tests/test_inventory_stock_cap_wms338.py` после операций действительно выполняются чтение поля из БД и `assert cap == OPERATOR_CAP`: резерв — `181–185`, отмена — `207–217`, недостача — `251–260`, передача — `287–300`.
   Однако тест недостачи использует `on_hand=10`, потолок 5 и недостачу 3 (`234,255`): обычного свободного запаса 5 хватает, поэтому старое присваивание вычитало бы **ноль**. Этот тест пропускает возвращение удалённой ошибки; нужен сценарий, реально затрагивающий FBS-часть.
   Резерв и отмена не проверяют наличие/удаление строк резерва или изменение свободного остатка. Тест передачи проверяет потолок, но не физическое списание.
   Передача и `apply_fbs_supply_write_off` здесь — один тест, не два независимых пути. Пятый тест проверяет чужого арендатора (`323–332`), шестой — прямой вызов формулы с заранее заданным свободным остатком (`359–372`).

9. **`f5b602f4`: часть старых ожиданий исправлена правильно, часть закрепляет проблему.**
   Полезная проверка сохранена в `backend/tests/test_fbs_pr140_shipment_write_off.py:574–586`: повторная передача и отмена, `assert [p.quantity for p in pools] == [4 for _ in quantities]` плюс проверка единственного физического списания.
   `backend/tests/test_fbs_stock_rule_service.py:637–643` проверяет сохранение потолка и изменение публикации после недостачи/приёмки.
   Ожидание `−51` требует пересмотра по пункту 2. Ozon-тест на строках `895–915` проверяет статусы и один потолок, но не доказывает заявленную атомарность: отсутствует проверка, что при нехватке второй позиции первая не зарезервирована.
   Не хватает сценариев сохранения прежнего потолка после продажи, конкурирующих резервов на последнюю штуку и полного цикла «резерв → передача → снятие резерва → пересчёт публикации».

10. **Ozon, документация и переход данных.**
    Отдельного нового дефекта Ozon в изменённых путях не обнаружил: общий резерв проверяет физическую доступность; очистка использует эквивалентный предикат; публикация берёт общий расчёт на `backend/app/services/ozon_fbs_sync_service.py:377–400`. Общие замечания выше распространяются и на Ozon.
    WMS-352/386 считаю незакрытыми по предоставленному вами факту, повторно этот вопрос не расследовал.
    `0a251bc6`: отчёт неточен — `docs/reviews/wms338-stock-fix-2026-09-10.md:19–23` заявляет пять мутаций, а строки `115–127` завышают доказательность тестов. Раздел `156–164` не доказывает исправление WMS-352/386.
    `453bb87d`: только замена символов минуса в комментариях теста на строках `694–697,756–757`; поведение не меняется.
    Изменений схемы и Alembic-миграций в семи коммитах нет. В отчёте нет решения о восстановлении данных или откате; утверждать, что миграцию рассмотрели и сознательно исключили, оснований нет.
    Если старый код уже уменьшил потолок 5 до 4, новый код сохранит 4 и не восстановит исходные 5. Нужна проверка затронутых значений и подтверждённое оператором восстановление; наличие таких строк на production я не проверял.
    Откат к базе возвращает расходование и прибавление потолка при отмене. Его нельзя считать семантически безопасным для резервов, созданных новой версией.

`git diff --check` прошёл. Выполнил только изолированную проверку арифметических функций в памяти; pytest, сборку фронта, браузер и обращения к production не запускал. Рабочее дерево осталось без моих изменений.

