**Вердикт: FAIL.** Независимое ревью фронта WMS-469, коммит `416292fa29b21adaa0c06899f7a8c56cc8b19cb8`, ветка `feat/wms469-stock-dialog`. Найдены четыре блокера, три дефекта и одно замечание. Технические нарушения ниже требуют исправления; принятие макета их не закрывает.

Прочитаны целиком основной `AGENTS.md`, `owner-cases.md`, `failure-cases.md`, требования R1–R24 с C1–C36 и D1–D5, исходник принятого макета и контракт API. Изменения просмотрены через `git show 416292fa -- <файл>`. Реальные формы запросов/ответов сверены с `backend/app/api/products.py` и `backend/app/api/fbs_sellers.py`; каталог backend в HEAD не отличается от `00e51a7d`. Серверные сервисы читались только для проверки последствий фронтовых запросов.

Браузер самостоятельно не открывал. Перечисленные ведущим ручные проверки приняты как предоставленные факты, включая адаптивность 1024/1280, обычное добавление связки, пересчёты, немедленный PUT для `served`, смену склада и ограничения селлера. Ниже отдельно приведены проверки кода; они не выдаются за браузерную приёмку или запросы к настоящей базе.

**F1 — блокер. «Сохранить» при всех снятых галках приёма заказов сбрасывает товарные правила через старую форму API.**

Место: `frontend/src/screens/ff/products-fbs/fbsStockBlocks.ts:331–341`, `FbsStockDialog.tsx:217–221`, `FbsStockDialogContainer.tsx:159–166`.

Подтверждение: сборщик начинает с `{}` и пропускает все блоки с `if (!binding.served) continue`. Кнопка остаётся доступной, а контейнер без проверки отправляет `rule: { by_binding: byBinding }`. Локальная проверка настоящего сборщика для единственной связки с `served=false` получила `{}`.

Это не безвредное пустое изменение: в `backend/app/services/fbs_stock_rule_service.py:860` новая обработка выбирается через `if rule.by_binding`. Пустой словарь попадает в старую ветку. Значения по умолчанию API — `same_everywhere=false`, `percent=0`, `units_mode=false`, `by_warehouse={}` (`products.py:403–415`). Далее сервис записывает эти поля товара и выполняет `pool.publish_enabled = None; pool.percent = percent`, где `percent` отсутствует (`fbs_stock_rule_service.py:1153–1183`). Независимые правила пулов теряются; сохранённый процент, например 50%, становится нулевым при последующем чтении старой модели.

Сценарий: сохранить правило, снять `served` у всех связок, нажать «Сохранить», затем вернуть приём заказов. Нарушены R7, R16, R24 и C13/C32: приём заказов не должен стирать публикацию и лимиты. Фронт не должен отправлять пустой набор изменений как старую форму правила; устранение неоднозначности API нужно согласовать с исполнителем бэка.

**F2 — блокер. Открытие окна уменьшает сохранённый операторский потолок без ввода нового числа.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:150–174`.

Подтверждение: эффект при изменении `products`, включая первое открытие, выполняет `if (draft.units > cap.free) { patchDraft(binding, { ...draft, units: cap.free }) }`. Он не различает новый ввод, смену склада и обычное чтение уже сохранённого правила.

Локально выполнен исходный компонент с контролируемыми состоянием и эффектами: серверный лимит 50, текущий свободный остаток 30; без редактирования поля черновик стал 30, а нажатие сохранения передало `{publish:true, mode:'units', value:30}`. Это же происходит с выключенной публикацией: эффект не проверяет `draft.publish`. Поэтому выключение/включение после расхода товара либо сохранение соседнего блока закрепляет уменьшенный потолок; после пополнения до 50 прежние 50 уже не восстановятся.

Ожидается по R8, R12, R14 и правилу проекта: ограничивать новый ручной ввод, сохранять прежний операторский лимит при изменении свободного остатка, публиковать `min(лимит, свободно)`. Бэк специально сохраняет нетронутый потолок через проверку `value_changed` (`fbs_stock_rule_service.py:882–892`), но фронт превращает его в изменённое значение. Исправление пересчёта при явной смене склада не оправдывает уменьшение при обычном открытии.

**F3 — блокер. Повторное открытие во время загрузки показывает товар A, но сохраняет правило товару B.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialogContainer.tsx:80–96,159–166,203`; `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:618,1851–1862`.

Подтверждение: до загрузки контейнер возвращает `if (!data) return null`; эффект загрузки имеет зависимости `[]`. Каталог может повторно вызвать `setFbsDialogRows(chosen)` и передаёт новый `chosen` тому же контейнеру без `key`. Старый запрос заполняет `data`, а сохранение берёт идентификаторы из уже новых свойств: `product_ids: chosen.map((one) => one.id)`.

Локальная проверка с задержанным ответом: начать загрузку A → до ответа передать выбор B того же селлера → завершить ответ A → сохранить. В данных показанного окна остался `products[0].id === 'a'`, в фактическом теле PUT оказался `product_ids: ['b']`; повторной загрузки не было. Для товаров одного селлера привязки общие, поэтому проверка принадлежности связки не устраняет ошибку адресата.

Ожидается по R1, R16, R17: отображаемые товары, черновик и адресаты сохранения должны относиться к одному открытию. Нужны согласованная смена экземпляра/загрузки при новом выборе и отбрасывание ответа предыдущего открытия.

**F4 — блокер. Массовое изменение WB перезаписывает нетронутую публикацию Ozon других выбранных товаров.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:145–146,217–219`; `fbsStockBlocks.ts:327–339`.

Подтверждение: для любого нетронутого блока используется `drafts[binding.id] ?? draftFromState(first.byBinding[binding.id])`. Сохранение перечисляет все видимые блоки и отправляет их значения, включая `publish`, независимо от того, менял ли оператор этот блок.

Локально проверена последовательность: первый товар WB-only имеет Ozon `applicable=false, publish=false, value=0`; у второго есть Ozon с включённым ручным лимитом 20. Изменяется только WB. Сборщик отправляет и Ozon `{publish:false, mode:'units', value:0}` для обоих товаров. Бэк применяет переданное правило ко второму товару и выполняет выключение его Ozon (`fbs_stock_rule_service.py:919–943`). Даже без WB-only товара разные нетронутые Ozon-правила заменяются правилом первого товара.

Ожидается по R8, R24, C28 и случаю B08: изменение WB не выключает и не стирает Ozon. Контракт разрешает частичный `by_binding`: отсутствующая соседняя привязка сохраняется. Принятый старт черновика с первого товара не отменяет независимость соседней площадки; нужно различать изменённые и нетронутые блоки при записи.

**F5 — дефект. После потери ответа нет сверки с сервером; следующее действие может отменить уже сохранённую смену склада.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialogContainer.tsx:108–135,145–155,168–169`.

Подтверждение: в `catch` выполняется только `setActionError(...)`; `await reload()` находится после успешного PUT. При сетевом исключении чтения фактического результата нет. Локальная проверка отклонённого PUT подтвердила: загрузчик вызван только один раз при открытии, старое `served` осталось в данных, показано `Failed to fetch`.

Конкретное последствие по коду: смена склада A→B зафиксировалась, ответ потерян. Окно продолжает хранить A. Следующее переключение приёма заказов отправляет `{ wms_warehouse_id: binding.wmsWarehouseId, served }`, то есть возвращает A вместе с новой галкой. Для потерянного ответа добавления окно также не выясняет, появилась ли связка. Прямое повторение одинакового PUT опирается на серверную уникальность, но требуемое восстановление состояния не реализовано.

Ожидается по R17, C22/C23 и B02/B09: перечитать фактическое состояние после неопределённого результата, сохранить черновик и безопасно продолжить ту же операцию. Успешный обычный PUT и отсутствие дублей на бэке не закрывают этот дефект фронта.

**F6 — дефект. Во время сохранения можно изменить правило, но успешный ответ закрывает окно и теряет новый ввод.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:429–431,469–472,501–511,528–535`; `FbsStockDialogContainer.tsx:162–174`.

Подтверждение: числовое поле, ползунок и галка режима используют `disabled={!draft.publish}`, без учёта `busy`; переключатель публикации вообще не имеет `disabled`. При ответе без обрезки контейнер без проверки нового черновика выполняет `close()`.

Локальная проверка исходных свойств контролов при `busy=true` подтвердила, что числовое поле включённой публикации остаётся доступным. Последовательность: отправить 20 → пока PUT выполняется ввести 10 → получить успешный ответ на 20. Новый черновик 10 уничтожается закрытием, сервер хранит 20. Дополнительно заблокированная «Отмена» не защищает запрос от закрытия: `AppDialog` получает безусловный `onClose={onClose}` (`FbsStockDialog.tsx:207`), а общий компонент передаёт закрытие по Escape/фону (`ui-kit/AppDialog.tsx:35`).

Ожидается по R16/R17: успешный ответ должен подтверждать именно сохраняемый черновик. Допустимо временно исключить редактирование на время его записи либо сохранить последующие правки и оставить окно открытым. Это техническая защита от потери ввода, а не новое продуктовое ограничение.

**F7 — дефект. Причина сбоя справочника Ozon спрятана в форме добавления; селлер её вообще не может увидеть.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:232–235,273–282,369–374,638–644`.

Подтверждение: в основном содержимом показана только `wbWarehousesError`; `ozonWarehousesError` передаётся исключительно в `BindingPicker` и выводится внутри него. При обычном открытии picker отсутствует. У селлера нет действия добавления, поэтому он не может открыть эту форму и прочитать полученную причину.

Сохранённый Ozon-блок остаётся с номером и чипом «название недоступно», но конкретный текст ошибки загрузчика в этом состоянии не отображается. Общая подсказка `warehouseNameIssueHint(...)` не получает саму ошибку и не заменяет её.

Ожидается по R19/C30 и правилу диагностики: рядом с сохранённой релевантной связкой должна быть доступна причина недоступности имени, в том числе в кабинете селлера. Достаточно показать уже полученный текст в основном содержимом окна.

**F8 — замечание. Серверная подпись об обрезке остаётся после исправления числа и смены склада.**

Место: `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:249,254–260`; `FbsStockDialogContainer.tsx:68,192–199`.

Подтверждение: подпись выбирается как `capNotes[binding.id] ?? serverClamps?.[binding.id]`. Редактирование удаляет только локальный `capNotes`; `serverClamps` после ответа не очищается ни новым вводом, ни перечитыванием после смены склада. Локально проверено: после серверной обрезки до 30 ввод допустимого числа 10 и `onCapNote(null)` оставляют подпись из `serverClamps`.

После смены склада эта подпись может продолжать сообщать число предыдущего склада. Ожидается по R6, R12 и макету: подпись описывает актуальное ограничение текущего блока; старый результат обрезки должен сниматься при соответствующем новом действии.

**Проверки и границы результата.**

Самостоятельно выполнены `npx tsc --noEmit -p tsconfig.app.json` — успешно — и четыре целевых файла Vitest: `fbsStockBlocks.test.ts`, `FbsStockDialog.render.test.tsx`, `fbsStockDialogLoader.test.ts`, `fbsSellerWarehouseRows.test.tsx` — **61/61**. Для запуска использован `--configLoader runner`: обычный запуск упёрся в запрет записи временного конфига внутри общего `node_modules`. Дополнительно прошли **7/7 диагностических проверок**, исполняющих исходные функции и обработчики компонентов с контролируемыми состоянием, эффектами и сетью. Они проверяли наличие описанных дефектов, поэтому их зелёный результат не означает исправность продукта. Временный файл этих проверок удалён.

Тесты арифметики действительно проверяют R9–R13: 50% от 50 и 30 даёт 40; округление выполняется по каждому товару; ручные 30 дают 75%; нулевой остаток не вызывает деления на ноль; обрезка и первый минимум проверены; WB100%+Ozon100% попадают в тело запроса. Однако тесты разметки используют `renderToStaticMarkup`, не выполняют эффекты и нажатия. Тест R17 просто передаёт готовый `actionError`. Сохранение через контейнер, потеря ответа, повторное открытие во время загрузки и сохранность нетронутой соседней площадки существующими тестами не проверены.

Непустая форма PUT, чтение `by_binding`, поля `clamps`, передача `marketplace` и внешнего идентификатора совместимы с API. Селлерский маршрут проверяет `sellerPermissions.products` и использует `key={catalogScopeKey}`; смена активного магазина пересоздаёт экран. Административные контролы учитывают `canEditBindings` и `editable`; серверный PUT защищён `require_fulfillment_admin`. Самостоятельный HTTP-тест 403 не выполнялся, его результат принят от ведущего.

По просмотренному diff изменения соседних экранов ограничены подключением окна, селлерской кнопкой и удалением устаревшей подписи; таблица каталога не перестраивалась. Используются MUI и общий ui-kit. Чип «заказы не принимаем» и недоступное добавление всех уже связанных складов прямо предусмотрены R7/R5 и не являются лишними ограничениями. Визуальные наложения или доступность длинных названий самостоятельно не проверялись. Полный build и результат общего Vitest 471/473 приняты из отчёта ведущего, повторно не запускались.

Отчёт записан в `docs/reviews/artifacts/wms-469/review-astra-front-1.md`. Код продукта не изменён. Файл отчёта не закоммичен по прямому указанию пользователя.
