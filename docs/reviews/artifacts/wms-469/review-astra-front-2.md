**Вердикт: FAIL.** Второй круг независимого ревью фронта WMS-469. Проверен коммит `7075a717e9f6d5b7ffb1989fd547a115f4e9fd56` в ветке `feat/wms469-stock-dialog`, текущий HEAD — `930f26ae7642e42f28014bad51915ed2454a616b`. Между этими коммитами различий в `frontend` нет. F1, F2, F3, F4 и F7 закрыты для сценариев первого отчёта. F5, F6 и F8 закрыты не полностью; дополнительно найдены F9 и F10. Ниже P1 означает высокий приоритет: возможны незапрошенное изменение данных или потеря ввода; P2 — неверное объяснение состояния пользователю.

Прочитаны основной `/Users/deniscivkunov/Projects/WMS/AGENTS.md`, обе библиотеки `owner-cases.md` и `failure-cases.md`, требования WMS-469, исходник принятого макета, контракт API и отчёт первого круга. Просмотрен `git show 7075a717`; серверные изменения `930f26ae` в `products.py` и `fbs_stock_rule_service.py` проверены в части контракта нового окна. Новые продуктовые требования не вводились.

Браузер самостоятельно не открывал. Переданные ведущим результаты стенда приняты как факты от ведущего, включая обычные сценарии F1/F2/F4/F5/F6/F8 и HTTP-тесты 5/5. Дополнительные результаты ниже получены чтением кода, запуском целевых тестов и исполнением исходных обработчиков и эффектов с управляемыми состоянием и сетью. Это не браузерная приёмка и не проверка настоящей базы.

Для краткости пути фронтовых файлов ниже указаны относительно `frontend/src/screens/ff/products-fbs/`. Номера строк относятся к проверенному HEAD.

**F1 — закрыт. Пустое сохранение больше не сбрасывает правила.**

В `FbsStockDialog.tsx:252–255` в сборщик попадают только изменённые блоки: `visible.filter((binding) => touched.has(binding.id))`. В `fbsStockBlocks.ts:339–345` сохраняется фильтр `if (!binding.served) continue`. Главное — `fbsStockDialogSession.ts:120` прекращает обработку до HTTP: `if (Object.keys(byBinding).length === 0) return { kind: 'nothing' }`.

Серверная защита согласована: `backend/app/api/products.py:1645` передаёт `by_binding_present="by_binding" in body.model_fields_set`; `backend/app/services/fbs_stock_rule_service.py:923–926` при явно переданном пустом наборе возвращает `FbsRuleSaveResult(updated_count=0)`. Поэтому даже прямой запрос с `by_binding: {}` не попадает в прежний разрушительный путь. Факт ведущего об отсутствии PUT при снятых `served` согласуется с кодом и тестом сессии.

**F2 — закрыт для открытия и обычного перечитывания. Новая ошибка при смене склада выделена в F9.**

В `FbsStockDialog.tsx:174–175` эффект теперь начинается с `if (!reclamp) return`. При открытии `reclamp` равен `null` (`:153`), поэтому сохранённые 50 при свободных 30 остаются 50. Простое изменение `products` без явной смены склада тоже не режет лимит. Без действий оператора `touched` пуст, следовательно PUT не отправляется.

Отдельно проверено включение публикации без изменения числа. `FbsStockDialog.tsx:470` вызывает `onDraft({ ...draft, publish: event.target.checked })`, а `patchDraft` в `:156–158` добавляет привязку в `touched`. Исполнение обработчика для выключенного лимита 50 при свободных 30 и последующего «Сохранить» сформировало `{publish:true, mode:'units', value:50}`. Законное изменение переключателя не потеряно. Сервер в `fbs_stock_rule_service.py:953–960` пропускает обрезку неизменённого ручного значения: `if not value_changed: continue`.

**F3 — закрыт для повторного выбора товаров во время первоначальной загрузки.**

`FbsStockDialogContainer.tsx:72–95` вычисляет ключ выбранных идентификаторов и перезапускает эффект с зависимостями `[session, chosenKey]`. Перед загрузкой выполняется `setData(null)`; результат принимается только через `if (alive && loaded) setData(loaded)`, а очистка эффекта выставляет `alive = false`. Это также отсекает ошибку прежней загрузки и результат прежней сессии при смене селлера/токена.

В `fbsStockDialogSession.ts:74–78` есть отдельная защита поколения: `const mine = ++generation` и `return mine === generation ? data : null`. Тест `fbsStockDialogSession.test.ts:167–188` действительно запускает две незавершённые загрузки, проверяет `null` для A и данные B. Вместе с зависимостями контейнера это закрывает прежнюю последовательность «показан A, сохранено B». Сам тест сессии React-контейнер не монтирует — граница покрытия учтена ниже.

**F4 — закрыт. Нетронутый соседний блок больше не включается в массовую запись.**

В `FbsStockDialog.tsx:148,156–158,252–255` набор `touched` начинается пустым, пополняется при изменении блока и фильтрует тело сохранения. `fbsStockBlocks.ts:338–345` перечисляет только переданные привязки, а `fbsStockDialogSession.ts:127` отправляет именно полученный частичный `rule.by_binding`.

Сервер в `fbs_stock_rule_service.py:1020–1025` объединяет прежние правила с присланными: `combined = dict(old_rules[product.id].by_binding)` и `combined.update(applied[product.id])`. Далее сохранение явных строк защищает и прежние соседние правила, которые до этого читались из общих полей товара. Внесённое в одном WB-блоке изменение не подставляет правило первого товара в нетронутый Ozon-блок. Потеря отметки уже изменённого, временно скрытого блока после серверной обрезки — другой дефект, F10.

**F5 — не закрыт полностью, P1. При отказе перечитывания следующий `served` всё ещё возвращает прежний склад.**

Обычный путь исправлен: `fbsStockDialogSession.ts:104–107,134–135` после исключения выполняет `data: await reread()`, а `FbsStockDialogContainer.tsx:109,181` принимает полученные данные. Это подтверждает переданный ведущим успешный сценарий восстановления.

Однако `fbsStockDialogSession.ts:82–86` при ошибке чтения возвращает `null`. В `FbsStockDialogContainer.tsx:109–112` тогда остаются старые данные, после чего безусловно выполняется `setBusy(false)`. Следующее изменение приёма заказов в `:127–129` отправляет `{ wms_warehouse_id: binding.wmsWarehouseId, served }` из этих старых данных. Нет предварительной сверки перед новым действием и нет исключения неизвестного склада из тела изменения `served`.

Диагностически исполнены исходные контейнер и сессия с подменными ответами: сервер принял A→B, ответ потерян, перечитывание тоже отказало. После восстановления сети нажатие `served=false` сформировало PUT с `wms_warehouse_id: A`; подменный сервер вернулся с B на A. Это то же последствие первого F5, оставшееся в ветке неуспешного восстановления. Даже штатный тест `fbsStockDialogSession.test.ts:151–163` ожидает `{ok:false, message:'offline', data:null}`, но последующее действие не проверяет.

Ожидается по R17 и B02/B09: после неопределённого результата новое действие не должно молча отменять уже принятую смену склада. Нужно восстановить фактическое состояние перед зависимым действием либо не передавать в нём неизвестное соседнее поле. Черновик правила при этом сохраняется.

**F6 — не закрыт полностью, P1. Открытая форма добавления позволяет начать вторую запись и преждевременно снять `busy`.**

Обычные контролы исправлены: переключатель публикации в `FbsStockDialog.tsx:469` получает `disabled={busy}`, поле, ползунок и режим в `:508,550,574` — `disabled={!draft.publish || busy}`. Выбор склада и `served` учитывают `busy` в `:425,450`. В `:242` используется `onClose={busy ? () => undefined : onClose}`; общий `frontend/src/ui-kit/AppDialog.tsx:35` направляет закрытие MUI в этот обработчик. По коду и диагностическому вызову обработчика закрытие при `busy` игнорируется; браузерный Escape принят от ведущего.

Пропущена уже открытая форма добавления. В `FbsStockDialog.tsx:311–319` компонент `BindingPicker` не получает `busy`; его поля в `:617–646,659–666` не имеют `disabled`. В `:225–229` завершение пары без проверки занятости вызывает `onAddBinding?.(next.warehouse, next.wmsWarehouseId)`.

Последовательность: открыть «Добавить склад», заполнить только склад ФФ, изменить лимит существующего блока, нажать «Сохранить» и задержать PUT правила. Открытая форма остаётся доступной; выбор внешнего склада запускает ещё один PUT. Исполнение обработчиков подтвердило этот вызов. Отдельное исполнение контейнера подтвердило продолжение: завершение добавления вызывает `setBusy(false)` в `FbsStockDialogContainer.tsx:112`, хотя первоначальный `session.saveRule(...)` в `:138` ещё не завершён. Контролы снова разрешают новый ввод, а ответ первоначального сохранения в `:145–147` затем закрывает окно — прежняя потеря ввода снова достижима.

Ожидается по R16/R17: все пути записи данного окна учитывают текущую запись; завершение одной операции не разблокирует интерфейс при другой незавершённой. Это исправление уже заявленной защиты F6, без нового ограничения складского процесса.

**F7 — закрыт. Причина недоступности имени Ozon показана и селлеру.**

В `FbsStockDialog.tsx:274–275` основной контент содержит `ozonWarehousesError && visible.some((one) => one.marketplace === 'ozon' && one.nameIssue === 'list_unavailable')` и выводит сам `ozonWarehousesError` через `ErrorNotice`. Условие не зависит от административного права или открытой формы добавления.

`FbsStockDialog.render.test.tsx:229–242` проверяет именно кабинет селлера с `canEditBindings:false`, отсутствием формы добавления, сохранённым Ozon-блоком без имени и текстом причины. Для такого первоначального отображения статический рендер — подходящая проверка.

**F8 — не закрыт полностью, P2. Новый ввод снимает подпись, но повторная смена склада после ошибки оставляет подпись прежнего склада.**

Основная правка верна: `FbsStockDialog.tsx:196–205` переносит серверные ограничения в `capNotes`; блок в `:291` получает только `capNotes[binding.id]`, без прежнего возврата к `serverClamps`. Ввод в `:543–548` вызывает `onCapNote(limitedBy)`, а `setNote` в `:160–165` удаляет запись при `null`. Исполнение эффекта серверной обрезки 25→20 и ввода 10 подтвердило исчезновение подписи.

Но снятие подписи при смене склада зависит от эффекта с `[products, actionError]` в `:174–191`. Очистка прежней ошибки в начале следующего запроса запускает его по старому складу и сбрасывает `reclamp` до получения нового состояния. Диагностически получено: после перехода на склад со свободными 100 в поле осталось 30 и подпись продолжила сообщать 30 от прежнего склада. Полная последовательность и причина приведены в F9; это одно исправление, а не отдельная предлагаемая механика.

Ожидается по R6/R12: подпись относится к актуальному складу и текущему действию, включая повтор после ошибки.

**F9 — новая находка, P1. Эффект смены склада уменьшает лимит после отказа либо ещё до ответа, используя прежний склад.**

Место: `FbsStockDialog.tsx:174–191,297–299`; `FbsStockDialogContainer.tsx:103–112`.

Подтверждение: выбор склада выставляет `setReclamp(binding.id)`. Эффект зависит от `[products, actionError]`, сразу делает `setReclamp(null)`, затем без проверки фактической смены склада выполняет `if (draft.publish && !draft.byPercent && draft.units > cap.free) { patchDraft(binding, { ...draft, units: cap.free }) }`. `patchDraft` не только меняет поле, но и отмечает блок для последующей записи.

Воспроизведены два варианта исполнения исходных обработчиков и эффектов:

1. На складе A сохранён лимит 50, свободно 30. Пользователь выбирает B; PUT отклонён, перечитанное состояние по-прежнему указывает A. Эффект уменьшает лимит до 30, и следующее «Сохранить» формирует правило с 30. Пользователь не вводил новое число, смена склада не состоялась.
2. При тех же 50/30 в окне уже есть ошибка предыдущего действия. Пользователь снова выбирает B. Контейнер немедленно делает `setActionError(null)` (`:105`); эффект срабатывает до ответа и обрезает число по A. Успешный ответ затем показывает B со свободными 100, но `reclamp` уже сброшен: число остаётся 30, вместе с подписью про прежние 30.

Ожидается по R6, R14 и R17: отказ смены склада оставляет прежний лимит и пользовательский черновик; очистка сообщения об ошибке сама по себе не меняет число. Если пересчёт выполняется после смены склада, основанием должно быть перечитанное состояние именно этой смены, а не изменение текста ошибки. Второй вариант также объясняет незакрытую часть F8.

**F10 — новая находка, P1. Серверная обрезка одного блока стирает отметку несохранённого изменения другого блока.**

Место: `FbsStockDialog.tsx:196–205,252–255`; `fbsStockBlocks.ts:339–340`.

Подтверждение: при любом новом `serverClamps` выполняется безусловное `setTouched(new Set())`. При этом запрос содержит только видимые изменённые блоки с `served=true`. Комментарий «всё отправленное сохранено» не означает, что отправлены все существующие черновики.

Воспроизведён путь: изменить ручной лимит Ozon с 50 на 40; снять у него `served`, сохранив черновик в окне; изменить WB с 50 на 80; нажать «Сохранить». В запросе только WB. Сервер сохраняет WB с обрезкой до 60 и возвращает `clamps`; окно остаётся открытым. Эффект очищает весь `touched`. Затем пользователь возвращает `served` Ozon: поле показывает его несохранённые 40, но повторное «Сохранить» формирует `{}`. Сессия возвращает `nothing`, окно закрывается, серверный Ozon остаётся 50.

Проверка исходного компонента подтвердила одновременно видимое значение 40 и пустое тело повторного сохранения. Это потеря законного изменения при обработке частичного запроса, а не требование сохранять скрытую строку сразу.

Ожидается по R7, R16/R17: подтверждение записи снимает отметки изменения только у действительно отправленных и сохранённых блоков. Черновик, не входивший в запрос, должен оставаться несохранённым и уйти при следующем сохранении после возвращения его строки.

**Что действительно проверяют тесты.**

Выполнены пять целевых файлов Vitest: `fbsStockBlocks.test.ts`, `FbsStockDialog.render.test.tsx`, `fbsStockDialogLoader.test.ts`, `fbsStockDialogSession.test.ts`, `fbsSellerWarehouseRows.test.tsx` — **78/78**. Запуск: `npx vitest run --configLoader runner` с перечисленными файлами.

Новые тесты сессии действительно исполняют запросы с подменным `fetch`: проверяют отсутствие пустого PUT, точное тело, повторное чтение, отбрасывание старой загрузки и отдельный результат серверной обрезки. Это поведенческие тесты сетевого модуля. Но они не исполняют эффекты и обработчики React-окна вместе с ним.

Рендер-тесты по-прежнему вызывают `renderToStaticMarkup` (`FbsStockDialog.render.test.tsx:1–19,49–53`), подменяют `AppDialog` простым контейнером и не выполняют `useEffect` или нажатия. Поэтому проверки F2 в `:103–115` не обнаружили бы изменение числа последующим эффектом; проверка `busy` в `:118–127` проверяет атрибуты первоначального состояния без открытой формы добавления. Проверки Escape/фона в этих тестах нет. Проверка F7 соответствует своему предмету — наличию текста в разметке.

`backend/tests/test_wms417_stock_http_contract.py:47–95` действительно транспилирует текущие `fbsStockBlocks.ts` и `fbsStockDialogSession.ts`. Однако в `:81–91` тест сам создаёт готовый черновик и передаёт единственную выбранную привязку: `ruleBodyFromDrafts([binding], ...)`. Дальнейшие HTTP-запросы и чтение результата проверяют серверное сохранение, включая независимость WB/Ozon при совпадающих внешних номерах. Обработчик переключателя, накопление/очистка `touched`, эффекты, занятость и закрытие окна в этот путь не входят. Результат **5/5** принят от ведущего; самостоятельно этот Python-набор не запускался.

Дополнительно выполнены **9/9 диагностических проверок** исходного фронтового кода: обработчики/эффекты компонента с управляемыми состояниями, а для восстановления и конкурирующих записей — также контейнер и сессия с подменным загрузчиком и сетью. Проверены включение без изменения числа, сохранность числа при открытии, отказ/повтор смены склада, открытая форма при `busy`, закрытие, снятие серверной подписи, потеря `touched`, отказ перечитывания и преждевременное снятие `busy`. Часть проверок утверждает наличие дефекта; их успешное завершение не означает исправность продукта. DOM и настоящий React-рендерер в этой дополнительной диагностике не использовались.

`npx tsc --noEmit -p tsconfig.app.json` прошёл. Обычный `npm run build` остановился на `EPERM` при записи двух `.tsbuildinfo` в общий `node_modules/.tmp`. Без изменения проекта отдельно успешно выполнены проверка `tsconfig.node.json` с `--tsBuildInfoFile /private/tmp/wms469-front2-node.tsbuildinfo` и `npx vite build --configLoader runner --outDir /private/tmp/wms469-front2-build`. Это проверка сборки, не деплой.

Отчёт сохранён в [review-astra-front-2.md](/Users/deniscivkunov/Projects/WMS/.worktrees/wms469-stock-dialog/docs/reviews/artifacts/wms-469/review-astra-front-2.md). Код продукта и требования не изменены. Файл не закоммичен по прямому указанию пользователя.
