# Хэндофф WMS-469 «Задать остаток по связкам складов» — 21.09.2026

Кому: следующей сессии ведущего (Claude) и владельцу. Что здесь: что сделано и чем проверено,
где было ревью, что конфликтует с etalon, как довести до прода, и хвосты вокруг.

## 1. Где всё лежит

- Ветка **feat/wms469-stock-dialog**, запушена, вершина **c661b646**. Worktree
  `/Users/deniscivkunov/Projects/WMS/.worktrees/wms469-stock-dialog`.
- База ветки: origin/etalon на момент **74665f91** (merge WMS-484). После этого etalon ушёл вперёд
  на ~55 коммитов другого чата (WMS-483, WMS-475, WMS-477, WMS-488) — см. раздел 4.
- Прод сейчас: **9f6f1521** (выкатка другого чата 21.09; в etalon есть).
- Документы в ветке:
  - постановка и приёмка — `docs/requirements/WMS-469.md` (R1–R26, D1–D7, C1–C38 с вердиктами, «Заключение: принято»);
  - макет, принятый владельцем (v3) — `docs/mockups/WMS-469-zadat-ostatok/index.html`,
    артефакт https://claude.ai/artifact/ADqjYeuBEuJv9rJsnQXnr7 ;
  - контракт API фронт↔бэк — `docs/reviews/artifacts/wms-469/api-contract.md`;
  - ревью — `review-opus-back-1/2/3.md` (бэк), `review-astra-front-1/2/3.md` (фронт), в этой же папке;
  - карточка задачи в `docs/KANONICHESKIY_BACKLOG.md` (раздел «## WMS-469 ·…»): диктовка владельца 18.09,
    уточнения 20.09 по макету, ответы на 4 вопроса.
- Локальный стенд ветки: `.claude/launch.json` конфиги **wms469sd-api** (uvicorn :18321, `--reload`,
  Postgres `wms469sd_stand`) и **wms469sd-web** (vite :5321). Сид: `scratchpad/seed469sd.json` сессии
  262429b5 (арендатор «ФФ Стенд WMS-469», admin469sd@example.com / stand-469-pass, селлер «ИП Фадина (стенд)»,
  склады «Основной» и «Подольск», мок-ключ WB → склад 501001 «E2E Seller Warehouse»). Вход в браузере:
  `localStorage.setItem('wms_token_ff', <токен из POST /auth/login>)` → `/app/ff/products`.
  У стендовых товаров нет карточек WB/Ozon — живую отправку на площадки на стенде не увидеть.

## 2. Что сделано и кем

| Часть | Коммиты | Кто | Суть |
|---|---|---|---|
| Бэклог + макет | adf9a39d, 87c8d2c3 | ведущий, Opus-макетчик | карточка из канонической копии; макет v1→v3 по правкам владельца |
| Постановка | 30ee2368, fc7c6b05 | Sol 5.6 (codex exec) | R1–R26, D1–D7 (D6: «включено + 0 %» = обмен нулём по слову владельца WMS-351; D7: выключенный транспорт фиксируется на паре товар×привязка) |
| Бэк | 00e51a7d, 930f26ae, 7704858c | Sol 5.6 | правило «товар × привязка» в существующей `fbs_binding_stock_pools` (+1 nullable колонка `publish_enabled`, миграция 20260920_0307); процент/число как одно значение; обрезка ручного числа по минимальному свободному остатку с именем товара; WB 100 % + Ozon 100 % без потолка; лимит не расходуется событиями; seller-scoped чтение привязок без прав на запись; событийная публикация по всем привязкам, склейка событий, повторы только временных ошибок, 429/Retry-After; пустой `by_binding` = ничего не менять |
| Фронт | 416292fa, 7075a717, 0fc6ba25 | Opus | окно переписано под макет: блок = склад селлера ↔ склад ФФ, галка «Принимаем заказы» как в бою, строка площадки с переключателем/ползунком/полем «шт»/галочкой «процентом», пустое состояние и «Добавить склад», связки помнятся по селлеру, кабинет селлера (связки и served только чтение); контейнер + сессия загрузки (поколения), busy-счётчик, перечитывание после сбоя, отметки изменённых блоков; тесты: fbsStockBlocks, session, render, **dom** (jsdom, react-dom/client + act; jsdom добавлен в devDependencies) |
| Слияние с etalon | c661b646 | ведущий | влит etalon 74665f91 (WMS-484 + реверт WMS-477): новое окно обёрнуто в `ErrorBoundary` по WMS-484, бэклог — обе карточки |

Порядок ревью по правилам проекта: Sol-бэк ревьюил **Opus** (3 круга: FAIL → FAIL → **PASS**),
Opus-фронт ревьюила **Astra** (3 круга: FAIL → FAIL → **PASS**). Все находки закрыты, ни одна не
закрыта «продуктовым одобрением». Ревьюеры не имели браузера и Postgres-стенда в песочнице —
конкурентные пробы Opus гонял на своей временной Postgres-базе; браузерные факты ревьюеры принимали
от ведущего/исполнителя.

## 3. Чем проверено (факты, не пересказ)

- ruff, mypy (461 файл после слияния) — чисто; `npx tsc --noEmit -p tsconfig.app.json`, `npm run build` — чисто.
- Бэк-тесты: `backend/tests/test_wms469_stock_dialog_backend.py` (по C-номерам) + соседние — на SQLite
  `-n auto` 214 passed; тот же файл **на настоящем Postgres последовательно** (`WMS_TEST_DATABASE_URL`,
  `-p no:xdist`) — 21 passed, в т.ч. 3 `postgresql_concurrency` (параллельные сохранения, держатель замка 6 с).
  После слияния c661b646: test_wms469 + test_wms417 — 23 passed / 3 skipped (skipped = Postgres-only).
- Фронт-тесты окна: 91 (в т.ч. 11 DOM); после слияния render+dom — 30/30. Полный vitest: 498 passed,
  3 failed — два чужих старых падения в `FfInboundRequestView.test.ts` (лимит строк) и один
  чувствительный ко времени в `InventoryScan.test.ts` (отдельно 19/19); к задаче не относятся.
- Браузер (ведущий, стенд :5321, 21.09): два товара с остатками 50 и 30 → окно с одним блоком
  «WB E2E Seller Warehouse ↔ Склад ФФ», без общего остатка в шапке; снял «процентом», ввёл 45 → поле 30,
  индикатор 75 %, подпись «товара «Chin-5563Izumrud/54-56 — платье изумруд» всего 30 штук»; «Сохранить» →
  в базе у обоих товаров quantity 30 / percent NULL / publish_enabled true; повторное открытие 30 / 75 %;
  снял «Принимаем заказы» → чип «заказы не принимаем», строка скрыта; вернул — 30 / 75 %.
- Браузер (исполнитель фронта, тот же стенд, 20–21.09): пустое состояние → «Добавить склад» → связка одним
  PUT; кнопка заперта, когда все склады добавлены; 80 при 50 → 50; 50 % → 40 шт (25+15), поле только чтение;
  served — отдельный PUT, «Отмена» не откатывает; смена склада ФФ правит ту же связку; отказ PUT (500) —
  лимит 50 остался, PUT правила не идёт; ошибка + смена на склад со 100 — число не обрезано; частичная
  обрезка одного блока не стирает черновик другого; потеря ответа → окно перечитало сервер; замедленный PUT
  4 с — контролы заперты, Escape не закрывает; 1024/1280; кабинет селлера — то же окно, связки/served
  заблокированы, PUT к связке 403.
- Приёмка: Sol-аналитик (замена, сессия постановки недоступна) — **38/38 подтверждено**, 0 нарушено,
  0 не проверено. C14–C24 (публикация) подтверждены тестами через настоящее складское движение и
  Postgres-пробами ревьюера, **не** живыми вызовами WB/Ozon.

Что НЕ проверялось: живые WB/Ozon; staging/production; ТСД; полный pytest (оставлен CI).

## 4. Конфликт с ушедшим вперёд etalon — главное для следующего шага

После моего слияния (74665f91) в etalon пришли WMS-483 (обнуление остатков / явный ноль по режимам,
«не открывать окно остатка, когда привязки не отдали», «пустое поле для незаданного лимита по складу»,
имена складов по ключам правила), WMS-475 (стикеры сразу при добавлении заказа — то же, что моя карточка
WMS-475 в etalon), WMS-477 (восстановлен), WMS-488 (изоляция AVpack, гранты магазинов). Пробное слияние
(`git merge-tree --write-tree origin/etalon origin/feat/wms469-stock-dialog`) даёт **конфликты в 6 файлах**:

1. `backend/app/models/fbs_binding_stock_pool.py` — обе стороны добавили по Boolean-колонке:
   у нас `publish_enabled` (nullable; NULL = старая строка, читается через legacy-поля Product),
   у WMS-483 `units_configured` (NOT NULL default false; «явный ноль от оператора» против «исторический
   ноль без правила»). Нужно оставить **обе** и согласовать семантику: сохранение через новое окно в
   режиме штук ставит `units_configured=true`; чтение legacy-строк с quantity 0 уважает `units_configured`
   (WMS-483) и `publish_enabled IS NULL` (WMS-469) одновременно. Это не механический merge — отдавать
   бэк-исполнителю (Sol) с обеими постановками (WMS-469 R7–R15, D5–D7; WMS-483 в бэклоге etalon).
2. **Миграции**: наша `20260920_0307` и их `20260920_0255` обе от `20260913_0306`; в etalon уже есть
   merge-ревизия `20260921_0489` (объединяет 0255 и 0488). Чтобы был один head: перепривязать нашу
   `20260920_0307` на `down_revision = "20260921_0489"` (или добавить merge-ревизию), проверить
   `alembic heads` = 1 и `scripts/ci/check_migrations.py`. Прод уже на 0255/0488/0489.
3. `backend/app/services/fbs_stock_rule_service.py` — WMS-483 (явные units-правила, ноль) против нашей
   ветки `by_binding` (материализация явных строк для всех привязок, обрезка, Product-поля).
4. `backend/app/services/fbs_stock_sync_service.py` — WMS-483 «обновлять управляемые нули WB раз в
   10 минут», «сохранять явный ноль между режимами» против нашей классификации временных/постоянных
   ошибок (`_is_retryable_wb_failure`) и изоляции ошибок привязок.
5. `frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx` — у нас файл переписан целиком; у WMS-483/484
   поверх **старого** окна: ErrorBoundary (уже перенесён в c661b646), «не открывать окно, пока привязки не
   отдали», пустое поле для незаданного лимита, имена складов по `fbsWarehouseRuleKeys.ts` (новый файл).
   Брать наш файл и перенести смысл правок WMS-483 в новую модель блоков (не открывать окно при
   недогруженных привязках — у нас это состояние контейнера/сессии; «незаданный лимит» — у нас поле
   пустое до ввода?). Решать с фронт-исполнителем (Opus) по постановке WMS-483.
6. `frontend/src/screens/ff/products-fbs/fbsStockDialogLoader.ts` и `docs/KANONICHESKIY_BACKLOG.md` —
   loader: их ключи правила по складам против наших `by_binding`; бэклог — механически, обе карточки.
   Auto-merge прошёл, но требует проверки: `FfProductsFbsPage.tsx`, `SellerProductsStockScreen.tsx`
   (WMS-488 меняла экран остатков селлера на 100 строк), `test_wms351_marketplace_publication_switches.py`,
   `fbsStockDialogLoader.test.ts`.

**Порядок доведения до прода (по правилам проекта):**
1. В worktree: `git fetch origin && git merge origin/etalon` → разрулить 6 конфликтов по п. 1–6
   (бэк — Sol с обеими постановками, фронт — Opus; ведущий код не пишет).
2. Проверки: `cd backend && ruff check . && mypy . && pytest -n auto tests/test_wms469_stock_dialog_backend.py
   tests/test_fbs_stock_rule_service.py tests/test_fbs_stock_sync.py tests/test_wms417_stock_http_contract.py
   tests/test_wms351_marketplace_publication_switches.py` + тесты WMS-483 (найти по `grep -l WMS-483 backend/tests`);
   Postgres-конкурентные: `WMS_TEST_DATABASE_URL=postgresql+psycopg://deniscivkunov@localhost:5432/<временная>
   pytest -p no:xdist -m postgresql_concurrency …` (создать базу, после — удалить; стенды не трогать);
   `cd frontend && npx tsc --noEmit -p tsconfig.app.json && npm run build && npx vitest run --configLoader runner
   src/screens/ff/products-fbs`; `alembic heads` — один; `bash scripts/ci/check-backlog-ref.sh origin/etalon`.
3. Короткий круг ревью **только по стыку с WMS-483** (бэк — Opus, фронт — Astra), затем аналитик
   дописывает в `WMS-469.md` вердикты по затронутым C (регресс WMS-483: явный ноль, 10-минутное обновление
   нулей, пустое поле) — приёмка не должна быть «на словах».
4. Браузер на стенде ветки (после слияния перезапустить `wms469sd-api`, миграцию стенд не гоняет —
   `WMS_AUTO_CREATE_SCHEMA=1` создаёт таблицы по моделям; для чистоты пересоздать базу `wms469sd_stand`).
5. Merge в etalon — только fast-forward после слияния (`git push origin <sha>:etalon`) или PR; выкатка
   **только по слову владельца «кати»**: `ssh root@sellerfocus.pro 'cd /opt/wms && ./scripts/deploy/prod-update.sh'`,
   затем сверить `git rev-parse HEAD` на сервере и `/api/health`. Перед выкаткой на сервере 97 % диска
   (см. память wms477) — pre-migration бэкапы чистить.

## 5. Риски и решения, о которых владелец должен помнить

- **D6**: боевые товары «публикация включена + 0 %» после первого сохранения через новое окно начнут слать
  площадке явный ноль (по слову владельца от 08.09 в WMS-351; WMS-376 «0 % = правила нет» уступает).
  В WMS-376 упоминались «Лэд лампы Н4» Манкаева. Если это нежелательно — одно правило до выкатки.
- **D4/R18**: в кабинете селлера связки складов и галка «Принимаем заказы» — только чтение (действующие права:
  админ ФФ). Владелец не возражал, но макет v3 показывал «Добавить склад» и в кабинете селлера — по факту
  кнопки там нет.
- **WMS-483 семантика нуля** должна выжить слияние — это главный регрессионный риск.
- Стенд не показывает живые вызовы WB/Ozon — «мгновенный обмен» (R20) доказан тестами и пробами, не боем.

## 6. Хвосты вне WMS-469 (из этой же сессии)

- **WMS-473** (приёмка сразу в короба) — на проде с 18.09 по приказу владельца **без ревью и приёмки**,
  вердикты C1–C24 в `docs/requirements/WMS-473.md` пусты. **WMS-474** (сканер отдаёт «<gs>») — хотфикс на
  проде, регистр подстановки без учёта регистра.
- **КИЗ Горячкиной (AVpack)**: у четырёх пулов палантинов в PDF ЧЗ название и артикул противоречат друг другу;
  привязки в WMS возвращены «по названию» по требованию владельца; решает селлер (карточки GTIN в ЧЗ);
  палантины до ответа не печатать. Выгрузка всех 142 КИЗ — `AVpack_KIZ_export_2026-09-19.xlsx`
  (в scratchpad сессии 262429b5). Одна кривая единица уехала 18.09 (заказ 5803306927).
- **Нумерация бэклога**: в etalon WMS-474/475 — мои (сканер, стикер при добавлении), в канонической копии
  (ветка fix/wms392-kiz-scan-unlock) под 474/475 — «жалоба про кнопку» и «QR напротив короба», 476 — «сверять
  GTIN при загрузке» + уточнение владельца про автопривязку по артикулу. Другой чат реализовал стикеры сразу
  под WMS-475. Разрулить при сверке WMS-000.
- **Диск Мака** забивается до нуля: `.worktrees` 28 ГБ (100+ копий; в wms483/wms484/wms477 — живые
  node_modules другого чата), сессии Codex 11 ГБ, Application Support Claude/Cursor по 11 ГБ. Безопасно чистить:
  `.mypy_cache`, `__pycache__` (но не удалять tracked `scripts/hooks/__pycache__/*.pyc` — они в гите),
  `~/Library/Caches/Google/Chrome/Default/Cache`, старые `.venv` в worktree до 10.09.
- Правило ресурсов (память `delegate-to-cheap-agents`): всё, кроме макетов/фронта, — Sol 5.6 через
  `codex exec -m gpt-5.6-sol … </dev/null` из каталога внутри репозитория; Opus — только макеты и фронт;
  ревью: Sol-код → Opus, Opus-код → Astra (`-m gpt-6-astra`). Codex в песочнице не пишет в `.git`
  (коммитит ведущий) и не поднимает Postgres/браузер (факты передавать).
