# WMS-526 — независимое ревью Astra, раунд 5

**Вердикт: PASS. T1 закрыт.** Оба исправленных теста воспроизводят смену состава короба внутри выполняющегося `retry-qr`, до настоящей сборки, и различают исправленную и прежнюю дефектную реализацию. Новых замечаний в проверенном diff нет.

Проверен detached HEAD `8067fccfe1fccf7efbaf912a0bb7635408c86d07` от 24.09.2026. Область ревью — только изменённые тесты из `git diff 33a06814..8067fccf` и замечание T1 из `review-astra-4.md`. Прочитаны AGENTS.md, требования WMS-526 и обе библиотеки `owner-cases.md` / `failure-cases.md` (библиотеки — из основного checkout, поскольку здесь отсутствуют). Ранее принятый продуктовый код повторно не пересматривался.

В первом тесте перед входом в обработчик подтверждён заказ A в коробе. Обёртка `assembly_svc.assemble_box_order` выполняет настоящие `clear_box` и `assign_orders(B)` в отдельных сессиях с commit и лишь затем вызывает сохранённую настоящую функцию сборки (`backend/tests/test_ozon_box_assembly.py:898–930`). Во втором тесте короб до входа пуст; назначение B также происходит в обёртке перед настоящей сборкой (строки 974–1001). Поэтому предварительный resolver прежнего обработчика успевает прочитать A либо `None`, а сборка уже видит B.

UUID сохранены до возможного rollback. Оба теста проверяют ответ 503, а через новую сессию — фактическую принадлежность короба B, причину в снимке короба и в данных B, отсутствие причины у A. Первый дополнительно проверяет код HTTP-ошибки `ozon_live_handoff_blocked`; оба проверяют точные код и текст сохранённой причины.

| Проверка | Фактический результат |
|---|---|
| `cd backend && /Users/deniscivkunov/Projects/WMS/backend/.venv/bin/python -m pytest -q tests/test_ozon_box_assembly.py tests/test_fbs_auto_assign_ozon_boxes.py` | **28 passed**, 2.67 с; 5 предупреждений deprecation от Swig |
| Те же два изменённых теста с настоящими прежними функциями из `26bee62b`, подставленными только в память | **2 failed, 16 deselected**, 0.79 с; оба падают на отсутствии причины у короба B: `None != expected_error`, строки 941 и 1011 |
| `git diff --stat 33a06814..8067fccf` | Только `backend/tests/test_ozon_box_assembly.py`: 120 добавлений, 53 удаления; продуктовый код не изменён |
| `git diff --check 33a06814..8067fccf` | PASS, exit 0 |
| `git diff --exit-code` после проверок | Exit 0; отслеживаемые файлы на диске не изменены |

Для контрольного запуска через AST извлечены `_resolve_ozon_box_order_id`, `retry_fbs_packing_box_qr` и `assemble_box_order` из `git show 26bee62b:…`. Функции исполнены в пространствах имён соответствующих модулей; восстановлен требуемый resolver-у импорт `FbsPackingBoxItem`, тестовый импорт обработчика направлен на прежнюю функцию. Декоратор регистрации маршрута снят, тела функций не менялись. Подстановка выполнена после сбора тестов в отдельном Python-процессе. Падения произошли на проверках результата, после успешных проверок 503 и фактического заказа B, а не из-за ошибки подстановки или подготовки данных.

Это детерминированное чередование операций на SQLite; реальное ожидание замков PostgreSQL, браузер, живой Ozon и деплой не проверялись. Вердикт относится к T1 и данному diff, а не к полной приёмке WMS-526.

Отчёт сохранён локально без коммита и push по прямому поручению. Код продукта и тесты на диске не менялись.
