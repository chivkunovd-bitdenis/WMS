**Для `94fb648548b515b98d3b5b64f414cae070733f02` относительно `eaa6ae9aecee875c50b31afa6b2cc224bb57d043` ограниченный ACCEPT пока не выдаю: найден один P2 в конкуренции проведения пересчётов.** Новых P1 и других подтверждённых P2 в проверенных межслойных взаимодействиях не нашёл. Все номера строк ниже относятся к frozen `94fb6485`, прочитанному через Git.

**P2 — два пересчёта могут взаимно заблокироваться при удалении общей тары после корректировки остатков.**

Проведение сначала блокирует товары, затем корректирует остатки через общий inventory-сервис — [inventory_count_service.py:1644](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/inventory_count_service.py:1644). При движении внутри складского короба `validate_container` берёт на короб совместную блокировку `FOR SHARE` — [inventory_container_service.py:187](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/inventory_container_service.py:187).

После движений тот же `post_count` вызывает удаление подтверждённой пустой тары на строках **1699–1706**. Удаление запрашивает на тот же короб исключительную блокировку `FOR UPDATE` — [inventory_count_service.py:1007](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/inventory_count_service.py:1007).

Точная последовательность для двух транзакций:

1. В складском коробе X есть остатки двух разных товаров P и Q. Созданы два пересчёта с разными товарными фильтрами: A содержит P, B содержит Q. Такой отбор разрешён в `create_count`, строки **320–323**.
2. В обоих документах оператор подтверждает X как пустой. `mark_place_empty` обнуляет строки соответствующего документа и сохраняет X в `empty_places`, строки **768–777**.
3. A проводит изменение P, B — изменение Q. Товары и строки остатков разные, поэтому обе транзакции доходят до движения и удерживают совместимые `FOR SHARE` на X.
4. A запрашивает `FOR UPDATE` для удаления X и ждёт совместную блокировку B. B запрашивает такой же `FOR UPDATE` и ждёт A.

Получается цикл ожидания. Проверка оставшегося товара выполняется **после** запроса исключительной блокировки, поэтому она не успевает вернуть штатный `container_not_empty`. Ошибка взаимной блокировки здесь не преобразуется в прикладной отказ и не повторяет операцию: HTTP-обработчик ловит только `InventoryCountError` — [inventory_counts.py:990](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/api/inventory_counts.py:990).

Это **сбой проведения, а не доказанная потеря товара**: прерванная транзакция откатывается. Исправление `977bd21a` закрывает уже завершившееся удаление другим документом; одновременное повышение `FOR SHARE → FOR UPDATE` оно не закрывает. Прочитанный тест `test_confirmed_empty_container_deleted_by_other_count_is_idempotent` также выполняет удаление перед проведением последовательно.

Обязательное исправление — исключить это повышение блокировки после движений: согласовать захват подтверждённой тары до изменений остатков с порядком блокировок товаров либо обеспечить корректный повтор всей транзакции. Дополнительный блокирующий `SELECT` в нынешнем месте цикл не устранит. **Последовательность установлена по фактическому коду; PostgreSQL replay в этом ревью не запускал.**

Остальные результаты ограниченной проверки:

- **Stock cap/min и сохранение режимов согласованы.** Увеличение потолка проверяется после Product-lock; прежнее или уменьшенное число допускается при упавшем free. Процентный режим сохраняет `pool.quantity`, а действующий frontend отправляет оба набора значений и правильную вложенную форму bulk-запроса. См. [fbs_stock_rule_service.py:735](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/fbs_stock_rule_service.py:735), строки **823–852**, и [FfProductsFbsPage.tsx:213](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:213). Публикация ограничивается `min(share, remaining)` внутри физического склада; резерв, снятие резерва и передача потолок не меняют.
- **Прежний merge/cancellation P2 остаётся закрытым.** Merge блокирует связанные строки с `NOWAIT` и при `55P03` откатывается с `merge_busy`; поэтому ожидание заказа под Product-lock не замыкается с обычной отменой. Остатки перечитываются после блокировок. См. [product_merge_service.py:111](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/product_merge_service.py:111), строки **217–237, 385–388**. Дубли операторских потолков отклоняются, автоматически не складываются.
- **Первая печать и аудит согласованы с Ozon.** Сохранён порядок `Order → PrintAsset`, повторная проверка связи и освобождение блокировок неактуальной попытки через savepoint — [fbs_print_asset_service.py:379](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/fbs_print_asset_service.py:379). Аудит первой печати не добавляет обратного захвата Supply/Product: `document_id` не имеет FK на документ, `product_id` в этом вызове не передаётся. Прежний отказ по печати не возобновляю.
- **Удаление WMS-111/112 не нарушило проверенные контракты.** Относящиеся cancellation, cancelled-after-pack, WB sync, shipment, tracking и API-файлы совпадают с `eaa6ae9a`; удалённый helper не вызывается из исполняемых сервисов. Обычное снятие резерва сохранено, отмена не приходует товар автоматически. STOP остаётся.
- **Новых повторных списаний и складских эффектов упаковки в рассмотренном delta не обнаружено.** WB/Ozon сохраняют проверку существующего `shipment_movement_id`; audit-вставки не создают дополнительных движений. Упаковка записывает факт работы, а список навигационных blockers остаётся пустым. Это вывод по коду, не приёмка операторского сценария.

Сам выполнил небольшую проверку извлечённых из frozen SHA чистых функций расчёта: падение свободного остатка, неизменность потолков, чтение процентов/штук, совпадающий номер склада WB/Ozon и отказ неоднозначному ключу — **PASS**. Целевой `git diff --check` — **PASS**. Базы, файлы и Git-состояние не менял; авторские PostgreSQL-прогоны не выдаю за свои. Живой браузер, runtime, CI и выпуск этим ревью не приняты; evolving billing и чужие worktree не проверял.