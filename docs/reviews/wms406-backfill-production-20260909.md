# WMS-406: исторические факты восстановлены на production

09.09.2026 на production `8dfb216d4061b15789982b72f7c1b82c57e3e897`
применён ранее разрешённый владельцем backfill для всех 11 организаций и всей
доказанной истории. Создан **161 OperationFact**, исправлено **2542 даты**.
Повторный сухой прогон показал **0 созданий и 0 исправлений у каждой организации**.
Ошибок записи и пропусков из-за начисления или счёта не было.

Это результат применения данных, а не заявление о проверке интерфейса или
всего релиза. Приложение, склад, заказы, тарифы, начисления и счета этим
заданием не изменялись. Машиночитаемое доказательство:
[wms406-backfill-production-20260909.json](wms406-backfill-production-20260909.json).

## Проверки до записи и доставка скрипта

HEAD `/opt/wms` проверен по SSH. API image:
`sha256:d5ccea71742417e9f0b07d95fe6c1890036845cf4e2cb41c8a014944345415c7`.
Runtime-модуль `fbs_order_billing_service.py` имеет SHA-256
`579b482ed6e10e32a74d2572ba15bebaaf1614ed51341a67a5a9b28e8066843c`.
Скрипт `backfill_fbs_order_facts.py` из отслеживаемого server HEAD и переданные
байты имеют одинаковый SHA-256
`01de7238b6219f8cbe1a768f5cbbea334215d4933df0cbc942deeb559a53b9a7`.

Резервная копия `/opt/wms/.deploy-backups/before-wms406-407-20260909/database.dump`:
410699177 байт, SHA-256
`afdc17cd9518a5a609b5026dc962e177454df6519517f096965ab4aa64cda819`.
Формат PostgreSQL custom подтверждён; `pg_restore --list` успешно прочитал
содержимое архива. Восстановление базы не выполнялось.

В API image отсутствует каталог `scripts`: Dockerfile копирует только app,
tests и alembic. Поэтому первоначальный импорт остановился до dry-run.
После согласованного root уточнения точные проверенные байты скрипта переданы
через stdin, загружены в `ModuleType` и `compile` отдельного Python-процесса.
Контейнерные и application-файлы не записывались. Использован существующий
`script.main()` с `--tenant <UUID> --apply`, без другого алгоритма записи,
без `--from` и без `--skip-dates`.

Перед каждым запуском проверялись server HEAD, ненулевая резервная копия и
SHA-256 скрипта; внутри процесса — SHA-256 скрипта и runtime helper. При каждой
транзакции назначались `statement_timeout=30000ms`, `lock_timeout=3000ms`.
Оба dry-run работали с `SET TRANSACTION READ ONLY` и проверкой `SHOW ... = on`.
Размер существующего пакета записи сохранён: 200 изменений.

## Фактический результат

Свежий dry-run выполнен в 09:30:44–09:30:52 UTC. Он показал 161 создание и
2542 передатировки: относительно плана 08:20 UTC только две даты уже не
требовали исправления у «Империи ФФ». До apply эти результаты переданы root.
Запись прошла в 09:32:40–09:32:57 UTC с кодом завершения 0.

| Организация | Создано фактов | Исправлено дат |
|---|---:|---:|
| FFAdam | 2 | 423 |
| Империя ФФ | 154 | 2103 |
| ПакХаб | 0 | 4 |
| AVpack | 5 | 12 |
| Остальные семь организаций | 0 | 0 |
| **Всего** | **161** | **2542** |

Повторный dry-run в 09:33:35–09:33:39 UTC дал нули по обоим счётчикам у всех
11 организаций; повторного apply не было. 15953 заказа без доказательства
передачи остались пропущенными. Их исторические факты не удалялись и даты
не выдумывались. Общее количество FBS-фактов в отдельном чтении базы выросло
с 11374 до 11535, ровно на 161.

## Доказательство сохранности

Во всех шести складских снимках — до и после обоих dry-run и самого apply —
совпали count и sum(quantity) отдельно по каждому tenant. Совокупно это
1769 строк остатков и 19600 единиц; эти числа не менялись в интервале проверки.

Дополнительно в независимых транзакциях READ ONLY / REPEATABLE READ перед
apply (09:32:10 UTC) и после него (09:33:31 UTC) сравнены число строк и
контрольная сумма **всех колонок каждой строки** 13 таблиц. Для каждой строки
использован `md5(row_to_json(t)::text)`, затем итоговая сумма по отсортированным
ID; запрос и точные результаты сохранены в JSON. Полностью совпали:

- `inventory_balances`, обе таблицы резервов FBS;
- `fbs_orders` и `fbs_order_products`;
- начисления и их строки, legacy/V2-счета, строки и источники V2;
- обе таблицы версий тарифов.

Финансовые таблицы в обоих снимках пусты. Множество дат FBS-фактов,
защищённых существующим начислением или прямой ссылкой счёта, тоже пусто
до и после; сам guard не отключался. Это проверка конкретного интервала,
а не утверждение, что работающая production-база никогда не меняется.

Полные stdout/stderr и исполнявшаяся оболочка сохранены локально в
`tmp/wms406-backfill-execution`; в Git включены этот отчёт и агрегированное
JSON-доказательство с контрольными суммами stdout. Заказы, товарные строки,
секреты и содержимое резервной копии в Git не выгружались.
