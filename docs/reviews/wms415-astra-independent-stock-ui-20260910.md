**В frozen `977bd21a6d9d8d2227edb0cbcd616205375c43f2` остаются четыре P2. P1 в назначенном срезе не установил. Отделение спорных 111/112 коммитом `5ec3df40` по исполняемому коду выполнено корректно.** Полную приёмку stock/UI этот снимок пока не проходит.

Код и документы читались через Git-объекты указанного SHA. Ниже номера строк относятся именно к нему. Сценарии UI установлены по обработчикам и состоянию компонентов; браузерного воспроизведения я не заявляю.

1. **P2 — браузерный Back закрывает приёмку в обход подтверждения несохранённого ввода.**

   В [App.tsx:2824](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/App.tsx:2824) эффект восстановления из URL при исчезновении `open_inbound` или смене маршрута напрямую закрывает документ и очищает выбранный ID. Проверка `ffDocDirty` находится в отдельном `closeFfDocument` на строке 2798, который здесь не вызывается.

   Сценарий: открыть приёмку из журнала, открыть габариты товара, изменить длину без сохранения и нажать Back. Значение остаётся только в `dimensionDraft`; [FfInboundRequestView.tsx:2102](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/FfInboundRequestView.tsx:2102) правильно сообщает родителю о несохранённом вводе, но URL-эффект удаляет форму без подтверждения. Это ошибка взаимодействия WMS-177 и WMS-179.

2. **P2 — вложенные окна FBS стирают подготовленное распределение без подтверждения.**

   Внешнее окно уже защищено `requestClose`, однако «Добавить товары в короб» на [FfFbsSupplyWorkspace.tsx:2778](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx:2778) закрывается прямым `setBoxAssignTarget(null)`. Повторное открытие на строках 2351–2354 очищает количества и выбранные позиции.

   Сценарий: ввести количества WB либо выбрать позиции Ozon, закрыть вложенное окно через Escape или клик вне окна, затем открыть снова. Подготовленный ввод исчезает без вопроса. Аналогичная цепочка есть у добавления заказов: закрытие на строках 2474/2524 обходится без проверки, повторное открытие очищает выбор на строке 521. Это оставшийся пробел WMS-179; прежнюю претензию к внешнему крестику FBS не повторяю — он уже защищён.

3. **P2 — одинаковые номера складов WB и Ozon ломают редактирование потолка через `/app/ff/fbs-stock`.**

   [FfProductsFbsPage.tsx:167](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/products-fbs/FfProductsFbsPage.tsx:167) задаёт строке WB числовой ключ `"123"`. Сервер при совпадении номеров возвращает раздельные ключи `"wb:123"` и `"ozon:123"` — [fbs_stock_rule_service.py:329](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/fbs_stock_rule_service.py:329). Диалог читает и изменяет значение по числовому ключу.

   Сценарий: сохранены потолки WB5/Ozon3 для складов с номером 123. Поле WB получает `0` вместо `5`. Изменение на `4` формирует одновременно `"123":4`, `"wb:123":5`, `"ozon:123":3`; сервер отклоняет правило с `warehouse_id_collision`.

   Цепочка подтверждена исполнением извлечённых frozen-функций с синтетическими входами, без HTTP и браузера. Несогласованность ключей существовала до `efe18c59/cbfb4b62`; это оставшаяся ошибка, а не регресс этих исправлений.

4. **P2 — общий stock-диалог обещает публикацию из суммарного остатка, хотя сервер считает отдельно по физическим складам.**

   [FbsStockDialog.tsx:181](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/ff/products-fbs/FbsStockDialog.tsx:181) передаёт общий `freeStock(product)` в распределение сразу между всеми направлениями. Исправленный сервер разделяет расчёт по физическим складам — [fbs_stock_rule_service.py:537](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/fbs_stock_rule_service.py:537).

   Синтетический сценарий: два физических склада, на каждом свободно 3 штуки, каждому направлению задано 50%. Frozen-функция интерфейса считает **3 + 3 = 6**, серверные функции — **1 + 1 = 2**. При этом диалог подписывает результат «уйдёт … прямо сейчас».

   Это неверное обещание интерфейса; отправку лишних четырёх штук finding не утверждает. Ошибка затрагивает общий диалог из отдельной страницы FBS и основного каталога. Она существовала раньше; старый finding о **серверном** warehouse preview исправлен и повторно не предъявляется.

**Отделение 111/112 считаю корректным в пределах проверки кода.** `5ec3df40` удаляет helper автоматического возврата, его вызовы, повторные обходы, новые поля классификации/времени/ссылки на возврат и соответствующее отображение. Поиск исключённых символов в исполняемом backend и относящихся frontend-файлах совпадений не дал.

Обычная отмена сохранена: [fbs_cancellation_service.py:178](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/app/services/fbs_cancellation_service.py:178) меняет статус, выполняет предусмотренное сторно начислений, отсоединяет заказ и освобождает резерв. `_release_reservation` продолжает делегировать общей операции остатков. Отмена не приходует физический товар. В Ozon сохранены импорты и вызовы этих операций; в merge сохранены блокировки ссылок с `NOWAIT` и обработка занятого объекта.

Собственная статическая проверка подтвердила совместимость всех четырёх вызовов `_apply_wb_status_to_order` в проверенных модулях. Все пять исходных спорных коммитов остаются предками frozen SHA — история не потеряна. Remote-архив заново не проверял. **STOP остаётся действующим; WMS-231 не даёт разрешения возобновлять автоматику или новые страницы 111/112.**

Старые исправленные findings не возвращаю в список: сохранность caps при переключении режимов, вложенный bulk JSON, общий источник free для GET/PUT, сортировка Product-lock в указанных unload-операциях подтверждены кодом. Для `977bd21a` обработка уже удалённой тары добавлена и в сохранение, и в проведение; чужой объект, ненулевой остаток и тара приёмки не трактуются как законно удалённая пустая тара. Старый warehouse P2 по этим путям закрыт на уровне кода. Статический разбор миграций также дал единственную конечную ревизию `20260910_0301`, поэтому прежний P1 о трёх heads больше не применим.

**Обязательные границы приёмки остаются следующими:**

- Проверить существующие inbound/MP/FBS окна через reload, Back/Forward, крестик, Escape и клик вне окна, включая вложенное распределение. Проверить обе реакции на подтверждение и отсутствие ложного предупреждения после успешного сохранения.
- Не считать WMS-177 полностью закрытым для FBS: [обычное открытие поставки:1020](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/frontend/src/screens/v2/FfFbsOrdersScreen.tsx:1020) не записывает `supply_id`, а закрытие на строке 1972 его не удаляет. Это прежняя незакрытая граница, не новый регресс рассматриваемых коммитов.
- Для stock пройти оба входа, single/bulk, проценты → прежние штуки после падения free, отказ увеличения, GET/PUT перечитывание, совпадающие номера WB/Ozon и два физических склада. Число оператора должно сохраняться; публикация ограничивается свободным остатком.
- Подтвердить складские сценарии и конкурентные операции на согласованном кандидате, затем отдельно CI и фактически развёрнутый SHA. Упаковка остаётся только признаком и не становится ограничением навигации.

Мои проверки — чтение frozen-кода, статический разбор и исполнение чистых функций в памяти. Авторские HTTP/PostgreSQL-тесты прочитаны, но за собственные прогоны не выдаются. БД, контейнеры, браузер и deploy не запускались; правок, Git-изменений и установки зависимостей не было. Dirty-документы координатора, работа WMS-418, mobile, секреты и клиентские данные в проверку не входили.

