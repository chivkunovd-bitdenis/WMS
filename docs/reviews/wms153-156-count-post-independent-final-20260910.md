**Ограниченный ACCEPT для `2798b3861216319ff0abe1af7432bd073bc01984`. Прежний P2 с повышением `SHARE → UPDATE` при проведении пересчётов закрыт; новых подтверждённых P1/P2 в этом исправлении не нашёл.**

Прочитал полный delta, отчёт и весь файл девяти PostgreSQL-сценариев. Родительская версия `inventory_count_service.py` совпадает с версией из ранее проверенного `94fb6485`, поэтому исправление относится непосредственно к найденному случаю.

Что подтверждено по коду:

- **Опасное ожидание убрано до первого движения.** После блокировки Product в порядке UUID вызывается `_lock_post_removal_stock`. Балансы берутся по ID, тара — по виду и ID, места палеты — по ID; дополнительные захваты используют `NOWAIT`. Конкурент, уже удерживающий Balance или Container и ожидающий Product, теперь вызывает отказ пересчёта, который освобождает Product полным откатом. См. [inventory_count_service.py:1615](/Users/deniscivkunov/Projects/WMS/.worktrees/wms062-warehouse-docs/backend/app/services/inventory_count_service.py:1615) и вызов на **1716**.
- **Учтены box, cargo_place и pallet, включая смежные остатки.** Предварительный захват включает нулевые строки и товары, скрытые фильтром документа. Для палеты захватываются также существующие остатки тех же товаров и места, используемые при расформировании. Это покрывает прочитанные поздние обращения `_detach_direct_pallet_balances` к остатку без тары в сортировке. Скрытые товары при этом не обнуляются: прежняя проверка `container_not_empty` сохраняется.
- **Отказы не оставляют частично проведённый документ.** `55P03` преобразуется в существующий `balance_changed_during_post` только после `session.rollback()`. Другие ошибки БД тоже откатываются и пробрасываются. Обычный `InventoryCountError`, включая `container_not_empty` после выполненного движения, теперь также вызывает полный откат. API уже возвращает для этих прикладных отказов 409. См. [inventory_count_service.py:1668](/Users/deniscivkunov/Projects/WMS/.worktrees/wms062-warehouse-docs/backend/app/services/inventory_count_service.py:1668).
- **Проверки принадлежности и удалённой тары сохранены.** Предварительная блокировка по физическому ID не заменяет проверки tenant, warehouse, вида тары и связи с приёмкой. Они выполняются прежним путём удаления и обработки устаревшего подтверждения. Чужой или неподходящий существующий объект не становится «уже удалённым».
- **Откат очищает отложенную публикацию.** Прочитан существующий обработчик `after_rollback`, очищающий очередь этой сессии — [fbs_stock_publish_service.py:183](/Users/deniscivkunov/Projects/WMS/.worktrees/wms062-warehouse-docs/backend/app/services/fbs_stock_publish_service.py:183).

Новые тесты проверяют именно нужные последствия: конкуренцию двух filtered-count для трёх видов тары; встречные `Container/Balance → Product`; отсутствие сохраняемых движений после отказа и последующего commit; успешное полное проведение с удалением/расформированием тары. Используются отдельные PostgreSQL-сессии и реальные сервисные операции; обёртки управляют очередностью. В двух сценариях ожидание дополнительно проверяется через `pg_blocking_pids`. См. [test_wms153_count_post_concurrency.py:81](/Users/deniscivkunov/Projects/WMS/.worktrees/wms062-warehouse-docs/backend/tests/test_wms153_count_post_concurrency.py:81).

**Граница приёмки:** дополнительный захват шире одного короба — он охватывает балансы соответствующих товаров, а для палеты также места. Конкурирующая работа может дать штатный 409 с необходимостью повторить проведение. Это не гарантия отсутствия всех взаимных блокировок в остальных warehouse/map-операциях и не приёмка всей складской ветки.

Авторские **9 + 43 = 52 PASS** и старое воспроизведение `40P01` отмечаю как его результаты, не собственный прогон. Сам тесты не повторял; `git diff --check` исправления прошёл. Код, файлы, БД и Git-состояние не менял. Новых сущностей, миграций, изменений упаковки или потолков этот commit не содержит. WMS-418 не затрагивал; его отдельный выпуск этим ревью не задерживается.

Координатор перенёс2798b386 как5c7d8656 и выполнил9 PostgreSQL tests нового concurrency-файла: PASS за12.68s. Scoped Ruff PASS. Общий release пока не выполнен; отдельный фильтр418 не зависит от этого исправления.
