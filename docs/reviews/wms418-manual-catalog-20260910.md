# WMS-418 — root проверил фильтр в полном каталоге

10.09.2026, около 11:52–11:58 МСК. Исходный код `95ab5fc48d0f4c7df808ec8a71d8d231d02e559c`.
Root сам вошёл учебным сотрудником через обычную форму и нажал элементы
существующего `http://127.0.0.1:5418/app/ff/products` в Chrome. Открыт полный
App с рабочим маршрутом, меню и темой WMS. Отдельного макета или подмены fetch
нет: Vite проксирует запросы в настоящий create_app() этого worktree на18418.
API использует отдельную SQLite с шестью синтетическими товарами, двумя
селлерами, без остатков и учётных записей маркетплейсов. Production не менялся.

Воспроизводимый запуск учебного API: `artifacts/wms418-manual-20260910/serve_catalog.py`.
Локальная БД и файлы исключены из Git. Тестовый пароль взят из существующего
test_products_ozon_catalog._register_admin; это не данные реального аккаунта.

Что root проверил кликами и чтением результата:

1. Обычный каталог загрузил все шесть товаров. В существующую строку фильтров
   добавлен «Передача остатков» с шестью предусмотренными вариантами.
2. «Включена на Ozon» дала товары1,2,4: учитывается и старое наследование NULL
   от WB для товара4. У всех нулевой остаток, но включённые товары видны.
3. Вместе с Первым учебным селлером осталось1,2. Дополнительные marketplaceOzon,
   категорияОдежда и поискOZ418-1 оставили ровно товар1, «Найдено:1из3».
   Скриншот полного окна визуально просмотрен root в tool result.
4. Выбрал товар1 checkbox, сменил передачу на «Выключена на обеих» — пустая
   выдача, остальные фильтры сохранены. Вернул Ozon — товар1 снова показан,
   обе checkbox перечитаны unchecked: скрытый прежний выбор не сохраняется.
5. Открыл существующий «Остаток для FBS», снял «Передавать остаток в Ozon»,
   нажал «Сохранить». Товар исчез из текущего Ozon-отбора без перезагрузки.
   Переключение на «Выключена на обеих» вернуло тот же товар при остальных
   фильтрах. Read-only SQLite подтвердил для WMS418-1 WB=0/Ozon=0.
6. Снял остальные фильтры. WB дал0,2,4; WBиOzon дал2,4; хотябыодна дал0,2,4;
   выключенонаобеих дал1,3,5. Состав прочитан из настоящих строк таблицы.

В учебном диалоге остатка ожидаемо показалось отсутствие WB Marketplace-ключа
и направлений: настройки/ключи не добавлялись. Это не помешало отключению Ozon.
Вызов browser fill("") не очистил видимое поле поиска; root прочитал его
value, затем обычные выделитьвсё/Backspace очистили поле. Причина поведения
инструмента не установлена, дефект приложения из этого не выводится.

Граница: это ручная UI-проверка изолированной установки, не production и не
прогон на тысяче товаров. Серверная пагинация/счётчики и фильтрация чужого
tenant отдельно проверены двумя существующими целевыми HTTP-тестами. Новых
изменений приложения и повторного полного набора тестов не потребовалось.

Для интеграции брать ТОЛЬКО кодовый95ab5fc4 и этот отчёт/seed. Весь worktree
merge запрещён: его база содержит старый спорный111/112. В общем кандидате
после переноса сохранить соседние правки stock и актуальный канон. Сам выпуск
и итоговое независимое ревью интеграционного diff остаются координатору.
