**ACCEPT в пределах проверки кода и миграционного пути — frozen `94fb648548b515b98d3b5b64f414cae070733f02`. Новых P1/P2 между проверенными срезами не обнаружил. Это не подтверждение готовности production к выкатке.**

Исходную production-ревизию `20260908_0257` принимаю из предоставленной read-only проверки. Сохранённый отчёт о deployed `eaa6ae9a` прочитан; повторного обращения к серверу не было.

Граф Alembic проверен по всем 163 ревизиям из Git: отсутствующих родителей и несвязанных ветвей нет, конечная ревизия одна — **`20260910_0301`**. От `0257` выполняются:

```text
0257 → 0100 → 0101 ┐
0257 → 0261        ├→ 0301
0257 → 0300        ┘
```

[Миграция 0301](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/alembic/versions/20260910_0301_merge_wms415_heads.py:3) действительно объединяет все три ветви. Собственная проверка через Alembic подтвердила путь из `0257`, продолжение из каждой промежуточной ветви и отсутствие повторных операций при уже достигнутой `0301`.

DDL согласован с проверенными моделями:

- `0100` добавляет `is_damaged`, заполняя существующие строки значением `false`; затем убирает серверное значение по умолчанию. Модель задаёт `False` при новых ORM-вставках.
- `0101` добавляет `empty_places` с начальным `[]`, сохраняя существующие документы.
- [0261:22](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/alembic/versions/20260910_0261_remove_legacy_fbs_quota.py:22) отказывает при оставшихся `is_fbs=true` **до своего DDL**. Удаляются только старый журнал, индекс и поле; миграция не переписывает остатки, движения, операторские потолки или обычные направления.
- Четыре таблицы `0300` совпадают с моделями по столбцам, типам, обязательности значений, внешним ключам, уникальности и индексам. Новые типы событий аудита добавлены как строковые константы, без изменения структуры `document_event`.

В [alembic/env.py:41](/Users/deniscivkunov/Projects/WMS/.worktrees/wms415-coord/backend/alembic/env.py:41) миграции выполняются внутри общей транзакции. В новых ревизиях нет явных commit или операций, выводящих DDL из неё. Deploy, backup и сетевые проверки **не изменились относительно принятого `f0bd66a2`**: старые обработчики останавливаются, backup проверяется, затем выполняются миграции и запускается приложение. Два ранее выложенных Ozon-файла также совпадают с `eaa6ae9a`.

**Восстановление данных требует отдельного внимания.** `downgrade` ревизии `0261` возвращает пустую схему журнала, а возврат до `0257` удаляет таблицы чата и новые поля. Поэтому успешный `upgrade → downgrade → upgrade` проверяет исполнимость схемы, но не сохранность появившихся после выпуска данных.

Для проверки backup подходит восстановление **в заранее созданную отдельную пустую БД**, с необходимыми ролями и расширениями:

```bash
pg_restore --single-transaction --exit-on-error \
  --dbname="$RESTORE_DB" "$BACKUP_FILE"
```

Это образец команды, **не выполненная операция**. `--single-transaction` обеспечивает применение восстановления целиком либо отказ; `--list` проверяет лишь оглавление архива. [Документация PostgreSQL](https://www.postgresql.org/docs/16/app-pgrestore.html). После появления новых production-записей возврат к старому backup требует решения об их сохранении.

Коммит исключения `5ec3df40` входит в кандидата; архивный исходный коммит `40a3216b…` существует в Git. Архив спорного исходного кода не следует использовать как готовую rollback-версию приложения.

**Самостоятельно выполнены:** проверка графа и порядка Alembic, генерация PostgreSQL-DDL для upgrade/downgrade пяти ревизий, исполнение отказа `0261` с подставным результатом проверки и сравнение схемы чата с моделями. PostgreSQL не запускал и SQL ни в одну БД не отправлял. Предыдущий PostgreSQL replay координатора не выдаю за собственный.

Перед реальной выкаткой остаются:

1. Подтвердить точный выпускаемый SHA, фактическую ревизию БД и отсутствие расхождения схемы с ожидаемой `0257`.
2. Пройти проверку реальной топологии и получить восстановимый backup; не обходить отказ `0261`.
3. Подтвердить завершение миграций на `0301` и запуск API/worker/beat/web из выпускаемого SHA.
4. Проверить публичные HTTPS-маршруты, настоящий клиентский IP через оба Caddy, закрытие внешних `8088/15174` и работу затронутых экранов.

Billing и неизменённый AUTH не пересматривались; файловых и runtime-изменений не было.