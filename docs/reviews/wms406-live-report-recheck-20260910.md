# WMS-406 — повторная проверка действующего отчёта 10.09.2026

Root проверил production по SSH и вызвал установленный `build_seller_report`
в транзакциях `REPEATABLE READ, READ ONLY`, без финансового расчёта и записей.
Контрольная сверка выполнена в 07:39 UTC (10:39 МСК).
Git HEAD `/opt/wms`: `0229831f7442823f086f0147ffa85aed05b8ca66`.
Действующие модули внутри `wms_prod-api-1` совпадают побайтно с прочитанным кодом:

- `billing_seller_report_service.py`: SHA-256 `70d6c1ac77dc145e2be49ef2cfbcdeec2867bb44dbe58719ee9c751838695d65`;
- `fbs_order_billing_service.py`: SHA-256 `579b482ed6e10e32a74d2572ba15bebaaf1614ed51341a67a5a9b28e8066843c`.

Для Loviana, период включительно по московскому времени:

| Период | Отгружено FBS | Упаковано |
| --- | ---: | ---: |
| 01.08–09.09.2026 | 931 | 931 |
| 01.08–10.09.2026 на момент чтения | 939 | 939 |

Для второго периода: 939 строк FBS, 939 уникальных исходных заказов, дублей 0.
Множество заказов отчёта совпало с результатом действующего
`confirmed_order_handover_dates` за период. Это проверка согласованности с
сохранёнными подтверждениями WMS, не независимая сверка физического склада с WB.
Функция использует `delivered_at` подходящей поставки и подтверждённые операции
`supply_deliver`; явное подтверждение операции приоритетнее изменяемой связи.

У этих WB-заказов отсутствуют строки `FbsOrderProduct`: прочитанная модель
определяет эту таблицу для позиций Ozon, WB использует `FbsOrder.product_id`.
Действующий контракт WB — одна штука на заказ; report использует fallback 1,
writer `_positions` — `(order.product_id, 1)`. Сумма quantity по таблице позиций
поэтому НЕ является отдельным подтверждением этих 939 штук.

Текущие статусы уже переданных заказов: sorted 406, cancelled 129, done 339,
defect 57, in_delivery 8. Отмена после передачи сама по себе не убирает
выполненную услугу из показателя отгрузки. Это не показатель продаж покупателям.

В организации Loviana сейчас только WB-заказы (10128); живой сценарий Ozon
этой выборкой не проверен. Более того, `record_fbs_order_confirmed` явно
сохраняет для Ozon прежний контракт статусов: WMS-406 менял WB. Нельзя
распространять вывод о подтверждённой передаче WB на все пути Ozon без проверки.

Root открыл `https://wms.sellerfocus.pro/app/ff/billing` в Chrome: показана
форма входа. Кнопки отчёта и отображение чисел в авторизованном интерфейсе
сейчас не проверены. Историческое восстановление повторно не запускалось;
тарифы, начисления, счета и склад не изменялись. Общая архитектура расчётов
WMS-408 остаётся проектом, а не выпущенной реализацией.
