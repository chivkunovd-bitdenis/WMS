# WMS-058 / WMS-084 / WMS-277 / WMS-395: итоговая независимая проверка

9 сентября 2026 года агент followup_final_adjudicator (Astra xhigh) прочитал
весь прикладной diff от production `40b722a4d2d0ff1a6a5899cdb2713a98d1265bc5`
до кандидата `35668258ac40ad212bdb15bbcafc2e269cf08455`: девять файлов приложения
и вызывающие их цепочки. Исходники, данные и внешние системы он не изменял,
тесты и браузер в этой проверке не запускал.

Обязательных дополнительных исправлений не найдено.

В WMS-058 скан тары выбирает источник без движения товара. При добавлении
сначала используется уже подобранное и ещё не разложенное количество;
физический подбор вызывается только для недостающего, с точным контейнером.
Блокировка строки поставки защищает вычисление разницы от двух сканов.
Все найденные вызовы `collect_into_box` проходят финализатор; промежуточные
операции выполняют `flush`, поэтому последующий отказ не сохраняет первую
часть. Вложенное создание короба и сортировочной ячейки отдельного commit
не добавляет.

WMS-084 и WMS-395 согласованы: существующая серверная отмена допускает WB-код
из пула, теперь оба места интерфейса допускают тот же источник. Успешный WB
DELETE предшествует локальной отвязке. Повторное назначение проверяет отсутствие
действующей привязки под блокировкой кода, удерживаемой до сохранения новой.
Код не становится `available`, а `introduced` сохраняется. Обычная замена и
терминальные состояния нового разрешения не получают. Запоздавший GET после
блокировки сверяет идентификаторы привязок и не применяет прежний ответ к
изменившемуся заказу.

WMS-277 перехватывает конфликт изменённого SKU до запроса габаритов. После
rollback сразу следует continue, а следующий вариант загружается заново.
Сопоставление, ID товара и складские операции не изменены. Противоречивый
вариант остаётся пропущенным: автоматического объединения размеров WB нет.

В diff не обнаружено новых навигационных блокеров WB/Ozon, складского эффекта
от признака упаковки или расширения отмены на Ozon. Прежнее замечание о
возврате MP-строки в россыпь повторно не предъявляется: публичный remove
требует draft, внутренний метод — confirmed/collecting; новой достижимой
цепочки не найдено.

Вердикт допускает переход к следующему этапу выпуска. Он не заменяет полный
CI, проверку фактического развёртывания, браузерную приёмку и обязательное
финальное ревью Opus max. Внешний положительный ответ настоящих WB/ЧЗ этой
проверкой не подтверждён.
