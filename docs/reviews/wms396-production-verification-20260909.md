# WMS-396 — production verification, 2026-09-09

Independent read-only verification **PASS** after root reported that the ordinary `prod-update.sh` deployment exited 0. At 13:02:20 UTC, production `/opt/wms` HEAD was exactly `0229831f7442823f086f0147ffa85aed05b8ca66`, the PR #212 merge. API, celery_worker and celery_beat each contained all 429 expected Python files under `/app/app` and `/app/alembic`, byte-for-byte SHA256 matches to reviewed source `4f124cfa646431aa6ae4a1d949bcfdb8155eab1b`. No files were missing, extra or different in any of these three containers.

All 179 web files matched the fresh baseline captured before deployment at 12:54:16 UTC from production `c3aa4dfaccbc99c6d58cf9e5a864bc248cc8ae3f`. The API, worker, beat and web containers were running and not restarting. Normal-TLS public GET `/api/health` returned HTTP 200 with `{"status":"ok"}`; GET `/` also returned 200, and its response body SHA256 matched runtime `index.html`. The first verification passed; no startup retry was needed. This establishes deployed files and health at the observation time, not a new browser acceptance test or a successful external marking-provider result.

The [verification result](artifacts/wms396-production-verification-20260909/verification.json) contains counts, differences, limited container states and public response outcomes. The [runtime manifests](artifacts/wms396-production-verification-20260909/runtime.json), [baseline](artifacts/wms396-production-verification-20260909/baseline.json), [expected Python manifest](artifacts/wms396-production-verification-20260909/expected-python.json) and [read-only verification script](artifacts/wms396-production-verification-20260909/verify-production.py) preserve the complete comparison. The subsequent source `0860fe221180f9d369c9ccbbe6697a6b33c153a4` changes tests and canonical documentation only; the Python expectation remains `4f124cfa`.

CI run [34353281295](https://github.com/chivkunovd-bitdenis/WMS/actions/runs/34353281295) was freshly read as completed/success. Its log reports **2305 passed, 52 skipped, 1 xfailed**, 14 warnings in 575.26 seconds; Ruff passed, Mypy found no issues in 436 files, and the frontend build completed. The [sanitized compact raw summary](artifacts/wms396-production-verification-20260909/ci-summary.txt) retains selected exact log lines, source line numbers and the complete source log SHA256; [CI metadata](artifacts/wms396-production-verification-20260909/ci.json) records the authoritative run state. CI checked the synthetic PR merge `53fd442` of `0860fe22` into `c3aa4dfa`; the independently checked production merge is `0229831f`. The earlier separately preserved PostgreSQL-only test proofs remain separate evidence; tests were not repeated for this verification.

Root additionally reported a bounded log read starting at 13:01:30 UTC: API 37 lines, worker 47 and beat 11, with zero traceback, ERROR or HTTP 5xx matches. This is explicitly a root-observed interval, not a second independent log check or a continuing guarantee. Root also reported deployment exit 0 and that database/Redis containers were not restarted. The verifier itself only read repository HEAD, file hashes, limited container states and public GET responses. It did not deploy, write application data, call the marking endpoint, inspect credentials or operate the mobile emulator.
