**SAFE-вердикт: `a40c124d` целиком пока не принимаю — найден один P2 в новой блокировке печатного актива. `1bd677f3` принимаю как ограниченное исправление требований Ozon по SKU на уровне кода.** Тесты, браузер и выпуск этим ревью не подтверждены.

**P2 · `a40c124d` — новая блокировка PrintAsset создаёт встречный порядок с Order.**

В [fbs_print_asset_service.py:1009](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/fbs_print_asset_service.py:1009) чтение содержимого теперь берёт печатный актив через `FOR UPDATE`. Далее первое открытие меняет `order.sticker_status` на строке 1058, а `session.flush()` на строке 1072 должен записать заказ. Получается порядок **PrintAsset → Order**.

Существующая встречная ветка есть в [ozon_box_assembly_service.py:148](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/ozon_box_assembly_service.py:148): `assemble_box_order` блокирует Order через `FOR UPDATE`, затем при восстановлении результата сборки вызывает `_invalidate_old_label` на строке 230. Этот helper обновляет тот же PrintAsset — строка 302. До этого обновления блокировка Order не освобождается.

Конкретный сценарий:

1. У заказа есть готовый печатный актив с `print_opened_at=None` и статусом стикера `ready`.
2. Сборка/восстановление Ozon блокирует Order и ожидает ответ проверки отправления.
3. Параллельный GET содержимого блокирует PrintAsset, читает заказ и при сохранении `print_opened` ждёт заблокированный Order.
4. Проверка Ozon возвращает результат; операция пытается обновить этикетку и ждёт PrintAsset.

Возникает цикл взаимного ожидания; PostgreSQL должен прервать одну из транзакций. **Это вывод из конкретного порядка операций в frozen-коде, не выполненный мной PostgreSQL-тест.**

Точка отката внутри `record_document_mutation` не защищает этот случай: блокировка актива взята раньше неё, а запись Order выполняется после helper. Поэтому утверждение «аудит не мешает складской операции» для всего патча сейчас не выполняется.

Исправление должно согласовать порядок блокировок чтения/открытия актива с существующими writers заказа и этикетки либо обеспечить однократную фиксацию открытия без обратного порядка. Нужна целевая проверка двух соединений: **открытие содержимого одновременно с обновлением этикетки**, с отсутствием взаимного ожидания и сохранением обычной операции. Добавлять запрет оператору ради истории не требуется.

В остальном ограниченном production-diff `a40c124d` дополнительных P1/P2 не установил:

- [document_event_service.py:272](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/services/document_event_service.py:272) берёт автора из контекста, установленного после аутентификации. Параметры сервисов не подменяют его; запрос имени проверяет одновременно ID пользователя и tenant.
- Имя и ID автора сохраняются в payload. [document_events.py:43](/Users/deniscivkunov/Projects/WMS/.worktrees/wms056-audit-trail/backend/app/api/document_events.py:43) использует снимок после переименования/удаления пользователя, а для старых событий сохраняет прежнее отображение связанного пользователя.
- Равные `before/after` пропускаются. Запись события не делает отдельного commit; ошибки записи изолируются вложенными точками отката. Это корректно для отказа записи истории, но не устраняет описанный внешний цикл блокировок.
- Используется существующий `DocumentEvent`; новых таблиц и колонок нет. Добавления в упаковке фиксируют изменения существующих полей и не добавляют складских движений, начислений или ограничений навигации.

Это оценка перечисленных мутаций, **не закрытие всей WMS-325**. Остальные пробелы из полного отчёта остаются за пределами принятого объёма.

**`1bd677f3` · ограниченный срез Ozon принимаю по коду, новых P1/P2 не нашёл.**

В [ozon_fbs_sync_service.py:624](/Users/deniscivkunov/Projects/WMS/.worktrees/wms352-ozon-publication/backend/app/services/ozon_fbs_sync_service.py:624) адресаты требований сохраняются по SKU; каталожная маркировка дополняет SKU соответствующей позиции. [ozon_fbs_marking_gate_service.py:35](/Users/deniscivkunov/Projects/WMS/.worktrees/wms352-ozon-publication/backend/app/services/ozon_fbs_marking_gate_service.py:35) ограничивает проверку нужными позициями. Обычная соседняя позиция больше не обязана иметь код только потому, что он нужен другому товару отправления.

Сохранены необходимые ограничения:

- отсутствие `requirements` не создаёт признак полного знания и не стирает ранее известные требования;
- старые записи без распределения по SKU проверяются консервативно;
- требуемый SKU, отсутствующий в сохранённом составе, не даёт положительной готовности;
- в проверке передачи `marking_required` и `marking_not_allowed` остаются предупреждениями через `soft = CHECK_WARNING`, а не навигационными блокировками.

Проверены фиксированные Git-источники, полный отчёт аудита и оба ограниченных production-diff. Авторские тесты прочитаны как свидетельства исполнителя; **сам тесты, БД, браузер и внешние операции не запускал**. Правок, Git-изменений и дочерних агентов не было.