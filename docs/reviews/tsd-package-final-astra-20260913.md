# Единое финальное Astra-review и аналитическая приёмка WMS-440–445

13.09.2026. **DEPLOYABLE TO STAGING.** Для предъявленного пакета не осталось подтверждённых кодовых P1/P2. Это допуск к выкладке на тестовый стенд по последнему решению владельца; завершение всех составных операторских и физических проверок не утверждается. Новые циклы review и дополнительные прогоны остановлены по timebox владельца. Продуктовый код в этом проходе не менялся.

## Предъявленная версия и предел проверки

Основной checkout `.worktrees/tsd-package-20260913`, ветка `codex/tsd-package-20260913`:
HEAD и origin совпали на **4b26a858ca3c885cf114786c1a47978858e912db**.
База сравнения — origin/etalon **675e69c7c97ac716a723249392499cdec8d275fc**.
Nested mobile HEAD — **f3c7f23abc33680a3f4a127634c753b73924e63d**; рабочее дерево mobile чистое.
Последняя дельта Android от сохранённого checkpoint 45676eee относится только к FBS ViewModel и его тестам; сохраняется вместе с этим отчётом как [восстановимый patch](artifacts/tsd-final-package-20260913/README.md).
SHA установленного APK сверён с локальным артефактом и приведён там же. Этот проход не пересобирал APK, не устанавливал серверный SHA работающего локального процесса и не проверял версию на стенде.

Единый итог использует чтение итоговых изменений и первичные сохранённые API/service/DB, browser и Android доказательства предыдущих адресных проверок. Их не называют новыми запусками на HEAD. Учитываются требования шести задач, актуальные правила AGENTS, навыки developer/analyst/product-decision и применимые правила из полных библиотек owner-cases/failure-cases, прочитанных в ходе работы над пакетом. Составные C-проверки с неполным покрытием остаются PARTIAL; таблица ниже отвечает именно на вопрос о допуске к staging. Она заменяет устаревшие статусы «разработчик исправляет» и прежние технические blocker-вердикты, а не переписывает непроведённые сценарии в PASS.

## Итог по задачам

| Задача | Итог | Основание и граница аналитической приёмки |
|---|---|---|
| WMS-440 | **READY** | Сохранённая приёмка C1–C15 подтверждает fresh draft A=3 → sorting, единый факт ФФ, клавиатуру и соседний seller plan/fact. Новых изменений этого пути после адресного закрытия дефекта нет. Полная приёмка задачи сохранена. |
| WMS-441 | **READY** | Исправления очереди/даты/последнего размещения и UUID закрыты. Есть native двух сотрудников → done и web перечитывание того же документа, конкурентные/повторные запросы проверены. C9 доказал потерю ответа после commit и восстановление одного факта; отдельная GET-503 ветка остаётся PARTIAL. |
| WMS-442 | **READY** | Прежние дефекты диалога, повторной печати, восстановления сохранённого задания, Windows copies и публикации закрыты. В итоговом workflow восстановлены branch triggers; publisher сверяет существующие immutable assets и SHA, не выполняет destructive clobber, новый release сначала draft. Физическая установка/печать C25 остаётся вне подтверждённого покрытия. |
| WMS-443 | **READY** | Единая серверная идентичность, ФИО/должность, сохранение ID/ролей и legacy-вход проверены в коде и сохранённых сценариях. Два native исполнителя одного документа дали разные правильные actor ID и видимое web-авторство. Весь migration/reset/APK11 набор в одном финальном UI-проходе не выполнен. |
| WMS-444 | **READY** | Независимые проверки закрыли D1/D3/D4, R13/C20 source accounting, полное удаление и pick/set. Ozon synthetic API→web→ledger показывает 3 × 40 + 3 × 30 = 210 руб., перечитывание не удваивает плату; fbs_pick больше не считается оплачиваемым FBS в детализации. Полный Android→ledger→web набор C18 остаётся PARTIAL. |
| WMS-445 | **READY** | C16–C20 подтверждены в независимых HTTP/DB/ViewModel проверках; F1–F3 закрыты. Рабочие статусы и tenant/effective seller/served фильтруются до pagination, просрочка не скрывает работу; scope-switch сбрасывает прежний UI и отвергает stale response, сохранённые pending операции не удаляет. Финальный native current queue проход не завершён. C21 реального клиента остаётся отдельным. |

## Доказательства и побочные границы

Для приёмки/сортировки/авторства используются [WMS-440 review](wms440-astra-review.md), [общее закрытие технических замечаний](wms441-445-final-astra-closure.md), актуальные [WMS-441](../requirements/WMS-441.md), [WMS-443](../requirements/WMS-443.md) и [совместный device checkpoint](artifacts/wms441_443_device_wip_20260913.json). Размещение сохраняет существующий документ и серверный факт; повтор одного намерения не превращается в новое движение. ФИО и должность не заменяют user ID и не повышают права. В частности, результат второго сотрудника не переписывает автора первого.

Для печати используются [Windows review](wms442-astra-windows-final-review.md), [приёмка диалога](wms442-astra-acceptance-20260913.md) и [публичный release review](wms442-astra-public-release-review.md). Последние F9/F10 из публичного review закрыты чтением окончательных `.github/workflows/print-agent-package.yml` и `tools/print-agent/publish_release.py`: обычная branch-сборка сохранена, publish ограничен release-тегом; повтор сверяет источник, список и содержимое assets, несовпадение останавливает публикацию до замены файлов. Новый publish в этом итоговом проходе не выполнялся. Новые print connection/job механизмы обслуживают явно требуемое подключение и восстановление печати, не вводят дополнительную плату или блокировку складских этапов.

Для расчётов используются [источники/C20](wms444-astra-c20-source-rereview.md) и [изолированный денежный web checkpoint](2026-09-13-wms444-final-wip.md). Смешанные known packed/unpacked/unknown партии сохраняют доказанное происхождение при уменьшении и полном удалении; новый unpacked подбор не возвращает старую готовность. Повтор абсолютного set0 не возвращает склад второй раз. Ставки контрольной матрицы: inbound10, return12, marketplace_outbound20, packing30, fbs_order40 руб./шт. Вспомогательный fbs_pick остаётся событием, но не оплачиваемой передачей; итог начисления привязан к реальному серверному триггеру и количеству. Доказательства service/DB не объявлены полным нативным C18.

Для текущей FBS очереди используются [review с закрытием F1–F3](wms445-astra-current-work-review-20260913.md) и [независимая матрица C16–C20](artifacts/wms445-astra-20260913/working_set_acceptance.py). Проверены new с supplier_status NULL/new, in_supply/assembling/packed; исключены неподходящий new supplier_status и in_delivery/sorted/done/cancelled/defect/external_processing. Старые рабочие WB/Ozon и просроченные без stock/mapping не теряются, фильтр применяется до limit/offset; 105 исторических строк не вытесняют рабочие страницы. Чужой tenant и seller закрыты, effective seller приоритетнее переданного параметра; WB served/active действует независимо от stock publication. Реальные обработчики терминальных статусов выводят заказы из очереди, ожидающие передачи сохраняют собственный рабочий статус. Повтор scope A→B больше не показывает старые заказы/выбор при ошибке и позднем ответе A. Проверка FBS не меняет вкладки упаковки, не создаёт новых сущностей/экранов и не удаляет сохранённое намерение уже начатой операции.

Новый изолированный native контур был подготовлен для контрольных 3 единиц, но остановлен на экране входа после команды владельца завершить review. Размещение не выполнено; ожидаемые значения fixture не выдаются за ledger/экран. Реальные аккаунты, production-передачи и WMS-433 не затрагивались.

## Открытые границы, не кодовые blockers для staging

1. **Физическое оборудование:** установка/запуск агента на рабочем Windows/macOS, реальный принтер и бумага, физический ТСД. Сохранённые build/checksum/эмуляторные результаты не заменяют эту приёмку.
2. **Неполное сквозное покрытие:** WMS-444 Android→ledger→web и WMS-445 current queue на финальном APK, часть составных UI/миграционных проверок и GET-503 часть WMS-441 C9 остаются PARTIAL. Это ограничение доказательств, подтверждённого кодового дефекта из него не следует.
3. **Реальный контур:** WMS-445 C21 «Виталик Хорс» недоступен из-за заблокированного Mac/отсутствия локальных данных. Synthetic WB/Ozon не засчитывается за него. Версия, миграции и поведение после выкладки на staging проверяются отдельно; этот отчёт не объявляет deploy или production acceptance.

**Подтверждённых открытых P1/P2: 0. Минимальный кодовый handoff не требуется.** Допуск: выкладывать проверяемый пакет на staging, сохраняя перечисленные границы открытыми. После выкладки нужны проверка фактически обслуживаемого SHA и доступности изменённых путей; это задача деплоя, а не новый цикл данного review.
