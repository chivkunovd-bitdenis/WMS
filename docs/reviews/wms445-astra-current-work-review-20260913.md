# WMS-445 — перекрёстное ревью рабочей ленты ТСД

**Вердикт: требуется исправление R12/C20.** Фильтр текущей складской работы
соответствует перечисленным статусам R11, но проверяемая реализация не обеспечивает
все заявленные границы селлера и смены учётного контекста. C21 «Виталик Хорс»
не выполнялась и не подменяется синтетическими тестами.

Ревьюер: отдельная Astra high после реализации Terra. Проверены основной commit
`a5a814b3f963097fb102ec5903f9fe04765eddbf` и commit вложенного mobile
`f9e5c9033ada8b17154a0ba52eed8fc67df2fa24`. Рабочий каталог —
`.worktrees/tsd-package-20260913`, ветка `codex/tsd-package-20260913`.
Прочитаны актуальные AGENTS.md основного checkout, требования WMS-445, навыки
wms-product-analyst и wms-product-decision, owner-cases.md и failure-cases.md целиком.
Код продукта ревьюер не менял. Временный отдельный Kotlin-тест удалён из mobile
после запуска; его исходник сохранён ниже как документ воспроизведения.

## Замечания для Terra

### F1 — P1: область селлера не удерживается в двух списках

В изолированной базе подготовлены сотрудник `fulfillment_staff` с правом
`can_packaging=true` и `seller_id=A`, два селлера A/B его организации, по заказу
и активной поставке каждого. Использованы настоящие зависимости авторизации
и HTTP-маршруты приложения, без подмены `get_effective_seller_id`.

Обычный `GET /operations/fbs-orders/worklist?status_group=tsd_working&marketplace=wb`
правильно возвращает только A. Однако тот же запрос с `seller_id=B` возвращает
заказ B с HTTP 200. В `backend/app/api/fbs_orders.py:546` явный параметр заменяет
действующую область селлера; последующая проверка ограничивает только организацию.

Одновременно `GET /operations/fbs-supplies/worklist?status_group=active&marketplace=wb`
без параметра селлера возвращает активную поставку B. Android делает именно этот
запрос в `FbsViewModel.kt:190`. Маршрут `backend/app/api/fbs_supplies.py:1230`
вообще не получает `get_effective_seller_id` и передаёт `seller_id=None` сервису.
Таким образом лента заказов и «В работе» показывают разную область данных.

Оба серверных пробела существовали до проверяемого diff. Это **незакрытое явное
R12**, а не утверждение, что новый фильтр впервые создал уязвимость. Смешения
разных организаций этим воспроизведением не обнаружено; два tenant отдельно
проверены штатным тестом и изолированы.

Исполнителю: применить действующую область селлера к обоим запросам, сохранив
всех селлеров своего tenant для сотрудника без seller scope. Явный чужой seller ID
не должен расширять scope. Не фильтровать по автору и не вводить новые сущности.
Добавить проверки scoped/unscoped сотрудника, двух селлеров и двух tenant для
заказов и активных поставок, включая явный параметр seller_id.

### F2 — P2: после успешной смены scope сохраняются выбранные ID прежнего входа

В том же экземпляре ViewModel: загрузить A с новым заказом `1`, отметить его,
сменить возвращаемый ApiProvider контекст на B и вызвать `loadOrders(append=true)`.
Ответ B содержит только заказ `2`; список правильно заменяется, но
`state.selected` остаётся `{1}`. Это воспроизведено отдельным Kotlin-тестом:
`Selected A IDs remain in B scope: [1]`.

Причина — fallback `?: state.value.selected` в `FbsViewModel.kt:207`.
`FbsScreens.kt:88` показывает кнопку «Новая поставка (1)» по этому набору,
а `create()` использует старые выбранные ID, хотя их уже нет в текущей ленте.
Это несоответствие контракта смены scope; нативный вход другого сотрудника
на устройстве в этом ревью не воспроизводился.

Исполнителю: при обнаруженной смене scope убрать выбор и контекст предыдущего
входа; сохранённую попытку восстанавливать только для нового scope. При обычном
обновлении того же входа не терять его выбор и начатую попытку. Проверить обе ветви.

### F3 — P1: ошибка загрузки нового scope оставляет данные старого tenant

В том же экземпляре ViewModel: загрузить контекст A с заказом `1`; переключить
ApiProvider на B; на новом `orders()` вернуть `IOException`. После выполнения
`loadOrders(append=true)` ошибка показана, но `state.orders` по-прежнему содержит
заказ A. Отдельный тест падает с `Old tenant order IDs remain: [1]`.

В `FbsViewModel.kt:179–201` различие scope вычислено до запросов, но замена
состояния отложена до успешного завершения **обоих** запросов. Общий `perform()`
при исключении сохраняет старое состояние и меняет только ошибку/busy.
Следовательно той же очистки нет и при отказе второго запроса поставок.

Исполнителю: при подтверждённом изменении scope очищать прежние данные до сети
и не публиковать ответ, если его контекст уже сменился. Добавить случаи отказа
orders и supplies и продолжение после повтора. Не удалять долговременную попытку
предыдущего сотрудника: при его возвращении она должна восстанавливаться в его
собственном scope. Проверка выполнена на ViewModel; утверждение о конкретной
production-сессии или межорганизационном раскрытии на устройстве не делается.

## Что проверено успешно и что осталось

| Область | Факт ревью | Граница |
|---|---|---|
| C16, точные статусы R11 | `tsd_working` содержит ровно new/in_supply/assembling/packed; дополнительное условие supplier NULL/new применяется только к new. Доставка, sorted, done, cancelled, defect и external_processing исключены. | Штатный HTTP-тест проверяет все эти состояния WB и new Ozon; полный симметричный набор Ozon и нативный экран не пройдены. Пустая строка supplier_status отдельно не проверена; SQL допускает NULL или case-insensitive new. |
| C17, старые и просроченные | В SQL этой группы нет условия даты/дедлайна, сопоставления или остатка. HTTP-тест подтверждает просроченный new WB 30-дневной давности, просроченный new Ozon и сортировку старейших. | Открытие старой активной поставки без подбора на устройстве ещё требует приёмки. |
| C18, страницы | Условие tsd_working стоит до cursor/limit. HTTP-проход с limit=1 возвращает каждый рабочий WB ID ровно один раз и завершается правильным cursor. Android запрашивает tsd_working для обеих площадок; штатный тест переключения WB→Ozon проходит. | Штатная фикстура имеет историю новее рабочих строк, а не больше страницы истории перед ними. Нативные возврат/длинная дата и расширенная пагинация ещё не пройдены. |
| C19, сохранение собственных операций | Diff не меняет синхронизацию, статусы, запись данных или передачу. `_apply_status` Ozon сохраняет локальные in_supply/assembling/packed при awaiting_deliver; новый SQL не отбрасывает их по supplier_status. Активные поставки по-прежнему запрашиваются отдельно. | Это чтение кода, не новый тест внешних sold/cancel/delivered/cancelled и не испытание потери ответа передачи. C19 не закрыта. |
| C20, границы | HTTP-тест штатного набора подтверждает tenant и served=false; чтение SQL подтверждает is_active/served без publish_enabled/автора. | Выявлены F1–F3. Два селлера, effective scope и отказы смены контекста отсутствуют в штатном тесте реализации. |
| Новые экраны/сущности | Два product-diff затрагивают только серверный read-фильтр и Android ViewModel. Миграций, таблиц, экранов, дизайна и новых действий нет. | Визуальное сравнение экрана в этом ревью не выполнялось. |
| C21 | Не проверено. | Реальные user/tenant/seller, endpoint/APK и проблемные заказы «Виталик Хорс» не установлены этим ревью. |

Новый параметр `tsd_working` — внутренний вариант запроса; состав прежних web-групп
не изменён. Связи поставок, резерв, остатки, печать и обработка внешней
неопределённости в двух commits не менялись. Прежняя приёмка C1–C15 не заменяется
этим ревью и не получает дополнительных положительных вердиктов.

## Независимые запуски

- Backend: `ruff check .` — успешно; `mypy .` — успешно, 458 файлов.
- Backend: `pytest -n auto tests/test_fbs_worklist_query_count.py` — **11 passed**.
- Android: фактический `:app:testDebugUnitTest` с FbsViewModelTest и дополнительным
  FbsScopeReviewTest — **56 tests, 2 failed**. Все 54 штатных теста прошли;
  оба дополнительных теста воспроизвели F2/F3. Начальный вызов Gradle был
  UP-TO-DATE и не использован как единственное доказательство.
- Review HTTP: scoped сотрудник A → default orders только A; explicit B orders
  возвращает B; default active supplies содержит B. Все три наблюдения `true`.
- Проверка документа: в этом worktree отсутствует `scripts/ci/check_task_documents.py`.
  Запуск актуального скрипта основного checkout с cwd этого worktree завершился
  сообщением «AGENTS.md и CLAUDE.md должны существовать и содержать одинаковые
  правила». Поэтому успешная проверка документа этим скриптом не заявляется;
  общие правила ветки в рамках ревью WMS-445 не изменялись.

Исходники воспроизведения и компактный результат:
[scope_repro.py](artifacts/wms445-astra-20260913/scope_repro.py),
[FbsScopeReviewTest.kt](artifacts/wms445-astra-20260913/FbsScopeReviewTest.kt),
[android-results.json](artifacts/wms445-astra-20260913/android-results.json).

HTTP-скрипт запускается из `backend` с `PYTHONPATH=.` через существующую Python
venv. Он использует тестовую SQLite из conftest, не локальную рабочую БД или
площадки. Для Kotlin скопировать приложенный файл в
`mobile/android/app/src/test/java/ru/wms/tsd/features/fbs/`, выполнить:

```sh
ANDROID_HOME=/opt/homebrew/share/android-commandlinetools GRADLE_OPTS=-Xmx1200m ./gradlew --max-workers=2 -Dkotlin.compiler.execution.strategy=in-process :app:testDebugUnitTest --tests ru.wms.tsd.features.fbs.FbsViewModelTest --tests ru.wms.tsd.features.fbs.FbsScopeReviewTest
```

Команда выполняется из `mobile/android`; после проверки убрать только собственную
временную копию. Подтверждённые F1–F3 передаются Terra для исправления и предметного
повторного ревью. Никакой продуктовой уступкой эти технические расхождения не закрыты.
