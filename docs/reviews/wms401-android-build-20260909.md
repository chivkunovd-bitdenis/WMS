# WMS-401: реальный Android APK, 09.09.2026

Из исходников мобильной ветки `codex/wms397-mobile`, commit `dfcb7624216a6bb26bae1641357d4145764b85cf`, собран новый debug APK. Исходники приложения не менялись. Это результат сборки и установки, не подтверждение работы полного ФБС на складе.

## Артефакт и проверки

Файл: `/Users/deniscivkunov/Projects/WMS/outputs/wms401-android-20260909/WMS-TSD-FBS-dfcb762-debug.apk`.
Размер: 45 215 728 байт. SHA256: `1b5ab4498a451416873441a7afdae695f86f74166ae3093ba2a1ca0e2bbabb09`.
Пакет `ru.wms.tsd`, versionCode 5, versionName `0.1.4-atol7`, minSDK 24 (Android 7), target/compile SDK 35. В DEX проверено наличие новых `FbsScreensKt` и `FbsViewModel`. `apksigner verify --verbose` успешен, схема v2; использована существующая debug-подпись. Release-подпись, ключи и remote мобильного репозитория не менялись.

Gradle 8.11.1, Java 17, существующий Android SDK 35. Один процесс Gradle, максимум два worker, JVM 2 GiB. Выполнены только `:app:testDebugUnitTest` с фильтрами `ru.wms.tsd.features.fbs.FbsViewModelTest` и `ru.wms.tsd.features.fbs.FbsApiContractTest` (9+2 теста, 0 ошибок/падений/пропусков), затем `:app:assembleDebug`. Обе команды BUILD SUCCESSFUL. XML результатов и `build-proof.json` лежат рядом с APK. Полные Android тесты не запускались.

## Реальная установка и граница показа

Использован существующий AVD `wms_tsd` (Android 15/API 35, arm64), 2 GiB RAM/2 CPU. `adb install -r` вернул Success; MainActivity запущена. Userdata не стирали, приложение не удаляли. При холодном старте появилось системное окно System UI is not responding; нажата Wait, после чего экран приложения доступен.

На реальном экране входа сохранился пользователь review-user, активной сессии нет. Кнопка «Войти по паролю» нажата и не переключает экран. Подтверждение в `android/app/src/main/java/ru/wms/tsd/features/auth/LoginScreen.kt`: ветка savedStaff.isNotEmpty && !showPinDialog показывает список; обработчик onLoginWithPassword снова выставляет showPinDialog=false. Поэтому при сохранённом сотруднике обычный экран пароля и настройки сервера недостижимы этой кнопкой. На момент этого отчёта код не исправлялся; факт передан root.

Скриншоты реального эмулятора: `outputs/wms401-android-20260909/02-accounts.png` и `03-password-click.png` (в основном checkout). Секретов на снимках нет. В опубликованном Git находятся отчёт и доказательства сборки; APK хранится в постоянном ignored outputs и восстанавливается сборкой указанного мобильного SHA.

Существующий staging bearer проверен read-only: auth/me возвращает ожидаемый QA tenant `9c31f3f4-ce62-4c1f-891a-295b278f1e69`, роль fulfillment_admin. Сам bearer не выводился и не подставлялся вместо пароля. В проверенных конкретных проектных QA-документах и сценариях обычный пароль этого пользователя не найден. Сброс пароля, создание пользователя, обход входа и очистка данных не выполнялись.

Следовательно, список → поставка → подбор → упаковка/КИЗ → короба в этой сессии ещё не открыты; физических ТСД, сканирования и печати эта проверка не подтверждает. Debug API по умолчанию `http://10.0.2.2:18080/`; подключение нового APK к staging через обычный вход пока не выполнено. Это не production-сборка и не утверждение готовности ФБС к работе операторов.


## Продолжение: исправление входа

После прямого поручения root исправлен только переход на существующую форму пароля: мобильный commit `d15f09d4c09c74438ebd6b31632db0c346525a6f`, ветка `codex/wms397-mobile`. Diff — 3 добавленных/2 удалённых строки в LoginScreen.kt; состояние относится только к отображению формы. AuthManager, AuthStore, PIN, пароли, токены и дизайн не менялись. Remote мобильного Git по-прежнему отсутствует; безопасный patch этого единственного изменения сохранён рядом как `wms401-login-password-d15f09d.patch` для восстановления через git am, без публикации другой истории мобильного репозитория.

Новый APK: `/Users/deniscivkunov/Projects/WMS/outputs/wms401-android-20260909/WMS-TSD-FBS-d15f09d-debug.apk`, 45 350 978 байт, SHA256 `700d581c23a65f26089cadc25438eb176598d21817474210374335c4fa5a5056`. Подпись v2 проверена apksigner, установка -r Success. Первоначально ошибочно использованный фильтр AuthManagerTest не нашёл тестов (название файла отличается от класса); исправленная команда AuthManagerUrlPersistenceTest выполнила все 5 тестов успешно и assembleDebug BUILD SUCCESSFUL. Новые ФБС unit tests не повторялись, поскольку правка затрагивает только выбор формы входа.

На эмуляторе проверено: Home → «Сменить» → сохранённый review-user → «Войти по паролю» действительно открывает форму Email/Пароль; «Настройки сервера» раскрывает адрес. Снимки до/после опубликованы рядом с отчётом. Старый обзорный аккаунт использует локальный API `http://10.0.2.2:18080/`: открытый реальный экран FBS Orders показал ошибку соединения именно с этим адресом, а не данные staging. Проверять tenant через auth/me выключенного локального сервера невозможно. Содержимое зашифрованного хранилища учётных данных не извлекалось. Причина промежуточного появления Home при первом нажатии после установки не установлена; чистый повтор через «Сменить» отдельно подтверждает требуемый переход на форму пароля.

Граница остаётся: новая сборка запускается, проблема кнопки исправлена и проверена, но учебный вход на staging и полный маршрут поставка/подбор/упаковка/короба ещё не пройдены. Никаких операционных записей и печати при этой проверке не выполнялось.

Через обычные настройки формы введён `https://web-production-9e7c1.up.railway.app/api/`, нажата «Проверить соединение»: реальный APK показал «Соединение установлено». Снимок `wms401-android-evidence/15-stage-health.png`. Это проверка health по HTTPS, не авторизация; кандидат адреса сохраняется штатным AuthManager только после успешного входа.


## Продолжение: штатный доступ и реальная совместимость API

**Исправление вывода об авторизации:** первоначальный401 получен из-за ошибки агента при извлечении строки, а не отказа действительного пароля. Greedy regex захватил часть следующей строки вместо значения. Root выделил отдельно строку «Пароль», затем применил `re.search(r"`([^`\n]+)`", line)` и подтвердил login200 тем же существующим аккаунтом. Правильный пароль не менялся. Прежние формулировки об отвергнутом штатном пароле ниже описывают ошибочную промежуточную интерпретацию; они отозваны.

Проверены конкретные проектные QA seed/scripts и исходная история создания доступа WMS-388. В Git пароль намеренно не хранится. В исходной проектной сессии 07.09 `01a07bff-a7bc-7af0-adb8-cb3958c74522` найдено финальное сообщение с логином `denis.tsd.staging@example.com` и переданным владельцу паролем. Пароль извлечён только в память, не выводился и не записывался. Первый POST `auth/login` вернул401, потому что мой regex ошибочно выделил другую строку вместо действительного пароля. Это не был запрос с правильными существующими данными. Read-only `auth/staff-accounts` с прежним QA admin bearer подтвердил существующего пользователя `32bc08bd-e9b3-476e-8992-5519d0bc8be4`, роль fulfillment_staff и must_set_password=false. Этот401 оказался отказом ошибочно извлечённой строки; действительный существующий пароль работает. Изменения доступа не выполнялись.

В текущем backend auth.py нет device/QR login; приложение использует существующие password/PIN методы. Проверенные сохранённые web QA-сценарии авторизуются bearer из `.secrets/staging-token.txt`, что не является паролем для Android. Ни перенос bearer в password, ни обход авторизации не выполнялись.

След от локального API18080 найден в отчёте 06.09: PG `wms375_browser_20260906` существует, но read-only запрос показал 4 других пользователя, 6 FBS-заказов и 1 поставку; review-user там нет. В существующем e2e.db — 0 FBS-заказов/поставок, в отдельной oldOzon browser DB — по одному. Docker daemon не запущен. Совпадающая существующая база и штатные данные доступа review-user не установлены; произвольная база не подставлялась ради успешного снимка.

Отдельно выполнены GET реального staging с существующим QA bearer и проверкой tenant9c31f3f4-ce62-4c1f-891a-295b278f1e69. Ответы временно передавались именно скомпилированным Kotlin serializers текущего мобильного приложения, используя уже загруженные библиотеки JVM; scratch payloads удалены после выхода, Gradle и тесты повторно не запускались. Скрипт `wms401-stage-readonly-contract.py`, безопасный результат `wms401-android-evidence/stage-contract-proof.json`.

- FBS orders worklist: HTTP200, 59 заказов, FbsOrderPage DECODE_OK.
- Active supplies worklist: HTTP200, 23 поставки, FbsWorklistResponse DECODE_OK.
- Только чтение разрешённой QA поставки `4e38cabf-b129-4a28-85b0-3dd3631da6a4`: HTTP200, 2 заказа, FbsWorkspace DECODE_OK.
- Pick-options этой поставки: HTTP200, 2 товара; отдельная Kotlin-декодировка списка в данном проверочном скрипте не выполнялась.
- Её packaging task `77ae4141-6aab-4abd-86d9-8b7d5b190328`: HTTP200, PackagingTask DECODE_OK.
- Все статически объявленные FbsApi GET/POST/DELETE пути и методы найдены в реальной OpenAPI-схеме staging. Наличие маршрута не доказывает успешность записи.

Это подтверждает совместимость перечисленных чтений, но не UI-маршрут staging и не операции scan/КИЗ/pack/boxes/deliver/печать: записывающие методы ФБС не вызывались. Проверки пользователя, ставок, склада и целостности не отключались. Приложение дополнительно не менялось.


## Подготовленный следующий UI-сценарий — ещё не выполнен

Эмулятор оставлен на настоящей форме входа d15f09d со staging URL. Повторно проверенная актуальная OpenAPI требует application/json LoginBody с email и password; tenant/role/applicationId не передаются. Root продолжает проверку других действующих штатных доступов; операция создания нового пользователя или сброса данных не разрешалась и не выполнялась.

Свежий read-only worklist подтвердил свободные учебные заказы WB500024 (`9a1cfb5e-0f4d-492b-9cbf-7d45130faaae`, товар45587c50…/ШК2000000000013) и WB500013 (`a2799a3c-7f11-42b7-87ad-adf61c5e71bc`, товар1f0cefc8…/ШК2000000000012). Оба seller50110328-fa03-4604-b2e4-8ca27fc8bb41, status=new, supply_id=null, pick/pack=pending. Перед записью состояние перечитывается; существующие контрольные поставки root не используются.

После штатной авторизации: выбрать две строки → «Новая поставка (2)» → уникальное учебное название WMS401 Android 0909 и существующая сдача Склад/СЦ → «Создать». На поставке последовательно открыть «Подбор», вернуться, открыть «Упаковка и этикетки», вернуться, открыть «Короба» ещё до завершения стадий; это проверяет свободу переходов. Для рабочего продолжения явный источник берётся из свежего pick-options этой новой поставки, сканируются источник и товар; результат проверяется перечитыванием workspace и снимком строки. На упаковке скан QR собственного WB-заказа, синтетический учебный ЧЗ, при необходимости обычное подтверждение, затем отметка упаковки; она не служит условием навигации или списания. На коробах «Новый короб», QR собственного заказа, повторное открытие и проверка сохранённого состава. План расширен прямым поручением: диалоги и Android-предпросмотр печати входят в проверку; физическая печать и передача поставки WB не выполняются.

Ввод скана в эмуляторе использует уже существующий `ru.wms.tsd.DEV_SCAN` с extra `barcode`, который приходит через тот же ScannerManager и FbsViewModel; это симуляция события сканера, а не физическая проверка ATOL/камеры. Никакие экранные/API-проверки ради сценария не отключаются.


## Реальный проход APK: контрольная точка 09.09, 13:56 МСК

Правильный существующий пароль введён через обычную форму: успешный вход QA denis.tsd.staging@example.com, fulfillment_staff, tenant9c31f3f4-ce62-4c1f-891a-295b278f1e69. После отдельного разрешения выполнена штатная первичная установка локального PIN нового учебного сотрудника; существующий review-user не менялся. Ранее полученный 401 относился к ошибочно извлечённой агентом строке, а не к действительному паролю: исправление извлечения ограничивает поиск одной строкой «Пароль». Сброса серверного пароля не было.

Реальные скриншоты сохранены в wms401-android-evidence. Экран FBS показывает сначала 23 активные поставки, и лишь ниже все заказы; это видимое отклонение от требования первого экрана «все заказы», не считается согласованным UX. Даты отображаются сырыми ISO-строками.

Старые свободные WB500024/500013 имели истёкший срок. Попытка создания отклонена; отдельный штатный preflight подтвердил deadline_passed. Сроки и проверки не менялись. Root создал штатным WB-эмулятором свежие WB500045/500046 и импортировал обычным sync. В APK выбраны только они; кнопкой «Создать» создана поставка WMS401-Android-0909-1346, id85a8476c-05b4-46a8-a90f-77d5199257b4, WB-GI-928D876298FC, seller50110328-fa03-4604-b2e4-8ca27fc8bb41. GET подтвердил ровно два заказа.

Через существующий DEV_SCAN введены FBSVID-BOX-03 → 2000000000013, затем FBSVID-BOX-04 → 2000000000012. Первый новый источник при ещё выбранном BOX-03 вызвал ошибку «Товар не найден по штрихкоду в этой поставке»; снимок 34-both-picked.png на самом деле фиксирует эту ошибку, а не успех обоих подборов. После переоткрытия подбора BOX-04 и второй товар распознаны, обе строки зелёные (36-both-picked.png). GET подтвердил picked=2. Обе кнопки «Отметить упакованным» затем дали packed=2 (39-both-packed.png).

Сравнение source.quantity из GET pick-options перед первым подбором и после обеих упаковок: все количества обоих товаров по точным источникам совпадают. Доказательство own-supply-initial.json и own-after-pack.json. Отдельного снимка непосредственно между последним подбором и упаковкой нет; доказан совокупный неизменный физический остаток за подбор+упаковку. Упаковку ради повторной проверки не отменяли.

«Все QR» вызвано: GET показал stickers_ready=2 и два реальных asset URL. Android-предпросмотр пока не подтверждён — AVD сильно задерживает даже adb shell и screencap при высокой нагрузке хоста. ЧЗ lookup отправлен со стикером только собственного WB500046; успешная запись ЧЗ и короба ещё не доказаны в этой контрольной точке. Проверка продолжается.
