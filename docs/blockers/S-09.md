# Блокировки · S-09

## Блокировки

Нет блокировок. Все кандидаты оказались техникой.

## Без обоснования

Нет.

## Разошлись слои

Нет.

## Отсеяно

**3 кандидата** → техника обработки данных:

1. **`low_stock_threshold` валидация** (`frontend/src/screens/shared/HonestSignProductPage.tsx:314`)
   - Ternary-оператор при подготовке тела запроса: если пользователь оставил поле пустым, отправляем `null`, иначе число.
   - Это валидация формы перед отправкой на сервер, не блокировка; пользователь может свободно редактировать поле.

2. **`codeIds` проверка на пустоту** (`frontend/src/utils/printMarkingCodeLabel.ts:417`)
   - Возврат массива или `null` — техника структурирования данных.
   - Не блокирует ни одно действие пользователя.

3. **Ошибка API парсинг** (`frontend/src/utils/readApiErrorMessage.ts:87`)
   - Извлечение сообщения об ошибке из ответа сервера (`x?.msg`).
   - Это обработка сетевого ответа, не правило домена.
   - Общий для S-03, S-04, S-05, S-07, S-08, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-29, S-31, S-32.
