# Блокировки · S-08

## Блокировки

## Без обоснования

## Разошлись слои

## Отсеяно

**Всего кандидатов: 2 — оба техника.**

- `frontend/src/screens/shared/HonestSignPoolPage.tsx:319` — вычисление метаданных корзины через условный оператор (`detail ? poolBasketMeta(detail) : null`); используется в опциональном рендере UI-элемента при наличии загруженных данных. Это защита от undefined, а не доменная блокировка.

- `frontend/src/utils/readApiErrorMessage.ts:87` (ОБЩИЙ файл) — парсинг ошибок валидации из Pydantic: `const msg = typeof x?.msg === 'string' ? x.msg : null` — обработка структурированного ответа сервера. Это техническое условие в цепочке парсинга, не блокировка пользователя.
