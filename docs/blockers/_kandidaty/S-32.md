# Кандидаты в блокировки · S-32

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (3)

- `скрытие-в-null` · `frontend/src/screens/v2/SellerSettingsScreen.tsx:238`
  ```
      226     // eslint-disable-next-line react-hooks/exhaustive-deps -- reload when permission or token changes
    227   }, [permissions.staff, token])
    228 
    229   async function refreshWbCardsCount(): Promise<void> {
    230     try {
    231       const res = await fetch(apiUrl('/products/wb-catalog'), {
    232         headers: { ...authHeaders(token) },
    233       })
    234       if (!res.ok) {
    235         return
    236       }
    237       const rows = (await res.json()) as unknown[]
>   238       setWbCardsCount(Array.isArray(rows) ? rows.length : null)
    239     } catch {
    240       // ignore
    241     }
    242   }
    243 
    244   async function onSave() {
    245     setError(null)
    246     setOkMsg(null)
    247     const trimmed = contentKey.trim()
    248     if (!trimmed) {
    249       setError('Введите API ключ.')
    250       return
  ```
- `скрытие-в-null` · `frontend/src/screens/v2/SellerSettingsScreen.tsx:346`
  ```
      334   async function onSaveMarkingCredentials() {
    335     setError(null)
    336     setOkMsg(null)
    337     setBusy(true)
    338     try {
    339       const body: Record<string, unknown> = {
    340         mchd_id: mchdId.trim() || null,
    341         mchd_valid_until: mchdUntil.trim() || null,
    342         signing_method: signingMethod,
    343         edo_route: edoRoute,
    344         marketplace,
    345         auto_introduce: autoIntroduce,
>   346         auto_emit_limit: autoEmitLimit.trim() ? Number(autoEmitLimit) : null,
    347       }
    348       if (czToken.trim()) {
    349         body.cz_token = czToken.trim()
    350       }
    351       if (suzToken.trim()) {
    352         body.suz_oms_token = suzToken.trim()
    353       }
    354       if (mpKey.trim()) {
    355         body.mp_api_key = mpKey.trim()
    356       }
    357       const res = await fetch(apiUrl('/operations/marking-codes/self/credentials'), {
    358         method: 'PATCH',
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-29, S-31 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
