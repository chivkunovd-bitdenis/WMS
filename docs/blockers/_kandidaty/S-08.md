# Кандидаты в блокировки · S-08

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (2)

- `скрытие-в-null` · `frontend/src/screens/shared/HonestSignPoolPage.tsx:319`
  ```
      307       if (res.ok) {
    308         setHistory((await res.json()) as HistoryEvent[])
    309       }
    310     } finally {
    311       setHistoryBusy(false)
    312     }
    313   }
    314 
    315   if (!poolId) {
    316     return null
    317   }
    318 
>   319   const basketMeta = detail ? poolBasketMeta(detail) : null
    320 
    321   return (
    322     <Stack spacing={2} data-testid={`${testIdPrefix}-page`}>
    323       <Stack direction="row" spacing={1} sx={{ alignItems: 'center' }}>
    324         <IconButton
    325           component={RouterLink}
    326           to={`${routeBase}/honest-sign`}
    327           aria-label="Назад к списку пулов"
    328           data-testid={`${testIdPrefix}-back`}
    329         >
    330           <ArrowBackOutlined />
    331         </IconButton>
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
