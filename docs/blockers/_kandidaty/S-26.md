# Кандидаты в блокировки · S-26

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (29)

- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:74`
  ```
       62   open: boolean
     63   requestId: string | null
     64   token: string
     65   authHeaders: (t: string) => Record<string, string>
     66   warehouseId: string | null
     67   busy: boolean
     68   catalogScopeKey?: string
     69   onClose: () => void
     70   onRefreshList: () => Promise<void>
     71 }
     72 
     73 function statusRu(status: string): string {
>    74   if (status === 'draft') return 'Черновик'
     75   if (status === 'submitted') return 'Запланировано'
     76   if (status === 'confirmed') return 'Подтверждено'
     77   if (status === 'shipped') return 'Отгружено'
     78   if (status === 'cancelled') return 'Отменено'
     79   return status
     80 }
     81 
     82 export function SellerMarketplaceUnloadDialog({
     83   open,
     84   requestId,
     85   token,
     86   authHeaders,
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:75`
  ```
       63   requestId: string | null
     64   token: string
     65   authHeaders: (t: string) => Record<string, string>
     66   warehouseId: string | null
     67   busy: boolean
     68   catalogScopeKey?: string
     69   onClose: () => void
     70   onRefreshList: () => Promise<void>
     71 }
     72 
     73 function statusRu(status: string): string {
     74   if (status === 'draft') return 'Черновик'
>    75   if (status === 'submitted') return 'Запланировано'
     76   if (status === 'confirmed') return 'Подтверждено'
     77   if (status === 'shipped') return 'Отгружено'
     78   if (status === 'cancelled') return 'Отменено'
     79   return status
     80 }
     81 
     82 export function SellerMarketplaceUnloadDialog({
     83   open,
     84   requestId,
     85   token,
     86   authHeaders,
     87   warehouseId,
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:76`
  ```
       64   token: string
     65   authHeaders: (t: string) => Record<string, string>
     66   warehouseId: string | null
     67   busy: boolean
     68   catalogScopeKey?: string
     69   onClose: () => void
     70   onRefreshList: () => Promise<void>
     71 }
     72 
     73 function statusRu(status: string): string {
     74   if (status === 'draft') return 'Черновик'
     75   if (status === 'submitted') return 'Запланировано'
>    76   if (status === 'confirmed') return 'Подтверждено'
     77   if (status === 'shipped') return 'Отгружено'
     78   if (status === 'cancelled') return 'Отменено'
     79   return status
     80 }
     81 
     82 export function SellerMarketplaceUnloadDialog({
     83   open,
     84   requestId,
     85   token,
     86   authHeaders,
     87   warehouseId,
     88   busy: parentBusy,
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:77`
  ```
       65   authHeaders: (t: string) => Record<string, string>
     66   warehouseId: string | null
     67   busy: boolean
     68   catalogScopeKey?: string
     69   onClose: () => void
     70   onRefreshList: () => Promise<void>
     71 }
     72 
     73 function statusRu(status: string): string {
     74   if (status === 'draft') return 'Черновик'
     75   if (status === 'submitted') return 'Запланировано'
     76   if (status === 'confirmed') return 'Подтверждено'
>    77   if (status === 'shipped') return 'Отгружено'
     78   if (status === 'cancelled') return 'Отменено'
     79   return status
     80 }
     81 
     82 export function SellerMarketplaceUnloadDialog({
     83   open,
     84   requestId,
     85   token,
     86   authHeaders,
     87   warehouseId,
     88   busy: parentBusy,
     89   catalogScopeKey = '',
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:78`
  ```
       66   warehouseId: string | null
     67   busy: boolean
     68   catalogScopeKey?: string
     69   onClose: () => void
     70   onRefreshList: () => Promise<void>
     71 }
     72 
     73 function statusRu(status: string): string {
     74   if (status === 'draft') return 'Черновик'
     75   if (status === 'submitted') return 'Запланировано'
     76   if (status === 'confirmed') return 'Подтверждено'
     77   if (status === 'shipped') return 'Отгружено'
>    78   if (status === 'cancelled') return 'Отменено'
     79   return status
     80 }
     81 
     82 export function SellerMarketplaceUnloadDialog({
     83   open,
     84   requestId,
     85   token,
     86   authHeaders,
     87   warehouseId,
     88   busy: parentBusy,
     89   catalogScopeKey = '',
     90   onClose,
  ```
- `скрытие-в-null` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:104`
  ```
       92 }: Props) {
     93   const [modalBusy, setModalBusy] = useState(false)
     94   const [modalError, setModalError] = useState<string | null>(null)
     95   const [detail, setDetail] = useState<UnloadDetail | null>(null)
     96   const [stockRows, setStockRows] = useState<StockRow[]>([])
     97   const [catalog, setCatalog] = useState<SellerWbCatalogRow[] | null>(null)
     98   const [pickerOpen, setPickerOpen] = useState(false)
     99   const [wbWarehouses, setWbWarehouses] = useState<WbWarehouse[]>([])
    100   const [plannedDate, setPlannedDate] = useState<string | null>(null)
    101   const detailRequests = useRef(createLatestRequestSequence())
    102   const stockRequests = useRef(createLatestRequestSequence())
    103   const activeRequestId = useRef<string | null>(null)
>   104   activeRequestId.current = open ? requestId : null
    105 
    106   const isDraft = detail?.status === 'draft'
    107   const isSubmitted = detail?.status === 'submitted'
    108   const modalBusyEffective = modalBusy || parentBusy
    109 
    110   const stockByProductId = useMemo(() => {
    111     const m = new Map<string, StockRow>()
    112     for (const row of stockRows) {
    113       m.set(row.product_id, row)
    114     }
    115     return m
    116   }, [stockRows])
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:106`
  ```
       94   const [modalError, setModalError] = useState<string | null>(null)
     95   const [detail, setDetail] = useState<UnloadDetail | null>(null)
     96   const [stockRows, setStockRows] = useState<StockRow[]>([])
     97   const [catalog, setCatalog] = useState<SellerWbCatalogRow[] | null>(null)
     98   const [pickerOpen, setPickerOpen] = useState(false)
     99   const [wbWarehouses, setWbWarehouses] = useState<WbWarehouse[]>([])
    100   const [plannedDate, setPlannedDate] = useState<string | null>(null)
    101   const detailRequests = useRef(createLatestRequestSequence())
    102   const stockRequests = useRef(createLatestRequestSequence())
    103   const activeRequestId = useRef<string | null>(null)
    104   activeRequestId.current = open ? requestId : null
    105 
>   106   const isDraft = detail?.status === 'draft'
    107   const isSubmitted = detail?.status === 'submitted'
    108   const modalBusyEffective = modalBusy || parentBusy
    109 
    110   const stockByProductId = useMemo(() => {
    111     const m = new Map<string, StockRow>()
    112     for (const row of stockRows) {
    113       m.set(row.product_id, row)
    114     }
    115     return m
    116   }, [stockRows])
    117 
    118   const catalogById = useMemo(() => {
  ```
- `проверка-статуса` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:107`
  ```
       95   const [detail, setDetail] = useState<UnloadDetail | null>(null)
     96   const [stockRows, setStockRows] = useState<StockRow[]>([])
     97   const [catalog, setCatalog] = useState<SellerWbCatalogRow[] | null>(null)
     98   const [pickerOpen, setPickerOpen] = useState(false)
     99   const [wbWarehouses, setWbWarehouses] = useState<WbWarehouse[]>([])
    100   const [plannedDate, setPlannedDate] = useState<string | null>(null)
    101   const detailRequests = useRef(createLatestRequestSequence())
    102   const stockRequests = useRef(createLatestRequestSequence())
    103   const activeRequestId = useRef<string | null>(null)
    104   activeRequestId.current = open ? requestId : null
    105 
    106   const isDraft = detail?.status === 'draft'
>   107   const isSubmitted = detail?.status === 'submitted'
    108   const modalBusyEffective = modalBusy || parentBusy
    109 
    110   const stockByProductId = useMemo(() => {
    111     const m = new Map<string, StockRow>()
    112     for (const row of stockRows) {
    113       m.set(row.product_id, row)
    114     }
    115     return m
    116   }, [stockRows])
    117 
    118   const catalogById = useMemo(() => {
    119     const m = new Map<string, SellerWbCatalogRow>()
  ```
- `скрытие-в-null` · `frontend/src/components/SellerMarketplaceUnloadDialog.tsx:587`
  ```
      575               <TableCell align="right" sx={{ width: 120 }}>
    576                 К отгрузке
    577               </TableCell>
    578               <TableCell sx={{ width: 92 }} />
    579             </TableRow>
    580           </TableHead>
    581           <TableBody>
    582             {detail.lines.map((ln) => {
    583               const cat = catalogById.get(ln.product_id)
    584               const img = cat?.wb_primary_image_url ?? undefined
    585               const barcode =
    586                 cat?.wb_primary_barcode ??
>   587                 (cat?.wb_barcodes.length ? cat.wb_barcodes[0] ?? null : null)
    588               const available = stockByProductId.get(ln.product_id)?.available ?? 0
    589               return (
    590                 <TableRow
    591                   key={ln.id}
    592                   hover
    593                   data-testid="seller-mp-line-row"
    594                   sx={{
    595                     '& td': { px: 1.25 },
    596                     '& td:first-of-type': { pl: 1 },
    597                     '& td:last-of-type': { pr: 1 },
    598                   }}
    599                 >
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:71`
  ```
       59   display_number?: string | null
     60   warehouse_id?: string
     61   warehouse_name?: string | null
     62   status: string
     63   line_count: number
     64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
>    71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:72`
  ```
       60   warehouse_id?: string
     61   warehouse_name?: string | null
     62   status: string
     63   line_count: number
     64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
>    72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:73`
  ```
       61   warehouse_name?: string | null
     62   status: string
     63   line_count: number
     64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
>    73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:74`
  ```
       62   status: string
     63   line_count: number
     64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
>    74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:75`
  ```
       63   line_count: number
     64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
>    75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:76`
  ```
       64   goods_qty_total?: number
     65   planned_shipment_date?: string | null
     66   created_at?: string
     67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
>    76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:79`
  ```
       67 }
     68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
>    79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:80`
  ```
       68 
     69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
>    80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:81`
  ```
       69 export function sellerDocumentStatusRu(status: string, docType: DocType): string {
     70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
>    81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:82`
  ```
       70   if (docType === 'mp_unload') {
     71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
>    82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:83`
  ```
       71     if (status === 'draft') return 'Черновик'
     72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
>    83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
     95   displayNumber: string | null
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:84`
  ```
       72     if (status === 'submitted') return 'Запланировано'
     73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
>    84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
     95   displayNumber: string | null
     96   warehouse_name?: string | null
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:85`
  ```
       73     if (status === 'confirmed') return 'Подтверждено'
     74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
>    85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
     95   displayNumber: string | null
     96   warehouse_name?: string | null
     97   status: string
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:86`
  ```
       74     if (status === 'collecting') return 'На сборке'
     75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
>    86   if (status === 'verified') return 'Проверено на складе'
     87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
     95   displayNumber: string | null
     96   warehouse_name?: string | null
     97   status: string
     98   operation_type?: InboundOperationType
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:87`
  ```
       75     if (status === 'shipped') return 'Отгружено'
     76     if (status === 'cancelled') return 'Отменено'
     77     return UNKNOWN_DOCUMENT_STATUS_LABEL
     78   }
     79   if (status === 'draft') return 'Черновик'
     80   if (status === 'submitted') return 'Передано на склад'
     81   if (status === 'receiving') return 'Принимается на складе'
     82   if (status === 'sorting') return 'В сортировке'
     83   if (status === 'done') return 'Проведено'
     84   if (status === 'primary_accepted') return 'Принято на складе'
     85   if (status === 'verifying') return 'Проверка на складе'
     86   if (status === 'verified') return 'Проверено на складе'
>    87   if (status === 'posted') return 'Оприходовано'
     88   return UNKNOWN_DOCUMENT_STATUS_LABEL
     89 }
     90 
     91 type DocumentRow = {
     92   type: DocType
     93   id: string
     94   date: string | null
     95   displayNumber: string | null
     96   warehouse_name?: string | null
     97   status: string
     98   operation_type?: InboundOperationType
     99   line_count: number
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:190`
  ```
      178     })
    179   }, [inboundSummaries, mpUnloadSummaries, sort, type])
    180 
    181   function openDocument(row: DocumentRow): void {
    182     if (row.type === 'inbound') {
    183       navigate(`../inbound/${row.id}`)
    184     } else if (row.type === 'mp_unload') {
    185       setMpDialogId(row.id)
    186     }
    187   }
    188 
    189   async function deleteDraftDocument(row: DocumentRow): Promise<void> {
>   190     if (!token || row.status !== 'draft') {
    191       return
    192     }
    193     const key = `${row.type}:${row.id}`
    194     const path =
    195       row.type === 'inbound'
    196         ? `/operations/inbound-intake-requests/${row.id}`
    197         : row.type === 'mp_unload'
    198           ? `/operations/marketplace-unload-requests/${row.id}`
    199           : null
    200     if (!path) {
    201       return
    202     }
  ```
- `проверка-статуса` · `frontend/src/screens/v2/SellerDocumentsScreen.tsx:398`
  ```
      386                     ? inboundOperationTypeLabel(r.operation_type)
    387                     : r.type === 'mp_unload'
    388                       ? 'Отгрузка на МП'
    389                       : 'Акт расхождений'}
    390                 </TableCell>
    391                 <TableCell sx={{ color: 'text.secondary' }}>{r.date ?? '—'}</TableCell>
    392                 <TableCell sx={{ color: r.displayNumber ? 'text.primary' : 'text.secondary' }}>
    393                   {r.displayNumber ?? '—'}
    394                 </TableCell>
    395                 <TableCell>{sellerDocumentStatusRu(r.status, r.type)}</TableCell>
    396                 <TableCell align="right">{r.line_count}</TableCell>
    397                 <TableCell align="right">
>   398                   {r.status === 'draft' && (r.type === 'inbound' || r.type === 'mp_unload') ? (
    399                     <Button
    400                       size="small"
    401                       variant="outlined"
    402                       color="error"
    403                       disabled={deleteBusyKey === `${r.type}:${r.id}`}
    404                       data-testid="seller-delete-draft"
    405                       onClick={(e) => {
    406                         e.stopPropagation()
    407                         setDeleteError(null)
    408                         setDeleteConfirmRow(r)
    409                       }}
    410                     >
  ```
- `скрытие-в-null` · ОБЩИЙ с S-12, S-28, S-29 · `frontend/src/components/WmsDateField.tsx:24`
  ```
       12   minDate?: string | null
     13   testId?: string
     14   slotProps?: {
     15     textField?: { size?: 'small' | 'medium'; fullWidth?: boolean; sx?: object }
     16   }
     17 }
     18 
     19 function parseIso(value: string | null): Dayjs | null {
     20   if (!value || !/^\d{4}-\d{2}-\d{2}$/.test(value)) {
     21     return null
     22   }
     23   const d = dayjs(value, 'YYYY-MM-DD', true)
>    24   return d.isValid() ? d : null
     25 }
     26 
     27 function toIso(next: Dayjs | null): string | null {
     28   if (next == null || !next.isValid()) {
     29     return null
     30   }
     31   return next.format('YYYY-MM-DD')
     32 }
     33 
     34 export function WmsDateField({
     35   label,
     36   value,
  ```
- `запрет-кнопки` · ОБЩИЙ с S-12, S-28, S-29 · `frontend/src/components/WmsDateField.tsx:64`
  ```
       52     if (iso == null) {
     53       return
     54     }
     55     if (iso !== value) {
     56       onChange(iso)
     57     }
     58   }
     59 
     60   const picker = (
     61     <DatePicker
     62       label={label}
     63       value={draft}
>    64       disabled={disabled}
     65       minDate={minDate ? parseIso(minDate) ?? undefined : undefined}
     66       onChange={(next) => {
     67         // Локальный черновик при наборе секций; не шлём null в родителя (MUI даёт null при закрытии).
     68         setDraft(next)
     69       }}
     70       onAccept={(next) => {
     71         setDraft(next)
     72         commitDraft(next)
     73       }}
     74       slotProps={{
     75         textField: {
     76           size: slotProps?.textField?.size ?? 'small',
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-27, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
