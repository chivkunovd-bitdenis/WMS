# Кандидаты в блокировки · S-30

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (1)

- `ранний-выход` · ОБЩИЙ с S-13 · `frontend/src/utils/notificationsApi.ts:68`
  ```
       56   })
     57   if (!res.ok) {
     58     throw new Error(`notifications_read_all_failed:${res.status}`)
     59   }
     60   const body = (await res.json()) as { marked: number }
     61   return body.marked
     62 }
     63 
     64 export function resolveNotificationLink(
     65   link: string | null,
     66   portal: 'seller' | 'ff',
     67 ): string | null {
>    68   if (!link) return null
     69   if (link.startsWith('/app/')) return link
     70   if (portal === 'seller') {
     71     return link.startsWith('/') ? `/app/seller${link}` : link
     72   }
     73   if (link.startsWith('/app/ff')) return link
     74   return link.startsWith('/') ? `/app/ff${link}` : link
     75 }
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
