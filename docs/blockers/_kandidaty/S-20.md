# Кандидаты в блокировки · S-20

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (8)

- `проверка-статуса` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:66`
  ```
       54   sellers?: { id: string; name: string }[]
     55   loading?: boolean
     56   error?: string | null
     57   onRetry?: () => void | Promise<void>
     58 }
     59 
     60 type SortKey = 'date' | 'number'
     61 type SortDirection = 'asc' | 'desc'
     62 
     63 function statusColor(
     64   status: string,
     65 ): 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'info' {
>    66   if (status === 'draft') return 'default'
     67   if (status === 'submitted') return 'info'
     68   if (isReceivingStatus(status)) return 'warning'
     69   if (isSortingStatus(status)) return 'secondary'
     70   if (isDoneStatus(status)) return 'success'
     71   return 'primary'
     72 }
     73 
     74 function sortValue(row: InboundQueueRow, key: SortKey): string {
     75   if (key === 'date') return row.planned_delivery_date ?? row.created_at ?? ''
     76   return row.display_number ?? row.document_number ?? row.waybill_number ?? ''
     77 }
     78 
  ```
- `проверка-статуса` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:67`
  ```
       55   loading?: boolean
     56   error?: string | null
     57   onRetry?: () => void | Promise<void>
     58 }
     59 
     60 type SortKey = 'date' | 'number'
     61 type SortDirection = 'asc' | 'desc'
     62 
     63 function statusColor(
     64   status: string,
     65 ): 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'info' {
     66   if (status === 'draft') return 'default'
>    67   if (status === 'submitted') return 'info'
     68   if (isReceivingStatus(status)) return 'warning'
     69   if (isSortingStatus(status)) return 'secondary'
     70   if (isDoneStatus(status)) return 'success'
     71   return 'primary'
     72 }
     73 
     74 function sortValue(row: InboundQueueRow, key: SortKey): string {
     75   if (key === 'date') return row.planned_delivery_date ?? row.created_at ?? ''
     76   return row.display_number ?? row.document_number ?? row.waybill_number ?? ''
     77 }
     78 
     79 // Решение заказчика 17.08.2026: подсветки строки нет вообще (ни заливки, ни полосы слева). Красным
  ```
- `проверка-статуса` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:183`
  ```
      171       baseRows
    172         .filter((row) => sellerFilter === 'all' || row.seller_id === sellerFilter)
    173         .filter((row) => statusFilter === 'all' || row.status === statusFilter)
    174         .filter((row) => rowMatchesSearch(row as InboundQueueRow & InboundSummaryRef, search))
    175         .sort((a, b) => {
    176           const left = sortValue(a, sortKey)
    177           const right = sortValue(b, sortKey)
    178           const result = left.localeCompare(right, 'ru', { numeric: true })
    179           return sortDirection === 'asc' ? result : -result
    180         }),
    181     [baseRows, search, sellerFilter, sortDirection, sortKey, statusFilter],
    182   )
>   183   const newCount = baseRows.filter((row) => row.status === 'submitted').length
    184 
    185   const title = workspace === 'reception' ? 'Приёмка на FF' : 'Сортировка'
    186   const subtitle =
    187     workspace === 'reception'
    188       ? 'Список документов для складской приёмки.'
    189       : 'Разложение принятого товара по ячейкам хранения. Доступно к резерву только то, что уже разложено.'
    190 
    191   const requestSort = (key: SortKey) => {
    192     if (sortKey === key) {
    193       setSortDirection((current) => (current === 'asc' ? 'desc' : 'asc'))
    194       return
    195     }
  ```
- `запрет-кнопки` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:228`
  ```
      216   return (
    217     <Box data-testid={workspace === 'reception' ? 'ff-reception-page' : 'ff-sorting-page'}>
    218       <PageHeader title={title} description={subtitle} />
    219 
    220       {workspace === 'reception' && onCreateDraft ? (
    221         <Stack direction="row" spacing={1} sx={{ mb: 2, justifyContent: 'space-between', flexWrap: 'wrap', gap: 1 }}>
    222           <Typography variant="body2" sx={{ fontWeight: 700 }} data-testid="ff-inbound-new-count">
    223             Новые приёмки: {newCount}
    224           </Typography>
    225           <Stack direction="row" spacing={1}>
    226             <Button
    227               variant="contained"
>   228               disabled={creatingDraft}
    229               onClick={() => openCreateDialog('inbound')}
    230               data-testid="ff-inbound-create"
    231             >
    232               Создать приёмку
    233             </Button>
    234             <Button
    235               variant="contained"
    236               color="secondary"
    237               disabled={creatingDraft}
    238               onClick={() => openCreateDialog('return')}
    239               data-testid="ff-inbound-create-return"
    240             >
  ```
- `запрет-кнопки` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:237`
  ```
      225           <Stack direction="row" spacing={1}>
    226             <Button
    227               variant="contained"
    228               disabled={creatingDraft}
    229               onClick={() => openCreateDialog('inbound')}
    230               data-testid="ff-inbound-create"
    231             >
    232               Создать приёмку
    233             </Button>
    234             <Button
    235               variant="contained"
    236               color="secondary"
>   237               disabled={creatingDraft}
    238               onClick={() => openCreateDialog('return')}
    239               data-testid="ff-inbound-create-return"
    240             >
    241               Создать возврат
    242             </Button>
    243           </Stack>
    244         </Stack>
    245       ) : null}
    246 
    247       {workspace === 'reception' ? (
    248         <Stack direction={{ xs: 'column', md: 'row' }} spacing={1.5} sx={{ mb: 2 }}>
    249           <FormControl size="small" sx={{ minWidth: 200 }}>
  ```
- `запрет-кнопки` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:338`
  ```
      326               >
    327                 {sellerOptions.map((seller) => (
    328                   <MenuItem key={seller.id} value={seller.id}>
    329                     {seller.name}
    330                   </MenuItem>
    331                 ))}
    332               </Select>
    333             </FormControl>
    334           </DialogContent>
    335           <DialogActions>
    336             <Button
    337               onClick={closeCreateDialog}
>   338               disabled={creatingDraft}
    339               data-testid="ff-inbound-create-cancel"
    340             >
    341               Отмена
    342             </Button>
    343             <Button
    344               variant="contained"
    345               onClick={() => void submitCreateDialog()}
    346               disabled={creatingDraft || !draftSellerId}
    347               data-testid="ff-inbound-create-confirm"
    348             >
    349               Создать
    350             </Button>
  ```
- `запрет-кнопки` · ОБЩИЙ с S-17 · `frontend/src/screens/ff/FfInboundQueuePage.tsx:346`
  ```
      334           </DialogContent>
    335           <DialogActions>
    336             <Button
    337               onClick={closeCreateDialog}
    338               disabled={creatingDraft}
    339               data-testid="ff-inbound-create-cancel"
    340             >
    341               Отмена
    342             </Button>
    343             <Button
    344               variant="contained"
    345               onClick={() => void submitCreateDialog()}
>   346               disabled={creatingDraft || !draftSellerId}
    347               data-testid="ff-inbound-create-confirm"
    348             >
    349               Создать
    350             </Button>
    351           </DialogActions>
    352         </Dialog>
    353       ) : null}
    354 
    355       {filtered.length === 0 ? (
    356         <Alert severity="info" data-testid="ff-inbound-queue-empty">
    357           {workspace === 'reception'
    358             ? 'Нет приёмок в очереди.'
  ```
- `проверка-статуса` · ОБЩИЙ с S-12, S-17 · `frontend/src/utils/inboundQueues.ts:36`
  ```
       24   has_discrepancy?: boolean
     25   created_at?: string
     26   sorting_remaining_qty?: number
     27 }
     28 
     29 export function filterReceptionQueue(rows: InboundQueueRow[]): InboundQueueRow[] {
     30   return rows
     31 }
     32 
     33 export function filterSortingQueue(rows: InboundQueueRow[]): InboundQueueRow[] {
     34   return rows.filter(
     35     (r) =>
>    36       (r.status === 'sorting' || r.status === 'verified') &&
     37       (r.sorting_remaining_qty ?? 0) > 0,
     38   )
     39 }
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
