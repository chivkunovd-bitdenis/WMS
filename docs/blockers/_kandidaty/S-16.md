# Кандидаты в блокировки · S-16

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (7)

- `ранний-выход` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:128`
  ```
      116 
    117 function emptyDirectionDraft(): DirectionDraft {
    118   return {
    119     name: '',
    120     comment: '',
    121     quantity: '',
    122     is_fbs: false,
    123   }
    124 }
    125 
    126 function directionQuantityFromDraft(raw: string): number | null {
    127   const trimmed = raw.trim()
>   128   if (!trimmed) return null
    129   const qty = Number(trimmed)
    130   return Number.isInteger(qty) && qty >= 0 ? qty : null
    131 }
    132 
    133 function matchesCatalogSearch(
    134   row: {
    135     name: string
    136     wb_vendor_code: string | null
    137     sku_code: string
    138     wb_primary_barcode: string | null
    139     wb_barcodes: string[]
    140   },
  ```
- `скрытие-в-null` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:130`
  ```
      118   return {
    119     name: '',
    120     comment: '',
    121     quantity: '',
    122     is_fbs: false,
    123   }
    124 }
    125 
    126 function directionQuantityFromDraft(raw: string): number | null {
    127   const trimmed = raw.trim()
    128   if (!trimmed) return null
    129   const qty = Number(trimmed)
>   130   return Number.isInteger(qty) && qty >= 0 ? qty : null
    131 }
    132 
    133 function matchesCatalogSearch(
    134   row: {
    135     name: string
    136     wb_vendor_code: string | null
    137     sku_code: string
    138     wb_primary_barcode: string | null
    139     wb_barcodes: string[]
    140   },
    141   query: string,
    142 ): boolean {
  ```
- `скрытие-в-null` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:516`
  ```
      504       setDirectionDrafts((current) => ({ ...current, [productId]: emptyDirectionDraft() }))
    505       if (directions[productId] == null) {
    506         await loadDirections(productId)
    507       }
    508     },
    509     [directions, loadDirections],
    510   )
    511 
    512   const directionProduct = useMemo(
    513     () => rows.find((row) => row.id === directionProductId) ?? null,
    514     [directionProductId, rows],
    515   )
>   516   const drawerDraft = directionProduct ? directionDraftFor(directionProduct.id) : null
    517   const deleteBusy = deleteTarget ? directionBusy.has(deleteTarget.productId) : false
    518 
    519   const closeDirections = useCallback(() => {
    520     setDirectionProductId(null)
    521     setEditingDirectionId(null)
    522     setDeleteTarget(null)
    523   }, [])
    524 
    525   const startDirectionEdit = useCallback((productId: string, direction: StockDirectionRow) => {
    526     setError(null)
    527     setEditingDirectionId(direction.id)
    528     setDirectionDrafts((current) => ({
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:926`
  ```
      914                           noWrap
    915                         >
    916                           Свободный FBO {p.stock_free_fbo}
    917                         </Typography>
    918                       </Stack>
    919                     </TableCell>
    920                     <TableCell sx={{ minWidth: 0 }}>
    921                       <Button
    922                         size="small"
    923                         variant={p.has_packaging_instructions ? 'contained' : 'outlined'}
    924                         color={p.has_packaging_instructions ? 'primary' : 'inherit'}
    925                         onClick={() => openPackagingEdit(p)}
>   926                         disabled={!canManageCatalog}
    927                         data-testid={`ff-packaging-edit-${p.id}`}
    928                         aria-label={p.has_packaging_instructions ? 'Редактировать ТЗ' : 'Добавить ТЗ'}
    929                         title={p.has_packaging_instructions ? 'Редактировать ТЗ' : 'Добавить ТЗ'}
    930                         sx={{
    931                           minWidth: 64,
    932                           ...(p.has_packaging_instructions
    933                             ? {}
    934                             : { color: 'text.secondary', borderColor: 'divider' }),
    935                         }}
    936                       >
    937                         ТЗ
    938                       </Button>
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:981`
  ```
      969                         borderLeftColor: 'divider',
    970                       }}
    971                     >
    972                       <Stack direction="row" spacing={0.25} sx={{ justifyContent: 'center' }}>
    973                         <Tooltip
    974                           title={`Коды маркировки: ${markingCount}`}
    975                         >
    976                           <span>
    977                             <IconButton
    978                               size="small"
    979                               aria-label={`Коды маркировки ${p.sku_code}: ${markingCount}`}
    980                               data-testid={`ff-catalog-marking-link-${p.id}`}
>   981                               disabled={!canManageCatalog}
    982                               onClick={() => navigate(`/app/ff/honest-sign/product/${p.id}`)}
    983                             >
    984                               <Badge
    985                                 badgeContent={markingCount}
    986                                 color="warning"
    987                                 invisible={markingCount <= 0}
    988                                 overlap="circular"
    989                               >
    990                                 <QrCode2OutlinedIcon
    991                                   fontSize="small"
    992                                   color={markingCount > 0 ? 'warning' : 'disabled'}
    993                                 />
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/FfProductsCatalogScreen.tsx:1017`
  ```
     1005                         <Tooltip
   1006                           title={
   1007                             p.fbs_stock_limit != null
   1008                               ? `Остаток FBS: ${p.fbs_stock_limit} шт`
   1009                               : 'Остаток FBS не задан'
   1010                           }
   1011                         >
   1012                           <span>
   1013                             <IconButton
   1014                               size="small"
   1015                               aria-label={`Остаток FBS ${p.sku_code}`}
   1016                               data-testid={`ff-catalog-fbs-limit-${p.id}`}
>  1017                               disabled={!canManageCatalog}
   1018                               onClick={() => openFbsLimitDialog(p)}
   1019                             >
   1020                               <Inventory2OutlinedIcon
   1021                                 fontSize="small"
   1022                                 color={p.fbs_stock_limit != null ? 'primary' : 'disabled'}
   1023                               />
   1024                             </IconButton>
   1025                           </span>
   1026                         </Tooltip>
   1027                       </Stack>
   1028                     </TableCell>
   1029                   </TableRow>
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-18, S-19, S-26, S-27, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
