# Кандидаты в блокировки · S-21

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (3)

- `скрытие-в-null` · `frontend/src/screens/v2/WildberriesScreen.tsx:213`
  ```
      201                     variant="secondary"
    202                     data-testid="wb-sync-supplies"
    203                     disabled={wbSuppliesSyncBusy || !wbHasSuppliesToken}
    204                     onClick={onStartWbSuppliesSyncJob}
    205                   >
    206                     {wbSuppliesSyncBusy ? '…' : 'Обновить поставки'}
    207                   </Button>
    208                 </div>
    209 
    210                 <p className="subtle" data-testid="wb-sync-status">
    211                   Синхронизация: {wbJobStatus ?? '—'}
    212                 </p>
>   213                 {wbJobResult ? <p data-testid="wb-sync-result">{wbJobResult}</p> : null}
    214                 <p className="subtle" data-testid="wb-supplies-sync-status">
    215                   Синхронизация поставок: {wbSuppliesJobStatus ?? '—'}
    216                 </p>
    217                 {wbSuppliesJobResult ? (
    218                   <p data-testid="wb-supplies-sync-result">{wbSuppliesJobResult}</p>
    219                 ) : null}
    220               </>
    221             ) : (
    222               <p className="subtle">Нужен хотя бы один селлер.</p>
    223             )}
    224           </Card>
    225 
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/WildberriesScreen.tsx:243`
  ```
      231               Товар должен быть привязан к тому же селлеру, что выбран выше; nm_id — из списка
    232               импортированных карточек.
    233             </p>
    234 
    235             <form data-testid="wb-link-product-form" noValidate onSubmit={onLinkProductToWb}>
    236               <label>
    237                 Товар
    238                 <Select
    239                   name="wb_link_product_id"
    240                   data-testid="wb-link-product-id"
    241                   required
    242                   defaultValue=""
>   243                   disabled={!wbSellerId}
    244                 >
    245                   <option value="" disabled>
    246                     — выберите —
    247                   </option>
    248                   {wbSellerId
    249                     ? products
    250                         .filter((p) => p.seller_id === wbSellerId)
    251                         .map((p) => (
    252                           <option key={p.id} value={p.id}>
    253                             {p.sku_code} — {p.name}
    254                           </option>
    255                         ))
  ```
- `скрытие-в-null` · ОБЩИЙ с S-01, S-22, S-23, S-24, S-25 · `frontend/src/screens/AppV2Screens.tsx:37`
  ```
       25   hint,
     26   children,
     27 }: {
     28   title: string
     29   hint?: string
     30   children?: ReactNode
     31 }) {
     32   return (
     33     <Card className="card">
     34       <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
     35         <div>
     36           <h3 style={{ margin: 0, fontSize: 16 }}>{title}</h3>
>    37           {hint ? <p className="subtle">{hint}</p> : null}
     38         </div>
     39         {children}
     40       </div>
     41     </Card>
     42   )
     43 }
     44 
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
