# Кандидаты в блокировки · S-29

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (26)

- `скрытие-в-null` · ОБЩИЙ с S-12, S-28 · `frontend/src/components/WbProductPickerDialog.tsx:372`
  ```
      360               setPickerSearch(e.target.value)
    361               setPickerError(null)
    362             }}
    363             onKeyDown={(e) => {
    364               if (e.key !== 'Enter' || !catalog) {
    365                 return
    366               }
    367               e.preventDefault()
    368               const input = e.currentTarget.querySelector('input')
    369               const rawSearch = input?.value || (e.target as HTMLInputElement).value || pickerSearch
    370               const productId = resolveProductIdByBarcode(catalog, rawSearch)
    371               const targetId =
>   372                 productId ?? (filteredPickerRows.length === 1 ? filteredPickerRows[0]!.id : null)
    373               if (!targetId) {
    374                 setPickerError(notFoundMessage)
    375                 return
    376               }
    377               if (disabledProductIds.has(targetId)) {
    378                 setPickerError(inDraftMessage)
    379                 return
    380               }
    381               incrementPickerQty(targetId)
    382               setLastScannedProductId(targetId)
    383               setPickerSearch('')
    384               setPickerError(null)
  ```
- `скрытие-в-null` · ОБЩИЙ с S-12, S-28 · `frontend/src/components/WbProductPickerDialog.tsx:504`
  ```
      492                     }}
    493                     data-testid={`${testIdPrefix}-row`}
    494                     data-in-draft={inDraft ? '1' : '0'}
    495                   >
    496                     {variant === 'ff' ? (
    497                       <FfProductLineCells
    498                         meta={productDisplayMetaFromCatalog(
    499                           r.id,
    500                           { sku_code: r.sku_code, name: r.name },
    501                           catalogById,
    502                         )}
    503                         printTestId={`${testIdPrefix}-print-${r.id}`}
>   504                         nameExtra={inDraft ? inDraftCaption(inDraftMessage) : null}
    505                       />
    506                     ) : (
    507                       <>
    508                         <TableCell>
    509                           <ProductPhotoThumb src={r.wb_primary_image_url} />
    510                         </TableCell>
    511                         <TableCell
    512                           sx={{
    513                             whiteSpace: 'normal',
    514                             wordBreak: 'break-word',
    515                             overflow: 'hidden',
    516                             pl: 2,
  ```
- `скрытие-в-null` · ОБЩИЙ с S-12, S-28 · `frontend/src/components/WbProductPickerDialog.tsx:549`
  ```
      537                           sx={{
    538                             pl: 2,
    539                             maxWidth: 440,
    540                             whiteSpace: 'nowrap',
    541                             overflow: 'hidden',
    542                             textOverflow: 'ellipsis',
    543                           }}
    544                           title={r.name}
    545                         >
    546                           <Typography variant="body2" sx={{ lineHeight: 1.25 }} noWrap>
    547                             {r.name}
    548                           </Typography>
>   549                           {inDraft ? inDraftCaption(inDraftMessage) : null}
    550                         </TableCell>
    551                       </>
    552                     )}
    553                     {renderTrailingBodyCells?.(r)}
    554                     {showAvailableColumn ? (
    555                       <TableCell align="right">{available}</TableCell>
    556                     ) : null}
    557                     {qtyCell(r, inDraft)}
    558                   </TableRow>
    559                 )
    560               })}
    561               {filteredPickerRows.length === 0 && emptyMessage ? (
  ```
- `скрытие-в-null` · ОБЩИЙ с S-12, S-26, S-28 · `frontend/src/components/WmsDateField.tsx:24`
  ```
       12   minDate?: string | null
     13   testId?: string
     14   slotProps?: {
     15     textField?: { size?: 'small' | 'medium'; fullWidth?: boolean; sx?: object }
     16   }
     17 }
     18 
     19 function parseIso(value: string | null): Dayjs | null {
     20   if (!value || !/^\d{4}-\d{2}-\d{2}$/.test(value)) {
     21     return null
     22   }
     23   const d = dayjs(value, 'YYYY-MM-DD', true)
>    24   return d.isValid() ? d : null
     25 }
     26 
     27 function toIso(next: Dayjs | null): string | null {
     28   if (next == null || !next.isValid()) {
     29     return null
     30   }
     31   return next.format('YYYY-MM-DD')
     32 }
     33 
     34 export function WmsDateField({
     35   label,
     36   value,
  ```
- `запрет-кнопки` · ОБЩИЙ с S-12, S-26, S-28 · `frontend/src/components/WmsDateField.tsx:64`
  ```
       52     if (iso == null) {
     53       return
     54     }
     55     if (iso !== value) {
     56       onChange(iso)
     57     }
     58   }
     59 
     60   const picker = (
     61     <DatePicker
     62       label={label}
     63       value={draft}
>    64       disabled={disabled}
     65       minDate={minDate ? parseIso(minDate) ?? undefined : undefined}
     66       onChange={(next) => {
     67         // Локальный черновик при наборе секций; не шлём null в родителя (MUI даёт null при закрытии).
     68         setDraft(next)
     69       }}
     70       onAccept={(next) => {
     71         setDraft(next)
     72         commitDraft(next)
     73       }}
     74       slotProps={{
     75         textField: {
     76           size: slotProps?.textField?.size ?? 'small',
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:102`
  ```
       90 type WarehouseRow = { id: string; name: string; code: string }
     91 const UNKNOWN_INBOUND_STATUS_LABEL = 'Статус уточняется'
     92 
     93 type Props = {
     94   token: string
     95   authHeaders: (t: string) => Record<string, string>
     96   warehouseId: string | null
     97   warehouses?: WarehouseRow[]
     98   onRefreshInboundList: () => void | Promise<void>
     99 }
    100 
    101 export function sellerInboundStatusRu(status: string): string {
>   102   if (status === 'draft') {
    103     return 'Черновик'
    104   }
    105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
    108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:105`
  ```
       93 type Props = {
     94   token: string
     95   authHeaders: (t: string) => Record<string, string>
     96   warehouseId: string | null
     97   warehouses?: WarehouseRow[]
     98   onRefreshInboundList: () => void | Promise<void>
     99 }
    100 
    101 export function sellerInboundStatusRu(status: string): string {
    102   if (status === 'draft') {
    103     return 'Черновик'
    104   }
>   105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
    108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:108`
  ```
       96   warehouseId: string | null
     97   warehouses?: WarehouseRow[]
     98   onRefreshInboundList: () => void | Promise<void>
     99 }
    100 
    101 export function sellerInboundStatusRu(status: string): string {
    102   if (status === 'draft') {
    103     return 'Черновик'
    104   }
    105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
>   108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:111`
  ```
       99 }
    100 
    101 export function sellerInboundStatusRu(status: string): string {
    102   if (status === 'draft') {
    103     return 'Черновик'
    104   }
    105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
    108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
>   111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
    123   if (status === 'verified') {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:114`
  ```
      102   if (status === 'draft') {
    103     return 'Черновик'
    104   }
    105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
    108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
>   114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
    123   if (status === 'verified') {
    124     return 'Проверено на складе'
    125   }
    126   if (status === 'posted') {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:117`
  ```
      105   if (status === 'submitted') {
    106     return 'Передано на склад'
    107   }
    108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
>   117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
    123   if (status === 'verified') {
    124     return 'Проверено на складе'
    125   }
    126   if (status === 'posted') {
    127     return 'Оприходовано'
    128   }
    129   return UNKNOWN_INBOUND_STATUS_LABEL
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:120`
  ```
      108   if (status === 'receiving') {
    109     return 'Принимается на складе'
    110   }
    111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
>   120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
    123   if (status === 'verified') {
    124     return 'Проверено на складе'
    125   }
    126   if (status === 'posted') {
    127     return 'Оприходовано'
    128   }
    129   return UNKNOWN_INBOUND_STATUS_LABEL
    130 }
    131 
    132 function operationTypeRu(operationType: unknown): string {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:123`
  ```
      111   if (status === 'sorting') {
    112     return 'В сортировке'
    113   }
    114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
>   123   if (status === 'verified') {
    124     return 'Проверено на складе'
    125   }
    126   if (status === 'posted') {
    127     return 'Оприходовано'
    128   }
    129   return UNKNOWN_INBOUND_STATUS_LABEL
    130 }
    131 
    132 function operationTypeRu(operationType: unknown): string {
    133   return inboundOperationTypeLabel(operationType)
    134 }
    135 
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:126`
  ```
      114   if (status === 'done') {
    115     return 'Проведено'
    116   }
    117   if (status === 'primary_accepted') {
    118     return 'Принято на складе'
    119   }
    120   if (status === 'verifying') {
    121     return 'Проверка на складе'
    122   }
    123   if (status === 'verified') {
    124     return 'Проверено на складе'
    125   }
>   126   if (status === 'posted') {
    127     return 'Оприходовано'
    128   }
    129   return UNKNOWN_INBOUND_STATUS_LABEL
    130 }
    131 
    132 function operationTypeRu(operationType: unknown): string {
    133   return inboundOperationTypeLabel(operationType)
    134 }
    135 
    136 function lineActualQty(line: InboundLine): number {
    137   return line.effective_actual_qty ?? line.actual_qty ?? 0
    138 }
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:164`
  ```
      152     line.actual_qty != null ||
    153     line.effective_actual_qty != null ||
    154     line.added_by_fulfillment ||
    155     line.posted_qty > 0
    156   )
    157 }
    158 
    159 export function shouldShowSellerInboundNoWarehouse(
    160   warehouseId: string | null,
    161   routeRequestId: string | null,
    162   detailStatus: string | null,
    163 ): boolean {
>   164   return !warehouseId && (!routeRequestId || detailStatus === 'draft')
    165 }
    166 
    167 export function shouldShowSellerInboundLoadError(
    168   hasDetail: boolean,
    169   localError: string | null,
    170 ): boolean {
    171   return !hasDetail && localError !== null
    172 }
    173 
    174 export function sellerInboundPendingText(routeRequestId: string | null): string {
    175   return routeRequestId ? 'Загружаем карточку приёмки…' : 'Создаём черновик…'
    176 }
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:453`
  ```
      441       return false
    442     } finally {
    443       setBusy(false)
    444     }
    445   }
    446 
    447   const parsedPlannedBoxCount = (): number | null => {
    448     const raw = plannedBoxCountDraft.trim()
    449     if (!raw) {
    450       return null
    451     }
    452     const n = Math.floor(Number(raw))
>   453     return Number.isFinite(n) && n >= 1 ? n : null
    454   }
    455 
    456   const displayDocumentNumber = useMemo(() => formatHumanDocumentNumber(detail), [detail])
    457 
    458   const draftFieldsDirty = useMemo(() => {
    459     if (!detail) {
    460       return false
    461     }
    462     const detailDate = detail.planned_delivery_date ?? ''
    463     const draftBox = parsedPlannedBoxCount()
    464     const detailBox = detail.planned_box_count ?? null
    465     return (
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:586`
  ```
      574 
    575   const closeDocument = () => {
    576     if (draftFieldsDirty && !window.confirm('Закрыть без сохранения?')) {
    577       return
    578     }
    579     navigateToDocuments()
    580   }
    581 
    582   const linePrintMeta = (line: InboundLine, cat: WbCatalogRow | undefined): ProductLineDisplayMeta => {
    583     const barcode =
    584       cat?.wb_primary_barcode ??
    585       line.wb_barcode ??
>   586       (cat?.wb_barcodes.length ? cat.wb_barcodes[0] ?? null : null)
    587     return {
    588       sku_code: cat?.sku_code ?? line.sku_code,
    589       product_name: cat?.name ?? line.product_name,
    590       seller_name: null,
    591       wb_primary_image_url: cat?.wb_primary_image_url ?? null,
    592       wb_primary_barcode: barcode,
    593       wb_barcodes: cat?.wb_barcodes ?? (line.wb_barcode ? [line.wb_barcode] : []),
    594       wb_vendor_code: cat?.wb_vendor_code ?? null,
    595       wb_nm_id: cat?.wb_nm_id ?? null,
    596       wb_size: cat?.wb_size ?? null,
    597       wb_color: null,
    598       wb_brand: null,
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:607`
  ```
      595       wb_nm_id: cat?.wb_nm_id ?? null,
    596       wb_size: cat?.wb_size ?? null,
    597       wb_color: null,
    598       wb_brand: null,
    599       wb_composition: cat?.wb_composition ?? null,
    600       packaging_instructions: null,
    601     }
    602   }
    603 
    604   const hasWarehouseContext = Boolean(warehouseId) || warehouses.length > 0
    605   const sellerCanEdit =
    606     detail != null &&
>   607     (detail.status === 'draft' ||
    608       (detail.status === 'submitted' && hasWarehouseContext))
    609   const draftLocked = detail != null && !sellerCanEdit
    610   const readOnlyHint =
    611     detail?.status === 'submitted' && !hasWarehouseContext
    612       ? 'Склад не загрузился, поэтому заявка открыта только для просмотра. Обновите страницу или вернитесь к документам.'
    613       : 'Приёмку взял в работу склад, изменения теперь вносит фулфилмент'
    614   const draftOperationLabel = operationTypeRu(detail?.operation_type ?? requestedOperationType)
    615   const newDraftTitle =
    616     draftOperationLabel === 'Возврат' ? 'Новая заявка на возврат' : 'Новая заявка на поставку'
    617   const pageTitle = detail
    618     ? sellerCanEdit
    619       ? detail.status === 'submitted'
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:608`
  ```
      596       wb_size: cat?.wb_size ?? null,
    597       wb_color: null,
    598       wb_brand: null,
    599       wb_composition: cat?.wb_composition ?? null,
    600       packaging_instructions: null,
    601     }
    602   }
    603 
    604   const hasWarehouseContext = Boolean(warehouseId) || warehouses.length > 0
    605   const sellerCanEdit =
    606     detail != null &&
    607     (detail.status === 'draft' ||
>   608       (detail.status === 'submitted' && hasWarehouseContext))
    609   const draftLocked = detail != null && !sellerCanEdit
    610   const readOnlyHint =
    611     detail?.status === 'submitted' && !hasWarehouseContext
    612       ? 'Склад не загрузился, поэтому заявка открыта только для просмотра. Обновите страницу или вернитесь к документам.'
    613       : 'Приёмку взял в работу склад, изменения теперь вносит фулфилмент'
    614   const draftOperationLabel = operationTypeRu(detail?.operation_type ?? requestedOperationType)
    615   const newDraftTitle =
    616     draftOperationLabel === 'Возврат' ? 'Новая заявка на возврат' : 'Новая заявка на поставку'
    617   const pageTitle = detail
    618     ? sellerCanEdit
    619       ? detail.status === 'submitted'
    620         ? `Заявка передана на склад · ${operationTypeRu(detail.operation_type)}`
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:611`
  ```
      599       wb_composition: cat?.wb_composition ?? null,
    600       packaging_instructions: null,
    601     }
    602   }
    603 
    604   const hasWarehouseContext = Boolean(warehouseId) || warehouses.length > 0
    605   const sellerCanEdit =
    606     detail != null &&
    607     (detail.status === 'draft' ||
    608       (detail.status === 'submitted' && hasWarehouseContext))
    609   const draftLocked = detail != null && !sellerCanEdit
    610   const readOnlyHint =
>   611     detail?.status === 'submitted' && !hasWarehouseContext
    612       ? 'Склад не загрузился, поэтому заявка открыта только для просмотра. Обновите страницу или вернитесь к документам.'
    613       : 'Приёмку взял в работу склад, изменения теперь вносит фулфилмент'
    614   const draftOperationLabel = operationTypeRu(detail?.operation_type ?? requestedOperationType)
    615   const newDraftTitle =
    616     draftOperationLabel === 'Возврат' ? 'Новая заявка на возврат' : 'Новая заявка на поставку'
    617   const pageTitle = detail
    618     ? sellerCanEdit
    619       ? detail.status === 'submitted'
    620         ? `Заявка передана на склад · ${operationTypeRu(detail.operation_type)}`
    621         : newDraftTitle
    622       : `Карточка приёмки · ${operationTypeRu(detail.operation_type)}`
    623     : routeRequestId
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:619`
  ```
      607     (detail.status === 'draft' ||
    608       (detail.status === 'submitted' && hasWarehouseContext))
    609   const draftLocked = detail != null && !sellerCanEdit
    610   const readOnlyHint =
    611     detail?.status === 'submitted' && !hasWarehouseContext
    612       ? 'Склад не загрузился, поэтому заявка открыта только для просмотра. Обновите страницу или вернитесь к документам.'
    613       : 'Приёмку взял в работу склад, изменения теперь вносит фулфилмент'
    614   const draftOperationLabel = operationTypeRu(detail?.operation_type ?? requestedOperationType)
    615   const newDraftTitle =
    616     draftOperationLabel === 'Возврат' ? 'Новая заявка на возврат' : 'Новая заявка на поставку'
    617   const pageTitle = detail
    618     ? sellerCanEdit
>   619       ? detail.status === 'submitted'
    620         ? `Заявка передана на склад · ${operationTypeRu(detail.operation_type)}`
    621         : newDraftTitle
    622       : `Карточка приёмки · ${operationTypeRu(detail.operation_type)}`
    623     : routeRequestId
    624       ? 'Карточка приёмки'
    625       : newDraftTitle
    626   const warehouseLabel = useMemo(() => {
    627     if (!detail) {
    628       return '—'
    629     }
    630     const matched = warehouses.find((w) => w.id === detail.warehouse_id)
    631     if (matched) {
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:763`
  ```
      751             />
    752             {displayDocumentNumber ? (
    753               <Typography
    754                 variant="body2"
    755                 color="text.secondary"
    756                 data-testid="seller-inbound-document-number"
    757               >
    758                 Номер: <strong>{displayDocumentNumber}</strong>
    759               </Typography>
    760             ) : null}
    761             <Chip
    762               label={sellerInboundStatusRu(detail.status)}
>   763               color={detail.status === 'draft' ? 'default' : 'primary'}
    764               data-testid="seller-inbound-status-chip"
    765             />
    766             <Typography variant="body2" color="text.secondary" data-testid="seller-inbound-operation-type">
    767               Тип: <strong>{operationTypeRu(detail.operation_type)}</strong>
    768             </Typography>
    769             <Tooltip title={warehouseLabel}>
    770               <Typography
    771                 variant="body2"
    772                 color="text.secondary"
    773                 noWrap
    774                 data-testid="seller-inbound-warehouse-label"
    775                 data-task-id="BL-2"
  ```
- `проверка-статуса` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:813`
  ```
      801                 Сохранить
    802               </Button>
    803             ) : null}
    804             <Button
    805               variant="outlined"
    806               disabled={busy}
    807               onClick={closeDocument}
    808               data-testid="seller-inbound-close"
    809               sx={{ whiteSpace: 'nowrap', flexShrink: 0 }}
    810             >
    811               Закрыть
    812             </Button>
>   813             {detail.status === 'draft' ? (
    814               <Button
    815                 variant="contained"
    816                 color="secondary"
    817                 disabled={draftLocked || busy || detail.lines.length === 0}
    818                 onClick={() => void submitToWarehouse()}
    819                 data-testid="seller-inbound-submit-warehouse"
    820                 sx={{ whiteSpace: 'nowrap', flexShrink: 0 }}
    821               >
    822                 Передать на склад
    823               </Button>
    824             ) : null}
    825           </Stack>
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:890`
  ```
      878                   >
    879                     Действия
    880                   </TableCell>
    881                 </TableRow>
    882               </TableHead>
    883               <TableBody>
    884                 {detail.lines.map((ln) => {
    885                   const cat = catalogById.get(ln.product_id)
    886                   const img = cat?.wb_primary_image_url ?? undefined
    887                   const barcode =
    888                     cat?.wb_primary_barcode ??
    889                     ln.wb_barcode ??
>   890                     (cat?.wb_barcodes.length ? cat.wb_barcodes[0] ?? null : null)
    891                   const printLineMeta = linePrintMeta(ln, cat)
    892                   const factActual = lineActualQty(ln)
    893                   const factDelta = factActual - ln.expected_qty
    894                   const showFactCaption = draftLocked && lineHasFactData(ln)
    895                   return (
    896                     <TableRow
    897                       key={ln.id}
    898                       hover
    899                       data-testid="seller-inbound-line-row"
    900                       sx={{
    901                         '& td': {
    902                           px: 1.25,
  ```
- `запрет-кнопки` · ОБЩИЙ с S-28 · `frontend/src/screens/v2/SellerInboundDraftScreen.tsx:1051`
  ```
     1039                           right: 0,
   1040                           zIndex: 1,
   1041                           bgcolor: 'background.paper',
   1042                           borderLeft: '1px solid',
   1043                           borderLeftColor: 'divider',
   1044                         }}
   1045                       >
   1046                         <Stack direction="row" spacing={0.5} sx={{ justifyContent: 'center' }}>
   1047                           <Tooltip title="Печать товарного ШК">
   1048                             <span>
   1049                               <IconButton
   1050                                 size="small"
>  1051                                 disabled={!printLineMeta.wb_primary_barcode}
   1052                                 onClick={() => setPrintMeta(printLineMeta)}
   1053                                 data-testid="seller-inbound-line-print-barcode"
   1054                                 aria-label="Печать товарного ШК"
   1055                               >
   1056                                 <PrintOutlined fontSize="small" />
   1057                               </IconButton>
   1058                             </span>
   1059                           </Tooltip>
   1060                           {sellerCanEdit ? (
   1061                             <Button
   1062                               size="small"
   1063                               color="error"
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
