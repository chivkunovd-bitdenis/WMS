# Кандидаты в блокировки · S-15

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (5)

- `запрет-кнопки` · `frontend/src/screens/ff/FfPendingMarkingPage.tsx:164`
  ```
      152         <Link component={RouterLink} to="/app/ff/packaging" variant="body2" data-testid="ff-pending-marking-back">
    153           ← К упаковке
    154         </Link>
    155         <Chip
    156           size="small"
    157           label={`${lineCount} строк`}
    158           data-testid="ff-pending-marking-count"
    159         />
    160         {rows.length > 0 ? (
    161           <Button
    162             size="small"
    163             variant="contained"
>   164             disabled={selectedPrintableCount < 1}
    165             onClick={openSelectedPrint}
    166             data-testid="ff-pending-marking-print-selected"
    167           >
    168             Печать выбранных ({selectedPrintableCount})
    169           </Button>
    170         ) : null}
    171       </Stack>
    172       {error ? (
    173         <Alert severity="error" sx={{ mb: 2 }}>
    174           {error}
    175         </Alert>
    176       ) : null}
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfPendingMarkingPage.tsx:196`
  ```
      184             Все требуемые КМ напечатаны.
    185           </Typography>
    186         </Paper>
    187       ) : (
    188         <TableContainer component={Paper} variant="outlined" data-testid="ff-pending-marking-table">
    189           <Table size="small">
    190             <TableHead>
    191               <TableRow>
    192                 <TableCell padding="checkbox" sx={{ width: 48 }}>
    193                   <Checkbox
    194                     checked={allPrintableSelected}
    195                     indeterminate={somePrintableSelected}
>   196                     disabled={printableRows.length < 1}
    197                     onChange={(e) => toggleSelectAllPrintable(e.target.checked)}
    198                     data-testid="ff-pending-marking-select-all"
    199                   />
    200                 </TableCell>
    201                 <TableCell>Документ</TableCell>
    202                 <TableCell>Товар</TableCell>
    203                 <TableCell>Ячейка</TableCell>
    204                 <TableCell align="right">Осталось</TableCell>
    205                 <TableCell align="right">Доступно в пуле</TableCell>
    206                 <TableCell align="right">Действия</TableCell>
    207               </TableRow>
    208             </TableHead>
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfPendingMarkingPage.tsx:215`
  ```
      203                 <TableCell>Ячейка</TableCell>
    204                 <TableCell align="right">Осталось</TableCell>
    205                 <TableCell align="right">Доступно в пуле</TableCell>
    206                 <TableCell align="right">Действия</TableCell>
    207               </TableRow>
    208             </TableHead>
    209             <TableBody>
    210               {rows.map((row) => (
    211                 <TableRow key={row.packaging_task_line_id} data-testid="ff-pending-marking-row">
    212                   <TableCell padding="checkbox">
    213                     <Checkbox
    214                       checked={Boolean(selected[row.packaging_task_line_id])}
>   215                       disabled={!rowPrintable(row)}
    216                       onChange={(e) =>
    217                         toggleRowSelected(row.packaging_task_line_id, e.target.checked)
    218                       }
    219                       data-testid={`ff-pending-marking-row-select-${row.packaging_task_line_id}`}
    220                     />
    221                   </TableCell>
    222                   <TableCell>{row.document_number ?? '—'}</TableCell>
    223                   <TableCell>
    224                     {row.product_name}
    225                     <Typography variant="caption" sx={{ display: 'block' }} color="text.secondary">
    226                       {row.sku_code}
    227                     </Typography>
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfPendingMarkingPage.tsx:274`
  ```
      262                           variant="caption"
    263                           data-testid={`ff-pending-marking-task-link-${row.packaging_task_line_id}`}
    264                         >
    265                           Открыть задание
    266                         </Link>
    267                       </Stack>
    268                     )}
    269                   </TableCell>
    270                   <TableCell align="right">
    271                     <Button
    272                       size="small"
    273                       variant="outlined"
>   274                       disabled={!rowPrintable(row)}
    275                       onClick={() => openRowPrint(row)}
    276                       data-testid={`ff-pending-marking-print-${row.packaging_task_line_id}`}
    277                     >
    278                       Печать
    279                     </Button>
    280                   </TableCell>
    281                 </TableRow>
    282               ))}
    283             </TableBody>
    284           </Table>
    285         </TableContainer>
    286       )}
  ```
- `запрет-кнопки` · ОБЩИЙ с S-14 · `frontend/src/components/MarkingPrintDialog.tsx:1674`
  ```
     1662             </Button>
   1663           ) : separateMode ? (
   1664             <Button onClick={onClose} disabled={busy} data-testid="marking-print-separate-close">
   1665               Закрыть
   1666             </Button>
   1667           ) : (
   1668             <>
   1669               <Button onClick={onClose} disabled={busy}>
   1670                 Отмена
   1671               </Button>
   1672               <Button
   1673                 variant="contained"
>  1674                 disabled={printDisabled}
   1675                 onClick={() => void handlePrint({ forceReprint: forceReprintOnConfirm })}
   1676                 data-testid="marking-print-confirm"
   1677               >
   1678                 {effectiveReprint ? 'Перепечатать' : 'Печать'}
   1679               </Button>
   1680             </>
   1681           )}
   1682         </DialogActions>
   1683       </Dialog>
   1684     </>
   1685   )
   1686 }
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
