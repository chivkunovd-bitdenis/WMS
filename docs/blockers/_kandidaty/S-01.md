# Кандидаты в блокировки · S-01

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (2)

- `скрытие-в-null` · `frontend/src/screens/v2/ProductsScreen.tsx:57`
  ```
       45   const filtered = useMemo(() => {
     46     const s = q.trim().toLowerCase()
     47     if (!s) {
     48       return products
     49     }
     50     return products.filter((p) => {
     51       const hay = `${p.name} ${p.sku_code} ${p.seller_name ?? ''} ${p.wb_vendor_code ?? ''}`.toLowerCase()
     52       return hay.includes(s)
     53     })
     54   }, [products, q])
     55 
     56   const selected = useMemo(
>    57     () => (selectedId ? products.find((p) => p.id === selectedId) ?? null : null),
     58     [products, selectedId],
     59   )
     60 
     61   return (
     62     <Screen title="Товары (SKU)" subtitle="Таблица SKU, детали, создание и привязки">
     63       {catalogError ? (
     64         <Card className="card">
     65           <p className="error" data-testid="catalog-error">
     66             {catalogError}
     67           </p>
     68         </Card>
     69       ) : null}
  ```
- `скрытие-в-null` · ОБЩИЙ с S-21, S-22, S-23, S-24, S-25 · `frontend/src/screens/AppV2Screens.tsx:37`
  ```
       25   hint,
     26   children,
     27 }: {
     28   title: string
     29   hint?: string
     30   children?: ReactNode
     31 }) {
     32   return (
     33     <Card className="card">
     34       <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
     35         <div>
     36           <h3 style={{ margin: 0, fontSize: 16 }}>{title}</h3>
>    37           {hint ? <p className="subtle">{hint}</p> : null}
     38         </div>
     39         {children}
     40       </div>
     41     </Card>
     42   )
     43 }
     44 
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
