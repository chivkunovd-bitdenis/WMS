# Кандидаты в блокировки · S-27

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (2)

- `запрет-кнопки` · ОБЩИЙ с S-05 · `frontend/src/screens/shared/HonestSignScreen.tsx:533`
  ```
      521 
    522       <Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}>
    523         <Tooltip
    524           title="Выберите селлера"
    525           disableHoverListener={!importDisabled}
    526           disableFocusListener={!importDisabled}
    527           disableTouchListener={!importDisabled}
    528         >
    529           <Box component="span" sx={{ display: 'inline-flex' }}>
    530             <Button
    531               variant="contained"
    532               startIcon={<UploadFileOutlined />}
>   533               disabled={importDisabled}
    534               onClick={() => openImport()}
    535               data-testid={`${testIdPrefix}-open-import`}
    536             >
    537               Загрузить КМ
    538             </Button>
    539           </Box>
    540         </Tooltip>
    541         <Button
    542           variant="outlined"
    543           startIcon={<TimelineOutlined />}
    544           onClick={() => navigate(`${routeBase}/honest-sign/ledger`)}
    545           data-testid={`${testIdPrefix}-open-ledger`}
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
