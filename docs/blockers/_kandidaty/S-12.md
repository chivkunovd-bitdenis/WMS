# Кандидаты в блокировки · S-12

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (59)

- `скрытие-в-null` · `frontend/src/screens/ff/FfMarketplaceUnloadBoxAddDialog.tsx:378`
  ```
      366           return
    367         }
    368         setScanBarcode('')
    369         onAddSuccess?.(1)
    370         await onUpdated()
    371         await loadPickOptions({ silent: true })
    372         return
    373       }
    374       const errText = await scanRes.text()
    375       let errDetail: string | null = null
    376       try {
    377         const errBody = JSON.parse(errText) as { detail?: unknown }
>   378         errDetail = typeof errBody.detail === 'string' ? errBody.detail : null
    379       } catch {
    380         errDetail = null
    381       }
    382       if (errDetail === 'plan_limit_exceeded' && !allowOverPlan) {
    383         setPendingReadyBoxBarcode(barcode)
    384         setReadyBoxOverPlanOpen(true)
    385         return
    386       }
    387       if (addressStorageEnabled && !activeLocationId && errDetail === 'location_required') {
    388         setError('Сначала выберите ячейку или отсканируйте её штрихкод.')
    389         return
    390       }
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:189`
  ```
      177   id: string
    178   documentNumber: string | null
    179   plannedDate: string | null
    180   createdAt: string | null
    181   status: string
    182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
>   189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:190`
  ```
      178   documentNumber: string | null
    179   plannedDate: string | null
    180   createdAt: string | null
    181   status: string
    182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
>   190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:191`
  ```
      179   plannedDate: string | null
    180   createdAt: string | null
    181   status: string
    182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
>   191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:192`
  ```
      180   createdAt: string | null
    181   status: string
    182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
>   192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:193`
  ```
      181   status: string
    182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
>   193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:194`
  ```
      182   lineCount: number
    183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
>   194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:195`
  ```
      183   sellerName: string | null
    184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
>   195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:196`
  ```
      184   extraLabel: string | null
    185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
>   196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:197`
  ```
      185   ffModified: boolean
    186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
>   197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
    209 function kindRu(kind: DocKind): string {
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:198`
  ```
      186 }
    187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
>   198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
    209 function kindRu(kind: DocKind): string {
    210   if (kind === 'inbound') return 'Приёмка'
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:199`
  ```
      187 
    188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
>   199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
    209 function kindRu(kind: DocKind): string {
    210   if (kind === 'inbound') return 'Приёмка'
    211   if (kind === 'outbound') return 'Отгрузка'
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:200`
  ```
      188 function statusRu(status: string): string {
    189   if (status === 'draft') return 'Черновик'
    190   if (status === 'confirmed') return 'Утверждено'
    191   if (status === 'approved') return 'Утверждено'
    192   if (status === 'rejected') return 'Отклонено'
    193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
>   200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
    205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
    209 function kindRu(kind: DocKind): string {
    210   if (kind === 'inbound') return 'Приёмка'
    211   if (kind === 'outbound') return 'Отгрузка'
    212   if (kind === 'marketplace_unload') return 'Отгрузка на МП'
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:205`
  ```
      193   if (status === 'collecting') return 'На сборке'
    194   if (status === 'shipped') return 'Отгружено'
    195   if (status === 'submitted') return 'Запланировано'
    196   if (status === 'cancelled') return 'Отменено'
    197   if (status === 'primary_accepted') return 'Принято на складе'
    198   if (status === 'verifying') return 'Проверка'
    199   if (status === 'verified') return 'Проверено'
    200   if (status === 'posted') return 'Проведено'
    201   return status
    202 }
    203 
    204 function docStatusRu(kind: DocKind, status: string): string {
>   205   if (kind === 'discrepancy_act' && status === 'confirmed') return 'Передан на FF'
    206   return statusRu(status)
    207 }
    208 
    209 function kindRu(kind: DocKind): string {
    210   if (kind === 'inbound') return 'Приёмка'
    211   if (kind === 'outbound') return 'Отгрузка'
    212   if (kind === 'marketplace_unload') return 'Отгрузка на МП'
    213   return 'Расхождение'
    214 }
    215 
    216 function formatSignedQty(value: number): string {
    217   return value > 0 ? `+${value}` : String(value)
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:907`
  ```
      895       if (attachRes.ok) {
    896         setScanBarcode('')
    897         await loadDocDetail()
    898         await loadPackagingTask()
    899         await onRefreshFfSupplyExtras()
    900         return 'ok'
    901       }
    902       const attachText = await attachRes.text()
    903       let attachDetail: string | null = null
    904       try {
    905         const attachBody = JSON.parse(attachText) as { detail?: unknown }
    906         attachDetail =
>   907           typeof attachBody.detail === 'string' ? attachBody.detail : null
    908       } catch {
    909         attachDetail = null
    910       }
    911       if (attachDetail === 'plan_limit_exceeded' && !allowOverPlan) {
    912         return 'over_plan'
    913       }
    914       setModalError(
    915         attachDetail ?? attachText.slice(0, 200) ?? 'Не удалось добавить короб.',
    916       )
    917       return 'error'
    918     } catch (e) {
    919       setModalError(
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1126`
  ```
     1114         return
   1115       }
   1116       await loadDocDetail()
   1117       await onRefreshFfSupplyExtras()
   1118     } catch (e) {
   1119       setModalError(e instanceof Error ? e.message : 'Не удалось убрать товар из короба.')
   1120     } finally {
   1121       setModalBusy(false)
   1122     }
   1123   }
   1124 
   1125   const canUseMpBoxDestructiveControls =
>  1126     docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1127   const canUseMpBoxOperationalControls =
   1128     docModal === 'marketplace_unload' &&
   1129     (unloadDetail?.status === 'confirmed' || unloadDetail?.status === 'collecting')
   1130 
   1131   useEffect(() => {
   1132     if (!canUseMpBoxDestructiveControls) {
   1133       setBoxMenuAnchor(null)
   1134       setBoxMenuTargetId(null)
   1135     }
   1136     if (!canUseMpBoxOperationalControls) {
   1137       setBoxAddDialogBoxId(null)
   1138       setBoxImportOpen(false)
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1129`
  ```
     1117       await onRefreshFfSupplyExtras()
   1118     } catch (e) {
   1119       setModalError(e instanceof Error ? e.message : 'Не удалось убрать товар из короба.')
   1120     } finally {
   1121       setModalBusy(false)
   1122     }
   1123   }
   1124 
   1125   const canUseMpBoxDestructiveControls =
   1126     docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1127   const canUseMpBoxOperationalControls =
   1128     docModal === 'marketplace_unload' &&
>  1129     (unloadDetail?.status === 'confirmed' || unloadDetail?.status === 'collecting')
   1130 
   1131   useEffect(() => {
   1132     if (!canUseMpBoxDestructiveControls) {
   1133       setBoxMenuAnchor(null)
   1134       setBoxMenuTargetId(null)
   1135     }
   1136     if (!canUseMpBoxOperationalControls) {
   1137       setBoxAddDialogBoxId(null)
   1138       setBoxImportOpen(false)
   1139       setPendingAttachBarcode(null)
   1140       setMpAttachConfirmOpen(false)
   1141       setMpAttachOverPlanOpen(false)
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1714`
  ```
     1702 
   1703   const docTitle =
   1704     docModal === 'marketplace_unload'
   1705       ? 'Отгрузка на маркетплейс'
   1706       : docModal === 'discrepancy_act'
   1707         ? 'Акт расхождения'
   1708         : ''
   1709   const unloadDisplayNumber = useMemo(
   1710     () => formatHumanDocumentNumber(unloadDetail),
   1711     [unloadDetail],
   1712   )
   1713 
>  1714   const mpDraft = docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1715   const mpSubmitted =
   1716     docModal === 'marketplace_unload' && unloadDetail?.status === 'submitted'
   1717   const mpConfirmed = docModal === 'marketplace_unload' && unloadDetail?.status === 'confirmed'
   1718   const mpCollecting =
   1719     docModal === 'marketplace_unload' && unloadDetail?.status === 'collecting'
   1720   const mpExecutionPhase = mpConfirmed || mpCollecting
   1721   const mpCancellable = mpSubmitted || mpConfirmed || mpCollecting
   1722   const mpPackagingComplete = useMemo(() => {
   1723     const task = unloadDetail?.linked_packaging_task
   1724     if (!task) {
   1725       return false
   1726     }
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1716`
  ```
     1704     docModal === 'marketplace_unload'
   1705       ? 'Отгрузка на маркетплейс'
   1706       : docModal === 'discrepancy_act'
   1707         ? 'Акт расхождения'
   1708         : ''
   1709   const unloadDisplayNumber = useMemo(
   1710     () => formatHumanDocumentNumber(unloadDetail),
   1711     [unloadDetail],
   1712   )
   1713 
   1714   const mpDraft = docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1715   const mpSubmitted =
>  1716     docModal === 'marketplace_unload' && unloadDetail?.status === 'submitted'
   1717   const mpConfirmed = docModal === 'marketplace_unload' && unloadDetail?.status === 'confirmed'
   1718   const mpCollecting =
   1719     docModal === 'marketplace_unload' && unloadDetail?.status === 'collecting'
   1720   const mpExecutionPhase = mpConfirmed || mpCollecting
   1721   const mpCancellable = mpSubmitted || mpConfirmed || mpCollecting
   1722   const mpPackagingComplete = useMemo(() => {
   1723     const task = unloadDetail?.linked_packaging_task
   1724     if (!task) {
   1725       return false
   1726     }
   1727     return task.status === 'done' || task.is_complete
   1728   }, [unloadDetail?.linked_packaging_task])
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1717`
  ```
     1705       ? 'Отгрузка на маркетплейс'
   1706       : docModal === 'discrepancy_act'
   1707         ? 'Акт расхождения'
   1708         : ''
   1709   const unloadDisplayNumber = useMemo(
   1710     () => formatHumanDocumentNumber(unloadDetail),
   1711     [unloadDetail],
   1712   )
   1713 
   1714   const mpDraft = docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1715   const mpSubmitted =
   1716     docModal === 'marketplace_unload' && unloadDetail?.status === 'submitted'
>  1717   const mpConfirmed = docModal === 'marketplace_unload' && unloadDetail?.status === 'confirmed'
   1718   const mpCollecting =
   1719     docModal === 'marketplace_unload' && unloadDetail?.status === 'collecting'
   1720   const mpExecutionPhase = mpConfirmed || mpCollecting
   1721   const mpCancellable = mpSubmitted || mpConfirmed || mpCollecting
   1722   const mpPackagingComplete = useMemo(() => {
   1723     const task = unloadDetail?.linked_packaging_task
   1724     if (!task) {
   1725       return false
   1726     }
   1727     return task.status === 'done' || task.is_complete
   1728   }, [unloadDetail?.linked_packaging_task])
   1729 
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1719`
  ```
     1707         ? 'Акт расхождения'
   1708         : ''
   1709   const unloadDisplayNumber = useMemo(
   1710     () => formatHumanDocumentNumber(unloadDetail),
   1711     [unloadDetail],
   1712   )
   1713 
   1714   const mpDraft = docModal === 'marketplace_unload' && unloadDetail?.status === 'draft'
   1715   const mpSubmitted =
   1716     docModal === 'marketplace_unload' && unloadDetail?.status === 'submitted'
   1717   const mpConfirmed = docModal === 'marketplace_unload' && unloadDetail?.status === 'confirmed'
   1718   const mpCollecting =
>  1719     docModal === 'marketplace_unload' && unloadDetail?.status === 'collecting'
   1720   const mpExecutionPhase = mpConfirmed || mpCollecting
   1721   const mpCancellable = mpSubmitted || mpConfirmed || mpCollecting
   1722   const mpPackagingComplete = useMemo(() => {
   1723     const task = unloadDetail?.linked_packaging_task
   1724     if (!task) {
   1725       return false
   1726     }
   1727     return task.status === 'done' || task.is_complete
   1728   }, [unloadDetail?.linked_packaging_task])
   1729 
   1730   const loadPackagingTask = useCallback(async () => {
   1731     if (!token || !authHeaders || !docModalId || docModal !== 'marketplace_unload') {
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1727`
  ```
     1715   const mpSubmitted =
   1716     docModal === 'marketplace_unload' && unloadDetail?.status === 'submitted'
   1717   const mpConfirmed = docModal === 'marketplace_unload' && unloadDetail?.status === 'confirmed'
   1718   const mpCollecting =
   1719     docModal === 'marketplace_unload' && unloadDetail?.status === 'collecting'
   1720   const mpExecutionPhase = mpConfirmed || mpCollecting
   1721   const mpCancellable = mpSubmitted || mpConfirmed || mpCollecting
   1722   const mpPackagingComplete = useMemo(() => {
   1723     const task = unloadDetail?.linked_packaging_task
   1724     if (!task) {
   1725       return false
   1726     }
>  1727     return task.status === 'done' || task.is_complete
   1728   }, [unloadDetail?.linked_packaging_task])
   1729 
   1730   const loadPackagingTask = useCallback(async () => {
   1731     if (!token || !authHeaders || !docModalId || docModal !== 'marketplace_unload') {
   1732       setPackagingTask(null)
   1733       setPackagingTaskError(null)
   1734       return
   1735     }
   1736     if (!unloadDetail?.linked_packaging_task) {
   1737       setPackagingTask(null)
   1738       setPackagingTaskError(null)
   1739       return
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1775`
  ```
     1763     if (!unloadDetail || docModal !== 'marketplace_unload' || !docModalId) {
   1764       return
   1765     }
   1766     if (mpTabInitForRef.current === docModalId) {
   1767       return
   1768     }
   1769     mpTabInitForRef.current = docModalId
   1770     setMpUnloadTab('plan')
   1771   }, [unloadDetail, docModalId, docModal])
   1772 
   1773   const mpDateEditable =
   1774     docModal === 'marketplace_unload' &&
>  1775     (unloadDetail?.status === 'draft' ||
   1776       unloadDetail?.status === 'submitted' ||
   1777       unloadDetail?.status === 'confirmed')
   1778   const mpCollectSummary = useMemo(() => {
   1779     if (!unloadDetail || docModal !== 'marketplace_unload') {
   1780       return null
   1781     }
   1782     const planned = unloadDetail.lines.reduce((sum, ln) => sum + ln.quantity, 0)
   1783     // MPU-03: ln.picked_qty приходит с бэкенда посчитанным по содержимому коробов
   1784     // (backend/app/api/marketplace_unload_requests.py, _picked_by_product), а подбор
   1785     // с 2026-08-16 короба не трогает вообще — поэтому это поле всегда 0 и счётчик
   1786     // «Подобрано» не сходился с реальным подбором. Настоящий источник — аллокации
   1787     // подбора (unloadDetail.pick_allocations), как и написано в комментарии ниже
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1776`
  ```
     1764       return
   1765     }
   1766     if (mpTabInitForRef.current === docModalId) {
   1767       return
   1768     }
   1769     mpTabInitForRef.current = docModalId
   1770     setMpUnloadTab('plan')
   1771   }, [unloadDetail, docModalId, docModal])
   1772 
   1773   const mpDateEditable =
   1774     docModal === 'marketplace_unload' &&
   1775     (unloadDetail?.status === 'draft' ||
>  1776       unloadDetail?.status === 'submitted' ||
   1777       unloadDetail?.status === 'confirmed')
   1778   const mpCollectSummary = useMemo(() => {
   1779     if (!unloadDetail || docModal !== 'marketplace_unload') {
   1780       return null
   1781     }
   1782     const planned = unloadDetail.lines.reduce((sum, ln) => sum + ln.quantity, 0)
   1783     // MPU-03: ln.picked_qty приходит с бэкенда посчитанным по содержимому коробов
   1784     // (backend/app/api/marketplace_unload_requests.py, _picked_by_product), а подбор
   1785     // с 2026-08-16 короба не трогает вообще — поэтому это поле всегда 0 и счётчик
   1786     // «Подобрано» не сходился с реальным подбором. Настоящий источник — аллокации
   1787     // подбора (unloadDetail.pick_allocations), как и написано в комментарии ниже
   1788     // про «Раньше подпись была...».
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1777`
  ```
     1765     }
   1766     if (mpTabInitForRef.current === docModalId) {
   1767       return
   1768     }
   1769     mpTabInitForRef.current = docModalId
   1770     setMpUnloadTab('plan')
   1771   }, [unloadDetail, docModalId, docModal])
   1772 
   1773   const mpDateEditable =
   1774     docModal === 'marketplace_unload' &&
   1775     (unloadDetail?.status === 'draft' ||
   1776       unloadDetail?.status === 'submitted' ||
>  1777       unloadDetail?.status === 'confirmed')
   1778   const mpCollectSummary = useMemo(() => {
   1779     if (!unloadDetail || docModal !== 'marketplace_unload') {
   1780       return null
   1781     }
   1782     const planned = unloadDetail.lines.reduce((sum, ln) => sum + ln.quantity, 0)
   1783     // MPU-03: ln.picked_qty приходит с бэкенда посчитанным по содержимому коробов
   1784     // (backend/app/api/marketplace_unload_requests.py, _picked_by_product), а подбор
   1785     // с 2026-08-16 короба не трогает вообще — поэтому это поле всегда 0 и счётчик
   1786     // «Подобрано» не сходился с реальным подбором. Настоящий источник — аллокации
   1787     // подбора (unloadDetail.pick_allocations), как и написано в комментарии ниже
   1788     // про «Раньше подпись была...».
   1789     const distributed = unloadDetail.pick_allocations.reduce(
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:1890`
  ```
     1878   useEffect(() => {
   1879     if (!unloadDetail || docModal !== 'marketplace_unload') {
   1880       return
   1881     }
   1882     if (!mpStepEnabled(mpUnloadTab)) {
   1883       setMpUnloadTab('plan')
   1884     }
   1885   }, [docModal, mpStepEnabled, mpUnloadTab, unloadDetail])
   1886 
   1887   const draftDoc =
   1888     mpDraft ||
   1889     mpSubmitted ||
>  1890     (docModal === 'discrepancy_act' && divergeDetail?.status === 'draft')
   1891   const canDeleteMpUnloadLine = mpDraft
   1892   const mpLineDraft = mpDraft || mpSubmitted
   1893 
   1894   const mpLineProductIds = useMemo(
   1895     () => new Set(unloadDetail?.lines.map((ln) => ln.product_id) ?? []),
   1896     [unloadDetail?.lines],
   1897   )
   1898 
   1899   const mpStockByProductId = useMemo(() => {
   1900     const m = new Map<string, number>()
   1901     for (const row of warehouseAvailableProductPicklist) {
   1902       m.set(row.id, row.available)
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2194`
  ```
     2182               <MenuItem value="planned_desc">Плановая дата ↓</MenuItem>
   2183               <MenuItem value="planned_asc">Плановая дата ↑</MenuItem>
   2184               <MenuItem value="created_desc">Дата создания ↓</MenuItem>
   2185               <MenuItem value="created_asc">Дата создания ↑</MenuItem>
   2186             </Select>
   2187           </FormControl>
   2188         </Stack>
   2189       </Paper>
   2190 
   2191       <Table size="small">
   2192         <TableHead>
   2193           <TableRow>
>  2194             {!isMpShipmentsPage ? <TableCell>Тип</TableCell> : null}
   2195             <TableCell>Номер</TableCell>
   2196             <TableCell>Плановая дата</TableCell>
   2197             <TableCell>Создано</TableCell>
   2198             <TableCell>Статус</TableCell>
   2199             <TableCell>Селлер</TableCell>
   2200             <TableCell>Доп.</TableCell>
   2201             <TableCell align="right">Строк</TableCell>
   2202           </TableRow>
   2203         </TableHead>
   2204         <TableBody>
   2205           {rows.length === 0 ? (
   2206             <TableRow>
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2250`
  ```
     2238                     setUnloadDetail(null)
   2239                     setDivergeDetail(null)
   2240                     setModalError(null)
   2241                     setSelectedInboundLineId('')
   2242                     setLineProductId('')
   2243                     setDocModal('discrepancy_act')
   2244                     setDocModalId(row.id)
   2245                   }
   2246                 }}
   2247                 data-testid="ff-docs-row"
   2248                 data-doc-kind={row.kind}
   2249               >
>  2250                 {!isMpShipmentsPage ? <TableCell>{kindRu(row.kind)}</TableCell> : null}
   2251                 <TableCell>{row.documentNumber ?? '—'}</TableCell>
   2252                 <TableCell>{row.plannedDate ?? '—'}</TableCell>
   2253                 <TableCell>
   2254                   {row.createdAt ? formatDateTimeLocal(row.createdAt) : '—'}
   2255                 </TableCell>
   2256                 <TableCell>
   2257                   {docStatusRu(row.kind, row.status)}
   2258                   {row.ffModified ? (
   2259                     <Chip
   2260                       size="small"
   2261                       label="Изменено ФФ"
   2262                       color="warning"
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2498`
  ```
     2486                   // ✓ — фактически закончен; … — доступен, но работа ещё идёт;
   2487                   // без значка и серым (disabled) — пока недоступен.
   2488                   const done = mpStepDone(step.value)
   2489                   const enabled = mpStepEnabled(step.value)
   2490                   const label = done ? `${step.label} ✓` : enabled ? `${step.label} …` : step.label
   2491                   const tab = (
   2492                     <Tab
   2493                       key={step.value}
   2494                       // Пункт 1 итерации 2026-08-14: назад можно, вперёд нельзя — как в FBS
   2495                       // (frontend/src/screens/v2/FfFbsSupplyWorkspace.tsx, STAGES/currentStageIndex).
   2496                       label={label}
   2497                       value={step.value}
>  2498                       disabled={!enabled}
   2499                       data-testid={step.testId}
   2500                     />
   2501                   )
   2502                   if (enabled) return tab
   2503                   // Disabled-таб MUI без обёртки <span> не показывает Tooltip.
   2504                   return (
   2505                     <Tooltip key={step.value} title={mpStepBlockedReason(step.value)}>
   2506                       <span>{tab}</span>
   2507                     </Tooltip>
   2508                   )
   2509                 })}
   2510               </Tabs>
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2593`
  ```
     2581                   <Table
   2582                     size="small"
   2583                     data-testid="ff-supplies-doc-lines"
   2584                     sx={{ tableLayout: 'fixed', width: '100%' }}
   2585                   >
   2586                     <TableHead>
   2587                       <TableRow>
   2588                         <FfProductTableHeadCells showPrint={false} />
   2589                         <TableCell align="right">План</TableCell>
   2590                         {mpExecutionPhase ? (
   2591                           <TableCell align="right">Осталось</TableCell>
   2592                         ) : null}
>  2593                         {canDeleteMpUnloadLine ? <TableCell align="right" width={56} /> : null}
   2594                       </TableRow>
   2595                     </TableHead>
   2596                     <TableBody>
   2597                       {(() => {
   2598                         const lines = unloadDetail.lines
   2599                         const productCols = 6
   2600                         const mpCols = mpExecutionPhase ? 1 : 0
   2601                         const emptySpan = productCols + 1 + mpCols + (canDeleteMpUnloadLine ? 1 : 0)
   2602                         if (lines.length === 0) {
   2603                           return (
   2604                             <TableRow>
   2605                               <TableCell colSpan={emptySpan}>
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2617`
  ```
     2605                               <TableCell colSpan={emptySpan}>
   2606                                 <Typography variant="body2" color="text.secondary">
   2607                                   Пока нет строк
   2608                                 </Typography>
   2609                               </TableCell>
   2610                             </TableRow>
   2611                           )
   2612                         }
   2613                         return lines.map((ln) => {
   2614                           const picked = ln.picked_qty ?? 0
   2615                           const remaining = ln.quantity - picked
   2616                           const lineShowsDiscrepancy =
>  2617                             unloadDetail.status === 'shipped' && Boolean(ln.has_discrepancy)
   2618                           const displayMeta = productDisplayMetaFromCatalog(
   2619                             ln.product_id,
   2620                             ln,
   2621                             catalogById,
   2622                           )
   2623                           return (
   2624                             <TableRow
   2625                               key={ln.id}
   2626                               sx={
   2627                                 lineShowsDiscrepancy
   2628                                   ? { '& .MuiTableCell-root': { color: 'error.main' } }
   2629                                   : undefined
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2889`
  ```
     2877                       </Stack>
   2878                     </AccordionDetails>
   2879                   </Accordion>
   2880                     {canUseMpBoxDestructiveControls ? (
   2881                       <Menu
   2882                         anchorEl={boxMenuAnchor}
   2883                         open={Boolean(boxMenuAnchor)}
   2884                         onClose={closeBoxMenu}
   2885                         data-testid="ff-mp-box-menu"
   2886                       >
   2887                         <MenuItem
   2888                           data-testid="ff-mp-box-menu-print"
>  2889                           disabled={!boxMenuTargetId || !boxById.get(boxMenuTargetId ?? '')?.internal_barcode}
   2890                           onClick={() => {
   2891                             const box = boxMenuTargetId ? boxById.get(boxMenuTargetId) : null
   2892                             if (box) {
   2893                               requestPrintBoxBarcode(box)
   2894                             }
   2895                             closeBoxMenu()
   2896                           }}
   2897                         >
   2898                           Печать ШК
   2899                         </MenuItem>
   2900                         <MenuItem
   2901                           data-testid="ff-mp-box-menu-copy"
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2891`
  ```
     2879                   </Accordion>
   2880                     {canUseMpBoxDestructiveControls ? (
   2881                       <Menu
   2882                         anchorEl={boxMenuAnchor}
   2883                         open={Boolean(boxMenuAnchor)}
   2884                         onClose={closeBoxMenu}
   2885                         data-testid="ff-mp-box-menu"
   2886                       >
   2887                         <MenuItem
   2888                           data-testid="ff-mp-box-menu-print"
   2889                           disabled={!boxMenuTargetId || !boxById.get(boxMenuTargetId ?? '')?.internal_barcode}
   2890                           onClick={() => {
>  2891                             const box = boxMenuTargetId ? boxById.get(boxMenuTargetId) : null
   2892                             if (box) {
   2893                               requestPrintBoxBarcode(box)
   2894                             }
   2895                             closeBoxMenu()
   2896                           }}
   2897                         >
   2898                           Печать ШК
   2899                         </MenuItem>
   2900                         <MenuItem
   2901                           data-testid="ff-mp-box-menu-copy"
   2902                           disabled={!boxMenuTargetId || modalBusy}
   2903                           onClick={() => {
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:2952`
  ```
     2940               {divergeDetail.inbound_intake_request_id
   2941                 ? ` · приёмка ${divergeDetail.inbound_intake_request_id.slice(0, 8)}…`
   2942                 : ''}
   2943             </Typography>
   2944           ) : null}
   2945           {docModal !== 'marketplace_unload' ? (
   2946           <Table size="small" data-testid="ff-supplies-doc-lines" sx={{ tableLayout: 'fixed', width: '100%' }}>
   2947             <TableHead>
   2948               <TableRow>
   2949                 <FfProductTableHeadCells showPrint={false} />
   2950                 <TableCell>Строка приёмки</TableCell>
   2951                 <TableCell align="right">Расхождение</TableCell>
>  2952                 {draftDoc ? <TableCell align="right" width={56} /> : null}
   2953               </TableRow>
   2954             </TableHead>
   2955             <TableBody>
   2956               {(() => {
   2957                 const lines = divergeDetail?.lines ?? []
   2958                 const emptySpan = 6 + 1 + 1 + (draftDoc ? 1 : 0)
   2959                 if (lines.length === 0) {
   2960                   return (
   2961                     <TableRow>
   2962                       <TableCell colSpan={emptySpan}>
   2963                         <Typography variant="body2" color="text.secondary">
   2964                           Пока нет строк
  ```
- `запрет-кнопки` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:3048`
  ```
     3036                     ))}
   3037                   </Select>
   3038                 </FormControl>
   3039               ) : null}
   3040               <FormControl fullWidth size="small">
   3041                 <InputLabel id="ff-doc-line-product">Товар</InputLabel>
   3042                 <Select
   3043                   labelId="ff-doc-line-product"
   3044                   label="Товар"
   3045                   value={lineProductId}
   3046                   onChange={(e) => setLineProductId(String(e.target.value))}
   3047                   data-testid="ff-supplies-line-product"
>  3048                   disabled={Boolean(selectedInboundLineId)}
   3049                 >
   3050                   <MenuItem value="" disabled>
   3051                     Выберите SKU
   3052                   </MenuItem>
   3053                   {docProductPicklist.map((p) => (
   3054                     <MenuItem key={p.id} value={p.id}>
   3055                       {p.sku_code} — {p.name}
   3056                       {'available' in p ? ` · доступно для FBO ${p.available}` : ''}
   3057                     </MenuItem>
   3058                   ))}
   3059                 </Select>
   3060               </FormControl>
  ```
- `проверка-статуса` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:3203`
  ```
     3191             ) : null}
   3192             {draftDoc && docModal !== 'marketplace_unload' ? (
   3193               <Button
   3194                 variant="contained"
   3195                 color="secondary"
   3196                 disabled={modalBusy}
   3197                 onClick={() => void submitDoc()}
   3198                 data-testid="ff-supplies-doc-submit"
   3199               >
   3200                 Передать на FF
   3201               </Button>
   3202             ) : null}
>  3203             {docModal === 'discrepancy_act' && divergeDetail?.status === 'confirmed' ? (
   3204               <>
   3205                 <Button
   3206                   variant="contained"
   3207                   color="primary"
   3208                   disabled={modalBusy}
   3209                   onClick={() => void resolveDiscrepancyAct('approve')}
   3210                   data-testid="ff-discrepancy-act-approve"
   3211                 >
   3212                   Утвердить акт
   3213                 </Button>
   3214                 <Button
   3215                   variant="outlined"
  ```
- `скрытие-в-null` · `frontend/src/screens/ff/FfSuppliesShipmentsPage.tsx:3270`
  ```
     3258       docModalId &&
   3259       docModal === 'marketplace_unload' &&
   3260       boxAddDialogBoxId &&
   3261       boxById.get(boxAddDialogBoxId) ? (
   3262         <FfMarketplaceUnloadBoxAddDialog
   3263           open
   3264           onClose={() => setBoxAddDialogBoxId(null)}
   3265           requestId={docModalId}
   3266           boxId={boxAddDialogBoxId}
   3267           boxLabel={
   3268             (() => {
   3269               const idx = mpBoxesOrdered.findIndex((b) => b.id === boxAddDialogBoxId)
>  3270               const ordinal = idx >= 0 ? idx + 1 : null
   3271               const barcode = boxById.get(boxAddDialogBoxId)?.internal_barcode?.trim()
   3272               if (ordinal != null) {
   3273                 return barcode ? `Короб ${ordinal} · ${barcode}` : `Короб ${ordinal}`
   3274               }
   3275               return barcode ?? 'Короб'
   3276             })()
   3277           }
   3278           readOnly={!canUseMpBoxOperationalControls}
   3279           token={token}
   3280           addressStorageEnabled={addressStorageEnabled}
   3281           catalogById={catalogById}
   3282           warehouseStockByProductId={mpStockByProductId}
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28, S-29 · `frontend/src/components/WbProductPickerDialog.tsx:372`
  ```
      360               setPickerSearch(e.target.value)
    361               setPickerError(null)
    362             }}
    363             onKeyDown={(e) => {
    364               if (e.key !== 'Enter' || !catalog) {
    365                 return
    366               }
    367               e.preventDefault()
    368               const input = e.currentTarget.querySelector('input')
    369               const rawSearch = input?.value || (e.target as HTMLInputElement).value || pickerSearch
    370               const productId = resolveProductIdByBarcode(catalog, rawSearch)
    371               const targetId =
>   372                 productId ?? (filteredPickerRows.length === 1 ? filteredPickerRows[0]!.id : null)
    373               if (!targetId) {
    374                 setPickerError(notFoundMessage)
    375                 return
    376               }
    377               if (disabledProductIds.has(targetId)) {
    378                 setPickerError(inDraftMessage)
    379                 return
    380               }
    381               incrementPickerQty(targetId)
    382               setLastScannedProductId(targetId)
    383               setPickerSearch('')
    384               setPickerError(null)
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28, S-29 · `frontend/src/components/WbProductPickerDialog.tsx:504`
  ```
      492                     }}
    493                     data-testid={`${testIdPrefix}-row`}
    494                     data-in-draft={inDraft ? '1' : '0'}
    495                   >
    496                     {variant === 'ff' ? (
    497                       <FfProductLineCells
    498                         meta={productDisplayMetaFromCatalog(
    499                           r.id,
    500                           { sku_code: r.sku_code, name: r.name },
    501                           catalogById,
    502                         )}
    503                         printTestId={`${testIdPrefix}-print-${r.id}`}
>   504                         nameExtra={inDraft ? inDraftCaption(inDraftMessage) : null}
    505                       />
    506                     ) : (
    507                       <>
    508                         <TableCell>
    509                           <ProductPhotoThumb src={r.wb_primary_image_url} />
    510                         </TableCell>
    511                         <TableCell
    512                           sx={{
    513                             whiteSpace: 'normal',
    514                             wordBreak: 'break-word',
    515                             overflow: 'hidden',
    516                             pl: 2,
  ```
- `скрытие-в-null` · ОБЩИЙ с S-28, S-29 · `frontend/src/components/WbProductPickerDialog.tsx:549`
  ```
      537                           sx={{
    538                             pl: 2,
    539                             maxWidth: 440,
    540                             whiteSpace: 'nowrap',
    541                             overflow: 'hidden',
    542                             textOverflow: 'ellipsis',
    543                           }}
    544                           title={r.name}
    545                         >
    546                           <Typography variant="body2" sx={{ lineHeight: 1.25 }} noWrap>
    547                             {r.name}
    548                           </Typography>
>   549                           {inDraft ? inDraftCaption(inDraftMessage) : null}
    550                         </TableCell>
    551                       </>
    552                     )}
    553                     {renderTrailingBodyCells?.(r)}
    554                     {showAvailableColumn ? (
    555                       <TableCell align="right">{available}</TableCell>
    556                     ) : null}
    557                     {qtyCell(r, inDraft)}
    558                   </TableRow>
    559                 )
    560               })}
    561               {filteredPickerRows.length === 0 && emptyMessage ? (
  ```
- `скрытие-в-null` · ОБЩИЙ с S-26, S-28, S-29 · `frontend/src/components/WmsDateField.tsx:24`
  ```
       12   minDate?: string | null
     13   testId?: string
     14   slotProps?: {
     15     textField?: { size?: 'small' | 'medium'; fullWidth?: boolean; sx?: object }
     16   }
     17 }
     18 
     19 function parseIso(value: string | null): Dayjs | null {
     20   if (!value || !/^\d{4}-\d{2}-\d{2}$/.test(value)) {
     21     return null
     22   }
     23   const d = dayjs(value, 'YYYY-MM-DD', true)
>    24   return d.isValid() ? d : null
     25 }
     26 
     27 function toIso(next: Dayjs | null): string | null {
     28   if (next == null || !next.isValid()) {
     29     return null
     30   }
     31   return next.format('YYYY-MM-DD')
     32 }
     33 
     34 export function WmsDateField({
     35   label,
     36   value,
  ```
- `запрет-кнопки` · ОБЩИЙ с S-26, S-28, S-29 · `frontend/src/components/WmsDateField.tsx:64`
  ```
       52     if (iso == null) {
     53       return
     54     }
     55     if (iso !== value) {
     56       onChange(iso)
     57     }
     58   }
     59 
     60   const picker = (
     61     <DatePicker
     62       label={label}
     63       value={draft}
>    64       disabled={disabled}
     65       minDate={minDate ? parseIso(minDate) ?? undefined : undefined}
     66       onChange={(next) => {
     67         // Локальный черновик при наборе секций; не шлём null в родителя (MUI даёт null при закрытии).
     68         setDraft(next)
     69       }}
     70       onAccept={(next) => {
     71         setDraft(next)
     72         commitDraft(next)
     73       }}
     74       slotProps={{
     75         textField: {
     76           size: slotProps?.textField?.size ?? 'small',
  ```
- `проверка-статуса` · ОБЩИЙ с S-02 · `frontend/src/screens/ff/FfDashboard.tsx:180`
  ```
      168 
    169   const shipmentRows = useMemo<ShipmentCalendarRow[]>(() => {
    170     const fbs = fbsRows.map((row) => ({
    171       id: row.id,
    172       date: row.date,
    173       direction: row.direction,
    174       boxesCount: row.boxes_count,
    175       shipmentType: 'FBS' as const,
    176       title: row.title,
    177       source: 'fbs' as const,
    178     }))
    179     const mp = mpUnloadSummaries
>   180       .filter((row) => row.status === 'submitted' && row.planned_shipment_date)
    181       .map((row) => ({
    182         id: row.id,
    183         date: row.planned_shipment_date!,
    184         direction: row.warehouse_name ?? 'Направление не указано',
    185         boxesCount: row.goods_qty_total ?? row.line_count,
    186         shipmentType: 'MP' as const,
    187         title: row.marketplace_label ?? 'Маркетплейс',
    188         source: 'mp' as const,
    189       }))
    190     const fbo = outboundSummaries
    191       .filter((row) => row.status === 'submitted' && row.planned_shipment_date)
    192       .map((row) => ({
  ```
- `проверка-статуса` · ОБЩИЙ с S-02 · `frontend/src/screens/ff/FfDashboard.tsx:191`
  ```
      179     const mp = mpUnloadSummaries
    180       .filter((row) => row.status === 'submitted' && row.planned_shipment_date)
    181       .map((row) => ({
    182         id: row.id,
    183         date: row.planned_shipment_date!,
    184         direction: row.warehouse_name ?? 'Направление не указано',
    185         boxesCount: row.goods_qty_total ?? row.line_count,
    186         shipmentType: 'MP' as const,
    187         title: row.marketplace_label ?? 'Маркетплейс',
    188         source: 'mp' as const,
    189       }))
    190     const fbo = outboundSummaries
>   191       .filter((row) => row.status === 'submitted' && row.planned_shipment_date)
    192       .map((row) => ({
    193         id: row.id,
    194         date: row.planned_shipment_date!,
    195         direction: row.warehouse_name ?? 'Направление не указано',
    196         boxesCount: row.goods_qty_total ?? row.line_count,
    197         shipmentType: 'FBO' as const,
    198         title: row.marketplace_label ?? 'FBO',
    199         source: 'fbo' as const,
    200       }))
    201     return [...fbs, ...mp, ...fbo].sort((a, b) => {
    202       if (a.date !== b.date) return a.date.localeCompare(b.date)
    203       return `${a.shipmentType}-${a.direction}`.localeCompare(`${b.shipmentType}-${b.direction}`)
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:174`
  ```
      162   /** Separate printing stage: reuses the standard ЧЗ/ШК dialog without packing actions. */
    163   printOnly?: boolean
    164   /** Embedded packing stage can keep physical packing free of print controls. */
    165   hidePrintActions?: boolean
    166   /** Единое поле скана также принимает штрихкод готового короба (WHB-/INB-);
    167       состоянием коробов владеет родитель, поэтому скан короба делегируется наверх. */
    168   onBoxBarcodeScan?: (barcode: string) => void
    169   onClose?: () => void
    170   onUpdated: (task: PackagingTask) => void
    171 }
    172 
    173 function statusLabel(status: string): string {
>   174   if (status === 'draft') return 'Черновик'
    175   if (status === 'in_progress') return 'В работе'
    176   if (status === 'done') return 'Выполнено'
    177   if (status === 'cancelled') return 'Отменено'
    178   return status
    179 }
    180 
    181 function locationLabel(code?: string | null): string {
    182   if (!code) return '—'
    183   return code === '__SORTING__' ? 'Сортировка' : code
    184 }
    185 
    186 function taskTotals(task: PackagingTask): { total: number; done: number; remaining: number } {
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:175`
  ```
      163   printOnly?: boolean
    164   /** Embedded packing stage can keep physical packing free of print controls. */
    165   hidePrintActions?: boolean
    166   /** Единое поле скана также принимает штрихкод готового короба (WHB-/INB-);
    167       состоянием коробов владеет родитель, поэтому скан короба делегируется наверх. */
    168   onBoxBarcodeScan?: (barcode: string) => void
    169   onClose?: () => void
    170   onUpdated: (task: PackagingTask) => void
    171 }
    172 
    173 function statusLabel(status: string): string {
    174   if (status === 'draft') return 'Черновик'
>   175   if (status === 'in_progress') return 'В работе'
    176   if (status === 'done') return 'Выполнено'
    177   if (status === 'cancelled') return 'Отменено'
    178   return status
    179 }
    180 
    181 function locationLabel(code?: string | null): string {
    182   if (!code) return '—'
    183   return code === '__SORTING__' ? 'Сортировка' : code
    184 }
    185 
    186 function taskTotals(task: PackagingTask): { total: number; done: number; remaining: number } {
    187   const total = task.lines.reduce((sum, line) => sum + line.qty_total, 0)
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:176`
  ```
      164   /** Embedded packing stage can keep physical packing free of print controls. */
    165   hidePrintActions?: boolean
    166   /** Единое поле скана также принимает штрихкод готового короба (WHB-/INB-);
    167       состоянием коробов владеет родитель, поэтому скан короба делегируется наверх. */
    168   onBoxBarcodeScan?: (barcode: string) => void
    169   onClose?: () => void
    170   onUpdated: (task: PackagingTask) => void
    171 }
    172 
    173 function statusLabel(status: string): string {
    174   if (status === 'draft') return 'Черновик'
    175   if (status === 'in_progress') return 'В работе'
>   176   if (status === 'done') return 'Выполнено'
    177   if (status === 'cancelled') return 'Отменено'
    178   return status
    179 }
    180 
    181 function locationLabel(code?: string | null): string {
    182   if (!code) return '—'
    183   return code === '__SORTING__' ? 'Сортировка' : code
    184 }
    185 
    186 function taskTotals(task: PackagingTask): { total: number; done: number; remaining: number } {
    187   const total = task.lines.reduce((sum, line) => sum + line.qty_total, 0)
    188   const done = task.lines.reduce((sum, line) => sum + line.qty_done, 0)
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:177`
  ```
      165   hidePrintActions?: boolean
    166   /** Единое поле скана также принимает штрихкод готового короба (WHB-/INB-);
    167       состоянием коробов владеет родитель, поэтому скан короба делегируется наверх. */
    168   onBoxBarcodeScan?: (barcode: string) => void
    169   onClose?: () => void
    170   onUpdated: (task: PackagingTask) => void
    171 }
    172 
    173 function statusLabel(status: string): string {
    174   if (status === 'draft') return 'Черновик'
    175   if (status === 'in_progress') return 'В работе'
    176   if (status === 'done') return 'Выполнено'
>   177   if (status === 'cancelled') return 'Отменено'
    178   return status
    179 }
    180 
    181 function locationLabel(code?: string | null): string {
    182   if (!code) return '—'
    183   return code === '__SORTING__' ? 'Сортировка' : code
    184 }
    185 
    186 function taskTotals(task: PackagingTask): { total: number; done: number; remaining: number } {
    187   const total = task.lines.reduce((sum, line) => sum + line.qty_total, 0)
    188   const done = task.lines.reduce((sum, line) => sum + line.qty_done, 0)
    189   return { total, done, remaining: Math.max(0, total - done) }
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:336`
  ```
      324   const productLabelForLine = (ln: PackagingTaskLine) =>
    325     displayMetaToProductLabel(productDisplayMetaFromCatalog(ln.product_id, ln, catalogById))
    326 
    327   const lineBarcodeForScan = (ln: PackagingTaskLine) => {
    328     const displayMeta = productDisplayMetaFromCatalog(ln.product_id, ln, catalogById)
    329     return resolveProductPrimaryBarcode(displayMeta) || ln.sku_code
    330   }
    331 
    332   const lineRemaining = (ln: PackagingTaskLine) =>
    333     Math.max(0, ln.qty_need_pack - ln.qty_packed_in_task)
    334 
    335   const panelTotals = taskTotals(task)
>   336   const taskEditable = task.status !== 'done' && task.status !== 'cancelled'
    337   const isMpUnloadTask = Boolean(task.marketplace_unload_request_id)
    338   const pickResyncWarningText = isMpUnloadTask
    339     ? 'Состав коробов изменился. Количества в задании пересчитаны; уже упакованное в задании сохранено — проверьте строки.'
    340     : 'Подбор по ячейкам изменился. Количества в задании пересчитаны; уже упакованное в задании сохранено — проверьте строки.'
    341   const orderedEvents = (task.events ?? []).slice().sort(comparePackagingEventsAsc)
    342   const reversibleEvents = orderedEvents.filter(
    343     (event) =>
    344       (event.action === 'scan_pack' || event.action === 'manual_pack') &&
    345       event.reversed_at === null,
    346   )
    347   const lastReversibleEvent = reversibleEvents[reversibleEvents.length - 1] ?? null
    348   const canUndo = taskEditable && reversibleEvents.length > 0 && !printOnly && !isMpUnloadTask
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:413`
  ```
      401       { reprint: opts?.reprint },
    402     )
    403   }
    404 
    405   /** MPU-05 (18.08): единая кнопка печати листа отгрузки (накладная + ТЗ в одной форме
    406       с колонками «Кол-во» (план) и «Факт» (пусто, под руку) — печатная форма уже
    407       готова в printShipmentPackagingSheet, здесь только подключение и передача данных. */
    408   const printPackagingSheet = () => {
    409     printShipmentPackagingSheet({
    410       documentNumber: formatHumanDocumentNumber(task) ?? task.document_number ?? task.id,
    411       documentType: 'Отгрузка на маркетплейс',
    412       sellerName: taskSellerLabel(task),
>   413       shipmentDate: task.created_at ? new Date(task.created_at).toLocaleString('ru-RU') : null,
    414       warehouseName: task.warehouse_name ?? task.warehouse_code ?? 'Склад',
    415       createdAt: task.created_at ?? null,
    416       items: task.lines.map((ln) => {
    417         const displayMeta = productDisplayMetaFromCatalog(ln.product_id, ln, catalogById)
    418         return {
    419           product_name: displayMeta.product_name,
    420           vendor_code: displayMeta.wb_vendor_code ?? '',
    421           sku_code: displayMeta.sku_code,
    422           barcode: resolveProductPrimaryBarcode(displayMeta) || null,
    423           wb_nm_id: displayMeta.wb_nm_id,
    424           photo_url: displayMeta.wb_primary_image_url,
    425           instructions: ln.packaging_instructions,
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:734`
  ```
      722       if (!res.ok) {
    723         setError(await readPackagingApiErrorMessage(res))
    724         return
    725       }
    726       onUpdated((await res.json()) as PackagingTask)
    727     } finally {
    728       setBusy(false)
    729     }
    730   }
    731 
    732   const manualTask =
    733     !task.marketplace_unload_request_id &&
>   734     task.status !== 'done' &&
    735     task.status !== 'cancelled'
    736 
    737   const lineHasOverflowActions = (ln: PackagingTaskLine) =>
    738     ln.requires_honest_sign && ln.qty_marking_printed > 0
    739 
    740   const openLineMenu = (event: MouseEvent<HTMLElement>, ln: PackagingTaskLine) => {
    741     event.stopPropagation()
    742     setLineMenuAnchor(event.currentTarget)
    743     setLineMenuLine(ln)
    744   }
    745 
    746   const closeLineMenu = () => {
  ```
- `проверка-статуса` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:735`
  ```
      723         setError(await readPackagingApiErrorMessage(res))
    724         return
    725       }
    726       onUpdated((await res.json()) as PackagingTask)
    727     } finally {
    728       setBusy(false)
    729     }
    730   }
    731 
    732   const manualTask =
    733     !task.marketplace_unload_request_id &&
    734     task.status !== 'done' &&
>   735     task.status !== 'cancelled'
    736 
    737   const lineHasOverflowActions = (ln: PackagingTaskLine) =>
    738     ln.requires_honest_sign && ln.qty_marking_printed > 0
    739 
    740   const openLineMenu = (event: MouseEvent<HTMLElement>, ln: PackagingTaskLine) => {
    741     event.stopPropagation()
    742     setLineMenuAnchor(event.currentTarget)
    743     setLineMenuLine(ln)
    744   }
    745 
    746   const closeLineMenu = () => {
    747     setLineMenuAnchor(null)
  ```
- `запрет-кнопки` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:1053`
  ```
     1041                       {/* Заказчик 18.08: кнопка растягивалась fullWidth на всю колонку и
   1042                           выглядела баннером. Крупная (size large), но по размеру
   1043                           содержимого и прижата к правому краю колонки «Действия». */}
   1044                       <Stack spacing={0.5} sx={{ alignItems: 'flex-end' }}>
   1045                         {!hidePrintActions ? (
   1046                           <Button
   1047                             variant="contained"
   1048                             size="large"
   1049                             startIcon={<PrintOutlined />}
   1050                             aria-label={`Печать товара ${displayMeta.product_name}`}
   1051                             onClick={() => openLinePrint(ln)}
   1052                             data-testid={`ff-packaging-line-print-${ln.id}`}
>  1053                             disabled={!barcodeReady && !ln.requires_honest_sign}
   1054                             sx={{ whiteSpace: 'nowrap' }}
   1055                           >
   1056                             ШК + ЧЗ
   1057                           </Button>
   1058                         ) : null}
   1059                       </Stack>
   1060                       {lineHasOverflowActions(ln) ? (
   1061                         <IconButton
   1062                           size="small"
   1063                           aria-label="Дополнительные действия"
   1064                           disabled={busy}
   1065                           onClick={(e) => openLineMenu(e, ln)}
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:1694`
  ```
     1682             width: 'min(1120px, calc(100vw - 64px))',
   1683             maxWidth: 'calc(100vw - 64px)',
   1684             maxHeight: '92vh',
   1685             boxSizing: 'border-box',
   1686           },
   1687         },
   1688       }}
   1689       data-testid="ff-packaging-create-dialog"
   1690     >
   1691       <DialogTitle>Создать задание на упаковку</DialogTitle>
   1692       <DialogContent>
   1693         <Stack spacing={2} sx={{ mt: 1 }}>
>  1694           {error ? <Alert severity="error">{error}</Alert> : null}
   1695           <FormControl fullWidth size="small">
   1696             <InputLabel id="ff-packaging-wh-label">Склад</InputLabel>
   1697             <Select
   1698               labelId="ff-packaging-wh-label"
   1699               label="Склад"
   1700               value={warehouseId}
   1701               onChange={(e) => setWarehouseId(String(e.target.value))}
   1702               data-testid="ff-packaging-create-warehouse"
   1703             >
   1704               {warehouses.map((w) => (
   1705                 <MenuItem key={w.id} value={w.id}>
   1706                   {w.name}
  ```
- `запрет-кнопки` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:1842`
  ```
     1830                 </TableBody>
   1831               </Table>
   1832             </TableContainer>
   1833           ) : null}
   1834         </Stack>
   1835       </DialogContent>
   1836       <DialogActions>
   1837         <Button onClick={onClose} disabled={busy}>
   1838           Отмена
   1839         </Button>
   1840         <Button
   1841           variant="contained"
>  1842           disabled={createBlocked}
   1843           onClick={() => void submit()}
   1844           data-testid="ff-packaging-create-submit"
   1845         >
   1846           Создать
   1847         </Button>
   1848       </DialogActions>
   1849     </Dialog>
   1850   )
   1851 }
   1852 
   1853 export function FfPackagingPage({ token }: PageProps) {
   1854   const location = useLocation()
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:1950`
  ```
     1938           >
   1939             Осталось промаркировать
   1940           </Button>
   1941         </Badge>
   1942         <Button
   1943           variant="contained"
   1944           onClick={() => setCreateOpen(true)}
   1945           data-testid="ff-packaging-create-open"
   1946         >
   1947           Создать задание
   1948         </Button>
   1949       </Stack>
>  1950       {error ? <Alert severity="error">{error}</Alert> : null}
   1951       {selected ? (
   1952         <FfPackagingTaskPanel
   1953           token={token}
   1954           task={selected}
   1955           onClose={closeTask}
   1956           onUpdated={(t) => {
   1957             setSelected(t)
   1958             void load()
   1959           }}
   1960         />
   1961       ) : (
   1962         <Paper variant="outlined" data-testid="ff-packaging-queue" sx={{ overflowX: 'hidden' }}>
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-14 · `frontend/src/screens/ff/FfPackagingPage.tsx:2128`
  ```
     2116 
   2117   return (
   2118     <Dialog
   2119       open={open}
   2120       onClose={onClose}
   2121       fullWidth
   2122       maxWidth={false}
   2123       slotProps={{ paper: { sx: { width: 'min(1200px, 96vw)', maxHeight: '92vh' } } }}
   2124       data-testid="ff-packaging-dialog"
   2125     >
   2126       <DialogTitle>Задание на упаковку</DialogTitle>
   2127       <DialogContent>
>  2128         {error ? <Alert severity="error">{error}</Alert> : null}
   2129         {task ? (
   2130           <FfPackagingTaskPanel
   2131             token={token}
   2132             task={task}
   2133             unloadLabel={unloadLabel}
   2134             onUpdated={setTask}
   2135           />
   2136         ) : null}
   2137       </DialogContent>
   2138       <DialogActions>
   2139         <Button onClick={onClose} data-testid="ff-packaging-dialog-close">
   2140           Вернуться к отгрузке
  ```
- `проверка-статуса` · ОБЩИЙ с S-17, S-20 · `frontend/src/utils/inboundQueues.ts:36`
  ```
       24   has_discrepancy?: boolean
     25   created_at?: string
     26   sorting_remaining_qty?: number
     27 }
     28 
     29 export function filterReceptionQueue(rows: InboundQueueRow[]): InboundQueueRow[] {
     30   return rows
     31 }
     32 
     33 export function filterSortingQueue(rows: InboundQueueRow[]): InboundQueueRow[] {
     34   return rows.filter(
     35     (r) =>
>    36       (r.status === 'sorting' || r.status === 'verified') &&
     37       (r.sorting_remaining_qty ?? 0) > 0,
     38   )
     39 }
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-04, S-05, S-07, S-08, S-09, S-10, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
