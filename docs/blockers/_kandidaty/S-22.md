# Кандидаты в блокировки · S-22

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (8)

- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:324`
  ```
      312                 <ul className="list-plain" data-testid="inbound-detail-lines">
    313                   {inboundDetail.lines.map((ln) => (
    314                     <li key={ln.id} data-testid="inbound-detail-line">
    315                       {ln.product_name} ({ln.sku_code}) — план {ln.expected_qty}
    316                       {typeof ln.actual_qty === 'number' ? ` · факт ${ln.actual_qty}` : ''}
    317                       {' · '}
    318                       принято {ln.posted_qty}
    319                       {ln.storage_location_code ? ` · ячейка: ${ln.storage_location_code}` : ''}
    320                     </li>
    321                   ))}
    322                 </ul>
    323 
>   324                 {inboundDetail.status === 'draft' && canEditInboundDraft ? (
    325                   <form data-testid="inbound-line-form" noValidate onSubmit={onAddInboundLine}>
    326                     <label>
    327                       Поиск SKU
    328                       <Input
    329                         value={productQuery}
    330                         onChange={(e) => setProductQuery(e.target.value)}
    331                         placeholder="SKU, штрихкод WB или название…"
    332                         aria-label="Поиск SKU для приёмки"
    333                         data-testid="inbound-line-product-search"
    334                       />
    335                     </label>
    336                     <label>
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:392`
  ```
      380                       </label>
    381                     ) : null}
    382                     <Button
    383                       type="submit"
    384                       data-testid="inbound-line-submit"
    385                       disabled={opsBusy || products.length === 0}
    386                     >
    387                       {opsBusy ? '…' : 'Добавить строку'}
    388                     </Button>
    389                   </form>
    390                 ) : null}
    391 
>   392                 {inboundDetail.status === 'draft' &&
    393                 inboundDetail.lines.length > 0 &&
    394                 (isFulfillmentAdmin || isFulfillmentSeller) ? (
    395                   <Button
    396                     type="button"
    397                     data-testid="inbound-submit-request"
    398                     disabled={opsBusy}
    399                     onClick={onSubmitInboundRequest}
    400                   >
    401                     {opsBusy ? '…' : 'Отправить заявку'}
    402                   </Button>
    403                 ) : null}
    404 
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:405`
  ```
      393                 inboundDetail.lines.length > 0 &&
    394                 (isFulfillmentAdmin || isFulfillmentSeller) ? (
    395                   <Button
    396                     type="button"
    397                     data-testid="inbound-submit-request"
    398                     disabled={opsBusy}
    399                     onClick={onSubmitInboundRequest}
    400                   >
    401                     {opsBusy ? '…' : 'Отправить заявку'}
    402                   </Button>
    403                 ) : null}
    404 
>   405                 {inboundDetail.status === 'submitted' ? (
    406                   <div data-testid="inbound-primary-accept-panel">
    407                     {isFulfillmentAdmin ? (
    408                       <>
    409                         <p className="subtle">Привоз принят без пересчёта.</p>
    410                         <Button
    411                           type="button"
    412                           data-testid="inbound-primary-accept"
    413                           disabled={opsBusy}
    414                           onClick={onPrimaryAcceptInboundRequest}
    415                         >
    416                           {opsBusy ? '…' : 'Принято первично'}
    417                         </Button>
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:427`
  ```
      415                         >
    416                           {opsBusy ? '…' : 'Принято первично'}
    417                         </Button>
    418                       </>
    419                     ) : (
    420                       <p className="subtle" data-testid="inbound-seller-read-only">
    421                         Ожидает первичной приёмки фулфилментом.
    422                       </p>
    423                     )}
    424                   </div>
    425                 ) : null}
    426 
>   427                 {inboundDetail.status === 'primary_accepted' ||
    428                 inboundDetail.status === 'verifying' ||
    429                 inboundDetail.status === 'receiving' ? (
    430                   <div data-testid="inbound-verify-panel">
    431                     {isFulfillmentAdmin ? (
    432                       <>
    433                         {boxIntakeMode ? (
    434                           <div data-testid="inbound-box-intake-panel" style={{ marginBottom: 16 }}>
    435                             <p className="subtle">
    436                               Необязательно: поштучная приёмка через короб (скан INB-…, затем ШК
    437                               товара). Или укажите факт по строкам ниже.
    438                             </p>
    439                             {!activeIntakeBox ? (
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:428`
  ```
      416                           {opsBusy ? '…' : 'Принято первично'}
    417                         </Button>
    418                       </>
    419                     ) : (
    420                       <p className="subtle" data-testid="inbound-seller-read-only">
    421                         Ожидает первичной приёмки фулфилментом.
    422                       </p>
    423                     )}
    424                   </div>
    425                 ) : null}
    426 
    427                 {inboundDetail.status === 'primary_accepted' ||
>   428                 inboundDetail.status === 'verifying' ||
    429                 inboundDetail.status === 'receiving' ? (
    430                   <div data-testid="inbound-verify-panel">
    431                     {isFulfillmentAdmin ? (
    432                       <>
    433                         {boxIntakeMode ? (
    434                           <div data-testid="inbound-box-intake-panel" style={{ marginBottom: 16 }}>
    435                             <p className="subtle">
    436                               Необязательно: поштучная приёмка через короб (скан INB-…, затем ШК
    437                               товара). Или укажите факт по строкам ниже.
    438                             </p>
    439                             {!activeIntakeBox ? (
    440                               <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:429`
  ```
      417                         </Button>
    418                       </>
    419                     ) : (
    420                       <p className="subtle" data-testid="inbound-seller-read-only">
    421                         Ожидает первичной приёмки фулфилментом.
    422                       </p>
    423                     )}
    424                   </div>
    425                 ) : null}
    426 
    427                 {inboundDetail.status === 'primary_accepted' ||
    428                 inboundDetail.status === 'verifying' ||
>   429                 inboundDetail.status === 'receiving' ? (
    430                   <div data-testid="inbound-verify-panel">
    431                     {isFulfillmentAdmin ? (
    432                       <>
    433                         {boxIntakeMode ? (
    434                           <div data-testid="inbound-box-intake-panel" style={{ marginBottom: 16 }}>
    435                             <p className="subtle">
    436                               Необязательно: поштучная приёмка через короб (скан INB-…, затем ШК
    437                               товара). Или укажите факт по строкам ниже.
    438                             </p>
    439                             {!activeIntakeBox ? (
    440                               <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    441                                 <Input
  ```
- `проверка-статуса` · `frontend/src/screens/v2/InboundScreen.tsx:553`
  ```
      541                         >
    542                           {opsBusy ? '…' : 'Завершить пересчёт'}
    543                         </Button>
    544                       </>
    545                     ) : (
    546                       <p className="subtle" data-testid="inbound-seller-read-only">
    547                         Идёт пересчёт на фулфилменте; доступен просмотр.
    548                       </p>
    549                     )}
    550                   </div>
    551                 ) : null}
    552 
>   553                 {inboundDetail.status === 'verified' || inboundDetail.status === 'sorting' ? (
    554                   <div data-testid="inbound-receiving-panel">
    555                     {inboundDetail.has_discrepancy ? (
    556                       <p className="error" data-testid="inbound-discrepancy-flag">
    557                         Есть расхождения (план ≠ факт)
    558                       </p>
    559                     ) : (
    560                       <p className="subtle">Факт подтверждён, можно проводить остатки.</p>
    561                     )}
    562                     {isFulfillmentAdmin ? (
    563                       <>
    564                         {inboundDetail.lines.map((ln) => {
    565                           const target = ln.actual_qty ?? ln.expected_qty
  ```
- `скрытие-в-null` · ОБЩИЙ с S-01, S-21, S-23, S-24, S-25 · `frontend/src/screens/AppV2Screens.tsx:37`
  ```
       25   hint,
     26   children,
     27 }: {
     28   title: string
     29   hint?: string
     30   children?: ReactNode
     31 }) {
     32   return (
     33     <Card className="card">
     34       <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
     35         <div>
     36           <h3 style={{ margin: 0, fontSize: 16 }}>{title}</h3>
>    37           {hint ? <p className="subtle">{hint}</p> : null}
     38         </div>
     39         {children}
     40       </div>
     41     </Card>
     42   )
     43 }
     44 
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
