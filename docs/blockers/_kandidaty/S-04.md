# Кандидаты в блокировки · S-04

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (9)

- `ранний-выход` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:128`
  ```
      116     />
    117   )
    118 }
    119 
    120 function formatDt(value: string | null): string {
    121   if (!value) return '—'
    122   const d = new Date(value)
    123   if (Number.isNaN(d.getTime())) return '—'
    124   return d.toLocaleString('ru-RU')
    125 }
    126 
    127 function stockErrorText(code: string | null): string | null {
>   128   if (!code) return null
    129   const labels: Record<string, string> = {
    130     wb_unavailable: 'Wildberries временно недоступен',
    131     wb_auth_failed: 'Проверьте токен Wildberries',
    132     readback_mismatch: 'Wildberries принял не все остатки',
    133     product_mapping_missing: 'Не найден товар для выгрузки',
    134     wb_token_read_only_401:
    135       'Ключ WB создан «только на чтение». Нужен ключ с правом публикации остатков.',
    136     unsafe_zero_blocked:
    137       'Остаток не опубликован: защита не даёт обнулить остаток в кабинете продавца. Проверьте наличие товара на складе и запустите синхронизацию заново.',
    138     unsafe_stock_unknown:
    139       'Остаток не опубликован: не удалось надёжно посчитать доступное количество для этого товара.',
    140   }
  ```
- `проверка-статуса` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:205`
  ```
      193       detail: row.lastErrorCode
    194         ? stockErrorText(row.lastErrorCode) ?? 'Синхронизация завершилась с ошибкой'
    195         : undefined,
    196     }
    197   }
    198   if (row.lastErrorCode) {
    199     return {
    200       label: 'ошибка публикации',
    201       color: 'error',
    202       detail: stockErrorText(row.lastErrorCode) ?? 'Синхронизация завершилась с ошибкой',
    203     }
    204   }
>   205   if (row.lastSyncStatus === 'confirmed') {
    206     return { label: 'публикация включена', color: 'success', detail: 'Wildberries подтвердил остаток' }
    207   }
    208   if (row.lastSyncStatus === 'pending') {
    209     return {
    210       label: 'публикация включена',
    211       color: 'warning',
    212       detail: 'Ждём подтверждения от Wildberries',
    213     }
    214   }
    215   if (row.lastSyncStatus === 'conflict') {
    216     return {
    217       label: 'публикация включена',
  ```
- `проверка-статуса` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:215`
  ```
      203     }
    204   }
    205   if (row.lastSyncStatus === 'confirmed') {
    206     return { label: 'публикация включена', color: 'success', detail: 'Wildberries подтвердил остаток' }
    207   }
    208   if (row.lastSyncStatus === 'pending') {
    209     return {
    210       label: 'публикация включена',
    211       color: 'warning',
    212       detail: 'Ждём подтверждения от Wildberries',
    213     }
    214   }
>   215   if (row.lastSyncStatus === 'conflict') {
    216     return {
    217       label: 'публикация включена',
    218       color: 'warning',
    219       detail: 'Wildberries вернул расхождение по остатку',
    220     }
    221   }
    222   if (row.lastSyncStatus === 'nothing_to_publish') {
    223     return {
    224       label: 'нечего публиковать',
    225       color: 'warning',
    226       detail:
    227         'Ни по одному товару не задано распределение остатка на этот склад — нажмите «Остатки» и укажите количество',
  ```
- `проверка-статуса` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:222`
  ```
      210       label: 'публикация включена',
    211       color: 'warning',
    212       detail: 'Ждём подтверждения от Wildberries',
    213     }
    214   }
    215   if (row.lastSyncStatus === 'conflict') {
    216     return {
    217       label: 'публикация включена',
    218       color: 'warning',
    219       detail: 'Wildberries вернул расхождение по остатку',
    220     }
    221   }
>   222   if (row.lastSyncStatus === 'nothing_to_publish') {
    223     return {
    224       label: 'нечего публиковать',
    225       color: 'warning',
    226       detail:
    227         'Ни по одному товару не задано распределение остатка на этот склад — нажмите «Остатки» и укажите количество',
    228     }
    229   }
    230   return {
    231     label: 'публикация включена',
    232     color: 'default',
    233     detail: 'Синхронизация ещё не запускалась',
    234   }
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:908`
  ```
      896                           Выделено: {row.allocatedPoolTotal} шт
    897                         </Typography>
    898                       ) : null}
    899                     </Stack>
    900                   </TableCell>
    901                   <TableCell>{formatDt(row.lastSyncAt)}</TableCell>
    902                   <TableCell align="right">
    903                     <Stack direction="row" spacing={0.5} sx={{ justifyContent: 'flex-end', alignItems: 'center' }}>
    904                       <Button
    905                         size="small"
    906                         variant="outlined"
    907                         onClick={() => void openPool(row)}
>   908                         disabled={!row.binding}
    909                         data-testid="fbs-stock-pool-btn"
    910                         sx={{ minWidth: 0, px: 1 }}
    911                       >
    912                         Остатки
    913                       </Button>
    914                       <IconButton
    915                         size="small"
    916                         aria-label="Ещё действия по складу"
    917                         onClick={(e: MouseEvent<HTMLElement>) =>
    918                           setRowMenuAnchor({ wbId: row.wbId, el: e.currentTarget })
    919                         }
    920                         data-testid="fbs-stock-row-menu-btn"
  ```
- `скрытие-в-null` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:925`
  ```
      913                       </Button>
    914                       <IconButton
    915                         size="small"
    916                         aria-label="Ещё действия по складу"
    917                         onClick={(e: MouseEvent<HTMLElement>) =>
    918                           setRowMenuAnchor({ wbId: row.wbId, el: e.currentTarget })
    919                         }
    920                         data-testid="fbs-stock-row-menu-btn"
    921                       >
    922                         <MoreVertIcon fontSize="small" />
    923                       </IconButton>
    924                       <Menu
>   925                         anchorEl={rowMenuAnchor?.wbId === row.wbId ? rowMenuAnchor.el : null}
    926                         open={rowMenuAnchor?.wbId === row.wbId}
    927                         onClose={() => setRowMenuAnchor(null)}
    928                       >
    929                         <MenuItem
    930                           onClick={() => {
    931                             setRowMenuAnchor(null)
    932                             void handleSync(row)
    933                           }}
    934                           disabled={busy || !row.isMapped || !row.stockSyncEnabled}
    935                           data-testid="fbs-stock-sync-one"
    936                         >
    937                           Выгрузить остатки сейчас
  ```
- `запрет-кнопки` · `frontend/src/screens/v2/FfFbsStockSyncScreen.tsx:944`
  ```
      932                             void handleSync(row)
    933                           }}
    934                           disabled={busy || !row.isMapped || !row.stockSyncEnabled}
    935                           data-testid="fbs-stock-sync-one"
    936                         >
    937                           Выгрузить остатки сейчас
    938                         </MenuItem>
    939                         <MenuItem
    940                           onClick={() => {
    941                             setRowMenuAnchor(null)
    942                             void openStatus(row)
    943                           }}
>   944                           disabled={!row.binding}
    945                           data-testid="fbs-stock-status-btn"
    946                         >
    947                           Статус синхронизации
    948                         </MenuItem>
    949                         <MenuItem
    950                           onClick={() => {
    951                             setRowMenuAnchor(null)
    952                             setPendingDisable(row)
    953                           }}
    954                           disabled={!row.binding?.is_active || savingWbId === row.wbId}
    955                           data-testid="fbs-stock-disable-binding"
    956                           sx={{ color: 'error.main' }}
  ```
- `проверка-статуса` · ОБЩИЙ с S-03 · `frontend/src/screens/v2/fbsApi.ts:1037`
  ```
     1025   body: { idempotency_key: string; confirmed_preflight_version?: string | null },
   1026 ): Promise<FbsWorkspace> {
   1027   const res = await fetch(apiUrl(`/operations/fbs-supplies/${id}/deliver`), {
   1028     method: 'POST',
   1029     headers: jsonHeaders(token, ah),
   1030     body: JSON.stringify(body),
   1031   })
   1032   return jsonOrThrow<FbsWorkspace>(res)
   1033 }
   1034 
   1035 /** Delivery is allowed only after packaging task completed (supply.status === packed). */
   1036 export function canDeliverFbsSupply(supply: FbsSupply): boolean {
>  1037   return supply.status === 'packed'
   1038 }
   1039 
   1040 // Лист подбора (Экран 3). GET /operations/fbs-supplies/{id}/picking-list → { items }.
   1041 export type FbsPickingItem = {
   1042   article: string
   1043   sku_code: string | null
   1044   size: string | null
   1045   product_name: string
   1046   quantity: number
   1047 }
   1048 
   1049 export async function getFbsPickingList(
  ```
- `скрытие-в-null` · ОБЩИЙ с S-03, S-05, S-07, S-08, S-09, S-10, S-12, S-14, S-16, S-18, S-19, S-26, S-27, S-28, S-29, S-31, S-32 · `frontend/src/utils/readApiErrorMessage.ts:87`
  ```
       75     const text = await res.text()
     76     if (!text) {
     77       return `Ошибка ${res.status}`
     78     }
     79     const data = JSON.parse(text) as { detail?: unknown }
     80     const d = data.detail
     81     if (typeof d === 'string') {
     82       return API_DETAIL_MESSAGES_RU[d] ?? d
     83     }
     84     if (Array.isArray(d)) {
     85       const parts = d
     86         .map((x: { msg?: string; loc?: unknown }) => {
>    87           const msg = typeof x?.msg === 'string' ? x.msg : null
     88           const loc = x?.loc
     89           const field =
     90             Array.isArray(loc) && loc.length > 0
     91               ? String(loc[loc.length - 1])
     92               : null
     93           if (msg && field) return `${field}: ${msg}`
     94           if (msg) return msg
     95           return JSON.stringify(x)
     96         })
     97         .filter((p): p is string => Boolean(p))
     98       // FastAPI/Pydantic часто возвращают много строк — ограничим длину.
     99       return parts.join('; ').slice(0, 240)
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
