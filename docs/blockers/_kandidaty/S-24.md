# Кандидаты в блокировки · S-24

Вычесано механически. Часть окажется техникой, а не блокировкой — это нормально,
скрипт сознательно тащит с запасом. Твоя работа — отделить доменные правила.

## Экран (6)

- `проверка-статуса` · `frontend/src/screens/v2/OutboundScreen.tsx:260`
  ```
      248                       })
    249                     }}
    250                   >
    251                     Печать накладной
    252                   </Button>
    253                 ) : null}
    254 
    255                 <ul className="list-plain" data-testid="outbound-detail-lines">
    256                   {outboundDetail.lines.map((ln) => (
    257                     <li key={ln.id} data-testid="outbound-detail-line" data-line-id={ln.id}>
    258                       {ln.product_name} ({ln.sku_code}) — отгружено {ln.shipped_qty} из {ln.quantity}
    259                       {ln.storage_location_code ? ` · ячейка: ${ln.storage_location_code}` : ' · ячейка не назначена'}
>   260                       {outboundDetail.status === 'draft' &&
    261                       isFulfillmentAdmin &&
    262                       !ln.storage_location_id &&
    263                       ln.shipped_qty < ln.quantity ? (
    264                         <form
    265                           data-testid="outbound-line-storage-form"
    266                           data-line-id={ln.id}
    267                           noValidate
    268                           onSubmit={onSaveOutboundLineStorage}
    269                         >
    270                           <label>
    271                             Ячейка отбора
    272                             <Select
  ```
- `проверка-статуса` · `frontend/src/screens/v2/OutboundScreen.tsx:297`
  ```
      285                               ))}
    286                             </Select>
    287                           </label>
    288                           <Button
    289                             type="submit"
    290                             data-testid="outbound-line-storage-save"
    291                             disabled={opsBusy || outboundRequestLocations.length === 0}
    292                           >
    293                             Сохранить ячейку
    294                           </Button>
    295                         </form>
    296                       ) : null}
>   297                       {outboundDetail.status === 'draft' && isFulfillmentAdmin ? (
    298                         <Button
    299                           type="button"
    300                           size="sm"
    301                           variant="danger"
    302                           data-testid="outbound-line-delete"
    303                           disabled={opsBusy}
    304                           onClick={() => onDeleteOutboundLine(ln.id)}
    305                         >
    306                           Удалить строку
    307                         </Button>
    308                       ) : null}
    309                     </li>
  ```
- `проверка-статуса` · `frontend/src/screens/v2/OutboundScreen.tsx:313`
  ```
      301                           variant="danger"
    302                           data-testid="outbound-line-delete"
    303                           disabled={opsBusy}
    304                           onClick={() => onDeleteOutboundLine(ln.id)}
    305                         >
    306                           Удалить строку
    307                         </Button>
    308                       ) : null}
    309                     </li>
    310                   ))}
    311                 </ul>
    312 
>   313                 {outboundDetail.status === 'draft' && canEditOutboundDraft ? (
    314                   <form data-testid="outbound-line-form" noValidate onSubmit={onAddOutboundLine}>
    315                     <label>
    316                       Товар
    317                       <Select
    318                         name="outbound_product_id"
    319                         data-testid="outbound-line-product"
    320                         required
    321                         defaultValue=""
    322                       >
    323                         <option value="" disabled>
    324                           Выберите SKU
    325                         </option>
  ```
- `проверка-статуса` · `frontend/src/screens/v2/OutboundScreen.tsx:370`
  ```
      358                       </label>
    359                     ) : null}
    360                     <Button
    361                       type="submit"
    362                       data-testid="outbound-line-submit"
    363                       disabled={opsBusy || products.length === 0}
    364                     >
    365                       {opsBusy ? '…' : 'Добавить строку'}
    366                     </Button>
    367                   </form>
    368                 ) : null}
    369 
>   370                 {outboundDetail.status === 'draft' &&
    371                 outboundDetail.lines.length > 0 &&
    372                 isFulfillmentAdmin ? (
    373                   <Button
    374                     type="button"
    375                     data-testid="outbound-submit-request"
    376                     disabled={opsBusy}
    377                     onClick={onSubmitOutboundRequest}
    378                   >
    379                     {opsBusy ? '…' : 'Отправить заявку'}
    380                   </Button>
    381                 ) : null}
    382 
  ```
- `проверка-статуса` · `frontend/src/screens/v2/OutboundScreen.tsx:383`
  ```
      371                 outboundDetail.lines.length > 0 &&
    372                 isFulfillmentAdmin ? (
    373                   <Button
    374                     type="button"
    375                     data-testid="outbound-submit-request"
    376                     disabled={opsBusy}
    377                     onClick={onSubmitOutboundRequest}
    378                   >
    379                     {opsBusy ? '…' : 'Отправить заявку'}
    380                   </Button>
    381                 ) : null}
    382 
>   383                 {outboundDetail.status === 'submitted' ? (
    384                   <div data-testid="outbound-ship-panel">
    385                     <p className="subtle">Строки в работе</p>
    386                     {isFulfillmentAdmin ? (
    387                       <>
    388                         {outboundDetail.lines.map((ln) =>
    389                           ln.shipped_qty < ln.quantity ? (
    390                             <Card key={ln.id} as="div" className="card" style={{ marginBottom: 10 }}>
    391                               <p className="subtle" style={{ marginTop: 0 }}>
    392                                 {ln.sku_code} — осталось отгрузить {ln.quantity - ln.shipped_qty} из{' '}
    393                                 {ln.quantity}
    394                               </p>
    395                               <form
  ```
- `скрытие-в-null` · ОБЩИЙ с S-01, S-21, S-22, S-23, S-25 · `frontend/src/screens/AppV2Screens.tsx:37`
  ```
       25   hint,
     26   children,
     27 }: {
     28   title: string
     29   hint?: string
     30   children?: ReactNode
     31 }) {
     32   return (
     33     <Card className="card">
     34       <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
     35         <div>
     36           <h3 style={{ margin: 0, fontSize: 16 }}>{title}</h3>
>    37           {hint ? <p className="subtle">{hint}</p> : null}
     38         </div>
     39         {children}
     40       </div>
     41     </Card>
     42   )
     43 }
     44 
  ```

## Сервер

Отказы бэкенда собраны в `night/blockers/SERVER.md` — ищи там те,
что относятся к твоему экрану, и сверяй с фронтовыми.
